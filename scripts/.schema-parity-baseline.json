{
  "_comment": "ACCEPTED dev↔prod divergences (scripts/check-schema-parity.mjs). Every entry is debt, not a decision — the target is an empty object. A NEW divergence, or an accepted one whose signature changes, fails the gate. Lower this as items are reconciled; re-init only after refreshing the snapshots.",
  "accepted": {
    "check:changed:rep_team_events.rep_team_events_practice_plan_sent_audience_check": "(practice_plan_sent_audience = ANY (ARRAY['named'::text, 'coaches'::text, 'staff'::text, 'chosen'::text])) :: (practice_plan_sent_audience = ANY (ARRAY['named'::text, 'coaches'::text, 'staff'::text]))",
    "column:only-dev:rep_team_events.practice_plan_sent_to": ""
  }
}
