{
  "_comment": "ACCEPTED dev↔prod divergences (scripts/check-schema-parity.mjs). Every entry is debt, not a decision — the target is an empty object. A NEW divergence, or an accepted one whose signature changes, fails the gate. Lower this as items are reconciled; re-init only after refreshing the snapshots.",
  "accepted": {
    "check:changed:rep_budget_lines.rep_budget_lines_line_kind_check": "(line_kind = ANY (ARRAY['cost'::text, 'funding'::text, 'sponsorship'::text, 'other_income'::text])) :: (line_kind = ANY (ARRAY['cost'::text, 'funding'::text, 'sponsorship'::text]))",
    "check:only-dev:rep_budget_lines.rep_budget_lines_split_mode_check": "",
    "column:only-dev:budget_categories.team_id": "",
    "column:only-dev:rep_budget_lines.split_mode": "",
    "constraint:only-dev:budget_categories.budget_categories_team_id_fkey": "",
    "index:only-dev:budget_categories_team_idx": ""
  }
}
