# Allow specific package scripts to run without manual approval
# This fixes the "pnpm approve-builds" error in Amplify
# core-js added with the Next 16.3.0 bump (new transitive dep; postinstall is a banner only)
only-built-dependencies[]=sharp
only-built-dependencies[]=unrs-resolver
only-built-dependencies[]=core-js
