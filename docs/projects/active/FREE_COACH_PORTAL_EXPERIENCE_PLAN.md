# Free Coach Portal Experience — Fix & Delight Plan

**Status:** **Phase B3 ✅ BUILT + OWNER BROWSER-QA PASSED 2026-07-27 (uncommitted — commit OK pending)** — mockups + decisions ◆K–◆P approved same day (artifact `0a4a74e5-d07b-471c-a62b-325c8b751a23`); B3.3 DROPPED by owner call (pressure ladder holds); build record in the B3 section below. Tranche A + B1 all COMMITTED on dev (A1 `23492301`, A2 `6e76c6a5`, A3 `d8f7850a`, A4 `fec03c9a`, B1 `31f49194`) — **none on prod yet**. Remaining: **B2 coach alerts** (needs a design pass + has a live prod push dependency). Phase A1 ✅ COMMITTED 2026-07-25 (23492301). **Phase A2 ✅ COMMITTED 2026-07-26 on dev (6e76c6a5)** — mockups approved 2026-07-25 (= visual spec), built + 4-lens adversarial review (7 confirmed findings fixed in-pass), owner browser-QA'd (one fixture fix: tournaments org column is `org_id`, schema-verified). NOT on prod. **Phase A3 ✅ COMMITTED 2026-07-27 on dev (d8f7850a, 36 files, `git show --stat` verified clean)** — mockups approved 2026-07-26 = visual spec (incl. A3.2 + A3.3), built + QA addendum, owner browser re-QA verdict "all clear" 2026-07-27, `/docs` sweep landed in the same commit. NOT on prod. Next: A4 flow polish + B1.
**A1 addendum (owner QA feedback 2026-07-25):** shell header cleanup — removed the decorative auto-color team dot (`teamColor(name)` swatch) from the mobile topbar + desktop rail (`CoachPortalShell.tsx/.module.css`, unused classes + import removed), and the lifecycle chip no longer renders in state `complete` (persistent "COMPLETE" after an event read as stale noise; live/game-day/upcoming/future-month labels still show — gate at both render sites in `CoachPortalShell.tsx`).
**A1 addendum 2 (owner QA 2026-07-25):** lifecycle-chip warm-theme contrast — `.coachLifecycleChipComplete`/`Future` in `app/coaches/coaches-portal.module.css` used `--white-30`/`--white-40` ink, which warm remaps to line/dim tokens (`--white-30` → `--home-line-strong`, a border color) → "COMPLETE" was illegible on warm cards. Switched both to `--text-tertiary` (white-40 on dark / `--home-dim` ink on warm). Shell's `.teamChip` (`--white-45` → `--home-dim`) already legible, untouched.
**A1 addendum 3 (owner QA 2026-07-25):** olive upgrade-CTA fix — the three warm "paper doorway" upsell cards (`ScopeCeilingInterest`, `ScopeShelf`, `CoachExploreCatalog.premiumBlock`) compose consumer `warmVars`, whose `--home-lime: var(--logic-lime)` capture resolves INSIDE the coach warm gate (which remaps `--logic-lime`→`--home-olive`, globals.css:523) → CTA fills rendered olive (E3 ban). Fixed by re-pinning `--home-lime: #D9F99D` on each composing class (value-identical to root lime → dark output unchanged) + CTA ink normalized to `--home-lime-ink`. Other coach lime fills read the gate literal directly and were already correct.
**A1 review follow-ups parked:** orphaned `/api/auth/me` route (delete in cleanup tranche); free-record back link (`/coaches/tournaments`) vs rail Tournaments tab (`/coaches/team/{id}/tournaments`) are two same-labeled links to sibling destinations — resolves in A2 (shell rework retargets the tab) / A3 list consolidation; desktop rail flashes "Get started" during context load on record pages (pre-existing, dies with the A2 shell rework); reset-page destination fetches have no timeout (pre-existing shape, advisory).
**Source:** 14-agent workflow audit (2026-07-25), all key claims adversarially verified with file:line evidence. PM brief: `FREE_COACH_PORTAL_EXPERIENCE_PM_BRIEF.md`.
**Persona:** first-time coach who registers a team for one tournament via the public site and lands in the free (account-level, org-less) Coaches Portal at `app/coaches/**` (basic_coach_* model).

**PROCESS GATE (owner directive 2026-07-25, binding for every phase of this project):** any phase that changes what a screen looks like or how it's laid out (A2 shell integration, A3 regroup, B1 quick-win surfaces, B3 bridge placements — anything beyond pure copy/logic) MUST present **mockups during its planning step and get owner approval BEFORE build starts**. Approved mockups ARE the visual spec, including existing elements (per `memory/feedback_build_to_approved_mockups.md` — label NEW vs RESTYLED vs UNCHANGED). No design-changing phase proceeds to code on a written description alone.

**Verified baseline (what already works — do not regress):**
- Signup→landing is 1 click post-review: `Submit Registration` creates the auth account (email pre-confirmed, `app/api/auth/coach-signup/route.ts:50`), signs in client-side, inserts the registration (`app/api/register/route.ts:357-377`), and redirects straight to `/coaches/tournaments/{registrationId}?welcome=1` (`components/public/RegisterContent.tsx:473-476`). No hub/team-picker/verify-email detour. Multi-team branching correctly suppressed for first-timers.
- Post-login destination resolution for a single-basic-team coach correctly fast-paths to their team (`lib/user-contexts.ts:296-310`, `lib/auth-destination.ts:77-88`).

---

## TRANCHE A — FIX (correctness + IA)

### Phase A1 — Auth dead-end + navigation defects (highest priority, small diffs)

**A1.1 Password-reset lands coaches on a 404 (HIGH).**
`app/(consumer)/auth/reset-password/page.tsx:74-92` hardcodes `dest='/admin'`, overridden only via `/api/platform-admin/me` or `/api/auth/me`. `/api/auth/me` requires an `organization_members` row (`lib/api-auth.ts:169-179`) which org-less basic coaches never have → 401 → coach is sent to `/admin`, which has **no route** (zero `app/admin/**` matches) → authenticated user passes proxy (`proxy.ts:117-131` only redirects `!user`) → `app/not-found.tsx` "ROUTE_NOT_FOUND" screen. Fires for *every* free coach who resets a password — the persona most likely to reset, since their password was set inline during registration.
**Fix:** reuse the shared destination resolver (`/api/auth/destination` → `getAuthDestinationDetail()`, same as login's `resolveLoginDestination`, `app/(consumer)/auth/login/page.tsx:24-36`). Never fall back to `/admin`.
**Accept:** coach with only a basic team resets password → lands on `/coaches/team/{basicTeamId}` (or honored safe `next`).

**A1.2 FlipPill drops bottom-left on mobile (shared component — fixes free AND paid portals).**
Convention (verified): FlipPill pins top-right of the title row at every width; mobile degrades to icon-only, never re-stacks (`components/shared/FlipPill.module.css:148-166`; `components/admin/AdminEventHeader.tsx:143-146` + `.module.css:51-58` with no mobile column override; `components/Navbar.module.css:158-172` absolute top-right).
Root cause: `components/coaches/CoachTournamentRecord.module.css:199-208` `@media (max-width:600px){ .header{flex-direction:column} }` with no compensating alignment → FlipPill (2nd child, `CoachTournamentRecord.tsx:460-475`) falls under the title flush-left.
**Fix (pick one):** (a) keep `.header` a row at all widths and let title wrap/truncate (matches AdminEventHeader spirit — preferred), or (b) add `align-items:flex-end` in the mobile block.
**Accept:** ≤600px, pill sits top-right beside/level with the title on both free and paid tournament-record pages.

**A1.3 Missing "← Tournaments" back link on the free record page.**
`CoachTournamentRecord.tsx:445-449` already renders a back link when `backHref` is passed; paid call site passes it (`app/[orgSlug]/coaches/teams/[teamId]/tournaments/[registrationId]/page.tsx:43`), free call site doesn't (`app/coaches/tournaments/[teamId]/page.tsx:48-55`).
**Fix:** pass `backHref={COACHES_TOURNAMENTS_PATH}` (`lib/coaches-portal-routes.ts:4`).

**A1.4 Wrong active tab: Overview highlighted on tournament-record pages.**
`components/coaches/CoachPortalShell.tsx` `sectionActive()` (lines 194-206) deliberately claims `/coaches/tournaments/{regId}` for the Overview tab (regex vs `team.registrationIds`, lines 196-202), while the Tournaments tab's href is `/coaches/team/{basicTeamId}/tournaments` (`sectionHref`, 191-193) whose `startsWith` check (line 205) can never match the record URL. Same logic drives desktop rail (218-234) and mobile bottom nav (355-379).
**Product call (recommended, confirm at build):** Tournaments tab should be active on a tournament-record page. Move the registrationIds match from the Overview branch to the Tournaments branch (ensure the two branches never both claim a route).
**A1.4b:** retitle the `/coaches/tournaments` page h1 from "Coaches Portal" to "Tournaments" (`app/coaches/tournaments/page.tsx` header) — it's the Tournaments tab's destination.

**A1.5 Auth-family branding + banned tagline.**
"Coaches Portal" appears nowhere in `app/(consumer)/auth/**`. Banned phrase "FieldLogicHQ — Tournament Management Platform" (violates brand canon, `memory/project_brand_name.md`) at `forgot-password/page.tsx:35`, `reset-password/page.tsx:106`, `reset-confirm/page.tsx:58`.
**Fix (minimum):** replace the tagline with canon copy on all three. **Stretch:** contextual "Sign back in to your Coaches Portal" framing when `next` targets `/coaches/*` (the common deep-link path: `app/coaches/team/[basicTeamId]/page.tsx:83-84` redirects signed-out visits straight to generic `/auth/login`, bypassing the friendlier `/coaches/join`). Reset-flow copy should acknowledge "your account was created when you registered your team" — coaches never had a deliberate set-a-password moment.

### Phase A2 — Consumer shell integration (NEW; ratified 2026-07-25 — the two-family ruling)

**Ruling:** free portal joins the CONSUMER family (see `docs/agents/strategy/BUSINESS_DECISIONS.md` 2026-07-25 + `memory/design_decisions.md` 2026-07-25). The portal becomes a lens over the tournament inside the one app; its own nav vocabulary retires.

- **A2.1 Bottom nav → global consumer nav** (Home · Scores · Chat · Account — the same bar public tournament pages keep per the 2026-07-20 nav-merge decision). CoachPortalShell's own bottom nav + "More" sheet retire. Account utilities (sign out / feedback / help / all-workspaces) relocate to the Account tab per consumer convention.
- **A2.2 Chat unification.** Verified pre-ratification: the consumer Chat tab already lists basic-coach team rooms — `lib/chat-service.ts` `listRoomsForUser` backs BOTH `/api/chat/rooms` (coach portal) and `/api/consumer/chat/inbox` (consumer tab); membership resolves via `lib/chat-resolvers.ts:245-253` (basic_coach_team_users → registrations → tournaments). The portal's Chat section retires; add a **"Team chat" doorway** (card/link) on team Overview so chat stays discoverable in-context.
- **A2.3 Persistent team-space header** (shell-level, modeled on the public event header + AdminEventHeader conventions): team identity always; tournament identity + lifecycle pill when in a tournament context; **FlipPill top-right at shell level** — retires per-page Flip placement (closes the parked structural follow-up; the A1.2 CSS fix becomes belt-and-suspenders).
- **A2.4 Top-line scrolling section tabs** (generalize the `TournamentTopTabs` pattern: frosted pill row, edge-fade, active-tab auto-center): Overview · Tournaments · [activated Tier-2 sections] · **Explore pinned at the end as the "add more" affordance**. Reuse A1's `sectionActive` ownership logic for tab state. No slot budget → progressive disclosure no longer overflows.
- **A2.5 Desktop:** keep a left rail (TournamentSideRail-style) carrying the same section list + vocabulary.
- **A2.6 Multi-team:** switcher moves into the header; Home's multi-team workspace card still lands on hubs (`lib/user-contexts.ts` buildTournamentRegistrationContext multi-team branches) — acceptable v1, direct-linking polish later.
- **Build prerequisites:** `/design` pass on header composition, tab-row behavior, and the chat doorway (per the strategy handoff); confirm SW cache denylist needs no change (`/coaches` top-level already authed-denylisted); typecheck + full restart + `/review` (shared shell + nav seams).

**A2 MOCKUPS APPROVED 2026-07-25 (gate satisfied — mockups ARE the visual spec):** artifact `claude.ai/code/artifact/ae44a341-2417-44f6-b743-4501a59c63a0` (round 4). 7 mobile screens + desktop rail frame, warm+dark, every element labeled NEW/RESTYLED/UNCHANGED. Built from a 6-agent code-spec extraction (consumer nav anatomy, TournamentTopTabs recipe, event-header grammar, current-shell inventory, warm token gate, nav seams). **Owner-ratified decisions bound into the spec:**
- **Flip placement:** shell header top-right at every width (icon-only ≤900px), coexisting with the header team switcher (team NAME = switcher tap target, pill keeps its own right lane; AdminEventHeader flex convention). Back-link-row alternative mocked and REJECTED (scrolls away; breaks corner symmetry with admin/public; reopens per-page drift). Scope-clarity lever = label ("⇄ Live site" during live event, "Public site" otherwise), never position. Logged in design_decisions.md 2026-07-25.
- **A — Bottom bar in team space: NO tab lit** (header carries place identity; lighting Home would lie).
- **B — Team-only header eyebrow: "COACHES PORTAL"** (org-less persona has no org name to show).
- **C — Team-chat doorway at the TOP of Overview** (above the stat strip — game-weekend priority).
- **D — Tournaments tab targets the TEAM-SCOPED list** (`/coaches/team/{id}/tournaments`) — also resolves the A1 review follow-up (two same-labeled links to sibling destinations).
- **E — Team-only header meta keeps the next-event line** (lifecycle chip + "Purple Classic · Aug 14–16" style).
- **Other spec facts from the mockups:** persistent header absorbs BOTH existing page headers (Overview/Tier-2 identity band via TeamSectionShell + record page's own title block) — text-only chrome (no logo/color dot, per A1 addendum); lifecycle chip adopts the public pill form, states/complete-hidden rule unchanged; section tab row = TournamentTopTabs recipe (frosted, 44px mono pills, 2px underline active, edge-fade, auto-center), Chat removed from sections, Explore pinned last; bottom bar standardizes at the app-wide 72px (coach 64px retires); loading = header/tab-row shimmer while context resolves, global bar renders instantly (kills skeleton-tabs flash + rail "Get started" flicker); More-sheet utilities (sign out / feedback / help / all-workspaces / email) relocate to Account tab; desktop = consumer top bar + 248px rail in the public-rail idiom (identity block w/ chip + next event + switcher, soft-fill active w/ left accent bar, footer keeps only "Live on FieldLogicHQ"); warm default + dark both ship.

**A2 BUILD RECORD (2026-07-25, uncommitted on dev — owner QA pending):**
- **Shell (`CoachPortalShell.tsx/.module.css`, rewritten):** global `ConsumerNav` mounted with a new `variant="coach"` (warm + pref-gated like the four tabs; wordmark topbar desktop-only via `.topbarCoach`; the coach mount supplies `--consumer-top-h`/`--consumer-bottom-h` itself — review finding, the bars' heights collapse without them). Persistent sticky team/event header (eyebrow "COACHES PORTAL" or "{tournament} · {org}"; title = team name, chevron switcher popover when multi-team; meta = lifecycle chip [public pill form, complete hidden, live/game-day = true-lime fill via `var(--home-lime, var(--logic-lime))` — no literals, guardrail-clean] + dates/next-event line; FlipPill top-right in event context only). Section tab row ≤900px (TournamentTopTabs recipe: 44px mono pills, edge-fade mask, auto-center effect; Overview · Tournaments · activated Tier-2 · Explore last; zero-team → single "Get started" pill — review finding). Desktop ≥901px (breakpoint moved from 1024): rail redressed in the public-rail idiom (identity block w/ chip + viewed-event name — review finding: event context names the VIEWED tournament, never nextEvent; accent-soft active + left bar; footer = "Live on FieldLogicHQ" only), header band hidden on team-only desktop pages (`.mheadTeamOnly`). More sheet / old 5-tab bar / 56px topbar / brand lockup / rail utilities all retired. Switcher = light popover w/ Escape + outside-click (no scrim — review finding: fixed bars stack above any scrim the header context can host).
- **Context API (`lib/basic-coach-teams.ts`):** `CoachTeamContext` + `registrations[]` (per-registration tournament identity: name/slug/org/dates/`tournamentIsPublic` [active|completed — same rule as the record page] /per-reg chip) + `nextEvent` (the lifecycle-driving registration; null when complete). Additive — `?context=1` shell fetch is the only consumer.
- **Chat (A2.2):** Chat removed from TIER1; `/coaches/team/{id}/chat` → `redirect('/chat')` (rooms API/tab enforce own auth — security-lens verified); `chat.module.css` deleted (first delete was a PowerShell bracket-glob no-op — use `-LiteralPath` on `[bracketed]` paths). NEW `CoachTeamChatDoorway` at top of Overview: fetches `/api/chat/rooms`, scopes to the team via tournamentIds AND divisionIds vs `room.settings.divisionIds` (review finding: `ref_sub_id` is an OPAQUE uuid, not a division id; without division scoping a two-teams-same-tournament coach saw the sibling team's room), leads with most-recent room, summed unread pill, links to `/chat`.
- **Pages:** identity band deleted (`team.module.css`); Overview + `TeamSectionShell` render slim mono-eyebrow h1 heads with help/meta cluster (`.sectionHeadEnd`); `CoachTournamentRecord` gained `hideHeader` (free call site passes it; Premium call site byte-unchanged, keeps in-page header + Flip). `formatEventRange` shows both years on cross-year ranges (review finding).
- **Account tab:** coach-gated rows "Coaches Portal help" (→ /coaches/help) + "Send feedback" (`AccountFeedbackRow`, FeedbackWidget direct). ConsumerNav's "Coaches Portal" util pill hidden on the coach variant (self-referential — review finding).
- **Review:** high-risk tier, 4 lenses (correctness/security/regression/runtime-CSS), 7 confirmed findings fixed in-pass, 0 unresolved Critical/High. **Known follow-ups:** (1) ✅ `/docs` sync DONE 2026-07-26 (coaches guide: getting-around paragraph, Explore = last tab, chat recipe + join-FAQ rewritten for the global Chat tab + Overview doorway, new "Where did the portal's menu go?" FAQ); (2) feedback request-id is best-effort-null on a direct `/account` load (provider mounts in shells only — pre-existing pattern, advisory); (3) desktop record pages show both the rail select and the header chevron switcher (accepted); (4) optional "⇄ Live site" label variant during live events (decided lever, not built); (5) record back link still targets the account-wide hub — full list consolidation is A3.3. **(6) ✅ `/marketing` handoff DONE 2026-07-26:** companion-vs-HQ framing live on /for-coaches, the /pricing coach card + coach FAQs, Explore premium block, and the afterglow upgrade panel (canon: PRICING_PAGE_COPY.md 2026-07-26 amendment); in the same pass, /pricing's stale static "coming soon" coach copy (segment card, hero sub, 3 FAQs) made checkout-gate-aware — it said the Coaches Portal wasn't buyable while Founding Season checkout was open (drift vs PLAN_PRICING_FACTS.md, now reconciled).

### Phase A3 — Tournament-record page regroup (the "stacked unrelated items" fix)

Current state (verified): `CoachTournamentRecord.tsx` renders up to **13** independently-gated `<section>` blocks in one scroll (lines 443-644), no tabs/anchors/accordion/mini-TOC anywhere in the render tree. Duplication: fee state ×3 (`TeamHQ.tsx:421-445` fee strip, `:340-353` checklist row, `TournamentStatusBlock.tsx:80-100`), organizer contact ×3 (`TeamHQ.tsx:438-442`, `:514-518`, `TournamentStatusBlock.tsx:109-124`), schedule info ×3 (hero countdown/today-card `TeamHQ.tsx:447-480`, Schedule section, next-steps line). Adjacent near-duplicates: "Your entry" (588-607) vs "Registration Details" (609-623). Title register drifts ("Payment"/"Your status" dynamic swap :417-418, "That's a wrap", "Your roster" vs "Registration Details").

**Design decision:** do NOT copy the public tournament's multi-page tabs (`lib/tournament-page-tabs.ts` + 4 chrome renderers). A registration record is one thing; splitting adds clicks. Adopt the public site's **within-page grammar** instead: eyebrow + heading section headers (public reference: `.section-header` primitive, `app/globals.css:1161-1188`; adopt the grammar in coach-portal styling, not necessarily the global class).

**Target structure — 4 zones:**
1. **Status & Payment** — one authoritative block: status badge/headline, fee amount + due date + how-to-pay + single contact line. Kill the triplication; hero keeps status + countdown only.
2. **Schedule** — one block handling live bridge / empty / published states (merge the two current Schedule sections, `CoachTournamentRecord.tsx:507-522` and `565-579`).
3. **Your Team** — roster submit + head-coach editor; merge "Your entry" + "Registration Details" into one card (editable fields + inert facts together).
4. **From the Organizer** — announcements + welcome-banner resource links (Tournament Home / Schedule / Rules).

Plus: mobile sticky anchor pills (4 anchors, one per zone — decide at build how these sit under A2's section tab row: in-page anchors are one level below the section tabs, so keep them subtle or fold into a compact sticky sub-nav); consistent title register; keep phase-adaptivity (pending/accepted/game-day/result) *within* zones rather than adding/removing whole sections where feasible.

**A3.2 Team Overview page rhythm** (`app/coaches/team/[basicTeamId]/page.tsx`): only "Tournament history" (246-287) has a visible h2; the stat strip (180-191, aria-label only), CoachLiveEventCard (196-200), and CoachOverviewInvite (205-212) are headless. Give every block the same header treatment; this page also gains the A2.2 "Team chat" doorway.
**A3.3 (optional/backlog):** the same registration list renders on 3 routes with 3 card styles (Overview history block; `app/coaches/team/[basicTeamId]/tournaments/page.tsx:37-97`; `app/coaches/tournaments/page.tsx:173-219`). Consolidate card component; consider whether the team-scoped list page earns its keep for single-team coaches once A2's Tournaments tab targets are settled.

**A3 MOCKUPS APPROVED 2026-07-26 (gate satisfied — mockups ARE the visual spec):** artifact `claude.ai/code/artifact/18507c26-7755-4e42-92e2-5476c620f99a` (round 1 + the two-tournament chrome scenario). 4 mobile phases + desktop + Premium shared-surface frame + A3.2 Overview + A3.3 consolidation, warm+dark, NEW/RESTYLED/MOVED/UNCHANGED labels. Decisions ◆A–G accepted as recommended: (A) zone order fixed in every phase (Z1 compresses when settled); (B) quick-jump = compact 34px sticky sub-nav under the A2 tab row, colour-only active state, mobile+free-shell only; (C) share button joins the hero result card, afterglow upsell strip sits under the hero; (D) hero checklist + both "What's next" strips fully dissolved (Z1 carries a roster pointer row); (E) game-day hero says "Game day!" + one next-game line; (F) two header tiers — record zones wear the public eyebrow+heading grammar (Barlow pair, accent eyebrow), Overview keeps the slim mono register on every block; (G) A3.3 in scope — one registration card + team-scoped back link.

**A3 BUILD RECORD (2026-07-26, uncommitted on dev — owner QA pending):**
- **Record page (`CoachTournamentRecord.tsx/.module.css`):** four zones with ids `#status/#schedule/#team/#organizer` (`ZoneHead` eyebrow+heading pairs); Zone 1 = rewritten `TournamentStatusBlock` (all phases now: Status row always; pending/waitlist Decision row + fee preview [moved from hero] + promise note; accepted Fee/Check-in/Roster rows + how-to-pay; roster pointer row hands to Zone 3; EXACTLY one contact statement per state — rejected keeps its reach-out copy); Zone 2 = single Schedule zone (live bridge / accepted-empty / pending-quiet states merged); Zone 3 = roster submit + merged entry card (native `<details>` head-coach editor [`HeadCoachEditor` gains `embedded`] + facts grid; Dates row dropped — hero + shell header already state dates); Zone 4 = resource pills (moved from welcome banner, permanent, never for rejected) + announcements. Result: share → hero result card via new TeamHQ `shareSlot`; afterglow strip (thanks + `ScopeCeilingInterest` + organizer line) under hero, free tier only. Rejected renders Zone 1 + Zone 4 only, no jump nav. Dead `.cta*`/`.detailGrid`/`.sectionTitle` styles removed.
- **Hero (`TeamHQ.tsx/.module.css`):** tournament variant slimmed to identity + status badge + countdown (+ result card); checklist HUD, fee preview/strip, contact lines, today-card all removed (props dropped); "Game day!" headline + one next-game line (count-aware); standalone stat-strip variant untouched except aria-label "Team HQ"→"At a glance".
- **Quick-jump (`CoachRecordJumpNav.tsx`, NEW):** 34px sticky row under the shell header (`top: var(--coach-mhead-h)`), mono links, colour-only active via rAF scrollspy (recolour only — never resizes sticky chrome per the design-log guardrail), smooth scrollIntoView w/ reduced-motion fallback; ≥901px hidden; renders only when `hideHeader && !rejected`. Shell (`CoachPortalShell.tsx`) publishes `--coach-mhead-h` via ResizeObserver in a `useLayoutEffect` (pre-paint — kills the scroll-restore flash; house pattern per OrgNavSync/ChatPanel). Desktop `.zone` scroll-margin adds `--coach-top-strip` (media-split — the var exists at all widths but offsets only ≥901px).
- **Welcome banner:** slimmed to the celebration line (resources section + styles removed).
- **A3.2 (`app/coaches/team/[basicTeamId]/page.tsx` + `CoachTeamChatDoorway`):** slim mono h2s on every persistent block — "Team chat" (INSIDE the doorway — it renders null until a room exists, a page-level h2 would orphan), "At a glance", "Your event", "Tournament history" (existing); invite stays headerless by design.
- **A3.3 (`CoachRegistrationCard.tsx/.module.css`, NEW):** hub-card treatment as the single design (lifecycle chip w/ badge fallback, dot-joined meta + card-formatted date range [date-only-safe parsing], lime hover, ⇄ Fan view line) across Overview history / team-scoped list / hub; monogram dropped (hashed the TOURNAMENT name via `teamColor`); Overview passes `fanView=null` (P3 rev-4 — the live-event card owns that door). Free record back link → team-scoped list via `findLinkedBasicTeamForRegistration` (ownership-checked; hub fallback for registration-only coaches); `lib/email.ts` hub-path literals → `COACHES_TOURNAMENTS_PATH` constant. `CoachNextSteps.tsx/.module.css` DELETED (orphaned). Roster "organizer added players" plural copy fixed ("aren't").
- **Review (4-lens adversarial funnel, high-risk tier):** security/tier-seam + blast-radius lenses clean (money redaction double-gated, back link IDOR-safe, no orphaned refs); 5 confirmed findings fixed in-pass — waitlist Decision-row copy (no false "few days" promise), rejected excluded from resource pills, Dates row dedup, `useLayoutEffect` pre-paint measure, desktop scroll-margin top-strip. Advisories logged: `<details open>` relies on phase stability across `router.refresh()` (safe today — head-coach save never changes phase inputs); owed+no-instructions+no-contact renders no mailto (pre-existing); `jumpItems` identity churn per refresh (harmless).
- **Verify:** typecheck ✓, focused lint ✓, verify:changed full suite ✓ (all token ratchets zero). Owner QA + `/docs` zone-label sweep pending; NOT committed.

**A3 addendum (owner QA feedback 2026-07-27, built same day):**
1. **Auto-highlight replaces the "Highlight my team" button** (`CoachLiveSchedule`): the coach's team is pinned on the public schedule/scorebug automatically — seeded ONCE per device+tournament (`fl-coach-autopin:*` localStorage flag, reading the follow store directly since the hook hasn't hydrated in the first effect pass); never overrides an existing highlight and never re-pins after a deliberate public-site un-star. Button + hint + their CSS removed.
2. **Overview page head is one row at every width** (`.headRow` double-class override of the shared ≤700px column stack) — the "?" sits beside the h1, not on its own row.
3. **"Your tournaments" leads Overview** (retitled from "Tournament history"), event card follows; the A2 chat doorway is REMOVED (owner reversal of the A2.2 doorway call — the global Chat tab is the one chat door; `CoachTeamChatDoorway.tsx/.module.css` deleted).
4. **Team-tools divider** (`.toolsDivider`, hidden for registration-less teams): "Your team — beyond this tournament" eyebrow + free-floor-canon note ("free tools… carry over to every event" + Explore link) between the tournament group and At a glance — team tools must never read as tournament homework.
5. **Chat round trip:** the coach shell's Chat tab (both bars, via new `ConsumerNav` `chatBackPath` prop) deep-links `/chat?back=<current portal path>`; `/chat` renders a slim "← Back to your Coaches Portal" return bar when the param is a sanitized internal `/coaches` path (safeNextPath + prefix check — fans/direct visits never see it; the return lands on the exact page the coach left).
6. Verify re-run green (typecheck/lint/verify:changed); dev server restarted clean.

**A3 `/docs` sweep ✅ DONE 2026-07-27 (in commit d8f7850a).** `lib/help-content/coaches.tsx` — the coach guide serves both the customer help page and the platform-admin support mirror, so one edit covers both. Changes: the record-page walkthrough rewritten around the four zones (order, what each holds, the fee-stated-once promise, the free-portal mobile jump row, the rejected short-record case); `faq-pay-entry-fee` → "Status & Payment" (the old "Payment" section name is gone, but **"How to pay" is still a real sub-panel label** — verified in `TournamentStatusBlock`, don't remove that term); `faq-coach-own-team-alerts` rewritten for automatic highlighting (button gone; kept "highlight my team" as a search keyword so coaches who remember it land on the answer); the A2 "Team chat doorway" copy removed from the chat section + `faq-tournament-chat-join` and replaced by the Chat-tab-is-the-one-door framing + the "← Back to your Coaches Portal" round trip (documented in 3 places: getting-around tour, chat section, `faq-coach-nav-moved`); the `overview` section now walks the real page order incl. the team-tools divider (this section is what the Overview's "?" drawer opens, so it's the first thing a confused coach reads); Premium tournament copy points at the same four zones + states the tier rule out loud. ~40 new search terms (search matches metadata, never rendered prose). No hub/anchor changes needed — no sections added, renamed, or removed, so every deep link still resolves.

**A3 accuracy notes worth keeping (verified in code during the sweep, for whoever documents this next):** Zone 1's title is dynamic — "Status & Payment" normally, "Status" when `moneyRedacted` (a money='off' Premium assistant coach). Check-in state appears from `tournament.start_date` onward, NOT at acceptance. Pending/waitlist see a fee *preview*, not the Fee row (the Fee row needs `status`, i.e. accepted). Auto-highlight only seeds while the event is live (`if (!live) return`). The quick-jump row renders only when `hideHeader && !isRejected` — free shell, non-rejected.

### Phase A4 — Registration-flow polish

**A4.1 Confirmation email deep link.** `lib/email.ts:167-169` (`registrationConfirmationHtml`) hand-builds `next=/coaches/tournaments` (list) despite having `p.registrationId`; sibling emails use `coachPortalUrl()` (`lib/email.ts:142`) → `/coaches/tournaments/{registrationId}`. **Fix:** use the helper. One line.
**A4.2 CTA signals account creation.** Desktop hero button says "Register" (`components/public/TournamentHomeContent.tsx:1015-1019`), mobile "Register a Team" (:506-508); neither hints an account is created. First mention of "Coaches Portal" is the mid-form password field (`RegisterContent.tsx:1040-1056`). **Fix:** one-line subtext near the CTA or atop the form: registering also sets up their free Coaches Portal.
**A4.3 Sign-in-failure branch misleads.** `RegisterContent.tsx:417-424`: if signup succeeds but immediate `signIn()` fails, copy says "sign in… to finish" — but `/api/register` never ran; the coach signs in to an empty portal. **Fix:** copy must state the registration was NOT submitted; preserve form state and offer retry (re-submit after manual sign-in if feasible).
**A4.4 Existing-account collision round trip.** `RegisterContent.tsx:393-411`: 409 → signs in, resets to `form` step, demands Review→Submit again. **Fix:** after successful sign-in, proceed directly to submission (or at minimum return to `review`).
**A4.5 (low):** surface the "just here to follow? no account needed" off-ramp on the tournament home near the CTA, not only inside the form (`RegisterContent.tsx:846-862`).

**A4 BUILD RECORD (2026-07-27):**
- **A4.1 ✅** `registrationConfirmationHtml` stopped hand-building its join URL and now calls `coachPortalUrl()` like its 8 siblings, so the one email every new coach opens finally deep-links to `/coaches/tournaments/{registrationId}` instead of the list. `coachPortalUrl` gained an optional `registered?: boolean` — the old hand-built string carried `registered=1` and `/coaches/join` branches on it (`fromReg`), so a naive swap would have silently downgraded the post-registration welcome. Review verified all 13 other call sites are byte-identical and that the deeper `next` survives `normalizeCoachPortalNext` → `/auth/login` → `safeNextPath`.
- **A4.2 + A4.5 ✅** Both register CTAs on the tournament home (desktop hero + mobile pre-CTA) gained one supporting line — "Registering/Joining the waitlist also sets up your free Coaches Portal…" plus a quieter follow-only off-ramp. Waitlist-aware wording. New `.heroCtaNote`/`.preCtaNote` (+ `*Quiet` spans); desktop needed a fragment since `.heroCta` is a flex row (parent `.heroContent` is plain block — verified, no layout regression).
- **A4.3 ✅** The signup-ok-but-signIn-failed message now states the registration was **not** submitted and the organizer received nothing (control flow verified: `/api/register` is strictly downstream and the branch returns before it). The existing "Try Again" button + preserved answers already worked, so this was copy — **except** for file uploads: the form step unmounts on the way to the error card and a remounted `<input type="file">` always renders empty, so a `required` file field blocks the retry via native validation. The message now only promises "answers still filled in" and appends a re-attach clause when a file field is required/filled.
- **A4.4 ✅ (owner call 2026-07-27 — plan's original "auto-submit" recommendation REJECTED).** The existing-account path already fast-paths a coach with no teams straight to submission; only a coach **with** teams is returned to the form, and that's a genuine choice — auto-submitting would silently attach the registration to a guessed team. Kept the choice, fixed the framing: the notice now says the registration hasn't been submitted yet, and the page scrolls to a new `id="coach-team-picker"` instead of the top of the form. Review verified no signup→409 loop is possible (`loadCoachTeams` sets `signedInCoachEmail`, never reset), the picker always renders when the branch fires, the notice self-clears via `handleReview`, and the double-submit guard is untouched.
- **Sibling fix found by review (in scope because A4.3's copy routes coaches into it):** the 409 sign-in-failure message asserted "that password didn't match — reset it", which is incoherent on the retry after a just-created account (made with that exact password, `email_confirm: true`, so no pending-confirmation state either) and could push a coach with the RIGHT password into a needless reset. It also never said the registration hadn't gone through. Rewritten to state the outcome and offer the likely cause without asserting it.
- **Review:** high-risk tier, 4 lenses (email blast-radius / A4.3 control-flow truth / 409 client-state + concurrency / public-home layout regression). 2 confirmed findings, both fixed in-pass; 2 accepted advisories (rAF scroll timing degrades gracefully to a top-scroll; keyframe defect below). typecheck ✓, focused lint ✓, token ratchets ✓ zero, date-correctness ✓ zero.

**Adjacent bug fixed in the same commit (pre-existing, found by the A4 review, owner-approved 2026-07-27): CSS-Modules keyframe scoping killed 13 public animations.** Turbopack rewrites an `animation:` name inside a CSS Module to the module-scoped identifier, but `@keyframes fadeInUp` / `fadeIn` live in `app/globals.css` (global scope) — so every module reference resolved to a keyframe that was **never defined** and the animation silently no-oped. Verified against compiled output (`.next/dev/static/chunks/app_[orgSlug]_Home_module_css_*.css`: `animation: .7s .3s both Home-module__WFQYJq__fadeInUp`, with no matching `@keyframes`). Nothing *looked* broken because an unmatched `animation-name` simply doesn't run, so the `both` fill never applies and no element was stuck at `opacity:0`. Affected: `Home.module.css` ×7 (hero badge/title/sub/CTA/CTA-note/stats/game cards), `register.module.css` ×1, `schedule.module.css` ×2, `standings.module.css` ×2 (`fadeInUp` + row `fadeIn`), `TournamentFlipPill.module.css` ×1. **Fix:** define the keyframes locally in each module so the scoped reference resolves (proven mechanism — the same files' `float`/`myTeamLivePulse`/`scrollPulse` are local and work); values identical to the globals, which stay for non-module consumers. **Still open, same class, deliberately NOT touched here (admin surface, different area):** `schedule-admin.module.css` references `adminSlideDown`, which is defined in `admin-common.module.css` — a cross-module keyframe reference, equally broken. **General rule for this codebase: a CSS Module must define every keyframe it animates; a global `@keyframes` is invisible to it.**

---

## TRANCHE B — DELIGHT & CONVERT

### Phase B1 — Zero-new-backend quick wins (mostly reuse of shipped fan-side pieces)

- **B1.1 Add-all-games-to-calendar** on the record page schedule: reuse `lib/export/ics.ts` `downloadICS()` / `lib/team-calendar.ts` `downloadTeamScheduleICS` (already wired on public `ScheduleContent.tsx` / `MyTournamentCard.tsx`), offered to the coach when a schedule exists.
- **B1.2 Tappable directions:** `components/coaches/CoachLiveSchedule.tsx:246` prints `game.location` as flat text; swap in `components/LocationLink.tsx` (already used by paid schedule + public game page).
- **B1.3 "We're in!" share button** on `CoachWelcomeBanner.tsx`: reuse `components/public/SharePageButton.tsx` (as the afterglow already does, `CoachTournamentRecord.tsx:534`), pointing at the public tournament page (gate on `publicCtx` resolving); OG card already exists (`app/[orgSlug]/[tournamentSlug]/opengraph-image.tsx`).
- **B1.4 Organizer logo in hero:** `tournaments`/`organizations` carry `logo_url` (`lib/db.ts:663,1934,1994`) not selected by the record page's queries (`CoachTournamentRecord.tsx:108-121,154-161`); select + render beside the monogram.
- **B1.5 "Meet the field":** the record page already queries accepted teams (`CoachTournamentRecord.tsx:144-150`, `id,name`); add `division_id` to the same select → "38 teams in — 9 in your division" + monogram chips. Gate on organizer's `publicHiddenPages` not hiding Teams.
- **B1.6 "Where you stand" standings widget:** reuse the public standings computation via the same client the schedule bridge uses (`usePublicTournamentLive` / `fetchPublicTournamentData` with the `standings` section, computed server-side in `lib/public-tournament-data.ts`). Pool rank/record during round-robin; bracket position once seeded.

**B1 MOCKUPS APPROVED 2026-07-27 (gate satisfied — mockups ARE the visual spec):** artifact `claude.ai/code/artifact/f613df11-d66a-493f-86eb-b2013a41bf29` — 4 phone frames (pre-schedule / published / mid-event / first-visit) in A2 chrome, warm+dark toggle, NEW/RESTYLED labels. **Decisions ◆H/◆I/◆J all accepted as recommended:** (H1) "Where you stand" lives INSIDE the Schedule zone — standings are the outcome of the games listed above them; a 5th zone would break the A3 fixed zone order and sit empty most of the event. (I1) "Meet the field" is the Schedule zone's PRE-EVENT state — that zone was a near-empty "not published yet" apology for the longest stretch of the coach's wait. (J1) the "We're in!" share is BOTH — celebration copy on the welcome banner AND a permanent "Share this tournament" beside the organizer resource pills, because the banner is first-visit-only and dismissible, so one tap of the X would otherwise destroy the most shareable thing a coach has.

**B1 BUILD RECORD (2026-07-27, uncommitted — owner QA + commit OK pending):**
- **Schedule zone owns three of the six** (◆H1/◆I1): `CoachStandingsSnapshot` leads the zone once `gp > 0` (rank/record/differential, ranked WITHIN the coach's pool when the division is pooled — the same filter `StandingsContent` uses; whole-division rank is a different and wrong answer to "where do we stand"); `CoachScheduleCalendarButton` above the list it exports; `CoachMeetTheField` renders only in the no-games branch, so it steps aside the moment real games arrive.
- **B1.1 calendar:** ICS events are built SERVER-side and passed down ready-made, so the client ships a handful of small objects instead of the tournament's whole games/teams/divisions payload; `downloadICS` is dynamically imported on click so the `ics` package stays out of the record bundle. Cancelled games are included deliberately (exported `STATUS:CANCELLED` so a re-import cleans the coach's calendar). Copy states the snapshot-not-subscription caveat.
- **B1.2 directions:** `LocationLink` replaced the flat location text — which forced a row restructure: the row card was itself an `<a>`, and an anchor inside an anchor is invalid HTML. The game deep-link moved to an inner `.rowMain` (carrying the row's former flex layout) with the venue as a sibling; hover/press feedback moved to `.rowInteractive` on the card. No venue records are passed (the record page doesn't load venues) so LocationLink falls back to a Maps search on the location text — its normal path when an address is unset.
- **B1.3 share (◆J1):** `SharePageButton` on the welcome banner (celebration) + permanently beside the Zone 4 resource pills. Both gated on the tournament actually being public; neither renders for a rejected team.
- **B1.4 logo:** `logo_url` added to the tournaments + organizations selects; tournament logo wins over the org's (a multi-event org brands events separately, and this record is about ONE event — matches the DATA_DICTIONARY note that the per-event value overrides). `object-fit: contain` (cropping a club crest is worse than letterboxing), hidden ≤380px.
- **B1.5 field:** `division_id` added to the EXISTING accepted-teams select — no new query. Suppressed when only the coach's own team is accepted ("1 team in" reads as an empty room).
- **Gating:** every new block honors the organizer's public-page choices. Standings use the CANONICAL `isPublicPageEnabled` helper, not a raw `public_hidden_pages` check, so a bracket-only tournament is suppressed the same way the public standings/champions/playoffs pages suppress it.
- **Review (correctness + tier-seam lens):** tier seams verified intact — no new block renders money (so `moneyRedacted` is unaffected) and none are gated on `suppressUpsell` (correct: they're informational, and the standing rule is that the tournament experience is identical across tiers). 3 findings fixed in-pass: (1) the live/broadcast row still referenced the `rowLink` class the restructure deleted, so it rendered a literal `"undefined"` class and silently lost its hover/press feedback; (2) the divisions select lacked `playoff_config`, so the coach's standings snapshot ignored division-level tie-breaker overrides, the run-diff cap, AND any coin-toss result an organizer recorded — meaning the snapshot could disagree with the public standings page for exactly the ties a human had to settle; (3) the raw hidden-pages check for standings bypassed the bracket-only suppression rule.
- **⚠ Known performance follow-up (pre-existing, NOT introduced here, out of scope to fix in this diff):** `getStandings` takes no tournament id — it calls `getGames(undefined)`/`getTeams(undefined)`, which skip the `tournament_id` filter and scan the games/teams tables PLATFORM-WIDE, then narrow client-side. Existing callers (public standings/champions/playoffs, `TournamentHomeContent`) already do this, but B1.6 adds a **high-frequency** call site: every accepted coach's record page load during an event. Worth giving `getStandings` an optional tournament-id scope — a shared `lib/db.ts` signature change touching many callers, so it belongs in its own unit of work.
- **Verify:** typecheck ✓, focused lint ✓, token ratchets ✓ all scopes zero, date-correctness ✓ zero. Dev server restarted clean (new files added) and verified serving.

### Phase B2 — Coach notifications (biggest capability gap; needs design pass first)

Verified current state: **no channel fires to anyone on a routine single-game edit** (`app/api/admin/games/route.ts:770-797` writes and returns; only the bulk rain-delay shift calls `syncGameDayRemindersAfterReschedule` :736, which is email-only, first-game-only, org-disableable, and keyed to the registration-time email — `lib/game-day-reminders.ts:71-132`, `lib/email.ts:112-121`). Fan push targeting is 100% `fan_follows`/`fan_push_subscriptions` (`lib/fan-notify.ts:81-86`), never written by the basic-coach model (explicitly intentional today: comment at `app/coaches/team/[basicTeamId]/page.tsx:193-195`). `lib/notify.ts` recipient resolution is org-membership-shaped (:133-177) — structurally can't reach org-less coaches. `/account/notifications` has no `coaches_basic` branch by design ("later phases", `app/(consumer)/account/notifications/page.tsx:80-116`). CoachPortalShell has no notifications entry anywhere (:300-322, :467-487). NotificationEventType has no reschedule member (`lib/types.ts:1834-1858`).

**B2.1 (M, in-app, no infra decision needed): schedule-change banner.** `CoachLiveSchedule.tsx`'s 30s game-day poll already receives full game rows but its `LivePatch` (line 74) merges only score/status, silently dropping time/location changes. Surface the diff: "Schedule updated — your 2:00 vs Thunder moved to 3:15, Field 4."
**B2.2 (design decision): real coach alerts.** Options: (a) bridge basic-coach team ownership into the existing fan push pipeline (auto- or one-tap follow-your-own-team; today's "Highlight my team" hint explicitly says it doesn't alert), or (b) extend `notify()`/prefs with a `coaches_basic` recipient shape. Scope the minimum lovable: push/email on schedule change + game final for the coach's own team, with an opt-in toggle inside the portal (shell needs a notifications entry either way).
**Dependencies/cautions:** prod Android PWA push issue (VAPID mismatch — see `memory/project_push_delivery_diagnosis.md`); single-game-edit should also trigger the email resync, not just the bulk tool; a reschedule NotificationEventType addition touches the notification-settings matrix (coordinate with `/plan` + notification-settings project).

---

#### B2 RE-BASE + DECISIONS (2026-07-27, owner-accepted; planning artifact `claude.ai/code/artifact/0b2839a8-5d32-437f-a7a1-e21cfa0c70a0`)

**Three premises above are stale — verified against code 2026-07-27, not plan text:**
1. **⚠ "Fan push targeting is 100% `fan_follows`, never written by the basic-coach model" is NO LONGER TRUE.** A3's auto-highlight (2026-07-27) calls `useFollowedTeam().follow()` → `saveFollowedTeam()` → **`syncFollowToAccount('follow', …)`** → `POST /api/consumer/follows`, which writes a real **account-level `fan_follows` row**. So **every signed-in free coach whose event is live is already following their own team server-side.** Option (a) in B2.2 is therefore ~90% already built as an unintended side effect: `notifyFansForGame` (live/final) can already reach them, subject to a push subscription + `fan_score_alerts` (tournament_plus). Nobody has told them, there's no in-portal switch, and the tier dependency is invisible.
2. **The real gap is wider than "coaches": a single-game edit notifies NOBODY.** `app/api/admin/games/route.ts` `action === 'update'` writes and returns — no fan notify, no coach notify, no email resync. `notifyFansForGame` is called **only** from `lib/tournament-scoring-service.ts` (score/status), never from a schedule edit. So a fan who deliberately followed the team and opted into alerts gets nothing either. This is a platform capability gap, not a coach-tier patch.
3. **The portal already receives the change and discards it.** `CoachLiveSchedule`'s poll reads the full game rows (time, location, status, score) and its `LivePatch` keeps only score+status. B2.1 needs no infrastructure at all.

**B2 splits into THREE pieces, not two:**
- **B2.1 — in-portal change notice.** Surface the discarded diff. No infra, no permissions, no gate, no dependency.
- **B2.2 — the alerts panel.** Mostly *surfacing* an existing capability, not building one. Honest framing required: coaches are ALREADY followed, so the panel says "here's what you're set up to receive + the off switch", not "turn on alerts".
- **B2.3 — actually send the schedule change.** The genuine missing capability; serves families as much as coaches.

**Decisions ◆Q–◆S — all accepted as recommended (owner, 2026-07-27):**
- **◆Q1 — the FOLLOW system owns schedule-change alerts, not a coach-only channel. And this was already decided.** `PLAN_PRICING_FACTS.md` carries a **Decided-but-not-yet-built** line from **2026-07-06**: *"Fan tournament-message push = Tournament Plus — new anonymous-fan push for organizer day-of messages + schedule changes gates at `tournament_plus`… (RAIN_DELAY_DAYOF_OPS_PLAN.md Feature A)"*. **B2.3 IS that item.** Coaches ride the follow they already have; families get it in the same send; the tier question is pre-answered. Do NOT build a parallel `coaches_basic` recipient shape — `notify()` is org-membership-shaped and org-less coaches can't reach it without new plumbing, and it would leave families with nothing.
- **◆R1 — B2.1 (the in-portal notice) is FREE on every plan, always.** Push is the paid convenience; *being informed* is not. A coach on a free organizer's event still finds out — when they open the app rather than by phone buzz. **This is a gating decision → logged to `BUSINESS_DECISIONS.md` 2026-07-27.** The free-event case is the COMMON case for the first-tournament persona, not an edge case.
- **◆S1 — the single-game edit path also triggers the game-day reminder resync, in the SAME unit of work as B2.3.** `syncGameDayRemindersAfterReschedule` already exists and is simply never called from the manual edit path, so today a hand-moved game leaves the "your first game is tomorrow" email announcing a time that is no longer true.
- **Inherited rule (non-negotiable, from the 2026-07-27 pressure-ladder entry):** a schedule-change alert is a **SERVICE** message and carries **no Premium ask on any channel** — not in the push, not in the email, not in the banner. Where the organizer's plan limits what we can send, say so plainly and stop; never aim an upgrade prompt at a coach who isn't the one who could buy it.

**⚠ BLOCKING DEPENDENCY (owner action, not a build task): prod push is broken and unverified.** Android installed-PWA push has delivered nothing on prod since 2026-07-04 while the in-app bell works — i.e. the break is between server send and device delivery (config, not code). The self-serve **"Test this device"** diagnostic shipped to prod on 2026-07-04 and **has never been run**. Building B2.3 on an undelivering channel produces a feature that looks finished and reaches nobody. See `memory/project_push_delivery_diagnosis.md`.

**Sequenced build order (accepted):** **0.** owner runs "Test this device" on prod → **1. B2.1** (unblocked by anything) → **2. B2.3** (needs ◆Q/◆R + a working push channel) → **3. B2.2** (little to describe before B2.3 exists) → **4. ◆S1** rides with B2.3.

**Open questions carried into build (not yet decided):**
- **Batching/volume:** an organizer can move 20 games in 10 minutes while building a schedule. Whatever B2.3 sends must arrive as ONE message, not twenty — the batching window is undecided and is a real design input, not an implementation detail.
- **Horizon:** does a change to a game three weeks out alert the same way as one two hours out? Same message, very different interruption tolerance.
- **Privacy posture:** B2.3 would be the first *operational* push to a family-side follower (today's fan pushes are scores). Existing follow relationship, but worth a look before it ships (PIPEDA/CASL sensitivity — same class as the family-features review).

**Process note:** the planning artifact's frames are **sketches, not an approved spec** — the PROCESS GATE in this plan's header still applies and B2.1/B2.2 need approved mockups before build. **Plate 1 RATIFIED as the B2.1 spec by the owner 2026-07-28.**

**✅ PUSH BLOCKER CLEARED 2026-07-28** — the owner ran "Test this device" on prod and it **delivered successfully**. Prod push is confirmed working end-to-end from a real device; B2.3 is unblocked. (Re-test before any push-dependent release rather than assuming this holds.)

#### B2.1 BUILD RECORD (2026-07-28, uncommitted — owner QA + commit OK pending)

- **What a coach sees** (`CoachLiveSchedule.tsx` + `.module.css`): while the game-day poll is running, a moved or cancelled game raises an amber **"Schedule updated"** bar naming what happened — *"Your 2:00 p.m. vs Northside Thunder moved to 3:15 p.m. at Diamond 4."* — and the affected row picks up an amber border plus a `was 2:00 p.m. at Diamond 2` line. Tap the bar to dismiss. **The row marker deliberately OUTLIVES the dismiss**: the bar is the interruption, the row line is the record, and it's the thing a coach checks twice.
- **Mechanism:** no new infrastructure. The 30s poll has always carried each game's date/time/location; the merge kept only score+status and discarded the rest. It now diffs against a `gamesRef` baseline (a ref, NOT the state updater — computing the diff inside the updater would make it impure and StrictMode's double-invoke would duplicate every entry), applies the change, and records it.
- **Copy rules:** one change is named in full (the whole point — a coach mid-event shouldn't hunt for which game moved); **2+ collapse** to "N of your games have changed — see the highlighted rows below", because five stacked sentences on a phone is a wall, not an alert. A cancellation outranks a time/place difference in the same poll. Only the LATEST change per game is kept, but the ORIGINAL "was" baseline is preserved across repeated moves, so two moves in one sitting read as one correction rather than a history.
- **Ladder compliance:** service message, zero CTA, zero Premium mention, **free on every plan** (◆R1). A dismissed bar re-raises only when a genuinely NEW change arrives.
- **Theming:** amber via `--warning` / `--warning-rgb`, which the coach warm gate already remaps to `--home-amber` — correct in both skins with no literals (token ratchets zero).
- **✅ BOTH DEVIATIONS RULED ON + BUILT 2026-07-28** (owner, after a placement mockup — artifact `2a333df4-7a7c-4453-8b52-f9d96019b318`, both placements × warm + dark; PROCESS GATE satisfied, mockup approved before code):
  1. **Placement → option B: the bar is hoisted ABOVE the Schedule zone head.** Built via a **React portal**, not a second poll and not a context indirection: the record page renders an empty slot div above `ZoneHead` (gated on `showLiveBridge`, and zero-height because the zone is block flow, not a gapped flex container), and `CoachLiveSchedule` portals the bar into it while keeping **sole ownership of the poll**. A portal costs nothing at first paint here because the bar never exists on the server render — it only ever appears from a client-side poll. Slot lookup runs **post-commit in an effect**, deliberately not in a `useState` initializer: on a client-side navigation (Overview → record page) the new tree renders before it is committed to the document, so a render-time lookup would miss and the bar would silently fall back inline for the rest of that visit. `alertSlotId` is optional and the bar falls back to inline rendering when the slot is absent, so a future page that drops the slot degrades placement instead of losing the alert. New `.changeBarHoisted` carries the bar's own bottom margin, since portalling out of `.bridge` leaves that flex `gap` behind. Applies to the Premium record too — the record is one shared surface by design.
  2. **Window → from SCHEDULE PUBLICATION, not game day.** `pollEnabled = canLinkPublic && !isResultPhase`; the branch already requires a published division schedule, so the only added gate is "the event isn't over" (a finished event can't be rescheduled and must not poll). **Cadence is now tiered:** game day keeps 30s, the run-up polls at **120s** — widening the window multiplies polling across every coach's record page for every day before the event, and a run-up change is not a seconds-matter event the way a game-day move is. ⚠ **Load is now spread over the whole run-up rather than one day — worth watching on the first multi-day event.** Note the in-app bar was never the only pre-game-day channel: B2.3's push already covers every future game with no cutoff, so this closes an *in-app* gap (the "was 2:00 p.m." comparison), not a notification gap.
- *Original flags, kept for the record:*
  1. **Placement.** Plate 1 drew the bar above the zone head (page level). It renders at the top of the schedule block instead, which on game day sits *below* the standings snapshot and calendar button. Reason: the poll must stay single-owner (`CoachLiveSchedule` owns it by design — see its header comment), and the record page is a server component that can't hold this client state. Lifting it to page level means either a second poll (rejected) or a portal/context indirection — a real change, not a tweak. **Offer to the owner as a follow-up.**
  2. **Window.** The poll runs on game day only (`pollEnabled = canLinkPublic && phase === 'game_day'`), so a change made the night before does NOT raise a bar — it simply renders correctly on next load, with no "was" comparison (the server render is the baseline). Widening the poll window has load implications across every coach's record page and is its own decision.
- **Not covered:** the LIVE broadcast row doesn't take the changed-row marker (a game in progress moving is vanishingly rare and the marquee row has no place for the line).
- **Verify:** typecheck ✓, focused lint ✓, token ratchets ✓ all scopes zero, date-correctness ✓ zero. Client-side label helpers mirror the server twins' expressions exactly (the server module is `server-only` and can't be imported into a client component) — both timezone-safe.

#### B2.3 MOCKUPS APPROVED 2026-07-28 (gate satisfied — plates ARE the visual spec)

Artifact `claude.ai/code/artifact/9fbb7b7a-a9bb-4941-adbb-a626527a1cf1` — 5 plates: the push itself (single move / cancellation / roll-up, plus the explicit "what it never says" block), the batching + horizon model, the organizer-side notice line (Plus + free variants, dark + light), the switch copy (dark + warm), and the free-organizer case. Owner sign-off 2026-07-28 ("Approved — build it").

**Decisions ◆T1–◆T4 — all accepted as recommended (owner, 2026-07-28):**
- **◆T1 batching = automatic, quiet window.** Queue each change; a `*/5` sweep sends one message per team once their changes have been quiet for 10 minutes; imminent games skip the wait. The organizer-confirmed-bar alternative was mocked and REJECTED — perfect batching, but "if they never click, nobody is told" is close to the gap B2.3 exists to close.
- **◆T2 horizon = every future game, no cutoff.** A coach who booked a hotel for Saturday 8am needs to know it moved to Sunday. Urgency changes DELIVERY (straight through vs quiet window), never whether we send. Past/started/played games send nothing.
- **◆T3 no new switch — schedule changes ride `event_news`.** **This supersedes the 2026-07-06 three-category decision** (`RAIN_DELAY_DAYOF_OPS_PLAN.md` owner decision 2, "Score alerts / Schedule changes / Tournament messages"), which was written against the anonymous `fan_push_subscriptions` shape *before* account-level `fan_alert_prefs` replaced it. The bell has described that switch as "rain delays & day-of updates" since A2 — a third row splits a promise users have already read. Recorded in the dictionary at `fan_alert_prefs.event_news`.
- **◆T4 privacy = ship, record the reasoning.** No commercial content (the ladder forbids any ask), discloses only what the public schedule already shows, rides an explicit follow + an explicit push opt-in revocable in one tap, and honors both the per-category switch and the account-level master pause. Not legal advice; posture recorded rather than counsel-gated.

**A premise the brief carried that verification improved:** the batching problem is much smaller than "20 games in 10 minutes" suggests. **`divisions.schedule_visibility` is the real gate** — a division that hasn't published has shown nobody its times, so there is nothing to correct, and the twenty-edit case IS the building phase. The quiet window only has to cover post-publish editing, which is genuinely small.

#### B2.3 BUILD RECORD (2026-07-28, uncommitted — owner QA + commit OK pending)

- **What changes for a coach or family** (`lib/schedule-change-notices.ts` NEW, `lib/fan-notify.ts`): a move or cancellation on a **published** schedule now reaches the phone of everyone following that team — the coach (already auto-followed since A3) and families, in one send. One change is named in full ("Your 2:00 p.m. vs Northside Thunder moved — Now 3:15 p.m. at Diamond 4"); two or more collapse to a count, with cancellations called out. Voice inherited from B2.1's in-portal bar; `possessive()` twinned locally (the B3 helper lives in a client component, this module is `server-only`).
- **What changes for an organizer** (`schedule/page.tsx` edit modal): editing a published, still-scheduled game now says so before saving — amber "Saving alerts {Team A} and {Team B} followers" on Tournament Plus, or the plain-spoken free variant naming the limit. **The only surface where a plan sentence is allowed**, because the organizer IS the party who could buy it; the alert itself carries no ask on any channel.
- **Mechanism (mig 205 `game_change_notices` + pg_cron `schedule-change-notices` `*/5`):** each person-visible change to a published game is queued per affected team; the sweep (`/api/platform-admin/schedule-change-notices`, cron-or-super-admin, same door as dues/digest) groups by (tournament, team), holds for a **10-minute quiet window** (max hold 60 min so a slow editor can't stall a batch forever), and sends one push. A game starting within **6 hours** skips the wait entirely. Kind/opponent/current time are re-read from `games` at send time, so **move-then-cancel sends a cancellation** and **move-then-move-back sends nothing**; only the `was_*` baseline comes from the row, and it is the EARLIEST one so two edits read as one correction. Claim-then-send (conditional `sent_at` stamp before the push) makes overlapping sweeps unable to double-buzz.
- **Trigger surface:** `PATCH /api/admin/games` — `update`, `cancel`, and **`revert-to-scheduled`** (a follower told the game is off must be told it is back on). Pure `generatorLocked` / notes / bracket-code / duration edits are silent: venue is diffed on the human `location` string, never the structured ids, so the message can always name the new place.
- **◆S1 delivered:** the single-game paths now call `syncGameDayRemindersAfterReschedule`, so a hand-moved game no longer leaves the "your first game is tomorrow" email stating a time that is no longer true. It inherits the **publish gate** via `recordGameScheduleChanges`'s returned `affectedTeamIds` — deliberately, so a draft schedule can't cause reminder emails to be scheduled ahead of publishing. Awaited, not `void`-ed: this deployment has no `waitUntil` bridge (`reference_next_after_amplify`).
- **The bulk tool routes through the same sender (the brief's open question):** `bulk-reschedule` now queues the same per-team notices and returns their ids; `ShiftDayModal` holds them and the announce step passes them as `supersedeNoticeIds`, so completing it **with notify on** stands the automatic notices down — one buzz per rain delay, not two. A **site-only** post (notify off) supersedes nothing, and **skipping** the step leaves the automatic notice to do its job. Today, skipping means nobody is told at all.
- **Copy (◆T3):** `FanAlertsCard` "Event news" blurb → "Schedule changes, cancellations and announcements"; `FanNotificationBell` → "Schedule changes, rain delays & day-of updates"; the honest free-event gate line widened from "doesn't offer alerts" to "doesn't send phone alerts — following, live scores and schedule updates in the app all still work there."
- **Gating:** push rides the existing `fan_score_alerts` (Tournament Plus) gate inside `fanPushContext` — **no new key, no price/plan/capacity change, zero drift vs `PLAN_PRICING_FACTS.md`**; this executes its 2026-07-06 Decided/not-yet-built line. `event_news` category + the account-level master pause are both honored (the pause filter already lives in `mergedTargets`).
- **Verify:** typecheck ✓, focused lint ✓ (0 errors; the 13 `any` warnings in the games route are all pre-existing lines, none added), `verify:changed` ✓ on every ratchet — token-debt zero in all five CSS scopes, date-correctness zero, snapshot freshness #205, dictionary coverage OK. **Schema parity intentionally RED**: `game_change_notices` is dev-only until mig 205 is applied to prod. That is the gate working — **apply 205 to prod BEFORE promoting this code to master** (the sweep route and the queue writer both read the table).
- **⚠ Release dependencies:** (1) mig 205 on prod; (2) `CRON_SECRET` + Vault base-url must already be configured on prod — they are, since the digest and dues ticks run there, but the new job needs the migration to schedule it; (3) **re-test push on a real device before the release** rather than assuming the 2026-07-28 result holds.
- **Known follow-up, not built:** sent/superseded notice rows are never pruned (both partial indexes exclude them, so reads stay cheap) — add a retention sweep only if volume justifies it.

**B2.3 `/review` — 4-lens adversarial funnel, high-risk tier (2026-07-28). 12 confirmed findings, all fixed in-pass; 0 unresolved.** Lenses: recipient-correctness/privacy · send-volume/concurrency · message-truthfulness · regression/blast-radius. Deterministic gate first (typecheck ✓, focused lint ✓ 0 errors, all ratchets ✓; `check:migrations` red = the intended prod-behind-dev release gate).

*Refuted with evidence (no change needed):* per-team recipient scoping cannot cross team/tournament/org; the Tournament Plus gate is unbypassable (every recipient line sits behind `fanPushContext`); the `event_news` category filter and its legacy `notify_messages` mapping are correct for the new caller; the account master pause and its same-endpoint anonymous-leak guard are inherited intact; the claim-then-send UPDATE is genuinely race-free under Postgres READ COMMITTED (EvalPlanQual re-checks the predicate post-lock, so the loser returns zero rows); `recordGameScheduleChanges` structurally cannot throw, so a committed bulk reschedule can't 500; the new admin modal block can't crash on partial load and adds nothing to the inline-TSX colour baseline; move-then-cancel, move-then-move-back, double-move and cancel-then-restore all narrate correctly; no date-only-parsed-as-UTC bug (the known repo-wide class) anywhere in the new label helpers.

*Fixed — High:*
1. **Cross-tenant suppression.** `supersedeScheduleChangeNotices` filtered on client-supplied ids ALONE. The route scope-checks `tournamentId` but never the id list, so one org could have suppressed another org's pending alerts. Now takes `tournamentId` and scopes the update to it.
2. **The urgency bypass had no memory.** Urgency was re-evaluated fresh every 5-minute tick with nothing recording "we just told this team", so eight game-day edits over twenty minutes produced 4–5 separate buzzes — the exact promise this module exists to keep. Added a **15-minute per-team repeat cooldown** (`REPEAT_SEND_COOLDOWN_MINUTES`) that outranks urgency, with `MAX_HOLD_MINUTES` still the anti-starvation escape hatch.
3. **A reassignment edit could state a false time.** The admin modal submits team ids together with date/time, and the notice was built from the AFTER teams with the BEFORE baseline — so a newly-assigned team was told "your 2:00 moved" for a slot it never held, while the outgoing team got nothing. Restructures are now **deliberately silent**: no true "your game moved" sentence exists for a changed matchup, and the brief's own standard is that a false notification is worse than none.
4. **A committed edit could return a false 500.** The post-write snapshot read sat outside the announce try/catch; a network throw there would have failed a save that had already succeeded. Folded inside.
5. **A plain "Save game" could hang on Resend.** The reminder re-sync was awaited before the response and makes per-team third-party calls with no client timeout. Now fire-and-forget, matching the shipped bulk path; the queue write stays awaited because that IS the feature.

*Fixed — Medium:*
6. **Unpublish-after-queue leaked hidden times.** The publish gate was only tested at enqueue. An organizer pulling a schedule back to draft would still have had those times pushed. Re-tested at send time from the game's current division.
7. **Rain delay could double-message.** If the automatic notice fired before the organizer finished composing theirs, the stand-down no-op'd and the follower got both. Bulk notices now carry a `hold_until` reserving a 10-minute announce window.
8. **Bulk notices were composed from PLANNED, not committed, state.** The reschedule RPC guards each row on `status='scheduled'`, so a game scored between plan and write is silently skipped — and would have been announced as moved anyway. Now re-reads the settled rows and diffs against those.
9. **A large bulk shift could split one team's batch across two sweeps** (2000-row cap + one shared `created_at` + no tiebreaker → arbitrary slice → two messages). Added `id` as a deterministic secondary sort; the new cooldown also absorbs the residual case.

*Fixed — Low:*
10. `collapse()`'s earliest-baseline pick fell back to iteration order on tied timestamps — now breaks ties on `id`, so it is genuinely order-independent under `UPDATE … RETURNING`.
11. A game with no prior time rendered "Your **vs** Thunder moved" — now "Your **game vs** Thunder moved".
12. Per-group `try/catch` in the sweep: rows are claimed before composing, so an unexpected throw would have stranded them AND aborted every group queued behind.

*Advisories logged, no change:* diffing venue on the human `location` string is safe for every current call site but is an implicit contract (a future caller setting venue ids without recomputing `location` would go silent); the reminder re-sync still runs for every affected team rather than only when the team's FIRST game moved (idempotent, and now off the request path); the bracket-dependent delete loop pays two extra light reads per iteration (verified it can never notify); no optimistic-concurrency guard on the before/after snapshot pair (mirrors the pre-existing lack of one on the underlying update — affects notification copy only, not data).

**Live query smoke-test (not just typecheck):** every sweep query — the `hold_until`-filtered pending read with deterministic ordering, the cooldown lookup, the atomic claim, the tenant-scoped supersede, and the publish re-check — was executed against the live dev schema and returned clean; anon confirmed **permission denied** on the new table (RLS posture verified live, not from the migration comment, per the house rule).

#### B2.2 MOCKUPS APPROVED 2026-07-28 (gate satisfied — plates ARE the visual spec)

Artifact `claude.ai/code/artifact/d211e479-c947-4da6-be5f-72c8ac309f5f` — the door (unchanged) + the rejected coach-only alerts screen, the card in four states (Plus / free-event / device-not-enabled / not-yet-following), and the recipient-timing gap. Owner accepted all three recommendations 2026-07-28; the plates were drawn to them, so that acceptance IS the plate sign-off (stated to the owner at the time).

**◆V1–◆V3 — all accepted as recommended:**
- **◆V1 — a coach becomes a recipient at ACCEPTANCE, not at event-live.** See the re-base below; this is the phase's real content.
- **◆V2 — one card, not two.** The coach-aware line joins the existing "Followed teams" card. Two cards carrying the same two switches would imply two settings, and the first coach to turn one off and find the other still on would be right to distrust the screen.
- **◆V3 — the panel names the team, the event and the limit, and stops.** No batching windows, no timing rules; a coach needs to know whether their phone will buzz, not how the queue works.

**⚠ B2.2 RE-BASE — the phase was scoped as "the alerts panel" and that premise was wrong twice over:**
1. **The door already exists.** `/account/notifications` is linked from the Account tab for every signed-in user (`app/(consumer)/account/page.tsx`). A coach-only alerts surface inside the portal was **mocked and REJECTED** — it would be a third notification vocabulary (exactly what A2 retired) and two places claiming the same switches.
2. **The panel wasn't the gap — the recipient list was.** Verified: the auto-follow that makes a coach a recipient was seeded inside `CoachLiveSchedule` behind `if (!live) return`. **So a coach only became reachable once their event went LIVE, while organizers do most of their rearranging in the days before.** A B2.3 alert fired in that window reached the families who had followed the team deliberately — and *not* the coach. B2.3 was correct; the coach simply wasn't on the list yet.

#### B2.2 BUILD RECORD (2026-07-28, uncommitted — owner QA + commit OK pending)

- **◆V1 (`CoachTeamAutoFollow.tsx` NEW, headless):** the seeding is **extracted out of `CoachLiveSchedule`** and mounted on **any accepted registration record**, so "am I a recipient?" no longer depends on "is the live schedule on screen?" — the conflation that caused the hole. Every original guarantee is carried over byte-identically: seeded once per device+tournament via the **same** `fl-coach-autopin:*` key (so a coach already auto-followed under the old behaviour is not re-seeded), reads the follow store directly because the hook hasn't hydrated on the first effect pass, never overrides an existing highlight, and **never re-pins after a deliberate un-star**. Gated strictly on `team.status === 'accepted'` — pending, waitlist and rejected never seed.
- **Why client-seeding-on-view is enough (and a server accept-time write was NOT built):** an account-path push requires a `push_subscriptions` row, which only exists once the coach has granted permission **inside the app**. A coach who has never opened the portal cannot receive a push regardless of follow state, so seeding at first view of an accepted record loses nothing real — and it avoids editing `app/api/admin/teams/route.ts`, which a concurrent session had in flight.
- **◆V2/◆V3 (`FanAlertsCard` + `getFanAlertOverview`):** the card gained a coach lead line above the switches (full ink, not the dim note register — it is the card's most load-bearing sentence for a coach). `getFanAlertOverview` now returns `coached[]` by intersecting the user's follows with `getCoachedRegistrationTeamIds` (verified same id namespace: `fan_follows.entity_id` for `entity_type='team'` and a basic-coach registration id are both `teams.id`). Lookup failure is caught and degrades to **no lead line, never no card**.
- **The "not yet following" state from Plate 2 was deliberately NOT built.** ◆V1 made it near-unreachable: an accepted coach is followed on first view. What remains is a pending team (no schedule exists yet, so nothing to alert about) or a coach who deliberately un-followed — and putting an "Alert me" button in front of someone who just turned it off is nagging, not service. The plate framed that state as conditional on ◆V1; ◆V1 resolved it.
- **`/review` (recipient-timing lens, high-risk): 7 of 8 claims REFUTED with evidence** — the accepted-only gate holds in every render branch; no double-seed or effect loop (the `follow` identity is `useCallback`-stable on primitives, and StrictMode's double-invoke short-circuits on the synchronously-written seed key); a deliberate un-follow still sticks despite the broader mount; `CoachLiveSchedule` is clean after the extraction (`teamDivisionId` genuinely unused, single call site updated, `live` still drives `InstallAppPrompt`); the id namespaces genuinely match so the line cannot claim a team is "yours" when it isn't; **no cross-user leak** (both lookups scoped to the session user; the cache is React `cache()`, per-request, not global); and failure degrades correctly. **2 findings fixed in-pass:**
  1. **(Medium) A comment of mine described behaviour that doesn't exist.** I threaded an `email` param through to catch email-matched-but-unclaimed registrations — but the underlying helper destructures `email` and never uses it (the real email-matching lives in a different function this path never calls). The parameter was **inert plumbing with two comments asserting it worked**. Dropped the param; both comments now state the truth — coached is decided by the link table alone, which agrees with the auto-follow by construction.
  2. **(Low) The lead line asserted a history it can't vouch for.** It said "you were followed automatically **when** your team was accepted" — but the seed fires on the coach's next view, and a coach who had already followed their own team by hand would be told it was automatic. Rewritten to state the **rule** ("Coaches are followed to their own team automatically"), which is true in every case and answers the same question.
- **Verify:** typecheck ✓, focused lint ✓ 0 errors, token ratchets ✓ all six scopes (tsx baseline unchanged at 427 — the new CSS is tokens-only), date-correctness ✓ zero, org-context guard ✓. `/account/notifications` renders 200 on a restarted server. ⚠ One typecheck run mid-build failed on a **FOREIGN** Premium roster page a concurrent session had mid-edit; it cleared on its own — attribute before chasing.
- **⚠ Behaviour change worth naming at release:** both alert categories ride the same follow, so an accepted coach now starts receiving **live-score alerts** for their own team earlier too, not just schedule changes. Owner-accepted trade (◆V1); it is why this was a decision rather than an implementation detail.

### Phase B3 — Premium bridges (moment-based, not banners; leverage Founding Season $0-until-2027-01-01)

Verified gap: the highest-value persona (single team, one tournament) never sees the hub pitch banners (hub auto-redirect, `app/coaches/page.tsx:65-70`), Chat and the team tournaments list carry zero premium presence, and `ScopeCeilingInterest` in the afterglow only fires when `afterglowBasicTeamId` resolves. All new copy reuses the existing `checkoutOpen` / `isFoundingSeasonPromoActive('team')` gates (`lib/plan-config.ts`) — no new pricing surfaces; reconcile any copy against `docs/agents/strategy/PLAN_PRICING_FACTS.md`.

- **B3.1 Personal afterglow:** lead the "That's a wrap" block with the coach's real result (record computed at `CoachTournamentRecord.tsx:366-381`, already fed to the hero) + "Premium keeps this: season history, playing time, awards." Also fix `afterglowBasicTeamId` reliability so the block can't silently vanish for register-flow-only coaches.
- **B3.2 Second-tournament nudge:** on the team tournaments list when `history.length >= 2` — roster/history/next-season carry-forward line. Fires on a page that currently pitches nothing.
- **B3.3 Multi-game-day lineup callout:** one dismissible line under the live schedule when `todayGames.length >= 2` in `game_day` phase — free roster is name+jersey only; Premium lineups/depth chart are the honest answer.
- **B3.4 Solo-coach line in Chat** (first-open/empty-state, not per-message): free model has no assistant-coach concept at all (verified); bridges to Premium Staff.
- **B3.5 (optional) fee toil counter:** dynamic line above the fees ScopeShelf using the page's own `totals.unpaid` when it crosses a threshold.

**B3 RE-BASE (2026-07-27) — four premises above are stale or in conflict; verified against code, not plan text:**
1. **B3.1's "reliability fix" targets an unreachable state.** `canUserAccessTournamentRegistration` resolves access *by* `findLinkedBasicTeamForRegistration` — the record can only render for a coach who owns the link, so `afterglowBasicTeamId` is never null when the strip renders. Nothing to fix; the finding instead exposed a **duplicated lookup** (gate + afterglow ran the same query twice per load), now collapsed.
2. **B3.1's "That's a wrap block" no longer exists** (dissolved in A3): the result lives in the hero result card (with the share button), and the afterglow strip sits under the hero. B3.1 is therefore a copy rewrite of that strip, sitting directly beneath a card that already states the record — so it must not restate it.
3. **B3.4's surface is gone.** A2 retired the portal's Chat section and A3 confirmed the reversal; the coach's chat is the app-wide Chat tab, **shared with fans and paying coaches**. An upsell there is a tier/audience leak. Relocated (◆M1).
4. **B3.3 conflicts with a LOCKED decision.** The pressure ladder (`FREE_TIER_STRATEGY_PLAN.md` D5; `app/coaches/team/[basicTeamId]/page.tsx` C4 comment: *"never on the tournament variant — game day stays pitch-free, locked decision"*) forbids exactly this. **Owner call 2026-07-27 (◆L1): hold the line — B3.3 DROPPED, not excepted.** Reopening it requires amending D5 via `/strategy`, not a component-level exception.

**B3 MOCKUPS APPROVED 2026-07-27 (gate satisfied — mockups ARE the visual spec):** artifact `claude.ai/code/artifact/0a4a74e5-d07b-471c-a62b-325c8b751a23` — 5 plates (afterglow / Premium tier-seam proof / tournaments shelf / game-day both-ways / Explore + fees) in A2 chrome, warm+dark toggle, NEW/RESTYLED/UNCHANGED/ABSENT labels. **Decisions ◆K–◆P all accepted as recommended:** (K1) the afterglow strip names the SEASON, never restates the record — the hero card owns that number; (L1) drop B3.3, weekend stays pitch-free; (M1) the solo-coach bridge relocates to Explore's capability list; (N1) the second-tournament nudge reuses the existing ScopeShelf, no new pitch anatomy; (O1) the fee-toil line ships at a 5+ threshold, stated as fact; (P1) **success criterion 6 restated** — a one-tournament coach who never opens team tools honestly gets ONE pushed moment (the afterglow) plus Explore always one tap away in the tab row. That is the ladder working, not a gap.

**B3 BUILD RECORD (2026-07-27, uncommitted — owner QA + commit OK pending):**
- **B3.1 afterglow (`CoachTournamentRecord.tsx/.module.css`, `ScopeCeilingInterest.tsx/.module.css`):** strip now opens on a lead line in full ink — "That's the first line of {Team}'s season history." — with an s-aware `possessive()` helper (coach-entered names made "Reds's" often enough to notice) and a neutral "That's a wrap for {Team}." fallback driven by the SAME `hasFinalRecord` flag the hero result card uses, so the two can't disagree. Supporting line names what leaves with the event (record / who played / awards). `ScopeCeilingInterest`'s checkout-open branch — whose only call site is this strip — retitled from the generic "Ready for the full toolkit?" to **"Keep the season, not just the weekend"** with a new mono eyebrow matching ScopeShelf's register; the gated interest-capture branch is untouched (different job). Promo/price sentence still single-sourced from `isFoundingSeasonPromoActive('team')`.
- **Duplicate-lookup cleanup:** `canUserAccessTournamentRegistration` now returns the **linked Basic team id** instead of the `'explicit'` marker — access and team identity are the same fact from the same query. The record page gates on it and reuses it for the afterglow (one lookup, was two); `findLinkedBasicTeamForRegistration` import dropped from the record. Only other caller (`/api/coaches/basic-teams`) moved from `access === 'explicit'` to `Boolean(access)`.
- **B3.2 tournaments shelf (`ScopeShelf.tsx`, team-scoped `tournaments/page.tsx`):** new `section="tournaments"` on the EXISTING shelf (◆N1 — reuse, not a new anatomy), gated by the caller at `history.length >= 2`. Free portal only by route (`app/coaches/team/**` has no Premium call site), so no `suppressUpsell` seam is involved.
- **B3.4 (`CoachExploreCatalog.tsx`):** "Assistant coaches with their own sign-in" added to `PREMIUM_FEATURES` in the mockup's position (after Attendance tracking). Verified accurate: the Premium Staff section is head-coach-gated only, **no plan gate** — it's part of the standalone $29 portal, not Club-only.
- **B3.5 (`ScopeShelf` `lead` prop + fees page):** the shelf gained an optional `lead` line rendered above the eyebrow in full ink. The fees page counts **distinct players with an unpaid fee**, not unpaid rows — fees are per-player and one player can carry several, so "9 unpaid" and "9 people to chase" are different numbers and only the second is the coach's workload. Legacy player-less rows excluded rather than guessed at. Threshold 5; below it nothing renders.
- **Tier seam:** nothing new reaches the Premium record. B3.1 sits inside the pre-existing `isResultPhase && !suppressUpsell` block; B3.2/B3.4/B3.5 are on free-portal-only routes. `moneyRedacted` is unaffected — no new element renders money (the fee-toil line is the coach's OWN team-dues ledger on a free-portal page, not tournament entry fees, and that page has no Premium call site).
**B3 `/strategy` ✅ DONE 2026-07-27.** New entry at the top of `docs/agents/strategy/BUSINESS_DECISIONS.md` (43rd): **the pressure ladder is promoted to a binding, centrally-recorded rule** — it had never been in the log, living only in `FREE_TIER_STRATEGY_PLAN.md` §D5 and a code comment, which is precisely why B3's own plan section proposed breaking it. The entry (a) states the rule (the game weekend never sells), (b) **enumerates the complete permitted-ask surface list** for the first time (post-event afterglow = the one earned pushed ask; the quiet shelf on Roster/Schedule/Fees/Announcements/Overview/Explore + the team Tournaments list at 2+ non-rejected entries; **nothing** pre-event or game-day; **Chat permanently out of scope** — it's shared with fans and paying coaches, so a pitch there is an audience leak not just a ladder breach), (c) records the corrected success measure (◆P1), and (d) carries a **binding reopening clause**: the game-day pitch may only be revisited by amending that entry, never by a component-level exception. Handoffs: `/marketing` none outstanding (copy canon already amended); `/billing` none (no gate/price change, zero drift vs `PLAN_PRICING_FACTS.md`); **`/plan` — B2 inherits the rule: a schedule-change alert is a SERVICE message and must never carry a Premium ask, on any channel.** §D5 in `FREE_TIER_STRATEGY_PLAN.md` now points at the log entry.

**B3 `/docs` ✅ DONE 2026-07-27.** `lib/help-content/coaches.tsx` (one module serves both the customer help page and the platform-admin support mirror). **The post-event wrap-up had NO coverage in the guide at all** — a pre-existing gap B3's rebuild exposed. Added: a wrap-up paragraph in the `tournaments` section (result card anatomy, the honest no-scores case, four sections stay put, same on both tiers) + NEW `faq-after-tournament-ends` (popular) which also states out loud that the afterglow note is the ONLY place the portal mentions upgrading during a tournament; NEW `faq-fees-two-counts` explaining why the Fees header count (unpaid *fees*) can differ from the new toil line (*people*), incl. the reconciling two-number form and the 5-player threshold; the `explore` section now names what Premium adds, incl. assistant coaches with their own sign-in and the fact that a free team is a single login. ~35 new search terms across three sections' `keywords`/`searchText`. **No anchors renamed or removed and no sections added/removed** — two new FAQ ids only, so every existing deep link still resolves and no hub card/quickLink change was needed. The B3.2 tournaments-list shelf is deliberately NOT documented: it's a passive footer in the same family as the four existing shelves, none of which the guide documents either.

**B3 addendum (owner QA feedback 2026-07-27, built same day) — the wrap-up hero was 4× redundant, and the dates disagreed:**
1. **Result-phase hero is now the result card and nothing else** (`TeamHQ.tsx` `resultOnly` branch + `.heroResultOnly`). Owner compared the built screen against the approved mockup and asked for the mockup's compaction. Suppressed in the result phase: the `"That's a wrap!"` headline, the status badge, the `heroSub`/`heroSubOrg` tournament + org lines, and the trailing `.heroDates` line. Every one of those is stated immediately above the hero in BOTH shells — the free portal's persistent header (`{tournament} · {org}` eyebrow + team name + dates) and Premium's in-page header (`{team}` / `{tournament} ({year}) - {org}`) — so the hero repeated the identity a third time and the dates a third AND fourth time. Registration status keeps exactly one home: the Status & Payment zone below. Card tightened to the mockup's proportions: lead demoted to a 0.78rem caption (the RECORD is the headline, "Event complete · {dates}" is its timestamp), record numeral up to 1.6rem, hero padding/margin pulled in.
2. **Share control is a compact pill** (`.resultShareBtn` passed to `SharePageButton`'s `className`). Its default is a full-bleed block, which inside the card outweighed the score beside it. Presentation only — same component, same behaviour.
3. **⚠ Date bug the QA screenshot exposed (pre-existing, NOT introduced by B3): the record hero printed a day EARLIER than the shell header on the same screen** — header "Jul 16–18, 2026", hero "Jul 15 - Jul 17, 2026". Cause: the record hand-rolled its range with `new Date('2026-07-16').toLocaleDateString(...)` — a date-only string parses as UTC midnight and renders as the previous day in any behind-UTC zone (the documented gotcha in `memory/reference_timezone_date_math_gotcha.md`; the ratchet doesn't catch this shape). Fix: `formatEventRange` + its manual `YYYY-MM-DD` parse were **lifted out of `CoachPortalShell` into `lib/timezone.ts` as `formatEventDateRange` / `parseDateOnlyParts`**, and both the shell and the record now call it — they can no longer disagree by construction. **Anything rendering an event date range should use this helper.**
- **Review (4-lens adversarial funnel, high-risk tier — lenses: tier-seam/security, access-gate blast radius, correctness + claim-honesty, runtime-CSS).** Both MANDATORY lenses came back clean with evidence: (a) every B3 element sits inside the pre-existing `isResultPhase && !suppressUpsell` block, `moneyRedacted`'s fee flattening is untouched and `TournamentTeamHQProps` carries no fee field at all, and no `app/[orgSlug]/**` route renders `ScopeShelf`; (b) the gate change is behaviour-identical — `findLinkedBasicTeamForRegistration` returns a Postgres uuid (no falsy-but-valid value), `teamId === registrationId`, no caller compared the `'explicit'` literal, and the id never reaches a payload it didn't already reach (it was always passed to `ScopeCeilingInterest`). **4 confirmed findings, all fixed in-pass:**
  1. **(High) The second-tournament shelf counted REJECTED entries.** `getBasicCoachTournamentHistoryForTeam` applies no status filter (unlike its sibling `findUnlinkedEmailMatchedRegistrations`, which explicitly excludes rejected), so a coach turned away from event A and accepted to event B would be told *"Second event, same roster… carries your roster from one tournament to the next"* with no first event behind them. Gate now counts non-rejected entries; the list itself still shows every entry, rejections included — that IS their record.
  2. **(Medium) The fees page showed two uncorrelated numbers.** The header counts unpaid ROWS, the new toil line counts PEOPLE; a player carrying several unpaid fees makes them diverge on the same screen ("12 unpaid" beside "9 players still owe"). The line now states both when they differ (`"12 unpaid fees across 9 players — that's 9 reminders to send by hand."`) and collapses to the mockup's short form when they agree. Same fix closes the advisory that `.filter(Boolean)` silently dropped orphaned player-less rows — the row total includes them, so their presence now forces the reconciling form instead of understating.
  3. **(Low) "…carries your roster, staff and results… into next season" overstated.** Roster and staff genuinely roll forward (`startNextRepSeason`), but a completed season's results are retained read-only, not merged. Reworded: roster + staff carry forward; results are "kept in one place", which is what Insights actually does.
  4. **(Low) `DATA_DICTIONARY.md` documented the old `'explicit'|null` contract** for the access gate, with a stale line reference. Updated in the same unit of work per the schema/behaviour-change rule.
  - **Advisory logged, no change:** "season history across every event" aggregates WITHIN a season (past seasons appear as a count, not merged data). The copy doesn't claim cross-season, so it stands — but don't extend it to "across seasons" without building that.
- **Verify:** typecheck ✓, focused lint ✓, full `verify:changed` ✓ (token ratchets all six scopes zero, date-correctness zero, snapshot freshness OK, schema parity 0, dictionary coverage OK, org-context guard clean, observability 100%). The only lint output was 14 pre-existing `no-explicit-any` **warnings** in a concurrent session's `app/api/admin/teams/route.ts` — FOREIGN, 0 errors, not B3.

---

## Premium portal direction — OPERATOR family (decided 2026-07-25; later tranche, light touch)

Ruling (same BUSINESS_DECISIONS.md 2026-07-25 entry): Premium stays workspace-shaped alongside org admin — deliberately NOT consumer-chromed. **No rebuild.** Standing rules + queued work:
- **Tournament parity rule:** the tournament-record surface (`CoachTournamentRecord`) stays SHARED between tiers — every free-tier tournament improvement (A3 zones, B1 quick wins, B2 alerts) lands in Premium automatically. Tiers differ in season tools, never in tournament experience.
- **When the Premium nav is next touched:** group `CoachesSidebar`'s ~14 sections into domains — Team (roster · staff · documents) / Season (schedule · attendance · lineups · development · tryouts) / Money (accounting suite) / Events (tournaments) / Insights (history · awards) / Comms (chat · announcements · notifications) — and converge shell components with admin conventions (shared header/Flip placement, mobile bottom-nav behavior, CollapsibleCard).
- **Upgrade-moment narration:** one screen at Basic→Premium upgrade explaining the shell change ("your team now has a workspace") so the mental model upgrades with the plan.
- **Marketing (at A2 build, via /marketing):** "tournament companion vs team operations HQ" tier language on `/for-coaches`, the pricing coach card, and upsell surfaces.

## Out of scope / adjacent notes
- **Online fee payment:** manual-only platform-wide by documented decision (`FREE_TIER_STRATEGY_PLAN.md` §9, decided 2026-06-07; prior adjudication TOURNAMENT_SEAM_UX_REVIEW A13 → Low/Advisory). Not re-opened here. Narrow follow-ups worth a backlog card: nudge organizers to fill `payment_instructions` at accept-time (field is optional/unenforced; worst case = bare mailto, `TournamentStatusBlock.tsx:116-125`), and `/strategy` should log the payments-deferral decision into `docs/agents/strategy/BUSINESS_DECISIONS.md` (currently only in the plan doc).
- **Housekeeping candidate for cleanup tranches:** orphaned duplicate PATCH `app/api/registrations/[id]/route.ts` still writes `payment_status` without `markPaidInFullPatch` stamping (no live caller found) — delete or fix before anything wires to it.
- **Shell-level FlipPill** (render the flip in CoachPortalShell like AdminEventHeader does, so placement can't drift per-page) — fold into any future coach-nav rework.

## Verification
- Owner browser-tests per AGENCY_RULES (mobile ≤600px for A1.2/A1.4; password-reset round trip for A1.1; a fresh registration for A3/B1 items).
- `npm run verify:changed` per phase; `npm run typecheck` for phases touching shared modules (A1.4 shell, A2 record component, B2 notification types).
- A1.1 and B2 touch auth/notification seams — run `/review` after each of those phases.
- Dev-server restart before handoff on phases that add/delete files or touch shared modules.

## Success criteria
1. Zero dead ends: password reset always lands a coach back in their portal.
2. On any coach page, the correct tab is highlighted and the flip + back affordances are where the platform convention puts them.
3. The tournament record reads as 4 labeled zones; fee/contact/schedule each stated once, authoritatively.
4. A coach can get schedule→calendar, directions, and standings without leaving the portal.
5. A schedule change reaches the coach (banner minimum; push/email once B2.2 lands).
6. ~~Premium impressions for the single-team persona go from ~0 to ≥2 natural moments per tournament lifecycle, all Founding-Season-aware.~~ **RESTATED at B3 (◆P1, owner-accepted 2026-07-27):** a coach who registers, plays and never opens team tools gets exactly **one pushed moment** — the post-event afterglow — plus **Explore**, permanently one tap away as the last section tab but never pushed at them. A coach who does open team tools additionally meets the quiet shelf on whichever pages they use, and the tournaments shelf once they enter a second event. All Founding-Season-aware. The original ≥2-pushed target is incompatible with the pressure ladder (D5) and was chasing a number rather than the promise; the ladder wins.
7. After A2: the free portal wears the consumer app's chrome (global bottom nav + top section tabs + event-style header, Flip header-right) — no third nav vocabulary remains; team chat reachable from both the global Chat tab and the team Overview doorway.
