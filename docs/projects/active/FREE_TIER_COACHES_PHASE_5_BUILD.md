# Phase 5 — Tournament Coach Experience · Build Plan

**Status:** SCOPED 2026-06-09 (10-agent workflow: 8 surface maps → architect synthesis → adversarial critic) · awaiting owner sign-off on the open decisions before build · branch `feat/free-tier-coaches`
**Parent:** [FREE_TIER_COACHES_UNIFIED_PLAN.md](FREE_TIER_COACHES_UNIFIED_PLAN.md) Phase 5 · detail ref [COACHES_EXPERIENCE_EVAL_PLAN.md](COACHES_EXPERIENCE_EVAL_PLAN.md) Phases B/C/D/E
**Builds slice-by-slice with browser verification between slices** (same cadence as Phase 4's 4a–4d).

---

## PM brief — what changes for the tournament coach

Today a tournament coach lands on a thin, static record: a status word, a details list, and a schedule that just says "Home/Away" with no opponent. Coaches whose teams were **added by the organizer or imported via CSV often see nothing** — an empty portal or a not-found page — because the portal only shows registrations they linked themselves.

After Phase 5, the tournament experience comes alive on the **same shared foundation** as the free Basic team home:

- **A phase-aware Team HQ hero** that changes with the tournament's life stage — a checklist while pending/prepping, a "first game in 2 days" countdown once accepted, opponent names + tappable links to the public game page when the schedule is live, a live scoreboard on game day, and a celebration + a single well-timed "want more?" invitation **only after** the event.
- **Payment & check-in at a glance** — Paid / Owes (with how to pay and who to contact) and check-in status, all **read-only** (the organizer still records payment manually; there is never a "pay now" button).
- **Claim your team by email** — imported/admin-added coaches find and claim their registration just by signing in with the matching email; coach emails carry a persistent "see your team in the portal →" link that doubles as the claim.
- **Assign the head coach + contact per tournament** — so acceptance and notifications route to the actual coach, not whoever filled out the form.
- **Submit a roster for the event** — pick players from the master roster, fill in only what *this* organizer requires (e.g. birthdates / jerseys), submit; the organizer confirms at check-in. **Critical:** organizer requirements apply **only to the event submission** — they never force extra data onto the coach's permanent free master roster (which stays identity-only).

**Organizer (new):** in Event Settings, toggle per-tournament roster requirements (require roster? DOB? jersey? waiver? min/max). Those choices drive and hide exactly what the coach is asked for — no over-collecting.

**Role differences:** organizers author requirements and remain the only ones who record payment/check-in; coaches see their own status and submit their roster + head-coach assignment; the registrant controls who is assigned. Free stays free; every "beyond free" nudge is **express-interest, not a purchase**, and appears **exactly once, after the event** — never as pre-event pressure.

---

## ⚠ Deploy-time prerequisite (caught by the critic — do NOT skip)

> **✅ CLEARED 2026-06-11 — owner confirmed migrations 114–117 + 123 + 124 have been applied to prod.** This deploy gate is satisfied; the historical detail below is retained for context. Re-run `npm run check:migrations` at promote time to re-confirm. (Note: mig 124 = `teams.coach_email`, added by 5l, joined the same gate.)

The C-snapshot source table **`basic_coach_team_players` (+ events/fees/announcements) is dev-only** — migrations **114–117 are ABSENT in prod** (verified against `schema-dump-columns-prod.json`). `tournament_roster_players` exists in prod, but the master it copies *from* does not. **5j adds migration 123** (`tournament_roster_players.source_player_id`, dev-only) whose FK targets `basic_coach_team_players` — so **114 must apply before 123**. **Before any Phase-5 prod deploy:** apply **114, 115, 116, 117, then 123** with `node scripts/apply-migration-api.mjs <file> --prod` (in that order) and confirm `npm run check:migrations` passes. The `/release` agent already gates master/promote on this drift check. This branch currently deploys as a unit, so the risk is low — but it is a hard gate, not optional.

---

## Build tracks (two independent streams + groundwork)

- **Groundwork (serial, first):** 5·0 → 5a → 5b
- **Claim/email track (parallel):** 5c → 5e → 5d  *(hard prerequisite for any WRITE on admin-created/imported teams: 5j, 5l)*
- **Hero/roster track (serial):** 5f, 5g → 5h → 5i → 5j → 5k
- **Then:** 5l (head-coach) → 5m (afterglow + reminder) → 5n (email controls + master off-switch) → **5o (review: public-pages Coaches Portal nav link — closing item)**

**Recommended order:** `5·0 → 5a → 5b → 5c → 5e → 5d → 5f → 5g → 5h → 5i → 5j → 5k → 5l → 5m → 5n → 5o`

---

## Build log

- **5·0 + 5a + 5b (detail status block) BUILT 2026-06-09** (typecheck clean for these files; focused lint clean; awaiting browser verification). 5a+5b were built together — a pure-plumbing 5a isn't browser-verifiable on its own.
  - **Scope calls:** the register-page fee-resolver refactor was **DROPPED** — the coach surface reuses the canonical `getRegistrationAttentionFee` / `computeRegistrationAttentionPaymentStatus`, so the public payment path stays untouched (lower risk, same parity). List + team-home **payment chips deferred** to a small follow-up.
  - **New:** `lib/coaches-status.ts` (consolidated the 3 registration-status maps), `lib/coach-status-model.ts` (`buildCoachTournamentStatus`), `components/coaches/TournamentStatusBlock.tsx` (+ css). Detail page SELECTs widened; status block mounted for accepted teams.
  - **3-lens adversarial review folded in:** fixed numeric-as-string (PostgREST returns `numeric` as strings → `hasSchedule: Boolean('0.00')` mis-fired → explicit-$0 rendered "Owed · $0"; now `Number()`-coerced) and gross-vs-remaining fee (now mirrors the organizer's `getPaymentDue` — deposit-phase vs balance); display timestamps render in `America/Toronto`. **Dismissed** the `today`-timezone finding as a false positive — the organizer page also computes `today` via `new Date().toISOString()` (UTC), so the coach's UTC `today` matches it; switching to Eastern would *create* a divergence.
  - **⚠ Unrelated:** `app/platform-admin/observability/page.tsx` has a typecheck error from the concurrent observability workstream on this branch — not this slice.
  - **Restart required** before browser testing (new files + new shared lib modules).
- **5c (claim-by-email) BUILT 2026-06-10** (typecheck + focused lint clean; awaiting browser verification + dev restart).
  - **What:** discovery (`getClaimableRegistrationsForUser`/`countClaimableRegistrationsForUser` — exact email match via `escapeLike` + JS backstop; excludes rejected + platform-admin), a "Claim your team(s)" prompt on the `/coaches` hub, a routing fix (`buildClaimableContext`) so a context-less admin-added coach lands on the hub, and `registrationId` on the resend-access + registration-confirmation email links. The claim ACTION **reuses the existing `/coaches/join?registrationId=` screen** (no new endpoint; explicit claim per Q3 — no silent auto-link).
  - **3-lens adversarial review folded in:** (HIGH) made `linkTournamentRegistrationToBasicCoachTeam` race-safe — foreign-link pre-check + 23505 handling + orphan-team rollback, and the claim POST maps it to a clean **409 "already claimed"** instead of a 500 + ghost team; (MEDIUM) gated the routing claimable-scan to `contexts.length === 0` so org owners/admins/officials never pay the unindexed `teams.email` scan on `/home`; (MEDIUM) added the platform-admin guard to `/coaches` + a staff early-return in discovery; (MEDIUM) excluded `status='rejected'` registrations.
  - **Deferred (noted, not bugs):** the `/home` "teams to claim" hint only fires for fully context-less users (the hub always shows the prompt, so multi-context coaches still discover claimables there); the discovery query is unindexed — fine at current scale, add a trigram/`lower(email)` index if `teams` grows; **5d** still needed to seed `teams.email` on Add-Teams/import (so the no-email cohort becomes claimable) — discovery only surfaces teams that already have a matching email.
- **5e (coach-email footer + acceptance/rejection/payment polish) BUILT 2026-06-10** (typecheck + focused lint clean; awaiting browser verification + dev restart).
  - **What:** new `coachPortalFooter`/`coachPortalUrl` in `lib/email.ts` → a claim-aware "See your team's schedule, status & updates in your Coaches Portal →" footer added to the portal-less coach emails (payment-confirmation, waitlist, schedule-published, payment-reminder); acceptance button made claim-aware (carries the registration); rejection dead-end → soft "reach out about another division/future event" bridge; payment-confirmation "on the diamond" → multi-sport. `registrationId`+`coachEmail` wired consistently across all 6 send sites (3 accept/reject/payment paths + schedule-publish + payment-reminders + register-waitlist). **Footer stays in coach-template CONTENT, never `wrap()`** — org/billing/founding/auth/league/rep emails untouched.
  - **DEFERRED:** the computed fee-amount + due-date line in the acceptance email (would need fee-select widening across all 3 accept sites; the organizer payment-instructions block already conveys cost + 5b shows the fee in-portal). Clean follow-up that must touch all 3 sites together.
  - **2-lens adversarial review folded in (both PASS, only LOW):** fixed `htmlToText` to decode `&rarr;`/`&larr;` (plain-text emails no longer show a literal `&rarr;`); fixed the already-linked-coach interstitial — `/coaches/join` now auto-redirects to the team when the signed-in coach already has access (new `alreadyLinked` flag on `GET /api/coaches/basic-teams`), removing the mild acceptance-button regression for self-registered coaches. Verified `wrap()` untouched + footer only in the 4 coach templates + all 6 sites consistent.
  - **Restart required** (shared `lib/email.ts`).
- **5d (opt-in claim email on admin add/import) BUILT 2026-06-10** (typecheck + focused lint clean; awaiting browser verification + dev restart).
  - **What:** `manualTeamRegistrationHtml` ("Team Registered" email) is now claim-aware (gains the 5e portal footer w/ registrationId+email). **Add-Teams:** its existing "notify the team" email is claim-aware + a **staff-email guard** (rejects a platform-admin email as a team contact, only when one is provided). **Bulk importer:** a new **opt-in checkbox** (default OFF, shown only when there are new teams) → the commit route accepts `sendPortalEmails`; after the batch is committed, a **best-effort, post-commit** `sendImportPortalEmails` emails each newly-created team that has an email, **skipping no-email + staff-email rows**; response carries `emailsSent` (shown in the dialog).
  - **Email stays OPTIONAL** — no new required fields; no-email teams add/import exactly as before; the import send is opt-in + skips no-email rows.
  - **2-lens adversarial review folded in (both PASS, only LOW):** fixed stale opt-in state (toggle resets per preview / file change). **Deferred (non-exploitable, noted):** the importer STORES a staff email (vs Add-Teams which rejects) — harmless because 5c discovery + the `/coaches` guard + claim-POST all exclude staff; and a broader **email-escaping hardening pass** (team/coach names interpolated raw across ~all transactional templates — pre-existing; do as one consistent pass, not a spot-fix).
  - **Files:** `lib/email.ts`, `lib/import/types.ts`, `app/api/admin/teams/route.ts`, `app/api/admin/tournaments/[tournamentId]/registrations/import/commit/route.ts`, `components/admin/import/TournamentTeamsImportDialog.tsx`.
  - **Restart required** (shared `lib/email.ts`).
- **5f (organizer roster requirements, authoring only) BUILT + BROWSER-VERIFIED 2026-06-10** (typecheck + focused lint + `check:dictionary` clean; dev restarted + smoke 200).
  - **What:** 7 flat keys on `TournamentSettings` (`roster_require`, `roster_require_dob`, `roster_require_jersey`, `roster_require_waiver` bool; `roster_waiver_text` text ≤2000; `roster_min_players`/`roster_max_players` int 1–99 | null) — **all default-OFF/blank** (absent/`false`/`''` = no requirement / default text; ⚠ opposite polarity from `coach_email_*`). New "Roster Requirements" CollapsibleCard in Event Settings (master Event-roster On/Off → reveals 3 sub-requirement On/Off rows + min/max inputs + min>max inline warning; sub-values persist when master toggled Off), full state/hydration/saved-snapshot/auto-save wiring mirroring the existing cards. Patch-settings: 7 keys whitelisted with **explicit per-key sanitizers before the silent fall-through** (strict-boolean for flags; trim + `ROSTER_WAIVER_TEXT_MAX_LENGTH` cap for waiver text; null/`''`-clear + `Number.isInteger` + 1–99 for min/max). Dictionary settings catalog updated (same unit of work). No migration; nothing reads the keys until 5h/5k.
  - **Owner follow-up (same day, after first browser look):** (1) a non-toggleable **"Player names · Always"** row at the top of Required Details (full name always comes from the coach's saved roster); (2) waiver On now reveals a **waiver-statement textarea** — new `roster_waiver_text` settings key; `''`/absent = the shared **`DEFAULT_ROSTER_WAIVER_TEXT`** in new `lib/roster-requirements.ts` (5k imports it for the real checkbox); (3) a live **"What the Coach Will See" preview panel** at the bottom of the card (per-player fields, roster-size sentence applying max-wins, the waiver checkbox + effective text). 2-lens review of the follow-up (4 confirmed→all LOW, 1 refuted) folded in: sanitizer now **trims** waiver text (whitespace-only → `''` → default, so preview = stored = 5k) + uses the shared max-length constant; plan-doc key counts + 5k waiver contract updated (this edit).
  - **3-lens adversarial review (7 agents; 4 confirmed → 2 distinct, 0 refuted) folded in:** (MEDIUM) **min>max is storable** (UI warns but auto-saves; merge-patch validates keys independently so per-request rejection can't close it) → locked + documented the reader rule **min>max = no-minimum (max wins)** in `lib/types.ts`, the dictionary line, and the 5k spec above. (LOW, accepted) opening Event Settings stamps the 6 keys (`false`/null) into legacy tournaments via the pre-existing no-op-save-on-load — behaviorally neutral under all-OFF polarity; readers must key on **values, never key presence**; the real cure (dirty-check in the page's auto-save effect, stops ALL no-op saves) is a pre-existing follow-up, out of 5f scope.
  - **Files:** `lib/types.ts`, `app/api/admin/tournaments/route.ts`, `app/[orgSlug]/admin/tournaments/settings/event/page.tsx`, `docs/agents/db/DATA_DICTIONARY.md`.
  - **Restart required** (shared `lib/types.ts`).
  - **Verify in browser:** Event Settings shows the new card default-Off; toggle On → sub-toggles + min/max appear; set values, wait for auto-save, reload → persists; min>max shows the inline warning but still saves; a legacy tournament reads all-Off.
- **5g (extract shared `TeamHQ` shell, standalone variant) BUILT + BROWSER-VERIFIED 2026-06-10** (typecheck + focused lint clean; dev restarted [both duplicate dev stacks stopped → `.next` cleared → one fresh server] + smoke `/platform-admin/login` 200, no Supabase EACCES; standalone strip browser-verified pixel-identical at all 3 widths).
  - **What:** lifted the verified Phase-4 Team-HQ stat strip into a new shared **server** component `components/coaches/TeamHQ.tsx` (+ co-located `TeamHQ.module.css`) with discriminated-union props — **only the `standalone` variant exists** this slice (5h/5i add `tournament`). The standalone variant renders the EXACT 5-item strip (Roster/Schedule/Fees/Announcements/Tournaments); callers pass already-computed counts/values as props (the component never re-fetches). `formatMoney` (en-CA/CAD/2dp) moved into TeamHQ verbatim; `LocalDateTime` usage unchanged (default `Upcoming` fallback). The hq* CSS (+ all 3 breakpoints: default 5-col → 701–1120px 2-col → ≤700px 1-col) copied byte-identical into `TeamHQ.module.css`, then **removed** from `team.module.css` (kept `.breadcrumb`/`.rosterCount`/`.historyList`/`.historyRow`). Page consumes `<TeamHQ variant="standalone" …/>`; dead `lucide-react` icon + `LocalDateTime` + `formatMoney` references removed from the page.
  - **3-lens adversarial review (CSS verbatim-parity · render/value parity · import-path/webpack-gotcha + dead-code) ALL PASS, zero regressions.** Only finding = 1 LOW (no exhaustiveness guard on the single-member `switch`). Tried a `props satisfies never` guard — it does **not** compile for a single-member union (TS keeps `props` non-`never` in `default`) → reverted + left an inline ⚠ comment telling 5h to add the new `case` **and** the guard once the union has ≥2 members (then it both compiles and enforces).
  - **Zero behavior change** — pure refactor. **⚠ CRITIC CARRY-FORWARD:** re-run this standalone pixel-diff AGAIN after 5h and 5i (they mutate the shared TeamHQ component).
  - **Files:** `components/coaches/TeamHQ.tsx` (new), `components/coaches/TeamHQ.module.css` (new), `app/coaches/team/[basicTeamId]/page.tsx`, `app/coaches/team/[basicTeamId]/team.module.css`.
  - **Restart required** (new shared component file) — done.
  - **Verify in browser:** standalone team home strip pixel-identical at all 3 widths (5-col / 2-col 701–1120px / 1-col ≤700px), correct counts, shipped tokens, no console errors.
- **5h (phase-adaptive tournament hero: Pending / Accepted-Prep) BUILT 2026-06-10** (typecheck + focused lint clean; dev restarted [server stopped → fresh `npm run dev`; owner declined the `.next` clear, smoke 200 confirmed the cache is not stale] + smoke `/platform-admin/login` 200, no EACCES; BROWSER-VERIFIED 2026-06-10 — accepted-prep path confirmed [monogram + "You're in!" + ACCEPTED pill, no duplicate header badge, "First game in 34 days" countdown, Registered/Accepted/Fee checklist with Roster + Check-in correctly absent, status block below; standalone strip unchanged]).
  - **What:** the first `tournament` variant of the shared `TeamHQ` shell (5g). New `lib/coach-tournament-phase.ts` (`deriveCoachTournamentPhase` → `pending|rejected|accepted_prep|schedule_live|game_day|result`; the coach analogue of `lib/tournament-phase.ts`, gated by THEIR acceptance not just the event clock). The tournament detail page derives the phase and **replaces the old static status card** with `<TeamHQ variant="tournament" …/>`; the 5b `TournamentStatusBlock` ("Your status") + Registration Details / Schedule / Announcements sections are untouched. Hero renders: team monogram + faint hue wash (`lib/team-color`), a status pill, a phase headline (Pending → "Registration submitted" + the existing status copy; Rejected → soft "Not selected for this event" + organizer contact bridge; Accepted → "You're in!" + a live **Countdown** "First game in N days" via the public `Countdown` primitive when before start, "Event underway" on game day, "Event complete" after), and a read-only **checklist HUD** wired to the 5a columns (✓ Registered · ✓ Accepted · Fee Paid/Owed · Roster Submitted/Confirmed [positive-state ONLY — no submit prompt; that's 5k] · Check-in [game day only]). **Honest + pitch-free:** NO public link / Follow / pay button / roster prompt / Premium pitch in ANY phase (5i adds the public game-day bridge; 5m the afterglow). Dead `.statusCard/.statusDesc/.contactLine` + the now-redundant header status badge (`.statusBadge`) removed.
  - **4-lens adversarial review (5g no-regression · phase-derivation · honesty/read-only · plumbing/null-safety): 3 PASS + 1 FAIL, folded in.** **5g no-regression = PASS** (standalone strip byte-identical; hero CSS shipped-tokens-only, color-mix toward transparent = dark-safe; exhaustiveness guard now compiles with the 2-member union). Fixed the one **HIGH**: the "Event underway" line was gated on `game_day || schedule_live`, so a **null-startDate** `schedule_live` team showed "Event underway" (because `!beforeStart` is true when there's no date) → re-gated to **`phase === 'game_day'` only** (game_day always has a non-null start; this also removed the dead unreachable `schedule_live` clause that two lenses flagged). Folded the **LOW redundant-status** finding by dropping the duplicate header badge (the hero pill owns status). Null-safety/plumbing PASS (status is null for non-accepted teams and never dereferenced; Countdown only when `beforeStart && startDate`; type-only imports for the model/phase types).
  - **Files:** `lib/coach-tournament-phase.ts` (new), `components/coaches/TeamHQ.tsx`, `components/coaches/TeamHQ.module.css`, `app/coaches/tournaments/[teamId]/page.tsx`, `app/coaches/tournaments/[teamId]/detail.module.css`.
  - **Restart required** (new lib + shared component) — done.
  - **⚠ CARRY-FORWARD:** re-run the 5g standalone pixel-diff AGAIN after 5i (it mutates the shared `TeamHQ` once more). 5i extends the accepted phases (schedule_live/game_day) with opponent names + public game links + live scorebug; 5m adds the result-phase afterglow.
  - **Verify in browser:** pending/waitlist reg → "Registration submitted" hero, NO public links, 2-step strip (Registered ✓ / Awaiting decision); rejected → soft contact bridge; accepted (schedule unpublished, future date) → "You're in!" + countdown + checklist (Fee/Roster states), detailed status block still below; check-in line only from game day; the standalone team home strip still pixel-identical.
  - **DEFERRED (owner, 2026-06-10):** the hero's Fee row ("Owed") and the 5b status block's Fee detail ("Owed · $100 · due …") show the fee twice — intentional glance-vs-detail split, reads fine in the browser. **Do NOT spot-fix** — fold into the **end-of-Phase-5 full coaches-portal design/UX overview** (the combined standalone+tournament pass already deferred to after Phase 5).
- **5i (game-day bridge: opponent names + public deep-links + live scorebug) BUILT 2026-06-10** (typecheck + focused lint clean — 0 project-wide TS errors; dev restarted [single stack stopped; owner-precedent restart WITHOUT `.next` clear] + smoke `/platform-admin/login` **200**, no EACCES; **BROWSER-VERIFIED 2026-06-11**).
  - **What:** new **client island** `components/coaches/CoachLiveSchedule.tsx` (+ co-located `.module.css`), mounted on the tournament detail page **directly under the hero** (replacing the old static Home/Away schedule `<section>`) for accepted teams whose **division** schedule is published. Renders: opponent names + deterministic monograms (`lib/team-color`); each row a `<Link>` to the public game page `/{org}/{tournament}/schedule/{gameId}`; a **live broadcast scorebug** row for the in-progress game (reuses `usePublicTournamentLive` 30s poll [game-day-gated via `coachPhase==='game_day'`, tab-hidden-pause] + `RollingNumber` [no re-fire on unchanged]); W/L/T + final score on completed rows, Cancelled chip, side/time/location meta; a **Follow** toggle (reuses `lib/follow` → SAME `fl_follow_team_{org}_{tournament}` key as the public dock) + `InstallAppPrompt`. Server resolves opponents from the **accepted-teams** set; **single-poll** design (the live game is an inline broadcast row, no second polling surface). Poll merges only volatile fields (scores+status) by game id, so it can't re-introduce an unresolved name. **TeamHQ + the standalone strip UNTOUCHED** — the bridge is a separate component (not a TeamHQ variant), which is why the 5g pixel-diff carry-forward is trivially safe.
  - **Gating (honesty):** bridge mounts only for `accepted` + division `published_teams|published_generic`; public **links/poll/follow additionally** require the tournament to be public (`active|completed`) — else it renders **static** (names + final scores, no links/live/follow). Pending/waitlist/rejected → NO bridge (no public section at all). NULL home/away (TBD bracket) → "TBD". Still pitch-free (Follow/Install are free utilities; afterglow ask = 5m).
  - **4-lens adversarial review (honesty/gating · live-merge/polling · null-safety · no-regression) + adversarial verify of HIGH/MED: 1 HIGH confirmed + 1 LOW folded; 2 LOWs dismissed (reviewer-acknowledged theoretical/cosmetic).** **HIGH fixed:** `scheduleVisible` collapsed `published_teams`+`published_generic`, so a **`published_generic`** division (organizer publishes times but DELIBERATELY HIDES matchups) leaked real opponent names AND deep-linked to a public page that shows those same games as TBD → now mirrors the public `getTeamDisplay`: a real name only under **`published_teams`** + a real assigned id (≠ `NIL_UUID`), otherwise `home/away_placeholder ?? 'TBD'` with a neutral monogram. Added `home_placeholder`/`away_placeholder` to the games SELECT + a `namesPublished` flag. **LOW fixed:** the LIVE broadcast row no longer implies a real "0–0" before a score is posted (RollingNumber coalesces null→0) — shows **"Awaiting score"**. **No-regression = PASS** (TeamHQ git-confirmed out of the changeset → standalone strip byte-identical; section reorder preserves status block / registration details / announcements + the empty/unpublished schedule notes; dead inline-list CSS removed from `detail.module.css`).
  - **Files:** `components/coaches/CoachLiveSchedule.tsx` (new), `components/coaches/CoachLiveSchedule.module.css` (new), `app/coaches/tournaments/[teamId]/page.tsx`, `app/coaches/tournaments/[teamId]/detail.module.css`.
  - **Restart required** (new component files) — done (no `.next` clear; smoke 200 confirmed cache not stale).
  - **⚠ CARRY-FORWARD CLEARED:** 5g standalone pixel-diff re-run after 5i = **PASS** (TeamHQ not in the 5i changeset).
  - **Verify in browser:** `/dev-test-org/live-demo` game day → live broadcast scorebug updates on poll WITHOUT re-firing on unchanged scores; opponent names + working deep-links to the public game page; Follow syncs with the public dock (same key); a **`published_generic`** division shows TBD/placeholder opponents (NOT real names) and the linked page matches; pending/unpublished/rejected show NO bridge; the standalone team-home strip is still pixel-identical at all 3 widths.
  - **BROWSER-VERIFIED 2026-06-11:** live scorebug confirmed rolling on a real score change (3→4) WITHOUT a page refresh + staying still on unchanged polls; `published_teams` (Milton Bats U11) showed real opponent names + monograms + working game-page deep-links + Follow toggle; `published_generic` (denver bobcats U13) correctly showed **TBD** opponents (real teams hidden) with the linked public page matching; graceful "record not found" on a stale/deleted registration id confirmed. Test data via `scripts/setup-5i-milton-live.mjs` + `scripts/add-denver-games.mjs` (+ `bump-milton-score.mjs` for the live roll). ⚠ Re-running `seed-live-tournament.mjs` regenerates all team ids → re-claim + navigate via the UI, not pasted record URLs.
- **As-we-go fix during 5i verification — coaches claim screen (`/coaches/join`) BUILT + VERIFIED 2026-06-11:** removed the reintroduced `btn-primary` gradient + `btn-sm` on the "Existing/New Team" segmented control → `btn-lime` (active) / `btn-ghost` (inactive), per the locked button convention; fixed the unreadable native dropdown with **`select { color-scheme: dark }` globally** (+ a `[data-color-mode="light"] select { color-scheme: light }` guard for public light-mode pages) — a latent bug affecting every dark `<select>`. Files: `app/coaches/join/page.tsx`, `app/globals.css`. **DEFERRED (small 5c polish / holistic coaches review):** when signed in as a NON-matching account, `/coaches/join` still renders the full chooser and only 403s on submit instead of an up-front "this team was registered under a different email" message; and that page shows the public marketing nav (SIGN IN/GET STARTED) rather than a coach-auth-aware nav.
- **5j (C-snapshot submit API — first coach-side roster WRITE) BUILT 2026-06-11** (typecheck **0 errors** + focused lint clean + `check:dictionary` clean; migration 123 applied dev-only + snapshots refreshed [watermark #123] + DRIFT records it; dev restarted [single stack stopped; owner-precedent no-`.next`-clear] + smoke `/platform-admin/login` **200** in 78ms, proxy.ts running, no EACCES; **awaiting browser verification**).
  - **What:** the first coach-side WRITE on the tournament surface. New `app/api/coaches/tournaments/[teamId]/roster/route.ts` — **GET** returns everything 5k needs (parsed `requirements` incl. resolved `waiverText`, the coach's `masterPlayers`, the current `snapshot` rows w/ `sourcePlayerId`, and `state` flags: accepted/locked/tournamentCompleted/canSubmit/submitted+confirmed timestamps); **POST** copies selected master players into `tournament_roster_players` (`source='coach'`, `source_player_id` stamped) and stamps `teams.roster_submitted_at` ONLY. The `[teamId]` path param is a tournament **registration** id (`teams.id`), so auth is the new `requireCoachRegistrationAccess` (explicit-link ownership via the now-exported `findLinkedBasicTeamForRegistration`) — **not** `requireBasicCoachTeamOwner`; the guard also returns the owned `basicCoachTeamId` so the master read is the correct team (IDOR-safe).
  - **Lock ladder (mirrors the organizer gate):** reject when `tournament.status='completed'` OR `team.status!='accepted'` OR `teams.roster_confirmed_at IS NOT NULL` (→ 409 "contact the organizer"). Coach may freely **re-submit while only `roster_submitted_at` is set**; locked once the organizer confirms at the gate (`confirm_roster`/`save_gate_roster`). Re-submit = **delete-then-insert scoped to `.eq('source','coach')`** (preserves any `source='admin'` rows).
  - **Master/snapshot SEAM (structural):** new **pure** `buildTournamentRosterSnapshot` in `lib/basic-coach-roster.ts` — accepts the FULL override surface (per-player jersey/DOB/position/notes; `name` ALWAYS from the master, `position` snapshot-only with no master fallback) + enforces requirements server-side (required DOB/jersey, min/max with the locked **min>max⇒no-minimum** rule, waiver ack, ≥1 player, no dup/foreign id). It **never** calls `updateBasicCoachTeamPlayer` or writes the master — built with the full surface so **5k just calls it** (no API re-touch). New shared `parseRosterRequirements` in `lib/roster-requirements.ts`.
  - **Migration 123 (`tournament_roster_players.source_player_id`, dev-only):** uuid, nullable, **FK → `basic_coach_team_players(id)` ON DELETE SET NULL** (snapshot stays independent of the master; gate/admin rows null). Next-free confirmed against the **live dev DB** (13 cols, no `source_player_id`) + files (max 122). **Joins migrations 114–117 in the Phase-5 prod deploy gate** (`check:migrations`) — 114 must apply before 123 (FK dep). DATA_DICTIONARY (3 "deferred to Phase 5" notes flipped) + snapshots + DRIFT updated, same unit of work.
  - **Owner-confirmed sub-decisions (AskUserQuestion before build):** lockout = lock on confirm / re-submit while only submitted; FK = ON DELETE SET NULL; replace scope = `source='coach'` only.
  - **Adversarial review (ultracode workflow — 5 lenses [auth/IDOR · seam/integrity · requirements · migration/drift · shape/no-regression] → adversarial verify each; 7 agents):** the auth/seam/requirement/migration logic held up — only **1 confirmed finding folded** (the final `teams.update({roster_submitted_at})` wasn't error-checked like the delete+insert above it → a silent stamp failure could return a false success; now destructured + `throw`). The one dismissal argued "mirror the gate's bare update" — rejected: the "mirror the gate" lock is about lock semantics + intentional delete-insert non-atomicity, not replicating a missing error check.
  - **Known limitations (noted, not bugs):** the delete-then-insert is non-atomic (delete OK, insert throws → empty roster + stale timestamp) — same as the organizer gate, acceptable for a single-coach single-team submit; FK TOCTOU (master player deleted between fetch and insert → 500) is rare.
  - **Files:** `supabase/migrations/123_tournament_roster_source_player.sql` (new), `app/api/coaches/tournaments/[teamId]/roster/route.ts` (new), `lib/basic-coach-roster.ts` (+`buildTournamentRosterSnapshot`), `lib/roster-requirements.ts` (+`parseRosterRequirements`), `lib/coach-team-guard.ts` (+`requireCoachRegistrationAccess`), `lib/basic-coach-teams.ts` (exported `findLinkedBasicTeamForRegistration`), `docs/agents/db/DATA_DICTIONARY.md`.
  - **Restart required** (new files + shared libs + migration) — done.
  - **Verify in browser** (`/dev-test-org/live-demo`, sign in as `b2cowan@outlook.com`; first add MASTER-roster players via the team-home RosterEditor at `/coaches/team/[basicTeamId]` so there's something to copy): POST to the new route for a claimed **accepted** team → `tournament_roster_players` rows appear with `source='coach'` + `source_player_id` set + `roster_submitted_at` stamped + `roster_confirmed_at` untouched; re-submit replaces only the coach rows; a POST after the organizer confirms returns 409; a POST to an unclaimed/foreign registration returns 403; the master `basic_coach_team_players` is unchanged. (UI to drive this = 5k.)
- **5k (select-from-master submit UI + requirements wiring — the master/snapshot SEAM in the UI) BUILT + BROWSER-VERIFIED 2026-06-11** (typecheck **0 errors project-wide** + focused lint clean [the concurrent observability rep-teams route errors cleared too]; dev restarted [single stack stopped; owner-precedent no-`.next`-clear] + smoke `/platform-admin/login` **200** in 86ms + `/coaches/tournaments/*` **307** [compiles + redirects unauth], no EACCES; **owner confirmed functionally working as expected 2026-06-11**).
  - **What:** the coach-facing roster submit surface that drives the 5j API. New **client island** `components/coaches/TournamentRosterSubmit.tsx` (+ co-located `.module.css`), mounted on the tournament detail page (under "Your status") **only for accepted teams when a roster is required OR one was already submitted**. On mount it **GETs** the 5j endpoint (requirements + master roster + current snapshot + lock state), pre-selecting the prior submission. The coach **ticks which master players go to this event** (travel-roster friendly), fills **only the organizer-required** DOB/jersey as **per-event overrides**, ticks the waiver if required, and submits → **POST** to the 5j route. After submit, `router.refresh()` flips the server-rendered hero checklist + status block.
  - **TeamHQ wiring:** added an optional `rosterRequired` prop; the tournament Roster checklist row now shows **"Not submitted"** (awaiting clock) when required-but-unsubmitted → **"Submitted"** → **"Confirmed"** (was positive-state-only in 5h). The **standalone variant is byte-identical** (prop added only to the tournament union member; review's no-regression lens confirmed via type-isolation + git-diff). Detail page: `settings` added to the tournament SELECT, `parseRosterRequirements`, the `rosterRequired` prop, and the gated mount.
  - **SEAM (structural, the critical rule):** the UI writes ONLY to the snapshot API — it never calls the master roster endpoint or mutates `basic_coach_team_players`. Required DOB/jersey are snapshot-only overrides; a **NEW** snapshot DOB (changed from the loaded value) fires the same guardian-consent checkbox as the master `RosterEditor`. Client validation mirrors `buildTournamentRosterSnapshot` (min/max via `effectiveMinPlayers` so **min>max⇒no-min**; required DOB/jersey; waiver) — the API stays authoritative.
  - **Adversarial review (ultracode workflow — 4 lenses [seam · requirement-parity · TeamHQ no-regression · state/null-safety] → adversarial verify each; 18 agents; 7 confirmed → 4 distinct → all folded; 7 dismissed correctly confirmed seam-preserved / standalone byte-identical / checklist-correct):**
    1. **(HIGH) DOB consent re-fired on unchanged re-submit** — `newDob` compared to the *master* DOB, so re-opening a roster with a previously-consented override DOB demanded consent again → now tracks **`initialDob`** (the value loaded this session) and re-prompts only when the coach *changes* it (mirrors `RosterEditor`'s `originalDob`); applied to both the validation gate and the UI consent-checkbox visibility.
    2. **(HIGH) waiver reset on re-submit** — pre-tick `waiverAccepted` when `requireWaiver && rosterSubmittedAt` (prior submission already acknowledged; Q7 = no stored document).
    3. **(MEDIUM) position was dead plumbing** — in the Selection type + POST payload but no UI + organizer-assigned at the gate → **removed** from the coach Selection + payload (snapshot `position` stays organizer-controlled; the read-only locked view still displays it).
    4. **(defensive) null-guards** — added `if (!s) continue` / null-filtered the submit map (not actually reachable — `selectedIds` is derived from `sel` — but cheap hardening the reviewers flagged twice).
  - **Honest + pitch-free:** card only for accepted + (required||submitted); pending/rejected → no card; no upsell (afterglow = 5m).
  - **Files:** `components/coaches/TournamentRosterSubmit.tsx` (new), `components/coaches/TournamentRosterSubmit.module.css` (new), `components/coaches/TeamHQ.tsx` (rosterRequired prop + Roster row), `app/coaches/tournaments/[teamId]/page.tsx` (settings SELECT, requirements, mount).
  - **Restart required** (new component files + shared `TeamHQ`) — done.
  - **Verify in browser** (`/dev-test-org/live-demo`, `b2cowan@outlook.com`): on an organizer tournament with Roster Requirements ON (set via Event Settings → Roster Requirements), an accepted claimed team shows the **Your roster** card; ticking players + filling required DOB/jersey + waiver → submit → hero Roster row flips to **Submitted**; a NEW DOB shows the consent checkbox, an unchanged re-submit does **not** re-prompt consent/waiver; min/max blocks out-of-range; the master roster at `/coaches/team/[basicTeamId]` is **unchanged**; once the organizer confirms at check-in the card goes **read-only** ("contact the organizer").
- **5l (head-coach & contact assignment — Part 2) BUILT 2026-06-11** (typecheck **0 errors project-wide** + focused lint clean [only pre-existing `any` warnings on untouched lines] + `check:dictionary` clean; migration **124** `teams.coach_email` applied dev-only + snapshots refreshed [watermark #124] + DRIFT auto-records it; dev restarted [both dev stacks stopped → :3000 clear → fresh `npm run dev`, owner-precedent no-`.next`-clear] + smoke `/platform-admin/login` **200** "✓ Ready in 365ms" no EACCES + new PATCH route **401** unauth; **awaiting browser verification**).
  - **What:** the registrant assigns/changes the team's head-coach **NAME** + an **OPTIONAL** coach contact email per tournament from the Coaches Portal. New client island `components/coaches/HeadCoachEditor.tsx` (+ co-located CSS) mounted in its own "Head coach" section on the tournament detail page (above "Registration Details") for any **non-rejected** team — pending included, so the contact email can route the acceptance email (owner-approved widening of the kickoff's "accepted teams"). New `PATCH /api/coaches/tournaments/[teamId]/route.ts` — the **sibling** of the 5j roster route: same auth (`requireCoachRegistrationAccess` → 401/403), error shape, `withObservability`.
  - **Q2 — NEW `teams.coach_email` (mig 124, text/nullable, dev-only):** next-free confirmed vs files (max 123) AND the live dev DB (no `coach_email`). `teams.email` **STAYS the access/claim key, never overwritten**; recipient sites prefer `coach_email ?? email`. Joins migrations 114–117 + 123 in the Phase-5 prod deploy gate (`check:migrations`). Dictionary anchor `<!-- dict:col:teams.coach_email -->` + line added (column-sealed `teams` — same unit of work); snapshots refreshed; DRIFT lists it under "Only in DEV (2)".
  - **Critic must-fix (`teams.coach` NOT NULL on prod):** the PATCH **rejects an empty name with 400** (validate min-length 1 after trim — chosen over silent coercion; never writes null/''); the editor's Save is also disabled while the name is empty (client backstop). **Mass-assignment safe** — only `coach` + `coach_email` whitelisted. Email optional: regex-validated + lowercased + **staff-email rejected** (mirrors register/add-teams); empty/null **clears** `coach_email` (routing reverts to `teams.email`).
  - **Recipient wiring (shared):** new **`resolveCoachRecipient(team)`** in `lib/email.ts` (trimmed `coach_email` → else `email` → else `''`). Adopted at all **5** post-registration coach-facing send sites — `registrations/[id]` (accept/reject/payment), `admin/teams` bulk loop (accept/reject/payment), the bulk route `sendStatusEmails`, `schedule-publish`, `payment-reminders`. `select('*')` sites pick up `coach_email` automatically; the 3 explicit-SELECT sites had `coach_email` added to the query **and** the `TeamRow` type, and their `!team.email` skip-guards now guard on the **resolved recipient** (with `coachEmail: team.email ?? undefined` to keep the footer arg typed once the narrowing is gone). **The claim-link footer (`coachPortalFooter`) intentionally KEEPS `teams.email`** (recipient ≠ claim arg). **Create-time emails left on `teams.email` by design** (register confirmation/waitlist, manual add-team, import-commit, resend-access — `coach_email` is structurally null then / those carry the claim key).
  - **Q10:** reassignment does **not** notify the organizer (V1). `teams.coach` (name) flows to the existing admin-registrations display + email greetings with no migration; `router.refresh()` after save flips the server-rendered "Coach" line + hero greeting.
  - **Adversarial review (ultracode workflow — 4 lenses [auth/IDOR/write-safety · recipient correctness/no-regression · migration/dictionary/PROD-deploy · UI state/validation] → adversarial verify each High/Med; 4 agents): 0 confirmed High/Med, 1 LOW** (auth/IDOR, never-null `teams.coach`, recipient no-regression, and the PROD-deploy-gate lenses all clean). **LOW** = the post-event **results-finalized** email (`tournamentResultsFinalizedHtml`, `app/api/admin/tournaments/route.ts`) still routes to `teams.email`, NOT `coach_email` — the reviewer itself concluded INTENTIONAL/out-of-scope (mig 124 enumerates exactly the 5 routed types; not a regression; never reads `coach_email`). **Deferred to 5m** (which rewrites that template for the afterglow + adds `coachPortalFooter` — adopt `resolveCoachRecipient` there with the registrationId/email dedupe rule).
  - **⚠ PROD-deploy note (Lens C):** the 3 explicit-SELECT sites now select `coach_email`, which **does not exist on prod until mig 124 lands** → those queries would 400 on prod-without-124 (the `select('*')` sites degrade gracefully). This is exactly the existing Phase-5 deploy gate's job (114–117 + 123 + **124** before this code, `check:migrations`); the branch deploys as a unit. Not new, but now wider.
  - **Known intentional limitation:** when `coach_email` ≠ `teams.email`, the rerouted email's claim-link footer prefills the *registrant* email, so the coach receiving it can't claim with their own email — acceptable (the registrant owns the account/claim; the coach is only notified). Claim/footer improvements are 5m/5o.
  - **Files:** `supabase/migrations/124_team_coach_email.sql` (new), `app/api/coaches/tournaments/[teamId]/route.ts` (new), `components/coaches/HeadCoachEditor.tsx` (new) + `.module.css` (new), `app/coaches/tournaments/[teamId]/page.tsx` (mount + `coach_email` in SELECT), `lib/email.ts` (+`resolveCoachRecipient`), `app/api/registrations/[id]/route.ts`, `app/api/admin/teams/route.ts`, `app/api/admin/tournaments/[tournamentId]/registrations/bulk/route.ts`, `app/api/admin/schedule-publish/route.ts`, `app/api/admin/tournaments/[tournamentId]/registrations/payment-reminders/route.ts`, `docs/agents/db/DATA_DICTIONARY.md`.
  - **Restart required** (new files + shared `lib/email.ts` + migration) — done.
  - **Verify in browser** (`/dev-test-org/live-demo`, sign in as `b2cowan@outlook.com`; Milton Bats U11 / denver bobcats already claimed — re-claim via the UI if the seed was re-run): on a claimed team's tournament detail page the **Head coach** card shows the current name + (empty) contact email; change the name + add a valid email → Save → the "Coach" line in Registration Details + the hero greeting update; an empty name disables Save (API also 400s); an invalid email shows the inline hint + disables Save; clearing the email saves null (routing reverts to the registration email); a staff email is rejected. Then accept/decline/mark-paid the team from admin → the coach email lands at `coach_email` when set (else the registration email); the email's portal/claim footer still uses the registration email.
- **5m (afterglow earned-ask + game-day reminder email) BUILT 2026-06-11** (typecheck **0 errors project-wide** + focused lint clean [only pre-existing `any` warnings on untouched lines]; **NO migration**; dev restarted [dev stack stopped → :3000 clear → fresh `npm run dev`, owner-precedent no-`.next`-clear] + smoke `/platform-admin/login` **200** "✓ Ready in 394ms" no EACCES; **awaiting browser verification**).
  - **In-portal afterglow (result phase ONLY, plan-independent — J5-050):** `TeamHQ` tournament result phase → distinct "That's a wrap!" headline + an afterglow block (final **W-L-T** from completed games + a **"View final standings"** link when public) and the prep checklist **collapses** (J5-052). New afterglow **section** on the detail page (after the schedule/payoff, before "Your status") = `SharePageButton` (team public profile, public-only) + the **single earned ask**: own-team `ScopeCeilingInterest` (real capture, reused verbatim from 4d) + a soft "run your own event" `/pricing` discovery link. Renders for EVERY coach regardless of org plan (gated only by `coachPhase==='result'` + public-tournament for the links). `CoachLiveSchedule` gains `isResult` → retires the install banner + softens the "live scorebug" follow copy post-event (J5-054).
  - **Results-finalized email (the 6th coach send):** `tournamentResultsFinalizedHtml` reframed to a real express-interest afterglow (leads with standings, `coachPortalFooter` added, `billing=annual` dropped from `teamUrl`); the send site routes to **`resolveCoachRecipient`** (`coach_email ?? email`), dedup stays keyed on `teams.email` (claim key → footer correct), passes `registrationId`+`coachEmail`. **Folds the 5l carry-forward** (the deferred recipient/footer item).
  - **Game-day reminder email (Q4):** new transactional `gameDayReminderHtml` (first-game time/venue + opponent honesty-gated by `published_teams` + check-in expectation + `coachPortalUrl` deep link). Scheduled in `schedule-publish` for the **evening before** each accepted team's first game via Resend `scheduled_at` (`sendMarketingEmail` `skipOptOutCheck:true` → the org marketing opt-out can't suppress it). **Best-effort + fully isolated** (try/catch — can never break publishing). **Cancel-then-schedule per team → re-publish idempotent.** New per-recipient **`cancelScheduledEmailForRecipient`** wired into all 3 reject paths (registrations/[id], admin/teams bulk, bulk route — threads orgId through `sendStatusEmails`) so a withdrawn/rejected team's reminder is cancelled. Past-evening-before → skip.
  - **Express-interest repair (owner chose B):** `/coaches/start` **gated** state → a real `CoachStartInterest` capture (new client + co-located CSS; posts to the public `/api/early-access`, `featuresInterested:['coach_portal']`, session-prefilled, works signed-out) instead of a disabled checkout dead-end (J5-051). The **non-gated** `TeamSignupClient` checkout path is untouched. This makes the `/coaches` hub's existing "Express interest" card a genuine capture — **no hub markup change needed** (the card already routes to `/coaches/start`); the 2nd hub card stays a soft `/pricing` discovery link.
  - **Adversarial review (ultracode — 5 lenses [reminder-sched · reminder-cancel · results-email · afterglow-ui · coaches-start] → adversarial verify; ⚠ the first run 529'd ENTIRELY [0 tokens] so it was RE-RUN [8 agents, ~450K tokens] — never trust an all-failed workflow as a clean pass): 1 distinct real bug folded (caught by 2 lenses) + 1 LOW documented.** **(MEDIUM, FIXED) case-sensitivity mismatch** — `resolveCoachRecipient` only `.trim()`'d while the reminder's stored `email_sends.recipient_email` was verbatim and the cancel queried `.toLowerCase()`; a mixed-case address (admin manual-add stores `teams.email` un-lowercased; `coach_email` is always lowercased) → cancel found 0 rows → **duplicate reminders on re-publish + a stale reminder firing to a rejected team**. **Fix: lowercase in `resolveCoachRecipient`** (its output is both the stored recipient_email AND the cancel key → both agree; the footer/claim links use `teams.email` separately so unaffected; a lowercased `to:` is delivery-safe). **(LOW, documented — NOT fixed)** the per-recipient cancel keys on `recipient_email` only (no team id), so two teams sharing one coach address collapse to a single reminder and rejecting one clears it — but the **schedule side ALSO keys per-recipient** (cancel-then-schedule), so the model is consistently per-recipient end-to-end (a shared-coach-address gets ONE reminder). Acceptable for a best-effort transactional nicety; a true per-team reminder would need a team discriminator on `email_sends` (**deferred follow-up**).
  - **Honesty/pitch-free preserved:** the earned ask (afterglow section + ScopeCeilingInterest + /pricing link) is reachable ONLY in the result phase; pending/accepted/game_day stay pitch-free.
  - **Files:** `lib/email.ts` (results-finalized reframe + `gameDayReminderHtml` + `coachPortalUrl` exported + `resolveCoachRecipient` lowercased), `lib/email-sender.ts` (+`cancelScheduledEmailForRecipient` + `COACH_GAME_DAY_REMINDER_EMAIL_KEY`), `app/api/admin/schedule-publish/route.ts` (reminder scheduling), `app/api/admin/tournaments/route.ts` (results-finalized recipient/footer/billing), `app/api/registrations/[id]/route.ts` + `app/api/admin/teams/route.ts` + `app/api/admin/tournaments/[tournamentId]/registrations/bulk/route.ts` (cancel-on-reject), `components/coaches/TeamHQ.tsx` (+`.module.css`), `components/coaches/CoachLiveSchedule.tsx`, `app/coaches/tournaments/[teamId]/page.tsx`, `components/coaches/CoachStartInterest.tsx` (+`.module.css`, new), `app/coaches/start/page.tsx`.
  - **Restart required** (shared `lib/email.ts`/`lib/email-sender.ts` + new component files) — done.
  - **Verify in browser:** (afterglow) a COMPLETED tournament's coach detail page shows "That's a wrap!" + W-L-T + standings link + Share + the express-interest capture + the soft /pricing link, prep checklist gone — renders even for a free-tier org; pending/accepted/game-day show NO ask. (results email) finalize results → email leads with standings, has the portal footer, routes to `coach_email` when set, no billing link. (reminder) publish a schedule with a future first game → a reminder is scheduled; reject a team → its reminder is cancelled; re-publish doesn't duplicate. (gated start) `/coaches/start` shows the express-interest capture (not a disabled form); submitting records an early-access lead.
- **5n (organizer automatic-email controls + master pause-all) BUILT 2026-06-11** (typecheck **0 errors project-wide** + focused lint clean [only the 2 pre-existing `set-state-in-effect` warnings on untouched lines 362/395] + `check:dictionary` clean — 113 tables; **NO migration**; dev restarted [whole stack stopped → :3000 clear → fresh `npm run dev` "✓ Ready in 340ms", owner-precedent no-`.next`-clear] + smoke `/platform-admin/login` **200** in 509ms, proxy.ts running, no EACCES; **awaiting browser verification**).
  - **Reconciliation (the #1 scope call, owner-ruled before build):** the `coach_email_*` gating system AND its Event-Settings UI ("Automatic Coach Emails" sub-section in *Notifications & Contact*) **already existed** for the 4 status emails — so 5n **EXTENDED** it rather than building a parallel system. The plan-doc's "no UI exists / build a `shouldSendAutomaticCoachEmail` helper / add `email_auto_*` keys" notes were stale. Owner rulings (AskUserQuestion): **(1)** the post-event **results** email gets **NO per-type toggle** — it keeps its dedicated "Post-Event Results" / `notify_teams_on_complete` switch (default-OFF, Plus-gated); the master pause-all is layered on top. **(2)** Two **separate** toggles (Schedule published + Game-day reminder), not combined. **(3)** Plan-gating **unchanged** (J5-061 deferred — schedule keeps its `schedule_notification` Plus gate, results keeps `post_tournament_summary`).
  - **Core gating (`lib/email.ts`):** `CoachEmailType` += `'schedule' | 'game_day'`; `coachEmailEnabled` now returns false when `coach_email_pause_all===true` (master short-circuit → all 6 per-type sends inherit it for free); new `coachEmailsPaused(settings)` helper for the results site (which doesn't flow through `coachEmailEnabled`). Master = **NO transactional carve-out** (suppresses acceptance/rejection too — locked).
  - **Send-site gates:** `schedule-publish` (added `settings` to the tournament SELECT) — schedule-published email loop wrapped in `coachEmailEnabled(settings,'schedule')`; game-day reminder gated on `coachEmailEnabled(settings,'game_day')` with the per-team **cancel kept UNCONDITIONAL** (so disable + re-publish *clears* already-scheduled reminders, re-enable reschedules). `admin/tournaments` results loop — early-return `status:'skipped' reason:'paused'` when `coachEmailsPaused(settings)`, layered on the existing `notify_teams_on_complete` gate (`settings` added to `CompletionNotificationTournament` + the completed SELECT). **Payment-reminders / announcements / resend-access intentionally NOT gated** (manual tools).
  - **Settings keys (no migration):** 3 new `tournaments.settings` flat keys — `coach_email_schedule`, `coach_email_game_day` (default ON / absent=enabled, same polarity as the existing 4) + `coach_email_pause_all` (default OFF / absent=not paused — **opposite polarity**, `true` DISABLES). Whitelisted in `ALLOWED_SETTINGS_KEYS` + the strict-boolean sanitizer branch (`app/api/admin/tournaments/route.ts`); declared in `lib/types.ts`; documented in the DATA_DICTIONARY settings catalog (same unit of work).
  - **Event Settings UI:** extended the existing "Automatic Coach Emails" list with **2 new per-type rows** (Schedule published, Game-day reminder) + a divider-separated master **"Pause all automatic emails"** toggle; the per-type list **dims to opacity .5** + a warning note when paused (values persist, honored on un-pause). Wired through **all 10** state-contract spots (useState, saved-init, hydration vars, hydration setters, **hydration `setSaved`**, patch payload, post-save `setSaved`, performSaveRef deps, dirty-check, auto-save deps).
  - **J5-007 fold-in (`RegisterContent.tsx`):** new client `confirmationEmailEnabled = settings.coach_email_pause_all!==true && settings.coach_email_confirmation!==false` (mirrors `coachEmailEnabled(...,'confirmation')` without importing the server email module; `settings` is already client-side — it's read for `show_fees_on_register`). The success "Watch your email" item now shows honest copy ("What happens next" / no inbox promise) when confirmation won't send. **Verified server-side the `confirmation` gate covers BOTH the confirmation receipt AND the waitlist receipt** (`app/api/register/route.ts:504`), so the waitlist branch is accurate too.
  - **Adversarial review (ultracode — 5 lenses [gating · polarity/defaults · reminder cancel/idempotency · Event-Settings wiring no-regression · J5-007 client parity] → per-finding adversarial verify; 7 agents, ~508K tokens — a real pass, not an all-failed 529 run): 1 distinct MEDIUM folded** (caught by 2 lenses) **+ 1 dismissed** (same issue, over-stated as HIGH data-loss → correctly downgraded). **MEDIUM (FIXED):** the **hydration `setSaved` snapshot omitted** the 3 new keys (present in init-default, post-save, dirty-check, deps, payload — but not the on-load snapshot), so a tournament with **non-default** 5n values read state≠saved on load → a **spurious one-time auto-save** (~1.2s after load) re-persisting identical values. **Not data loss** (the payload sends current/correct state). Fixed by adding the 3 keys to the hydration `setSaved`, matching the existing 4 toggles. Gating/polarity/reminder/J5-007 lenses came back clean.
  - **Files:** `lib/email.ts`, `lib/types.ts`, `app/api/admin/tournaments/route.ts`, `app/api/admin/schedule-publish/route.ts`, `app/[orgSlug]/admin/tournaments/settings/event/page.tsx`, `components/public/RegisterContent.tsx`, `docs/agents/db/DATA_DICTIONARY.md`.
  - **Restart required** (shared `lib/email.ts` + `lib/types.ts`) — done.
  - **Verify in browser** (`/dev-test-org/live-demo`, sign in as `b2cowan@outlook.com`; Event Settings → *Notifications & Contact* → Automatic Coach Emails): the 4 existing toggles + 2 new (Schedule published, Game-day reminder) + master **Pause all**; toggle one Off, save, reload → persists (and **no spurious save on load**); turn **Pause all On** → per-type rows dim + warning note; with Pause all On, no coach emails send (accept a team → no acceptance email; publish a schedule → no schedule/reminder; finalize results → no results email). Turn a single per-type off (e.g. Schedule published) → publishing sends no schedule email but acceptance still sends. Game-day reminder Off + re-publish → previously-scheduled reminders are cancelled. On the public **register success screen**, set `coach_email_confirmation` Off (or Pause all On) → the "Watch your email" step becomes "What happens next" with no inbox promise.

## Slices

### 5·0 — Pressure-ladder cleanup (pulled EARLY per critic) · low · ✅ BUILT 2026-06-09 (lint clean; awaiting browser verification)
**Goal:** remove the always-on pre-event Premium CTAs so no incremental deploy ever violates pitch-free. The afterglow *ask* is added later in 5m; this slice only **removes**.
**Built:** removed the detail-page `ctaSection` (Premium "Take your team further" + "Run your own tournament") and the list-page `CtaCards` (both the empty-state and bottom positions) + the now-dead `hasPremiumAccess`/`getUserAccessContexts` plumbing in both files; left a comment pointing to the 5m afterglow ask. No shared modules / no new files → no dev restart. `lint:focused` clean.
- Detail page Premium CTA (`app/coaches/tournaments/[teamId]/page.tsx` ~L272); list 2-card CtaCards grid (`app/coaches/tournaments/page.tsx` ~L250–273).
- **Why early:** the critic flagged that shipping 5b/5h/5i before 5m leaves these live on pre-event surfaces. Decoupling removes the rule violation from slice one.
- **Verify:** pre-event coach surfaces show no Premium pitch; no dead imports left.

### 5a — Shared status + fee-resolver foundation (data plumbing, no UI) · medium
**Goal:** the read foundation everything consumes.
- Widen the coach tournament SELECTs to carry `payment_status, payment_collected_at, check_in_status, checked_in_at, roster_submitted_at, roster_confirmed_at` (+ tournament `settings`/fee columns) — `app/coaches/tournaments/[teamId]/page.tsx`, `lib/basic-coach-teams.ts` (`getBasicCoachTournamentTeamsForUser`, `getBasicCoachTournamentHistoryForTeam`, the registration mapper).
- New `lib/coaches-status.ts` — consolidate the 3 duplicated `STATUS_*` maps. **⚠ fix:** the three are NOT identical (detail has full-sentence `STATUS_DESC`; others are label/badge only) — preserve all three shapes.
- New `lib/fee-schedule.ts` — promote the register page's `resolveFeeSchedule` + `getRegistrationAttentionFee` into one resolver; repoint `lib/registration-attention.ts` + the register page.
- **⚠ fix:** `payment_status` is NOT NULL on dev but NULLABLE on prod, and `lib/db.ts` coerces missing→`'paid'` while the gate coerces→`'pending'`. The mapper must coerce NULL→`'pending'` to match the gate (else a never-paid team reads as Paid).
- **Verify:** `npm run typecheck`; register page fee/deposit/due unchanged; coach detail renders unchanged with the new columns in the SSR payload.

### 5b — Generalized status block on the coach record (B) · medium
**Goal:** show the coach their payment + check-in + roster *status*, read-only, no pay button.
- New `lib/coach-status-model.ts` (normalizes organizer-row OR coach-ledger into one view-model with an `editable` discriminator); new `components/coaches/TournamentStatusBlock.tsx`; mount on detail; compact payment chip on history rows.
- Reuse `computeRegistrationAttentionPaymentStatus` (organizer branch), `FeeEditor` totals (coach branch), `CheckInBoard` STATUS_META labels, register `PaymentPanel` copy.
- **⚠ fix (critic):** keep 5b **read-only status display only** — do NOT render any requirement-driven roster/DOB/jersey checklist row here (that's owned by 5h/5k after 5f exists). Otherwise it would prompt "submit roster" even when the organizer set require_roster=false.
- **⚠ fix:** gate the check-in line to **on/after game day** (default `not_arrived` would otherwise read as a problem all season). [open Q: confirm]
- **Verify:** linked accepted reg shows Fee (Owed+due / Paid date) + game-day check-in; no pay button; NULL-payment (prod-shape) reads pending.

### 5c — Claim-by-email discovery + registrationId-in-links (the GAP) · medium
**Goal:** close the empty-portal gap; make `/coaches/join` work without a registrationId; append registrationId to coach join links.
- New `getClaimableRegistrationsForUser(userId,email)` (exact-normalized `teams.email` match, anti-joined against `basic_coach_team_registrations`); add email-match fallback to `canUserAccessTournamentRegistration` + `getBasicCoachTournamentTeamsForUser` + `buildTournamentRegistrationContext`.
- `/coaches/join` claim-mode (list discovered regs / handle the N>1 multi-team case); `GET /api/coaches/basic-teams` returns the claimable list; append `registrationId` to resend-access + register-confirmation + access-reminder links.
- Reuse `linkTournamentRegistrationToBasicCoachTeam` (verbatim — ownership + exact-email + idempotent) + `getPendingTournamentRegistrationForUser`.
- **⚠ fix (critic):** delivers near-zero value before 5d (the empty-`teams.email` cohort) — verify 5c with a team that *has* an email. `UNIQUE(tournament_team_id)` means first-claim-wins when two accounts share an email string → must be a clear UX state, not a 500.
- **Security:** [open Q] explicit user-initiated **Claim** vs silent auto-link, given email verification is deferred to Phase 8. Exact normalized equality (not ILIKE); claim is read+link only (no payment).
- **Verify:** admin team with `teams.email` = test coach email → coach signs in → `/coaches/join` lists + claims it → detail reachable; empty-email team does NOT appear.

### 5d — Optional claim-email on admin add/import (NOT a teams.email requirement) · medium · depends 5c, 5e
**Goal:** make the claim path reliable for admins who DO want their coaches in the portal — **without ever requiring coach name/email** for admins who don't.
- **DECISION (owner, 2026-06-10): email stays OPTIONAL — never hard-required.** Confirmed current behavior: Add-Teams requires only name + division + tournament; `teams.coach`/`teams.email` are optional (stored as `''`); email is required only when the existing "notify the team" toggle is on; the importer maps email only if the CSV has the column. We **keep all of this**. A no-email team is simply **not claimable-by-email** — a normal, documented state, not an error (the organizer manages it directly; 5c's discovery just doesn't surface it).
- So 5d does NOT touch the Add-Teams/import *requirements*. It only adds a **send portal-access / claim email** path:
  - A new **opt-in toggle (default off)** on Add-Teams (reuses the `notifyTeam` pattern) + per-import in the importer commit, that — when the row HAS an email — sends a claim email (carrying `registrationId` via the 5e `coachPortalFooter()`), so admins who want to invite coaches can, in one step.
  - When email is blank, the toggle is a no-op (nothing to send) — no validation error, no forced email.
- Reuse the existing notifyTeam toggle + `coachPortalFooter()` (from 5e) + `getOrgOwnerEmail` fallback.
- **Defense-in-depth (from the 5c review):** also reject `isPlatformAdminEmail` as a team contact in Add-Teams + import commit (mirrors the register route) so a staff email never lands in `teams.email`.
- **Verify:** Add-Teams with the claim toggle off + no email → team created, no email, not claimable (expected). Add-Teams with an email + toggle on → coach gets a claim email landing on `/coaches/join` pre-selected. CSV import with an Email column + opt-in → each coach with an email gets a claim email; rows without email import fine and silently.

### 5e — Shared coach-email footer CTA + status-email polish · medium
**Goal:** one shared coach-email footer ("see your team in the portal →" carrying registrationId), enrich acceptance, add a rejection soft bridge, fix the voice nit.
- New `coachPortalFooter({registrationId,email,next})` appended **inside coach-facing template content only — NEVER in the shared `wrap()`** (wrap is shared with org/billing/founding/auth/league/rep). Enrich `acceptanceHtml` with fee+due; soft express-interest bridge in `rejectionHtml`; remove "on the diamond" from `paymentConfirmationHtml`.
- **⚠ fix:** the accept/reject/payment trigger is **duplicated across 3 files** (`api/registrations/[id]`, `.../registrations/bulk`, `api/admin/teams`) — every template-arg change must land in all three.
- **Copy owned by `/marketing`** — this slice defines the slots; coordinate exact words.
- **Verify:** trigger accept/reject/payment from all 3 admin paths; every coach email carries the footer; acceptance shows fee+due; rejection shows the bridge; click footer → claimable `/coaches/join`.

### 5f — Organizer roster requirements in settings JSONB (authoring only) · medium · ✅ BUILT + BROWSER-VERIFIED 2026-06-10 (see build log)
**Goal:** organizers author per-tournament requirements; no migration.
- Flat keys in `lib/types.ts` TournamentSettings: `roster_require, roster_require_dob, roster_require_jersey, roster_require_waiver, roster_waiver_text, roster_min_players, roster_max_players`.
- "Roster Requirements" CollapsibleCard in Event Settings (segmented On/Off + min/max number inputs); whitelist in `ALLOWED_SETTINGS_KEYS` + sanitizers in `app/api/admin/tournaments/route.ts`.
- **⚠ fix (critic):** the patch-settings merge has a **silent fall-through** (`sanitized[k]=v`) that persists any whitelisted key with no type check — each of the 6 keys needs an explicit sanitizer branch (bool for flags; `Number.isInteger`+range+null-clear for min/max) **before** the fall-through.
- **Defaults MUST be all-OFF** (legacy tournaments unaffected; do NOT copy the scope keys' "null blocks activation" semantics). Update `DATA_DICTIONARY.md` same unit of work (`check:dictionary`).
- **[open Q]** flat keys vs nested object. Recommend flat (per-division override is deferred).
- **Verify:** toggle + min/max, save, reload → persists; `typecheck` + `check:dictionary`; legacy tournament reads all-OFF.

### 5g — Extract shared Team-HQ shell, standalone mode (D, zero behavior change) · low · ✅ BUILT + BROWSER-VERIFIED 2026-06-10 (see build log)
**Goal:** lift the verified Phase-4 `hqStrip` into one shared source-agnostic `TeamHQ` component; swap the standalone page to it with pixel/token parity. De-risks the no-regression rule before any tournament wiring.
- New `components/coaches/TeamHQ.tsx` (discriminated-union props; `standalone` variant = the exact 5-item strip); consume on `app/coaches/team/[basicTeamId]/page.tsx` (counts passed as props).
- **⚠ fix (critic):** re-run the pixel-diff no-regression check **again after 5h/5i** (they mutate the same shared component).
- **Verify:** standalone strip pixel-identical (5-col → 2-col 701–1120px → 1-col), correct counts, shipped tokens, no console errors.

### 5h — Phase-adaptive tournament hero: Pending / Accepted-Prep (D static) · medium · depends 5a,5b,5g · ✅ BUILT + BROWSER-VERIFIED 2026-06-10 (see build log)
**Goal:** render `TeamHQ` in `tournament` mode; derive Pending / Accepted-Prep from `teams.status` + `schedule_visibility` + dates; checklist HUD wired to 5a columns; replace the static status card. No live polling, no public links yet.
- Add `tournament` mode + phase derivation; mount on detail (copy the server-side in-progress derivation from `[orgSlug]/[tournamentSlug]/layout.tsx`, don't import the client follow module). Reuse `Countdown`, `lib/team-color`.
- **⚠ HONESTY fix:** pending/waitlist/rejected teams have NO public profile (`lib/public-tournament-data.ts` filters to accepted) — Pending renders **no Follow button, no public link**. Accepted-but-unpublished-division sits in Accepted-Prep, not Schedule-live.
- **Verify:** pending + accepted-unpublished regs show correct phase + checklist + Countdown, NO public links; old static card gone.

### 5i — Game-day bridge: opponent names, public deep links, live scorebug (D live) · high · depends 5h · ✅ BUILT 2026-06-10 (see build log)
**Goal:** opponent names + monograms, schedule rows → public game links, Schedule-live/Game-day/Result phases with Countdown + live scorebug, strictly-gated Follow/alerts(express-interest)/Install.
**Built (decisions vs scope):** delivered as a standalone client island `CoachLiveSchedule` (NOT a TeamHQ variant — TeamHQ is a server component; this keeps the standalone pixel-diff trivially safe) mounted under the hero; **single-poll** (live game = inline broadcast row). **D2 (owner-approved):** Follow + Install + live scorebug ship now; **alerts express-interest DEFERRED to 5m** (kept 5i pitch-free). `Countdown` stays in the hero (5h). **Critic HIGH folded:** `published_generic` must show placeholder/TBD (not real names) — division-level visibility now distinguishes `published_teams` vs `published_generic`, mirroring the public `getTeamDisplay`.
- Server-resolve opponent names (teams-by-tournament lookup); rows → `<Link>` to `/{orgSlug}/{tournamentSlug}/schedule/{gameId}`; live scorebug via `usePublicTournamentLive` + `RollingNumber`.
- Reuse `RollingNumber`, `Countdown`, `lib/follow` (same localStorage key as the public dock), `InstallAppPrompt`. **Do NOT mount `ScoreTicker`/`MyTeamDock`** (they need `OrgNavContext`/`useParams`/accepted-only payload — they'll fail in the coach shell).
- **⚠ fix (critic):** handle NULL `home_team_id`/`away_team_id` (TBD bracket games) → "Home/Away/TBD" fallback; opponent-name resolution must respect the honesty rule (resolve from the published schedule set). **Division-level** `schedule_visibility` (not just tournament-level) must gate the bridge.
- **Verify:** `/dev-test-org/live-demo` game day → live scorebug updates on poll without re-firing on unchanged scores; opponent names + game links; Follow stays in sync with the public dock; bridge absent for non-public teams.

### 5j — C-snapshot submit API (C) · high · depends 5c · ✅ BUILT 2026-06-11 (see build log)
**Goal:** first coach-side roster write — copy selected master players into `tournament_roster_players`, stamp `roster_submitted_at`.
**Built:** new `app/api/coaches/tournaments/[teamId]/roster/route.ts` (GET serves 5k's data; POST writes the snapshot) gated by the new `requireCoachRegistrationAccess`; pure `buildTournamentRosterSnapshot` normalizer (the seam — never mutates the master); `parseRosterRequirements`; migration 123 adds `source_player_id` (FK ON DELETE SET NULL, dev-only). Lock ladder mirrors the organizer gate; re-submit replaces only `source='coach'` rows. Adversarial review (5 lenses + verify): 1 fix folded (error-check the timestamp stamp). **Owner-confirmed sub-decisions:** lock-on-confirm, FK ON DELETE SET NULL, replace `source='coach'` only.
- New `app/api/coaches/tournaments/[teamId]/roster/route.ts` (auth via a `canUserAccessTournamentRegistration` wrapper — **NOT** `requireBasicCoachTeamOwner`; the path param is a tournament `teams.id`, not a basic team id). Clone the gate's delete-then-insert with `source='coach'`; reject writes once `roster_confirmed_at` is set.
- Separate snapshot normalizer in `lib/basic-coach-roster.ts` that accepts position + tournament-required DOB/jersey overrides **and must NOT call `updateBasicCoachTeamPlayer`** (the seam).
- **⚠ fix (critic):** define the **post-gate-confirm coach state** (recommend: read-only view of the confirmed snapshot + "contact organizer to change"). Note `save_gate_roster` stamps both submitted+confirmed while `confirm_roster` stamps only confirmed — different lockout behavior. Build the normalizer with the override-field surface from the start so 5k doesn't re-touch the API.
- **[open Q]** add `tournament_roster_players.source_player_id` (tiny migration → provenance/idempotent re-submit) vs keep delete-then-insert (no migration). Recommend **add it** (dictionary deferred it to Phase 5; master built snapshot-compatible for this).
- **Verify:** linked accepted coach POST → rows with `source='coach'`, `roster_submitted_at` stamped, `roster_confirmed_at` untouched; re-submit replaces; post-confirm POST rejected; `typecheck`.

### 5k — C-snapshot select-from-master UI + requirements wiring · high · depends 5f,5j,5h · ✅ BUILT + BROWSER-VERIFIED 2026-06-11 (see build log)
**Goal:** the coach roster-submit island (select-from-master, travel-roster friendly) that reads 5f requirements to drive+hide DOB/jersey/waiver + enforce min/max, writing per-snapshot overrides **without ever touching the master**.
**Built:** `components/coaches/TournamentRosterSubmit.tsx` (client island, GETs the 5j API, select-from-master + per-event DOB/jersey overrides + waiver + min/max, POSTs, `router.refresh()`); `TeamHQ` gains `rosterRequired` → Roster checklist row "Not submitted"→"Submitted"→"Confirmed" (standalone byte-identical); detail page mounts it for accepted teams when required||submitted. SEAM enforced (snapshot API only; new-DOB consent gate). 4-lens review (18 agents) → 4 fixes folded: initialDob-based consent (no re-prompt on unchanged re-submit), waiver pre-tick on prior submission, removed dead `position` plumbing, defensive null-guards. **Q7 (waiver = checkbox-only) was already locked.**
- New `components/coaches/TournamentRosterSubmit.tsx` on the accepted block; pass `tournaments.settings` requirements; `TeamHQ` checklist reads requirements to render/hide the Roster row + flip on `roster_submitted_at`.
- Reuse `RosterEditor` editor + **its DOB consent gate (must fire for snapshot DOB too)**; `CheckInBoard` Submitted/Confirmed/None ladder (coach-worded).
- **⚠ CRITICAL SEAM:** "require DOB" prompts for the **snapshot copy only** — never writes back to `basic_coach_team_players`. The master editor must NOT read `tournaments.settings`; forbid the 5k UI from reusing the master roster save endpoint for any field. min/max enforced app-layer (API + inline).
- **⚠ min>max rule (from the 5f review):** a `roster_min_players > roster_max_players` pair IS storable (Event Settings warns but auto-saves; the merge-patch API validates each key independently, and a single-key patch can create the pair against a stored value). 5k MUST treat min>max as **no-minimum (max wins)** — never an unsatisfiable gate that blocks every submission. Documented in `lib/types.ts` + the dictionary settings catalog.
- **Waiver checkbox label (5f follow-up):** render the organizer's stored `settings.roster_waiver_text` (already trimmed at write) as the agreement statement; when `''`/absent, fall back to **`DEFAULT_ROSTER_WAIVER_TEXT` from `lib/roster-requirements.ts`** — never hardcode the wording. The organizer's Event-Settings preview shows exactly this `text || default` rule, so 5k must match it.
- **[open Q]** "require waiver" = checkbox-at-submit acknowledgment (no storage, no migration) vs persisted. Recommend checkbox-only V1.
- **Verify:** DOB/jersey appear only when required; snapshot DOB written but `basic_coach_team_players` UNCHANGED; min/max blocks out-of-range; checklist Roster row flips on submit.

### 5l — Head-coach & contact assignment (Part 2) · medium · depends 5c · ✅ BUILT 2026-06-11 (see build log)
**Goal:** the registrant assigns/changes the team's head-coach name (+ optional coach contact email) per tournament; writes `teams.coach`.
**Built:** `HeadCoachEditor` island + `PATCH /api/coaches/tournaments/[teamId]` (5j-sibling auth); mig 124 `teams.coach_email` (dev-only, `teams.email` untouched as the claim key); `resolveCoachRecipient` (`coach_email ?? email`) at the 5 post-registration coach-email sites; empty name → 400 (never null/''); staff-email rejected; footer keeps `teams.email`. Review: 0 High/Med, 1 LOW → results-finalized email recipient deferred to 5m.
- New `components/coaches/HeadCoachEditor.tsx` (RosterEditor mold) + `PATCH /api/coaches/tournaments/[teamId]/route.ts` (owner-gated via `canUserAccessTournamentRegistration`).
- **⚠ fix (critic):** `teams.coach` is **NOT NULL on prod** (nullable on dev) — the PATCH must never write null/`''`; coerce empty → existing value or registrant name, validate min-length 1.
- **[open Q]** contact email: **Option A** overwrite `teams.email` (no migration, but `teams.email` is ALSO the portal access/claim key → overwriting orphans the registrant's access) vs **Option B** new nullable `teams.coach_email` column (migration; recipient sites prefer `coach_email ?? email`). Recommend **Option B** (only clean way to separate "who runs the account" from "who coaches"). If B: this touches the same 3 accept/reject files as 5e — **sequence after 5e or batch the edits**.
- **[open Q]** does reassignment notify the organizer?
- **Verify:** edit head-coach name persists to admin registrations + email greetings; if B, coach email reroutes acceptance while registrant still reaches the portal; `typecheck` (+ `check:dictionary`/snapshot refresh if B).

### 5m — Afterglow earned-ask + game-day reminder email (E) · high · depends 5e,5i · ✅ BUILT 2026-06-11 (see build log)
**Goal:** the single earned ask + the game-day reminder. (CTA-removal already done in 5·0 — this slice only ADDS.)
**Built:** result-phase afterglow (TeamHQ "That's a wrap!" + W-L-T + standings link + collapsed checklist; detail-page section = Share + own-team `ScopeCeilingInterest` real capture + soft /pricing bridge; plan-independent); reframed `tournamentResultsFinalizedHtml` (express-interest + footer + `resolveCoachRecipient`, drops billing — folds the 5l carry-forward); transactional `gameDayReminderHtml` scheduled evening-before via Resend `scheduled_at` (best-effort/isolated, cancel-then-schedule idempotent) + per-recipient `cancelScheduledEmailForRecipient` on the 3 reject paths; `/coaches/start` gated → real `CoachStartInterest` capture (J5-051, makes the hub "Express interest" card genuine). NO migration. Review (re-run after a full 529): 1 MEDIUM folded (lowercase `resolveCoachRecipient` — casing mismatch broke reminder cancel) + 1 LOW documented (shared-coach-address reminders collapse per-recipient).
- `TeamHQ` Result phase: two **express-interest** bridges (own-team → Coaches Portal; org → advocacy ask), gated to completed events; `SharePageButton` only when a public profile exists.
- Extend `tournamentResultsFinalizedHtml` to a true two-bridge afterglow + **reframe the checkout-flavored `/coaches/start?billing=annual` link to express-interest** (both in-portal and in the email — the email link is the more checkout-flavored one). New game-day reminder template.
- **⚠ CARRY-FORWARD from 5l (the review's 1 LOW):** the results-finalized email is the **6th coach-facing automatic send** but still routes to `teams.email` — when 5m adds the `coachPortalFooter` here, also switch its recipient to **`resolveCoachRecipient(team)`** (add `coach_email` to that route's team SELECT) so it matches the other 5 sites; keep the email/dedupe key on `teams.email` so the claim/upsell links stay correct (cf. J5-059's registrationId pick rule).
- **⚠ fix (critic):** game-day reminder is **transactional** — route via `lib/email.ts sendEmail` (no opt-out, no marketing footer) OR if using `email-sender.ts` `scheduled_at`, pass `skipOptOutCheck=true` so the **organizer's marketing opt-out can't suppress** it; orgId = `tournament.org_id`.
- **[open Q]** reminder firing: **Resend `scheduled_at`** at publish time (zero infra; can't auto-re-target if game times change) vs a new pull endpoint + external scheduler (rep-teams precedent). Recommend Resend `scheduled_at` for V1.
- **Copy owned by `/marketing`.**
- **Verify:** completed event shows the two-bridge express-interest block; pre-event shows none; finalize results → extended email w/ express-interest link (not checkout); reminder fires with no marketing footer.

### 5n — Organizer automatic-email controls + master off-switch (NEW, owner-requested) · medium · depends 5e, 5m
**Goal (Q11 = SENDER / organizer, locked 2026-06-09):** the tournament admin controls which automatic coach-facing emails THEIR event sends — an individual off-toggle per email type plus a single "pause all automatic emails" master switch. Tournament-scoped, authored in Event Settings, **mirrors the 5f roster-requirements settings-JSONB pattern (no migration)**.
- **Emails gated (organizer→coach, automatic):** registration confirmation, acceptance, rejection, payment confirmation, schedule-published, **game-day reminder (5m)**, results-finalized/afterglow.
- **Storage:** flat keys in `tournaments.settings` JSONB (no migration) — `email_auto_pause_all` + per-type `email_auto_acceptance`/`_rejection`/`_payment`/`_schedule_published`/`_game_day_reminder`/`_results` (**default ON = current behavior**). Reuses the 5f `ALLOWED_SETTINGS_KEYS` + per-key boolean sanitizer machinery 1:1.
- **UI:** an "Automatic Emails" CollapsibleCard in Event Settings (per-type segmented On/Off + a master "Pause all automatic emails" toggle), reusing the 5f Event-Settings primitives + `notification-labels`-style labels/descriptions.
- **Gate:** a single helper `shouldSendAutomaticCoachEmail(tournamentSettings, type)` checked at each send site (the routes already have tournament context to read `tournaments.settings`); master pause short-circuits all. 5m's reminder + the 5e-touched sends consult it.
- **Master = all off:** the organizer is explicitly choosing to handle comms manually, so "pause all" suppresses every automatic coach email (individual toggles give granularity to keep e.g. acceptance on while muting reminders). **No transactional carve-out** — the organizer owns the consequence. *(Revisit only if a coach-missed-acceptance complaint surfaces.)*
- **NOT in scope:** recipient/coach-side per-user opt-out + no-account token opt-out (that was the "recipient" path, not chosen). The org-level CASL **marketing** unsubscribe (migration 099) stays independent of this transactional-email control.
- **Reuse:** the 5f settings-JSONB authoring chain, `notification-labels.ts` copy pattern, the Event Settings CollapsibleCard/segmented primitives.
- **Verify:** toggle a type off in Event Settings → that email no longer sends for that tournament; master pause suppresses all; defaults (no keys set) = every email still sends; `typecheck` + `check:dictionary` (new settings keys documented).

### 5o — REVIEW: public-pages "Coaches Portal" nav link (owner-requested 2026-06-09, FINAL item) · review/spike
> **⛔ DEFERRED to Phase 8 (owner-decided 2026-06-11) → PHASE 5 COMPLETE (15/16 built, 5o deferred).** The public-pages "Coaches Portal" nav link is built ONCE alongside the Phase-8 email-verification + "verify → portal" auto-land work (so it isn't a throwaway pre-auto-land widget). Interim reachability — the register success-screen "Open Coaches Portal" button + the emailed claim-link footers (`coachPortalFooter`) — is deemed sufficient until then. The J5-004 "land every registrationId-bearing entry point team-deep + collapse the 3 hand-rolled URL builders into `coachPortalUrl`" finding (below) rides into the same Phase-8 unit.
**Goal:** evaluate the options for surfacing a "Coaches Portal" link in the **public-facing** navigation so coaches can find / return to their portal. Deliverable is an **options memo + recommendation** — not an assumed build (a follow-up build slice only if approved).
- **WHERE:** marketing-site nav (`components/Navbar`) vs public **tournament** nav (desktop `TournamentSideRail` + mobile top/bottom nav) vs both.
- **WHEN:** always-shown vs **session-aware** (only when signed-in, or only when the user actually has a coach context via `getUserAccessContexts`).
- **SIGNED-OUT behavior:** link to `/coaches/join` or `/auth/login?next=/coaches` vs hide entirely.
- **Reconcile with Phase 8** ([FREE_TIER_COACHES_UNIFIED_PLAN.md](FREE_TIER_COACHES_UNIFIED_PLAN.md) Phase 8 already proposes a session-aware "Coaches Portal" link in the public tournament nav for *post-register reachability* / the "stranded after a secondary success-screen link" gap) — **build it once**; this review may absorb or scope that item.
- **Angle:** discoverability + growth (returning coaches browsing public pages) and the post-register reachability safety net.
- **Depends on:** nothing hard; best reviewed after the portal surfaces (5b–5m) exist so the link lands somewhere meaningful. **This is the project's closing item.**

---

## Cross-cutting decisions (locked by the synthesis)

1. Build read/visibility foundation (5·0/5a/5b) before any hero/roster UI; extract the shared shell (5g) before any tournament hero variant (5h/5i).
2. Claim track (5c→5e→5d) is parallel but a **hard prerequisite for any coach-side WRITE on admin-created/imported teams** (5j, 5l) — `canUserAccessTournamentRegistration` returns null pre-claim, so for imported teams **every** Phase-5 surface (read AND write) is gated behind a completed claim. "Visibility works immediately" is true only for self-registered/already-linked teams.
3. The only two relational-schema decisions are isolated + gated on owner input: `tournament_roster_players.source_player_id` (5j) and `teams.coach_email` (5l). Everything else is settings-JSONB or pure UI.
4. Organizer requirements = flat settings keys for V1 (per-division override deferred → would justify a nested object).
5. Master/snapshot seam enforced structurally: only the C-snapshot copy reads `tournaments.settings`; the master editor never does; "require DOB" prompts only the snapshot row.
6. Pressure ladder is cross-cutting: pre-event stays pitch-free (5·0 removes the live CTAs first), the single earned ask is the afterglow (5m).
7. Reuse public primitives as clean drop-ins only (`RollingNumber`, `Countdown`, public `CountUp`, `usePublicTournamentLive`, `team-color`, `follow`, `InstallAppPrompt`, `SharePageButton`); never mount `ScoreTicker`/`MyTeamDock` in the coach shell.
8. Email footer goes in coach-template content, never `wrap()`; accept/reject/payment edits land in all 3 trigger files.

## Decisions

**Locked 2026-06-09 (owner via AskUserQuestion):**
- **Q1 — `tournament_roster_players.source_player_id`: ADD IT** (5j carries a small migration → provenance + idempotent re-submit + "N of M submitted" UX).
- **Q2 — coach contact email: NEW `teams.coach_email` column** (5l carries a migration; recipient sites prefer `coach_email ?? email`; `teams.email` stays the portal access/claim key).
- **Q3 — claim posture: EXPLICIT Claim click** (5c; no silent auto-link until Phase 8 email verification; exact normalized equality; claim = read+link only).
- **Q4 — game-day reminder: Resend `scheduled_at`** at publish time (5m; `cancelScheduledEmail` on withdrawal).
- **➕ NEW (owner-requested): automatic-email opt-out + master switch → new slice 5n.** Every automatic coach email gets an individual off-toggle plus a single "pause all automatic emails" master toggle (modeled on the per-user notification preferences). 5m's reminder + all 5e-touched coach emails are gated on it. **Control-owner = open (see below).**

**Owner's call still open:**
- **Q11 — who controls the 5n email toggles:** recipient (coach) vs organizer (sender) vs both. *(asking now)*

**Taking the recommended default unless you object:**
- Q5 coach fee display = **binary** Paid/Owes (parity with the organizer gate).
- Q6 importer/Add-Teams claim email = **opt-in** (default off).
- Q7 "require waiver" = **checkbox-only** V1 — no signed record/document stored; the acknowledgment statement itself lives in `settings.roster_waiver_text` (organizer-authored, default = `DEFAULT_ROSTER_WAIVER_TEXT`; added in the 5f follow-up).
- Q8 requirements keys = **flat** (per-division override deferred).
- Q9 check-in line = **gated to game day** (default `not_arrived` reads as a problem otherwise).
- Q10 reassignment (5l) = **does not notify** the organizer in V1.

## Critic's must-fix checklist (folded into the slices above)

- [ ] Deploy gate: migrations 114–117 to prod before Phase-5 prod deploy (`check:migrations`).
- [ ] 5a: coerce NULL `payment_status` → `pending` (prod-nullable; avoid false "Paid").
- [x] 5l: never write null/`''` to `teams.coach` (prod NOT NULL). — DONE: PATCH validates name min-length 1 → 400 on empty (no silent coercion); mass-assignment-safe (only `coach`/`coach_email`); mig 124 `teams.coach_email` joins the prod deploy gate; review = 0 High/Med, the LOW (results-finalized recipient) deferred to 5m.
- [ ] 5·0: remove pre-event CTAs first (no pitch-free violation window).
- [ ] 5b: read-only status only — no requirement-driven checklist before 5f.
- [ ] 5c: verify with a team that has `teams.email` (near-zero value before 5d).
- [ ] 5f: explicit per-key sanitizer branch before the silent fall-through.
- [x] 5g: re-run the no-regression pixel diff after 5h/5i. — done after 5h AND 5i (PASS both times; 5i never touched TeamHQ — git-confirmed out of the changeset + no-regression review lens = clean).
- [x] 5i: NULL home/away → TBD; division-level visibility gates the bridge. — DONE + extended: `published_generic` distinguished from `published_teams` (shows placeholder/TBD, never the hidden real name — mirrors public `getTeamDisplay`); the live public surface also requires a public (active|completed) tournament.
- [x] 5j: define post-gate-confirm coach state; build normalizer with override fields. — DONE: post-confirm = read-only + "contact the organizer" (409 once `roster_confirmed_at` set; re-submit allowed while only submitted); pure `buildTournamentRosterSnapshot` carries the full override surface (jersey/DOB/position/notes) so 5k doesn't re-touch the API; never mutates the master. Migration 123 (`source_player_id`, FK ON DELETE SET NULL) joins the 114–117 prod deploy gate.
- [x] 5m: transactional reminder path (org marketing opt-out can't suppress). — DONE: game-day reminder via `sendMarketingEmail({ skipOptOutCheck:true })` (Resend `scheduled_at`, evening-before, best-effort/isolated); per-recipient `cancelScheduledEmailForRecipient` on reject/re-publish. Review fix: `resolveCoachRecipient` lowercased so the stored `recipient_email` matches the (lowercased) cancel lookup. LOW deferred: per-team reminder keying (shared-coach-address collapses per-recipient).

## Journey-audit inputs (platform user-journey audit)

- **J1 (tournament organizer, routed 2026-06-11):** J1-074 confirms the organizer-side Roster Requirements authoring (5f) promises a coach-side flow that 5j/5k deliver — no scope change; keep the 5f→5j→5k order. (Evidence: docs/projects/active/journeys/JOURNEY_J1_TOURNAMENT_ORGANIZER.md)

### J5 (tournament coach) — walked 2026-06-11, BEFORE the 5j–5o build · 65 findings (21 High) · full detail + evidence in [journeys/JOURNEY_J5_TOURNAMENT_COACH.md](journeys/JOURNEY_J5_TOURNAMENT_COACH.md)

**Design input the unbuilt slices must absorb (all verified):**

- **5j/5k — roster submit + requirements visibility (J5-027, J5-036):** 5k's checklist must read requirements from `tournaments.settings` VALUES, never key presence (the 5f stamping gotcha), and widen the Team HQ tournament SELECT to include `settings` — which simultaneously unblocks J5-028's payment-instructions rendering, so do the SELECT change once for both. Add the awaiting-state row pattern ("Roster · Needed") and a NEW hero summary line — "This organizer requires: player names, birthdates, jerseys · 9–15 players". Pin section order across states (J5-030) before 5j adds a roster section. Housekeeping: delete the two orphaned `.cta*` CSS blocks in detail.module.css + tournaments.module.css (J5-036).
- **5m — afterglow + game-day reminder (9 findings):** **non-negotiable: the in-portal afterglow must render with ZERO dependency on the organizer's plan (J5-050)** — today the only post-event channel is Plus-gated, so free-org coaches get total silence after a championship. Two near-free result-hero wins first: "View final standings" link (canLinkPublic + slugs already computed) and a W-L-T line from already-fetched games; then trophy/placement + SharePageButton (J5-049). Collapse the prep checklist in result phase (J5-052); pass coachPhase into CoachLiveSchedule to retire live-updates copy (J5-054). **Repair the ask before it fires:** /coaches/start's gated state must become a real express-interest capture (reuse ScopeCeilingInterest's API) and be the destination for ALL express-interest labels (J5-051); the results email leads with placement, gains the coachPortalFooter (define a registrationId pick rule — recipients dedupe by email), drops `billing=annual` (J5-059). Game-day reminder content spec (J5-058): first-game time + venue + opponent honesty-gated by `published_teams`, check-in expectation, coachPortalUrl deep link to Team HQ, transactional (no opt-out suppression), Resend `scheduled_at` at publish, cancel on withdrawal/rejection. Consider a "play again" value bridge (J5-053). Hub's two always-on pitch cards (J5-016) need an **owner ruling** (the capability card was once explicitly sanctioned; the pressure-ladder lock postdates it).
- **5n — organizer email controls (J5-007):** every toggle 5n adds widens an existing honesty gap — the register success screen promises "check your inbox" unconditionally while the confirmation email is org-toggleable; condition the success-screen step on the same setting (settings JSONB already client-side). Cross-ref J5-061: surface the product question of whether coach-facing schedule/results emails should be plan-gated at all, given the coach — not the org — is the free tier's customer.
- **5o — public-nav / reachability (J5-004):** NEW scope beyond the current spec: export `coachPortalUrl` from lib/email.ts and make every registrationId-bearing entry point land team-deep — register success CTA, confirmation email, resend-access (promises team access, lands on the list), hub claim `next=`. Three hand-rolled URL builders exist for one destination; one already drifted.
- **Built-slice refinements (32 findings — 5a/5b/5e/5h/5i + 5c/5d adjacents):** themes: (1) **dates** — one America/Toronto convention for display (J5-025 off-by-one on every Team HQ screen) and one for day-logic (J5-039: UTC "today" flips phase at ~8pm Eastern, freezing the live scorebug mid-final — the marquee 5i feature dies in the 8pm–midnight window); (2) **fee truthfulness** — money strip + payment instructions (J5-028), past-due urgency (J5-034), check-in explainer (J5-047); note **J5-026 (High, backlog, fix-now): organizer single-row "mark paid" + check-in gate write only `payment_status='paid'` while the 5a resolver ignores it whenever a fee schedule exists → organizer sees Paid, coach sees OWED**; (3) **5e email pass ship-now Highs** — bulk accept/reject emails send literal "Division division for Tournament" placeholders (J5-056) and fall back to fieldlogichq@gmail.com as the contact (J5-057); then subjects keyed on team-not-event, escaping pass + plain-text entity decode, acceptance fee line, stale create-account copy, claim-CTA prominence for admin-added coaches; (4) **5h hero honesty** — lifecycle chips + live-first sort (J5-014 High), list/detail phase agreement (J5-018: list `isActive` ignores `status`), pending/waitlist info dead-end (J5-029), schedule checklist row + section stability (J5-030), real first-game countdown (J5-031); (5) **5i completion** — **no PWA manifest under /coaches/** so the install prompt is dead on Android and a broken promise on iOS (J5-040 High), Today spotlight + most-recent-LIVE guard (J5-041/043), the hide-schedule-page third honesty gate (J5-042), public quick links + pre-event share (J5-044/045), static-mode explainer (J5-046). **J5-032 (four phases, one template) is the umbrella acceptance test: when these land, the four Team HQ screenshots should no longer be mistakable for each other.**
- **Fix-now backlog Highs surfaced by this walk (not slice-owned):** J5-012 orphaned basic teams → linked registrations permanently inaccessible AND unclaimable (membership-only ownership, claim throws "already claimed by another account", platform-admin delete-user does no coach-team cleanup; live-DB evidence) · J5-026 paid-vs-owed precedence (above).

### J2 (rep head coach) — walked 2026-06-13 (Phase 4) · 10 findings route here · full detail in [journeys/JOURNEY_J2_REP_HEAD_COACH.md](journeys/JOURNEY_J2_REP_HEAD_COACH.md)

The J2 full coach walk routes 10 afterglow/interest + premium-money refinements into Phase 5 (report is the source of truth; screenshots gitignored under `tests/uat/results/journeys/J2/`). 0 refuted.

- **Interest/afterglow (extends 5m's ask infrastructure):** **J2-004 (Med)** `/coaches/start` names the free Coaches Portal but renders no link to it — add a "Don't have a team home yet? Create one free →" → `/start/team` in both the lead and saved states, and reword "your free Coaches Portal stays free" for a never-onboarded coach. **J2-009 (High, design-lens)** the `/coaches/start` page stacks marketing before the form on mobile and competes with an in-column "START FREE TRIAL" — form-first on mobile. **J2-027 (Low)** express-interest submits send no confirmation to the coach OR a platform-side lead notification (both interest paths only write `early_access_leads`). **J2-030 (Low)** `CoachStartInterest` → `/api/early-access` silently defaults `releaseNotificationsConsent=true` (CASL concern), asymmetric with the team-home path (defaults false) — align the two. **J2-015 (Low)** a new team lands cold; the welcome email is the only nudge — a first-run checklist/banner.
- **Premium accounting suite (now live after the J4-001 fix — franchise voice ≠ franchise mechanics):** **J2-033 (Med)** the accounting hub's unconfigured banner tells the coach "your org admin will set up dues schedules… before this view shows data" — but *she* owns dues + budget writes, and the same hub links her to set them; reword for the coach's role, keep the admin-dependency note only for genuinely org-owned allocations. **J2-034 (Med)** the dues-reminder windows disagree — the hub promises 30/7-day guardian emails but the only manual button (`Send Due Reminders`) posts with no window → scans only 3 days, returning "no installments due within 3 days" for a parent due in 10; let the coach pick the window or widen the default + align the copy. **J2-035 (Med)** coach dues "Mark Paid" is one unconfirmed click writing a permanent, non-reversible team-ledger income entry (409 on re-mark, no unmark) — the dues-side sibling of J4-013; add a confirm + a coach/treasurer "mark unpaid" that voids the paired entry, solve jointly with J4-013. **J2-037 (High, design-lens)** the Budget-vs-Actual *empty state* ("No budget plan found") looks like a broken page — a ghost icon + a plain blue link in a black void; give it a lime-bordered card with a headline + benefit + `btn-lime` CTA. **J2-038 (Low)** BvA renders every single-line item's Actual/Variance as a hardcoded "—" (actuals populate only at category total / period rows) — compute a per-line actual so single-line items answer "are we over on this line?". (Distinct from J4-024: there the org-side BvA had no actuals at all.)
