# FieldLogicHQ — Business Decisions Log

**Canonical, binding record of durable business decisions** (pricing, packaging, positioning, GTM/segment focus, monetization, commercially-driven roadmap sequencing). Maintained by the `/strategy` agent. Newest first.

**Status legend:** `Decided` (ratified by the owner) · `Proposed` (recommended, awaiting ratification) · `Superseded by [#]` · `Reversed`.

**How this file is used:** `/strategy` reads it on every invocation — entries marked `Decided` are binding across all chats unless explicitly superseded. Customer-facing changes carry a **Handoff** to `/marketing` (copy), `/billing` (gates), and/or `/plan` (build). Do not delete history — supersede.

---

### 2026-08-11 — The chooser's revisit trigger FIRED: `/demos` ships as a single shareable demo address for outbound industry contact — the funnel's own chooser is untouched

**Status:** Decided (owner, 2026-08-11 — *"I want a page that has that so I can send to people in the industry for them to try all demos… we can put a simple link in the marketing pages footer for now"*). ✅ BUILT on `dev` 2026-08-11, verified rendering, **not committed and not on production**.

**Decision:** A standalone demo chooser exists at **`/demos`** — both "See it live" doors (tournament sandbox `riverdale-minor-ball`, coach sandbox `riverdale-ridge`) on one page, at one short top-level address, reserved so no org slug can shadow it.

This exercises the 2026-08-10 ruling's **own reopening condition** rather than overriding it. That ruling deferred the chooser "revisit only when a campaign needs a single shareable URL" (logged twice — the part-3 resolution and the standing HANDOFF trigger), and the design twin phrased the same carve-out as "a campaign needing a single URL may get a tiny unlisted chooser — deferred until a campaign asks." **A campaign has now asked.** The use is outbound: a link handed to industry contacts who should walk *both* worlds, for whom "which persona are you?" is the wrong question.

**What the 2026-08-10 ruling KEEPS — the reversal is partial and narrow:**

1. **The funnel's chooser is unchanged.** The homepage persona questions remain THE chooser for cold prospects; hero twin doors, persona-page doors, the pricing "see it first" line and the `/for-clubs` both-halves block are all untouched. `/demos` is an *inbound landing pad for an outbound link*, not a funnel surface, and nothing on-site routes a prospect to it in preference to a persona door.
2. **"No nav item" STILL HOLDS.** Reached by a footer link only — never top nav, never mobile bottom nav.
3. **The ungated-door ruling (2026-08-02) is honoured in full**: no form, no email, no interstitial, no lead capture. Two doors and a plain description of what is behind each.
4. **`/demos` is deliberately NOT in the sitemap** (which is an explicit allow-list). It stays unlisted to search — the "unlisted" half of the design carve-out survives in the sense that matters commercially: this is an address we hand out, not a page we compete for.

**Two deliberate departures, both recorded here so they are not read later as drift:**

- **The page itself is NOT gated on `sandboxDoorsVisible()`** — a departure from binding design rule 5 ("every door is gated on the doors flag"). Rationale: the page follows the same contract as the doors it links to — *the door is always live wherever the sandbox is seeded; the flag governs whether the marketing site ADVERTISES it*. Gating the page would kill a link already sent to a named person, which is the one failure this page exists to prevent. **The footer link is what the flag governs**, so with the doors off the build collapses back to exactly the design ruling's "tiny unlisted chooser."
- **The chooser is LISTED (footer), where the design carve-out said "unlisted."** Owner-directed. This is the substantive amendment and `memory/design_decisions.md` must be amended to match — see Handoff.

**Recorded alongside, deliberately, not as a defect:** the same unit of work fixed the marketing header reading a *demo* session as a *login*. Because the doors establish a real session for a shared fictional account, a prospect who had only ever pressed "See it live" came back to the homepage and was offered "Account" and "Open app →" for an account that was never theirs. **A demo session is not an identity on marketing ground, and now ends there** (real customer sessions untouched; the coach demo re-enters through its own door rather than showing a login wall to an anonymous arrival). Logged as *context*, not as a decision — but it is a funnel-presentation fact, and the reason it belongs here is that it was invisible for the eight days the doors were open.

**Rationale:** The 2026-08-10 argument against a chooser was that the persona questions ask about the visitor's own life rather than our catalogue, which is a better chooser *for a stranger*. That argument does not reach an industry recipient who was sent a link precisely because they should see everything — for them the persona fork is friction and an arbitrary guess. The deferral was never a judgement that the artifact was wrong; it was a judgement that no one needed it yet.

**Affects:** demo discoverability, outbound/industry GTM motion, marketing footer inventory, the 2026-08-10 placement ruling (partially resolved), `memory/design_decisions.md` rules 2 and 5.

**Handoff:**
```
HANDOFF → /design (REQUIRED — binding-record drift, currently live in code)
- memory/design_decisions.md 2026-08-10 rule 2 says "NO standalone chooser page and NO nav item…
  a campaign needing a single URL may get a tiny UNLISTED chooser". The chooser now exists and is
  FOOTER-LISTED. Amend rule 2 to record the trigger as fired and the listing as owner-directed;
  leave "no nav item" standing. Add the rule-5 carve-out (page ungated / footer link gated).
- ⚠ SCOPE DEFECT to rule on: the owner asked for "the marketing pages footer", but the footer
  component also renders on the signed-in consumer app (/discover, /scores, /following, /account,
  /family), so "Live Demos" currently shows to paying customers and families. Rule 1 says the door
  pattern is "marketing layout only". RECOMMEND restricting the link to the marketing surface.
HANDOFF → /marketing
- Copy pass on the /demos page (shipped as working copy: "Walk the real product. Nothing to sign
  up for.", the two card descriptions, the can-I-break-it / can-I-see-both pair) and on the footer
  label "Live Demos". Folds into the door-label copy pass already owed from 2026-08-10.
HANDOFF → owner
- One standing revisit trigger from 2026-08-10 REMAINS OPEN: the 2027-01-01 ungated-door re-ask in
  the January conversion runbook. The chooser trigger is now closed by this entry.
```

**Supersedes:** nothing wholesale. **Resolves** the deferred part 3 of the 2026-08-07 proposal and closes the chooser half of the 2026-08-10 standing revisit triggers.

---

### 2026-08-10 — Premium Coaches Portal seats clarified: coaching staff is unlimited, the bundled tournament-admin side keeps the free-tier 3-seat guard; officials exemption extended to the portal; config label aligned to the canonical name

**Status:** Decided (owner, 2026-08-10 — officials flip and label alignment ratified directly; the seat ruling ratified after a clarification pass established exactly who a "seat" is)

**Decision:** Three rulings out of the 2026-08-10 docs-audit drift check:

1. **The 3-seat cap on the `team` (Premium Coaches Portal) plan STANDS — and is now precisely scoped.** It governs only the workspace's org-side admin members (the admin-console Members door: the coach + up to 2 co-organizer/staff invites for the portal's one included free-grade tournament). **Coaching staff — assistants and helpers — is unlimited on every tier**: they join via team-level invites with no plan gate and no count, and accepting one never creates an org-member row (verified in code — the seat check lives solely on the admin members invite route). The 2026-05-28 "no seat limits on paid tiers" ruling is **clarified, not contradicted**: it governs the paid ORG tiers; the portal is a team product whose bundled tournament tooling deliberately keeps the free tier's abuse-guard shape.
2. **The officials/scorekeeper seat exemption (2026-06-22) extends to the `team` plan.** `officialsFreeSeats` flips false→true so a standalone coach's gate/scorekeeper volunteers stop counting against the 3 seats. Every other tier already exempts them; the portal was the one gap, and the original rationale (don't tax multi-field events for scorekeepers) applies verbatim.
3. **The `team` plan's config label becomes "Premium Coaches Portal"** (was "Coaches Portal", the product-family name) per the 2026-06-22 naming canon, so billing/admin surfaces that render the plan label carry the tier name marketing surfaces already hardcode.

**Rationale:** The audit flagged PLAN_PRICING_FACTS' "paid plans = unlimited seats" against config's `seatLimit: 3` on `team`. Investigation showed both were right about different things — the doc sentence was the bug (it read as a coaching-staff cap, which does not exist). The officials gap and the label mismatch fell out of the same check.

**Affects:** PLAN_PRICING_FACTS.md (seats bullet rewritten + changelog), `lib/plan-config.ts` (two one-line changes), platform-admin surfaces rendering the plan label and the seat-limit error string. **No price, SKU, capacity band, feature key or gate value moves.**

**Handoff:**
```
HANDOFF → /billing — flip `officialsFreeSeats: true` on `team`; relabel `team` to
  "Premium Coaches Portal"; sanity-check the label's render points (platform-admin plan
  chips, the members screen's seat-limit error string). Drift check on completion.
HANDOFF → /marketing — none. No customer copy changes; marketing already uses the
  canonical name.
```

**Supersedes:** nothing — clarifies the scope of 2026-05-28 (seat limits) and closes a gap in 2026-06-22 (officials exemption).

---

### 2026-08-10 — THE DOORS ARE OPEN: both demos are public on production, and the funnel now has a zero-contact proof path for every live product

**Status:** Decided AND executed (owner-directed across 2026-08-08 → 2026-08-10; doors: *"flip the doors"*; placements: *"looks good, I approve those recommendations"* + the Club-block/reroute follow-ups: *"go ahead"*). Live on production as of the 2026-08-10 release (verified against the live site and databases, not plans).

**Decision:** The three-part go-public decision (Proposed 2026-08-07, below) is executed in full — with the placement set ratified alongside it:

1. **Both demo worlds are seeded on production** (2026-08-08) and **re-anchor on schedule on both databases** — the demos self-maintain; nothing depends on a person remembering to freshen them.
2. **The marketing doors are ON** (2026-08-10, a production-scoped setting — one deliberate action, exactly as the 2026-08-02 ruling prescribed). This is the entry that ruling's handoff recorded as owed "when the production door opens."
3. **Placements (design ruling 2026-08-10, binding in `memory/design_decisions.md`):** the demo is *proof one step after every claim, never competing with the ask*. Per product: homepage hero twin door → persona page (hero + bottom) → a text-weight "see it first" line under the two paid pricing cards. **No standalone chooser and no nav item** — the hero's side-by-side doored cards are the browse experience. The both-curious visitor is served on `/for-clubs` ("Both halves are live today", two doors — coming-soon becomes proof-by-parts) and, in a follow-up build, by an in-demo "walk the other half" cross-door.
4. **Route-to-proof:** every Club touchpoint now navigates to the Club page (where the two doors and the interest form both live) instead of opening the interest form directly. League Plus touchpoints deliberately stay form-first — no demo exists there, and a door pattern must never promise one.

**Resolution of the 2026-08-07 proposal's three parts:** part 1 (the coach front door) executed and extended; part 2 (per-demo addresses — `/see-it-live/tournament`) **not executed**, the bare address still means the tournament demo; part 3 (bare-address chooser) **deferred by ruling** — revisit only when a campaign needs a single shareable URL.

> **⚠ Amended 2026-08-11 — the chooser trigger FIRED.** Part 3 is no longer deferred: the chooser
> shipped as **`/demos`** (a top-level shareable address, not the bare `/see-it-live`, which still
> means the tournament demo). See **2026-08-11 — The chooser's revisit trigger FIRED** at the top of
> this log for what carried over and what changed. The "no nav item" half of rule 3 below **stands**;
> the "no standalone chooser" half is now scoped to the funnel, not to outbound links. The OTHER
> standing trigger in the Handoff below (the 2027-01-01 ungated-door re-ask) remains open.

**Rationale:** The funnel changed shape: a stranger can now find, walk, and sign up for either live product with zero contact and zero forms — the ungated-door posture (2026-08-02) applied to the whole shop window. The demos went public only after both were verified self-maintaining and health-checked on production; the two demo-integrity defects found during the launch window (a Saturday false alarm in the checker, a Sunday roll-forward in the re-anchor) were fixed and shipped in the same release, so the surfaces being advertised are the surfaces being maintained.

**Affects:** GTM funnel (zero-contact proof path), marketing entry points on all five surfaces, demo discoverability, express-interest routing for Club.

**Handoff:**
```
HANDOFF → /marketing — door-label + demo-adjacent copy pass (owed since the door-open step was
  defined; labels shipped as working copy: "See it live — no sign-up", "See a live tournament
  first", "See a coach's season first", the Club block's "Both halves are live today").
HANDOFF → owner — two standing revisit triggers: the bare-address chooser (when a campaign needs
  one URL) — ✅ FIRED 2026-08-11, closed by the `/demos` entry at the top of this log — and the
  2027-01-01 ungated-door re-ask in the January conversion runbook, still OPEN.
HANDOFF → /plan — the in-demo cross-door follow-up build, when scheduled.
```

**Supersedes:** the 2026-08-07 PROPOSED doors entry below (parts 1+gating executed, part 2 dropped for now, part 3 deferred by the no-chooser ruling).

---

### 2026-08-07 — The homepage states AVAILABILITY, not the promo calendar: the Founding Season is deliberately NOT announced on the persona cards, and the Coaches Portal card stops saying "coming soon"

**Status:** Decided (owner delegated the call to `/strategy` on 2026-08-07 — *"go ahead with strategy and marketing"* — after being shown three options; the homepage correction itself was directed explicitly: *"we need to fix that"*). Not yet built.

**Decision:** Four rulings about what the homepage's persona cards may claim.

1. **The Coaches Portal card goes LIVE.** It has read "Coming soon · express interest", greyed out, since before the product launched — **and the product launched on production on 2026-07-24**. Verified against the live database, not notes: `plan_gating.team = live`, flipped by the owner at 01:13 UTC with the Phase 3 launch note; live `/for-coaches` renders "Free until Jan 1, 2027" ×5 and "Start free" ×8, `/start` offers "Coach a team · Free", `/pricing` shows the promo. The homepage is the **only** surface that hardcodes launch state instead of reading the gate, which is why it froze while every other surface flipped together. **It must read the gate like the others** — a hand-written "true" would re-arm the identical trap.

2. **Persona-card badges state availability and the absence of a payment barrier — never the promo calendar.** Both live cards carry the *same shape* of claim (the "free to start, no credit card" class; exact words are `/marketing`'s). Prices, the $29/$39 anchors, and the Founding Season end date live **one click later** on the persona pages and `/pricing`, where a promotion can actually be explained.

3. **League Plus and Club keep saying "coming soon"** — verified correct against the live gates (`league`, `club`, `club_large` all `early_access`). Only two of the four personas have something to sell, and the grid should say exactly that.

4. **The Founding Season is NOT announced on the homepage for either product.** This was the live question. Announcing it for the Coaches Portal alone while Tournament Plus carries the identical $0-until-2027-01-01 offer would advertise one and hide the other, side by side; announcing it for both turns a persona chooser into a pricing page.

**Rationale:** Three findings forced this, and each one kills a tempting alternative.

- **A "then $X" badge on the Tournament card would be FALSE.** The free Tournament plan is `monthlyPrice: 0, trialDays: 0` — permanently free, no expiry, and "Free plan — no time limit" is already a trust point on its own persona page. There is no free *period* to end. So the two products' free offers are structurally different (a permanent free floor vs a comped paid tier), and forcing identical price wording onto both would require lying about one.
- **A "$29 from January" badge on the Coaches card OVERSTATES the cost of coaching with us**, because a **free Basic Coaches Portal exists and never expires**. The homepage would be quoting the price of the tier it happens to link to, while the cheaper permanent option goes unmentioned — and the person most likely to bounce off a price on a homepage is the volunteer coach this whole promotion exists to recruit.
- **It trades against the promotion's own stated goal.** Founding Season was ratified (2026-07-20) on the reasoning that *free-in-2026 justifies the beta feel and the goal is feedback volume, not revenue*. A price anchor on the top-of-funnel acquisition surface buys expectation-setting — which `/for-coaches` and `/pricing` already deliver to anyone who gets that far — at the cost of the one metric the promotion is optimising for.

**Recorded alongside, deliberately, not as a defect:** the homepage routes coaches to the **comped Premium** door (`/for-coaches` → the Premium enrolment path) while the **free Basic** door sits on `/start`. That is the right call *during Founding Season* — Premium costs the customer nothing today and produces far better feedback than the Basic floor — but it stops being free-of-consequence on **2027-01-01**, when every coach who came through that door meets a $29 decision and the free alternative they were never shown. **⚠ This belongs in the January conversion runbook** (Phase 4 of `FOUNDING_SEASON_COACHES_FREE_PLAN.md`, still unwritten), not in a badge.

**Affects:** homepage persona cards (copy + the live/greyed treatment), positioning consistency across homepage ↔ persona pages ↔ pricing, the January 2027 conversion runbook.

**Handoff:**
```
HANDOFF → /marketing  (this session)
- Homepage Coaches Portal card: replace "Coming soon · express interest" with an availability +
  no-payment-barrier claim in the SAME shape as the Tournament card. Must NOT carry a price or the
  Founding Season end date. Card body may be tightened but keep "whether or not your organization
  is on FieldLogicHQ" — it is the standalone product's whole differentiator.
- Homepage Tournament card: unchanged in meaning; confirm its badge and the coaches badge read as
  a matched pair rather than two unrelated statements.
- /for-tournament-organizers hero button: drop "— No Credit Card" so both persona pages carry the
  same primary action wording (owner-directed 2026-08-07). The objection is already answered twice
  more on that screen — the line above the button and the trust list below it — so nothing is lost.
- /start chooser: "I was invited" is demoted from a card to a below-the-fold aside (owner-directed).
  Write that line: it must answer the ONE question an invitee actually has — which email address to
  use — because the invitation is surfaced by matching the address it was sent to.
HANDOFF → owner / January runbook
- The comped-Premium routing above: decide before 2027-01-01 whether the homepage should offer the
  free Basic floor as an alternative when the promo ends, or whether the runbook handles it.
HANDOFF → /plan
- Fold the homepage correction into the demo-doors build (see the Proposed entry below) or ship it
  standalone — it is a live self-contradiction on the highest-traffic page and does not depend on
  the demo decision.
```

---

### 2026-08-07 — PROPOSED: matching "See it live" doors for both demos, and addresses that say which demo they open

**Status:** **Superseded by "2026-08-10 — THE DOORS ARE OPEN"** (part 1 executed and extended; part 2 — per-demo addresses — not executed, the bare address still means the tournament demo; part 3 — the bare-address chooser — deferred by the no-standalone-chooser ruling, revisit when a campaign needs one URL).

**Proposal:** Three changes to how a prospect reaches the two no-login demos.

1. **The coach sandbox gets a front door.** It currently has **none** — nothing on the marketing site links to it, so five seeded teams, a five-step guided tour and a bespoke warm treatment are reachable only by someone who already knows the URL. Both live persona cards and both persona pages would carry the same "See it live — no sign-up" door.
2. **Addresses name their demo:** `/see-it-live/tournament` and `/see-it-live/coaches` (plural, matching the product's own name). Today the bare `/see-it-live` silently means *tournament*, which a reader cannot tell — and these links get forwarded and read aloud, where the address is the only context that survives.
3. **The bare `/see-it-live` becomes a two-option chooser** for context-free arrivals, and every existing shared link keeps working.

**Where the choice is made, and why not at the door:** the homepage's four persona questions ("Running a tournament?", "Coaching a single team?") already *are* the chooser, and they are a better one than any demo picker, because they ask about the visitor's own life rather than about our catalogue — a stranger can answer them without knowing what we sell. Every button therefore routes straight into the matching demo with nothing in between.

**⚠ Tension with the 2026-08-02 ungated ruling, and why this is argued compatible:** that ruling says *"no email, no form, **no interstitial**, no lead capture of any kind"*. The chooser is reached **only** by an arrival with no persona context — a forwarded link, a printed card, a URL read from a stage — because every on-site button points at a specific demo. Nobody the ruling protects ever sees it, it extracts nothing, and the alternative for that visitor is an arbitrary guess about which product they wanted. **If `/strategy` or the owner reads this as violating the ruling, the fallback is to leave the bare address pointing at the tournament demo** and keep changes 1 and 2 — the coach door is the part that matters.

**The gating question this depends on, still unmade:** marketing demo doors are hidden in production builds unless explicitly enabled, so **no demo is publicly advertised today**. The 2026-08-02 ruling's own handoff records that an entry is owed here **"when the production door opens"** — that entry has not been written because the door has not opened. Ratifying this proposal means deciding to open it.

**Affects:** marketing entry points, demo discoverability, the ungated-door posture.

**Handoff:** none until ratified. On ratification: `/marketing` (door labels + the chooser's two options, which must name *what is behind each door* rather than naming our products), `/plan` (build), and a follow-up entry here recording that the production door opened.

---

### 2026-08-06 — A cancelled subscription STOPS WORKING, immediately: the product enforces the end of the relationship, and the dunning window is the only grace there is
**Status:** Decided (owner, 2026-08-06 — chose option A from four presented). ✅ BUILT on `dev`, uncommitted; owner QA owed (`OWNER_QA_LEDGER.md` §1.19).

**Decision:** When an organization's subscription is cancelled, **access stops at once, across the whole product** — coaches portal, tournament operations, scorekeeper and check-in apps, the family portal, and public pages. **Data is retained untouched** (the existing 90-day retention window is unchanged), so resubscribing restores everything intact. Three boundaries decided with it:

1. **Past-due keeps working — deliberately.** A failed payment marks the account past-due and notifies the customer; only Stripe *giving up* after its full retry window produces a cancellation. The dunning window therefore **is** the grace period, and it is the only one. (Pinned by test so it cannot be "tidied" into suspending.)
2. **Free (Basic) coach portals are unaffected.** Those teams belong to a person, not to a paying organization — there is no subscription to cancel, and the person keeps their own work.
3. **The operator escape hatch stays.** A platform-admin "subscription status → active" override reinstates an org without touching Stripe.

**Rationale:** This did not change what we charge — it made the product do what we already told operators it did. An audit of all 51 platform-admin routes found that cancelling wrote a status, displayed a confirm dialog itemising what would "shut down", and then shut almost none of it down: the Coaches Portal stayed fully open **forever**, the scorekeeper app kept accepting scores, the family portal kept serving. The org kept its plan, so every paid feature stayed available with **no expiry and no job to clean it up**. Commercially this is the most expensive part of the Club plan continuing free of charge after payment stops, indefinitely.

Option A was chosen over the three alternatives because it is the only one that makes the existing confirm dialog *honest* — it is what the operator already believes they are buying — and because the safety objection to it turned out not to apply: the usual argument for a grace period (option C) is protecting a customer whose card merely expired, and that customer is never cancelled, only past-due. Option B (demote to free) was rejected for silently *giving* a cancelled Club a free product while destroying the record of what they were paying for. Option D (read-only window) was rejected **for now, not on the merits** — see the Proposed entry directly below.

**Affects:** Product enforcement of the cancelled state, and the credibility of the cancellation dialog. **No price, plan name, capacity band, SKU or feature gate changed** — `PLAN_PRICING_FACTS.md` and `lib/plan-config.ts` are correctly untouched, and were reconciled (not restated) to confirm nothing in them describes cancellation behaviour. Retention/purge behaviour is unchanged. Downstream customer-visible effect: a cancelled org's coaches and volunteers now meet a plain "subscription has ended — nothing has been deleted" screen instead of a working product.

**Handoff:**
HANDOFF → `/plan` — **done**: `docs/projects/active/PLATFORM_ADMIN_ACTION_ENFORCEMENT_PLAN.md` + audit findings + PM brief.
HANDOFF → `/billing` — **done at build, and deliberately narrow:** no gate, key, price or plan-config edit. Enforcement is one access rail keyed on `subscription_status = 'canceled'`, defaulting closed, with a build-failing allow-list for the handful of surfaces that must survive (the resubscribe path above all). ⚠ **One open operational item `/billing` should confirm before this is treated as safely reversible:** `ENTITLEMENT_GRANTS_ENABLED` must be `true` in the Amplify environment or the platform-admin override in boundary (3) is inert in production — it would report success and change nothing. Unverifiable from the repo.
HANDOFF → `/marketing` — **nothing required, and one prohibition.** No pricing or plan surface changes. Do **not** market this as a feature. If cancellation is ever described on a customer-facing surface, the only two things that may be promised are that access ends immediately and that **data is kept and returns intact on resubscribe** — never a grace period, never continued read access (that is option D, unratified).
HANDOFF → `/docs` — at commit: any help content describing what happens when an org cancels must match this (access ends now; data retained; resubscribe restores). Do not document a grace period.

**Supersedes:** nothing — first entry to state what cancellation *does to access*. **Relates to:** the 90-day billing-retention behaviour (unchanged by this, and now the sole mechanism protecting the customer's data).

---

### 2026-08-06 — PROPOSED: a cancelled club should eventually be able to READ its own history, not just be locked out
**Status:** **Proposed** — recommended, **awaiting ratification**. Explicitly *not* built and *not* binding. Raised as option D in the 2026-08-06 cancellation ruling above, where the owner chose immediate hard-block for now.

**Proposal:** Replace the hard lock with a **read-only retention window** for the life of the existing 90-day retention period: a cancelled organization can still *open* its own past seasons, rosters, schedules and records, but can change nothing, message nobody, and take no money. At the end of the window it closes fully, as today.

**Rationale (why it is worth keeping on the record):** a locked door is a worse goodbye than the product can afford at this stage. Three arguments: (1) **it matches behaviour the product already has** — a finished season is read-only-by-design in the coaches portal, so "your club, frozen" is an established idea rather than a new one; (2) **it is a reactivation surface** — a club that can still see last season's roster in February is one click from resubscribing, whereas a club facing a wall has already mentally left; (3) **it lowers the stakes of cancelling**, which matters for a pre-revenue product where a cautious buyer's real question is "how hard is it to get out, and do I lose everything?"

**The trade-off, stated honestly:** "read-only coaching" is a real surface to design and verify, not a switch — every write path in the portal would need a proven read-only mode, and getting it wrong means a cancelled org retaining a write it shouldn't. That cost is exactly why it was not chosen today. It should be revisited when there is either (a) churn data showing cancelled clubs coming back, or (b) a sales objection about lock-in that we can attribute.

**Affects:** if ratified, it would **supersede** the access half of the 2026-08-06 cancellation ruling (data retention is unchanged either way) and would need a `/plan` build plus a `/marketing` line, since "you keep read access for 90 days" becomes a promise worth making.

**Handoff:** none until ratified. Do not build, gate, or write copy from this entry.

---

### 2026-08-04 — The Club Shared Book is a CLUB-PLAN EXCLUSIVE: the first coaches-portal feature gated by the ORG's plan rather than the team's portal tier
**Status:** Decided (owner, 2026-08-04 — "let's go with your recommendations", all five §8 rulings in `docs/projects/archive/COACH_CLUB_SHARED_BOOK_PLAN.md` ratified as recommended). Feature itself is **mockup-gated, not yet built** — this entry binds the packaging, not a ship date.

**Decision:** When the Club Shared Book ships (teams inside one org opt in to share their opponent scouting books — labelled, read-only, revocable), it is available **only to organizations on the Club plan**. Non-Club orgs and standalone Premium-portal teams see **nothing anywhere** — no switches, no locked teaser (absent, not upsold in-app; the selling happens on marketing surfaces, not as a dangling lock in a coach's settings). The other four ratified rulings shape the product, not the packaging: admin-enables + head-coach-opts-in, see-only-while-sharing reciprocity, book line + observations + per-team records travel, and no cross-team blended stats.

**Rationale:** This is the clearest "you run a CLUB" feature the coaches portal has — its value literally scales with the number of teams an org runs, which is the Club plan's defining trait. It strengthens the tier's identity ("your club's collective scouting memory") with a benefit that **compounds each season the club stays**, i.e. it is retention and upgrade leverage, not a checkbox. Precedent boundary, stated deliberately: the scouting book itself (P1–P3) rides the per-team Premium portal entitlement with **no new billing gate** (Insights-hub precedent) — the *club layer on top* is the first coach feature keyed to the **org's** plan, and that is the right seam: individual teams buy the book; the club buys the sharing.

**Affects:** Plan packaging/inclusions (Club gains a named exclusive; **no price, band, or SKU change**). `PLAN_PRICING_FACTS.md` + `lib/plan-config.ts`/`lib/plan-features.ts` gain the feature key **at build time, in the build's same unit of work** — deliberately NOT edited today, so the Facts doc never advertises a gate that doesn't exist yet; the build prompt carries this obligation. Marketing positioning for the Club tier gains its coaches-portal headline when the feature ships.

**Handoff:**
HANDOFF → `/plan` — **done** (plan + PM brief exist; mockups published for sign-off, artifact `def742fe-…` stage 8). **P1 BUILT on dev 2026-08-05**, owner QA = ledger §1.16.
HANDOFF → `/billing` — **done 2026-08-05, at build:** feature key `club_shared_book` → `club` in `lib/plan-features.ts` (both Club bands via the rank ladder; **deliberately not granted to `team`**), org-level enable (`organizations.club_book_sharing_enabled`) + per-team opt-in (`rep_teams.share_club_book`), both re-checked server-side on every read and write. `PLAN_PRICING_FACTS.md` inclusions + reconcile line updated and the drift check run in the same change; **`lib/plan-config.ts` needed no edit** (it holds no per-feature list), and no price, name, band or SKU moved.
HANDOFF → `/marketing` — at ship (not before): Club plan card/persona gains the collective-book story; convey "your club's scouting memory compounds with every team and every season" without promising cross-club or automatic sharing.
HANDOFF → `/docs` — at ship: the scouting guide's club section states the opt-in deal (what travels, reciprocity, revocation) in the coach's language.

**Supersedes:** nothing — extends the scouting-book packaging posture (no-gate, per-team) with the org-level exception it always anticipated (scouting plan §9 Q5).

---

### 2026-08-04 — Playing time is MEASUREMENT, never a fairness verdict: the app describes where minutes go and stops implying every team owes everyone equal ones
**Status:** Decided (owner, 2026-08-04, in the Game-Day Mode P1 session — reword set approved as proposed).

**Decision:** All customer-facing copy about **playing time** describes and measures; it never judges or implies the platform expects "fair" (equal) distribution across the roster. Rep teams deliberately lean on stronger players — a coach reviewing playing time wants **context** ("of our top 3 pitchers, who has pitched the most this week?"), not an audit against a fairness standard the team never promised. Approved reword set (exact words, owner-ratified):

| Surface | Today | Becomes |
|---|---|---|
| Insights doorway + report title | "Is playing time fair?" | **"Where is playing time going?"** |
| Overview tile verdicts | "Fairly even" / "Uneven" | **"Evenly spread" / "Leans on a few"** |
| Family-facing player recap | "the team's fair-play band" | **"the team's typical range"** |
| Lineup auto-fill copy | "a fresh fair arrangement" | **"a fresh arrangement with even bench rotation"** |

**Boundaries, decided with it:** (1) **Tryout fairness vocabulary is KEPT** — blind evaluation and the fairness receipt are an *evaluation-integrity* promise (everyone scored on one scorecard before names were revealed), a different concept coaches actively want to show parents. (2) **Features are unchanged** — bench-sit warnings, pitching caps, the even-rotation lineup option and the A-squad skew modes all stay; only the moral framing goes. (3) **Old release notes stay as written** — they are a historical record. (4) The family recap line is the highest-stakes instance: telling parents their child sat "in the fair-play band" sets an expectation a rep team never made, and invites the complaint at exactly the audience most likely to file it.

**Rationale:** The platform's paying rep segment runs deliberately unequal minutes. Copy that frames the metric as "fairness" makes the product take a side against its own customer's coaching model, and (worse) arms parents with the platform's own vocabulary in playing-time disputes. Descriptive language keeps the same data useful to both coaching styles: a balance-minded coach still sees "Leans on a few" and acts; a skew-by-design coach sees the same words as confirmation, not accusation.

**Affects:** In-app copy across the coaches portal (Insights doorway + playing-time report, Overview tile, family player recap, lineup auto-fill, related help-guide entries) — a copy sweep, no price/plan/gate/schema change. Game-Day Mode P1 (in flight) builds with the new vocabulary from day one. Future idea recorded separately in the ideas backlog: contextual playing-time queries ("who has pitched the most this week among X?") — a feature, not part of this ruling.

**Handoff:**
HANDOFF → build (new chat) — execute the app-wide copy sweep as its own unit of work: `docs/projects/archive/PLAYING_TIME_VOCAB_SWEEP_PLAN.md`. Tryout-fairness copy explicitly out of scope.
HANDOFF → `/docs` — help-guide entries that quote "Is playing time fair?" (Insights, lineups, recap guides) update **in the same sweep**, not before.
HANDOFF → `/marketing` — check brand/marketing surfaces (persona pages, feature copy) for "fair playing time" framing; align anything found to descriptive vocabulary. No pricing surface is implicated.
HANDOFF → `/billing` — **nothing.** No gate, key or plan config. Recorded so nobody goes hunting.

**Supersedes:** nothing. **Relates to:** the Game-Day Mode P1 build (first surface to ship the new vocabulary); the Tryout Insights / tryout-report work (whose fairness-receipt vocabulary this ruling deliberately protects).

---

### 2026-08-03 — What "names are baseline" MEANT for the sections that switch was gating: the duties a person already holds decide, and no new grant is created. Plus an honest correction to the "nobody's access narrows" claim
**Status:** Decided (owner, 2026-08-03 — ruled from mockups drawn side by side, https://claude.ai/code/artifact/1f7c75ac-b7bc-42c7-b4bf-69fe71a70a5a; all three questions answered as recommended). **AMENDS the A1 player-names entry below, which was silent on the question this settles.** ✅ BUILT on `dev` 2026-08-03, uncommitted — owner QA owed (`OWNER_QA_LEDGER.md` §1.9c).

**The gap this closes.** A1 retired the `roster` grant. The re-audit A1's *own handoff demanded* found that grant had been doing **two jobs**:

- **(a) "May a player's name be rendered here?"** — A1 makes this unconditionally yes. Settled.
- **(b) "Does this SECTION exist for this person?"** — the roster page, attendance, the development board, Insights, the Overview tiles and the depth chart all read `roster` as a **stand-in** for this second question. **A1 said nothing about it.**

⚠ Retiring the grant with nothing behind (b) would have opened every one of those surfaces to a **Helper** — a *widening*, which A1's own text says it is not. So (b) needed an answer, and the answer had to avoid inventing the very kind of switch A1 was deleting.

**Decision:**

1. **OPTION A — section visibility follows the DUTIES a person already holds** (attendance, lineups, internal notes, team money, documents, tryouts, or being head coach). **No new grant, no new vocabulary**: the model gets strictly smaller, exactly as A1 promised. It is **self-correcting** — grant a helper attendance and the roster page opens by itself, with no second switch for anyone to remember and no list to keep in step. **Option B** (retire the switch and let every section simply open) was drawn beside it and **declined**: it hands a parent volunteer the roster, the development board and the season's numbers.
2. **It delivers the complaint that prompted A1 in the first place.** *"There shouldn't be an assistant coach that can see players' dues with no players' names."* Under Option A, holding **money** *is* record access — so the dues page names people instead of showing "Player #12" and costing a phone call. Before, that combination was a dead grant.
3. **The staff card keeps a standing line** where the control used to be, telling a head coach what is true now and pointing at **Contacts & birthdates** as the switch that does the real protecting. Rationale: anyone who set the old switch will come looking for it, and a grid that simply lost a control explains nothing.
4. **Change notice = release notes + help guides only.** Actively notifying affected head coaches was **offered and declined** — it advertises a control that never worked. ⚠ `/release` owes **one line at promote time** (assistants who had the roster hidden gain those sections); wording in the plan's §7.5.

**⚠ CORRECTION TO A CLAIM THIS LOG SHOULD NOT CARRY UNCHALLENGED.** A1's briefing and plan both asserted **"nobody's access narrows."** That is *very slightly* too strong, and `/review` found the exception: an assistant holding `roster: 'view'` as their **only** record-ish grant — attendance, lineups **and** documents all explicitly switched off — loses the roster page, the development board and Insights. **Blast radius verified as ZERO: `rep_team_coaches` holds 0 assistant-coach rows on BOTH dev and prod (queried 2026-08-03), and A1 is not on prod.** Recorded rather than fixed: the durable consequence is that the model **can no longer express "sees the team list, holds no duties"** — that bundle is now indistinguishable from a Helper, which is precisely what Option A means. Fixing it would require resurrecting the retired grant or inventing a new one, both of which the ruling forbids. ⚠ **Item 4 was ruled partly on the premise that nothing narrows; the owner should know the premise is 99%-true rather than 100%-true, and it did not change the ruling because nobody is affected.**

**Rationale:** The alternative to a *union of existing duties* was a bespoke per-surface gate, which is exactly the fragmentation A1 deleted (four surfaces honoured the old switch and four ignored it, because each asked its own question). One predicate with many call sites is the shape the codebase already praises; it also means the nav, the route and the Overview tile for a given section cannot drift apart, because they now literally ask the same function. The trade-off, stated plainly: the union is **wider than some individual doors strictly need** — someone granted only *money* or only *documents* also reaches the roster page and the season review. That follows directly from Option A and is accepted; narrowing any single door later is an owner decision, not a quiet tightening.

**Affects:** **No price, plan name, capacity band, SKU, feature key or gate change.** Drift check run 2026-08-03 against `PLAN_PRICING_FACTS.md`, `lib/plan-config.ts` and `lib/plan-features.ts`: **no divergence and no edit required** — every surface here rides the existing **Premium Coaches Portal / `team`** entitlement, and the single `roster` string in `lib/plan-features.ts` is prose in a comment, not a feature key. The Facts doc needs no change beyond the reconcile line already added for the A1 sibling entry. Product model: the capability model loses one grant and one special case and **gains nothing**; "which sections exist" becomes a derived answer rather than a stored one.

**⚠ ONE BUILD-TIME DECISION, NOT AN OWNER RULING — flagged for the guardian switch-on.** The *"also connected to this team as a family member"* label (from the season-review/double-record entry above) counts **verified GUARDIAN links as well as followers.** Reason: a guardian keeps the same schedule/results access a follower does, so removing that person from Staff leaves them connected just the same — counting only followers would have re-created the false sentence that entry exists to delete, arriving through a different door. **Dormant while `GUARDIAN_TIER_ENABLED` is off** (the tier refuses every write), and deliberately decided in advance so the switch-on inherits it rather than discovering it. ⚠ This is the *only* piece of that work with **no automated test** — it needs real database rows — so it rests on owner QA.

**Handoff:**
HANDOFF → `/plan` — **nothing new.** A1 is BUILT on `dev`, uncommitted; remaining work is owner QA (`OWNER_QA_LEDGER.md` §1.9c), which needs a **second signed-in helper account**. Plan + build record: `docs/projects/archive/COACH_ROSTER_BASELINE_A1_PLAN.md`.
HANDOFF → `/docs` — **already done** (2026-08-03). The assistant-coaches guide now states that names are baseline, carries a "where did the Roster switch go?" FAQ, and names Contacts & birthdates as the real boundary.
HANDOFF → `/billing` — **nothing.** No gate, key or plan config. Recorded as "nothing" so a later reader does not go hunting.
HANDOFF → `/marketing` — **nothing.** No customer-facing surface sells roster visibility.
HANDOFF → `/release` — ⚠ **one line owed at promotion**, per item 4.
HANDOFF → owner — the guardian switch-on inherits the guardian-counts decision above, and should re-check the removal copy against **three** record types rather than two.

**Amends:** the **2026-08-03 A1 player-names entry** (below) by answering the question it left open. **Relates to:** the **2026-08-03 season-review / double-record entry** above (built in the same sitting; its family label is what the guardian note governs); the **2026-06-25** locked assistant-capability decision, already amended by A1.

---

### 2026-08-03 — A SEASON REVIEW is a coach's, not a helper's: the archive's door set becomes per-PERSON as well as per-season. And one adult's staff and family records stay SEPARATE — the fix for the seam is one honest sentence, not a merge
**Status:** Decided (owner, 2026-08-03 — all three recommendations in `COACH_SEASON_REVIEW_AND_DOUBLE_RECORD_RECOMMENDATION.md` accepted as written, from a recommendation session that deliberately wrote **no code**. Closes both questions the Practice Plans Phase 4 session raised and declined to resolve.)

**Decision:** Three rulings, one closed-on-evidence finding, and one handoff.

1. **(A) Season's End and Season Wrapped are COACH surfaces. A non-coach adult — a Helper today, and anyone like them later — does not receive a season review in a finished season.** The reasoning is **altitude, not names**: after the A1 ruling immediately below there is nothing on the Wrapped card a helper is forbidden to see (its only person-level line is `First name #number`, now baseline). The question is whether a whole season — the record, the win streak, the closest game, season-wide attendance, the award winner — belongs to someone who ran one station on one Tuesday. It does not. ⚠ **This must NOT be built as a roster gate** — that is the toggle A1 retires. It rides the binding **archive-is-opt-in** rule in `CLAUDE.md`, whose third question ("does it show what the coach could see AT THE TIME?") answers itself for a helper: what they could see at the time was one practice.
2. **The rule this establishes: the archive's door set is answered per SEASON today (`APPROVED_ARCHIVE_DOORS`) and must also be answered per PERSON.** Season's End is the first door needing both, and will not be the last — a Helper is the first non-coach adult on a staff list, so this ruling is the precedent for everyone who follows.
3. **(C) The follower / helper / (future) guardian records for one adult stay SEPARATE. No merge, no shared identity record, and no combined "people connected to this team" view.** The permitted maximum is a quiet **"also follows the team" LABEL** on the staff card — computed at render by comparing normalised emails, **joining no data and offering no action**. Reasoning: the relationships run in **opposite directions of trust** (a helper is *invited by the coach*; a follower *asks and is approved*), they carry different consent, and the separation is what keeps the boundary auditable — `lib/family-access.ts` states its own contract that one file answers *"who is in?"* and a different file answers *"what do they see?"*, and **neither can quietly answer both**. A joined record is a third thing that answers both. ⚠ The combined-list option was **offered and declined**: one list implies one record and invites one Remove button, which is the merge arriving through the UI instead of the schema.
4. **(D) One verified defect gets fixed, and it is a sentence.** The staff-removal confirmation reads *"{name} loses access to this team immediately."* For an adult who also holds a family follow that is **false** — the family layer is untouched by a staff removal, so they keep the schedule, the results and any shared game page. The head coach most likely to read that line carefully is the one removing someone after a problem. The dialog is corrected to say what it actually does and to name the other relationship when one exists. **Copy plus one lookup — no schema, no merge**, and it is the fix that keeps mattering when the third record arrives.
5. **(B) CLOSED ON EVIDENCE, not ruled — Season Wrapped's share path stands as designed.** Verified: a live **client-side** export exists (a "Share your season" button draws a 1080×1080 PNG in the browser and hands it to the OS share sheet, with a download fallback) and there is **no server route, no hosted image, no public link and nothing indexable**. What can leave: team and season name, the record, streak dates, the closest game with opponent and date, an attendance percentage, and one person-level line — *"MOST AWARDED · Emma #7"*. Money is excluded by construction. The share-safe first-name-plus-jersey label is a **considered design** that a prior adversarial review already tightened once. **No project, no further decision** — but it is why ruling 1 matters: a helper today can press that button and publish a child's first name.

**Rationale:**

*On why Phase 4 was right and incomplete.* Phase 4's `/review` found a helper being redirected into Season's End and fixed it — correctly, at the place it was looking. Verification for this session found **six paths reach Season's End and Phase 4 closed one**: the portal-root sign-in redirect (which needs no click and is where a helper actually lands), the archive sidebar — where **"Season's End" is the one closed-season door no grant governs**, falling through to `default: return true` — both team switchers, the `?year=` season rail, and a typed URL. ⚠ **And the server SENDS it in every case:** the Wrapped route gates only on *"did you hold an assignment on this season"* and *"is the season closed"* — **it checks no capability at all.** The screen Phase 4 added is real and good; it sits one level below the door people use, which is the exact defect class `CLAUDE.md` names for archives ("the unit of work is every page reachable from the door, not the door"). **This ratifies a build-time judgement as a rule and extends it to where the rule actually bites.**

*On why "harmless" was not the standard.* The honest counter-argument — *the names are baseline anyway, and a season review is a warm gesture toward a volunteer* — was considered and recorded. It loses because the archive ruling is opt-in **by default**: the test is not "is this harmful", it is "did someone decide". Left alone, reachability decides, and it decides the same way for every non-coach persona added after this one.

*On why the double record is not a defect to fix.* The Phase 4 gate examined this and accepted it; reopening it was legitimate, and the re-examination **confirms the original judgement**. The safety property holds — a helper grant does not widen the family layer and the family layer does not widen staff access — and the two lists already display the **email address** on both sides, so a head coach who wonders is not blocked from finding out. What is missing is correlation, not information. The smallest honest fix is therefore the sentence in ruling 4, not a structure.

**Affects:** **No price, plan name, capacity band, SKU, feature key or gate change.** Drift check run 2026-08-03 against `PLAN_PRICING_FACTS.md`, `lib/plan-config.ts` and `lib/plan-features.ts`: every surface at issue (Season's End, Wrapped, the staff panel, the family panel) rides the **existing Premium Coaches Portal / `team` entitlement**; no plan key, feature key, price or band is touched. **The Facts doc needs no edit and this entry deliberately restates no price.** Product model: the archive gains a **per-person** dimension it did not have — previously "which doors exist in a finished season" was the only question asked. Identity model: **unchanged, deliberately** — the three record types stay unjoined, and the only permitted addition is a render-time label. Head-coach UX: one confirmation dialog stops overstating what it does. Roadmap: no new project; all follow-through lands inside work already scoped.

**Handoff:**
HANDOFF → `/plan` — ⚠ **SEQUENCING: blocked until Practice Plans Phase 4 is committed** (shared files, same constraint as A1). Three items: (a) close all six doors to Season's End — one gate on the nav label plus one capability check in the Wrapped route closes them together, since every door leads to that route; (b) correct the staff-removal confirmation and add the "also follows the team" label (one lookup serves both); (c) ⚠ **A1's scope absorbs item E below — do not spin a separate project for it.**
HANDOFF → `/plan` (E — **the finding that matters most, carried into A1**) — **both helper-recognition mechanisms are keyed on `roster === 'off'`, the exact state A1 retires.** `hasNoTeamRecordAccess()` drives the *"This season has finished"* screen; `staffKindLabel()` prints the word **"Helper"** on the head coach's staff list. Give a helper roster visibility and **both stop matching**: the honest end-of-season screen stops firing (silently reopening the door Phase 4 closed) and every helper is relabelled **"Assistant"**. Neither is a gate — that is true and both files say so — but both are load-bearing UX. A1 must re-derive "does this bundle have record altitude / is it a helper" from something other than the grant it retires. A1's own handoff already warned to *"re-audit the surface list rather than trust this entry"*; **this is what it was warning about.**
HANDOFF → `/docs` — the assistant-coaches and **Season's End** help sections describe who reaches a season review; they need updating **when the build lands, not before**. Pairs with the A1 docs handoff already on record.
HANDOFF → `/billing` — **nothing.** No gate, key or plan config. Recorded as "nothing" so a later reader does not go hunting for a gate that was never added.
HANDOFF → `/marketing` — **nothing.** No customer-facing surface describes who receives a season review, and nothing here is a sold inclusion.
HANDOFF → owner — **two items.** (1) ✅ **The standing "duplicate-records seam" item from the 2026-08-03 Helper entry (ruling 3) is now ANSWERED** — the answer is *keep them separate, fix the sentence* — so the guardian switch-on inherits a ruling rather than an open question. What it still must decide at switch-on: the third record is the only one carrying a **stored consent artifact** and the only one tied to a specific child, so its removal is not symmetrical with the other two, and the corrected removal copy must be re-checked against three record types rather than two. Nothing prevents one email holding all three — fine, but it should be a recorded decision at switch-on, not an omission found afterwards. (2) ✅ **Verified safe, so the obvious fear can be set aside:** the family access panel (followers, approval queue, schedule visibility) is mounted on the Roster page but gates itself **server-side on `rosterPii`**, which a helper does not hold — so **A1 opening the roster page to helpers does NOT hand them the family queue.** The Gate 2 "two doors" property survives A1 intact.

**Supersedes:** nothing. **Relates to:** the **2026-08-03 A1 player-names entry** immediately below (this ruling depends on it — names are baseline, so ruling 1 is an *altitude* decision, not a privacy one — and hands it item E); the **2026-08-03 Helper entry** (ruling 3's accepted limitation and its standing owner item are what rulings 3–4 here resolve); the **2026-08-01 family-layer two-tier entry** (the follower/guardian tiers are unchanged); the **2026-08-01 archive rulings D-F1/D-F2/D-F7** and the binding archive-is-opt-in rule in `CLAUDE.md`, which ruling 1 extends from *which doors* to *which people*. Evidence: `docs/projects/archive/COACH_SEASON_REVIEW_AND_DOUBLE_RECORD_RECOMMENDATION.md`.

---

### 2026-08-03 — Player NAMES, NUMBERS and POSITIONS are BASELINE for everyone with portal access. The "Roster: Hidden" grant is retired, because an audit found it was already fiction in half the places it claimed to work
**Status:** Decided (owner, 2026-08-03 — *"any user should have roster read access to the names and numbers, items that show up in other areas of the app. There shouldn't be an assistant coach that can see players' dues with no players' names, or practice plans without players' names in groups."* Scope put to the owner as three options; **"everyone with portal access, including helpers"** was chosen over the narrower assistant-coaches-only reading.)
> ⚠ **AMENDED 2026-08-03 — read the entry two above before acting on this one.** This entry settles *whether a name may be rendered* and is **silent on which SECTIONS the retired grant was gating** (the roster page, attendance, the development board, Insights, the Overview tiles, the depth chart). Building it literally would have opened all of them to a Helper. The amendment rules **Option A — the duties a person already holds decide** — and also **corrects this entry's "it is a simplification, not a widening" framing**: one rare bundle does narrow (blast radius verified zero on dev and prod). ✅ BUILT on `dev` 2026-08-03, uncommitted.

**Decision:** A player's **first name, last name, jersey number and position** are visible to **anyone who holds portal access to that team** — head coach, assistant coach, or Helper. They stop being a grantable thing.

1. **The `roster` view/hidden control is retired from the head coach's duty grid.** `roster: 'off'` ceases to be a reachable state for a person with portal access.
2. **The real privacy boundary is unchanged and untouched:** guardian contacts, player date of birth, medical notes and emergency contacts stay behind the separate **Contacts & birthdates** grant, off by default. That grant is what was always doing the actual protective work.
3. **This makes the Helper's `planPlayerNames` grant redundant.** It exists to show a Helper names on the practice plan while holding `roster: 'off'`; with roster basics baseline, a Helper simply has roster visibility and the special-case grant can be retired with the toggle.

**Rationale:**

*The switch did not do what its label promised.* Audited 2026-08-03 across every coach surface that renders a player's name. With **Roster: Hidden** set, an assistant was blocked from the roster page, attendance and the development board — and still saw every player's full name and number in the **lineup builder**, the **playing-time report**, the **dues page**, and **awards**, because each of those gates on its own capability (`lineups`, `money`, awards) and never consults `roster`. Four surfaces honoured the switch; four ignored it. A head coach flipping it believed they had protected something they had not, which is worse than not offering the control — it is a **safety claim the product cannot keep**.

*Names are not the sensitive thing, and they are unavoidable in the work.* You cannot build a batting order, chase a family's dues, or run a station against anonymous rows. Every one of those screens is *supposed* to name the player. The information that actually needs protecting — how to contact a child's parent, when they were born, what their medical notes say — already lives behind its own switch and is unaffected by this.

*It is a simplification, not a widening.* This removes a control and a special-case grant; it does not add either. Contrast the **first-names-only tier** rejected in the Helper entry earlier the same day, which was declined partly on blast radius — *"it would change what 'roster visibility' means on every screen in the portal."* This ruling moves the opposite way: roster visibility stops being a variable, so every screen means the same thing.

*It closes a live gap between the log and the code.* The Helper entry (same date, below) ruled that a Helper sees **"full roster basics … exactly as any assistant coach does today."** The implementation delivered something narrower — `roster: 'off'` plus a `planPlayerNames` grant showing names **only** on the practice plan. This ruling makes the code match the decision that was already on record.

**Affects:** **No price, plan name, capacity band, SKU or gate change** — drift check run against `PLAN_PRICING_FACTS.md` (2026-08-03): nothing here is a sold inclusion, and no plan config is touched. Product model: the capability model **loses** one grant (`roster`) and one special case (`planPlayerNames`); ~8 route gates stop needing to ask a question with one possible answer. Head-coach UX: one fewer switch on the staff card. Positioning: unchanged. **Amends** the 2026-06-25 locked assistant-capability decision ("per-assistant area toggles"), which listed roster view/off as one of the toggles.

**Handoff:**
HANDOFF → `/plan` — ⚠ **SEQUENCING: do not start until Practice Plans Phase 4 (helpers) is committed.** All five files this touches — the capability resolver, the staff duty grid, the nav visibility gate, the practice-plan route, and the new helper home screen — were mid-edit by that agent when this was ruled. Recorded as an **addendum at the end of `COACH_PRACTICE_PLANS_PLAN.md`** so the helper project hands it forward on completion. Whoever picks it up should **re-audit the surface list rather than trust this entry** — the helper work was itself changing those gates as this was written.
HANDOFF → `/billing` — **nothing.** No gate, key or plan config.
HANDOFF → `/marketing` — **nothing.** No customer-facing surface describes roster visibility as a sold feature.
HANDOFF → `/docs` — the assistant-coaches help section describes the Roster grant as a switch a head coach can turn off; it needs updating when the build lands, not before.
HANDOFF → owner — **one standing item.** The `roster` capability is retired for *people*, but the redaction helper it feeds is also what protects **guardian PII**; the build must confirm that removing the roster toggle leaves the Contacts & birthdates gate exactly as strong. This is the one way a simplification here could quietly weaken something real.

**Supersedes:** nothing outright. **Amends:** the assistant capability model (2026-06-25 locked decisions) by removing one of its toggles. **Relates to:** the **2026-08-03 Helper entry** immediately below — this ruling makes ruling 1 of that entry true in code, and retires the `planPlayerNames` mechanism it introduced.

---

### 2026-08-03 — A HELPER is a non-coach adult on the coaching staff: full roster basics, no first-names-only tier, and the "preset not a role" premise is made TRUE by two additive capability changes rather than assumed
**Status:** Decided (owner, 2026-08-03 — the **privacy sign-off (Gate 1)** and the **family-layer reconcile (Gate 2)** that D30/§8.1 of `COACH_PRACTICE_PLANS_PLAN.md` made blocking preconditions on the Phase 4 build. Ruled from three scope options presented alongside the verified findings below; the narrower "first names and a jersey number only" scope was **offered and declined**.) **Gates CLEARED — Phase 4 may build.**

**Decision:** Four rulings that together unblock the closing phase of Practice Plans.

1. **A Helper sees FULL roster basics — first name, last name, jersey, position — exactly as any assistant coach does today. No new "first-names-only" visibility level is created.** A non-coach adult reading minors' names on a practice plan is accepted as a new category of access, on the same reasoning Player Development's **D2** accepted existing tryout consent as covering coach-authored records: the Helper is invited by the head coach, is standing on the field with those children, and receives no more than the printed sheet already hands them.
2. **The Helper stays a PRESET, not a permission model — and two additive capability changes are made so that is actually true.** (a) The **schedule** permission splits into *see the schedule* vs *change the schedule*, so a Helper can read a practice without being able to create, edit or delete team events. (b) **Team staff-chat membership becomes capability-aware** instead of deriving purely from "is on the staff", so a Helper is **not** auto-enrolled in the coaching staff's private room. ⚠ **Explicitly NOT a third `coach_role`** — both changes live inside the existing capability resolver, so "what can this person see?" still has exactly one answer in one place.
3. **Gate 2 reconcile — PASSED, with one accepted limitation recorded rather than resolved.** A Helper **cannot** see or approve family connections (that queue rides the guardian-contacts capability, which the preset does not grant), so a helper grant does not widen the family layer and the family layer does not widen staff access. The tier distinction holds: **a family follower sees the schedule ENTRY; only staff see the PLAN.** Helper is **Premium-only by inheritance** — it hangs off premium staff machinery — consistent with the 2026-08-01 premium-only ruling. **ACCEPTED LIMITATION:** a parent who is *both* a family follower and a Helper exists as **two unrelated records** the coach meets in two separate lists, with nothing indicating they are the same person. **No merge is being built.** When the guardian tier is switched on, the same adult could hold three. This is logged as a **known seam for whoever scopes the guardian switch-on**, not a Phase 4 defect.
4. **Sequencing: Phase 4 is settled and built BEFORE the owner's QA sweep**, which then covers Practice Plans Phases 2, 3 and 4 in one pass. The production release waits on that sweep.

**Rationale:**

*On the visibility ruling.* The alternatives were a helper who sees no names at all (the coach tells them who is in the group) and a new first-names-only tier. The no-names version is safe but weak — it hands back the bookkeeping the field screen exists to remove. The first-names tier is the privacy-optimal answer and was rejected on **blast radius**: it would change what "roster visibility" means on every screen in the portal that renders a player's name, immediately before a large aging release. The decisive argument for full basics is that **the Helper's alternative is the printed sheet, which already carries every child's full name, the date, the start time and the address** — the phone version is not a wider disclosure than the paper one it replaces, and it is *revocable*, which paper never is.

*On making the preset true rather than assuming it.* D30 chose a preset precisely so no new permission model would exist. Verification this session found the premise did not hold as written: the plan's stated bundle ("schedule visibility on, all writes off") was **not expressible** — the schedule permission is one switch covering viewing *and* creating/editing/deleting events — and **team staff-chat membership is derived from the staff assignment and documented as explicitly not a capability toggle**, so a parent volunteer would have been auto-seated in the room where coaches discuss children. That is the same exposure class the **2026-08-01 family-chat cut** was made to avoid, arriving through a side door. Two additive changes cost far less than a third role and keep D30's actual intent — one place answering "what can this person see?" — intact. **The premise survives because it was repaired, not because it was correct.**

*On what was already sound.* The practice plan is head-coach-write-only, and the practice-plan read is source-gated on roster visibility (Phase 3's `/review` fix) — a caller without roster access never receives a player list at all, so the Helper's names ruling is a **deliberate grant**, not a leak.

**Affects:** **No price, plan name, capacity band, SKU, feature key or gate change.** Drift check run against `PLAN_PRICING_FACTS.md` (2026-08-03): the Helper rides the existing **Premium Coaches Portal** entitlement, paid plans already carry **unlimited seats**, and nothing in the inclusions log describes staff-invite composition as a sold inclusion — **the Facts doc needs no edit, and this entry deliberately restates no price.** Identity scope: the platform now has a **non-coach adult who is nonetheless staff** — the first person on a team's staff list who is not a coach. Product model: the capability model gains a read/manage split and a chat-membership key (additive; existing coaches are unaffected by default). Roadmap: clears the last gate on the closing phase of Practice Plans, which is the last item before a large unpromoted release.

**Handoff:**
HANDOFF → `/plan` — update `COACH_PRACTICE_PLANS_PLAN.md` **§8.1** (both gates now cleared, with the ruling), **§9.2** (Phase 4's storage line stays "None" — ⚠ the two capability changes are code, not schema; if a migration appears, the preset has drifted into a role), the **D29/D30** rows, and the status header. Record the two capability changes as a build-time decision with their blast radius.
HANDOFF → `/design` — mockups for the two surfaces round 5 did **not** draw: the **Helper preset on the staff invite path** (beside "Assistant coach", with what it grants stated in plain words at the moment of inviting), and the **helper's one-screen portal**. ⚠ Round 5 drew the helper-access *decision* — the rejected shareable link versus an invited preset — not the helper's own screens. The portal should reuse the shipped **"My station"** view rather than introduce a second one.
HANDOFF → `/billing` — **nothing.** No new gate, key or plan config; Helper inherits the premium team entitlement that already governs the staff machinery. Recorded as "nothing" so a later reader does not go hunting for a gate that was never added.
HANDOFF → `/marketing` — **nothing owed now.** The Helper is a staff-invite option inside a plan a customer already has; no plan card, comparison table or persona page changes. ⚠ If it is ever marketed, it is a **Premium** capability, and the copy must never imply a helper can be given a link — see the standing rule below.
HANDOFF → owner — **two standing items.** (1) The **duplicate-records seam** in ruling 3 belongs in the guardian switch-on scope, whenever counsel clears it. (2) **The no-shareable-link rule is permanent and unaffected by this ruling** — a Helper reaches a plan by invitation and sign-in, never by a URL. An unauthenticated link to a practice plan is a public page naming ten children alongside a date, a start time and a street address; forwarded once, it is public permanently.

**Supersedes:** nothing. **Relates to:** the **2026-07-11 G3** entry (its safeguarding preconditions stand; this is a staff-side grant, not a family-side one, and touches none of them); the **2026-07-11 G4** entry (its surviving half — practice visibility and richer family features ride Premium, no new SKU, no per-family fee — is untouched); the **2026-08-01 family-layer two-tier entry** (the follower/guardian tiers are unchanged; ruling 3 is the "two doors" check that entry made necessary); the **2026-08-01 family-chat cut** (whose reasoning is what ruling 2b protects); Player Development's **D2** consent finding (the precedent ruling 1 argues from).

---

### 2026-08-02 — Colour choice at tournament creation is ENTITLEMENT-GATED, with NO free-tier tease: a plan that cannot have the feature does not see the control at all
**Status:** Decided (owner, 2026-08-02 — closes the question the Tournament Creation Live Preview plan explicitly parked as "an owner pricing decision"; *"put the colour swatch row on the wizard when used by a tournament plus user and not show it when it is the free tier"*). **BUILT on `dev` 2026-08-02, uncommitted; owner QA owed (`OWNER_QA_LEDGER.md` §5.1).**

**Decision:** The setup wizard's live public-page preview carries a row of the nine theme presets **only for orgs whose plan includes `advanced_tournament_branding` (Tournament Plus and above)**, and the member must additionally hold `manage_branding`. A free **Tournament** org sees **no row, no greyed-out or locked swatches, and no upgrade prompt** — the wizard is exactly what it was. Both gates are enforced **server-side** on the two creation paths, not merely hidden in the UI.

Three riders settled with it:
1. **Presets only in the wizard.** The nine presets; custom hex, logo and banner stay in Branding settings. A creation wizard does not grow a second branding screen.
2. **The pick is persisted, and only an explicit pick is.** Choosing a colour creates the tournament with it; leaving the row alone stores nothing, so the event keeps **inheriting the org's theme and follows a future org rebrand**. That distinction — identity of its own vs. follows the parent — is the actual product rule, and it is invisible on the day and material a season later.
3. **On the reuse path an explicit pick overrides the branding copied from the source event.** Otherwise the newest instruction would lose to a copy.

**Rationale:** The alternative on the table was the mockup's original beat — show the full swatch row during free signup as a taste-of-premium, repainting live, then gate the save. The owner rejected the tease. That is consistent with the brand's standing rule against implying a free plan includes what it does not (the same rule that governs the sandbox's "honestly labelled as Plus" rider in the entry below), and it avoids the worst version of an upsell: one that lets someone design something and then takes it away at the moment of saving. **Trade-off accepted and stated plainly: we forgo the single highest-intent upsell moment in the product** — an organizer who has just watched their own event assemble itself is as receptive as they will ever be — in exchange for a free tier that never advertises its own walls. ⚠ **Sharpest right now:** during Founding Season the upgrade this would tease costs **$0**, so the tease has never been cheaper to honour and the ruling has never been cheaper to hold; both facts change on **2027-01-01**, which is why the revisit trigger below exists.

**Affects:** **No price, plan name, capacity band, SKU, feature key, or gate change.** Drift check run against `PLAN_PRICING_FACTS.md` (2026-08-02): `advanced_tournament_branding` → `tournament_plus` in `lib/plan-features.ts` is unchanged and already correctly recorded; this decision governs **where an existing entitlement is surfaced and whether its absence is advertised**, not what the entitlement is or costs. Facts doc needs no edit. Positioning: extends "gate the room, not the visitor" to its inverse case — where the visitor genuinely cannot enter, we do not put a locked door in their path. Product: first place in the platform where a **tournament's branding is set at creation rather than after the fact**.

**Handoff:**
HANDOFF → `/marketing` — **nothing owed now; one line if the revisit reverses this.** In-app help was already updated at build time (create + reuse guide sections and two FAQs describe the row, who sees it, and the follow-the-org default). No pricing-page, persona-page or comparison-table change: nothing about what a plan *includes* moved. ⚠ If the tease is ever adopted, the copy for it goes through `/marketing` and the pricing-facts reconciliation, per the plan's own note.
HANDOFF → `/billing` — **nothing.** No new gate, key, or plan config; the build reuses `advanced_tournament_branding` + `manage_branding` unchanged. Deliberately recorded as "nothing" so a later reader does not go looking for a gate that was never added.
HANDOFF → owner — **revisit trigger 2027-01-01**, alongside the sandbox's (entry below) in the Founding-Season conversion runbook: when Tournament Plus stops being free, re-ask whether the free tier should see the swatches it cannot use. The expectation is that the answer holds, but it must be **re-asked, not assumed**.

**Relates to:** `TOURNAMENT_CREATION_LIVE_PREVIEW_PLAN.md` (v1.1 section records the build; the v1 plan's "Explicit non-goals / open options" is what this closes); the Founding-Season promos (why the trade-off is cheap today and dearer in January); the 2026-07-11 tournament-branding reframe line in `PLAN_PRICING_FACTS.md` (same entitlement, different surface — unaffected).

---

### 2026-08-02 — The product demo is UNGATED at the door. Openness is the competitive asset while the product is free anyway; what gets gated is the ROOM, not the visitor
**Status:** Decided (owner, 2026-08-02 — put to the owner as an explicit competitive-risk question ahead of the Tournament Admin Sandbox build; *"I agree with your recommendations"*)

**Decision:** Five rulings that together set the GTM posture for the "See it live" sandbox — and, because the machinery is shared, for the Coach Sandbox behind it.

1. **No gate at the door. No email, no form, no interstitial, no lead capture of any kind.** "See it live" signs the visitor straight into a live demo tournament. The value of the surface is entirely in the frictionlessness; anything placed in front of it is subtracted from the thing itself.

2. **The competitive-exposure risk is assessed as effectively zero, and the reason is our own pricing.** Tournament is free with no credit card and no time limit, and **Tournament Plus is $0 for anyone until 2027-01-01** (Founding Season). A competitor who wants to see everything does not need the sandbox — signing up free gives them *more* than the sandbox does, including write access and their own event. Gating the demo while running that offer would be locking a window beside an open door. Compounding it: an email gate does not filter competitors (throwaway address) — it filters the skeptical volunteer organizer we are actually trying to reach.

3. **What a demo exposes beyond the marketing site is execution, not information.** The feature list is already public on the persona and pricing pages. What the sandbox additionally shows is *how the product feels* — the schedule-health engine reacting to a bad drag, the game-day dashboard triaging a Saturday — which costs quarters of engineering to copy, not a screenshot. Meanwhile "click here, it is running right now" is a claim a competitor selling by feature-list-and-sales-call structurally cannot match. **The openness is the moat, not the leak.**

4. **The gating that DOES apply is curation of the surface, not capture of the audience.** A defined set of admin areas is **hidden in the sandbox rather than allowed to dead-end**: billing/subscription screens (a comped demo org's billing page answers a question nobody is asking yet), staff invitations (outbound, therefore entirely locked controls — a screen made of locked buttons reads as a broken product, not a careful one), data tools/exports (exporting invented data teaches nothing), and deep settings forms. This applies the binding **archive-is-opt-in** principle from `CLAUDE.md` — *hide the entry point rather than let it dead-end; a link that 404s is the same bug wearing a politer face* — to a second surface. Curating for demo quality, not for secrecy.

5. **Revisit trigger: 2027-01-01.** When Tournament Plus stops being free, the sandbox becomes the only free look at Plus and the premise in ruling 2 weakens. The expectation is that the position still holds — at that point the sandbox does upgrade-demo duty, which is more valuable, not less — but it must be **re-asked, not assumed**, as part of the January 2027 conversion runbook.

**Demo-org parameters ratified in the same session (D1–D4, from the approved Phase 1 mockups):**

- **D1 · Naming — fictional-realistic, one shared invented world.** The demo org is the **Riverdale Minor Ball Association** running the **Riverdale Summer Classic**; teams are the Rapids, Cyclones, Marauders and Sharks. Extends the existing **"Riverdale"** fictional convention already used by the Chunk-G coach budget sample, so both sandboxes speak one invented world and nothing can be mistaken for a real association. All contacts invented.
- **D2 · Plan tier — COMPED TOURNAMENT PLUS, not the free tier.** A free-tier demo undersells the product and makes the sandbox argue against its own upgrade. **This is an internal comp of an org we own — not a repricing, not a new SKU, and not a customer-facing offer.** The $39/$390 list price is untouched. Binding rider: any Plus-only capability visible in the sandbox stays **honestly labelled as Plus**, per the brand rule against implying a free plan includes what it does not.
- **D3 · Reset cadence — 2-hour replay, nightly date re-anchor, with a visible `mm:ss` countdown in the sandbox banner.** The visible clock is the proof the demo is running rather than recorded; the nightly re-anchor is what stops it ever reading as stale.
- **D4 · Phase 2 "moments dock" (Registration week / Game day / The morning after) — DEFERRED, not cut.** Ship the one canonical moment (semifinal morning) first; registration-week needs a second seeded state with pending teams and payments, and is better designed after watching real prospects use the first one.

**Rationale:** Our funnel today has **no "see it working" path** — every CTA on every public surface is a signup, another marketing page, or a lead form. A skeptical volunteer organizer breaking a fake tournament with their own hands in the first ninety seconds, *before* anyone asks for an email, is the strongest acquisition asset available to us, and because it is the real product it can neither go stale nor over-promise. Gating it converts that asset back into the lead form it was built to replace. The counter-argument the owner raised — competitors seeing the full functionality — was tested against the live pricing record (`PLAN_PRICING_FACTS.md` §Promotions) and does not survive it: we are currently giving the paid tier away to anyone with an email address, so there is nothing the sandbox discloses that a competitor cannot already obtain more completely and more quietly. Trade-off accepted and stated plainly: **we will never know who used the demo unless they convert.** No attribution, no retargeting list, no MQL count off this surface — only the conversion itself. That is the price of the friction being zero, and at pre-revenue scale the conversion matters more than the list.

**Affects:** **No price, plan name, capacity band, SKU, gate or feature key changes — `PLAN_PRICING_FACTS.md` is unaffected and no `lib/plan-config.ts` / `lib/plan-features.ts` change is required.** Drift check run: D2 is a comp applied to an org we own (precedent: the Founding-Season `platform_override` comp path), not a plan or catalog change; the demo org must **not** appear in plan gating, the price catalog, or admin metrics. GTM/funnel: this is the **first** proof-before-signup path in the product, and it is shareable and QR-able at real events — the cheapest word-of-mouth channel available to us. Positioning: consistent with brand §11 (*position against the old way, never against a named product*) — the sandbox is an anti-sales-call stance that names nobody. Roadmap: Phase 1 is dev-only; **opening the door on production is a separate owner decision** and gets its own entry here when it happens.

**Handoff:**
HANDOFF → `/marketing` — **not yet; at build/ship, not now.** Then: the door label and the objection-killing line beneath it on `/for-tournament-organizers` and the homepage Tournament persona card; the sandbox banner; the blocked-save nudge family (card / toast / locked-outbound); the locked-outbound line. ⚠ Constraints: never name a competitor; never imply the demo is the visitor's own data or that anything they do is kept; the honesty claim ("nothing here is saved") must survive every rewrite — it is the thing that makes a shared no-login account defensible. Draft copy exists in the approved mockups as a starting point only.
HANDOFF → `/billing` — **one item, and a prohibition.** Comp the demo org to Tournament Plus using an **existing** comp mechanism. Do **not** create a "demo" plan, a demo feature key, or a new gate — and keep the demo org out of the price catalog and any billing-derived metric.
HANDOFF → `/plan` — **this build.** `TOURNAMENT_ADMIN_SANDBOX_PLAN.md` to record D1–D4 as ratified, add the curated (sandbox-hidden) admin surfaces to Phase 1 scope, and add **search-engine exclusion of the demo org's public pages** to the hygiene slice alongside the existing `/discover`, admin-metrics and observability exclusions. Mirror the ungated posture + curation principle into `COACH_SANDBOX_SEASON_PHASES_PLAN.md`, which inherits the shared machinery.
HANDOFF → owner — **two future entries owed here:** one when the production door opens, and one at the **2027-01-01 revisit trigger** (ruling 5), which belongs in the January 2027 Founding-Season conversion runbook.

**Relates to:** the Founding-Season promos (Tournament Plus and Premium Coaches Portal free until 2027-01-01) — ruling 2 rests on them, which is exactly why ruling 5 exists; the binding **archive-is-opt-in** ruling in `CLAUDE.md` (ruling 4 reuses its hide-don't-dead-end principle); `COACH_SANDBOX_SEASON_PHASES_PLAN.md` (shares the demo-mode machinery and inherits this posture); the Ideas Backlog entry *"Play With a Live One First"*, which this closes out for the organizer track.

---

### 2026-08-01 — The drill library ships PER-TEAM **and** CLUB-WIDE together; the library is an instrument (live-season only), and a coach pulls their own history forward into it
**Status:** Decided (owner, 2026-08-01 — GATE 2 of the Practice Plans Phase 2 build prompt, put to the owner before the table was designed)

**Decision:** Two rulings that together settle the shape of the Practice Plans **drill library** (Phase 2).

1. **BOTH scopes ship in Phase 2 — a per-team drill library AND an org-authored shared club-wide set.** A coach's team keeps its own private drills; an org owner/admin additionally curates a small shared drill set that appears in **every** team's picker alongside their own. Teams may *use* shared drills but cannot rename, retire or delete them — that stays with the org admin. This is the **mig-184 shape adopted up front** rather than retro-fitted: shared rows are stored with the owning team left empty and the org set, exactly as shared tags and shared award types already are, and they surface on the **existing** org shared-library screen as a third section rather than a new admin surface.

2. **The drill library is LIVE-SEASON ONLY — its door is hidden in a completed season — but a coach can pull drills forward out of past seasons into the current library.** The library is a *reusable instrument*, not a record of a season, so it fails the archive test the same way tryouts and money do and gets no archive door. Two facts make this cost the coach nothing: a team's drill library is **team-scoped, not season-scoped**, so it simply carries forward on its own (the team row persists; only the program year turns over); and what genuinely *is* season-trapped — the practice plans written in past seasons — becomes **importable into the current library**, mirroring the import-from-the-org-set idiom. A past plan continues to read correctly for ever because the drill text it captured lives on the plan itself.

**Rationale:**

*On shipping both scopes now.* The owner was offered per-team-now (with club-wide as a cheap, precedented later widening) and chose to build both. The decisive commercial point is that **a club-wide drill library is a genuine reason Club sits above League on the ladder** — it is the first artifact in the product where a club's *coaching method* becomes an org-level asset rather than twelve coaches' private notes, which is a retention and standardization story a per-team library cannot tell. Building it up front also avoids the one real cost of sequencing: a second migration and a second pass over the picker, the preview and the promotion path. The known trade-off, stated plainly: **the shared set has nobody to author it on day one** — the library ships empty by design (sport-neutrality: no seeded drills, no fixed category list, ever), so the club-wide half delivers value only once a club has drills worth sharing. Ruling 2's import path is what shortens that wait, because a coach's own past practice plans become the seed instead of a blank screen.

*On the archive posture.* Applies the binding **archive-is-opt-in** ruling (`CLAUDE.md`, 2026-08-01) to its second new feature. The owner's instinct — "the library doesn't need to be accessible in an archived season, but the coach should be able to import from past seasons" — is the record/instrument distinction arrived at independently, and it lands on the cheaper and more useful side: no season-carrying page subtree to build, no need to reconstruct what the library looked like at the time (which the library does not record), and no dead-end link. ⚠ **Import reads a past season's plans into the live season's library. That is a deliberate, recorded cross-season READ** — head-coach-only, copies forward, writes nothing into the finished season — and it is the one place in this feature that addresses a past season at all.

**Affects:** **No price, plan name, capacity band, SKU, gate or feature key changes — `PLAN_PRICING_FACTS.md` is unaffected and no `lib/plan-config.ts` / `lib/plan-features.ts` change is required.** Drift check run: the per-team library rides the existing **Premium Coaches Portal / Club** per-team entitlement that already gates the whole coaches portal; the shared set rides the **existing org rep-teams admin area**, which is Club-module territory already. Packaging consequence, recorded but *not* a new gate: a **standalone** Premium Coaches Portal coach whose org is not on Club has no org admin and therefore no shared set — they get their own team library, which is the correct and honest outcome and is the ladder working as designed. Positioning: Club gains a concrete, demonstrable club-wide asset; `/marketing` may use it **only once shipped**.

**Handoff:**
HANDOFF → `/marketing` — **none yet; do not write ahead of the ship.** When Phase 2 lands, the Club/`for-clubs` story may add "a shared drill library every team's coaches can pull from." ⚠ Never imply seeded or supplied drill *content*, drill videos, or sport-specific starter drills — the library ships empty and coach-authored, permanently (cut list, plan §11). Never imply families or players see drills.
HANDOFF → `/billing` — **none.** No gate moves, no new feature key. Do not add one: both halves ride entitlements that already exist.
HANDOFF → `/plan` — **this build.** `COACH_PRACTICE_PLANS_PLAN.md` §9.2 Phase 2 + D14–D20/D27 to be updated with both rulings, the shared-set shape, and the import-from-past-seasons path; the archive posture to be recorded against the `APPROVED_ARCHIVE_DOORS` / `APPROVED_SEASON_AWARE_ROUTES` decision point.

**Relates to:** the 2026-06-22 Club capacity-band structure (this strengthens the Club rationale without changing a number); mig 184's org-authored shared library (the structural precedent, and the reason "both now" is cheap); the binding archive-is-opt-in ruling in `CLAUDE.md`.

---

### 2026-08-01 — FAMILY CHAT IS CUT. Chat stays coaches-and-admins-only; the guardian tier ships BUILT-BUT-SWITCHED-OFF behind counsel
**Status:** Decided (owner, 2026-08-01 — *"you can remove slice 4 from the project, I don't intend to introduce family chats any time soon, we can limit chats to coaches and admins for now"*)

**Decision:** Two rulings that together close the last open questions in the Chunk D family layer.

1. **Family chat is CUT — not deferred, not sequenced later, removed from the roadmap.** Chat remains a **staff surface only**: coaches, assistant coaches, and org admins, plus the existing organizer↔coach tournament rooms. **No portal tier gets family chat, because family chat is not being built.** Revisiting it requires a new entry here, not a revival of the old plan section — the safeguarding preconditions below have not been met and would all still apply.

2. **The GUARDIAN tier (parent linked to a specific child) is BUILT and SWITCHED OFF.** It ships behind a server-side flag that is off by default and refuses every guardian request, invite, claim and coach route while off — no guardian connection and no consent record can be created, by the UI or by a direct API call. **Turning it on remains gated on the PIPEDA/CASL counsel sign-off being recorded in this log.** The consent wording in the product is an explicitly-marked, on-screen-labelled placeholder pending counsel's text.

**Rationale:**

*On the cut.* Family chat was always the highest-liability item in the chunk and the furthest from ready. Discovery verified the chat engine has **no remove/ban capability anywhere** — so it was never one slice, it was a moderation-toolkit build plus a counsel re-check plus the room itself, and G3's safeguarding preconditions (no unsupervised 1:1 adult–minor DMs, staff delete-with-visible-notice) could not be met without that toolkit. Cutting it removes the single largest child-safety exposure in the product and returns the capacity to the recap and byproduct work that carries the retention thesis. It also **resolves a live contradiction** rather than leaving it for a future session to trip over (see Supersedes).

*On building the guardian tier behind a flag.* The owner challenged the original "do not build until counsel signs off" framing and was **right about half of it**: a coach INVITING the guardian email already on the player's roster row is **not a new trust decision** — that address is already on the player, already receives team email, and already receives tryout-offer links that identify the child. What IS new is (a) the standing view of a child's records a connected guardian receives, and (b) an unsolicited requester **asserting** a parental relationship the coach cannot truly verify. Those two are precisely what the counsel questions cover. Building now behind a hard switch converts sign-off from a build into a copy change and a flag flip, at the cost of one screen's rework if counsel reshapes the flow rather than the wording. That distinction has been added to the counsel packet as context on its verification question.

**Affects:** **No price, plan name, capacity band, SKU, gate or feature key changes — `PLAN_PRICING_FACTS.md` is unaffected by either ruling** (the guardian tier rides the same premium team entitlement as the rest of the family layer, on or off). Roadmap: Chunk D's remaining scope is **Slice 3 only** (coach byproducts — postgame email draft, player season recap, keepsake card, printable certificates, engagement counts, org adoption rollup). Positioning: the **January 2027 conversion story loses family chat** and now rests on practice visibility, the schedule families can finally see, and the season recap — a narrower claim than the original plan assumed, and the honest one.

**Handoff:**
HANDOFF → `/marketing` — **none yet, and one standing prohibition.** No family-layer copy until ship (per the 2026-08-01 retention entry). When it is written: **never imply family/parent chat exists or is coming** — the old G4 line promising "chat basics with any coach portal" is void and must not resurface in any comparison table, plan card or persona page. Also never imply live stats or public sharing.
HANDOFF → `/billing` — **none.** No gate moves. The guardian switch is an environment flag, deliberately **not** a plan gate and deliberately **not** a platform-admin toggle: a counsel-gated capability should require a conscious configuration change, not be one mis-click away from live.
HANDOFF → `/plan` — **DELIVERED:** `COACH_PORTAL_CHUNK_D_FAMILY_EXPERIENCE_PLAN.md` §5 records the cut with its reasoning and §9 carries the Slices 0–2 build record; `COACH_PORTAL_CHUNK_D_SLICE_3_BUILD_PROMPT.md` is written for the next session. PM brief updated to strike the chat promise.
HANDOFF → owner — **the counsel packet** (`COACH_PORTAL_CHUNK_D_COUNSEL_PACKET.md`) is ready to send. Its answers become a new entry here, and that entry is what turns the guardian switch on.

**Supersedes:** the **"family team-chat basics are included with ANY coach portal"** clause of the 2026-07-11 **G4** entry (annotated below). That clause had been flagged since 2026-08-01 as needing reconciliation against the premium-only ruling; the conflict is now **moot rather than resolved** — there is no family chat at any tier. G4's other half (practice visibility + richer family features ride Premium; no new SKU, no per-family fee) **stands unchanged**. Also closes the **team-chat-membership-for-verified-family** limb of the 2026-07-11 **G3** entry (annotated below); G3's verified-family links, practice visibility, verification on-ramps and safeguarding preconditions otherwise stand.

**Relates to:** the 2026-08-01 premium-only two-tier entry (this completes it — the guardian half now has a posture, and the chat question that entry left open is closed); the 2026-08-01 Chunk D retention entry (its no-public-sharing and no-pitch-by-pitch bounds are untouched).

---

### 2026-08-01 — The family layer is PREMIUM-ONLY, two-tiered (guardian vs family follower), parent-initiated with coach approval, and schedule access is a coach-controlled setting
**Status:** Decided (owner, 2026-08-01, ruled in two passes on the Chunk D discovery deliverables + mockups; mockups v3 approved same day = binding visual spec)
**Decision:** Four rulings that together settle how families connect to a team — closing the "is any family surface tier-gated?" question the 2026-08-01 retention entry left open:
1. **Premium portals only.** Every family surface in Chunk D (family schedule view, team link, game pages, calendar feed, recap delivery) rides the **Premium Coaches Portal / Club** entitlement. Free/Basic teams get none of it — the family layer becomes a concrete, parent-visible answer to "what does Premium get me," aimed squarely at the January 2027 conversion. (G4's separate "family team-chat basics with ANY portal" clause is NOT touched — it gets reconciled against this ruling when the chat slice is scoped.)
2. **Two tiers of family connection.** A **guardian** is player-linked, capped at **2 per player** (one per household), and is the accountable adult: money reminders, registration-related matters, the player's season recap/keepsake, the announcements archive, chat later. A **family member follower** is **team-level, never tied to a player** — schedule, results, game pages, calendar subscription, game-update notifications — and **uncapped as a product matter** (coach approves and can remove anyone; only an abuse ceiling applies). Supersedes G3's "~5 family accounts per player" cap. Because followers carry no child data, the follower tier may ship **ahead of** the guardian tier's PIPEDA/CASL counsel gate.
3. **Parent-initiated, coach-approved (the GameChanger model, minus public discovery).** The coach shares one revocable **team family link**; whoever opens it declares guardian-of-a-named-player or team follower, and lands in the coach's approval queue. **No public search for a team or child, ever** (G3 safeguard). Coach direct invites and league/club registration auto-verify remain as secondary on-ramps.
4. **Schedule access is a per-team coach setting:** Staff only / **Families (default)** / Public link. The standing no-login team page exists only at Public link; sharing an individual game page is always an explicit per-game coach act.
**Rationale:** the two-tier split matches how families actually work (one accountable adult; many people who just want to know where the game is) without squeezing grandparents through a child-data consent model built for guardians — the follower tier needs no child link at all, which both lightens the legal surface and lets the most-wanted piece (schedule following) ship first. Premium-only converts the layer from a giveaway into the renewal argument. Parent-initiated matches the incumbent behaviour families already know (GameChanger/TeamLinkt) while keeping the one boundary that matters for minors: the link only travels where the team sends it.
**Affects:** packaging (Premium Coaches Portal inclusions grow: family layer), gating (all Chunk D family surfaces on the premium team entitlement; no new SKU, no price change, no per-family fee — G4's posture holds), identity scope (a two-tier family relationship now exists in the product model), roadmap (follower tier before guardian tier; chat still last). `PLAN_PRICING_FACTS.md` change-log updated alongside this entry; config reconciliation happens at `/billing` wiring time.
**Handoff:**
HANDOFF → `/billing` (at Slice 1–2 build) — gate every family surface on the premium team entitlement (org-native Club teams pass via the org plan); no new keys beyond a family-layer feature flag if useful; reconcile `PLAN_PRICING_FACTS.md` + `lib/plan-config.ts` in the same unit of work and run the drift check.
HANDOFF → `/marketing` — **still none until ship** (per the 2026-08-01 retention entry). When written: the family layer is a **Premium** benefit; never imply live-stats; never imply public sharing.
HANDOFF → `/plan` — DELIVERED: `docs/projects/archive/COACH_PORTAL_CHUNK_D_FAMILY_EXPERIENCE_PLAN.md` (+ discovery §10 rulings record, PM brief, approved mockups artifact `2b0cbcab-9848-4a69-a0e6-a8c90cc85eb2` v3).
**Supersedes:** the ~5-accounts-per-player cap inside the 2026-07-11 G3 entry (annotated below); closes the "tier-gating left OPEN" item in the 2026-08-01 Chunk D retention entry (annotated below). G3's verification on-ramps and safeguarding preconditions otherwise stand; G4 otherwise stands.

---

### 2026-08-01 — Acquisition chrome is for PROSPECTS: Pricing + "Run a Tournament" leave workspace-holders' chrome; a customer's public pages never carry Pricing on phones
**Status:** Decided (owner, 2026-08-01 — top-nav repair rulings R11 + R9: "I agree with your recommendations"; the operator removal was owner-directed: *"should we just remove plans from the app so we don't taint the experience?"*)
**Decision:** Two rulings, one question — where does FieldLogicHQ sell itself inside its own product?
1. **Operators see no acquisition chrome.** For any signed-in user holding ≥1 workspace (org admin, coach, team workspace), the consumer strip's **Pricing link AND the "Run a Tournament" persona menu are removed**. Anonymous visitors and workspace-less accounts (prospects) keep both, unchanged. Operators reach plans where billing lives — their in-app plan & billing screen, which carries the one "Compare all plans →" door out to the pricing page. This **amends the WI-1/WI-2 state-based chrome** (Desktop Public UX Phase 1, ratified 2026-07-30) by adding the operator state to its matrix; it does not supersede the prospect-facing behavior, which stands.
2. **A customer's org public pages never show Pricing at phone widths — ratified as deliberate, not a gap.** The 2026-08-01 D1 phone-shed had removed it by side-effect while the recorded wording over-claimed ("the bar provides them"); the audit flagged it (finding D6). Ruled: the shed is correct — a paying customer's page is not our billboard (same boundary as the org-branding and phone-frame rulings) — and the recorded wording gets trued up to say Pricing is deliberately dropped, with Discover/Account/Sign-In genuinely provided by the bar. Acquisition on public surfaces stays where it already is: free-tier event pages (banner + powered-by).
**Also asked and answered (owner): is Pricing redundant beside "Run a Tournament" for prospects? NO — keep both.** Pricing is the low-commitment research door (price transparency is load-bearing for the self-serve free-tier motion); the persona menu is the action door. Removing Pricing would gate cost information behind a CTA — the "call us" pattern this platform deliberately doesn't run. Revisit only with click data (the existing nav-beacon pattern can measure Pricing-link usage first).
**Rationale:** an operator already pays or operates; a permanent sales link in their everyday chrome reads as being marketed to inside a tool they've adopted. Prospects are the audience acquisition chrome exists for. After this change the rule is one sentence: **prospects on FieldLogicHQ-owned surfaces see both doors; operators see neither; customers' branded pages advertise nothing.** Upgrade intent for existing customers is served at the point of action (billing screens; the now user-aware pricing page's deep-link CTAs — repair ruling R4).
**Affects:** navigation chrome only. **No price, plan name, capacity, gate, or SKU changes — `PLAN_PRICING_FACTS.md` untouched.** Positioning: sharpens the "we don't upsell inside the product" posture.
**Handoff:**
HANDOFF → `/plan` — none needed; both rulings are already work items in `docs/projects/active/TOP_NAV_REPAIR_PLAN.md` (R11 Phase 4, R9 wording true-up rides the build; audit evidence in `TOP_NAV_CONSISTENCY_FINDINGS.md` D6).
HANDOFF → `/billing` — none. No gates move.
HANDOFF → `/marketing` — none. No customer-facing copy changes; the wording true-up is internal docs/comments and rides the build.
**Supersedes:** nothing. Amends the WI-1/WI-2 chrome matrix (plan-level ratification 2026-07-30, `DESKTOP_PUBLIC_UX_PHASE1_PLAN.md`) and extends the 2026-08-01 phone-frame entry below with the Pricing ratification.

---

### 2026-08-01 — How much FieldLogicHQ sits on a customer’s public org pages: the app frame is PHONE-ONLY, and org section navigation is a League/Club benefit
**Status:** Decided (owner, 2026-08-01 — D1 ruled from a two-device mockup: *"for D1, I agree with the phone only approach"*; the tier gate accepted with the Stage F design verdict)
**Decision:** Two rulings, one question — how much of our chrome wraps a page a customer pays to make theirs.

1. **D1 — the app frame on org public pages is PHONE ONLY.** An org’s public pages (org home, league, teams, archives) now carry the FieldLogicHQ app bottom bar at **phone widths (≤900px)** and **no platform chrome above it at any width**. Desktop is untouched: the customer’s identity row stays the top of their own page. **Binding follow-on: adding the desktop strip later RE-OPENS this decision — it does not extend it.** A second wordmark above the customer’s name is the option that was rejected, not a later increment of the one that was chosen.

2. **Org SECTION NAVIGATION is a League/Club benefit.** The section tab row (Home / League / Archives) on an org’s public site is gated on `module_public_site` — League and above. Tournament and Tournament Plus keep the lighter breadcrumb instead.

**Rationale:** the gap D1 closes was a **price-ladder inversion**: a free-tier tournament family kept Home/Scores/Chat/Account throughout their visit, while a **League/Club** family lost the app the moment they left the org home — navigation quality running backwards against what customers pay. Full parity (our strip on top as well) fixes that but sandwiches a paying customer’s branded page between two FieldLogicHQ bars, and every design lens marked it down on brand alone — which is exactly why it was held as a packaging call rather than settled inside a navigation pass. Phone-only takes the half of the fix that costs the customer nothing visually: on a phone the club still owns the top of the screen, which is the part that reads as theirs. On the tier gate: on Tournament tiers the only possible entries are Home and Archives — a whole chrome layer to hold two words — and the 2026-07-31 ruling already established that the platform-preset org page is correct-by-design there. Section navigation is the same kind of thing as org branding: **it is what having standing programs between events looks like, so it belongs to the tiers that have them.**
**Affects:** no price, plan name, capacity, gate or SKU change. **`PLAN_PRICING_FACTS.md` untouched** — section navigation rides the existing `module_public_site` entitlement, so this confirms current gating rather than moving it. Positioning only: it adds a second concrete answer to "what does org-level actually get me?" alongside branding.
**Known gap recorded with this decision (not caused by it):** there is **no rep-teams index page**. `/{orgSlug}/teams` is a redirect shim, so the org home’s "Tryouts Are Open" card points at a known broken link, and "Teams" therefore ships as a breadcrumb label but **not** a tab. The tab row deliberately refuses to offer a destination that is only a redirect rather than launder that defect into navigation.
**Handoff:**
HANDOFF → `/marketing` — **optional, low priority.** No copy is wrong today. If the League/Club value story is ever restated, org **section navigation** is now a second concrete org-level benefit to name beside org branding (both ride the same entitlement). Nothing to change unless that page is being rewritten anyway.
HANDOFF → `/billing` — none. No gate moves; the row reads the existing `module_public_site` entitlement.
HANDOFF → `/plan` — **one real item: the rep-teams index page.** It closes the "Tryouts Are Open" broken link AND completes the section row (Teams becomes a tab in a single edit — both consumers read one table). Small, and it is the only thing standing between the current row and its intended shape.
HANDOFF → `/design` — none; both rulings came out of the 2026-08-01 `/design` gate and are logged in `memory/design_decisions.md` with the geometry and crumb-retirement rules.
**Relates to:** the 2026-07-31 "Org-level branding stays a LEAGUE/CLUB benefit" decision (this extends the same boundary from identity to navigation, and does **not** supersede it); the 2026-07-30 "Built on FieldLogicHQ" paid-credit decision (the same question — how much platform presence sits on a customer’s public surface — answered from the other side); the 2026-07-25 two-family experience model. Build detail: `docs/projects/active/NAV_UNIFICATION_PLAN.md` (D1 + Stages F/G). Mockup: `claude.ai/code/artifact/eef5768d-d2f4-4775-9194-03d23a5e39b2`.

---

### 2026-08-01 — The family experience (Chunk D) is a RETENTION play. No public sharing of a child's data at this stage, and we are not chasing GameChanger's pitch-by-pitch analytics
**Status:** Decided (owner, 2026-08-01 — *"coaches retention is fine, we don't need public sharing at this stage"*, and *"we do not intend to replace the game changer pitch by pitch analytics at this time"*)
**Decision:** Chunk D — the parent/guardian-facing set — is scoped as a **RETENTION** play, not an acquisition play. It exists to make coaches and clubs **keep** using FieldLogicHQ by making the coach's work visible to the families already attached to the team. Three bounds, all binding:
1. **Known audience only.** Delivery is to the coach and to guardians already on the roster. **Nothing that names a child leaves the authenticated product.**
2. **Public sharing is DEFERRED, NOT CANCELLED.** Recorded explicitly so a future session does not re-propose it as though it were never considered. Revisiting it requires a new entry here, and carries a consent model (per-family opt-in, likely an org-level policy switch) as a precondition.
3. **We are not competing with GameChanger on pitch-by-pitch analytics** at this time — no live play-by-play capture, no per-pitch/per-at-bat charting, no in-game scorer app in scope.
**Scope widened at the same time (owner):** the next session is a **discovery + strategy** exercise, not a build — evaluate the five ledger items, propose its own, study the **GameChanger / TeamSnap / TeamLinkt** parent models, and answer *what would actually entice a coach or an organization to choose us*. Brief: `docs/projects/archive/COACH_PORTAL_CHUNK_D_FAMILY_EXPERIENCE_DISCOVERY_PROMPT.md`.
**Rationale:** the two plays are different products, not two sizes of one. Retention keeps the value inside the paid relationship (a coach renews because the parents noticed; a club renews because its coaches would object if it didn't) and requires **no new exposure category** — guardians already receive dues reminders and announcements, so a card or a recap reaching them crosses no new line. Acquisition would require the opposite: a surface good enough for a stranger, therefore probably free, therefore marketing surface — and every child-level item would then need a consent model, because "shareable" plus "a named eleven-year-old" is a materially different legal and reputational posture. Choosing retention first is also the cheaper way to learn whether families care at all, and it is reversible; the reverse is not. **Verified while scoping and it sharpens the bet: no public surface in the product carries a player's name today**, so public sharing would be a first, not an extension. **Also verified: there is no parent/guardian IDENTITY** — a guardian is roster data (name/email/phone on a player row), not an account; fan accounts follow **teams**, never players. Every TeamSnap-shaped family feature depends on a link that does not exist, so the first question of the chunk is whether the platform needs a guardian identity and what it costs (PIPEDA/CASL), not which features to build. This also corrects the readiness review's judgement that Chunk D is "presentation work because the ingredients already exist" — true for team-level items, **false for anything naming a child**.
**Affects:** roadmap sequencing and scope for the last Coaches Portal chunk; **no pricing, packaging or gate change — `PLAN_PRICING_FACTS.md` untouched.** Whether any family-facing surface is tier-gated is deliberately left OPEN for the discovery session to recommend; a gating decision requires a new entry here. **[CLOSED 2026-08-01: the two-tier/premium-only entry above rules the family layer Premium-only.]**
**Handoff:**
HANDOFF → `/plan` — the discovery brief is written and committed. Plan + PM brief + approved mockups before any code (owner-mandated); `/review` at the **high-risk** tier because a family layer touches minors' data.
HANDOFF → `/marketing` — **none yet, deliberately.** No customer-facing claim about a family/parent experience should be written until the discovery session reports; in particular do not imply a GameChanger-style live-stats experience.
HANDOFF → `/billing` — none. No gate moves; revisit only if discovery recommends tier-gating a family surface.
**Relates to:** the 2026-07-20 D2 Coaches Portal self-serve acquisition decision (this is the retention counterpart on the same tier); `PROGRAM_COACH_PORTAL.md` §1.1 chunk D; the PIPEDA/CASL constraint on the family layer noted in `memory/MEMORY.md`; the 2026-07-30 CASL consent-unbundling decision (same posture toward families' data).

---

### 2026-07-31 — Org-level branding stays a LEAGUE/CLUB benefit. Tournament tiers are not orgs-with-a-brand, and there is no "club" at $39
**Status:** Decided (owner, 2026-07-31 — "no I don’t think we need to introduce org level benefits like this for them, that is reserved for leagues and clubs")
**Decision:** **Tournament ($0) and Tournament Plus ($39) do not get org-level branding** — no org logo, no org theme, no org hero banner on their `/{orgSlug}` address. That address keeps rendering in the platform preset. Their branding investment stays where their product is: **per-event**, which they already control fully and independently per tournament. Org branding remains a **League / Club / Club·Association** benefit, alongside the rest of the org-level public site.
**Vocabulary correction (binding):** there is **no "club" concept at the Tournament tiers.** A Tournament-Plus customer is a **tournament organization** that runs more than one tournament — not a club, not a league. "Club" is a tier name (and the shape of a real multi-program organization); using it loosely for any paying org is wrong in customer copy, help content, plans and mockups. Prior navigation write-ups and mockups from 2026-07-30/31 used "club" generically and have been corrected.
**Rationale:** the question arose from a navigation review, which observed that a Tournament-Plus org running 2+ events has a public `/{orgSlug}` page listing them — rendered in FieldLogicHQ navy with a platform badge above the customer’s own name — and asked whether that page should be brandable. The owner’s answer draws the line at the product boundary rather than the visual one: **a tournament organization sells events, and an event is the branded unit.** An org-level identity is what a league or club buys, because they have standing programs (house league, rep teams, archives) that persist between events. Granting org branding at $39 would blur the one distinction the ladder rests on, for a page most Tournament-tier orgs never render at all (a single active tournament redirects straight into that event).
**Affects:** no code change. Nothing shipped granted org branding — the 2026-07-31 navigation work added a Discover link (navigation, not identity) and made each event card wear **its own event’s** colour (the customer’s existing per-event branding, previously flattened to one org colour on that list — explicitly NOT an org-level benefit). **`PLAN_PRICING_FACTS.md` untouched:** this confirms the existing gating rather than changing it; `module_public_site` already sits on League and above.
**Handoff:**
HANDOFF → `/marketing` — **DELIVERED 2026-07-31.** The empty state read "This organization hasn’t set up their public site yet" under a FieldLogicHQ badge on a **paying** customer’s public address between events — blaming them for not configuring something these tiers structurally cannot configure, and naming a League/Club feature on a page where it is unavailable. **It also rendered broken:** the string sat in a JS expression rather than JSX, so its HTML escape was never interpreted and visitors saw the literal `&apos;` on screen. **Approved copy — empty state: "No tournaments are running right now."** (the Follow button already sits beneath it and carries the action; a follow-aware second sentence was rejected because that button does not always render). **Draft state: "A tournament is being set up. Registration opens when it is ready."**, replacing a pair of lines that said the same thing twice in two vocabularies ("publishes" vs "activates registration"), referred to "the organizer" in the third person on that organization’s own page, used internal jargon ("draft tournament pages are private"), and promised details "will appear here" when a newly-live tournament actually redirects visitors straight into the event. The duplicate notice was retired; its CSS rule is now orphaned and left for a cleanup pass (the file is contended by concurrent work).
HANDOFF → `/billing` — none. No gate moves.
HANDOFF → `/plan` — none. Closes the open packaging question raised in `docs/projects/archive/NAVIGATION_MODEL_PLAN.md`; that doc is corrected to record the decision rather than pose it.
HANDOFF → `/design` — none required. Noted for completeness: every org `/{orgSlug}` page is permanently dark at every tier (no org-level colour-mode setting exists), so a League/Club customer shipping a light-mode event page still hard-flips to dark on their own org page. Unrelated to this decision, unfixed, and only worth raising if org branding is ever revisited.
**Relates to:** the four-tier ladder in `PLAN_PRICING_FACTS.md`; the 2026-07-30 "Built on FieldLogicHQ" paid-credit decision (same question — how much platform presence sits on a customer’s public surface — answered from the other side); `memory/project_navigation_model.md`.

---

### 2026-07-30 — Tryout registration: marketing-email consent UNBUNDLED from applying (CASL)
**Status:** Decided (owner accepted the recommendation 2026-07-30 — "go ahead with your recommendations"; built the same day)
**Decision:** The public tryout registration form no longer makes email consent a condition of applying. **Required to submit (two boxes):** consent to data collection + guardian/eligibility confirmation. **Email consent is a separate OPTIONAL box, unchecked by default**, reframed as genuine marketing consent (org news + future-season announcements; wording via `/marketing` if it ever changes). **Tryout STATUS emails — offer, waitlist, release note, welcome — are transactional updates about the family's own application: sent regardless of the box (subject to the D-E9 decision-email switch on the org/coach side), and the form states this plainly.** The applicant export gained a **News-Email Consent** column so the record is auditable; rows consented before the unbundling carry all three confirmations as true.
**Rationale:** CASL disfavors making marketing consent a condition of a service that doesn't require it, and treats application-status email as transactional. The forced bundle was simultaneously a compliance risk, an unfriendly ask of families, and a polluted consent record (nobody could tell a genuine opt-in from a coerced one). Trade-off accepted: the marketing list only grows from real opt-ins — smaller, but usable.
**Affects:** the public tryout form, the applicant export/consent record, and every FUTURE marketing/news send to tryout families. **No pricing, plan, or gate impact — `PLAN_PRICING_FACTS.md` untouched.**
**Handoff:**
HANDOFF → counsel (via the already-open facilitated-payments session) — confirm the transactional/marketing split and the two required confirmations as framed; a cheap add-on question, not a blocker (the change moves in the safer direction either way).
HANDOFF → `/billing` — none.
HANDOFF → `/marketing` — none now; any future org-news/announcement feature that emails tryout families **must gate on the stored news-consent flag** (nothing reads it today, which is correct while no marketing sends exist — the first one to ship inherits this as a binding requirement).
HANDOFF → `/plan` — none; built 2026-07-30 (form + submit validation + export column + admin/coach help guides trued).
**Relates to:** D-E9 (decision emails opt-in everywhere tryout decisions are made — `memory/design_decisions.md` 2026-07-30 Chunk E entry + same-day admin amendment), the product-side sibling of this compliance-side ruling.

---

### 2026-07-30 — Paid-tier public tournament pages carry a subtle "Built on FieldLogicHQ" credit
**Status:** Decided (ratified by owner 2026-07-30 in the Desktop Public UX Phase 1 conversation — "built for fieldlogichq is good but should be subtle")
**Decision:** Public tournament pages on **paid org tiers** (Tournament Plus, League Plus, Club incl. Club · Association) — which today carry **zero** FieldLogicHQ presence — get a **quiet, text-only credit** in the page-footer region: "Built on FieldLogicHQ" (final wording via `/marketing`), at most a quiet link onward. **Binding constraint: SUBTLE** — no banner, no badge, no CTA block, small type, rendered in the org's own theme tokens so it defers to the org's branding. **Free-plan events are unchanged:** they keep the existing richer Powered-by badge + dismissible acquisition banner. The plain org home page (no tournament in URL) is in scope for the same treatment.
**Rationale:** Paid-tier tournaments are the platform's busiest, best-looking public surfaces and the 2026-07-30 desktop UX audit verified they are the only surface class with zero path back to FieldLogicHQ — inverting the funnel (the weakest events do all the selling). This trades a small slice of the paid "your brand only" feel for baseline acquisition presence, consistent with `BRAND_STRATEGY.md` §9's intent that a tournament "must feel like it belongs to a bigger platform." Pre-revenue, desktop web is the only acquisition surface — leaving the busiest pages unlinked is the costlier error.
**Affects:** Public tournament + org home page presentation on paid tiers only. **No price, plan-name, capacity-band, gate, or SKU change.** `PLAN_PRICING_FACTS.md` untouched — reconciled: nothing there (or in the inclusions log) records "no platform branding" as a sold paid inclusion, and this is compatible with `advanced_tournament_branding` (2026-07-11 G1 reframe), whose theming/custom-appName/branded-space inclusions are undiminished (the credit renders inside the org's theme).
**Handoff:**
HANDOFF → `/marketing` — final credit wording + ruling on whether it carries a quiet "Run your own tournament →" link or stands bare; verify no public copy (pricing, `/for-*`, branding feature page) promises "your brand only / white-label" in words that this credit would contradict (none found in the Facts doc during this log's reconciliation).
HANDOFF → `/billing` — **none.** No gate or plan-config change; plan-conditional presentation only.
HANDOFF → `/plan` — none needed; already scoped as WI-10 (Chunk C) in `docs/projects/archive/DESKTOP_PUBLIC_UX_PHASE1_PLAN.md` — **now unblocked** by this entry.

---

### 2026-07-30 — Budget starter: DECIDED. Structure only — the product never proposes a dollar figure; the full starter ships in one chunk
**Status:** **Decided** (owner, 2026-07-30 — ratifies the 2026-07-29 Proposed entry below; built as Coach Portal **chunk G** the same day)
**The two rulings:**
1. **D-G1 — STRUCTURE ONLY. The product never proposes a dollar figure.** The starter tells a coach **what** to budget for, never **how much**. The line, precisely: a number the **coach types** is their own and is fine — asking "roughly what does an entry fee cost you?" is legitimate, because they answer it. A number **the product supplies, prefills, or suggests** is not. Option (b) of the proposal — grounding suggestions in the platform's real tournament entry fees — was **considered and rejected**: costs swing hard by region, sport, age and level, and anchoring a coach low means they under-collect and a real family ends the season short. This binds ALL of Money: `budget_items.suggested_amount` stays NULL for every platform default, no numeric placeholders, no benchmark copy anywhere (including help docs). Reintroducing product-supplied figures requires a new entry here.
2. **D-G2 — the FULL starter in one chunk**, not the cheapest slice: the guided questions producing a real, editable starting budget **and** the clearly-labelled sample (a named fictional team, fenced, uncopyable — its invented numbers are the one sanctioned place dollars appear, ratified as build decision D1). The "ship the sample alone first" option was offered and declined.
**Build decisions ratified with it (owner, 2026-07-30, "I agree with all of your recommendations"):** sample carries fictional numbers behind a fence (D1) · five tap-only questions + the coach's-own-numbers ×N calculator (D2) · worksheet-as-preview, nothing writes until confirmed (D3) · lives on the empty Budget page + the Money hub anchor deep-links in (D4) · questionnaire retires once a budget exists, the derived "not in your plan yet" checklist is the permanent mid-season door (D5) · the sample is a permanent quiet reference (D6).
**Affects:** activation/retention on the paid tier only. **No price, plan-name, capacity-band, gate, or SKU change.** `PLAN_PRICING_FACTS.md` untouched; no pricing fact asserted by any starter or sample copy.
**Handoff:** `/marketing` — public copy may now say the Budget tool includes a guided starter, a standard checklist of what teams budget for, and a worked example; it may **never** promise suggested costs or benchmarks. `/billing` — none. `/plan` — chunk H (month-grid view, generalized payables, import/export templates) is the agreed follow-on, tracked in `PROGRAM_COACH_PORTAL.md` §1.1.
**Relates to:** the 2026-07-29 Proposed entry directly below (ratified as written, option (a) chosen).

---

### 2026-07-29 — Budget starter for first-season coaches: PROPOSED. Structure and a worked example, not invented dollar figures
**Status:** ~~Proposed~~ → **ratified 2026-07-30 at option (a) — see the Decided entry above** (kept for history per the no-delete rule)
**Proposal:** Build a **first-season budget starter** in the premium Coaches Portal. A coach answers a few plain questions (how many tournaments, roughly what each costs, any off-season training block) and lands on a real, editable starting budget — plus a clearly-labelled **sample** budget and sample budget-vs-actual so they can see what a finished one looks like before building their own. Tracked as **chunk G** in `docs/projects/active/PROGRAM_COACH_PORTAL.md` §1.1.
**Two constraints proposed as part of it:**
1. **Structure before numbers.** The volunteer coach's real question is *"what am I forgetting?"* — a missed umpire-fee or tournament-deposit line wrecks a season; being 10% off on a line does not. A checklist of what comparable teams budget for, with amounts left blank, is most of the value.
2. **Do NOT ship invented dollar figures.** Anchoring a coach low means they under-collect and finish the season short — a real harm to a real family, not a UX blemish. Real costs swing hard by region, sport, age group and competitive level.
**Rationale:** Budget is a **paid-tier feature** (the free Basic portal has a manual fee ledger only; a season budget, dues automation and budget-vs-actual are Premium). Making the paid tier's most intimidating surface approachable is an **activation and retention** play on a tier customers have already bought — not an acquisition claim, and not a reason to change what is sold. The gap is narrower than it first appears and that is what makes it buildable: **season rollover already carries a planned budget and fee template into year 2+**, so the coach facing a blank page is specifically the first-season one. It also extends two patterns the 2026-07-28 readiness review named as strengths to protect — the Money hub's always-name-one-next-action flow (which the review called the single best answer in the portal to feeling overwhelmed) and the Tryouts stage-by-stage flow (named the reference for explaining yourself to a first-timer) — rather than introducing a new idiom.
**⚠ THE DECISION THIS IS GATED ON — where suggested amounts come from.** Two honest options; the owner picks before any build:
- **(a) Structure only.** Ship the line-item scaffold with amounts blank. Zero risk of anchoring, ships soonest, leaves the "is this normal?" question unanswered.
- **(b) Ground suggestions in the platform's own data.** Tournament entry fees are real figures already in the system, so *"teams entering tournaments like yours typically pay $X–$Y per event"* is defensible, specific, and something no spreadsheet can tell a coach — a genuine differentiator. Costs more work and needs enough real data before it can say anything true.
A third option — inventing plausible-looking figures — is **rejected in this proposal** for the harm reason above, and should not be reintroduced without a new entry.
**Affects:** roadmap sequencing within the Coach Portal program (proposed slot: **after chunk A**, same surfaces, which should be good on a phone first). **No price, plan-name, capacity-band, gate, or SKU change proposed.** `PLAN_PRICING_FACTS.md` untouched — no pricing fact is involved, and none may be asserted by any starter copy. **Nothing customer-facing changes until ratified.**
**Handoff (all conditional on ratification — do nothing yet):**
HANDOFF → owner — rule on the gating decision (a) or (b) above. Nothing proceeds without it.
HANDOFF → `/plan` — **do not** write an implementation plan yet. Chunk G's shape is recorded in `PROGRAM_COACH_PORTAL.md` §1.1; it needs the gating decision, its own plan + PM brief, and approved mockups.
HANDOFF → `/marketing` — **no public copy may promise budget templates, suggested costs, or benchmarks** until this is ratified and built. If option (b) is ever chosen, any benchmark claim needs its own accuracy review before it appears in customer-facing copy.
HANDOFF → `/billing` — none. Budget stays where it is; this proposes no new gate.
**Relates to:** 2026-07-20 D2 Coaches Portal self-serve acquisition (the tier this would improve retention on); the 2026-07-28 premium portal readiness review (this is **not** one of its 85 findings — it is an owner-raised addition, recorded as such so a future reader does not go looking for it there).

---

### 2026-07-28 — Native mobile app: KILLED. The installed web app is the mobile strategy; no store build, no companion app
**Status:** Decided (ratified by owner 2026-07-28 in conversation — "go ahead and kill the native mobile app and log it")
**Decision:** The **native/companion mobile app is killed.** The strategy brief (mission, level-of-effort, store path, MVP scope of Home-Today / scorekeeper view / alerts) is retired — **no implementation plan will be written and none is wanted.** The **installed web app (PWA) is the mobile strategy**, full stop. Revisit only via a *new* decision entry, not by anyone re-reading the old brief.
**Rationale:** The brief has sat unadvanced since early June while the installed web app absorbed essentially all of its intended scope: home-screen install, push notifications (confirmed delivering on production 2026-07-28), offline fan experience, and role-routed navigation via The Flip. A store build would add app-store review cycles, a second release train, platform fees, and per-OS QA — recurring costs paid out of a solo-dev capacity that has higher-value work queued (Coach Portal readiness, the Standings remodel, the January billing conversion). Nothing in the brief is currently unreachable on the web app. **This does not reverse the 2026-07-11 store-wrap decision** — that entry already made a store presence conditional on pre-agreed triggers and Android-first; this entry closes the *separate* "build a native companion app" track and makes explicit that no work proceeds absent those triggers.
**Affects:** roadmap, engineering capacity allocation. **No price, plan-name, capacity-band, gate, or SKU change. Nothing customer-facing changes.** `PLAN_PRICING_FACTS.md` untouched (no pricing fact involved).
**Handoff:**
HANDOFF → `/plan` — **do not** write a mobile-app implementation plan. The strategy brief is archived, not pending.
HANDOFF → `/marketing` — no public copy currently promises a native app; **do not introduce any**. "Install to your home screen" stays the mobile message.
HANDOFF → `/billing` — none.
**Relates to:** 2026-07-11 store-wrap on pre-agreed triggers, Android first (unchanged and still the governing entry for any future store presence); 2026-06-28 mobile app + public tournament discovery (already superseded by the 2026-07-11 unified-app decision — this closes its remaining native-app thread); 2026-07-11 unified app RATIFIED (one install identity — the PWA that absorbs this scope).

---

### 2026-07-28 — League & Club: the managed early-access cohort is WITHDRAWN; League is parked pending a full capability evaluation
**Status:** Decided (ratified by owner 2026-07-28 in conversation — "league is just overall not ready for production, you can remove this item and I will later start a project to do a full evaluation of the league capabilities once I finalize the coach portal work")
**Decision:** Four parts.
1. **The ~August 2026 managed early-access cohort is WITHDRAWN** (5–10 Club, 10–15 League under the Founding-Season comp). Not re-dated — removed from the roadmap. The 2026-06-22 entry that ratified it is superseded.
2. **League is parked as a whole.** It re-enters as a **full League capability evaluation**, owner-started, **after the Coach Portal work is finalised**. No launch decision is open meanwhile and no readiness/hardening work is scheduled.
3. **Availability is unchanged and stays unchanged:** League Plus, Club, and Club · Association remain **early-access / non-purchasable**; the **free League floor stays `Held` with its flag OFF in production**. Nothing is to be flipped while parked.
4. **The cohort shape is preserved as an input, not rejected.** 5–10 Club / 10–15 League, invite-only, founder-managed, under comp — that shape re-enters the future evaluation as a starting proposal if the capability clears the bar.
**Rationale:** The 2026-06-22 decision made the open date contingent on a readiness checklist rather than the calendar; the checklist was written and **never walked**, so "is League ready?" has never had an evidence-based answer. What *is* known does not support opening: house-league in-season work was never started and carries live defects — most seriously, **waitlist/pending audiences silently email nobody while reporting a green "0 delivered" success**, provider failures are counted as sent, and there is no guardian dedupe, reply path, or rainout notification. A league admin cannot presently trust that a message reached parents, which alone makes the product unfit for a real season. Two further checklist dependencies (Billing & Accounting Coherence; Admin IA & Multi-Module Nav) are also unstarted, and **no Club has ever been provisioned end-to-end** through to a full coaching staff in populated portals. Parking is therefore consistent with the original ratification, not a reversal of it. Inventory preserved at `docs/projects/active/PROGRAM_LEAGUE_AND_CLUB.md`.
**Commercial consequences (recorded deliberately):**
- **The top of the ladder stays unvalidated.** Club $219 · Club · Association $379 · League Plus $89 remain untested against a single paying customer — precisely what the cohort existed to fix. Willingness-to-pay on the highest-ACV tiers stays a hypothesis, and the 2026-06-22 Club band/reprice/whole-staff decisions stay unvalidated with it.
- **Near-term acquisition narrows** to Tournament, Tournament Plus, and the Coaches Portal. The 2026-06-22 GTM decision (serve all club sizes via structure; focus acquisition on mid-size clubs) is **not reversed**, but its club-facing half is unexecutable while Club is unsellable — treat mid-size-club acquisition as **suspended, not abandoned**.
- **Club Repackaging is complete, live on prod since 2026-06-24, and currently sells to nobody** — built value, parked.
- **The January 2027 conversion gets simpler:** there is no League/Club early-access cohort to convert.
- **Facilitated payments** attaches to Tournament Plus, League Plus and Club (2026-07-28). Tournament Plus is unaffected; the League/Club attachment points move out with this park. No change to that decision.
**Affects:** GTM sequencing, roadmap, segment focus. **No price, plan-name, capacity-band, gate, or SKU change.** Reconciled against `PLAN_PRICING_FACTS.md` — **zero pricing drift**: the table already records League Plus / Club / Club · Association as non-purchasable early-access and the free League floor as `Held`. Only the *reason* for "Held" changes (parked, not awaiting imminent launch), trued in the Facts doc in this same unit of work.
**Drift catches (recorded per the /strategy drift duty):**
1. **Stale-by-implication:** `/pricing`, the home page, the comparison table, `/for-leagues`, and the org billing page all describe League Plus and Club as **"coming soon" / "available soon — express interest."** Dateless, so nothing is broken today, but "coming soon" decays into a false promise the longer the park runs. → `/marketing` to rule on how long it may stand.
2. **Genuine live contradiction, independent of this park:** the `/pricing` closing CTA reads *"League Plus, Club, **and the Coaches Portal** are coming soon — express interest to be notified."* The **Coaches Portal has been live for self-serve acquisition since 2026-07-23** (2026-07-20 decision D2; the Facts doc records it purchasable at $0 comp). The pricing page tells visitors they cannot yet buy something they can buy today. → `/marketing`, fix independently of the park.
**Handoff:**
HANDOFF → owner — start the **full League capability evaluation** once the Coach Portal work finalises; the defect inventory and dependency status are held in `PROGRAM_LEAGUE_AND_CLUB.md`. **One check while parked:** confirm no organization holds house-league access via a comp/override — League and Club are non-purchasable and the free-floor flag is off in production, so the communications defect should be unreachable, but a comped org would be exposed.
HANDOFF → `/billing` — **no action; explicitly do NOT flip.** League Plus / Club / Club · Association stay `early_access`; the free League floor flag stays OFF in production.
HANDOFF → `/plan` — the 2026-06-22 handoff ("define the ready-to-open go/no-go checklist + hardening scope") is **WITHDRAWN**. Do not schedule readiness or hardening work. The future evaluation is owner-initiated and will produce its own plan + PM brief.
HANDOFF → `/marketing` — (a) rule on how long "coming soon" may stand for League Plus and Club, and soften toward timeline-free interest capture if the park will run months; (b) **fix the pricing-page CTA that still lists the Coaches Portal as coming soon** — that one is wrong today.
**Supersedes:** **2026-06-22 — Open League + Club to managed early-access after a product-hardening sprint (~Aug 2026)** — its cohort shape survives as an input to the future evaluation; its open-date intent and `/plan` checklist handoff are withdrawn.
**Relates to:** 2026-06-22 League naming (free "League" / paid "League Plus" — unchanged; the parked rename work rides with the park); 2026-06-22 Club capacity bands + Club reprice to $219 + whole-staff-in-Club (all live in config, all still unvalidated against a real club); 2026-06-22 GTM mid-size-club focus (club half suspended); 2026-07-28 facilitated payments (League/Club attachment points move out); 2026-07-20 Founding Season (no League/Club cohort to convert in January).

---

### 2026-07-28 — Premium coach portal points standalone teams at the public /discover directory (Coach Portal Launch Batch 1, D4); discovery-directory project docs trued up to SHIPPED
**Status:** Decided (ratified by owner 2026-07-28 in conversation — batch decisions D1–D4 accepted at the recommendations, then "discovery should be live and not in planning… we should be good to point there. go ahead and log in strategy and go ahead with implementation, mockups approved")
**Decision:** The premium Coaches Portal team-Tournaments **empty state for standalone/workspace teams with no registrations** carries a secondary (ghost-styled) **"Browse public tournaments" CTA into the live `/discover` directory**. The primary action stays the how-registering-works guide; the directory CTA is deliberately secondary. The org-owned-team empty state does **not** point at the directory — its honest path is the org admin linking the registration. This is the first paid-portal surface that funnels coaches into the public directory.
**Rationale:** The directory is live on prod (2026-07-22 Unified Home bundle) with **opt-out listing** (see the 2026-07-22 discoverable-by-default entry), so it is a real, growing destination — and a standalone premium coach staring at an empty Tournaments page is precisely the "find an event to enter" moment. The funnel feeds the flywheel from both sides: coach browses → finds an org's event → registers (email-matched, auto-appears in their portal) → organizers see platform-side demand. Ratified as decision D4 of Coach Portal Launch Batch 1 (`docs/projects/archive/COACH_PORTAL_LAUNCH_BATCH1_PLAN.md`; owner-approved mockup artifact = binding spec, 2026-07-28).
**Drift catch (recorded per the /strategy drift duty):** `PUBLIC_TOURNAMENT_DISCOVERY_DIRECTORY_PLAN.md` + `_PM_BRIEF.md` still carried **"Status: Planning"** headers while the surface has been live on prod since 2026-07-22 (SSR + SEO, tournament-keyed, sitemap/robots, Event Settings toggle). Headers trued up 2026-07-28, including the opt-in→opt-out supersession already logged 2026-07-22. Full phase-checkbox reconcile + archive move of that plan = open follow-up (small, non-blocking).
**Affects:** coach-portal empty-state surface + a new cross-surface acquisition funnel; directory-project doc status. **No price, plan-name, capacity-band, gate, or SKU change; `PLAN_PRICING_FACTS.md` untouched (no pricing fact involved).**
**Handoff:**
HANDOFF → build (Coach Portal Launch Batch 1, in flight 2026-07-28) — implement per the approved mockup spec; in-product empty-state wording is owner-approved via that spec.
HANDOFF → `/docs` — coach tournaments help gains the "find events in the public directory" pointer for standalone teams (rides Batch 1's existing help-sync task).
HANDOFF → `/marketing` — none (no public/marketing surface changes; in-product copy covered by the approved spec).
HANDOFF → `/plan` — none (already planned + ratified inside Batch 1).
**Relates to:** 2026-07-22 tournaments discoverable by default (the supply base this funnel feeds); 2026-06-28 mobile app + public tournament discovery (directory as top-of-funnel — this adds a paid-portal on-ramp); 2026-07-27 pressure ladder (not implicated — paid-portal surface, and the CTA sells nothing).

---

### 2026-07-28 — Facilitated payments: the Q4 scoping questions are substantially PRE-ANSWERED by the owner-commissioned investigation; take-rate model resolved (margin-on-top, expressed as a market-band all-in rate); direct-charges architecture Proposed; tier attachment = OPEN owner ruling
**Status:** Mixed, deliberately: **take-rate MODEL resolution = Decided** (it executes the 2026-06-22 entry's own mandatory handoff, and the evidence leaves only one viable reading); **tier attachment = Decided** (ratified by owner 2026-07-28 via decision prompt — "tournament plus, club and league"); **money-flow architecture = Decided** (direction ratified by owner 2026-07-28 in conversation — "we go with the direct model, we never hold the funds"; counsel sign-off remains the hard launch gate confirming the regulatory reading); **rate number + rails + who-pays = Decided** (Option B rail-differentiated launch anchor, ratified by owner 2026-07-28 in conversation — "I agree with option B, log it"); **risk posture (payout holds / reserves / ToS) = Proposed** (locks at build with counsel input). Owner accepted the investigation report and directed the follow-through 2026-07-28 in conversation ("go ahead").
**Decision:**
1. **Take-rate model (Decided — resolves the "~2.5% + $0.30" ambiguity flagged 2026-06-22):** FieldLogicHQ's take is a **margin layered into a market-band all-in rate charged on each transaction — NOT an all-in ~2.5%**, which is underwater on every card transaction (Stripe Canada base card cost confirmed live at 2.9% + $0.30 CAD). The verified market band for youth-sports registration processing (primary-source confirmed 2026-07-28): TeamSnap 3.25% + $1.50 · SportsEngine HQ 3.25% + $2.00 (atop a $69/mo sub) · Jersey Watch 3.5% + $1 · Tourney Machine 2.9% + $1 · RAMP "under 2%" org-absorbed (corroborated, not primary-confirmed). **Rate DECIDED (ratified 2026-07-28 — Option B, rail-differentiated launch anchor, recalibratable from real conversion/rail-mix data): cards 3.5% + $1.50 all-in · bank debit (ACSS/PAD) 2.5% + $1.00 all-in · family-pays default; the organizer always nets the full team fee.** The visible bank discount steers families to the profitable rail (on an $800 fee: card nets FieldLogicHQ ≈ $3.14, bank ≈ $14.00; a single-rate 3.25% + $1.50 anchor would net a structurally flat ≈ $1.20/txn on cards — full unit-economics table in the scoping plan §1). **Open riders on the number (implementation of it, not reopening it):** (a) fee displayed "+ GST/HST" vs tax-inclusive — accountant call with real margin stakes (tax-out-of-fee turns cards negative); (b) international-card (+0.8%) surcharge-or-absorb policy for US teams; (c) whether orgs may opt to absorb the fee (org-absorb override).
2. **Rails (Decided with the rate):** cards via Stripe (2.9% + $0.30 cost) **plus Canadian pre-authorized debit (ACSS/PAD) at 1% + $0.40 capped at $5.00** — on a $1,000 team fee that is ~$5 of cost vs ~$29 by card, so steering large fees to PAD is the margin engine at an unchanged all-in rate (trade-off: ~5-business-day settlement).
3. **Architecture (Decided — ratified by owner 2026-07-28: "we go with the direct model, we never hold the funds"; counsel sign-off remains the hard launch gate on the regulatory reading):** Stripe Connect **direct charges on organizer-owned connected accounts** (Express-style hosted onboarding; Stripe collects KYC), with FieldLogicHQ's take collected as an application fee. Chosen over destination charges because the Bank of Canada's marketplace guidance (Dec 2025) turns on whether funds ever land in an account the platform **owns or controls**: direct charges match the "not a PSP, no RPAA registration" scenarios; destination charges match the must-register scenario — and destination charges additionally make the platform eat every organizer's chargebacks. FINTRAC/MSB is a **separate regime with no on-point guidance** — unresolved either way. **Counsel sign-off remains the hard launch gate** (unchanged from 2026-06-22); briefing memo ready at `docs/projects/active/FACILITATED_PAYMENTS_COUNSEL_BRIEF.md`.
4. **Risk posture (Proposed):** payouts held until post-event (standard ticketing-industry practice; the tournament-cancelled-after-payout scenario is the classic blowup), rolling reserves on new organizer accounts, and three-party ToS making the organizer the merchant of record who owns refund/cancellation policy and indemnifies the platform. Fee shown before checkout (fee opacity is the market's #1 complaint theme — a differentiation opportunity, not just hygiene).
5. **Tier attachment (Decided — ratified 2026-07-28 via decision prompt):** payment processing attaches to **ALL PAID ORG TIERS — Tournament Plus, League Plus, and Club (incl. Club · Association)**. Free org tiers and the Coaches Portal (not an org product) are excluded. Rationale: the MVP flow (tournament registration) is a Tournament Plus surface; League/Club inherit for dues and league flows; the take-rate keeps the feature revenue-positive wherever enabled while preserving an upgrade reason for free-tier orgs. Recorded in `PLAN_PRICING_FACTS.md` as a Decided/not-yet-built line (same unit of work).
6. **Sequencing (unchanged, restated):** launch **H1 2027, after the Jan 2027 billing cliff** — the 2026-06-22 sequencing rationale stands. Building earlier is permitted; **launching** earlier requires amending this entry. Effort is bounded: MVP ≈ 3–4.5 weeks, full feature ≈ 6–10 weeks solo-dev+AI (workstream model in the scoping plan).
**Rationale:** Owner-commissioned 2026-07-28 investigation (8-agent research workflow + independent primary-source fact-check: codebase state, money-flow inventory, strategy record, Stripe Connect Canada pricing, competitor rates, Canadian compliance). Key inputs: zero in-app participant→org payment infrastructure exists (everything is manual bookkeeping + e-transfer instructions); the existing subscription-billing stack provides reusable patterns but no marketplace wiring; the Registration Command Center already computes payment status, so collected payments feed an existing surface. Full findings folded into `docs/projects/active/FACILITATED_PAYMENTS_SCOPING_PLAN.md` (updated 2026-07-28).
**Affects:** monetization-model detail and roadmap. **No price, plan-name, capacity-band, gate, or SKU change today; nothing customer-facing changes.** PLAN_PRICING_FACTS.md untouched by design — the rate and tier attachment enter the Facts doc in the same unit of work as their ratification, not before.
**Handoff:**
HANDOFF → owner — (a) ~~rule on tier attachment~~ **done 2026-07-28** (all paid org tiers); (b) book the fintech-counsel session using `FACILITATED_PAYMENTS_COUNSEL_BRIEF.md` (the long-lead item — RPAA/FINTRAC/ToS); (c) accountant confirmation that GST/HST applies to the platform fee.
HANDOFF → `/plan` — write the build plan + PM brief once counsel lands (Phase-0 decisions lock first; MVP slice = tournament registration fees feeding the Registration Command Center, hybrid with the manual mark-paid path; gate at the three paid org tiers).
HANDOFF → `/billing` — none yet (nothing built or gated). At build: Connect webhooks are a separate endpoint from subscription billing; the take-rate + tier gate enter `PLAN_PRICING_FACTS.md` + plan config in the same unit of work, then run the drift check.
HANDOFF → `/marketing` — none until launch nears; no public copy may promise payment collection before the counsel gate clears.
**Relates to:** 2026-06-22 facilitated-payments direction (this executes its scoping handoff early); 2026-06-22 Club capacity-bands ("later, payment volume" as a Club-size dimension — relevant to the tier-attachment ruling); 2026-07-20 Founding Season (the cliff the launch stays behind).

---

### 2026-07-27 — Knowing your game moved is FREE; the phone buzz is the paid part. Schedule-change alerts ride the existing follow system (confirms the 2026-07-06 fan-push decision covers coaches)
**Status:** Decided (ratified by owner 2026-07-27 in conversation — B2 planning decisions ◆Q/◆R/◆S accepted as recommended: "I agree with your recommendations")
**Decision:** Three parts, governing Free Coach Portal **Phase B2 (coach alerts)** — none of which changes a price, plan name, capacity band, or SKU.
1. **The in-portal schedule-change notice is FREE on every plan, always (◆R).** When an organizer moves or cancels a game, the coach's own schedule inside the portal says so — on a free organizer's event exactly as on a Tournament Plus one. **Push notification remains gated at Tournament Plus; *being informed* is not gated at all.** The distinction: the paid feature is the buzz in your pocket, not the fact. A coach on a free event finds out when they open the app.
   - **Why this is a real decision and not a detail:** the first-tournament coach persona overwhelmingly enters **free** organizers' events, so "the organizer isn't on Tournament Plus" is the **common case**, not an edge case. Gating the in-app notice too would mean the portal *knows* a coach's game moved and deliberately says nothing — the single most corrosive thing the free tier could do to the "companion" positioning ratified 2026-07-25.
   - **Honesty requirement (binding):** where the organizer's plan means we can't send to a phone, the portal says so plainly and **does not aim an upgrade prompt at the coach** — the coach is not the party who could buy it. Pitching them would be both useless and a pressure-ladder breach.
2. **Schedule-change alerts ride the EXISTING follow system, not a new coach-only channel (◆Q) — and this was already decided.** `PLAN_PRICING_FACTS.md` carries a **Decided / not-yet-built** line from **2026-07-06**: *"Fan tournament-message push = Tournament Plus — new anonymous-fan push for organizer day-of messages **+ schedule changes** gates at `tournament_plus`."* **B2's hard half IS that item.** No new SKU, no new gate, no second capability: coaches ride the follow they already have, and **families following the same team are served by the same send**. A parallel coach-only channel was rejected — the org notification system is org-membership-shaped and free coaches have no organization, so it would need new plumbing *and* still leave families with nothing.
   - **Material re-base worth recording:** as of A3 (2026-07-27) **every signed-in free coach whose event is live is already following their own team at the account level** — the automatic team-highlight mirrors the follow to the account, not just the device. This was an unintended side effect, and it means the coach-reach half of B2 is largely already built. What's missing is that nobody has told them, and that **nothing in the product fires on a schedule change at all** — for coaches *or* fans.
3. **Alerts carry no ask (inherited, restated here for the build).** Per the pressure-ladder entry below, a schedule-change alert is a **service** message: no Premium mention in the push, the email, or the in-app notice.
**Rationale:** B2 was scoped as "coach alerts" and is really the platform's largest unclosed notification gap — a single-game edit currently notifies nobody at all, including a fan who deliberately followed the team and opted in. Framing it as one capability serving coaches and families (rather than a coach-tier feature) closes a bigger hole for less work and uses a gate that was already decided a month ago. Making the in-app half free is what keeps the free portal honest during Founding Season, when the acquisition funnel matters more than the gate. Full verification + sketches: `docs/projects/active/FREE_COACH_PORTAL_EXPERIENCE_PLAN.md` (B2 RE-BASE section) and artifact `claude.ai/code/artifact/0b2839a8-5d32-437f-a7a1-e21cfa0c70a0`.
**Affects:** gating (one new **free** in-app behaviour; the push half uses the already-decided `tournament_plus` gate), positioning of the free Coaches Portal, and B2's build sequencing. **No price, plan-name, capacity, SKU, or new gate key.** Reconciled against `PLAN_PRICING_FACTS.md` — zero drift; the 2026-07-06 line already covers the paid half.
**✅ Operational blocker CLEARED 2026-07-28.** Android installed-PWA push had delivered nothing on production since 2026-07-04. The owner ran the self-serve **"Test this device"** diagnostic on prod and it **delivered successfully** — the channel works. (The 2026-07-04 diagnosis was that the break sat between server send and device delivery, i.e. configuration; whatever fixed it, prod push is now confirmed working end-to-end from a real device.) **B2.3 is unblocked.** Re-test before any release that depends on push, rather than assuming this result holds.
**Handoff:**
HANDOFF → owner — run **"Test this device"** on production (Account → Notifications) before B2.3 is built. Five minutes; determines whether this is a redeploy or a key-pair fix.
HANDOFF → `/plan` — B2 implementation plan + PM brief, in the accepted order: B2.1 (free, unblocked) → B2.3 (the send) → B2.2 (the panel) → the stale-reminder-email fix (◆S1) riding with B2.3. Carry the three undecided design inputs: **batching** (an organizer can move 20 games in 10 minutes — one message, not twenty), **horizon** (three weeks out vs two hours out), and a **privacy look** before the first *operational* push to a family-side follower.
HANDOFF → `/billing` — none. Confirm at build that the push half reuses the existing `fan_score_alerts`-family gate rather than adding a key; run the drift check.
HANDOFF → `/marketing` — at build only: the free-event "we can't send to your phone on this event" wording must state the limit plainly with **no upgrade CTA aimed at the coach**.
**Relates to:** 2026-07-06 fan tournament-message push = Tournament Plus (this confirms its scope covers coaches and schedules its build); 2026-07-27 pressure ladder (the no-ask rule alerts inherit); 2026-07-25 two-family model (the "companion" promise this protects); 2026-07-20 Founding Season Coaches Free (why the free half matters now).

---

### 2026-07-27 — The PRESSURE LADDER is promoted to a binding, centrally-recorded rule: the game weekend never sells. Free-portal ask surfaces enumerated; Free Coach Portal success criterion 6 corrected
**Status:** Decided (ratified by owner 2026-07-27 in conversation — B3 mockup decisions ◆L1 and ◆P1 accepted as recommended: "go ahead with your recommendations")
**Decision:** Three parts.
1. **The pressure ladder becomes a first-class, binding commercial rule and is recorded HERE for the first time.** Until today it existed only inside a project plan (`FREE_TIER_STRATEGY_PLAN.md` §D5, "in-portal pre-event surfaces stay pitch-free; the one earned ask is at the afterglow") and a code comment on the team Overview ("never on the tournament variant — game day stays pitch-free — locked"). That is why it was nearly broken by accident (see Rationale). **The rule: the Basic Coaches Portal never pitches Premium on a pre-event or game-day tournament surface. The tournament weekend is not an advertising surface.**
2. **Where an ask IS permitted — the complete, enumerated list** (this had drifted in practice and was written down nowhere central):
   - **The post-event afterglow** on the tournament record — **the one earned, pushed ask.** Free tier only; suppressed for a paying coach (the record is a shared surface across tiers).
   - **Team-tools pages**, via the quiet info-first shelf (never a pushed interstitial, never a modal): Roster · Schedule · Fees · Announcements · Overview · Explore, and **as of B3** the team Tournaments list at **2+ non-rejected entries**.
   - **Nothing else.** No pre-event tournament surface, no game-day surface, no live schedule, no chat.
   - **Chat is explicitly and permanently out of scope for upsell** — since the A2/A3 re-chrome the coach's chat is the app-wide Chat tab, shared with fans and with paying coaches. Any pitch there is an audience/tier leak, not merely a ladder violation.
3. **Free Coach Portal success criterion 6 is CORRECTED.** It targeted "≥2 natural Premium moments per tournament lifecycle" for the single-team persona. That target and the pitch-free promise cannot both be true, and chasing the number is what produced the game-day proposal. **Restated and accepted:** a coach who registers, plays and never opens team tools gets **ONE pushed moment** (the afterglow) plus **Explore permanently one tap away in the section tab row but never pushed at them**. Coaches who do open team tools meet the quiet shelf on the pages they actually use. Impressions are no longer a success measure for this program; **the measure is that the one ask lands at a moment of felt need, in language about what the coach just did.**
**Rationale:** B3's own plan section — written by the 2026-07-25 audit, before A2/A3/B1 shipped — proposed a Premium lineup pitch under the live schedule on multi-game days. It reached build planning before anyone noticed it contradicted a rule the product has deliberately kept since the Coaches Experience work. **A rule that lives only in a project plan and a code comment is a rule that gets broken by the next person who reads neither.** The owner's call was to hold the line and drop the pitch rather than grant an exception, on the reasoning that the moment it targets is the hour a coach is least able to act on anything, and that the clean weekend is a large part of why the free portal reads as a tool rather than a funnel. Founding Season ($0 through 2027-01-01) makes the trade cheap in 2026: the cost of a quieter funnel this season is a season of feedback, not revenue — which is the strategy already chosen. Full re-base evidence + the mockups carrying ◆K–◆P: `docs/projects/active/FREE_COACH_PORTAL_EXPERIENCE_PLAN.md` (B3 section) and artifact `claude.ai/code/artifact/0a4a74e5-d07b-471c-a62b-325c8b751a23`.
**Reopening clause (binding):** the game-day / pre-event pitch may be revisited, but **only by amending this entry** — never by a component-level exception, a "just this once" flag, or a plan section that doesn't cite it. Any future plan proposing an in-tournament upsell must reference this decision and argue against it explicitly.
**Affects:** positioning and in-product monetization posture for the Basic Coaches Portal; the Free Coach Portal program's success criteria. **No price, plan name, capacity band, gate, SKU, or packaging change** — reconciled against `PLAN_PRICING_FACTS.md`, zero drift found.
**Handoff:**
HANDOFF → `/marketing` — none outstanding. B3's in-portal copy is already logged as the **2026-07-27 amendment** at the bottom of `PRICING_PAGE_COPY.md`, which also adds four verified claims to the approved coach upsell vocabulary: *assistant coaches with their own sign-in*, *season history across every event*, *playing time*, *team awards* (each checked against a shipped Premium surface before use). Future coach upsell copy draws from that vocabulary.
HANDOFF → `/billing` — none (no gate or price change). Note only: every B3 price/date line reads from the existing Founding Season promo gate, so no new pricing surface was created.
HANDOFF → `/plan` — when B2 (coach alerts) is planned, it inherits this rule: a schedule-change alert is a **service** message and must never carry a Premium ask, on any channel.
HANDOFF → owner — the ladder's enumerated ask-surface list above is now the reference. If a future surface wants an ask, it gets added here first, not shipped first.
**Relates to:** 2026-07-25 two-family experience model (the free portal as "tournament companion" — this is the commercial rule that keeps the companion honest); 2026-07-20 Founding Season Coaches Free (the $0 posture that makes the trade cheap in 2026); 2026-06-22 Coaches Portal packaging (untouched).

---

### 2026-07-25 — Two-family experience model: Basic Coaches Portal joins the CONSUMER app family ("tournament companion"); Premium Coaches Portal stays in the OPERATOR family ("team operations HQ")
**Status:** Decided (ratified by owner 2026-07-25 in conversation — "I agree with this, go ahead and run strategy")
**Decision:** The platform's experiences consolidate from three navigation languages (public/consumer, admin, coach) to exactly **two families**, and the two coach tiers are deliberately split across them:
- **CONSUMER family** — fans + **Basic (free) Coaches Portal**. The free portal re-chromes into the one consumer app: the global consumer bottom nav (Home · Scores · Chat · Account) everywhere, a persistent event/team-style header carrying identity + the Flip control top-right (matching admin/live-site convention), and portal sections in a horizontally-scrolling top tab row (the same pattern the 2026-07-20 tournament-nav-merge decision established for public tournament pages). The free portal presents as a **lens over the tournament the coach signed up for**, inside the one app — not a separate third app. Feasibility verified in code before ratification: the consumer Chat tab already lists the basic-coach team room (same room-resolution path), and consumer Home already renders a "Coaches Portal" workspace card back to the team (direct link for single-team coaches).
- **OPERATOR family** — org admins + **Premium Coaches Portal**. Premium keeps (and leans into) the **workspace shape**: sidebar/desktop + operator bottom nav/mobile, grouped sections, admin conventions. It is **deliberately NOT consumer-chromed** — a ~14-section season tool doesn't fit a scrolling tab row, and the workspace form factor is part of what the tier sells.
- **Tournament-weekend parity is a standing rule:** the tournament-record surface stays **shared** between tiers (already one component), so every free-tier tournament improvement lands in Premium automatically. **The tiers differ in season tools, never in tournament experience** — coaches don't pay for a better game weekend.
- **Premium near-term = light touch, no rebuild:** when its nav is next touched, group the sidebar into domains (Team / Season / Money / Events / Insights / Comms); converge premium shell components with admin's over time so operators learn one language; add a one-screen "your team now has a workspace" narration at the upgrade moment.
- **Tier language for positioning:** free = **"tournament companion"**, Premium = **"team operations HQ"** — the shell difference IS the value ladder ("the same class of workspace the leagues run on").
**Rationale:** The free portal is the acquisition funnel (every public tournament registration creates one — Founding Season posture, 2026-07-20) and its 2026-07-25 UX audit found the mental-model mismatch ("I signed up for a tournament" vs a third standalone app), a third nav vocabulary, and per-page Flip placement drift as structural defects. Integrating it into the consumer app fixes those at the root, gives coaches Scores/Chat one tap away on game weekends, and lets the top-tab row absorb progressive feature disclosure without the bottom-nav slot budget. Keeping Premium operator-shaped preserves density for a genuinely operator persona AND converts the chrome difference into the cleanest upsell story the tiers have had; unifying both tiers into consumer chrome would flatten that story and cramp the season toolset. Full analysis + verified evidence: `docs/projects/active/FREE_COACH_PORTAL_EXPERIENCE_PLAN.md` (audit 2026-07-25, 14-agent adversarially-verified + follow-up feasibility checks).
**Affects:** consumer + coach app IA and roadmap sequencing (free-portal shell integration becomes Phase A2 of the active plan; record-page regroup shifts to A3); positioning/tier language for `/for-coaches`, pricing coach card, and upsell surfaces (at build). **No price, plan-name, capacity, gate, or SKU change; no migration.** Basic/Premium naming canon unchanged.
**Handoff:**
HANDOFF → `/plan` — done same session: `FREE_COACH_PORTAL_EXPERIENCE_PLAN.md` + PM brief resequenced (A2 = consumer shell integration; A3 = record-page regroup) with the premium light-touch roadmap recorded as a later tranche.
HANDOFF → `/design` — log the two-family chrome ruling in the design decisions log (consumer family = event header + top tab row + global nav; operator family = workspace shell; Flip always header-right in both); the free-portal shell spec (header composition, tab row behavior, team-chat doorway on Overview) is a `/design` pass at A2 build.
HANDOFF → `/marketing` — at A2 build (not before): adopt "tournament companion vs team operations HQ" framing on `/for-coaches`, the pricing page coach card, and in-portal upsell surfaces; keep the "your free tools stay free" promise and Founding Season price lines intact. Naming canon applies.
HANDOFF → `/billing` — none (no gate/tier change).
**Relates to:** 2026-07-11 G1 one role-routed app (this completes its coach-facing half); 2026-07-20 global-nav-on-tournament-pages decision (the top-tab + persistent-global-nav pattern this extends to the coach team space); 2026-07-20 Founding Season Coaches Free (the acquisition funnel this experience serves); 2026-06-22 Coaches Portal packaging + bridge economics (untouched — inclusions and price unchanged).

---

### 2026-07-24 — Account model: "Verified Network" — open the membership axis, gate the ownership axis; amends the 2026-06-19 single-org decision
**Status:** Decided (ratified by owner 2026-07-24 in conversation — "let's go with your recommendation")
**Decision:** Resolve the owner-commissioned account-model deep dive (`docs/agents/strategy/ACCOUNT_MODEL_FREEDOM_ANALYSIS.md` + PM brief) with the **"Verified Network"** posture. The account model has two axes with opposite economics, and they get opposite rules:
- **Membership axis (CHEAP — belonging to / coaching across multiple organizations) → UNLIMITED and FREE.** Non-owner org membership, rep + assistant coaching, tournament coaching, and following are explicitly open across any number of orgs. This **codifies as an intentional rule what the code already largely ships** (rather than "multi-membership by exception"). The hard UX rule holds: a single-everything user never sees any multi-anything UI.
- **Ownership axis (EXPENSIVE — owning a 2nd real-org subscription or a 2nd Premium Coaches Portal) → ALLOWED BUT VERIFIED.** A light "tell us you run more than one organization" step gates it:
  - **During Founding Season ($0 comp, through 2027-01-01):** the step is a **non-blocking lead-gen form** — nothing is blocked; it gathers signal on who genuinely runs multiple orgs and keeps the manual January conversion queue tidy.
  - **Paid period (from 2027-01-01):** the step becomes an **approval that unlocks a real, full-price, self-serve checkout**, split by what's being bought:
    - **2nd REAL ORG** (two genuinely separate associations) → **approve-then-buy**: confirm it's a distinct entity, not one club splitting into cheaper tiers to arbitrage price, then self-serve purchase at full price.
    - **2nd PREMIUM PORTAL** (a coach with two teams) → **funnel-to-Club-first**: pitch the Club plan (whole coaching staff's portals included) and only sell the standalone 2nd portal if Club genuinely doesn't fit.
  - **Scale posture:** at current volume the "approval" is the owner reading an email; heavier verification tooling is deferred until volume justifies it (same threshold posture as the manual January conversion runbook, ~50 accounts).
- **ONE home org is unchanged.** Officials/scorekeepers are **excluded** from cross-org freedom (near-zero demand + officiating conflict-of-interest concern).
**Rationale:** During Founding Season everything is $0-comped and there is ~1 real prod org, so every freedom question is **revenue-neutral in 2026** — the only live cost of permissiveness is support complexity and a longer manual January queue. The membership axis is free (a role never bills) and is the referral network the 50→500 growth model runs on, so restricting it is all cost and no benefit. The ownership axis is where "more subscriptions = more money" is real **and** where cannibalization concentrates (a club splitting into two cheap tiers instead of one Club; a coach stacking standalone portals instead of the club buying Club). The verification step is cheap insurance that separates the genuinely-additive multi-association operator (welcome, full price) from tier-arbitrage (funnel to the bigger sale). Full evidence: a 186-agent workflow (evidence sweep + adversarial verify of 147 capability claims + option-package judging), adversarially verified.
**Affects:** account/identity model, packaging (a 2nd org / 2nd portal is a separate full-price subscription, verification-gated — no new SKU, no price change), positioning, roadmap sequencing, and the January-2027 conversion. **No plan-name, price, capacity-band, or existing-gate change.**
**Amends:** the **2026-06-19 "single-org by default, multi-membership by exception, ONE home org"** decision (auto-memory `decision_one_to_one_vs_multi_org`; not previously in this log — see drift finding A of the 2026-07-24 audit): membership multiplicity is now **explicitly open** (not "by exception"); ownership multiplicity is now **allowed-but-verified** (not soft-blocked). ONE home org and the separate free-vs-paid coach models are **unchanged**.
**Resolves:** the parked **one-Premium-portal-per-owner DB safeguard** (2026-06-19 "decision #2", PARKED) — **BUILD it**, on a **watch-only-during-promo → hard-enforce-at-January** timeline.
**Reinforces (does not reopen):** the **2026-06-22 Coaches Portal anti-cannibalization** economics (standalone Premium as a bridge to a Club sale) — the funnel-to-Club-first rule for a 2nd portal is the mechanism that honors it in the paid period.
**Handoff:**
HANDOFF → `/plan` — implementation plan + PM brief for: (a) the verification step (promo lead-gen form → paid-period approval-unlocks-checkout, one review flow reused for 2nd-org and 2nd-portal + cross-org head/paid coaching); (b) the funnel-to-Club-first flow at a 2nd-portal attempt; (c) the **fix-regardless-of-package bug bundle** — org-create wrongly blocking a portal-owning coach from creating their first real org; billing cancel/downgrade defaulting to the caller's home org instead of the invoking org; ungated `/api/league/create` second-org path; reinstate-after-suspend one-org loophole; cross-org head-coach promotion loophole; (d) prerequisites before opening portal multiplicity — bundle the doubled Coach Insights digest, add distinguishable Stripe receipts, add a minimal refund audit trail.
HANDOFF → `/billing` — build the one-Premium-portal-per-owner DB safeguard (watch-only → enforce at January); reconcile `PLAN_PRICING_FACTS.md` (add a "Decided / not-yet-built" line: 2nd org / 2nd portal = separate full-price subscription, verification-gated) in the same unit of work + run the drift check. No price/gate value change.
HANDOFF → `/marketing` — (at build, not before) the verification-step copy ("run more than one organization?"), the funnel-to-Club-first pitch at a 2nd-portal attempt, and **update the live pricing FAQ** ("If you run multiple associations, each one is managed as its own organization", `app/pricing/page.tsx`) to disclose that **each organization is billed separately** before multi-org is marketed. Naming canon applies.
HANDOFF → security/`/plan` — two support-tooling findings surfaced by the analysis and ratified for closure: the reset-password-link **impersonation vector** (a support-tier operator can establish a live session as any customer via the raw recovery link), and the **"Complete Transfer" cross-org write blast radius** (a support action on Org A can cancel Org B's subscription + suspend its members). Add a counterpart-org confirmation to Complete Transfer; stop surfacing the raw recovery link to the operator (email it) and/or add audit + forced re-auth.
HANDOFF → owner — notification-pause master switch stays **account-wide** for now (revisit if multi-org prevalence grows); January-2027 conversion stays **manual** until the comp cohort outgrows ~50; the `org_id = null` audit blind spot on account-wide support actions is logged for a later decision.
**Relates to:** 2026-06-19 identity decision (amended); 2026-06-22 Coaches Portal anti-cannibalization + Club whole-staff inclusion (reinforced); 2026-07-20 Founding Season Coaches Free ($0 comp cohort this gate governs during the promo); 2026-07-20 free-first follows (the following half of the open membership axis).

---

### 2026-07-22 — Tournaments are DISCOVERABLE BY DEFAULT (opt-out) — grow the discovery base
**Status:** Decided (ratified by owner 2026-07-22 in conversation — "just make tournaments visible by default")
**Status:** Decided (ratified by owner 2026-07-22 in conversation — "just make tournaments visible by default")
**Decision:** The public discovery directory ([/discover](../../../app/discover/page.tsx)) + in-app search flip from **opt-in to opt-out for tournaments**. A newly-created tournament **lists automatically**; the organizer hides an event by turning off the existing Event Settings → Tournament Overview "list in directory" toggle. The default for `tournaments.list_in_directory` changes `false → true` (mig 197). **No backfill** — existing tournaments keep their stored value (the two prod events are already listed; historically-unlisted events stay unlisted unless the organizer opts in). **Available on all plans (no tier gate)** — unchanged.
**Scope / guardrails (deliberately narrow):** Only **tournaments** change. **Organizations were already discoverable by default** (`is_discoverable`/`is_public` default true — unchanged). **Teams** stay findable **only through a listed tournament** (search exposes team NAME + event only, never rosters/PII — the existing `toPublicTeam` choke point holds). **Coaches' private team-workspace homes remain excluded** from discovery. A default-listed **draft** still does not surface until it goes active (the public-status gate `status IN ('active','completed')` still ANDs at query time). **No per-team hide** added (the event-level toggle covers a whole event).
**Rationale:** The working assumption is that most events want to be found, so defaulting to visible grows the /discover base — more fans finding events feeds the free-first acquisition/network-effect posture (the QR-at-the-diamond moment). Cost of the flip is near-zero given tiny current volume; the opt-out toggle is the privacy countermeasure, and no PII surface changes (public-data-only, unchanged visibility model).
**Affects:** Discovery directory reach; tournament create/settings default; in-app help copy that frames listing as "opt-in"; data dictionary (`tournaments.list_in_directory`). **No price, plan-name, tier, or gate change.**
**Handoff:**
HANDOFF → `/docs` — update the in-app help that describes the directory as an organizer **opt-in**; reframe as **listed by default, toggle off to hide** (tournament/discovery help guide).
HANDOFF → `/marketing` — only if any public/marketing copy frames directory listing as opt-in ("list your event in the directory"); reframe to default-on + hide control. No new claims.
HANDOFF → `/billing` — none (no gate/tier change).
HANDOFF → release — mig 197 is **DEV-only / prod-pending**; apply to prod before promoting the code (commit `db1a81a1`).
**Relates to:** 2026-07-19 org discoverability predicate (orgs already default-discoverable — this extends the same posture to tournaments); 2026-07-20 free-first follows (discovery + follow are the two halves of the free-first acquisition loop).

### 2026-07-20 — Free-first follows for ALL entity types: tournament + organization follows need no account (Unified Home Phase 6)
**Status:** Decided (ratified by owner 2026-07-20 in the Phase 6 mockup round — "lock it in")
**Decision:** The device-first, no-account follow model teams have always had **extends to both new Phase 6 follow types**: a fan can follow a **whole tournament** or an **organization** with one tap, signed out, stored on the device — exactly like team follows. Sign-in is a **benefit nudge, never a wall**: an account adds cross-device sync and (per the unchanged 2026-07-14 decision) score alerts. Device follows render on the signed-out Home/Scores exactly as device team follows do; the existing **explicit claim-on-sign-in offer** ("add this device's follows to your account?") extends to all three entity types — never silent merging. **Supersedes the Phase 6 plan's "account-only for v1" recommendation** (plan §4 updated same day). The owner **explicitly declined the all-account alternative** (requiring an account for team follows was rejected as too much re-architecture of the shipped device/pin system) — the my-team pin and device team follows work exactly as today. Boundaries kept: alerts remain account-only; a device tournament/org follow never seeds a my-team pin (independence is mockup Round F6, pending its own ratification); org-follow eligibility reuses the Phase 2 org-search predicate.
**Rationale:** One legible rule a fan can hold — "follow anything free; sign in to make it travel" — restoring the consistency the mixed model broke (an account gate for events beside no-account team follows read as arbitrary; owner flagged it on the rev-1 mockups). Extends the validated free-first acquisition posture (free-app conversion strategy): the QR-at-the-diamond moment stays frictionless for events, the thing casual fans most want to follow. Anonymous follows are public-data-only — no PII/PIPEDA surface. Accepted trade: anonymous follows stay invisible to any future organizer-facing follower counts (already true for teams); the sign-in nudge is the countermeasure.
**Affects:** Consumer follow UX (Phase 6 mockup artifact `d6a7c08b` rev 2 is the free-first spec); **no price, tier, plan-name, or gate change; no migration expected**.
**Handoff:**
HANDOFF → `/plan` — reflect free-first in the Phase 6 implementation plan + PM brief once the mockup round is fully ratified (F1–F6; in flight in the same session). Plan §4 account-only line already corrected.
HANDOFF → `/marketing` — at build (not before): follow-button + nudge copy (sign-in framed as sync + alerts benefit, never a requirement; tournament-scoped follow language per the 2026-07-18 entry's rider) and the extended claim-offer wording.
HANDOFF → `/billing` — none (no gate change; alerts gating untouched).
**Relates to:** 2026-07-18 two-tier follow model (this executes its Phase 6 layer); 2026-07-14 account-scoped fan alerts (**unchanged** — this does NOT reopen anonymous alerts); 2026-07-06 / 2026-07-11 G2 free-follow legibility posture (extended to new entity types); 2026-07-19 org discoverability predicate (reused as org-follow eligibility).

### 2026-07-20 — Founding Season extends to Premium Coaches Portal ($0 until 2027-01-01); Coaches Portal goes LIVE self-serve; January 2027 conversion = manual runbook; team-gate drift resolved deliberately
**Status:** Decided (ratified by owner 2026-07-20 in conversation — "whatever we have to do, I want the premium coaches portal free until January 1st")
**Decision:** Four-part package. **(D1)** The **Premium Coaches Portal ($29/mo · $290/yr, one team) joins the Founding Season promo: $0 until 2027-01-01**, self-serve. This is a **promo, not a repricing** — the $29/$290 list price stands, remains the visible anchor everywhere ("Free until Jan 1, 2027 — then $29/mo"), and is what post-promo billing anchors to. This **knowingly pauses for 2026** the 2026-06-22 "keep the $29 standalone — do not cannibalize the Club bridge" economics; the bridge (standalone Premium until your org joins Club, then included) resumes at promo end. **(D2)** The **Coaches Portal goes live for self-serve acquisition now**: Basic (free) via the `/start` "Coach a team" door, Premium (comped) via the comp enrollment path — superseding the "still in development / not customer-ready" routing gate (audit 2026-07-20 confirmed the free portal is fully built: roster, schedule, fees, announcements, chat, tournament history all functional org-less). Launch flips ship **together** (door + $0 upgrade + copy + gate reopen) at Phase 3 of the build plan. **(D3)** **January 2027 conversion for the entire 2026 comp cohort (orgs + coach workspaces) is a manual, owner-executed runbook** — mass email (fall card-on-file window Oct 1–Dec 31), then per-account convert/downgrade/cancel by hand in January. Automated conversion (Founding Season Phase 4C) stays deferred; revisit only if the cohort outgrows hand-conversion (~50+). **(D4)** The discovered **`plan_gating.team='live'` drift on prod** (checkout accidentally chargeable at $29 since mig-065 seed + live Stripe prices 2026-06-02; zero sales) is resolved **deliberately: close to early-access immediately** (platform-admin change-request workflow, dev decision may lag for testing convenience), **reopen intentionally at the Phase 3 launch as the $0 comp path**.
**Rationale:** Owner's 2026 objective is **users on the platform giving feedback, not revenue** — free pricing honestly frames the beta feel and lets the product iterate without customers feeling they paid for something unfinished. Volume is tiny (1 org / 2 free tournaments as of today), so manual January conversion is cheap and automation is premature. The drift close removes the risk of someone paying $29 days before the same thing becomes free. Full verified problem inventory + build plan: `docs/projects/archive/FOUNDING_SEASON_COACHES_FREE_PLAN.md` (+ PM brief); audits = two 2026-07-20 ultracode workflow runs, adversarially verified.
**Affects:** Promotions (PLAN_PRICING_FACTS.md Promotions section + Premium Coaches Portal purchasable status), coach acquisition funnel (`/start` card, `/for-coaches`), billing surfaces + campaign/transactional emails that state $29 or "coming in 2027", plan gating (`team` key), January ops. **No list-price, plan-name, or capacity change.**
**Handoff:**
HANDOFF → `/plan` — done: `FOUNDING_SEASON_COACHES_FREE_PLAN.md` + PM brief (Phases 0–4).
HANDOFF → `/billing` — Phase 1 fix (SetupIntent card-only save replacing the mischarging trial-checkout "Add payment method"); Phase 3 comp enrollment path (`platform_override` + null subscription id) + gate reopen; immediate: close `plan_gating.team` on prod.
HANDOFF → `/marketing` — at Phase 3 launch (not before): `/for-coaches` coming-soon → live free framing, "$0 until Jan 1, 2027 — then $29/mo" price lines, spotlight_coaches_org/coach + spotlight_full_picture email updates, comp variant of the team-workspace welcome email. Naming canon (Basic/Premium Coaches Portal) and express-interest pattern apply.
HANDOFF → owner — (a) execute the prod gate close via platform-admin change requests; (b) Stripe production smoke test remains a HARD prerequisite before any real January charges (deadline Dec 31, tracked in STRIPE_PRODUCTION_SMOKE_TEST_TODO.md).
**Relates to:** 2026-06-22 Coaches Portal packaging (bridge economics **paused for 2026, not superseded**); Founding Season promo entry in PLAN_PRICING_FACTS.md (extended, same end date + runtime constants); 2026-07-11 G1 (one app — unaffected).

### 2026-07-20 — Global app nav persists on public tournament pages at EVERY tier; no plan-gated hide (Unified Home IA — Phase 5, tournament nav merge)
**Status:** Decided (ratified by owner 2026-07-20 via decision prompt)
**Decision:** On public tournament pages (`/{orgSlug}/{tournamentSlug}/*`), the global consumer nav (**Home · Scores · Chat · Account**) will be **persistently present on every plan tier** — free `Tournament` through `Tournament Plus` — with **no dial-back, no plan-gated hide, no per-org opt-out**. The tournament's own pages move into a horizontally-scrolling top tab row under its branded header; the global bar stays as an always-present bottom (mobile) / top-strip (desktop) app-identity layer. Restraint for branded events is **cosmetic only** — the global bar renders **platform-neutral** (a thin, quiet, dark utility strip; only its active tab borrows the tournament's accent colour), **never** re-skinned to the org's brand and **never** removed for paid tiers. Existing paid branding is **unchanged**: `advanced_tournament_branding` (custom theme/logo/hero/name across the whole tournament page) remains a Tournament Plus feature; this decision only makes the **platform-neutral global nav** universal, layered around — not replacing — the branded space.
**Rationale:** The largest, most-branded events draw the most **one-time visitors**, who are the platform's best conversion targets into a retained, signed-in app user (the tournament-visitor → follow → chat → account loop). Gating the global bar away from paid/branded events would strip the acquisition loop from exactly the **highest-value traffic** — the opposite of the intent. A persistent, platform-neutral bar is consistent with a promise already made and priced under **G1 (2026-07-11)**: all tiers get the same one-app-shell treatment; organizers were told their event lives as a **branded SPACE inside the one app**, not a separately-badged product. The white-label promise is protected by keeping the bar visually quiet/neutral (it reads as "the app," like GameChanger's own app chrome around a team-branded header), not by hiding it. Full design + rationale in `docs/projects/archive/UNIFIED_HOME_TOURNAMENT_NAV_MERGE_PLAN.md` + PM brief; source = 2026-07-20 12-agent nav-unification analysis.
**Affects:** consumer app IA / roadmap sequencing (a new Unified Home Phase 5, sequenced after the in-flight Phases commit + owner-test); positioning of what every tier gets on the shared app shell; **no price, plan-name, capacity-band, or gate change**; no migration; `advanced_tournament_branding` Plus gate **unchanged**.
**Handoff:**
HANDOFF → `/plan` — done: `UNIFIED_HOME_TOURNAMENT_NAV_MERGE_PLAN.md` + PM brief exist (Phase 0 = mobile vertical-space budget + mockup approval gates code).
HANDOFF → `/billing` — none (no gate/tier change; confirm at build that the global nav is NOT wired to any plan-feature flag — its universality is the decision).
HANDOFF → `/marketing` — none now. IF the pricing/branding copy ever describes the tournament page as fully white-label, add a light clarifier that a quiet platform nav is always present (the branded SPACE promise, not a chrome-free product). Not a copy change to make today.
HANDOFF → `/design` — the visual/interaction spec (neutral-bar skin, active-tab accent tint, "Overview" landing-tab rename, supersession of the "never two nav bars stacked" rationale) is logged separately in the design decisions log.
**Relates to / reinforces:** 2026-07-11 G1 (one role-routed app; branded SPACE inside one shell — this extends that promise to the in-app nav layer), 2026-07-18 two-tier follow model + logged-out Chat preview (same Unified Home IA program; no follow/chat semantics change — chrome only).

### 2026-07-19 — Fan-search org discoverability REUSES the existing `is_discoverable` flag (one switch, no new column); tournament/team-search vs org `is_public` interaction left open (Unified Home IA — Phase 2 search)
**Status:** **Decided** — (a) reuse of `is_discoverable` ratified by owner 2026-07-18 via a build-kickoff decision prompt; (b) the `is_public`-vs-directory interaction ruled 2026-07-19 (**keep as-is — per-tournament opt-in stands**, see below). Both now Decided.
**Decision:** The new fan-facing Home search for **Organizations** reuses the **existing `organizations.is_discoverable` flag** (bool, default-on, already live on dev+prod) rather than introducing a new `list_in_directory` org column. This makes org **fan-search visibility and the coach→org link-picker visibility ONE switch, not two**, and Phase 2 ships with **no database migration**. The org-search eligibility predicate = `is_discoverable AND is_public AND account_kind≠team_workspace AND plan_id≠team AND subscription_status≠canceled` (a strict superset of the `/{orgSlug}` public page's own notFound gate, so a search hit never resolves to a dead link). There is currently **no admin UI** to toggle `is_discoverable` (it defaults on for every real org); an org-facing opt-out control is a possible future fast-follow, not built this phase. Team search is scoped strictly to `list_in_directory` tournaments (name + event only, no PII).
**Rationale:** The DB house-rule schema check (decide column existence from live snapshots, never the plan) found `is_discoverable` already exists and already encodes "real, publicly-findable org" — the plan §3d had proposed a *new* column in apparent ignorance of it. Adding a second, near-identical visibility flag would invite drift (an org discoverable in one surface but not the other) and is redundant. Reuse also auto-excludes team-workspace shadow orgs (already provisioned `is_discoverable=false`). Owner picked reuse over "add the column as planned" and over "route to /strategy first." Full context in `docs/projects/archive/UNIFIED_HOME_IA_REDESIGN_PLAN.md` §3d (updated) + `memory/project_unified_home_redesign.md`.
**`is_public`-vs-directory ruling (Decided 2026-07-19, owner via decision prompt): KEEP AS-IS — the per-tournament opt-in stands on its own.** The tournament + team directory/search keys on **per-tournament `list_in_directory`** (+ status + canceled-org exclusion) and deliberately does **not** gate on org-level `is_public`. So an org that sets `is_public=false` (hiding its org home page) but leaves a tournament `list_in_directory=true` keeps that tournament — and its accepted team names — appearing in Browse and search. Rationale: no PII is exposed (event/team names + dates are already public via the tournament's own explicit opt-in), and the per-tournament toggle is the more deliberate, specific signal — letting an unrelated org-level switch silently override it would surprise organizers who intentionally listed one event. The two flags stay orthogonal by design: org `is_public` = "do I publish an org home page?"; tournament `list_in_directory` = "should people discover this specific event?" No clarifying-note UI was added (owner chose the plain keep-as-is option, not keep-as-is-plus-note). **NO code change** — this ratifies the existing (and now team-search-inherited) behavior.
**Affects:** consumer search behavior + org discoverability posture; **no price, tier, plan-name, or gate change**; no migration; **no code change** (both rulings preserve current behavior).
**Handoff:**
HANDOFF → `/plan` — none needed (Phase 2 built; plan §3d + §4 already updated to reflect the reuse).
HANDOFF → `/billing` — none (no gate/tier change).
HANDOFF → `/marketing` — none now; IF a future org-facing "hide my org from discovery" opt-out UI is built, its copy must make clear the one switch governs both fan search and the coach link-picker.
HANDOFF → owner — RESOLVED 2026-07-19: the `is_public`-vs-directory question was ruled keep-as-is (per-tournament opt-in stands). Nothing pending.
**Relates to:** 2026-07-18 two-tier follow model (same Unified Home IA program; Organizations search is the near-term surface, org *follows* remain the deferred Phase 6 layer).

### 2026-07-18 — Two-tier follow model: tournament-scoped team follows now; persistent "Teams" follow layer later (Unified Home IA)
**Status:** Decided (ratified by owner 2026-07-18) · Teams-phase composition (rep-team foundation, parent chat shape) = **Proposed** only, gated on the G3 PIPEDA/CASL review
**Decision:** Following is split into two distinct product concepts. (1) **Following a team IN a tournament** (today's only follow type) is a **tournament-scoped** relationship: it governs that tournament's pages (highlighted scores, my-team surfaces) and that event's notifications. On the redesigned app Home it presents **under a Tournaments section as a tournament card** — the followed team is the card's context line, and tapping in lands on the team's experience inside that tournament. It does NOT get a standalone cross-event team card. (2) **Following a Team** (future layer) is a **persistent** relationship with a durable team entity — schedules and scores across events, and eventually parent chat — shipping as its own later phase. Proposed (not yet ratified): build that persistent entity on the existing rep-team structure rather than a new model. Anything family/chat-facing in the Teams layer remains behind the G3 compliance preconditions.
**Rationale:** Keeps v1 honest about what a follow means today (tournament entries are ephemeral; no season-continuity entity exists) instead of shipping a confusing hybrid, while reserving a clean richer "Teams" concept that matches the GameChanger mental model parents already have. Resolves two redesign blockers (no season entity, no canonical cross-tournament team URL) by product definition rather than new architecture. From the 2026-07-18 unified-home analysis (12-agent workflow); full design in `docs/projects/archive/UNIFIED_HOME_IA_REDESIGN_PLAN.md`.
**Affects:** consumer app IA/roadmap sequencing (Home's Following section is tournament-first now; Teams + Organizations sections slot in later); no price, tier, gate, or plan-name change; fan-alert gating (2026-07-14 entry) unchanged and account-scoped as decided.
**Handoff:**
HANDOFF → `/plan` — done: `UNIFIED_HOME_IA_REDESIGN_PLAN.md` + PM brief (Phase 6 = whole-tournament/org follows; Phase 7 = gated Teams layer, to be scoped in its own plan when reached, incl. ratifying the rep-team foundation).
HANDOFF → `/marketing` (at build, not before) — follow-action copy on tournament pages should read as tournament-scoped so users aren't surprised the follow isn't a permanent team subscription; naming pass needed when the Teams layer arrives to keep the two follow types legible.
HANDOFF → `/billing` — none (no gate change).
**Relates to:** 2026-07-11 G1 (one role-routed app), 2026-07-11 G3 (family-layer preconditions bind the future Teams/parent-chat phase), 2026-07-14 (account-scoped fan alerts — unchanged).

### 2026-07-18 — Logged-out Chat surface = static illustrative preview only; member-only read posture unchanged
**Status:** Decided (ratified by owner 2026-07-18)
**Decision:** Chat becomes a permanent, always-visible tab in the consumer app nav (same nav shape signed-in and signed-out), and the pre-signup/logged-out (and signed-in non-member fan) state shows a **static, illustrative preview only** — a clearly fictional sample conversation plus dual-audience value pitch — **never real chat content**, in any form (no redacted feeds, no live counts, no real names). The member-only read posture at the data layer is deliberately unchanged: room content remains visible exclusively to authenticated room members. Fan-facing copy must not imply fans get team chat access (parent chat stays behind the G3 review). A report-a-message flow ships in the same release as the tab (owner decision, same date).
**Rationale:** Achieves both goals of the owner's nav vision (stable nav shape; the social layer visibly marketed pre-signup) without expanding the privacy posture of youth-sports conversations beyond what members consented to, and without touching the G3 PIPEDA/CASL boundary. Making member content publicly readable would be a consent + compliance expansion no nav redesign should decide.
**Affects:** what's visible pre-signup (acquisition surface framing); no change to the existing `tournament_chat` plan gate, pricing, or tiers. Any future proposal to gate *inbox visibility* (vs posting) by plan routes back through `/strategy` first.
**Handoff:**
HANDOFF → `/marketing` (at Chat-tab build) — the preview state's pitch copy (coach-framed + organizer-framed CTAs per the established dual-audience pattern; must not promise fan/parent chat).
HANDOFF → `/plan` — done: Chat tab phase in `UNIFIED_HOME_IA_REDESIGN_PLAN.md` (inbox aggregator, preview states, report-a-message, self-mute).
HANDOFF → `/billing` — none (gate unchanged).
**Relates to:** 2026-07-11 G3/G4 (family/parent chat preconditions — untouched by this decision).

### 2026-07-17 — Player Development privacy sign-off (D2 gate): CONDITIONAL — existing tryout consent accepted for coach-authored development data, two conditions attached
**Status:** Decided (conditional) — ratified by owner 2026-07-17 via decision prompt
**Decision:** The existing guardian consent model (mig-164: PIPEDA data-collection / CASL email / eligibility, captured once at **public tryout submission**) is accepted as **adequate for coach-authored player-development data** — free-text focus areas + notes, numeric measurables + notes, evaluation sessions, returning-player continuity links, and carry-forward decisions. This clears the D2 gate that has blocked prod promotion of the Player Development feature (Coaches Portal Phase 3). **Two conditions ride with the sign-off:** (1) add a **one-line transparency notice** in the org/privacy terms stating coaches may keep player-development records — a NOTICE, not a new consent checkbox/gate; (2) the **player-PII retention/purge policy** (already spun out as its own TODO, D3) is a **committed fast-follow** — development notes currently have no deletion window; resolved soon after ship, not a hard blocker for promotion.
**Rationale:** The **organization is the data controller** and coaches are its authorized operators; keeping skill-development notes is a normal, expected coaching purpose — not behavioural profiling, no new sensitive category (medical stays in its existing separate field), no third-party sharing, no automated decisions about a child. The design already minimizes exposure: coach-only visibility, **no parent-facing surface**, the continuity-links table stores **FK pointers only (never a copy of guardian PII)**, and the printable summary is generated **client-side + hand-delivered** (no server storage, no share link). **Gap acknowledged:** mig-164 consent is tryout-submission-only, so it does not reach (a) players an admin adds directly to a roster (no tryout signup → no consent row) or (b) ongoing coach development notes over a season — the transparency notice (condition 1) closes the reasonable-expectation gap without a new gate. Consistent with the standing PIPEDA/CASL-review posture on the family layer (2026-07-11 G3 preconditions).
**Affects:** roadmap sequencing (unblocks the prod-promotion bundle of migs 189–192 for Player Development, alongside the separate D6 `/marketing` copy pass, in progress); org privacy-terms copy (one added line); retention/compliance surface (D3 follow-up now load-bearing).
**Handoff:**
HANDOFF → `/marketing` — place the one-line development-records notice in the org/privacy terms copy (brief: coaches may keep player-development records — focus areas, measurements, evaluation notes — visible to the coaching staff only; state it plainly, no legalese, consistent with the no-surprise-use posture). Companion to the in-progress D6 tone pass on the match banner + carry banner + printable summary.
HANDOFF → D3 retention/purge project (via `/strategy` + `/db`, existing TODO) — development notes/measurables + continuity links must gain a retention window; this sign-off makes that follow-up load-bearing rather than optional.
HANDOFF → `/release` — the D2 privacy gate is now CLEARED (conditional); the Player Development prod bundle (migs 189+190+191+192, apply in order) may promote once the D6 copy pass lands and the privacy-terms notice is in place.
**Relates to:** 2026-07-11 G3 (PIPEDA/CASL-before-build precondition on the family layer — same compliance discipline, narrower scope here since Player Development is coach-only with no parent surface).

### 2026-07-14 — Fan score alerts require a signed-in account; anonymous alert opt-ins retired (following stays account-free)
**Status:** Decided (ratified 2026-07-14, owner-selected via decision prompt)
**Decision:** Receiving fan push alerts (game live/final scores, event news) now **requires a signed-in FieldLogicHQ account**. The one rule: *anyone can follow teams and watch live scores — no account; alerts are what signing in gets you.* Alert preferences are **account-level** (one "Followed teams" card on the unified Account → Notifications page — global toggles across all followed teams), never device-level: signed out, **no alert settings exist anywhere**, only a single consistent "Sign in to get score alerts" pitch. The **anonymous device-only alert opt-in path is retired for new opt-ins** at Slice 3 ship (existing anonymous opt-ins ≈ zero on prod; no migration program). **Anonymous following/browsing stays permanent and untouched** (locked Slice 1 posture). The **Tournament Plus gate on fan push is unchanged**; the G2 "alerts not offered by this event" legibility commitment is satisfied by one line on the notifications card naming the free-tier event(s).
**Rationale:** (1) **UX integrity** — the dual device+account model created two parallel settings stores; owner identified the trap (sign in, turn alerts off, an invisible device-level setting keeps notifying). One store makes that structurally impossible. (2) **Account-ladder strategy** — "get score alerts" is a materially stronger account-creation hook than "your follows travel," and account growth feeds the family/chat layers (G3/G4). (3) **Simplicity** — no signed-out settings surface, no merge semantics, smaller build. (4) **Pre-revenue timing** — zero real anonymous-alert usage means the marketed "no account needed for alerts" behavior can be retired at no migration cost, same logic as G1's icon retirement. Trade-off accepted: one extra step (sign-in sheet) at the field-side moment of highest intent.
**Affects:** delivery/identity architecture for fan push (account-routed); the shipped help copy "No fan account is required — alerts are tied to that person's device" (drift at build time, not today — current copy stays accurate until Slice 3 ships); the follow-time sign-in sheet's pitch copy; **no price, tier, plan-name, or gate change**.
**Handoff:**
HANDOFF → `/marketing` (at Slice 3 build, same unit of work) — update the fan-alerts help sections + FAQ (`lib/help-content/tournaments.tsx`) from "no account needed, per-device" to the one-rule framing; sign-in sheet copy leads with alerts as the benefit; keep "following free, no account" accurate and prominent.
HANDOFF → `/billing` — none new (Plus gate unchanged); at build, the G2 drift check now verifies the gate on the **account** path only (anonymous path retires).
HANDOFF → `/plan` — done: `UNIFIED_APP_PHASE2_SLICE3_BRIEF.md` rev 3 is the approved shape; plan doc Phase 2 item 6 updated.
**Amends:** [2026-07-11 G2] (gate + legibility reconfirmed; the "anonymous AND account-linked" dual-path enforcement language no longer applies — one account path) and the [2026-07-06] fan-push entry's anonymous-device framing (opt-in/revocable/operational-only posture carries over, now account-scoped).

### 2026-07-11 — Unified app RATIFIED: one install identity; per-tournament branded app icons RETIRED (G1)
**Status:** Decided (ratified 2026-07-11)
**Decision:** FieldLogicHQ ships **ONE role-routed app** with **one install identity**. **Per-tournament install identities are retired** — a tournament no longer installs as its own home-screen app; the scorekeeper install merges into the same identity. Organizers keep a **branded SPACE inside the one app** (theming, hero, custom name — the existing `advanced_tournament_branding` Plus feature) plus the **QR/deep-link on-ramp**: scanning a tournament's QR opens (or installs) the one app straight into that event. Owner confirmed 2026-07-10 there are **zero paying customers** and exactly **one unpaid test account** uses the branded icon → **no grandfathering or migration program**; existing installs orphan with a soft "get the new app" prompt.
**Rationale:** Root-cause fix for the owner's multi-app pain (a team in 3 tournaments = 3 icons); the only app-store-compliant shape (Apple 4.2.6's compliant example is literally "an event app with separate entries for each client event"; 4.3(a) bans per-team binaries; Google Play mirrors); matches the market leaders that solved this exact problem (GameChanger one-app directory; LeagueApps no-login tournament discovery). From the 2026-07-10 app-strategy investigation (16-agent workflow: recon + market research + 4 architectures + 3-judge adversarial panel) — full plan at `docs/projects/archive/UNIFIED_APP_CONSUMER_LAYER_PLAN.md` (+ PM brief + journey mockups linked there).
**Affects:** packaging **framing** of `advanced_tournament_branding` (keeps theming/appName/branded space + QR on-ramp; loses the "your own installable app icon" claim — a copy-only change at build time, **no price/tier/feature-key change**); PWA/install architecture; roadmap sequencing (unblocks the plan's Phases 0–3).
**Handoff:**
HANDOFF → `/plan` — done: `UNIFIED_APP_CONSUMER_LAYER_PLAN.md` Phases 0–1.
HANDOFF → `/marketing` (at Phase 0 build, not before) — reframe the App-Icon branding copy ("your own branded space in the app + your own QR/link", never "your own app"); the in-app help line describing the custom home-screen icon (`lib/help-content/tournaments.tsx`) must be updated in the same unit of work as the Phase 0 build. No live drift today — the current copy stays accurate until the build ships.
HANDOFF → `/billing` — none (no gate/price change; `advanced_tournament_branding` key unchanged).
**Supersedes:** the 2026-06-28 "Mobile app + public tournament discovery" **Proposed** entry — direction upheld, ratified, and extended (that entry's status is updated below).

### 2026-07-11 — Fan push stays Tournament Plus; follow UI must make the gate legible (G2)
**Status:** Decided (ratified 2026-07-11) — reconfirms 2026-07-06 — **Amended by [2026-07-14]: alerts now require a signed-in account (anonymous alert path retired); the legibility state lives on the unified notifications card**
**Decision:** All fan push (scores, schedule changes, day-of messages — anonymous **and** the new account-linked follows) stays gated at **Tournament Plus**, unchanged. New commitment: **following and browsing are free everywhere and never blocked**; on free-tier events the follow UI shows an explicit **"alerts not offered by this event"** state so the gate reads as organizer value, not fan punishment — and never silently swallows notifications.
**Rationale:** The unified follow feed makes the Plus fan-push pitch stronger ("follow anywhere, real alerts"), so the two-week-old halo-feature gate is kept intact rather than given away with the new account layer.
**Affects:** no gating/price change; a UI contract for the unified-app plan's Phase 2.
**Handoff:** → `/billing` (at Phase 2 build) enforce the Plus gate on **both** push paths (anonymous device subscriptions and account-linked follows) — one gate, two channels; drift check per the Facts doc. → `/marketing` (near ship) fan-notifications framing per the 2026-07-06 entry.
**Relates to:** 2026-07-06 fan-push entry (reconfirmed, annotated below).

### 2026-07-11 — Parent/guardian deferral NARROWLY reopened: verified family + team chat + practice visibility approved (G3)
**Status:** Decided (ratified 2026-07-11) — narrows the 2026-06-30 deferral — **AMENDED 2026-08-01: the "~5 family accounts per player" cap is superseded by the two-tier entry (guardians ≤2 per player; family followers are team-scoped and uncapped); the verification on-ramps are refined to parent-initiated + coach approval (no public search); safeguarding preconditions unchanged**
**Decision:** *(⚠ 2026-08-01: the **team chat membership** limb below is CLOSED — family chat was CUT; chat stays coaches/admins only. The ~5-per-player cap was already superseded by the two-tier entry, which sets guardians ≤2 per player. Verified family links, practice visibility, the verification on-ramps and the safeguarding preconditions all STAND.)* Approve exactly this slice of the family layer: **verified family relationships** (approval-gated, capped ~5 family accounts per player, no self-claim for young age groups), **team chat membership** for verified family, and **practice-schedule visibility** for verified family. Two verification on-ramps land in ONE status: **coach-approved** (tournament teams — request + approval queue in the coach's portal, registration-email match as an assist) and **registration-email auto-verify** (league/club — the org already knows the parent; they registered and paid). **Still deferred:** RSVP, athlete/parent season portfolio + habit tracker, athlete self-report check-ins, and broader family-communication surfaces — those await their own decision as originally scheduled.
**Safeguarding + compliance preconditions (bind the build):** family-visible team channels only — **no unsupervised 1:1 adult–minor DMs**; staff delete-with-visible-notice moderation; **PIPEDA/CASL review + consent capture before Phase 4 build starts**.
**Rationale:** This is the slice the unified-app strategy needs (parents in chat + practice schedules = the retention/network layer), it's the smallest possible reversal of the deferral, and the league/club auto-verify insight makes it far cheaper than the deferral assumed. The rest of the deferred family surface stays deferred.
**Affects:** roadmap (unblocks unified-app plan Phase 4 scoping), identity scope (a verified-family relationship exists; still no general "athlete user" class), compliance surface.
**Handoff:** → `/plan` — done (plan Phase 4). → legal review before build (Canadian privacy counsel checklist). → `/billing` per the packaging entry below.
**Supersedes (narrows):** part of the 2026-06-30 "Defer parent/guardian/athlete USER accounts" entry (annotated below); the rest of that deferral stands.

### 2026-07-11 — Family-feature packaging: chat basics with ANY coach portal; practice visibility + richer family features ride Premium (G4)
**Status:** Decided (ratified 2026-07-11) · org-purchased "fan pass" = **Proposed** only · **⚠ PARTIALLY SUPERSEDED 2026-08-01 — the "family team-chat basics with ANY coach portal" clause is VOID: family chat was CUT entirely (see the 2026-08-01 family-chat entry). No tier gets it, because it is not being built. The reconciliation this note previously called for is moot, not pending. The REST of this entry — practice visibility + richer family features ride Premium, no new SKU, no per-family fee — STANDS.**
**Decision:** When the verified-family layer ships: **family team-chat basics are included with ANY coach portal** (Basic/free or Premium) — the parent network is the growth loop, don't starve it. **Practice-schedule visibility to family + richer family features are a Premium Coaches Portal inclusion** ($29/mo standalone; included for the whole staff under Club per 2026-06-22) — a concrete, parent-visible reason for a coach to upgrade. **No new SKU, no price change, no per-family fee.** An **org/organizer-purchased "fan pass"** (season-long unlock of premium fan features for a whole event/community — GameChanger Team Pass analog, $239–449/season there) is logged as **Proposed** only, to evaluate after the unified-app plan's Phase 2 ships and real follow data exists.
**Rationale:** Balanced growth + revenue: free chat spreads the app through parents; the practice-schedule unlock converts coaches. Mirrors the market split (GameChanger keeps messaging free and monetizes content) without giving away the whole layer.
**Affects:** Premium Coaches Portal inclusions (Facts-doc change log updated), family-feature gating at Phase 4.
**Handoff:** → `/billing` (at Phase 4 build) gate practice-schedule visibility + richer family features at the premium-coach entitlement; leave family chat basics ungated across portals; reconcile `PLAN_PRICING_FACTS.md` + `lib/plan-config.ts`/`lib/plan-features.ts` in the same unit of work and run the drift check. → `/marketing` (near ship) frame as an included Premium benefit, consistent with the "no per-player fees" posture (2026-06-29 tryout bundling).
**Relates to:** 2026-06-29 tryout/eval bundling (same no-per-unit-fee posture); 2026-06-22 whole-staff-in-Club.

### 2026-07-11 — Store-wrap on pre-agreed triggers; Android first, one listing (G5)
**Status:** Decided (ratified 2026-07-11)
**Decision:** The store wrap happens when **any pre-agreed trigger fires** — (a) a sales prospect/customer asks for App Store presence, (b) recurring iOS push-reliability complaints, (c) premium coaches show daily-use habits — not on a calendar date. Sequence: **Android first** (TWA/Bubblewrap — near-zero code, reuses existing web push, Play $25 one-time); **iOS via Capacitor** (real native push) **only** on the push-reliability or credibility trigger (Apple $99/yr + Mac build + cert maintenance). Always **ONE listing** under the FieldLogicHQ account (aggregator model) — per-tournament/per-org listings are permanently foreclosed by store policy.
**Rationale:** Firms the 2026-06-28 "wrap on a trigger, not a date" principle into a concrete, pre-agreed trigger list so the store step becomes a checklist, not a debate. Cost/benefit per the 2026-07-10 investigation's store-policy research.
**Affects:** roadmap sequencing (unified-app plan Phase 6).
**Handoff:** → `/plan` — done (plan Phase 6). No `/marketing`/`/billing` action until a trigger fires.
**Relates to:** the 2026-06-28 entry's item 3 + cost notes (superseded/absorbed by this ratification).

---

### 2026-07-07 — Bulk "Rain delay" (shift-the-day) TOOL gates at Tournament Plus (automation = paid; manual + banner stay free)
**Status:** Decided (ratified 2026-07-07)
**Decision:** The bulk **Rain delay / "shift the day"** tool — pick a day (today or an upcoming day) and **move and/or cancel that day's remaining games in one atomic action**, with an optional prefilled announcement + notify hand-off — becomes a **Tournament Plus** feature, gated in the **same capability family as the auto-schedule generators** (`auto_schedule` / `playoff_generator`). This gates the **bulk scheduling automation** (the one-tap day shift/cancel), **not** the ability to communicate a delay. It is **distinct from — and complementary to — the 2026-07-06 fan-push decision** below: that entry gated *who gets pushed* (anonymous-fan push at Plus); **this gates the bulk scheduling tool itself.**
**Guardrail (day-of safety stays free — consistent with 2026-07-06):** a free Tournament org can still handle a rain delay, just not in one tap. Free keeps: **rescheduling/cancelling games one at a time** (manual, already free), **posting the free pinned rain-delay banner** on the public schedule (via the free Communication tool — "announcements free" per 2026-06-22), and the **free coach/staff email**. Only the *bulk convenience* (act on a whole day at once) is Plus. The locked Rain-delay entry doubles as a **contextual, compact game-day upgrade prompt** (blueprint-blue "Tournament Plus" framing per /design — never amber/danger).
**Rationale:** Extends the platform's established **"manual = free, automation/bulk = Plus"** line — single-game add + manual bracket build are free; the round-robin + bracket **generators** are Plus. A one-tap bulk day shift is squarely automation, so it sits on the paid side and groups cleanly with the generators under one "Tools" menu (a coherent paid "power tools" set). Because free retains a complete manual path + the free banner, nothing safety-critical is removed. **No price change** — packaging/gating only, reusing the existing Tournament Plus tier.
**Affects:** packaging/gating (what Tournament Plus includes among the schedule tools), the Schedule-page IA (Rain delay grouped with the generators + a game-day upsell chip), positioning (Plus "power tools" story). **No price, plan name, or capacity change.**
**Handoff:**
HANDOFF → `/billing`
- Gate the bulk Rain-delay tool at **`tournament_plus`**, reusing the generator feature family (add a key like `bulk_reschedule` / `day_of_ops`, or reuse `auto_schedule` — implementation choice). Gate **BOTH** the server bulk-reschedule endpoint (403 for free, like the generators' plan gate) **AND** the UI entry point (locked chip / menu item → upsell). Reconcile `lib/plan-config.ts` / `lib/plan-features.ts` / `PLAN_PRICING_FACTS.md` in the same unit of work and run the drift check. **Do NOT gate** the pinned-banner/announcement or manual single-game edit — those stay free.
HANDOFF → `/marketing` (later, near ship — not yet)
- Fold "one-tap rain delay / shift the day" into the Tournament Plus schedule-tools benefit (alongside the generators). Keep it accurate that free can still reschedule manually + post a rain-delay banner + email coaches; Plus adds the one-tap bulk shift. Do not write surface copy until the gate ships.
HANDOFF → `/design` (done)
- The locked-chip + upsell visual treatment is already set (blueprint-blue "Tournament Plus" prompt, non-punitive; lime reserved for the one primary action) — from the 2026-07-07 Schedule-page reorg /design pass.
**Drift check:** required at `/billing` wiring time — `PLAN_PRICING_FACTS.md`'s schedule-tools/automation gating line must gain the bulk Rain-delay tool at Plus, matched to `lib/plan-config.ts` / `lib/plan-features.ts`. No numeric/price drift (no price change).
**Relates to:** the **2026-07-06** fan-push-at-Plus decision (same project, complementary — that gated the push *channel*, this gates the bulk *tool*); the existing `auto_schedule` / `playoff_generator` Plus gates (the automation line this extends); the **2026-06-22** "announcements free, communications workflow paid" split (the free-banner path the guardrail leans on); the `RAIN_DELAY_DAYOF_OPS_PLAN.md` build.

---

### 2026-07-06 — Fan tournament-message push gates at Tournament Plus (keep the fan-push line intact)
**Status:** Decided (ratified 2026-07-06) — **Reconfirmed 2026-07-11** with a UI-legibility commitment for the unified-app follow UI (see [2026-07-11 G2 entry])
**Decision:** Gate the **new fan tournament-message / schedule-change push** at **Tournament Plus** — the **same tier as the existing `fan_score_alerts`** — rather than including it in the base **Tournament** tier. Treat all anonymous-fan push (scores + schedule changes + organizer day-of messages) as **one bundled capability on one side of the line**, not a per-category tier split. Net: Tournament Plus's fan-push feature broadens from "fans get score alerts" to "fans get a full notification channel — scores, schedule changes, and your day-of messages," which *strengthens* the Plus story rather than giving the halo feature away. Mechanically this is either broadening `fan_score_alerts` to cover the new categories or adding a sibling gate at the same `tournament_plus` tier — a `/billing` implementation choice; the strategic ruling is **same tier as fan score alerts**.
**Why not base tier:** (1) **Consistency** — the product already draws its fan-push line at Plus; a scores=Plus / messages=free split is confusing to communicate and awkward to build. (2) **Monetization** — `fan_score_alerts` is explicitly coded as "the signature halo feature"; the anonymous-fan phone push is the one visible fan-facing "wow" that pulls free→Plus, and pre-revenue we don't weaken it. (3) **Base tier is not left mute on day-of safety** — on free Tournament the organizer can still **post the rain-delay banner** (pinned site post → public schedule + `/news`, and public live scores already refresh free), and the **basic all-team email announcement to coaches is already free** (per the 2026-06-22 "announcements free, communications workflow paid" split). So the safety-critical audience — coaches/teams physically running the event — is reachable on base; what Plus adds is pushing to **anonymous fans' phones**, which is the premium fan-experience layer we already decided sits at Plus. The only thing gated away from base is fan *convenience* push, not the ability to communicate a delay.
**Considered and set aside:** a "one free safety category" carve-out (push a single *event-cancelled/weather* category free on base, mirroring the free basic-email floor). Rejected for launch — it forks the clean bundle, adds gate complexity, and pushing to anonymous fans is a different channel from emailing your own coaches. Revisit only if a real base-tier customer asks.
**Anti-spam / CASL posture (folds in Open Decision #6):** Anonymous-fan push is **opt-in device push, not email**, so CASL commercial-electronic-message rules don't directly bind day-of *operational* messages. Guardrail: keep it **opt-in, per-tournament, revocable (disable in-app), and rate-limited**, and restrict the channel to **tournament-operational** messages (scores, schedule, day-of notices) — **not** marketing. Any *promotional* push to fans would need a separate consent review before it's built; not in scope here.
**Affects:** packaging/gating (what Tournament Plus includes for the fan experience), positioning (Plus fan-halo story), and the interim build assumption in `RAIN_DELAY_DAYOF_OPS_PLAN.md`. **No price, plan name, or capacity change.** Not yet reflected in `PLAN_PRICING_FACTS.md`/`lib/plan-config.ts` — do so only on ratification + build.
**Handoff:**
HANDOFF → `/billing` (on ratification, when the feature is built)
- Gate fan tournament-message / schedule-change push at **`tournament_plus`** (broaden `fan_score_alerts` to the new categories, or add a sibling gate at the same tier — do not gate below Plus). Reconcile `PLAN_PRICING_FACTS.md` (the "Public fan experience" gating line) + `lib/plan-config.ts`/`lib/plan-features.ts` in the same unit of work and run the drift check. Add the opt-in / revocable / rate-limit / operational-only guardrails.
HANDOFF → `/marketing` (later, near ship — not yet)
- Re-frame the existing "fan score alerts" Plus benefit as a broader **fan notifications** channel (scores + schedule changes + day-of messages). Keep it accurate that base Tournament can still post a public rain-delay banner and email coaches; Plus adds the push-to-fans layer. Do not write surface copy until `/plan` defines the surface.
HANDOFF → `/plan`
- Interim build assumption in `RAIN_DELAY_DAYOF_OPS_PLAN.md` (reuse the `fan_score_alerts` tier) is **consistent with this recommendation** — no rework if ratified. Hold the base-tier banner + free coach-email path as the base-tier day-of story.
**Relates to:** the existing `fan_score_alerts` Plus gate ("signature halo feature") and `targeted_tournament_announcements` (already Plus); the 2026-06-22 "announcements free, communications workflow paid" split (the base-tier free-comms floor this leans on); the 2026-06-28 mobile-app / fan-experience direction (fan push is the premium fan layer).

---

### 2026-06-30 — Defer parent/guardian/athlete USER accounts; Coaches Portal builds coach-facing value first
**Status:** Decided (ratified 2026-06-30) — **NARROWED 2026-07-11:** verified family links + team chat + practice-schedule visibility are carved out and approved (see [2026-07-11 G3 entry]); RSVP, season portfolio/habit tracker, athlete self-report, and broader family-comms surfaces **remain deferred** as originally scheduled.
**Decision:** FieldLogicHQ will **not** add a parent/guardian/athlete **user class** (logins/accounts for families or players) to the Coaches Portal in the near term. All parent/athlete-user-facing work is deferred and revisited as a **dedicated decision at the END of the Coaches Portal Tryouts & Evaluation project** ("how do we incorporate parent users to the platform"). Until then the roadmap builds **coach-facing value only** — Phase 2 tryout suite + Phase 3 development tools, none of which need parent accounts. Specifically deferred to that decision: the pre-game attendance RSVP (Phase 1.2), the athlete/parent season portfolio + habit-streak tracker (Phase 4), athlete self-report arm-care check-in (Phase 5), and the family communication surfaces. **Exception:** the tryout-registration **consent gate (Phase 1.1) proceeds standalone** — it is a live-PIPEDA compliance fix (the public form already collects minors' PII without consent), needs no accounts, and is unrelated to the parent-user question.
**Rationale:** A parent/athlete user class implies a large new user population + an identity/auth/PIPEDA/notification expansion (guardians have no app identity today — they are email-only) that isn't warranted yet. Sequencing coach value first captures the near-term ROI (tryouts, evaluation, development tooling) at zero new-user cost and lets the parent-user question be decided deliberately once the coach product is proven, rather than incurring user-scale + compliance burden up front. Owner ruling 2026-06-30, after the Phase 1 code investigation showed the family-facing items (RSVP) are where the new-user cost concentrates (and that guardians are not push/in-app reachable without building identity).
**Affects:** roadmap sequencing (defers a user class + a feature cluster), product scope (Coaches Portal stays coach/staff-identity only near-term), and indirectly platform identity/auth + PIPEDA surface. **No pricing/packaging/gate change.**
**Handoff:**
HANDOFF → `/plan`
- Hold the parent/athlete-user deferral as a standing scope boundary for Coaches Portal planning. The Tryouts & Evaluation plan + Phase 1 plan already reflect it (1.2 paused; 1.1 proceeds standalone; Phase 2/3 coach-facing = active focus). Schedule a dedicated "parent/athlete users on the platform" scoping decision at the end of the project.
**Relates to:** the 2026-06-29 Coaches Portal scope decisions (no play-by-play; aggregation upload-only; tryout/eval bundled into Premium); the separate-models coach-portal architecture (free basic vs paid rep) + the noted future "account-per-free-coach" option; the 2026-06-25 Premium Coaches Portal cancel-only exit decision.

---

### 2026-06-29 — Tryout & Evaluation suite is bundled into the Premium Coaches Portal (no per-player fee)
**Status:** Decided (ratified 2026-06-29)
**Decision:** The full tryout & player-evaluation suite — candidate check-in/bib, blind scoring, configurable rubrics, multi-evaluator scoring, ranked roster decisions, offer/release comms — and the broader development/engagement features on the Coaches Portal roadmap are **included in the existing Premium Coaches Portal at no additional charge** ($29/mo per team standalone, or included for a club's whole coaching staff under Club). **No per-player fee, no per-tryout fee, no separate add-on, no new plan, no price change.** Lone open packaging sub-question: whether the compliance/audit export (consent log) leans Club-tier — default is keep it all-Premium unless the owner wants a Club hook. (Price points themselves are unchanged and remain governed by `PLAN_PRICING_FACTS.md`.)
**Rationale:** Bundling **is** the competitive wedge. Every dedicated tryout/evaluation tool monetizes per-player ($4–$10/player/yr — TeamGenius, SkillShark) or standalone (~$799/yr — Rizzler); a 60-player club pays $240–$600/yr elsewhere and **$0 incremental here**, on a tool that also runs their roster, lineup, schedule, and accounting. A per-player or add-on surcharge would discard that advantage and tax the exact coach-adoption loop the portal is built on. Logged so no future chat re-introduces a per-player tryout charge. From the 2026-06-29 4-pass Coaches Portal brainstorm + owner ratification.
**Affects:** packaging/inclusions (what the Premium Coaches Portal includes), positioning (bundled-vs-per-player stance). **No price/plan/capacity value changes.**
**Handoff:**
HANDOFF → `/billing` (when a tryout/eval phase is built)
- Gate the suite to the Premium Coaches Portal plan (no per-player meter, no add-on SKU); reflect the inclusion in `PLAN_PRICING_FACTS.md` + `lib/plan-config.ts` in the same unit of work; run the drift check. Resolve the consent-log Club-tier sub-question with the owner.
HANDOFF → `/marketing` (later, near ship — not yet)
- Frame tryout/evaluation as **included** in the Premium Coaches Portal; lead with "no per-player fees" as the explicit contrast vs per-player competitors. Do not write surface copy until `/plan` defines the surface.
**Relates to:** the 2026-06-22 "whole coaching staff included in Club / retire per-team meter" + "$29 standalone" decisions (this extends the same bundled posture to tryout/eval); the two 2026-06-29 Coaches Portal scope decisions below (no play-by-play; aggregation upload-only); the active `COACHES_PORTAL_TRYOUTS_EVAL_PLAN.md` + PM brief.

---

### 2026-06-29 — Coaches Portal stays operations-first: no live play-by-play / scorekeeping product line
**Status:** Decided (ratified 2026-06-29)
**Decision:** FieldLogicHQ will **not** build a live scorekeeping / play-by-play (per-at-bat, per-pitch) logging surface in the Coaches Portal. Consequently the entire **pro-analytics tier that depends on play-by-play data is OUT of scope** for now — explicitly: Quality At-Bat %, situational/RISP & count splits, opponent scout cards, win-probability "game story" graphics, auto-written post-game narratives, and the proactive "coaching pulse" regression-alert digest. The Premium Coaches Portal competes on **coaching operations + tryouts/evaluation + player development/engagement**, not in-game stat capture. All planned Coaches Portal roadmap work is chosen to avoid play-by-play: the active **Tryouts & Evaluation** project (Phases 1–2) and the planned Phases 3 (roster/development), 4 (engagement/retention), 5 (pitcher safety — pitch-*count* per outing only, not pitch-by-pitch).
**Rationale:** A scorekeeping/analytics capability is a **different product identity** (it competes with GameChanger, not TeamSnap), depends on sustained per-at-bat data discipline that **volunteer coaches won't reliably maintain** on a phone mid-game, and is a months-long platform bet. A 4-pass multi-agent brainstorm (2026-06-29: tryout/eval ideation → premier-suite "wow" scan → owner-filtered consolidation) confirmed every premium-analytics "wow" feature sits on a play-by-play foundation the product deliberately won't build. Owner ruling: stay operations-first; the higher-confidence value (run tryout day, evaluate players, track development, drive engagement) needs none of it. **No pricing/packaging/gate change** — this is a product-scope and roadmap-sequencing boundary.
**Affects:** roadmap sequencing (defines what Coaches Portal phases are/aren't built), product identity/positioning (operations + development, not analytics). No price/packaging/gate values change.
**Handoff:**
HANDOFF → `/plan`
- Treat "no play-by-play / scorekeeping surface" as a **standing scope guardrail** for all Coaches Portal planning. The active `docs/projects/archive/COACHES_PORTAL_TRYOUTS_EVAL_PLAN.md` (+ PM brief) already reflects it; future phase plans (P3–P5) must stay within it. Pitcher safety (P5) is **pitch-count-per-outing**, not per-pitch logging (the optional pitch-arsenal charting idea, which would need per-pitch entry, is flagged optional/out unless a pitching-coach audience asks).
**Relates to:** the 2026-06-29 user-upload-only stat-aggregation decision below (the *only* sanctioned path to "our own metrics" given no play-by-play); the 2026-06-25 Premium Coaches Portal separate-models / cancel-only decisions; the 2026-06-28 "premium coach is a section in the one app" decision.

---

### 2026-06-29 — Cross-platform stat aggregation is user-upload-only (no vendor contracts near-term)
**Status:** Decided (direction + constraint; the feature itself is **parked**, not scheduled) — ratified 2026-06-29
**Decision:** The future vision of FieldLogicHQ **aggregating a player's stats from other platforms and producing our own metrics** is **parked** (not now). When pursued, the **near-term path is coach-initiated upload of data the coach already owns** — i.e. a CSV the coach exports from a tool they already pay for (GameChanger, Blast, Pocket Radar, etc.) and uploads to us. That path needs **no vendor contract** because the coach owns the export. **Live API integrations with those vendors (which require contracts) are a deliberately later, bigger step — not the first move.** Established fact behind the constraint: **there is no free third-party source of a specific youth player's stats** (that data lives inside paid tools); the only genuinely free public datasets are **pro-league** stats, useful solely as generic benchmark/reference lines, never per-player youth data.
**Rationale:** Aggregation is the credible long-term answer to "produce our own stats" **without** building play-by-play (see the decision above), but live integrations carry contract, cost, and dependency risk that isn't warranted pre-scale. User-initiated upload captures most of the value at near-zero partnership risk and keeps us as the **aggregation/insight layer** rather than a data-pipeline operator. Surfaced and ruled on by the owner during the 2026-06-29 Coaches Portal strategy consolidation.
**Affects:** roadmap sequencing (parks a feature + fixes its eventual build shape), monetization/partnership posture (no near-term vendor deals), product positioning (aggregation layer, not scorekeeper). **No pricing/packaging/gate change.**
**Handoff:**
HANDOFF → `/plan` (later, when un-parked)
- When this is scheduled, scope it as **import-your-own-export first** (coach-uploaded CSV → mapped onto our player profiles/metrics), explicitly **deferring vendor API integrations** to a later phase that would require contract/legal review. Do not sequence an API integration ahead of the upload path.
**Relates to:** the 2026-06-29 "no play-by-play" decision above (this is the sanctioned alternative route to our-own-metrics); the "bring-your-own-data bridge" concept from the premier-suite scan.

---

### 2026-06-29 — Single source of truth for product-defining data (governance principle)
**Status:** Decided (ratified 2026-06-29)
**Decision:** Every **product-defining data element** — plan prices, capacity/limits, availability/gating, trial lengths, founding-season dates, plan/module labels, and "what a plan includes" — has **ONE designated authority**. Every other surface (customer copy, emails, the operator console, docs) **reads/derives** from that authority and **never copies** it. Two binding corollaries: **(a) no "ghost controls"** — an operator control must *enforce* what it displays or be removed; **(b) drift is made un-mergeable** via a CI ratchet, extending the pattern already used for the DB data-dictionary and design tokens to plan facts. The canonical human-readable record remains **`PLAN_PRICING_FACTS.md`** (kept matched to `lib/plan-config.ts`). **This is governance / data-quality — NOT a pricing or packaging change; no price, plan, gate, or capacity value changes.**
**Rationale:** Platform-admin QA (2026-06-29, Persona 3.3) found plan limits shown in the operator console had drifted from what the system actually enforces, because the same fact lived in several hand-synced places. A four-domain codebase audit (plan/billing, org/subscription, roles/access, content/copy) confirmed the pattern is **systemic — ~17 instances, several already wrong on customer-facing surfaces** (e.g. a public page describing Club with retired "three Coaches Portal accounts" language while the comparison table on the same page is correct; prices hardcoded into transactional emails; the founding-season date hardcoded in ~10 places). Each duplicated fact is a latent incident; some are live. The principle converts a recurring, manual reconcile into a structural guarantee, protecting customer trust at exactly the moments it's most fragile — price changes, promotions, and plan launches.
**Affects:** governance/process (binding rule for all agents and chats), positioning integrity (customer-facing consistency), roadmap sequencing (a dedicated hardening initiative), and — indirectly — every surface that currently restates a plan fact. No price/packaging/gate values change.
**Handoff:**
HANDOFF → `/plan`
- Owns the execution: `docs/projects/archive/SOURCE_OF_TRUTH_HARDENING_PLAN.md` (+ PM brief) — a 17-item findings register and 6 phases sequenced by customer blast radius (P0 live drift → kill copy-pasted constants → kill ghost controls → one access resolver → subscription-state reconciler → CI drift ratchets).
HANDOFF → `/marketing`
- Live customer-facing copy drift to correct: Club still described with retired **"three Coaches Portal accounts"** on `/pricing` + `/for-leagues` (should convey **"whole coaching staff included — every team, no per-team fee"** per the 2026-06-22 repackaging); a marketing email naming the product **"Tournament Coach Portal"** (canon = **"Coaches Portal"**); help text framing Accounting/Rep Teams as **"available as an add-on"** (à-la-carte add-ons were retired — bundled-only). Then derive remaining price/inclusion copy from the canonical source where structurally possible.
HANDOFF → `/billing`
- Owns the **`subscription_status`-vs-Stripe reconciler** (single idempotent authority; close the admin-demote-orphans-a-live-Stripe-sub and cancel-writes-DB-before-Stripe gaps — the H14 family).
HANDOFF → `/dba`
- Owns the **enforce-vs-remove** decision for the two display-only stores that caused the ghost-control bugs: `plan_config_overrides` (plan limits) and `platform_plan_module_entitlements` (the "Feature Matrix") — make them enforce, or remove the editors and read code config directly.
**Relates to:** the **2026-06-29 H8 decision** (unifying the in-app upgrade card onto the live gating toggle is a concrete *instance* of this principle — one authority for "is this plan available"); the **2026-06-22 Club repackaging** (the stale "3 accounts" copy is unpropagated packaging drift); and the existing DB-dictionary + design-token CI ratchets (the mechanism to extend). One P0 live fix already shipped this session: the Venue Library nav was hidden for **Club · Association** (sidebar gate omitted `club_large`) — corrected.

---

### 2026-06-29 — Plan-launch mechanics: the live "availability" toggle opens self-serve checkout *everywhere*, deploy-free (H8)
**Status:** Decided (ratified 2026-06-29)
**Decision:** The customer-facing **in-app upgrade card** (org admin → subscription/billing) will read the **live plan-availability toggle** managed in platform-admin → Plans & Pricing — the same DB-backed gating source the public **/pricing** page already reads — instead of today's **code-baked** plan setting that only changes on a deploy. Net effect: flipping a plan to **"Live"** in the console opens self-serve checkout **everywhere at once** (public pricing **and** the in-app upgrade button), with **no code release required**. This is the operational unlock for launching **Club · Association** (and any future tier) on an operator's schedule rather than an engineering deploy. **Hard guard (non-negotiable):** because the in-app Upgrade button starts a **real Stripe checkout**, it must stay **closed for any plan with no Stripe price configured**, even when toggled Live — so launch-readiness is gated on price IDs being wired first. **Sequencing:** wire the **Club · Association Stripe prices** before relying on the toggle to open in-app checkout for it.
**Rationale:** Surfaced during platform-admin QA (2026-06-29, walkthrough 3.1): the console toggle was **"half-powered"** — toggling League Plus to Live correctly updated the public /pricing page but the in-app upgrade card still read "Coming soon" until a deploy. Two divergent sources of truth (DB toggle for /pricing, code constant for the in-app card) make every launch require an eng release and risk the two surfaces disagreeing. Unifying on the live DB toggle makes "go live" an operator action, removes deploy-coupling from launches, and keeps the storefront and the actual buy-button in lockstep. The price-configured guard prevents the new power from exposing a broken checkout. **No pricing or packaging change** — this is launch *mechanics*, and it directly enables the already-decided Club · Association GTM.
**Affects:** gate mechanics (in-app gating source of truth), plan config, roadmap sequencing (Club launch path), and — indirectly — GTM/acquisition (lets the founder open tiers without eng). No price/packaging change; no marketing-copy change.
**Handoff:**
HANDOFF → `/billing`
- Own the implementation: point the in-app upgrade card's gating at the **live DB availability toggle** (the same resolver the public /pricing page uses) instead of the code-baked plan-config status, and add the **"no Stripe price ⇒ checkout stays closed"** guard so a plan toggled Live without a wired price can't open a broken checkout. Reconcile against **`PLAN_PRICING_FACTS.md`** and run the drift check. Keep the existing dev/local override paths intact. Treat as a money-touching, high-risk gating change (review before release).
**Relates to:** the 2026-06-26 "in-app paid→paid upgrade proration is not built" decision (both concern the in-app self-serve checkout path and its limits — sequencing note: opening higher tiers in-app via this toggle should account for the still-open paid→paid upgrade gap, i.e. an *existing paying* org upgrading still needs the Dashboard until that path is built); the Club · Association repackaging launch; the platform-admin Plans & Pricing approval-queue flow.

---

### 2026-06-29 — "Plan / Tier Trial" capability (operator-granted, timed, auto-reverting)
**Status:** Proposed (owner interested; explicitly deferred — relabelled the existing surgical grant for now instead of building this)
**Decision (proposed):** Build a **whole-plan timed trial** that a platform operator can grant to an org for a window, after which access **auto-reverts** to the org's base plan. Today the entitlement-override system can only (a) grant a **single section** for a window (the "Feature Access Trial", formerly "Module Trial") or (b) force a **subscription status**; there is **no way to let an org experience a whole higher tier** (e.g. "give this club League Plus for 14 days, then drop back") with hands-off reversion. This is a distinct tool from the surgical section grant — it trials the *bundle/tier*, matching the plans-first way the business reasons about access.
**Rationale:** Surfaced during platform-admin QA (2026-06-29, walkthrough 2.1) while clarifying that the app grants access by **plan** (a bundle of sections), not by individual module. A timed tier-trial is a real **sales/conversion + retention lever** — it lets the founder-managed early-access motion (and later support) put a prospect into the actual higher-tier experience risk-free and time-boxed, without manual cleanup or double-billing risk. It also closes the gap between the operator mental model ("trial a plan") and the only tools that exist ("trial a section" / "force a status"). Deferred now because relabeling the existing grant removed the immediate confusion and the build is non-trivial.
**Affects:** roadmap sequencing; operator entitlement tooling; (indirectly) GTM — supports the managed League/Club early-access trials. **No pricing/packaging change.**
**Handoff:**
HANDOFF → `/plan`
- Own the implementation plan + PM brief for an operator-granted, timed, auto-reverting **Plan/Tier Trial**. Technical anchor: the long-deferred `plan_tier` grant type in `lib/entitlement-grants.ts` (designed but never built; the override write route currently **rejects** it, and it needs an **effective plan-rank threaded through plan-feature gating** so a trial tier outranks the base plan for the window). Reuse the existing timed-grant engine (active-window + auto-revert + audit) rather than a parallel mechanism; decide how a tier trial composes with section grants and status overrides.
- Scope guardrails to call out: must not create a Stripe subscription or bill anything (access-only, like the other overrides); must auto-revert cleanly; should be operator-visible in the same Active Overrides surface.
**Relates to:** the timed-entitlement grant engine (now enabled locally, prod-off behind `ENTITLEMENT_GRANTS_ENABLED`); the 2026-06-22 managed early-access decision (a tier-trial would support that motion); the "plans are bundles of sections" access model clarified during QA.

---

### 2026-06-28 — Mobile app + public tournament discovery: one role-routed app, web-first, store-wrap on trigger
**Status:** Superseded by **[2026-07-11 — Unified app RATIFIED (G1)]** + **[2026-07-11 — Store-wrap triggers (G5)]** — direction upheld, ratified, and extended (per-tournament install identities retired; trigger list pre-agreed). ~~Proposed (owner accepts the direction in principle; no build committed)~~
**Decision (proposed):**
1. **ONE role-routed app, not several.** A single branded FieldLogicHQ app serves four end-user audiences through login-driven sections: (1) tournament **fan**, (2) tournament **coach**, (3) tournament **admin/volunteer**, (4) **premium coach**. Premium coach stays a **section inside the one app**, not a separate app — revisit splitting only if retention data later justifies it.
2. **Distribution model = "one app, distributed on the web now and in the stores later." The web app *is* the app.** Build features on the web (lands for all users immediately, no app-review tax); when a trigger appears, wrap the finished web app in a **thin native shell (Capacitor-style)** and submit to Apple App Store + Google Play. Explicitly **NOT** a separate rebuilt-from-scratch native codebase (React Native) — that discards the existing mobile-first UX investment and adds a permanent second-codebase maintenance tax for no audience-visible benefit.
3. **Wrap on a trigger, not a date.** Pursue the store wrapper when one appears: (a) demand for App-Store presence/credibility from owners or prospects, (b) iOS push-notification reliability complaints (the one real gap the existing PWA leaves), (c) a premium-coach daily-use product case, or (d) a native-hardware need.
4. **Public tournament discovery directory = the strategic crown jewel / marketing flywheel.** Every tournament on the platform discoverable in one place → network effect (more tournaments → more fans browsing → more organizers want in). **Build on the public WEB first** (SEO-indexable, shareable, search-discoverable) — an app-only directory throws away the marketing value. The same directory becomes the app's logged-out front door. Highest-leverage first concrete piece; helps the platform whether or not the store app ever ships.
5. **Directory visibility = OPT-IN per tournament.** ⚠️ **This specific point was SUPERSEDED by [2026-07-24 audit] → see the 2026-07-22 "Tournaments are DISCOVERABLE BY DEFAULT (opt-out)" decision, which flipped tournaments to listed-by-default with an opt-out toggle.** (The rest of this entry — the app-identity direction — was already superseded by the 2026-07-11 G1/G5 entries; this note corrects the one remaining stale statement the earlier supersession didn't cover.) ~~Default is **not** public, so we never publish data an organizer didn't deliberately choose to surface~~ — the PIPEDA rationale still holds (no player PII is surfaced by the flip; the event-level opt-out toggle is the privacy countermeasure). Live scores/schedules are the logged-out hook; anything with player PII stays behind login.
**Rationale:** Motivation is **end-user experience + credibility**, not internal ops. The platform is already mobile-first with a working PWA, so the marginal value of native is store presence + reliable iOS push — both captured cheaply by a wrapper, expensively by a rewrite. The directory is the real growth asset and is a *web* feature (SEO/shareability) that an app surfaces, so web-first both de-risks and front-loads the value. (Strategy discussion, 2026-06-28.)
**Costs (when wrapping):** Apple Developer Program **$99 USD/yr** ongoing (+ Mac or cloud-Mac build service ~$20–60/mo); Google Play **$25 USD** one-time. Wrapper is **one effort covering both platforms**. Apple may scrutinize "just a website wrapper" apps — native push + existing offline shell + browsable directory give it enough native substance to clear review.
**Affects:** roadmap sequencing, positioning/credibility, marketing acquisition (directory = top-of-funnel), product (opt-in visibility control). **No pricing change.**
**Handoff:**
HANDOFF → `/plan`
- Own the phased plan + PM brief. First concrete piece = the **opt-in public tournament discovery directory on web** (visibility toggle default-off, logged-out browse of live scores/schedules, PII behind login, SEO/shareable). Native store-wrap is a later, trigger-gated phase — scope it but don't sequence it ahead of the directory.
HANDOFF → `/marketing` (later, not yet)
- When the directory nears ship: positioning/copy for the public directory as a discovery surface + acquisition framing; messaging for the eventual store presence. Do not write directory copy until `/plan` defines the surface.
**Relates to:** the existing fan PWA / offline shell + alerts work, the Coaches Portal architecture (premium-coach section), and the public tournament experience.

### 2026-06-28 — Publish public release notes + a "shipped + loose horizon" forward view
**Status:** Decided (ratified 2026-06-28)
**Decision:** FieldLogicHQ will communicate product progress publicly via: **(1)** a public **`/changelog`** page on the marketing site listing shipped items (grouped New / Improved / Fixed), plus an **"On the horizon"** section of **3–6 undated themes** — deliberately **no dated public roadmap**; **(2)** an **in-app "What's New"** surface for logged-in admins & coaches with a seen-badge; **(3)** **no email channel in V1** — held for *major* releases later, to the existing consented early-access list (`release_notifications_consent`); **(4)** note generation is **draft-then-approve**, hung off the existing `/release` flow — **never fully auto-published**.
**Rationale:** Visible momentum drives retention/trust for monthly subscribers; a public changelog is self-serve answer to "do you support X / what's coming" and serves as sales proof next to `/pricing`. **Undated** horizon (not a committed roadmap) captures the trust upside without owning broken public deadlines or tipping the schedule to competitors. Human-in-the-loop publish prevents internal jargon/premature context leaking to customers. No-migration V1 keeps build cost/risk low (notes ship as versioned content with each release, so notes and features cannot drift).
**Affects:** positioning (public momentum/transparency posture), marketing site (new `/changelog`), in-app admin/coach chrome, the `/release` process, roadmap sequencing.
**Handoff:**
HANDOFF → `/marketing`
- Author the `/changelog` page copy + section framing (New / Improved / Fixed; "On the horizon" intro) in brand voice; ensure it reads as proof-of-momentum, not a promise of dates.
- Tone-check generated release-note drafts at publish time (plain customer language, no internal scope jargon).
- Add a changelog link in marketing nav/footer alongside `/pricing` + persona pages.
HANDOFF → `/plan`
- Owns `docs/projects/archive/RELEASE_NOTES_CHANGELOG_PLAN.md` (+ PM brief) — P1 public changelog + content model, P2 in-app What's New + seen-badge, P3 `/release` draft-and-tag automation.
**Not in V1 (deferred):** email-on-release to the consented list; cross-device seen-state; a "tell us what to build next" feedback hook from the horizon section.
**Relates to:** the `/release` agent workflow and the existing early-access consented-email list.

### 2026-06-26 — In-app paid→paid UPGRADE proration is not built (resolves Club-band open question)
**Status:** Decided (current-state truth + interim posture; tracks a build gap)
**Decision:** There is **no in-app path to upgrade between paid plans with proration** — and that is the **known, accepted interim state**, not a silent bug. Self-serve checkout always opens a *new* subscription, so an org that already pays and "upgrades" to a higher paid tier (Tournament Plus → League Plus, or **Club → Club · Association**) would get a **second, parallel subscription = double-billing** rather than a re-priced single one. In-app re-pricing-with-proration exists **only on the downgrade direction** (a paid→lower-paid or paid→free move reconciles Stripe correctly). **Interim rule until the upgrade path is built: paid→higher-paid moves must be done in the Stripe Dashboard** (edit the subscription item + apply proration manually); do not route an existing paying org through self-serve checkout to upgrade.
**Resolves:** the `CLUB_REPACKAGING_PLAN.md` open question "Mid-cycle band changes (Club ⇄ Club · Association): proration + upgrade/downgrade path." Answer: **downgrade = handled in-app with proration; upgrade = NOT built (Dashboard-only today).**
**Rationale:** Surfaced by a billing-agent code trace (2026-06-26) while defining the platform-admin "plan change ≠ billing change" operator copy (walkthrough finding B.1). Worth a logged entry because it's a real revenue/trust risk (double-billing a customer who tries to pay us *more*) and it gates the smooth-upgrade story for the new Club bands — leaving it as an un-owned "open question" is how it gets shipped around.
**Affects:** billing mechanics, Club-band upgrade experience, operator/owner billing guidance, roadmap.
**Handoff:** → `/plan` scope an in-app paid→paid upgrade-with-proration path (detect an existing active subscription and re-price the item with `create_prorations` instead of opening a new checkout); prioritize before Club · Association upgrades become common. · → `/billing` until then, guard self-serve checkout against creating a second subscription for an already-paying org, and ensure the platform-admin walkthrough B.1 copy + operator guidance state plainly that upgrades are Dashboard-only and the operator plan-change never calls Stripe (its only Stripe-touching control is Cancel Subscription). · → `/marketing` no customer copy should imply a one-click paid-to-paid upgrade until the path exists.
**Relates to:** the 2026-06-22 Club capacity-bands decision (this is the unbuilt half of its mid-cycle move story).

### 2026-06-25 — Premium Coaches Portal exit: cancel-only, no downgrade-to-free
**Status:** Decided (ratified 2026-06-25)
**Decision:** A Premium Coaches Portal coach has **no self-serve "downgrade to free (Basic Coaches Portal)" path** — by design. **"Cancel Premium" is the single exit**: it cancels the subscription, the Premium workspace goes inactive into the retention window, and the only return is **reactivating the same Premium** within that window (after which it purges). **No reverse data migration** (Premium roster/schedule/fees are *not* carried back into the free Basic team) and **no data export** on cancel. We deliberately do **not** make it easy to flip-flop between paid and free.
**Rationale:** Coaches don't need an easy paid↔free bounce, and because free Basic and Premium are **separate data models** (free is a *subset* — lineups/attendance/budget/documents don't exist in free), a "downgrade" would be inherently lossy. Cancel-with-reactivation is sufficient. Chosen as the "make cancel honest & safe" option over building parity, **minus the export** (owner ruling, 2026-06-25, after a code-mapped analysis of the org-vs-coach exit paths).
**Contrast:** Intentionally **differs from orgs**, where Tournament Plus → free Tournament is a clean self-serve downgrade (same org flips to the free tier, data retained). For coaches it is cancel-only.
**Affects:** packaging/exit model, coach cancel copy, coach-portal data hygiene. Pre-launch — the intended model from launch, not a change to live behavior.
**Handoff:** → `/marketing` (pre-launch) the Cancel Premium flow must state up front, *before confirm*, exactly what cancel does: the Premium portal + its data stay only through the reactivation window then are removed; the original free team remains but with **pre-upgrade data only**; Premium-era work does not carry back — so there is no "where did my data go" surprise. · Code: a data-hygiene fix (a cancelled/purged portal must not leave the free team pointing at a dead workspace) is being done this session, outside this log. · **Not building:** a downgrade-to-free UI, a reverse migration, or an export.
**Relates to:** the coach-portal free-vs-paid **separate-models** architecture decision (org-less `basic_coach_*` free vs org-scoped `rep_*` paid; copy-migrate on upgrade; "account-per-free-coach" unification noted as a future option). This exit-model decision is downstream of that separation.

### 2026-06-22 — Adopt a Business Decisions Log + `/strategy` steward
**Status:** Decided
**Decision:** Durable business decisions (pricing, packaging, positioning, GTM, monetization) are tracked here and stewarded by the `/strategy` agent, which hands copy to `/marketing`, gates to `/billing`, and plans to `/plan`. Scope = all business decisions, not just pricing.
**Rationale:** Pricing/product decisions were scattered across memory files, brand docs, and the README with no single source of truth and no owner to keep customer-facing copy in sync — which is how contradictions arise (e.g. the free-plan email-gate inconsistency below).
**Affects:** process/governance.
**Handoff:** none (meta).

---

### 2026-06-22 — Plan & pricing single source of truth + drift governance
**Status:** Decided
**Decision:** Establish ONE canonical reference for live plan/pricing/packaging facts — `docs/agents/strategy/PLAN_PRICING_FACTS.md`, kept matched to the app's plan configuration — and stop restating prices/names/gates in other docs. Brand strategy (§5/§7), the pricing-copy appendix, and the pricing memory file become **pointers**, not copies. `/strategy` runs a **drift check** (the checklist at the bottom of the Facts doc) on every pricing/packaging change and before any billing release; any agent touching a pricing fact **reconciles to the Facts doc and flags divergence to `/strategy`** instead of silently writing a new number. Any change updates the Facts doc + app config in the same unit of work.
**Rationale:** The same fact lived in ~5 hand-maintained places and drifted — the League/League Plus split was right in 2 docs and wrong in 3, surfacing only by luck during the marketing pass. Fewer copies + one owner + a check-on-change prevents customer-facing contradictions (a plan card promising what the table gates away, a price that disagrees with the app).
**Affects:** process/governance, all pricing/packaging docs.
**Handoff:** done this session — Facts doc created; brand §5/§7 + pricing memory converted to pointers; `CLAUDE.md` guardrail added. → `/marketing` convert the `PRICING_PAGE_COPY.md` appendix table to a pointer in its next pass.

### 2026-06-22 — League naming: free "League" floor + paid "League Plus" ($89)
**Status:** Decided (ratified 2026-06-22)
**Decision:** The $89/mo paid house-league tier is **"League Plus"**; a **free "League" floor** sits beneath it — mirroring Tournament (free) → Tournament Plus (paid). This **confirms the deliberate 2026-06-13 rebrand** (`docs/projects/archive/LEAGUE_REBRAND_PLAN.md`) and **corrects** the earlier framing (in this log + `BRAND_STRATEGY.md` §5/§7) that treated "League Plus" as a typo. Internal keys unchanged (paid tier `plan_id='league'`; free floor `free_floor='league_starter'`). Never write "League Starter"/"Starter" in customer copy.
**Rationale:** The newer, deliberate rebrand — with its own plan and already live in customer copy and the app label — supersedes the older general "four tiers: …League…" statement in the brand doc. The free→paid "Plus" parallel is learnable and feeds the free-tier acquisition funnel; reverting would orphan a shipped free tier. **Caveat:** the free "League" floor is held for a later public launch, so "League Plus" reads oddly in isolation until then — a launch-sequencing wrinkle, not a naming problem.
**Affects:** positioning, pricing copy. (App plan-config label was already correct.)
**Handoff:** → `/marketing` ensure `/pricing` + `/for-leagues` reflect League (free) / League Plus (paid) when the free floor launches; brand §5/§7 already corrected this session.
**Supersedes:** the "League Plus is a typo / canonical is League" note in the early-access entry below, and any "League $89" line in `BRAND_STRATEGY.md` §7.

### 2026-06-22 — No separate "select/development" team pricing
**Status:** Decided (ratified 2026-06-22)
**Decision:** Select and development teams are treated **identically to rep teams** on the platform — they count equally toward the Club capacity bands and receive the full platform (scheduling, accounting, budgeting, lineups, Coaches Portal). There is **no discounted lighter tier** for mid-level teams; whether to put them on the platform is the club's choice. This **resolves the open question** that previously gated the two Club decisions below: every team a club adds counts toward the band, and each gets a portal.
**Rationale:** No "select"-grade product exists to justify a discount, and every team consumes the same services even if a given team uses fewer of them. A separate cheaper class would also invite gaming the band thresholds. (Owner ruling, 2026-06-22 strategy session.)
**Affects:** plan config, pricing copy, positioning.
**Handoff:** → `/marketing` band/portal copy must convey "every team counts, full tools for each" — no implied select discount · → `/billing` band team-count includes all team types equally.

### 2026-06-22 — Coaches Portal: include the whole coaching staff in Club; retire the per-team meter
**Status:** Decided (direction; dollar thresholds set by the capacity-band decision below) — *ratified 2026-06-22*
**Decision:** Include the Premium Coaches Portal for a club's **whole coaching staff** in the Club plan; **retire the "$19/mo per team beyond 3" meter** entirely. Keep the **$29/mo standalone** portal for org-less coaches (a different buyer — do not cannibalize). Club-size differences are captured by the capacity bands below (and, later, payment volume), not a per-team meter.
**Rationale:** ~85–90% of Club buyers run more than 3 teams (median ~10), so "3 included + $19" made Club's *real* price ~$270–350/mo and taxed the exact coach-adoption loop that is the product's main in-org advocacy surface. Per-team metering also collided with the "no seat limits" posture and the free Basic floor. (Pricing & product review, 2026-06-20; club-size research, 2026-06-22.) Open question on mid-tier teams **resolved** by the "no separate select pricing" decision above.
**Affects:** plan config, pricing copy, positioning.
**Handoff:** → `/billing` retire the per-team meter; whole-staff portals included in Club · → `/marketing` re-frame Club as "whole coaching staff included," and **rewrite the coach pricing bridge** (the "$19/mo, $10 less than standalone" line is gone — new bridge: standalone $29 until your org joins Club, then your portal is *included*, you stop paying) · → `/plan` rollout plan.
**Supersedes:** the "3 team accounts + $19/additional team" structure in `BRAND_STRATEGY.md` §7 and the Segment 3/4 pricing-bridge copy.

### 2026-06-22 — Club pricing structure: capacity bands, not flat and not per-team meter
**Status:** Decided (structure + 15/30 boundaries; price points are the launch anchor, calibratable Q1 2027) — *ratified 2026-06-22*
**Decision:** Price Club as **two flat capacity bands**: **Club** (full platform + whole-staff coach portals, up to **15 teams**) at **~$219/mo**, and **Club Large / Association** (15–**30** teams) at **~$379/mo**, **custom above 30**. Band boundaries are **fixed at 15 and 30 teams regardless of team type** (all teams count — see "no separate select pricing"). The $219 / $379 figures ship as the **working launch anchor** and recalibrate from real conversion data in Q1 2027.
**Rationale:** A single flat price either undersells big associations or prices out small clubs; two bands capture both while staying predictable (volunteer treasurers hate variable bills) and avoiding per-unit nickel-and-diming. Boundary placed at the natural club/association gap so the numerous 8–12-team buyers sit cleanly inside the lower band. (Strategy discussion, 2026-06-22.)
**Affects:** plan config, pricing copy, positioning, README.
**Handoff:** → `/billing` two-band Club at 15 / 30 caps; custom quote above 30 · → `/marketing` band cards + comparison table + `/for-clubs` "what you pay"; **settle the "Club Large / Association" plan name** (brand canon owns plan names) · → `/plan` build plan.

### 2026-06-22 — Reprice Club upward from $179 (realized via the lower band)
**Status:** Decided — *ratified 2026-06-22*
**Decision:** The standard Club price moves up from $179; the uplift is **realized by the ~$219 lower band** above (the $199–249 range is superseded by that concrete figure). A **flat $179 Founding-rate lock** remains on the books as a *forward-looking* principle (never raise prices on a cohort you're converting) but is **dormant — there are currently no founding $179 clubs**, so no grandfathering is required. Calibrate the standard figure from conversion data in Q1 2027.
**Rationale:** The typical buyer is a large, high-value club; Canadian comparables (Crossbar ~$1,350 CAD/yr, Uplifter Gold) and module depth support a higher anchor. Adversarial review rejected an immediate jump to $229+ as premature — hence anchor-now / calibrate-later. (Pricing & product review, 2026-06-20.)
**Affects:** pricing copy, plan config.
**Handoff:** → `/marketing` Club priced at the new band anchor; founding-rate-lock framing held in reserve (no active cohort) · → `/billing` standard Club rate = lower-band price for new subscribers.
**Supersedes:** the $179/mo Club price in `BRAND_STRATEGY.md` §7.

### 2026-06-22 — Monetization: facilitated payments are the real size-scaler (scope Q4 2026, launch H1 2027)
**Status:** Decided (direction + sequencing). Rate / who-pays default / processor / legal posture remain **Proposed** pending Q4 scope. — *ratified 2026-06-22*
**Decision:** Pursue facilitated payments (a participant→org take-rate, family-pays default) as the primary mechanism that scales revenue with club size and funds the free floors. **Formally scope in Q4 2026; target H1 2027 launch** — deliberately *not* before the Jan 2027 billing cliff. The current "no payments" posture was always a **temporary setup-phase deferral**, not a strategic stance against payments — this is the planned graduation once the app is stable/productionalized, not a reversal of conviction.
**Rationale:** Every dominant Canadian competitor (RAMP, TeamLinkt, Spond) gives software away and earns on payments; a flat subscription alone leaves the biggest value metric uncaptured and is a harder sell against free. Sequencing matters: KYC friction + trust risk during a "free" season make a pre-cliff launch dangerous. (Pricing & product review, 2026-06-20.)
**Affects:** monetization model, roadmap, positioning.
**Handoff:** → `/plan` write the facilitated-payments scoping plan + PM brief (Q4 2026). **Must resolve in scope:** the "~2.5% + $0.30" figure is ambiguous and likely mis-stated — clarify whether it is FieldLogicHQ's **margin on top of** processor cost (~2.9% + $0.30, viable, lands ~5.4% all-in in competitor range) or an **all-in** rate (underwater on every transaction). Also resolve family-pays-vs-org-pays default, processor choice, and Canadian KYC / money-transmission / refunds / chargeback posture with counsel.

### 2026-06-22 — GTM: serve all club sizes via structure; focus acquisition on the mid-size beachhead
**Status:** Decided — *ratified 2026-06-22*
**Decision:** Pricing serves both small clubs and large associations (via bands + future payments), but **acquisition focus** is the mid-size, dual-stream suburban club (~8–14 rep teams) — numerous, able to pay (sits in the $219 lower band), self-serve-able — then land-and-expand up to large associations and down to small clubs. This is a **focus, not an exclusion**: a large association or small club that walks in is served; the spear simply points at the middle.
**Rationale:** Pricing architecture ≠ go-to-market focus. Inclusive pricing and a concentrated sales motion coexist; the mid-size dual-stream club is the lowest-hand-holding, highest-volume entry point and aligns with the tournament-funnel thesis. (Strategy discussion, 2026-06-22.)
**Affects:** positioning, GTM.
**Handoff:** → `/marketing` persona-page emphasis and acquisition messaging centered on the mid-size dual-stream club.

### 2026-06-22 — Founding-Season conversion mechanics: September annual lock-in opt-in
**Status:** Proposed — *owner deferred 2026-06-22; revisit when a real founding cohort exists and the cliff date is firm.*
**Decision (proposed):** Convert the Jan 2027 billing cliff into a warm opt-in — an annual-commitment offer ("commit annual before the deadline, lock your founding rate") to orgs that have **actually run a real event**, with the offer **triggered by having run that event rather than a fixed calendar date** (tournament orgs in early autumn; season-based League/Club orgs later, once they've lived a season). **Not** a graduated discount ramp (rejected — it creates a second cliff).
**Rationale:** The Founding Season has no conversion mechanics; an event-triggered annual opt-in creates prepaid, committed founding customers without anchoring the cohort to a discount. **Deferred** because it is entirely forward-looking — there is no founding cohort yet, the cliff is ~7 months out, and the send is legally gated regardless; nothing is lost by deciding it closer to the event with more information. (Pricing & product review, 2026-06-20.)
**Affects:** lifecycle/email, billing.
**Handoff:** none yet (deferred). **When activated:** → `/marketing` the opt-in email/banner · → `/billing` annual checkout. **Blocked on:** CASL consent posture (signup captures opt-out, not express opt-in) — confirm lawful basis with counsel before any commercial send.

### 2026-06-22 — Open League + Club to managed early-access after a product-hardening sprint (~Aug 2026)
**Status:** **Superseded by [2026-07-28 — League & Club: the managed early-access cohort is WITHDRAWN; League is parked pending a full capability evaluation].** The checklist this entry made the open date contingent on was written but never walked; the cohort is withdrawn rather than re-dated, and the `/plan` checklist handoff below is cancelled. The **cohort shape (5–10 Club, 10–15 League, invite-only, founder-managed, under comp) survives as an input** to the future capability evaluation. — *originally ratified 2026-06-22; superseded 2026-07-28*
**Decision:** Open **League** and **Club** to a **founder-managed** early-access cohort (5–10 Club, 10–15 League) under the Founding-Season comp, **after** the core flows are self-serve-verifiable by a real admin. Target ~August 2026. Don't open today; don't run open-door self-serve.
**Rationale:** You can't validate willingness-to-pay on the highest-ACV tiers while they're non-purchasable — and every Club decision above is currently untested against a single paying club. A small managed cohort produces that validation (and the cohort the deferred conversion play would later convert) without an open-door launch that accelerates the cliff and damages word-of-mouth. (Pricing & product review, 2026-06-20.)
**Affects:** GTM, roadmap.
**Handoff:** → `/plan` define the "ready to open" go/no-go checklist + hardening scope (this gates the open date). **Naming (resolved 2026-06-22):** the paid tier opened here is **"League Plus"** ($89) — see the League naming decision above. ~~Earlier note treated "League Plus" as a typo; that was wrong and is superseded.~~

### 2026-06-22 — Fix the free-tier email-gate contradiction
**Status:** Decided — *ratified 2026-06-22*
**Decision:** Resolve the contradiction where the free Tournament card promises "basic team/contact email" but the comparison table gates all email to Plus: **a basic all-team announcement is free**, and **templates, scheduling, delivery tracking, and segmentation are gated to Tournament Plus**. Reframe the Plus story as "announcements free, communications **workflow** paid."
**Rationale:** Contradictory copy on the primary acquisition page reads as a broken promise; this also closes a competitive gap (free competitors give basic email). Low-risk — the product already supports basic email on free. (Pricing & product review, 2026-06-20.)
**Affects:** pricing copy, comparison table, plan config.
**Handoff:** → `/marketing` reconcile the free card + comparison table around "announcements free, workflow paid" · → `/billing` enforce the split and add a **light volume guard on the free announcement** (mirror the Basic coach floor's ~100-contact / 10-sends-per-24h cap) to prevent abuse.

### 2026-06-22 — Exclude officials/scorekeepers from the free-tier 3-seat count
**Status:** Decided — *ratified 2026-06-22*
**Decision:** Don't count officials/scorekeepers (the most restricted, score-entry-only role) against the free Tournament plan's 3-seat cap; keep the cap for admin/staff seats only.
**Rationale:** Counting them punishes orgs running legitimate multi-field events with zero monetization upside — and those multi-field events show the product at its best. No abuse vector, since it's the most restricted role by design. Trivial change. (Pricing & product review, 2026-06-20.)
**Affects:** plan config, FAQ.
**Handoff:** → `/billing` exempt the score-entry-only roles (officials/scorekeepers) from the free-tier seat cap · → `/marketing` one-line FAQ clarification.

---

## Findings & context (not decisions — the evidence base behind the proposals above)

- **Competitive landscape (2026-06-20 review, 15 competitors):** The Canadian grassroots market is dominated by *free* software (RAMP, TeamLinkt, Spond) funded by payment take-rates (2.9–5% + fixed). US incumbents charge real money but are mostly USD-only, demo-gated, and opaque — a transparency/CAD gap FieldLogicHQ can exploit. Tournament Plus ($39) and League Plus ($89) are well-calibrated vs comparables; Club ($179) was under-priced; Premium Coaches Portal ($29) is defensible after FX (≈ TeamSnap Ultra).
- **Typical Canadian club size (2026-06-22 research, medium confidence):** Median target Club buyer runs **8–12 rep/competitive teams** (~10). Small 3–6, large 18–32+. ~85–90% of Club buyers exceed the old 3-included floor. By sport: hockey 8–12 (10–20 combined genders), softball 6–8 (9–13 with select), basketball 12–18, soccer 10–20, baseball 8–14. No official source — triangulated from real club rosters.
- **Adversarial review caution:** 10 of 16 aggressive "do-it-now" pricing moves were rejected as premature at zero customers. Theme: the Founding Season is a *product-validation* window; price-point fine-tuning becomes answerable only after 30–50 orgs run real seasons.

## Source documents
- Pricing & product review (2026-06-20): competitive analysis + 7 lenses + adversarial verification (run via multi-agent workflow).
- Canadian club team-count research (2026-06-22): per-sport rep-team counts + verification.
- `memory/project_pricing_strategy.md`, `memory/project_free_tier_strategy.md`, `docs/agents/brand/BRAND_STRATEGY.md`, `docs/agents/brand/PRICING_PAGE_COPY.md`.
