// Consumer support has no dedicated help page (all Help lives in the admin/coach portals),
// so support rows open a pre-addressed email — the right home for installed-app users who
// never see the marketing site. Owner call 2026-07-20. One home for the address: the
// stacked Account page and the desktop Help section both mount it.
export const SUPPORT_MAILTO = 'mailto:hello@fieldlogichq.ca?subject=FieldLogicHQ%20support';
