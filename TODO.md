# FieldLogicHQ — Project TODO List

This file tracks the ongoing tasks for the FieldLogicHQ platform (multi-tenant sports club and league management). AI models and the USER use this to coordinate work.


---

- [ ] **Unified App — Consumer Layer** — one-app strategy build-out. **Phases 0+1 COMMITTED; Phase 2 (fan accounts & follows) Slices 1+2 COMMITTED to dev (9fa97c07 + prior; mig 186 fan_follows still DEV-only, RLS-verified).** Slice 1 = account-at-follow bottom sheet + "just this device" escape, device→account claim, account-aware `/following`, fan access-context. Slice 2 = Following tab groups each followed team into Live now / Coming up / Recent (reuses the site's existing live-game-window logic, cross-tournament); Scores tab's "your live teams" strip removed (platform-wide live board only, no overlap with Following); in-app help synced. **Slice 3 BUILT 2026-07-14 — uncommitted, mig 188 fan_alert_prefs DEV-only (decision LOGGED in BUSINESS_DECISIONS.md): score alerts require a signed-in account — "Followed teams" card on Account → Notifications (two global toggles; honest "not offered" line names free-tier events; ?focus=fan deep link), account-routed delivery merged into all 4 fan dispatchers (legacy anonymous rows still served, new anonymous opt-ins → 410), public surfaces reworked to sign-in pitch / account switches, help docs synced; per-team + game-day-only DEFERRED. Closes Notification Settings Phase 3. PHASE 2 FEATURE-COMPLETE pending owner browser/device test (Slice 3 since COMMITTED 7d626328).** **Phase 3 (one-home connective tissue) COMMITTED to dev 2026-07-14 (0a69bebe, owner-approved; NO migration; brief rev 2 SIGNED OFF, strip → account chip + sheet per owner; + test-pass polish: /home & /auth moved into consumer shell, Account tab tightened + first consumer sign-out, fan-first signup links): "Your FieldLogicHQ" switcher home w/ compact live-aware Following section; tournament top-bar account chip → sheet w/ Coach view / Open admin / Scorekeeper rows for hats the account owns on that event; Fan view backlinks in the coach portal; Coaches Portal row+link in the consumer shell. §8 return-to-directory RATIFIED deferred → next design round. Pending owner browser test + /simplify + /review.** Then Phase 4 family (needs PIPEDA/CASL), Phase 5 league layer, Phase 6 store wrap. ⚠ apply migs 186 + 188 to prod before any master promote (see docs/projects/active/UNIFIED_APP_CONSUMER_LAYER_PLAN.md + UNIFIED_APP_PHASE3_BRIEF.md)
- [ ] **Free App Experience & Conversion** — pre-Phase-4 polish of the fan/family/coach free-app journeys + every free→paid conversion moment (fan→account→alerts, coach claim→Premium, organizer funnel). Owner-ratified 3-step process: agent sweep first (mechanical fixes silent, judgment report + labeled mockups) → owner-directed persona scripts → owner free-roam. §8 return-to-Discover folds in (coordinate w/ Tournament Mobile G5 More-sheet). Tournament-page internals excluded (Tournament Mobile owns them). **Step 1 sweep COMPLETE (mechanical fixes committed 09fc1f97); all 12 judgment calls OWNER-RATIFIED 2026-07-14. Lane 1 COMMITTED to dev 2026-07-15 (`eccab26b`, sequenced after Tournament Mobile R1 `66e5ee37` which carried the co-edited account-sheet N3b rows): nudge signup keeps the triggering follow + returns to the origin page (C2/S4), registration-fed Overview schedule tile w/ live score (C5), Overview Premium hook @ $29 (C4), Fan-view link on Overview (S5), audience-split acquisition banner + Account-tab organizer row + coaches-hub cross-sell (C6a–c), honest "Highlight" reword + one-tap own-team alerts + COACH chip (N3a/b), updated-ago hint / fan-card-first notifications / rail skeleton (S2/S3/S6); bundled the steward chat's C1 delay, C3 price lines, Club-only copy + Facts promo line. /simplify applied (one shared follow-mirror, parallelized queries, shared formatters); /review (high-risk, 5 lenses) 7 findings fixed incl. ⚠ sw.js DATA_CACHE carve-out for /api/public/tournament-viewer (per-user identity replayed offline; CACHE_VERSION→v4); /docs synced (coaches + tournaments guides). Full Playwright pass at 390×844; owner phone pass still owed — esp. the nudge-signup auto-attach (C2, not E2E'd). Lane 2 (N2/S1/N1) stays queued behind Tournament Mobile's remaining rounds.** (see docs/projects/active/FREE_APP_CONVERSION_UX_PLAN.md + PM brief)
- [x] **Scheduled Jobs Wiring + Coach Insights digest** — ✅ **COMPLETE ON DEV + COMMITTED, device-tested 2026-07-13.** Weekly "week in review" digest + 4 new insight rules + automatic daily dues reminders on a pg_cron+pg_net timer (machine-key auth, Vault-held URL/secret), finalized dues copy, plus two bugs found in testing and fixed (live notification badge never published for realtime; coaches portal mis-scoped multi-org users). ⚠ **Remaining = PRODUCTION PROMOTION ONLY** (deferred to the unified-app window): apply migs 183 + 187 to prod + set prod Vault, promote as one bundle with the coach opt-out off-switch. Plans archived. (see docs/projects/archive/SCHEDULED_JOBS_WIRING_PLAN.md + docs/projects/archive/COACH_INSIGHTS_DIGEST_PLAN.md)
- [x] **Tournament Mobile Polish** — ✅ **COMPLETE ON DEV 2026-07-17 — Track A + Rounds 1–4 all committed; plans + prompts archived.** Public tournament mobile experience review vs the Phase 3 mockup baseline (2026-07-14): ~55 verified findings (live-state honesty, 44px tap floor, hero/stack weight, one type system, light-mode contrast, 2 structural upgrades). **Execution split approved 2026-07-14: Track A (mechanical fixes — live-honesty guards, 44px sweep, light-mode contrast, small bugs) kicked off via TOURNAMENT_MOBILE_TRACK_A_PROMPT.md in its own chat; Track B = 3 mockup rounds in the review chat (R1 Home+Schedule → R2 Standings+Bracket → R3 Teams+chrome), each round: before/after mockups → owner decision → build.** Measurement harness shipped: scripts/mobile-review-capture.mjs. **Round 1 rev 3 (2026-07-14): G2 + G3 (unified header, navbar+hero merged) + G4 (mono quiet-label convention) ACCEPTED and logged to design decisions; G5 = More-sheet bottom nav (Home·Schedule·Standings·Teams·More; bell + account doors move into the sheet, initials chip removed) CONFIRMED by owner 2026-07-14 (logged to design decisions) — ⚠ supersedes Phase 3's mobile account chip, relay to the Phase 3 chat before either ships.** G1 resolves in Round 3. **ROUND 1 BUILT on dev 2026-07-15 (review chat, uncommitted): unified event header (org eyebrow · title · dates+stage pill, scroll-condense via measured-height vars; per-page title blocks retired ≤900px); stage-aware Home (Live Now block w/ championship up-next row, pre-event countdown+Register panel, gold champion lead, single CTAs, venue folded into snapshot); schedule slim follow pill + one-row controls + venue-line/sans-name/no-trophy rows + weekday day headers (page 1428→1006px); bottom nav = Home·Schedule·Standings·Teams·More opening the reworked never-empty tournament sheet (sign-in/hats/Following/Notifications — bell relocated as a row variant — + News + Rules; chip + bell now desktop-only); .badge data face + repaired eyebrow kicker. Typecheck clean; full 21-capture harness re-verified (no overflow; dark/light/Battle-Purple; anon+followed; 390+360); dev server restarted fresh. Owner iterated 5 polish passes (unified header single-header rev, More sheet regroup, logo → pre-event brand card, Get-the-app → More sheet platform row, count-privacy rule). **/simplify DONE 2026-07-15 (13 fixes: fanRoundLabel + isInstallEligibleBrowser hoisted to lib, GetAppLink deleted, badge-family pills, shared .live-dot, --chrome-top-h/-static-h composed tokens + 2 missed consumers fixed (rules anchors, schedule rail 641-900 band), SheetRow component, dead CSS purged; 4 skips noted).** **/review DONE 2026-07-15 (high-risk tier, gate green, 4 lenses + main-loop adjudication): 10 confirmed findings FIXED — incl. [High] pre-existing open redirect on /auth/login?next= (safeNextPath now applied, mirrors 696bc794), [High] live/up-next double-render, [High] .live-dot lime-pulse class collision, [Med] preview↔public mobile parity (data-live-chrome scoping keeps preview titles), [Med] partial "Winner SF2" jargon, [Med] past-due up-next pinning (J6-039); 6 low/info documented no-action; 3 refuted. Typecheck/lint/ratchets green; live probes re-verified.** Round 1 COMMITTED to dev 2026-07-15 (`66e5ee37`, incl. the concurrent chat's account-sheet rows by owner acknowledgment).** **ROUND 2 (Standings + Bracket/Playoff Picture) BUILT on dev 2026-07-15 (Round 2 chat, uncommitted): mockups artifact `a92fc65c` + R2-1/2/3 all owner-accepted at the recommendations, then built — standings result chips in status colors (D8), one mono card-header idiom (D9), coach qualifier → quiet second line via shared `splitTeamQualifier` (D3, also on Playoff Picture seeds/callouts), every stat column data-faced (D4), phones get TEAM·REC·RD·PTS with no sideways scroll at 360 + RF/RA behind the swipe while desktop keeps all 7 columns (R2-3), pool-play bracket embed folds into a tap-to-expand disclosure — bracket stays mounted; playoff-day bracket-on-top unchanged (R2-1/F3); Playoff Picture de-heroed ≤900px under the unified header w/ "Seeding & Matchups" kicker (C2), one-line narrative + stat-strip compression (R2-2), today's unresolved final renders an honest pending card ("Winner of Semifinal 1 vs Winner of Semifinal 2", fan round names, real time+diamond — A7), matchup meta joins the mono convention (D13). Typecheck/verify:changed green; dev server restarted fresh (was stale since 08:10); Playwright-verified at 390/360/desktop/light + pool-play↔playoff-day both states; live-demo restored to game day. Owner phone pass DONE 2026-07-15/16 with 3 test-driven refinements (mobile pending clock, wider name col + stacked capped-RD, full-width chips; completed-demo seeded with a ±7 RD cap as test data). /simplify DONE 2026-07-16 (7 fixes incl. badge-family chips + bracketGameLabel hoisted to lib). /review DONE 2026-07-16 (high-risk, 5 lenses; 6 confirmed FIXED incl. [High] decided finals never graduated into the matchup list — the championship vanished the moment it went live; + bracket mounts on first disclosure open, per-division collapse reset, stale mobile header override). /docs DONE (2 fan FAQs + search terms). COMMITTED to dev 2026-07-16.** **Track A COMMITTED to dev 2026-07-14 (`889273fd`, no migrations): all mechanical items (live-honesty guards, 44px sweep + `--tap-min` token, light-mode contrast incl. `-strong` status tokens, dock/banner/bracket/news small fixes) + /simplify cleanups + adversarial-review fixes + /docs sync; capture harness (now shipped in-repo) confirms every cited tap-floor target cleared and live surfaces stay honest; owner phone pass still recommended.** **ROUND 3 (Teams + team detail + chrome + G1) — mockups artifact `0d4161cc` delivered 2026-07-16; ALL FOUR decisions owner-accepted same day at the recommendations (G1 = dock auto-minimize) + a copy rider ("Form" → "Recent results"); BUILT on dev 2026-07-16 (Round 3 chat, owner-directed after the R2 commit unblocked it; uncommitted, no migrations):** G1 dock renders a right-anchored ★+LIVE+score pill (≥44px, ticker-min idiom, tap restores; wrap pointer-events:none) on Schedule + team detail only — full bar everywhere else; R3-1 team-page header = ONE 44px action row (Follow labeled + icon-only Calendar/Share ≤640px + NEW score-alerts bell via FollowAlertsToggle variant="icon" w/ lime-fill+ink ON state, gated fanAlertsEnabled && !finished from OrgNav) + stat strip kept + Recent-results live row gains the running score; R3-2 Teams tab = tabular-nums records, G4 mono pool kickers w/ team count, mono rank·pts/next-game/status lines, coach quiet line via shared splitTeamQualifier (team.coach wins, raw qualifier fallback), D11 nav labels → --font-data; R3-3 team rows re-skinned to the schedule-row language (mono ctx line, tabular scores, soft W/L/T chips both breakpoints incl. Recent-results pips, live-first ordering w/ 0–0 floor, ~112→~82px pitch, "N played · N live" kicker). Verified: typecheck + verify:changed green, Playwright computed-style probes 390/360/light/followed+anon (pill vs full-bar per route confirmed); dev server stop→.next purge→restart mid-verify (stale jest-worker errors — protocol followed). Design-log entries for R2-1/2/3 AND G1/R3-1/2/3 + rename written to memory/design_decisions.md (debt cleared). Owner phone pass + commit pending. **Round 4 (Day-First Schedule Timeline, F1/G2) BUILT + verified + COMMITTED to dev 2026-07-17: day selector + smart landing + single-day view (with the "All" overview and the D8 compact date rail + week-grouped jump list for long events), per-row pool tags with cross-pool self-hide, the §3b mobile zoom-out root-caused + fixed (control-row flex basis forced a 448px layout viewport — every schedule capture now measures device-true), dateless games ride along as a trailing TBD group, playoff games numbered + one plain-language slot wording ("Semifinal 1 winner") across list + bracket + standings embed + followed-card (owner phone-pass riders); /simplify (9 fixes incl. per-side pool attribution hoisted to the shared bracket lib) + /review (high-risk, 4 lenses; 6 confirmed findings fixed, all E2E re-verified) folded in. No migrations.** (see docs/projects/archive/DAY_FIRST_SCHEDULE_TIMELINE_PLAN.md + PM brief; docs/projects/archive/TOURNAMENT_MOBILE_POLISH_PLAN.md + PM brief)
- [ ] **Platform-Wide Notification Settings** — one "All your notification settings" page; **Phase 1 COMMITTED to dev (d6137c02)**; **Phase 2 BUILT on dev 2026-07-13 (uncommitted): simpler grouped view w/ tri-state rollups, assistant capability filtering, /home shortcut, CASL per-coach unsubscribe fix (mig 185 DEV-ONLY, prod-pending); free-coach reach DEFERRED**; Phase 3 (fan Following card) gated on Unified App Phase 2 (see docs/projects/active/NOTIFICATION_SETTINGS_PLAN.md + PM brief)
- [ ] **Notification engine — recipient-scoping fixes** (ticketed 2026-07-13 from the notification-settings audit) — coaches receive playoff/champions/score alerts for every tournament in the org (role isn't tournament-scoped like staff) and capability-blind pings reach assistants who can't act on them; own reviewed project, build with the next deliberate notify-engine touch (see docs/projects/active/NOTIFICATION_SETTINGS_PLAN.md — "Explicitly parked" table)
- [x] **Club Repackaging** — ✅ **COMPLETE + LIVE on prod 2026-06-24.** Two-band Club $219 / Club · Association $379, $19/team meter retired, 15/30 caps enforced, whole-staff portals included, operator-console swept; live Stripe prices wired for both bands. Post-launch 13-agent verification (core solid) + read-only live-prod data check. Two deferred-polish items now done (operator custom-cap UI + transfer/un-archive cap guards) plus a stale-billing-copy fix → shipped to `dev` (commit 1bbb891), pending the next prod promotion. One low-priority data-dictionary note deferred (file mixed with live chat work). Plan + brief archived. (see docs/projects/archive/CLUB_REPACKAGING_PLAN.md)
- [x] **Coach Money Hub Redesign** (owner UX walkthrough 2026-07-08) — ✅ **BUILT + /review'd + /docs'd + owner-tested (standalone) 2026-07-09 on `dev`; UNCOMMITTED, no migration.** Premium Coaches Portal Money section rebuilt from 7 stacked cards into a stage-aware command centre: "right now" guide anchor, cash-honest Money In/Out/On Hand/Headroom, Plan→Collect→Spend→Review grouped cards with live numbers, season-total⇄itemized buffer reconciliation, coach-created categories + structured expense picker + recategorize fix-it, $/% period splits + Split evenly, BvA export, dues⇄budget-generator cross-links, standalone-team cleanup, back-links + lime-button sweep. /review high-risk funnel: 5 fixes folded, 4 refuted. Help docs synced (new premium-money section + 4 FAQs). Remaining: owner org-linked walkthrough (with the org review) + commit. Plan + brief archived. (see docs/projects/archive/COACH_MONEY_HUB_REDESIGN_PLAN.md)
- [ ] **Coach Tags & Player Awards** (owner-scoped 2026-07-09) — per-team coach vocabulary for tracking + reporting: tag games ("Top in the province") → Insights "record vs tag" splits; team award library (MVP, Best Hitter) recorded per game/tournament → player profile + leaderboard (internal-only V1); expense tags + org-promote later. 4 owner decisions locked. **P1 game tags COMMITTED to dev 2026-07-10 (mig 181, applied dev+prod): tag library + chip picker (autocomplete-or-create) + rename/merge/delete manager on the game edit/detail surface.** `/review` (2026-07-10): 2 confirmed findings, both fixed — (1) High: editing tags on a recurring league game and saving with "This & future"/"All events" silently dropped the tag change (server never read `tagIds` on the series-scope branch); now applies to the edited occurrence regardless of scope, matching the per-occurrence design; (2) Low: tag delete returned a false-success on a cross-team/missing id — now 404s like its sibling rename route. `/docs` synced (new Tags recipe content + 2 FAQs in the coaches guide). **"vs tag" report BUILT on `dev` 2026-07-10 (uncommitted):** re-scoped against the shipped Insights V3 architecture per its own tile-cap rule (a new metric joins an existing report, it doesn't earn a new tile) — added a tag filter-chip row + per-tag record ("vs Top in the province: 3–5, 21 runs for, 34 against") to the existing "How are we doing?" results report, reusing the Lineups page's filter-chip idiom; self-hides per-tag chips with zero finalized games, self-hides the whole row with zero team tags. Owner approved an interactive mockup before build. `/review` (2026-07-10): 1 confirmed Medium finding, fixed — the per-tag runs-for/against total silently counted a scored-result-with-no-numbers game as 0–0 instead of excluding it (the season's existing "close games" stat already guarded against this same edge case; the tag total now matches that precedent). `/docs` synced: new FAQ on the results report + a cross-reference added from the tagging FAQ. **Owner browser-tested (seeded dev team, 12 finalized games/3 tags) and approved; committed to `dev` 2026-07-10 (commit `13858e7a`).** **P2 Player Awards — BUILT + reviewed + docs'd on `dev` 2026-07-12 (mig 182, applied dev-only; uncommitted).** Owner signed off on a clickable mockup first (two rounds of feedback: icon editing needed to cover the icon not just the name, and a bare text field for the icon was a bad ask — replaced with a ~28-icon tap-to-choose gallery + type-your-own fallback). Shipped: a per-team award-type library (seeded MVP/Best Hitter/Hustle Award, editable) + award records (player + type + optional game/tournament + date + note); a "Give an award" moment in the schedule page's game detail panel once a game has a final score plus a general entry point on the new report page; a trophy badge on scored games in the schedule list; a new "Awards" section on each player's profile ("2× MVP this season"); and a new 5th Insights hub tile, **"Who's earning it?"** (leaderboard + filter chips + full history with undo) — a new tile rather than folding into an existing report, since a leaderboard is a genuinely new report shape (unlike the tags "vs tag" metric). Retiring an award type never deletes it or its history. `/review` (5-lens high-risk funnel): no Critical/High findings; 6 confirmed small issues fixed in-tree (an inline-created award type not refreshing the parent list until reload, a missing trophy badge on the mobile day-sheet view, a defensive modal-state reset on event switch, a cancelled game still being awardable if it had stale scores, a silent delete-failure with no error shown, and a DB-layer belt-and-suspenders guard against an award ever carrying both a game link and a tournament label) — 2 other flagged concerns checked directly and ruled out (a suspected timezone/date-shift bug, refuted against this exact page's own existing date-handling convention; and a comment-syntax claim raised independently by two reviewers, refuted against the real file). `/docs` synced: new Awards content + 2 FAQs in the game-day-details guide, the player-profile FAQ updated to mention awards, and a new leaderboard FAQ added next to the existing tags FAQ. typecheck + focused lint clean throughout; dev server restarted (shared-module changes) and verified live. **Owner live-tested 2026-07-12, 2 UX corrections applied:** save now closes the modal (was stay-open-and-reset); "Manage award types" removed from the schedule page (once-a-season task, doesn't belong on every game) — lives only on the "Who's earning it?" report page. P2 feature-complete, uncommitted. **P3 (money tags + org-authored shared library) BUILT + reviewed + docs'd 2026-07-13; all three phases COMMITTED to `dev` (commit `104c2ea3`).** Org sharing pivoted from the originally-floated bottom-up "promote a team tag" to a top-down **org-authored Shared Library** (Admin → Rep Teams → Shared Library) — owner call: at 15+ teams, per-team promotion reintroduces the drift/dupes the feature fights. Money tags reuse the tag engine (money-capability gated) on expenses + Budget vs. Actual (filter + "vs {tag}" spend); colour (blue=org / green=team) not badges; new ADO-style search-combobox picker. `/review` (high-risk, 4 lenses): 5 fixed — incl. 2 High regressions where the widened tag/award library leaked org-shared items into the team-only managers — 7 refuted. `/docs` synced (money tags in the coaches money guide + a new admin "Shared library" section; fixed a stale P2 "manage award types" location note). Follow-up UI polish committed after: tag-dropdown drop-up when low in a modal, Add-Expense body scroll so the last field clears the footer, and **all coaches pop-up modals now open centered** (event detail + Add/Edit forms no longer dock right). **⚠ migs 182 (P2) + 184 (P3) are DEV-ONLY — apply to prod before promoting to master.** Plan + brief archived. (see docs/projects/archive/COACH_TAGS_AWARDS_PLAN.md + PM brief)
- [ ] **Tournament Chat — Adoption Dashboard Module** (owner-flagged 2026-07-06) — IN PROGRESS on `dev`. A "Coach sign-ups & chat" panel on the admin tournament dashboard (pre/post-event) surfaces the adoption funnel — teams with a coach email → coaches signed up → in the chat room — plus a one-click "remind not-yet-joined teams" (reuses the existing coach access-link email) and a tier-aware locked upsell for free-tier events. Reuses the canonical `resolveTournamentChatParticipants` resolver; no migration. (see docs/projects/active/CHAT_ADOPTION_DASHBOARD_PLAN.md + PM brief)
- [ ] **Dashboard Game-Status Sections (Now Playing / Up Next / Needs a Score)** — split the tournament dashboard's single "Now Playing" board into three honest, customizable sections so nothing that needs attention is hidden: Now Playing (in-window/being-scored only), Up Next (next scheduled games not yet started), and Needs a Score (past-window unscored games, incl. prior days). Buckets derive from a shared `lib/game-live-state.ts` classifier the Schedule screen also uses (drift-prevention). Follows the just-shipped "Now Playing" bug-fix (UTC→tournament-local via new `tournamentNow()` + end-of-window bound). No migration; phased (API buckets → Up Next panel → Needs a Score panel → Schedule refactor). PLANNED, not started. (see docs/projects/active/DASHBOARD_GAME_STATUS_SECTIONS_PLAN.md + PM brief)
- [ ] **Tech debt: prod build on webpack → revisit Turbopack** — prod build is pinned to `next build --webpack` (+`NODE_OPTIONS` heap in amplify.yml) so `sharp`'s native deps bundle into the Amplify Lambda; Next 16's default Turbopack drops `detect-libc` and 500'd branding/logo/PWA-icon routes (fixed + LIVE on prod 2026-06-24, commit 9440fed). Webpack is deprecated + makes CI builds slower (~10–15 min vs ~2). Revisit once Turbopack traces native-module deps under pnpm+Amplify (or adopt `node-linker=hoisted`). Verify any change by hitting a public `/{org}/{tournament}/apple-icon` → 200 image/png; bug is deploy-only. (see memory reference_sharp_turbopack_webpack)
- [ ] **Release Notes, Changelog & Loose Roadmap** — **P1–P3 BUILT on `dev` (unpushed) 2026-06-28; no migration.** Public `/changelog` (shipped items + undated "On the horizon") + in-app "What's New" w/ seen-dot for admins/coaches (standalone coaches hub deferred) + `npm run draft:notes` draft-then-approve generator wired into `/release` (date-tagged releases). No email in V1. Pending: owner browser test (needs dev-server restart) → deploy decision (see docs/projects/active/RELEASE_NOTES_CHANGELOG_PLAN.md + PM brief)
- [ ] **Public Tournament Discovery Directory** (Mobile/Discovery strategy Phase 1, 2026-06-28) — opt-in (default-off) public web directory of tournaments across the platform, searchable/filterable by sport/location/date, each linking to the existing public page; re-bases the existing `/discover` from org-level `is_public` onto a per-tournament opt-in (mig 158 `tournaments.list_in_directory`) + SSR/sitemap SEO; reuses current public-status + PII rules; no pricing change; 7 build decisions LOCKED 2026-06-28 (listing open to all tiers, province-level location filter, upcoming+active default, per-tournament opt-in, keep `/discover`) — ready to build (see docs/projects/active/PUBLIC_TOURNAMENT_DISCOVERY_DIRECTORY_PLAN.md + PM brief)
- [ ] **Facilitated Payments — Scoping (Q4 2026)** — scope participant→org payment processing as the size-scaler/free-floor funder; resolve take-rate (margin vs all-in), who-pays, processor, KYC/legal; build targets H1 2027 (see docs/projects/active/FACILITATED_PAYMENTS_SCOPING_PLAN.md)
- [ ] **League + Club Early-Access Readiness** — define the go/no-go "ready to open" checklist + hardening scope gating the managed early-access cohort (~Aug 2026) (see docs/projects/active/LEAGUE_CLUB_EARLY_ACCESS_READINESS_PLAN.md)
- [ ] **App Icon — Logo Size Control (Version A)** — **BUILT on `dev` 2026-06-24, unpushed.** A "Logo size" slider (Small↔Large, continuous; owner-chosen over preset chips) in Public Site → Advanced Branding → App Icon (Tournament Plus+) so organizers can make their logo sit larger/smaller in the installed home-screen icon, with a live preview that tracks the slider. Mirrors the mig 152/153 pattern: **mig 154 `tournaments.app_icon_scale` (smallint, nullable; 100=default stored as NULL) APPLIED TO DEV** + dictionary/snapshots refreshed (#154); same save flow + Plus/`manage_branding` gating. iOS box scales 156→110–172; Android box 280→196–290 (corner-safe maskable cap, so a square logo is never clipped). Panel copy notes phones cache installed icons (new look applies to new installs only). typecheck ✅ · focused lint 0-err ✅ · dictionary coverage ✅. ⚠ **mig 154 DEV-only / prod-pending** — apply to prod before promoting. Next: `/review` → `/docs` → owner browser test (real iPhone + Android). Version B (drag/pinch crop editor) out of scope. (see docs/projects/active/APP_ICON_LOGO_SIZE_PLAN.md + PM brief)
- [ ] **Platform-Admin Walkthroughs + Bug Hunt** — owner-driven manual QA of the operator console by support persona; ~25 confirmed issues pre-identified by a 7-area code map (incl. the stale tournament limit, gating-toggle two-source split, expired-override no-revert, club_large sweep gaps). team_limit silent-wipe FIXED 2026-06-23. Owner executes walkthroughs → reports → fix loop (see docs/projects/active/PLATFORM_ADMIN_WALKTHROUGHS.md)
- [ ] **Stripe Price-Configuration Validation + H8 Price-Guard** — routed to `/billing` 2026-06-29 from platform-admin QA 3.2 (wiring a Stripe price to a plan did almost no validation). Owner-approved set (hard-block: missing/inactive/one-time/wrong-interval/wrong-currency/wrong-environment; warn: amount mismatch / reuse / wrong product). **Phases 1+2 BUILT on `dev` 2026-06-29** (P1: shared validator + server hard-block enforcement on both apply paths + `isPlanCheckoutPriceConfigured` H8 readiness helper. P2: read-only validate endpoint + "Stripe price check" panel in the approval modal — per-line ✓/⚠/✗, Approve disabled on hard-block, warn requires a confirm checkbox; brought forward at owner request during QA. No migration; typecheck+lint clean; dev restarted; not pushed). Phase 3 (in-app card price-guard) lands with the H8 build (see docs/projects/active/STRIPE_PRICE_VALIDATION_PLAN.md + PM brief)
- [ ] **My Team Card — unify the followed-team card** (owner-flagged 2026-07-03; design locked + logged) — **BUILT + owner-testing-refined on `dev` 2026-07-03, unpushed; no migration; typecheck + focused lint green.** Unified card live on Standings + Schedule (mobile + desktop). Owner testing pass (against a mirrored prod "Battle of the Bats" in dev) drove 4 refinements (logged in design_decisions): slim the card (dropped the on-card unfollow star so both pages match the short look), keep the game-day dock on ALL pages incl. Schedule/Standings (owner accepts the overlap), remove the Schedule auto-scroll-to-today (supersedes the 2026-06-15 decision; kept the highlight, no button), and drop the on-card unfollow entirely — unfollow now lives on the linked team page's Follow/Following toggle (a first-try "Unfollow" chip was removed as row clutter; dock + desktop rail still offer it too). Owner browser-verifying; `/review` + `/docs` offered. The public "your followed team" card is drawn inconsistently: the Standings version is a flat cramped grey strip (hard to scan) while the Schedule version reads cleanly. Consolidate three drifted implementations (Standings strip + Schedule mobile `.scorebugBar` + Schedule desktop `.railCard`) into ONE shared presentational `MyTeamCard` (scorebug twin: avatar + `--font-data` 3-tier body + labelled NEXT UP/LIVE/FINAL rail + team-page link; per-surface rank-scope + `actions` slot + unfollow-star passed as props). Standings switches off `--logic-lime` → org `--primary`. Home `MyTournamentCard` + game-day `MyTeamDock` OUT of scope. ⚠ touches the live Schedule page the owner likes — verify mobile+desktop pixel-identical before sign-off. Aligns with the active Standings Remodel. (see docs/projects/active/MY_TEAM_CARD_UNIFY_PLAN.md + PM brief)
- [ ] **Live Tournament — Playoff Moment + Home/Standings Polish** — **BUILT on `dev` 2026-07-04 (unpushed; mig 175 APPLIED to dev+prod 2026-07-04 schema-only ahead of code, gate GREEN).** 5-item owner batch tested against the mirrored prod Battle of the Bats. (1+5) The **Playoff Moment**: when a bracket is first materialized it auto-fires a one-time "Playoffs are set" push to fans (score-alert followers, Tournament Plus+) AND staff, flips the home hero into a **Playoffs takeover** (badge + countdown to first knockout game + CTAs), and links to a new shareable **Playoff Picture** page (`/{org}/{tournament}/playoffs`) — template-generated (no AI) seeding article: narrative + stat callouts + seed list w/ cut line + opening matchups (Seed #N resolved to teams). Guard = new `tournaments.playoffs_published_at` (mig 175, atomic NULL→now() claim in bulk-save/save-bracket; backfilled so old brackets don't false-blast). (2) Home: dropped redundant "Today's Games" card, "Upcoming Games" + "Latest Results" now lead. (3) Standings mobile: all stat columns scroll horizontally, only Team pinned (PTS unpinned, REC pill dropped). (4) Run diff now shows true value with seeding-capped in brackets e.g. `+10 (+7)` (BotB has no cap set → single number). typecheck + verify:changed green (tokens/snapshots #175/dictionary/org-context). **mig 175 applied to prod 2026-07-04 (schema-only ahead of code, drift gate GREEN); `/review` high-risk funnel done (3 fixes: /playoffs standings-visibility gate, gate qualified-seeds on gamesStarted, filter cancelled playoff games); `/docs` done (Tournaments guide + 2 FAQs + RD-display note); dev restarted + all routes 200.** Next: owner browser test → promote code to master. (see docs/projects/active/LIVE_TOURNAMENT_PLAYOFF_MOMENT_PLAN.md + PM brief)
- [ ] **Champions Crowned — tournament-complete moment** (owner-flagged 2026-07-05) — **BUILT on `dev` 2026-07-05 (unpushed; mig 176 APPLIED to DEV only, prod-pending).** Mirrors the Playoffs-Set moment for the *end* of a tournament: when the whole tournament's playoffs are complete (every playoff game terminal + a decided championship final), it auto-fires a one-time "🏆 Champions crowned" alert (staff bell/push + Tournament Plus+ fan push via `notifyFansForChampions`), flips the public home hero to a **Champions celebration** (gold takeover, headlines the top-tier champion, "Final Results" CTA), and publishes a shareable `/{org}/{tournament}/champions` recap page (per-tier winners + final standings, follows Standings visibility). Guard = new `tournaments.champions_crowned_at` (notify-only, atomic NULL→now claim in `announceChampionsIfComplete`, wired at the shared scoring chokepoint `onScored`; backfilled so finished brackets don't false-blast). Fixed a latent tier bug: `lib/champions.ts` `decidedFinalFor` now picks the TOP-tier final (via `groupGamesByBracketId`), so tiered brackets crown the true championship winner (fixes home banner + OG image + team-profile too; OG refactored onto the shared fn). "Both, Tier 1 leads" (owner decision). Manual "mark completed / close out" stays separate. typecheck + verify:changed green; dev restarted; BOTB verified (home + /champions + OG all 200, crowns Brampton Blazers Gold not the Tier-2 winner). `scripts/reset-botb-champions-moment.mjs` arms the moment for owner testing. Next: owner browser test → `/review` + `/docs` → promote (apply mig 176 to prod first). (see docs/projects/active/CHAMPIONS_MOMENT_PLAN.md + PM brief)
- [ ] **Unified Tournament "Finished" State (public overview)** — **BUILT on `dev` 2026-07-06 (unpushed; NO migration; typecheck + focused lint green).** Collapses the two/three drifted "tournament is over" looks on the public overview into ONE consistent finished experience driven by the games actually being done — not by the admin "Mark Complete" flag (which stays an admin/reporting override). New `isFinished` signal in `TournamentHomeContent` = marked complete OR bracket decided (`isTournamentPlayoffsComplete`) OR a no-bracket round-robin played out past its end date; all finished sections (all-tier Champions incl. Tier 2, Event Summary, Top Standings) + hiding of "What To Check Now"/"Next On The Schedule" now key off it. Owner decisions (2026-07-06): **understated tournament-name hero for the finished state (retires the separate full-screen champion takeover on the overview — the "Champions Crowned" push/bell + `/champions` recap page are unchanged); auto-finish round-robin events too.** Shell agreement (cheap, no extra query): the top-bar phase pill + fan install/alert nudges now read a shell-level "effectively finished" (marked complete OR `champions_crowned_at` stamped OR past end date), threaded through OrgNavContext → TournamentNavSync → the layout, so nothing says "in progress" over a finished page (the game-day dock stays on the plain date-window signal so a followed team's live dock never goes dark mid-event). ⚠ shared context provider changed → dev-server restart before browser test. `/review` DONE (high-risk funnel; 3 edge-case fixes folded: guard against declaring finished while a non-playoff game is still scheduled, round-robin now finishes ON its end date not the day after, reverted the dock gate; 3 findings refuted). `/docs` DONE (Tournaments → Close Out: new "do I have to mark complete for fans to see final results?" FAQ + body clarification + search terms; champions FAQs already aligned). Next: owner browser test → promote.
- [ ] **Public Bracket — Venues, Clickable Cards, Standings Parity** — **BUILT on `dev` 2026-06-29 (no migration; typecheck + verify:changed green, pending owner browser test).** Public playoff bracket cards now show the live field/diamond on the meta line + are tappable through to the game-detail page; the Standings bracket now splits by tier/pool via a shared component so it matches the Schedule bracket (was merging tiers/duplicating finals). Also fixes renamed-facility staleness ("2"→"Diamond 2") on team/schedule/results/home surfaces by resolving venue labels live. ⚠ dev-server restart needed before browser test (new file + shared-module changes). (see docs/projects/active/PUBLIC_BRACKET_VENUE_CLICKABLE_PLAN.md + PM brief)
- [x] **Phone/Home-Screen Notifications (Push Enablement)** — ✅ **COMPLETE + LIVE on prod 2026-07-03 (promotion 97fc676b) + ARCHIVED.** Fixed "admin notifications only show in-app, never on the phone." Root cause = 3 gaps (prod deploy config never carried the VAPID push keys → server silently sent nothing; the opt-in was a buried per-event toggle whose "Allow" confirm rendered off-screen on mobile; every event defaulted push-off). Shipped: amplify.yml now forwards the VAPID keys (already set in Amplify 'All branches') to the server runtime; a one-tap "Turn on notifications" prompt in the admin shell (fires the OS Allow dialog in-gesture); push on by default for action-worthy events via a shared `PUSH_DEFAULT_ON_EVENTS` set; settings-table toggle prompts in-gesture; tier-aware in-dropdown "Notification settings" link (fixes tournament-tier bounce-to-dashboard); per-tournament chat-message opt-out. High-risk `/review` = 5 low-sev fixes folded. NO migration. Marketing-reviewed changelog line merged into the 2026-07-03 entry. Open follow-ups (not blocking): coaches-portal banner, `/docs` pass, retire now-unused `PushPermissionPrompt`. ⚠ owner-verify a real phone alert on prod. (see docs/projects/archive/PUSH_NOTIFICATIONS_ENABLEMENT_PLAN.md + PM brief)
- [ ] **Notification Center Rework** (owner-flagged 2026-07-06 via `/ux`) — **P0–P4 ALL BUILT on `dev` 2026-07-06 (unpushed, no migration); feature-complete, pending final `/docs` + light `/review` of P4 before ship.** P4 adds the **"See all" full notifications page** (`/{org}/admin/notifications` + coaches equivalent, reached from a new "See all" link in the bell footer): full-width Needs-attention/Activity zones + bundling, an Unread/All toggle (defaults to All — the archive view), zone filter chips, Mark-all-read, and Load-more pagination; shared display logic extracted so the page + dropdown never drift; no by-tournament filter (rows carry no tournament id). P3 takes chat off the bell entirely (messages + @mentions) so it's pure operations — the Chat tab already carried its own unread badge on both shells, so this just removes the redundant bell entries (fixing today's chat double-count); @mentions still reach users via Chat-tab badge + phone push. Bell list + unread count exclude chat from one shared source (no badge/panel drift). Verified live against seed data; typecheck + verify:changed GREEN; dev restarted clean. P2 adds an **"Unread" toggle** (on by default → the bell reads as an inbox you empty; flip to "All" to see read items dimmed) and **same-type bundling** (repeated items roll up to "5 new registrations" / "2 payments received"; one tap marks all read + opens the list; needs-attention + chat never bundle). typecheck + verify:changed GREEN; HMR-safe. Seed script refreshed so both bundles show on Battle of the Bats by default. P1 (bell restructure): the bell now leads with a pinned **"Needs attention"** section (only the items needing a decision — failed payment, coach access request, disputed score, roster request, tryout response, assistant approval, no-show — amber-accented, with a count, that clears as you handle each) above an **Activity feed grouped Today / Yesterday / Earlier**. Category (act/know/talk) is a typecheck-enforced map so new event types can't skip classification. No bundling yet (P2), chat still in the bell (P2→P3). typecheck + verify:changed GREEN; HMR-safe (no restart). P0 quick win: every notification type now has a distinct icon (added the 8 missing ones) + fixed the phone "square" push icon — the status-bar badge was a fully-opaque tile that Android alpha-flattens to a white square; now a transparent centered white chevron silhouette (84.5% transparent, verified), plus a borderless corner-clean Android maskable icon. `/design` approved + logged the direction (own transparent badge source; branded per-notification icon flagged as a follow-up). Service-worker cache bumped so installed devices refresh. Focused lint clean. The bell is one time-sorted list that becomes clutter on a busy tournament day (repeated "New registration" rows bury the one item needing a decision); the phone push icon renders as a square. Owner picked the **Full Act / Know / Talk** scope: pin **needs-attention** items (failed payment, coach access request, disputed score) on top, turn the rest into a **bundled, date-grouped activity feed** ("6 new registrations" not six rows), **move chat off the bell** onto the Chat tab's own unread badge (reuses the existing `useChatUnread`/`ChatUnreadBadge` already on the coaches shell), add a **"See all"** overflow page, and **fix the phone icons** (the status-bar badge is a filled square → Android paints it solid white; needs a transparent chevron silhouette). **No migration** — category + bundling are code-derived; delivery engine (notify/prefs/mute) untouched. 5 shippable phases (P0 icons+icon-map quick win → P1 categorize → P2 bundle → P3 chat-off-bell → P4 See-all). Icon redraw routes to `/design`. (see docs/projects/active/NOTIFICATION_CENTER_REWORK_PLAN.md + PM brief)
- [x] **Editable Email Campaigns** — ✅ **CODE-COMPLETE on `dev` 2026-07-09 + CLOSED/ARCHIVED** (P1 marketing content editing, P2 editable dates + needs-sending board, P3 dashboard honesty, + Batch D transactional wiring). Operators edit ALL platform emails' subject/body from the console — the 10 founding-season **Marketing / Campaign** emails (block-markup rows, send==preview==default, retired the preview mirror + `founding*Html`/`spotlight*Html` builders → SoT SOT-6/-14) AND all 24 transactional/system emails (auth, billing incl. Stripe webhook, tournament/tryout/league flows) via a `sendTransactionalEmail()` wrapper across ~40 send sites — with **send-byte-unchanged-unless-customised** safety verified in /review (0 defects). Editable planned dates + Past-due/Upcoming dashboard board + Overview grouped action-queue alerts. Fixed `password_reset` (was wired to nothing). Marketing P1/P2/overview = 3 commits on dev (2026-07-08→09); **Batch D + doc updates uncommitted** (owner folding into a combined commit with parallel work). ⚠ **RELEASE STEP REMAINS:** apply migs **179** (campaign content) + **180** (planned dates) to **prod** + refresh snapshots when deploying — feature won't work on prod until then. (see docs/projects/archive/EDITABLE_EMAIL_CAMPAIGNS_PLAN.md + PM brief)
- [ ] **Registration Health Panel** (owner-flagged 2026-07-09 via `/ux`) — **BUILT + owner browser-verified + `/review`'d + `/docs`'d on `dev` 2026-07-09 (uncommitted, no migration).** Registrations page opens (collapsed by default, like Schedule Health) with a score card (0–100, 4 tiles, clickable issue list) covering missing coach emails, payments (Plus), pipeline backlog, and capacity-vs-registration. Rebuilt to 1:1 match Schedule Health's visual language after owner feedback. Prerequisite fixes folded in: new `missing_email` attention bucket (+ a real bug fix in the tournament-dashboard route that would've miscounted every team as missing an email), dashboard Chat panel dead-end links now jump to a filtered Registrations view, duplicate "Open Chat" link removed, a visible "Showing: X · Clear" banner added for any dashboard attention deep-link. `/review` (high-risk tier, 4-lens funnel) found + fixed 2 real bugs: a "Needs action" tile dead-end click when only missing-intake teams exist, and a days-until-tournament-start calc that used raw UTC math instead of the codebase's timezone-safe helper (matches a prior prod incident, J6-056). Two Low/Advisory items documented, not code-fixed (a division-less-team edge case needing a live-data check; no color signal for over-100%-capacity divisions). `/docs` added a Registration Health FAQ + a "how do I add/fix a team's email" FAQ to the Teams & Registration guide, wired into the page's contextual help drawer, and updated the existing chat-adoption FAQ to describe the new clickable flag. Dashboard's own Registration panel intentionally left alone (owner declined a duplicate). Next: commit pending. (see docs/projects/active/REGISTRATION_HEALTH_PANEL_PLAN.md + PM brief)
- [x] **Playoff Schedule Health — "what could happen" metrics** (owner-flagged 2026-07-09 from a debug session) — **Phases 1 + 2A + 2B SHIPPED TO PROD in the 2026-07-09 release (committed in f697d31c, no migration); `/review`'d twice; `/docs`'d; Phase 2B redesigned after live owner testing.** Playoffs tab "Teams" KPI now reads the bracket's seed count instead of counting whatever bracket slots happen to have a game row; every bracket slot (whether or not its real team is known yet) now projects rest/back-to-back/max-day forward through undecided future rounds by walking the bracket's Winner-feed graph, labeled **"Seed #N"** until a real team resolves (manual seed, or standings once at least one round-robin result exists — fixed a bug where an unplayed 0-0-0 tie was showing a team name), then upgraded to the team name tagged **"Current"**/**"Projected"** — scoped to standard single-elimination brackets only. Second `/review` pass caught + fixed a real regression (the Playoffs-only "hide unresolved slot" filter had leaked into the unrelated slot-mode Generator preview, zeroing its team table); two lower-severity items flagged as follow-ups, not fixed (manual bracket editing can rewrite a completed game's wiring text with no guard; a minor exact-vs-fuzzy matching inconsistency). `/docs` added a new FAQ explaining the "Current"/"Projected" tags — the Schedule Health card itself had never been documented before. Verified live at localhost:3000 against the real Milton U11 bracket + unit tests; owner browser-tested mobile + desktop 2026-07-09. (see docs/projects/active/PLAYOFF_POTENTIAL_SCHEDULE_HEALTH_PLAN.md + PM brief)
- [ ] **Single Source of Truth (SoT) Hardening** — spun out of the platform-admin QA (2026-06-29) when plan limits shown in the console disagreed with what's enforced. A 4-domain audit found the pattern is systemic (~17 instances; several LIVE on customer surfaces). 5 shapes (ghost controls, copy-pasted constants, stale copy, inconsistent assembly, DB-vs-Stripe), 4 rules (one authority / derive-not-copy / kill ghost controls / CI drift-ratchet), 6 phases by blast radius. P0 live fix done: Venue Library nav for Club · Association. Governance principle logged in BUSINESS_DECISIONS.md (2026-06-29). Copy drifts → /marketing; subscription reconciler → /billing (see docs/projects/active/SOURCE_OF_TRUTH_HARDENING_PLAN.md + PM brief)

- [x] **Platform-Wide User Journey Audit** — COMPLETE 2026-06-13. All 5 phases done: 10 personas walked + routed (500 findings, 0 refuted), then Phase 5 cross-persona synthesis (dedupe map, 12 themes, master backlog, 7 fix-project breakdown). Plan + brief archived; the 10 journey reports stay in docs/projects/active/journeys/ (cited by the spin-outs); the cross-cutting master backlog lives in docs/projects/active/USER_JOURNEY_AUDIT_SYNTHESIS.md. Absorbed the Rep Teams franchise-model review via J4. Evaluation only — fix work spun out into the 7 projects below (see docs/projects/archive/USER_JOURNEY_AUDIT_PLAN.md)
- [x] **Trust & Integrity Hardening** (audit FP-1, FIX-NOW headline) — cross-app security + data-integrity roots. **COMPLETE 2026-06-15 (Phases A+B+C).** Phase A: J6-001 + J3-069/068 + authz sweep + J3-012/J4-012 org-context fail-closed (`requireOrgSlug` + `check:org-context` guard; ~189 files). Phase B: J4-001 (already fixed/self-guarding), J3-047 league timezone (`lib/timezone.ts`, America/Toronto V1), J4-020 ledger-corruption Blocker (mig 127 ⚠prod-pending), J5-026 mark-paid (`lib/mark-paid.ts`), J5-012/J4-036 multi-org account deletion. Phase C: J8-018/J10-019 login loops + `/auth/suspended` + suspension email, J5-035 metadata gate. **Browser-verified + migration 127 confirmed applied to prod (live-DB check 2026-06-17) — fully deploy-ready, local/unpushed.** The gate FP-2…FP-7 sit behind. (see docs/projects/active/TRUST_INTEGRITY_HARDENING_PLAN.md)
- [x] **Public Fan Experience** (audit FP-2, successor to the archived Public Visual Redesign) — ✅ **COMPLETE + browser-verified 2026-06-19** (on `dev`, local/unpushed, NO migrations). All phases A–E: fan-truthfulness + finale, alerts + offline shell, and polish/footer; absorbed the 32 unshipped redesign design items. Plan + brief archived (see docs/projects/archive/PUBLIC_FAN_EXPERIENCE_PLAN.md)
- [x] **Volunteer Day-of Experience** (audit FP-3, NEW — no plan owned the volunteer shells) — ✅ **COMPLETE + browser-verified; migration 130 confirmed applied to prod (live-DB check 2026-06-17).** Working sign-out on both volunteer screens, session/cross-org loop fixes, one-thumb score entry, non-destructive gate roster, right-door wayfinding. J8-020/021 deferred to FP-7. **ARCHIVED** (see docs/projects/archive/FP3_VOLUNTEER_DAYOF_PLAN.md + _PM_BRIEF.md)
- [ ] **House-League In-Season Trust** (audit FP-4, NEW; coordinates with Free Tier Strategy which owns league acquisition) — vanishing-season Blocker, schedule honesty (location/cancelled/postponed), comms spine (0-delivered, reply path, rainout notify), parent payment loop, a real schedule generator, email-injection fix-now (see docs/projects/active/HOUSE_LEAGUE_INSEASON_TRUST_PLAN.md)
- [x] **Tournament Organizer Experience** (audit FP-5) — ✅ **COMPLETE + browser-verified 2026-06-17** (on `dev`, local/unpushed). All 5 clusters: bracket-math trust (tie/coin-toss/forfeit), false strings, live game day, wizard/Event-Settings mental model, registrations + staffing + discovery. Carried forward: J1-001/003/004 (footer/brand/imagery → marketing footer-review), J1-078 (volunteer seats → /billing). Plan + brief + staffing + test plan archived (see docs/projects/archive/FP5_TOURNAMENT_ORGANIZER_PLAN.md)
- [ ] **Draft Launch Checklist — Guidance & Discovery** (follow-on to FP-5) — IN PROGRESS 2026-06-17 on `dev`. Recasts the tournament draft setup checklist from a flat "Optional — N of 8" junk drawer into a three-tier guided tour (Required gate unchanged at dates+divisions): grouped drawer (Recommended / Defaults you can fine-tune / Registration & fees — your call), **visible default values on the row** (game timing 90/15, tie-breakers H2H→Run Diff→…, contact) for the "oh yeah, set that" moment, drawer rename ("Schedule, venues, tie-breakers & more — N reviewed"), concept `?` tooltips, a go-live confirmation summary, and schedule float-up when a schedule is actually broken. Page + CSS + help-content only; no gate/API/migration change. Owner approved recs + 3 decisions. (see docs/projects/active/DRAFT_LAUNCH_CHECKLIST_GUIDANCE_PLAN.md and PM brief)
- [ ] **Coaches Portal — Tryouts & Evaluation** (scoped 2026-06-29 from a 4-pass multi-agent brainstorm: tryout/eval ideation → premier-suite "wow" scan → owner-filtered consolidation) — NOT STARTED, PLANNED. Owns **Phase 1 (consent gate + pre-game attendance pulse)** and **Phase 2 (Tryout & Evaluation: check-in/bib/walk-up · blind scoring default-ON · provincial tryout-window date-check · configurable rubric · no-account multi-evaluator scoring + bias flag · ranked drag-to-form-roster · one-click accept-to-roster+fees · branded offer/release emails)** of a 5-phase Coaches Portal value roadmap (P3 roster/dev · P4 engagement/retention · P5 pitcher safety, each its own future plan). Owner filters 2026-06-29: **NO play-by-play/scorekeeping · NOTHING PIPEDA-risky · cross-platform stat aggregation parked (user-upload-only, no vendor contracts)** — both logged in BUSINESS_DECISIONS.md. Consent gate fixes a LIVE PIPEDA gap (public tryout form already collects minor PII w/o consent) → ships first. ⚠ pre-work: fix the existing `getSportPack(DEFAULT_SPORT)`/hard-coded-positions sport-neutrality violation before metrics features; gating (proposed Premium core) MUST ratify via `/billing` + Facts doc before build; `tryout_scores` realtime needs REPLICA IDENTITY FULL. Tryout-surface architecture DECIDED 2026-06-29 (own dedicated surface, auto-creates a calendar entry from the tryout date — NOT a game-day event type). Packaging DECIDED + logged (bundled in Premium, no per-player fee, no price change). Seasonal (OBA window ~Jul 1–early Sep) → targets 2026 fall / 2027 spring. **Phase 1 handed to `/plan` 2026-06-29 → build-ready breakdown in COACHES_PORTAL_TRYOUTS_EVAL_PHASE1_PLAN.md** (1.1 consent gate build-ready; 1.2 attendance RSVP reshaped by code investigation — guardians have no app identity ⇒ email+token not push, and no scheduler infra ⇒ Resend-scheduled or manual; **1.2 blocked on owner trigger decision OQ1**). **Phase 1 split 2026-06-30 (owner): 1.1 consent gate BUILT + verified on `dev` (unpushed/uncommitted) — consent checkboxes (PIPEDA/CASL, marketing draft copy) + server-side consent timestamp/IP + admin Consent compliance column & export + forward-compatible org privacy-policy-link pipe; mig 164 APPLIED TO DEV (⚠ prod-pending), snapshots #164, dictionary/lint/typecheck green; needs dev-server restart before browser test + `/review`+`/docs` offered. 1.2 attendance RSVP ⏸ PAUSED** with ALL parent/guardian/athlete-USER work → dedicated "how to incorporate parent users" decision at the END of this project (logged in BUSINESS_DECISIONS.md 2026-06-30). Focus → coach-facing value (Phase 2 tryout suite + Phase 3 dev tools, no parent accounts needed). **Phase 2A IN PROGRESS on `dev` 2026-06-30** (greenlit; model confirmed via `/dba` Finding #30, UI direction via `/design` logged): **data foundation BUILT + verified** — mig 165 `rep_tryouts` (1:1 workspace) + `rep_tryout_sessions` (calendar via read-time projection, no event row) + `rep_tryout_registrations` bib/check-in, all RLS-enabled-no-policies; types + data layer in; snapshots #165, dictionary/lint/typecheck green; ⚠ mig 165 DEV-only/prod-pending. **2A.1 setup card + 2A.2 date-check BUILT in the Premium Coaches Portal** (owner surface decision 2026-06-30: tryout-day tooling lives in the COACHES PORTAL — new `…/coaches/teams/[teamId]/tryouts` page + nav — run by the assigned coach; for club teams the assigned coach manages tryouts too = new coach capability; corrected from an initial admin-Rep-Teams placement). Coach-scoped API (assigned-coach + active-program-year gate, fail-closed); "Tryout Day" card = add/edit/remove sessions + blind toggle (default ON) + provincial-window heads-up; lint/typecheck/org-context clean. **2A.3 mobile day-of check-in BUILT + reviewed** (a /review caught + fixed a prod-only timezone bug + a delete double-tap; access-control finder came back clean): `…/tryouts/check-in` — auto bib numbers, big-tap three-cue check-in rows, blind-by-default (bib-first), live progress, Undo on mis-tap, walk-up add (player name only; guardian fields stored empty for now); coach-scoped candidate API (assigned-coach + active-PY + per-reg ownership, fail-closed). lint/typecheck/org-context clean. **2A.4 BUILT (printable candidate sheet + schedule projection) → Phase 2A FUNCTIONALLY COMPLETE.** Printable sheet = branded PDF from the check-in screen (bib-only when blind). Schedule projection = tryout dates now show on the coach calendar (list/week/month) as distinct read-only "Tryout" chips linking to the Tryouts tab (built on the clean committed baseline after the concurrent schedule rework landed; no game-aggregate pollution). lint/typecheck/tokens clean. End-to-end: set up sessions → mobile check-in (bibs/walk-ups/blind) → print sheet → dates on calendar. REMAINING to close phase: help-docs pass + final `/review`. **Phase 2B PLANNED** (build-ready plan 2026-07-01, not started): rubric builder → multi-evaluator no-account mobile scoring + live composite + bias flag → ranked drag Offer/Waitlist/Cut board → one-click accept-to-roster+fees → branded offer/release emails + waitlist auto-promote; pre-work carry-overs = bib-uniqueness constraint + sport-neutrality + one-way reveal-names; 2B tables pre-confirmed (DBA Finding #30); offer/release EMAILS proceed (no accounts, not blocked by parent-users deferral). **2B STARTED on `dev` 2026-07-01: foundation built** — mig 166 `rep_tryout_rubrics` (scorecard, 1/tryout, JSONB categories, RLS-enabled) + **bib-uniqueness safeguard** (partial unique index + dedup + tolerant auto-assign, closes the 2A review finding); rubric types + data layer + coach-scoped rubric API (GET starter+scale / PUT normalize+upsert); sport-neutral starter via a configurable module. lint/typecheck/dictionary/org-context green. **2B.1 rubric builder UI BUILT** (testable): "Evaluation scorecard" card on the Tryouts tab — set up/edit a scorecard (name, 1–5 or 1–10 scale, categories with weights + optional evaluator notes), seeded from a softball/baseball starter. **2B.2 multi-evaluator no-account scoring + live scoreboard/bias flag BUILT + reviewed + committed `91c0f1ae`; 2B.3 ranked decision board + one-way reveal + score-lock (waitlist status via mig 168) BUILT + reviewed + committed `47f52d38`.** **2B.4 one-click accept-to-roster + fees BUILT on `dev` 2026-07-01** (OQ1 ratified: derive the standard schedule from the team's prevailing dues, NO new schema; OQ3 = assigned coach accepts + sets fees per existing dues access; OQ4 = no new gate, within Premium; OQ5 = identity/DOB/guardian prefill + optional number/position/jersey). **mig 169 `accept_tryout_and_create_dues` RPC** does roster-insert + status='accepted' + optional dues schedule/installments in ONE transaction (guards status='offered' under a row lock) — also **hardens the pre-existing non-transactional admin accept** (`acceptTryoutAndAddToRoster` refactored to call it). TWO surfaces share the path: admin Rep Teams applicant → accept drawer, and a NET-NEW coach `tryout-decisions/accept` endpoint + drawer on the decision board (offered → "Accept → add to roster" → flips to Accepted chip). Fees are a TOGGLE (accept-without-fees allowed), dues only RECORD what's owed (no card charge). Help docs (coach recipe + admin guide) updated. **Adversarial `/review` (high-risk, 4 lenses) folded in — 4 fixes: RPC unconstrained numeric (no truncation), per-installment >0 backstop, positional installment numbering, dictionary refresh; full atomic path live-verified via fixture rollback.** typecheck/lint/org-context/tokens/dictionary green. ⚠ **RPC caller-agnostic so 2B.5's token accept reuses it.** **2B.5 offer/release emails + guardian no-login response + waitlist nudge BUILT + committed `cae2b3a0` (2026-07-02).** Decisions ratified: D1 accept=coach-confirms (family Accept records intent + notifies coach, never auto-rosters), D2 auto-promote=flag-the-coach (no automatic family emails from system events), D3 offers carry a 7-day deadline (lazy expiry, no scheduler). **mig 170** adds offer token/deadline/response cols to `rep_tryout_registrations`. ONE shared decision-side-effects helper both surfaces call → org-branded offer (Accept/Decline links + respond-by) / waitlist / release emails (coach board previously sent NONE). NEW public no-login token page + API: GET read-only (email-scanner-safe), POST records accepted/declined (single-use + deadline + status guarded) → notifies the team's coaches; board + admin surface offer state (awaiting/accepted/declined/expired) + a "spot may have opened" waitlist nudge. `/marketing` tone pass on the 3 family emails + adversarial `/review` folded in (error-mask, logo-URL hardening, token guard, expiry-var). Committed as an ISOLATED slice alongside a concurrent Lineup Intelligence commit (`c918d8c6`); shared DB snapshot reconciled. **Phase 2B COMPLETE (2B.1–2B.5). ✅ TRYOUT PROJECT plans ARCHIVED 2026-07-02 — build complete on dev (2B.5 polish 84344de9), pending prod release + owner verification; ⏸ Phase 1.2 attendance DEFERRED (revisit via a new chat).** ⚠ migs 164–170 DEV-only/prod-pending; dev restart before browser test. (see docs/projects/archive/COACHES_PORTAL_TRYOUTS_EVAL_PHASE2B5_PLAN.md + PM brief) (see docs/projects/archive/COACHES_PORTAL_TRYOUTS_EVAL_PHASE2B4_PLAN.md + PM brief) (see docs/projects/archive/COACHES_PORTAL_TRYOUTS_EVAL_PHASE2B_PLAN.md) (see docs/projects/archive/COACHES_PORTAL_TRYOUTS_EVAL_PHASE2A_PLAN.md) (see docs/projects/archive/COACHES_PORTAL_TRYOUTS_EVAL_PLAN.md + PM brief + PHASE1 plan)
- [ ] **Coaches Portal — Lineup Intelligence** (design locked 2026-07-01; **P0 + P1 BUILT on `dev` 2026-07-02**, unpushed) — enrich per-player lineup data so auto-fill generates lineups a coach would actually run: Best/Okay/Never ranked positions (replaces primary/secondary), pitching depth chart (pitcher rank + arm-care innings cap), season-default caps (max-innings-per-position rotation rule + pitching ceiling + min-play floor) overridable per-game for tournaments, A-squad flag, and a smarter game-mode switch (Competitive / Balanced / Development, auto-picked from event type) where Competitive adds two dials — A-squad *balanced-sits* vs *prioritized* + independent *no-back-to-back-sits*. Two management surfaces: per-player page + team depth-chart board. Sport-neutral, additive migrations, phased P1–P6. Owner decisions D1–D5 + assumptions A1–A3 logged. **P0 (sport-neutral groundwork: teams now carry sport through to the coach portal + generator, no migration) + P1 (Best/Okay/Never ranked position picker replacing Primary/Secondary on the player page; generator honors Never as a hard block + ranked preference + leaves a flagged blank instead of aborting when a spot can't be filled) BUILT + typecheck/lint/org-context/dictionary green.** ⚠ mig 171 `rep_roster_players.lineup_profile` (additive jsonb; renumbered from a concurrent 170 dup) DEV-only/prod-pending. Per-game cap override now IN V1 scope (P3). **P1 browser-verified + COMMITTED `c918d8c`.** **P2 (pitching depth chart) BUILT + adversarially reviewed on `dev` 2026-07-02 (uncommitted): per-player "is a pitcher" toggle + rank (Ace/#2/#3) + max-innings arm-care cap; the mound leaves that player's Best/Okay/Never chips; auto-fill leads with the ace in competitive games, spreads innings down the order in balanced/development, never exceeds a pitcher's cap (blank + warning if the mound truly can't be covered); no migration (rides the existing lineup_profile jsonb). P2 COMMITTED `3421e0f8`.** **P3 (innings caps) BUILT + adversarially reviewed on `dev` 2026-07-02 (uncommitted): season-default caps on a new "Lineup rules" card on the team Settings page — max innings at one position (rotation), pitching innings ceiling, minimum innings per player (min-play floor) — each overridable per-game in the Auto-fill "Game rules" group (shows "Season default (N)", persists to that game); generator enforces all three as hard limits (rotation cap per non-mound position, pitching ceiling min'd with the per-player arm-care cap, min-play floor via must-play bench protection); any assigned coach may set the rules; a cap/floor of 0 = off. ⚠ mig 172 `rep_program_years.lineup_settings` + `rep_team_lineups.rules_override` (additive jsonb) DEV-only/prod-pending, snapshots #172.** **P4 (A-squad + competitive game modes) BUILT + adversarially reviewed on `dev` 2026-07-02 (uncommitted): per-player "Gold-medal starter" (A-squad) toggle; the Auto-fill "Mode" (Competitive/Balanced/Development) pre-picks from the game type (tournament→competitive, scrimmage→development, league→balanced), overridable; Competitive reveals two dials — A-squad emphasis (balanced-sits vs prioritized) + "nobody sits two innings in a row"; generator bench priority = min-play floor > no-back-to-back > A-squad protection; A-squad get their best spots; balanced-sits stays purely even-bench; dials are per-game (not persisted); no migration (A-squad rides lineup_profile jsonb). CORE roadmap (P1–P4) FUNCTIONALLY COMPLETE.** **P5 (team depth-chart board) BUILT on `dev` 2026-07-02 (uncommitted): new "Depth chart" coach page (nav under Roster; visible to any roster-viewing coach, editable only by the head coach) — a whole-team grid (players × field positions, with Pitcher + A-squad pinned beside the name) on laptop/tablet, reflowing to a per-player accordion on phones (reuses the player-page position editor); auto-saves per player via the existing roster save path (no Save button) with Undo/Redo; writes the same profile as each player's page so the two stay in sync; the player-page A-squad control became a matching gold star (new `--gold` token). Routed through `/design` (mockup approved, mobile=accordion + gold-token decisions logged) BEFORE build, then adversarially reviewed (5 lenses) with fixes folded in (undo/save-race hardening, redo-invalidation, 403/404 save-loop guard, cap client/server parity, view-only gating, a11y + touch targets). No migration (rides existing jsonb). typecheck/lint/org-context green.** NEXT: P6 analysis/PDF polish + `/docs`. (see docs/projects/active/COACHES_PORTAL_LINEUP_INTELLIGENCE_PLAN.md + PM brief)
- [ ] **Tryouts "Run Your Tryout" Flow UX** (owner UX review 2026-07-02) — the Tryouts page stacked 6 tools with no sequence/orientation; a new coach (or once-a-year veteran) can't tell what to do first or how results become their team. **BUILT on `dev` 2026-07-02** (guidance layer, no tryout-logic change): a top **4-stage progress header** (Set up → Tryout day → Decide → Build your team) that lights up by real state + always names the single next action, the tools **grouped under those 4 stages** with step labels + one-line purpose, a re-openable **"How tryouts work"** overview, and a **"Build your team" results footer** (offered/accepted/on-roster → Roster link) making the tryout→roster→lineup tie visible. New read-only `tryout-overview` endpoint aggregates phase state (no migration). Adversarial `/review` clean (auth matches sibling, no logic bugs). typecheck/lint/org-context/tokens green. committed 262eb258; plans archived 2026-07-02. (see docs/projects/archive/COACHES_PORTAL_TRYOUTS_FLOW_UX_PLAN.md + PM brief)
- [ ] **Coaches Portal — Player Development** (roadmap Phase 3: IDP focus areas + measurables + Development hub w/ Evaluation Sessions + returning-player continuity; scoped via 9-agent ultracode run 2026-07-17, rev 2 hub+sessions owner-directed same day) — mockups ACCEPTED + **3A COMPLETE + COMMITTED to dev 2026-07-17** (mig 189 dev-only): Development card on the player profile (focus areas + measurables + test-type library + quoted context), head-coach-only writes at BOTH app and RLS layers; 4 owner UX rounds folded in; /simplify (11 fixed incl. shared season-analytics assembly + local-merge mutations) + /review (high-risk, 5 lenses, 13 confirmed ALL FIXED incl. RLS tightened to D1) + /docs done; gates green. ⚠ D2 privacy sign-off gates prod ship; 3B hub+Evaluation Sessions IN BUILD next (see docs/projects/active/COACHES_PORTAL_PLAYER_DEVELOPMENT_PLAN.md + PM brief)
- [ ] **Player-PII retention/purge policy** (spun out of Player Development D3, 2026-07-17) — no purge job exists for declined/withdrawn tryout registrations or continuity links; needs a policy (window + owner) via /strategy + /db before it becomes debt (detail in COACHES_PORTAL_PLAYER_DEVELOPMENT_PLAN.md §decisions)
- [ ] **Coach Portal Growth** (spun out of the Coach UX walkthrough, 2026-06-17) — free→paid feature education + cross-shell brand continuity. **Phase 1 quick wins BUILT 2026-06-17 on `dev`**: per-page `ScopeShelf` education strip on Roster/Schedule/Fees/Announcements (quiet, below content, dismiss→faint, express-interest) + admin "FL" logomark + coaches-portal footer link for coach-admins. Phases 2+ (brand-chrome remainder + paid-portal cascade audit, welcome state, funnel instrumentation, self-serve checkout, deferred modal-layer admin) planned. From a 9-agent strategic analysis. (see docs/projects/active/COACH_PORTAL_GROWTH_PLAN.md + PM brief)
- [ ] **Coach Premium Upgrade Flow** (Coach walkthrough Step 7, 2026-06-18) — make upgrading from a FREE team a true upgrade, not a re-signup. Owner-decided: Premium stays **per-team** ($29/mo each), upgrade scopes to one team + carries its data; others stay free; **principle: Premium ≥ Free everywhere**. Target = 2 screens (value → confirm+pay), team pre-filled, roster/schedule/fees migrated in + honest "check these" summary. **Phase 1 (slim+smart signup) + Phase 1.5 (Basic fees per-player alignment) BUILT on `dev` 2026-06-18.** Full free↔Premium data-model reconciliation done (deterministic mapping; gaps closed not faked). Remaining: Phase 2 carry team id through checkout (→/review) → Phase 3 Premium parity (cancelled-event state, **build Premium team announcements**, accept incomplete migrated records, roster ordering; dev-only migs, →/review) → Phase 4 the migration on provision (idempotent, webhook-safe, →/review). **Phases 1–4 COMPLETE on `dev`; Phase 5 spun into its own plan (below).** (see docs/projects/active/COACH_PREMIUM_UPGRADE_FLOW_PLAN.md + PM brief)
  - [ ] **Phase 5 — In-Portal Season & Division** (companion, spun out 2026-06-19) — ✅ **BUILT on `dev` 2026-06-19** (typecheck + focused lint green; adversarial 4-lens review folded in; NOT pushed; owner browser-test pending). A standalone Premium head coach can **start next season themselves** (roster carries by default; optional fee-template + planned-budget carry; schedule fresh; previous season → read-only history) and **edit their team's division** in a new Settings page — no org-admin dependency; org-owned/adopted teams keep season + division control with the club admin. Verified to need **no migration** and **no `lib/db.ts`/`lib/types.ts` edits** (all existing tables/columns/helpers; carry logic in a new self-contained module). (see docs/projects/active/COACH_PREMIUM_PHASE5_SEASON_DIVISION_PLAN.md + PM brief)
- [ ] **Coach Chat Platform** (owner LOE request 2026-06-18, scoped via 2 multi-agent investigations) — NOT STARTED, PLANNED. One generic chat engine (rooms + members + messages + read-watermark, Supabase Realtime, reuses `notify()`/NotificationBell/push — no new notif infra), three surfaces on top: **A Tournament Chat** (one room/tournament for ALL coaches incl. free+paid, optional division sub-rooms, admin-moderated, Tournament Plus-gated — the core ask), **B Coach Peer Chat** (Premium coaches org-wide, League-gated), **C Coach↔Parent Chat** (needs a brand-new parent login tier — clones team_workspace_claims invite; PIPEDA/CASL consent dependency). Seen-receipts via last_read watermark; **no email, push+bell only**. ⚠ top risk = Realtime+RLS silent no-op (validate SELECT policy before UI). Phasing: P1 engine 5–7d · P2 tournament chat 8–11d · P3 peer 6–9d · P4 parent identity+chat 14–19d. **Core (P1+P2)=13–18d (~3–4wk); full platform=33–46d (~7–10wk).** Recommend ship P1+P2 first, P4 as its own project w/ legal review. 6 open decisions. **RESTRUCTURED 2026-06-19 → this is now the UMBRELLA; surfaces split into separate shippable projects (below): Tournament Chat (engine+core), In-Org Coach Chat (+assistant coaches), Cross-Org Messaging; Coach↔Parent stays a later track.** (see docs/projects/active/COACH_CHAT_PLATFORM_PLAN.md + PM brief)
- [ ] **Tournament Chat** (Coach Chat · Project 1; owner restructure 2026-06-19) — IN PROGRESS. **PROVING SLICE BUILT + reviewed + validated 12/12 on dev 2026-06-19**: engine foundation (mig 141 `chat_rooms`/`chat_room_members`/`chat_messages` + membership-based RLS + COLUMN-scoped grants + `chat_messages` realtime/REPLICA IDENTITY FULL). Live delivery + tenant-privacy proven on the real dev DB via `scripts/validate-chat-slice.mjs`. **`/review` (high-risk, 4 lenses) caught + FIXED a CRITICAL the initial 6/6 run missed — a blanket member UPDATE grant let any member self-promote to moderator / evade removal; fixed via column-scoped grants, re-validated 12/12** (now tests self-escalation, spoofed sender, removed-member). Lessons: column-scope grants on any user-writable table (RLS WITH CHECK can't compare old↔new); membership RLS is org-agnostic (cross-org ready); `chat_room_members` SELECT = own-rows-only to dodge RLS recursion; **SUBSCRIBED ≠ streaming** (UI loads history then treats realtime as updates); realtime won't drop a removed member's live socket until reconnect (surface must); first RLS-enabled realtime table on the platform. Dictionary + snapshots updated, gates green. ⚠ **mig 141 DEV-ONLY / prod-pending** (apply at release before promoting reading code). **SURFACE (Phase 2) BUILT on dev 2026-06-20** (owner decisions: both portals · core single-room first): participant resolver (`lib/chat-resolvers.ts` — unions claimed-free + direct-Basic + rep coaches, dedupe, "Not yet joined"; inverse resolver self-heals coach memberships), `lib/chat-service.ts` (room ensure/sync, history, post+notify, mute≤72h/delete/close), shared message + admin moderation API routes, **ChatPanel** (load-then-stream, dedupe, live soft-delete), admin **Chat tab** (Operations, UpgradeGate), coach Chat entry + page in **both** portals with unread badge, `chat_message` notify (push-ON/email-OFF), `tournament_chat`→tournament_plus gate. **NO schema change** (rides mig 141). typecheck ✅ · chat-file lint ✅ · proving-slice 12/12 ✅ · dev server restarted. ⚠ verify:changed has ONE pre-existing FOREIGN ratchet failure (`check-in-volunteer.module.css`, commit 01243f5 — not this work). Deferred: division sub-rooms, notification deep-links. Owner browser-test pending. (see docs/projects/active/TOURNAMENT_CHAT_PLAN.md + PM brief)
  - [ ] **Tournament Chat — UX & Design Review** (2026-06-22) — full mobile+desktop UX/design review of the built chat across all 3 surfaces, benchmarked vs iMessage/WhatsApp/Slack/Discord; 12-dimension adversarial review → 90 verified presentational findings (7 blocker / 35 high / 29 med / 19 low; engine untouched). 3-phase refinement plan. **Phase 1 BUILT on `dev` 2026-06-22** (owner forks: one shared dark style · coloured initials now · "last seen"+new-msg divider, no receipts · Phase 1 first): light-mode/token fixes, message grouping + date/new-messages dividers, coloured-initial avatars, autosize composer + jump-to-latest + scroll anchoring, full-height/edge-to-edge fixes (incl. the broken org-based portal height), accessible manage-panel dialog, and unread badges everywhere incl. the previously-blind admin desktop + the previously-unreachable league/club mobile Chat tab. Adversarially self-reviewed (0 blockers; 7 minor issues fixed); typecheck+lint clean. **Desktop composition fixed via /design agent (bounded full-height pane + centered ~820px reading column + medallion empty state, logged as a binding design decision); mobile scroll-lock + edge-to-edge.** **P2 BUILT on `dev` 2026-06-22** (most of P2 already shipped in P1+design pass; P2 build = desktop persistent room sidebar for multi-room coaches, tidied display-name fallback, roster initials avatars, calmer/dismissable status banners, final a11y polish; adversarially reviewed, 3 minor fixes incl. a desktop room-list CSS specificity bug; typecheck+lint clean; owner browser-test pending). (see docs/projects/active/TOURNAMENT_CHAT_UX_REVIEW.md + PM brief)
    - [ ] **Phase 3 — Conversational Depth (WhatsApp parity)** (scoped 2026-06-22 via engine-grounded feasibility pass; **owner scope LOCKED + go-ahead 2026-06-22**) — IN PROGRESS on `dev`. **Committed set (9):** Tier-1 core — pinned messages, reply/quote, @mentions, emoji-picker button, delete-own, in-conversation search, "read by N"/"last seen" — **+ emoji reactions + real polls** (both L, each a migration + short spike). **Dropped:** online presence. **Optional/unselected:** edit-own (build only on request). **Deferred:** typing, per-message "seen by" list (→ In-Org Coach Chat). Infra-heavy OUT of scope. Owner forks 2026-06-22: search = recent-window first (full history = fast-follow); reactions+polls = spike-first; reaction set 👍👎❤️✅😂🎉🙏; no new in-chat paywalls. **3A (no-migration quick wins) BUILT + each /review-passed on `dev` 2026-06-22:** emoji-picker button; delete-your-own-message; in-conversation search (recent-window V1); "read by N of M" receipt + a delete-UX polish pass (button beside the bubble, styled in-app confirm, contiguous-hover fix). **3B (conversational lift) BUILT + each /review-passed on `dev` 2026-06-23:** reply/quote (server-rebuilt snippet = anti-spoof, rides `metadata`, no migration); @mentions (member-directory picker + highlight + a distinct **`chat_mention`** notification that bypasses muted general chat — TS-union event type, no migration; new members endpoint); pinned messages (organizer pins → collapsible top banner, several pins, live; **mig 146 `chat_messages.pinned_at`/`pinned_by_user_id` APPLIED TO DEV** + dictionary/snapshots refreshed (#146), browsers can't self-pin, rides existing realtime publication; new pinned endpoint + pin/unpin moderate actions). ⚠ **mig 146 DEV-only / prod-pending** — apply to prod before promoting chat-reading code. Chat files lint + chat typecheck clean; ⚠ branch-wide `verify:changed`/full `typecheck` noise is FOREIGN (other agent's plan-repackaging migs 144/145 + token-drift file) — not chat. Owner browser-test pending; dev server restarted. **3C (richer interactions) IN PROGRESS:** **emoji reactions — SPIKE GREEN + FEATURE BUILT + adversarially reviewed on `dev` 2026-06-23.** New realtime-published table **mig 147 `chat_message_reactions` (DEV-applied, ⚠ prod-pending)**; proving slice grown to **23/23** (`scripts/validate-chat-slice.mjs`). **Spike caught + fixed an RLS-on-hard-DELETE leak** by soft-deleting reactions (Supabase doesn't RLS-gate hard-DELETE realtime events — same reason the engine soft-deletes messages); every reaction event is now INSERT/UPDATE and RLS-correct. Fixed 7-emoji set, SELECT-only grant (all writes = service-role server routes), denormalized `room_id`. UI: tap-a-reaction-button → 7-emoji row; chips with live counts; tap a chip → who-reacted names; self-react + multiple emojis per coach. 4-lens scoped review folded in (HIGH fix: reaction-summary load made non-fatal so a reactions-table issue can't 500 the whole chat). Chat files lint + typecheck clean; tokens clean; owner browser-test pending. **Real polls — SPIKE GREEN + FEATURE BUILT + adversarially reviewed 2026-06-23.** New realtime-published table **mig 148 `chat_poll_votes` (DEV-applied, ⚠ prod-pending)**; proving slice grown to **34/34** (same soft-delete/RLS discipline; a transient live-vote test failure traced to socket-churn in the harness, fixed with fresh clients). A poll IS a chat message (options+settings ride metadata; create/close reuse message realtime) — only the votes are a new live store. Owner decisions: organizers create · creator picks single/multiple · visible voters · creator can close. UI: composer "Create poll" builder, inline poll card with live bars/counts + who-voted, organizer close/reopen. 4-lens review folded in (cast-then-clear single-choice race fix; corrected multiple-choice %; de-optimized close; builder reset on room switch; dup-option + closedAt + rate-limit hardening). Lint/typecheck/tokens clean; owner browser-test pending. **3C (reactions + polls) BUILD COMPLETE; migs 147+148 prod-pending.** (see docs/projects/active/TOURNAMENT_CHAT_PHASE3_CONVERSATIONAL_DEPTH_PLAN.md + PM brief)
    - [ ] **Division Rooms ("Channels")** (deferred from Phase 3; scoped 2026-06-23) — **BUILT on `dev` 2026-06-24, unpushed; NO migration.** Organizer-created, division-scoped chat rooms on top of an always-present undeletable "All coaches" room (Slack-style channels). All 7 decisions confirmed at the recommended set (flexible multi-division · auto-maintained membership · undeletable All-coaches · division-scoped V1, deferring hand-add/cross-tournament/coach-created). Built: division-SET resolver; room model (`roomDivisionIds` + list/create/rename/delete with All-coaches protected) on the existing engine (`chat_rooms.ref_sub_id` opaque uuid + `settings.divisionIds`); division-aware sync + roster; coach self-heal into EVERY matching room; `…/chat/rooms` + `…/chat/rooms/[roomId]` API + `roomId` on `…/chat/moderate` (old single-room `…/chat` GET removed); organizer room-switcher + `NewRoomDialog` composer + per-room manage/rename/delete; coach sidebar tournament context label. typecheck/lint/dictionary/snapshot/org-context green. **Next:** adversarial `/review` on room-CRUD + membership routing + multi-room UI → browser verify → ship behind gate. (see docs/projects/active/TOURNAMENT_CHAT_DIVISION_ROOMS_PLAN.md + PM brief)
- [ ] **In-Org Coach-to-Coach Chat** (Coach Chat · Project 2; owner restructure 2026-06-19) — NOT STARTED, PLANNED; paid coaches within ONE org talk to each other (org-wide room + optional per-team), **League/Club gated**, reuses the Project-1 engine. **Introduces the assistant-coach concept** (teams get a full staff, not just a head coach; own building-block note; also feeds Tournament Chat). No Coaches Portal dependency. ~2–3wk. (see docs/projects/active/IN_ORG_COACH_CHAT_PLAN.md + PM brief + ASSISTANT_COACHES_BUILDING_BLOCK.md)
- [ ] **Coaches Portal IA/UX review** (owner-requested 2026-07-02) — **Phase 0 + Phase 1 + Phase 3 BUILT + adversarially reviewed on `dev` (no migration, unpushed).** Journey-first IA reorganization of the **Premium** portal (Basic shell owned by COACH_NAV_REBUILD_PLAN). Owner decisions #1/#3/#9 accepted 2026-07-04; **#2 REVERSED → Standings CUT 2026-07-06**; **#4–#8 accepted 2026-07-06** at the recommended answers (built same session). **Phase 3 (navigation rebuild) BUILT 2026-07-06:** regrouped sidebar + mobile More-drawer into five plain-language groups (Squad/Season/Money/Communication/Team admin); new **Lineups** nav item + landing page (front door for the buried lineup builder, deep-links each game into its builder); **Tryouts/Tournaments** hide into an **"Explore"** group until used (two cheap read-only per-assignment signals), still reachable; money-gated **"Last season"** Overview tile; **"Accounting"→"Money"** + **"History"→"Season Review"** renames (routes unchanged); shared `navVisible` gate extracted to one module. Typecheck/lint clean; **formal `/review` + `/docs` COMPLETE** (5 fixes folded across 2 review passes; org-invite 2nd-lime CTA FIXED, mobile primary-tab filter left as-is per owner, 1 pre-existing "missing email" badge now FIXED; coaches help guide synced with the new grouping + Lineups FAQ + Money/Season Review renames). **Phase 3 COMMITTED `3b0dabc1` + browser-verified 2026-07-06** (unpushed on `dev`). **Phase 4 (Review Depth) PLANNED** — own plan+brief (COACH_PORTAL_PHASE4_REVIEW_DEPTH_PLAN.md); build leads with the "who hasn't paid" list; placement card gated on a hosted-only-vs-cut owner decision. **Next:** build Phase 4 → promote Phase 3 when ready. **No phase needs a schema migration.** (see docs/projects/active/COACH_PORTAL_IA_UX_REVIEW_PLAN.md + PM brief)
  - [ ] **Assistant Coaches — first-class participants (paid side essentially DONE)** — P1 + **P2 DEPLOYED TO PROD 2026-07-06** (capability enforcement + head-coach invite/manage + admin oversight; migs 173+174 live) · **P4 BUILT on `dev` 2026-07-06, unpushed** (chat-membership revoked on coach removal — no migration) · **P3 free-Basic assistants DROPPED (owner: not needed on free)** · P5 house-league coaches = later/separate track. — code-grounded (7-investigator + verification workflow vs live dev+prod). **Headline:** the paid side already stores a head/assistant role + admin can already assign it, but the role is **not enforced** on ~15 coach surfaces — so an assistant added today inherits FULL head access (roster PII, money, mass-email). V1 = a **per-assistant capability model that closes that over-grant**; chat then picks assistants up free (resolvers already include all staff). Locked: team-scoped login (team guest, not a new account type / not an org member) · **head coach assigns duties per assistant** (least-privilege defaults; team money = Off/Read/Read+Write per assistant; PII+notes off; announce draft-only) · head coach administers in BOTH standalone+club-run, org admin override-only + optional approval lever · free everywhere capped 1–2 on free Basic · paid first → free Phase 3 → house league later. Phases: P1 paid capability+enforcement (gate) → P2 paid admin UI+invite+comms → P3 free → P4 chat lifecycle → P5 house league. (see docs/projects/archive/ASSISTANT_COACHES_PLAN.md + PM brief)
- [ ] **Cross-Org Coach Messaging** (Coach Chat · Project 3; owner request 2026-06-19; scoped + adversarially verified same day) — NOT STARTED, PLANNED; a fourth surface on the Coach Chat engine. Coach in one org messages a coach in another (e.g. arrange a scrimmage). Locked: free coaches excluded (their only chat = admin-hosted tournament chat); **both sides need a PAID coaching entitlement**; directory is opt-in. Recommended: leave multi-org intact, add a no-single-org "direct message" room type, ship **invite-by-link V1** (no directory, no cross-tenant PII, no legal review), defer the opt-in directory + connection-requests to V2 (CASL/PIPEDA legal gate). 3 structural fixes (room ownership, per-recipient notification stamping, account-type-aware gating). **Does NOT depend on the 1:1 decision.** Hard dependency: Coaches Portal must launch first (no standalone-coach audience exists yet). LOE ≈ 2.5–3.5wk for V1 on top of the core engine. (see docs/projects/active/CROSS_ORG_COACH_MESSAGING_PLAN.md + PM brief)
- [x] **Decision: "1 user = 1 org" vs. multi-org** (raised + DECIDED 2026-06-19) — analysis COMPLETE (9-investigator multi-agent run, every load-bearing claim adversarially verified vs live code; opposite-case stress-tested). **DECIDED: "single-org by default, multi-membership by deliberate exception" — NO irreversible one-org lock.** Owner answers: two-org admins rare, connected network appealing, coach-also-in-club = one login. Implementation = Identity Model Cleanup (COMPLETE + archived). (see docs/projects/archive/ONE_TO_ONE_VS_MULTI_ORG_DECISION_ANALYSIS.md + DECISION_PM_BRIEF)
- [x] **Identity Model Cleanup** ("single-org by default"; implements the decision below) — ✅ **COMPLETE + BROWSER-VERIFIED 2026-06-19 on `dev` (all 4 phases, each /review-passed; no migrations; ARCHIVED).** Identity model is now coherent + simple by default without a hard lock. **P1** removed the "Start something new" add-workspace card, signed-in existing-org users redirect home (no self-serve 2nd empty org), softened start-picker copy. **P2** sign-up/invite/accept rules now agree (all gates active-only; resolves the long-open "cross-org accept guard"); a coach's own Coaches Portal never counts so one-login coach+club works; org-create server-side guard; orgless auth fallback resolves a deterministic "home org"; "All Workspaces" link hidden for single-workspace users. **P3** one-paid-Coaches-Portal-per-email guard at checkout + provisioner (gated today; /review caught+fixed a reactivation double-portal gap). **P4** org-admin member delete runs orphan-team cleanup, accept-invite handles 2 pending invites (a 3rd candidate dropped as a non-bug). (see docs/projects/archive/IDENTITY_MODEL_CLEANUP_PLAN.md + PM brief)
- [ ] **Billing & Accounting Coherence** (audit FP-6, successor to the archived Stripe Integration) — bill-what-you-confirm lifecycle (J4-032/033), board numbers that don't lie (overdue/headroom/BvA/rollup), reversible money actions, whole-club Financial Summary (see docs/projects/active/BILLING_ACCOUNTING_COHERENCE_PLAN.md)
- [ ] **Admin IA & Multi-Module Navigation** (audit FP-7, NEW) — kill the tournament-first skew on league/club orgs: plan-aware dashboards/nav/public homepage, /home switcher voice, buried-module nav, reserved-slug guard; foldable into the per-tier projects (see docs/projects/active/ADMIN_IA_MULTIMODULE_NAV_PLAN.md)
- [x] **Platform-Admin Employee Experience Audit** — DONE + ARCHIVED 2026-06-13 (6 roles walked, 68 findings, 0/6 day-one, support-seam NO/support YES/product). Spun out into F1–F5 fix projects below (see docs/projects/archive/platform-admin-audit/SYNTHESIS.md)
- [x] **F1 · Platform-Admin API Hardening** (P0 / security, fast-track) — ✅ **COMPLETE + browser-verified 2026-06-15.** Several platform-admin API routes gated session-only not by-permission → any role could overwrite transactional email copy, mass-blast email, export the lead/feedback/error DBs; support could delete users; dev-tools had no super_admin gate. Fixed via one shared `requirePlatformAreaApi` helper + ~7 route guards (8d00c1e, not pushed). Plan archived (see docs/projects/archive/PLATFORM_ADMIN_API_HARDENING_PLAN.md)
- [x] **F2 · Platform-Admin Least-Privilege UX Consistency** (P1) — ✅ **COMPLETE + browser-verified 2026-06-15.** The #1 day-one blocker: controls rendered enabled then 403 with no "requires X" message; root = CustomerUsersClient had no role prop + the good "View-only for your role" note was copied nowhere. Fixed via new `RequiresAccess` note + role props on the 3 actually-ungated surfaces (Customer Users, Retention, Overview Action Queue); audit over-stated the rest (1917fa2, not pushed). Plan archived (see docs/projects/archive/PLATFORM_ADMIN_LEAST_PRIVILEGE_UX_PLAN.md)
- [x] **F3 · Platform-Admin Support Seam / Feedback Triage** (P1) — ✅ **COMPLETE 2026-06-14** (Phases 0–5 built; Phase 6 mailto deferred by owner). Support/billing can now triage + escalate feedback; default actionable filters; error-group→feedback reverse link. Realized Option B as a split `feedback` area (no new permission enum). Phase commits 736e0a7·d6abe5d·cc405bb·de69712·40fb78d on `feat/free-tier-coaches` (not pushed). ✅ **Browser-verified 2026-06-15; plan archived. mig 126 APPLIED TO PROD 2026-06-15** (4dd7698; additive/nullable, check-prod-migration-drift green) — prod-DB gate CLEARED. (see docs/projects/archive/PLATFORM_ADMIN_SUPPORT_SEAM_PLAN.md)
- [x] **F4 · Platform-Admin Operator SOPs + Day-One Orientation** (P2) — **BUILT + COMMITTED 2026-06-14 (43f8cde, not pushed)**: 6 net-new SOP sections (feedback/observability/change-requests/email-templates/email-batch/early-access) + expanded plans-pricing (risk ladder/Stripe checklist/tool matrix); Product Operator + Growth Operator role-path cards + 5 quick links; role-aware first-login orientation banner (reused HelpCallout, `pa_orientation_dismissed`) + Action Queue non-billing note. Content-only — no schema/API/new files; copy grounded in shipped F1/F3 behavior (support+billing write feedback, observability triage product-only, Escalate = badge+filter notifying no one, change-request price/gating/config auto-apply on approval). lint:focused + typecheck clean; no dev-server restart needed. ✅ **Browser-verified 2026-06-15; plan archived.** (see docs/projects/archive/PLATFORM_ADMIN_OPERATOR_SOPS_PLAN.md)
- [x] **F5 · Platform-Admin Operator Polish backlog** (P3) — ✅ **COMPLETE 2026-06-15** (all groups built/no-op; not pushed; owner browser smoke-test pending → archive plan+brief once verified). **Batch 6 (Group I date-format sweep / PF-5) BUILT + COMMITTED 2026-06-15 (06566dd):** one shared `lib/format-date.ts` formatter applied across all 17 date-rendering surfaces (replaced ~11 ad-hoc local helpers). Context-typed rule: forensic/legal timestamps stay ALWAYS absolute (audit log, overrides, feedback, email sent, change-requests, catalog/pricing); "freshness" signals (last sign-in, support-note time) render relative-when-recent + absolute >7d with exact-time tooltip; deadline "(Nd left)" preserved. `fmtSince`/`fmtDaysLeft` are CLIENT-ONLY (Date.now) → 6 server components stay absolute (no hydration mismatch). Locale = operator en-CA, NOT Toronto-pinned. Drift bugs fixed: observability "Last seen" missing year, email dashboard wrong toLocaleDateString API, plans-pricing 3 granularities. Audit log keeps a documented seconds-precision local helper. ⚠ new shared `lib/format-date.ts` → **restart dev server** before browser QA. typecheck + lint (0-err) + verify:changed clean; reviewed via /review high-risk funnel (3 lenses; 0 confirmed defects, 2 intentional advisories). Presentation-only. **Batch 1 (Groups B+C+D) BUILT + COMMITTED 2026-06-14 (1ea63ec):** Customer Users menu triage-order; League Starter hero badge + Entitlements scope-wall count; URL-addressable org-detail tabs (`?tab=`) + Retention deep-link to Billing. **Batch 2 (Group E billing polish) BUILT + COMMITTED 2026-06-14 (546f683):** Stripe subscription deep-link, Retention urgency badges + empty-state callout, Bulk-ops audit-log link, proper-cased override labels, bulk Module-Add-On product-access hint, override-form expiry warning, Billing Specialist Help Hub card. **Batch 3 (Group F growth polish) BUILT + COMMITTED 2026-06-14 (e599520):** Early Access default-New + overdue-first sort + date-range filter + Previous/Next pagination ("In view" summary); Overview growth-role default tab; founding_welcome preview sample values; email stats subtitle/tooltips; per-email recipient counts (new shared audience logic in lib/email-sender.ts). **Batch 4 (Group H read-only observer) BUILT + COMMITTED 2026-06-15 (d2c134e):** snapshot button hidden + API gated to manage_product (closed a read-only write hole); Billing plan/limit form collapses to a read-only summary for non-billing roles. (PAR-002 already done in F2.) **Batch 5 (Group G nav IA + guards) BUILT + COMMITTED 2026-06-15 (4b6f68e):** split "System" nav into "Support & Diagnostics" + "Governance", Email Templates → "Billing & Product"; "Email" → "Email Campaigns"; "Change Requests" → "Approval Queue" everywhere user-facing (route/area/anchor/table unchanged) + Plans-&-Pricing relationship callout; page kickers aligned; Delete-Org "Danger Zone" visual; Customer Users employee-exclusion callout; defensive requirePlatformAreaView guards on orgs/orgs[id]/audit (3 pages — customer-users already guarded). Reviewed via /review (2 findings fixed). ⚠ shared PlatformAdminNav.tsx → **restart dev server** before browser QA. (PAR-002 already done in F2.) (PAG-004 already shipped in F4; **Group A no-op**.) ⚠ Group F touches shared lib/email-sender.ts → **restart dev server** before browser QA. **All groups done (A no-op · B–I built); F5 COMPLETE.** Not pushed. ✅ **Browser-verified 2026-06-15; plan archived.** (see docs/projects/archive/PLATFORM_ADMIN_OPERATOR_POLISH_PLAN.md)
- [x] **Admin Role Parity (admin = owner − billing & user-mgmt)** — BUILT 2026-06-09 on `feat/free-tier-coaches` (tsc clean for all changed files; awaiting browser verification). Fixed the reported bug where a tournament **admin** was blocked from the branding/"Public Site" page (`userRole !== 'owner'` wall) and hidden from the Archive Tournament button. Owner-confirmed policy: branding → **all admin**, member management → **keep current split** (no change), org settings → **keep owner-only** (no change). Mechanism: new `manage_branding` capability (owner+admin defaults, shows in the per-member override editor) replacing raw owner checks on the branding page + `tournament-branding`/`tournament-logo`/`tournament-hero-banner` routes; archive button now gates on `create_tournaments` (matches the API). **Companion security fix:** `/api/billing/portal` + `/api/billing/create-checkout` had NO server-side role check (bare `getAuthContext`) — added `requireCapability(auth, 'billing')` so billing is owner-only in fact, not just hidden. ⚠️ shared `lib/roles.ts` changed → restart dev server before browser testing; verify owner checkout/portal still work. **Phase 2 (2026-06-09) closed the billing/ownership leak in the Coaches Portal Links flow** — `team-links` POST now owner-reserves `invite_billing`/`approve_billing` (`billing` cap) + `invite_ownership` (owner); the page hides those buttons from admins (decline stays admin). Remaining deferred: org-level raw owner-checks + tournament-slug page↔API mismatch (see [plan](docs/projects/active/ADMIN_ROLE_PARITY_PLAN.md) and [PM brief](docs/projects/active/ADMIN_ROLE_PARITY_PM_BRIEF.md))
- [ ] **Observability, Error Tracking & In-App Feedback** — **Phases 1 + 2 BUILT 2026-06-09** on `feat/free-tier-coaches` (tsc + lint 0-err + 13 unit tests + dictionary ratchet all clean; **migration 118 applied to DEV + PROD** (RLS-enabled-no-policies, leak-verified: anon/authenticated see 0 rows on prod — switched from RLS-disabled after a pre-prod check found prod grants anon default SELECT on public tables); `OBSERVABILITY_ENV` set in Amplify; dev server restarted/login-200/no-EACCES; end-to-end capture + grouping + email-scrub verified on live dev DB; deploy gate CLEARED — code can ship to master; awaiting browser verification). Scoped via 12-agent investigation + build-vs-buy design + a 17-agent adversarial review (9 real findings folded in: rate-limiter LRU+global-cap, distinct-org partial index, metrics re-merge-on-fail, email-value scrubbing). **P1 shipped:** migration 118 (6 tables + `record_error_event` RPC, RLS-off service-role), `lib/observability/*`, `instrumentation.ts onRequestError` global capture, `/api/client/error-capture` + `app/global-error.tsx` + wired `app/error.tsx` (the "logged automatically" lie now true), `org-context`/`notifications` wrapped + 4 hot routes' catch-block capture, new "Observability & Feedback" dictionary domain. A production+dev error-tracking + feedback "notification center" surfaced in platform-admin: central `withObservability()` wrapper captures API/server 5xx with org/user/route/severity/env attribution via AsyncLocalStorage + `getAuthContext`, fingerprints+groups identical errors into actionable issues (`error_groups`/`error_events`), calls-vs-errors chart (clone `CumulativeChart` SVG, **no recharts**) + metrics + filterable triage list (resolve/ignore/snooze) reusing the platform-admin `audit/page.tsx` + `requirePlatformAreaView` patterns; first-occurrence critical alerts via `lib/email.ts` to `ADMIN_EMAIL` (NOT the org-scoped bell — `notifications.user_id` is hard-bound to `auth.users`); coarse `request_metrics_rollup` + pg_cron fold/retention + heartbeat freshness + manual `/sweep` fallback; in-app `FeedbackWidget` (bug/feature/feedback) for org admin/coach/scorekeeper/public → `feedback_submissions`, auto-linking a bug report to its captured error via last `requestId`. **Recommended in-house Postgres (zero error-SaaS):** Amplify pnpm build fragility (commit `ad9dc66`) + Canadian data residency / consent-gated minor DOB + every layer has an in-repo precedent; design is a clean subset so Sentry capture can bolt on later (Phase 5) additively. New migration **118** (6 tables, RLS-disabled service-role, mirrors `email_sends`/`platform_events`; confirm number at build). 5 phases (~5–8 days core): 1 capture+schema (also fixes the `error.tsx` "logged automatically" lie), 2 dashboard, 3 feedback widget, 4 rollup/retention/alerting, 5 (deferred) in-bell admin alerts + full route coverage + optional Sentry. **Decisions locked (owner):** in-house Postgres + platform-admin-only feedback. **P2 shipped (owner scope: 24h default + §6 breakdowns + CSV export):** `'observability'` platform area (view super_admin/product/support, write super_admin/product) + System nav; `lib/observability/dashboard.ts` (service-role reads, `server-only`); `observability/page.tsx` (env toggle prod-default + 24h/7d/30d window, 4 MetricCards, `CallsVsErrorsChart` SVG no-recharts, freshness chip degrading pre-P4, §6 breakdowns routes/status-codes/client-vs-server/MTTR, filterable+paginated `error_groups` list + full-dataset CSV/XLSX export route); `[groupId]` detail (14-day sparkline + sampled events redacted stack/context + Resolve/Ignore/Snooze/Reopen); `status` route (`requirePlatformPermission('manage_product')` + `writePlatformAuditLog`); shared `MetricCard` extracted. 14-agent adversarial review folded in (sparkline off-by-one, full-set export, expired-snooze→open KPI, MTTR all-time label, sampled-count disclosure, `server-only` guard). Verified tsc/lint clean + restart/login-200/no-EACCES + smoke (unauth 307/403). **P4 shipped (rollup/retention/critical-alerts) 2026-06-10 — migration 122 applied DEV + PROD** (owner-approved; adds no tables/columns → `check:migrations` is BLIND so the prod apply was a deliberate manual step — verified live on prod: pg_cron installed, 2 jobs as postgres, `record_error_event`→jsonb, anon execute denied 42501, drift gate green; per-branch `OBSERVABILITY_ENV` console vars set): pg_cron 1.6.4 (verified both envs) + `obs_fold_metrics` (atomic `DELETE…RETURNING` drain, NEVER TRUNCATE; agg keys pre-normalized vs the index `coalesce`) + `obs_retention_sweep` (reopen expired snoozes / purge events>30d + recompute distinct-org / delete resolved>90d / trim rollups>1y / prune cron history) on `*/5` + `15 8` UTC, SECURITY DEFINER + anon-revoked (42501), success-only heartbeat; `record_error_event` DROP+recreate→jsonb transition flags (is_new/became_critical/regressed/reopened); `lib/observability/alerts.ts` emails ADMIN_EMAIL on new/escalation/regression critical (server+**explicit-production fail-closed**+per-worker hourly cap, **awaited** not detached — Lambda-freeze-safe); super_admin `/observability/sweep` route; `requireSuperAdmin` promoted into `lib/platform-auth.ts`; freshness chip fold/sweep-staleness + status=error amber; `amplify.yml` plumbs `OBSERVABILITY_ENV`. Owner decisions LOCKED 2026-06-10 (allowlist payments/auth/register/org-create; triggers new+escalation+regression, production+server only; recipient ADMIN_EMAIL; retention 30d/90d/1y/08:15 UTC). 3-agent pre-build design review + 6-dimension/3-lens post-build adversarial review folded; tsc + lint 0-err + 24 unit tests + dictionary ratchet + live-dev (cron/fold/sweep/flags/anon-deny/e2e-alert/403) all clean; dev restarted. **P3 shipped (in-app feedback — last core phase) 2026-06-10:** `FeedbackWidget` (portaled modal: Bug/Feature/Feedback pills + route-defaulted category + body) + launcher on AdminSidebar/bottom-nav/CoachPortalShell/scorekeeper/check-in; `POST /api/feedback` all-personas (anon-accepted, per-user 1/hr + IP throttle + global 30/min backstop, redact-before-write, awaited admin-notify + submitter confirmation); `platform-admin/feedback` triage (badges/filters/CSV-XLSX export/`[id]/status` `manage_product`+audit-log) + `Feedback` nav. **Owner-approved Mechanism A folded in** so the bug→error deep-link is broad: `proxy.ts` stamps `x-request-id` on every `/api/*` (fast-path; `/api/admin/*` keeps full proxy), `withObservability` adopts it, `onRequestError` threads it to `captureError`, client `window.fetch` wrapper stashes it. No migration (table sealed in 118); reuses `observability` area. 10-agent adversarial review → 4 fixes folded (request-id-on-global-capture-path, CSV formula-injection guard, fetch-wrapper HMR idempotency, modal portal+bottom-nav decouple). tsc/lint/34 unit tests green; ⚠️ restart dev server before browser pass (proxy.ts + new files + shared modules). **P5 ❌ CANCELLED 2026-06-10 (owner — won't be needed):** in-bell platform-admin alerts dropped (email alerting suffices); route-coverage sweep lives on as Mechanisms B+C of the [Route-Instrumentation plan](docs/projects/archive/OBSERVABILITY_ROUTE_INSTRUMENTATION_PLAN.md) (the active next step); Sentry stays an additive non-planned option. **Phases 1–4 + 3 done — observability is effectively complete** (see [plan](docs/projects/active/OBSERVABILITY_ERROR_TRACKING_PLAN.md) and [PM brief](docs/projects/active/OBSERVABILITY_ERROR_TRACKING_PM_BRIEF.md))
- [x] **Observability Route-Instrumentation Rollout** — ✅ **COMPLETE 2026-06-11 (Mechanism B at 100.0% — 388/388 wrappable handlers)**; built but NOT committed (branch = trunk). Formalizes the parent feature's "wrap routes incrementally + track coverage %" intent (Phase-5 sweep pulled forward + made concrete). Measured state: only **2 of 279** API routes instrumented, so the dashboard's calls-vs-errors denominator is near-empty and Phase-3 bug→error deep-links rarely resolve. Three independent mechanisms: **A** one global `requestId` stamp (proxy.ts cheap fast-path + `withObservability` adopt-incoming-header → universal `x-request-id`, the recommended Phase-3 fast-follow); **B** gradual per-route `withObservability` wrapping by risk×traffic tranche (money/identity → game-day → CRUD bulk → platform-admin; dev/seed excluded) via a skip-and-report codemod, one domain per PR; **C** surgical `captureError`-in-catch on money/identity swallowed-500s. No customer-facing change, no migration. **Mechanism A ✅ BUILT 2026-06-10; Mechanisms B + C Tranche 0 (money/identity) ✅ BUILT 2026-06-11 on `feat/free-tier-coaches`** — dependency-free codemod (`scripts/wrap-route-observability.mjs`, TS-compiler-API, skip-and-report + EOL-preserving + import-merge) wrapped **24 files / 30 handlers** (billing·auth·register·registrations·league/rep-register·org-create·admin-teams·admin-games; `client/error-capture` excluded) + added **9 swallowed-500 `captureError` catches** across 7 money/identity routes; new coverage tracker (`scripts/check-observability-coverage.mjs`) + shared `observability-route-exclusions.mjs` → coverage **0.8% → 11.1% (43/387)**; typecheck/lint 0-err, codemod idempotent; 4-dimension adversarial review (11 agents) → 4 minor/nit fixes folded; Tranche-0 smoke PASSED (statuses unchanged, `x-request-id` intact). **Tranche 1 (game-day: scorekeeper·official·check-in·schedule·divisions, B-only) ✅ BUILT 2026-06-11 — 6 files/10 handlers incl. an official↔scorekeeper shared-raw-handler refactor (correct per-route attribution, no double-wrap); typecheck/lint green, smoke 401/307 as expected. **Tranche 2 domain 1 — `api/admin/accounting` ✅ BUILT 2026-06-11** (B-only, 17 files/25 handlers, ZERO codemod skips/hand-wraps). **Tranche 2 domain 2 — `api/admin/rep-teams` ✅ BUILT 2026-06-11** (B-only, 29 files/47 handlers, 0 codemod-skips + a 4-file type-only `resolved.error!` hand-fix for a pre-existing `'error' in resolved` narrowing looseness the stricter wrapper surfaced). **Tranche 2 domain 3 — `api/admin/house-league` ✅ BUILT 2026-06-11** (B-only, 18 files/31 handlers, 0 skips/hand-fixes). **Tranche 2 domain 4 — remaining `api/admin/**` ✅ BUILT 2026-06-11** (B-only, 59 files/82 handlers, 0 codemod-skips + an 11-site `X.response!` type-only hand-fix across 9 tournament import/registration-fields routes + 1 pre-existing `prefer-const` cleanup → **`api/admin/**` now 198/198 wrapped**). **Tranche 2 domain 5 / LAST — `api/coaches/**` ✅ BUILT 2026-06-11** (B-only, 54 files/86 handlers, 0 codemod-skips + a 40-file/63-site `resolved.error!` type-only hand-fix; incl. untracked Free-Tier Phase-5 `coaches/tournaments/**`; smoke = pre-existing 500 on bogus-org unauth [resolveCoachContext throws → onRequestError; confirmed behaviour-preserving by reverting one file to HEAD] + `x-request-id` stamped). **→ Tranche 2 COMPLETE.** **Tranche 3 — `api/platform-admin/**` ✅ BUILT** (45 files/60, 0 skips/hand-fixes) + **`api/public/**` + `api/notifications/push/*` + 4 singletons (`early-access`/`feedback`/`plan-gating`/`send-message`) ✅ BUILT 2026-06-11** (13 files/13, 0 skips/hand-fixes). **🎉 MECHANISM B ROLLOUT COMPLETE — coverage 0.8% → 100.0% (388/388)** (only `api/dev/**` + `api/client/error-capture` excluded by design). All tranches: typecheck 0 / eslint 0-err / idempotent / smoke behaviour-preserving. **Per-domain hand-fixes (all type-only, behaviour-preserving): `resolved.error!` rep-teams+coaches; `X.response!` remaining-admin; 1 `prefer-const` cleanup — wrapper never relaxed.** NOT committed (branch = trunk → commit = prod deploy); per-domain staging lists in the plan Build Logs (see [plan](docs/projects/archive/OBSERVABILITY_ROUTE_INSTRUMENTATION_PLAN.md) and [PM brief](docs/projects/archive/OBSERVABILITY_ROUTE_INSTRUMENTATION_PM_BRIEF.md))
- [x] **Platform Admin Employee UX Evaluation** — full employee-perspective review of `/platform-admin/` (10 support scenarios, nav/IA, labelling, cross-links, help gaps); biggest find = comp/override system records intent but does **not** enforce expiry or auto-revert, so "comp free until a date" and "trial League on top of Tournament Plus then auto-revert" are unsupported (feature work). Prioritised fix list (H/M/L, tagged copy/reorg/feature). **H1–H7 shipped 2026-06-04** (transfer-ownership SOP + fixed stale delete-user guidance, Customer-Users actions SOP, "Coaches Portal Ownership Transfers" rename, org Members→Customer Users cross-link, dashboard alert drill-throughs, Support-tab Account Ownership transfer entry, Customer Users excludes FieldLogicHQ staff, **H4 role-based access: hidden/read/write matrix in `lib/platform-areas.ts` + `requirePlatformAreaView()` page guards + role-aware nav + in-context "requires X access" messaging**); **remaining: H8 timed-comps/trials feature (own project — needs migration + entitlement-layer change)**. ⚠️ H4 added a new lib file + changed shared `lib/platform-auth.ts` → dev server restart required before browser testing (see [eval](docs/projects/archive/PLATFORM_ADMIN_UX_EVAL.md) and [PM brief](docs/projects/archive/PLATFORM_ADMIN_UX_EVAL_PM_BRIEF.md))
- [ ] **Plan / Tier Trial** (Proposed/deferred, NOT scheduled; strategy 2026-06-29) — operator-granted, timed, auto-reverting WHOLE-PLAN trial (e.g. "give this club League Plus for 14 days," reverts at expiry); access-only (never touches Stripe), shown in the existing Active Overrides surface. Builds out the long-deferred `plan_tier` slice of the Timed Entitlements engine — needs an *effective plan rank* threaded through plan-feature gating (trial tier outranks base for the window, stored plan unchanged); reuses the existing timed-grant engine, no parallel mechanism. Plan + brief only; surfaced during platform-admin QA when relabelling "Module Trial" → "Feature Access Trial" (see docs/projects/active/PLAN_TIER_TRIAL_PLAN.md + PM brief)
- [ ] **Timed Entitlement Grants (Comps & Trials)** — H8 from the platform-admin eval: grants that turn access on for a window and **auto-revert at expiry**. Scenario B (trial a tier/add-on on a paid base — access-only, request-time, no cron/Stripe) ships first; Scenario A (free comp with billing pause/resume via Stripe) second. **/dba + /billing reviews done 2026-06-04:** extend `org_overrides` (NOT a new table) via additive ALTER; PostgREST embed on the hot path (no denormalized flag); request-time enforcement in the entitlement layer (`lib/module-entitlements.ts` + the 4 org loaders); tier trials must raise an **effective plan rank** (two gating systems) without mutating stored `plan_id`; Stripe is already built (only Phase G remains) so A1 (no sub → founding-style grant) + Scenario B are buildable now, A2 uses Stripe `pause_collection`. **Scenario B + A1 slice BUILT 2026-06-04 (lint+typecheck clean, behind `ENTITLEMENT_GRANTS_ENABLED` flag, default OFF):** migration 109 (org_overrides ALTER), `lib/entitlement-grants.ts` (computeEffectiveEntitlements + applyEntitlementGrants), wired into the 4 loaders, overrides API + OrgDetailClient "Module Trial (timed)" form + countdown. ⚠️ **Before testing: apply migration 109 to dev + `node scripts/refresh-db-schema.mjs` + restart dev server (new file + shared-module changes), then set the flag true.** Deferred: plan_tier (effective rank), A2 Stripe pause, embed optimization, tests (see [plan](docs/projects/active/TIMED_ENTITLEMENTS_PLAN.md) and [PM brief](docs/projects/active/TIMED_ENTITLEMENTS_PM_BRIEF.md))
- [x] **Public Tournament "Wow" Upgrade** — make the best-selling public page premium and live: real-time game-day refresh (schedule/standings/home/game-detail auto-update + score-flip), personalized "My Tournament" card with countdown + one-tap calendar add, visual polish pass, **Plus-gated anonymous fan score-alert push**, and a **per-tournament branded PWA** (installed app opens to that tournament; org logo/name). Also: a **neutral member PWA** (start_url=/home; install prompts in authed shells + post-signup nudge, off marketing root) and the **alerts toggle on schedule + standings**. Built 2026-06-02; migration 107 applied + VAPID set (dev+prod); browser verified 2026-06-03. Remaining: Phase 3 "game starting soon" reminders, iOS branded apple-touch-icon (see [plan](docs/projects/archive/PUBLIC_TOURNAMENT_WOW_PLAN.md) and [PM brief](docs/projects/archive/PUBLIC_TOURNAMENT_WOW_PM_BRIEF.md))
- [x] **Public Visual Redesign** — deeper premium/broadcast-grade visual pass, mobile-first + desktop: **Phase A foundation BUILT 2026-06-02 + Phase B signature moments (items 2–4) BUILT 2026-06-03 (browser verified 2026-06-03)**. Phase A: tabular numerals (`.score`/`.tabular` + per-module classes), `--highlight-top` card depth, `--blur-bar` frosted chrome + `@supports` fallback (top nav, mobile bottom navs), tap/press states, sliding-pill segmented controls (schedule stage/bracket, mobile stage, standings view toggle), staggered entrance (score-flash moved to row `::before` overlay so no re-fire on live poll), `--on-primary` contrast guardrail (lib/themes.ts luminance pick, all theming paths), global reduced-motion. Phase B: **My-Team game-day dock** (`MyTeamDock.tsx`, mounted in tournament layout, mobile-only, self-gates to followers on game day, frosted, tap-to-expand, live score/countdown), **broadcast LIVE scorecard + rolling-digit odometer** (`RollingNumber.tsx`; broadcast reserved for LIVE games only, odometer on marquee scores incl. scorebug/rail/dock), **standings diverging RD bar (desktop) + column legend** (REC/W/L/T/RF/RA/RD/PTS; an L5 form-pips column was built then removed per feedback — round-robin teams play ≤5 games so "last 5" duplicates the record). **Phase C depth/identity BUILT 2026-06-03**: broadcast home hero (`CountUp.tsx` + `Countdown.tsx` → count-up stats + pre-event "first pitch in…" ticker in plain words + org-colour duotone banner wash), team-profile theming (centralized `lib/team-color.ts`; whole profile themed by team hue via `--team-color`/`--team-accent`), bracket polish (`PublicBracketView` champion spotlight + mobile swipe carousel), illustrated empty states (`PublicTournamentState` — fixed `var(--lime)`/dark-bg ghost-token bugs) + layout-matched schedule skeleton. **Deferred bits FINISHED 2026-06-03**: sticky frosted schedule date headers + done/total progress chip (`.dateGroup` overflow removed; rows clipped by `.gamesList`), animated standings rank-change (▲/▼ + cell flash on the team CELL not the `<tr>`), champion spotlight on the SVG `LogicSyncBracket`. Per-screen standings/teams skeletons NOT built — those pages are SSR'd with initialData (no client loading state) so a skeleton would be dead code. **Also finished (as-we-go fixes):** registration CTAs lifecycle+capacity aware (`lib/registration-state.ts` → nav "Register"/"Join Waitlist" hidden on live/full/complete), season picker removed (pending IA spec), nav centering (equal-flex sides), hero badge cleanup + nav-clearance fix. **Phase D started 2026-06-03**: **share-score cards BUILT** — primary share is a **LINK** (drives traffic) backed by a **dynamic OG image** (`schedule/[gameId]/opengraph-image.tsx`, Next `ImageResponse`/Satori, 1200×630, org-coloured, renders server-side at request time; verified 200 PNG) + per-game `generateMetadata` (og:title/twitter summary_large_image); `lib/share-card.ts` canvas 1080² PNG is the secondary **"Save image"**; `ShareScoreButton` (compact button → popover with Share link / Share image) on the game-detail rail + My-Team dock (followed team's latest result, live OR final). Also fixed NaNm dock countdown (DB `time` is HH:MM:SS → `.slice(0,5)`). **Share expanding to 4 surfaces** (schedule rows, champion/podium, team profile, tournament home): **tournament home + team profile SHIPPED 2026-06-03** — new `SharePageButton` (link-only) + a **per-route OG-image hierarchy** (`[tournamentSlug]/opengraph-image.tsx` event card = default for all tournament pages, `teams/[id]/opengraph-image.tsx` team card, `schedule/[gameId]/opengraph-image.tsx` score card) + tournament-layout og metadata; all verified 200 PNG. **Champion/podium + schedule-list rows PARKED (user deferred 2026-06-03 — home/team/game share deemed sufficient; schedule-rows would need a portal popover for `.gamesList` clipping).** **Desktop left-rail app shell BUILT 2026-06-03** (`components/public/TournamentSideRail.tsx`): desktop-only (≥1024px) FULL-HEIGHT vertical nav rail (z-index above top nav) w/ branded header (logo+name) + Overview/sections (active accent) + footer; content wrapper `.shell` `padding-left:248px`; top-bar `.links` + duplicate `.logo` hidden ≥1024px. **Desktop top-bar context header BUILT 2026-06-03** (`TournamentNavStatus.tsx`): left = phase-aware status pill + dates (Upcoming+countdown / In progress / Completed) → **live score ticker on game day** (polls `usePublicTournamentLive`); right = **Share event** button by Register; status data via new `setTournamentStatus` channel (OrgNavContext→TournamentNavSync→layout). Both desktop-only (`.navStatusSlot`/`.navShareSlot`), client-synced. Mobile/tablet 100% unchanged (CSS-gated). tsc/lint clean, renders 200; awaiting browser verification at desktop width. Remaining Phase D (pull-to-refresh, offline shell) PARKED — user chose Phase E next. **Phase E STARTED 2026-06-04**: static pass done (new code is token-clean + orphan-free + a11y-labelled; fixed the ticker's hardcoded red → `--danger-rgb` + tabular-nums; verified no leftover form-pips/detailShare/railLabel). Remaining Phase E = the **cross-matrix browser walkthrough** (every surface, mobile+desktop, dark+light, branded+default) — see the new [QA checklist](docs/projects/archive/PUBLIC_VISUAL_REDESIGN_QA_CHECKLIST.md) (Shared/cross-cutting section = fix-once-applies-everywhere) — plus **token-debt** — guardrail + inventory DONE 2026-06-04 (`scripts/check-public-tokens.mjs` ratchet wired into `npm run verify:changed` + `npm run check:tokens`; baseline `scripts/.public-token-baseline.json`; auto-generated inventory at docs/projects/archive/PUBLIC_VISUAL_REDESIGN_TOKEN_DEBT.md). **Scoped to the public redesign it's only ~22 literals in 6 files** (the 245 was admin/scorekeeper/coaches, excluded from scope); fixed the 4 unambiguous `#EF4444`→`var(--danger)`; the 21 remaining `#fff` are case-by-case in the light-mode walkthrough (some are white-on-colour that correctly stay white) + 1 custom `#000000`. Not a ship blocker (dark mode unaffected). (see [plan](docs/projects/archive/PUBLIC_VISUAL_REDESIGN_PLAN.md), [PM brief](docs/projects/archive/PUBLIC_VISUAL_REDESIGN_PM_BRIEF.md), [QA checklist](docs/projects/archive/PUBLIC_VISUAL_REDESIGN_QA_CHECKLIST.md))
- [ ] **Public IA — "Season"/edition model rethink** (raised 2026-06-03; **picker REMOVED for now**, needs spec before re-adding) — the public `YearSelector` ("Season:") listed an org's tournaments by **bare year** and only showed when >1 existed; it broke when two tournaments shared a year (two identical "2026" tabs, both with a live dot), and the underlying question is whether standalone tournaments should have a "season" concept at all. **Removed 2026-06-03** via `SEASON_PICKER_ENABLED = false` in `components/YearSelector.tsx` (component logic preserved; flip the flag to restore). To bring it back the spec must define: (1) **what it represents** — a recurring-tournament/"edition" model vs. just "all the org's tournaments"; (2) **label & disambiguation** — show tournament name (not bare year), and how to handle multiple same-year or same-name editions; (3) **when to show it** — only for orgs/tournaments that are genuinely part of a series, not every multi-tournament org; (4) **where cross-edition navigation lives** — inline picker vs. a dedicated archive/all-tournaments page; (5) **data model** — whether tournaments need an explicit `series_id`/`edition` link rather than inferring from `year`. User chose "rethink more broadly" — spec the season/edition/archive IA first; do NOT just re-enable the bare-year tabs. (Active-pill colour was already de-gradiented — that part is settled and independent of the IA decision.)
- [x] **Tournament Admin Visual Redesign** ✅ CLOSED 2026-06-06 — premium "wow" / broadcast-grade pass on the admin organizers use to run events (`app/[orgSlug]/admin/tournaments/*`), mobile-first + desktop, layered on top of the functional mobile floor (codex_TOURNAMENT_OWNER_MOBILE) and per-page cleanup (agent_TOURNAMENT_DESIGN_REVIEW): Phase 0 principle update (mobile admin = first-class operating mode), Phase A foundation (comfortable/compact density, tabular nums, reduced-motion guard, press states, unified frosted chrome + bottom-sheet primitive, mobile-tuned grid, skeletons), Phase B game-day signature moments (mobile top app-bar + on-mobile notification bell, lifecycle-aware dashboard command hero, in-progress/next/overdue game rows, live shell presence, nav worklist counts, thumb score entry, count-up), Phase C builder/depth (mobile bracket round-carousel, desktop bracket connectors + champion spotlight, sticky schedule-health, conflict viz, registration/division card modes + capacity gauges, team avatars, attention-UI consolidation), Phase D bigger bets (game-day check-in, venue×time visual schedule timeline, optimistic/offline scoring, on-site payment, pull-to-refresh), **Phase E (project close-out): full page-by-page design QA across all admin pages** (token/class compliance, status=label vs action=button, status colour = row colour; fold into agent_TOURNAMENT_DESIGN_REVIEW). **Phase 0 principle update + Phase A foundation BUILT 2026-06-03 (tsc clean, dev-server restarted, smoke 200; browser verified 2026-06-03)** — admin `[data-density="comfortable"|"compact"]` tokens + toggle (sidebar footer + bottom-nav More) with no-flash script, shared rows/toolbar controls consume density tokens, frosted chrome via `--blur-bar`, `--highlight-top` panel/stat-card depth + press states, quieter mobile blueprint grid, mobile NotificationBell (was desktop-sidebar-only), reduced-motion already global from public Phase A; `BottomSheet` + `AdminSkeleton` primitives built (sheet migration + skeleton page-wiring deferred as follow-ups). **Phase B COMPLETE — B1–B8 BUILT 2026-06-03 (tsc clean, smoke 200; browser verified 2026-06-03)**: B1 mobile top app-bar (name + phase pill + switcher + bell, replacing the floating bell) + mobile notification-panel position fix; B2 lifecycle dashboard — `lib/tournament-phase.ts` + API-derived `isGameDay` (within event dates OR first game started) now drives the game-day board vs registration board, stat-card suppression, and the "Live" status label (existing boards, switched on the right signal — no new heroes); B4 phase-aware top-bar pill DRAFT→OPEN→LIVE→COMPLETED; B3 live game-row states (`GameList`, both Schedule + Results — Now/Overdue/Next left-stripes + mono chips on un-scored games only, expected-end via `resolveGameTiming`, per-minute tick, reduced-motion-safe); B5 nav worklist counts (`lib/admin-worklist.tsx` context + `/api/admin/tournament-worklist` endpoint → amber count badges on sidebar nav items + bottom-nav tabs: **Teams** = pending registrations, **Results** = games to finalize; refresh on tournament-switch / 90s / focus; counts self-adjust by phase; overdue-unscored deferred); B6 context-aware bottom-nav action strip (`AdminContextStrip` — docked above the bottom nav, phase/count-driven action [finalize > review > setup/summary], dismissible × with smart re-show when the action changes or count goes up [localStorage per-tournament], reserves content space via `--admin-strip-h`); B7 thumb score steppers (`GameList` scoring edit — mobile-only − / + buttons around each score input, clamp at 0; mobile stacks the two teams into full-width rows with big steppers; desktop unchanged); B8 count-up (`components/admin/CountUp.tsx` — counts 0→N on first data load + re-animates on poll-driven changes, poll-stable, reduced-motion snaps; on dashboard stat-card numbers + registration/game-day gauge mains). **Phase B COMPLETE (B1–B8).** **CLOSED 2026-06-06** — Phase E QA signed off (shared chrome incl. density-toggle-removed, Teams/Registrations, Check-in, Dashboard Active/Live, Settings cluster + Org Members). Dashboard-Completed + Post-Event Summary spun out to their own IA project (TODO item below); Schedule (playoff work in flight) + the remaining content pages deferred to ad-hoc `/design` review as issues surface; Org Settings out of scope. Standing conventions locked: btn-lime/ghost/danger/data only (no btn-primary outside modals, no btn-sm), CollapsibleCard primitive, status=label vs action=button. Archived: docs/projects/archive/ADMIN_VISUAL_REDESIGN_{PLAN,PHASE_A,PM_BRIEF,QA_CHECKLIST}.md
- [ ] **Dashboard "Completed" + Post-Event Summary IA** — Phase E QA: killed the duplicated post-event recap across the completed Dashboard and the Plus-gated Summary. **BUILT 2026-06-07 (tsc clean, lint 0 errors, awaiting browser verification).** Summary is now the **single canonical recap** in three ranked zones (Recap → Share the results → **What's next**, the last collapsed via `CollapsibleCard`); completed Dashboard is a **plan-aware hand-off** (Plus = thin pointer → Summary; Free = keeps recap + one compact upsell, since Summary is locked). **Champions now shown on the Dashboard banner** (all plans, never paywalled) via a new `champions` field on `/api/admin/tournament-dashboard` (computed from already-fetched data, no extra query). **Renewal reframed**: dropped "Keep Plus active" (auto-renew) → "What's next" = Reuse this setup + visible value-reflection line + one opt-in League/Club discovery line → `/pricing` (marketing-led, forbidden-words clean). Killed Summary's 2nd hero; `btn-sm`→`btn-data` sweep. No migration; no dev restart needed. Verify Free vs Plus on `/dev-test-org/completed-demo` (champion: Bears U11) (see [plan](docs/projects/active/DASHBOARD_SUMMARY_IA_PLAN.md) and [PM brief](docs/projects/active/DASHBOARD_SUMMARY_IA_PM_BRIEF.md))
- [x] **Schedule Timeline (venue × time)** — SPEC'D + decisions locked 2026-06-04 (D2 of the admin redesign): a third schedule view next to List/Bracket — a day grid (facilities × time) with drag-to-move write-back (via `handleSaveGame`), conflict/gap shading (reuses C5 `buildConflictMap`), a TBD tray, mobile single-facility swipe carousel. **Two division modes**: All-divisions (picker as highlight) + Single-division (only that division's games + only its facilities) with cross-division conflict alerts + faded ghost blocks always on. Decisions locked: Plus-gated, warn-and-allow on clash, derived time-of-day axis, 15-min snap, tap-to-move on mobile. Deferred: a date-range filter (default whole tournament, narrowable; maybe shared across views). Phased D2.1 read-only grid → D2.2 drag → D2.3 mobile → D2.4 polish. **D2 COMPLETE — all phases BUILT 2026-06-04 (tsc + lint clean; awaiting browser verification)**: D2.1 cross-division grid; D2.2 drag-to-move + D2.2b unscheduled tray / "+ Field" add-column / drag-to-place + D2.2c full venue-occupancy "busy" ghosts (Outlook-style, see all bookings before dropping) + D2.2d severity-correct conflicts (overlap=red, buffer=amber) with travel-buffer tooltips + legend; D2.3 mobile single-field pager (swipe between fields, tap-to-edit `RescheduleSheet` on `BottomSheet` with field picker + 15-min stepper + live conflict + "next free slot"); D2.4 live drag drop-preview band (green/amber/red) + game-day "now" line + keyboard a11y (Enter opens the sheet). Plus IA consolidation (stage→view: round-robin = List/Timeline, playoffs = List/Bracket/Timeline) + divisions-only multi-select scope (pools colour-coded on cards). Optional follow-ups only: per-field done/total chip, remember-last-field, migrate legacy schedule sheets onto `BottomSheet` (see [plan](docs/projects/archive/SCHEDULE_TIMELINE_PLAN.md) and [PM brief](docs/projects/archive/SCHEDULE_TIMELINE_PM_BRIEF.md))
- [x] **Gate / Team Check-In + Coach Roster** — Phase D bet of the admin redesign: game-day team check-in (Present / No-show, pay-at-gate, roster confirm) + a gate-volunteer view. Migration 110 adds `teams` check-in columns + a new `tournament_roster_players` table (replaces the vestigial `teams.players` jsonb the DB review flagged). **Phases 1, 2, 4, 5 BUILT 2026-06-04 (tsc + lint clean; awaiting browser verification)**: (1) foundation — migration applied dev + types + `scripts/apply-migration-api.mjs`; (2) admin Check-in board (`app/[orgSlug]/admin/tournaments/check-in/`, shared `components/admin/CheckInBoard.tsx`, `/api/admin/check-in`) — gauges, filters, per-team arrival/payment/roster, tap-sheet to check in / no-show / mark paid / confirm-or-enter roster; (4) gate-volunteer surface (`app/[orgSlug]/check-in/`, new `check_in_teams` capability, `proxy.ts` guard, tournament picker, reuses the board); (5) dashboard "Team Check-in" arrived/total panel on the game-day board. **Finishing touches BUILT 2026-06-04**: no-show teams flagged on the admin schedule (`GameList`), `team_no_show` bell notification on no-show, and migration 111 dropped the vestigial `teams.players` jsonb (removed `Player[]` from `Team` + all writers). **Phase 3 (coach roster submission) MOVED** → the Coaches Experience project below (done at the end). Remaining: browser verification; optional waivers/per-day/Stripe-at-gate deferred (see [plan](docs/projects/archive/GATE_CHECKIN_PLAN.md) and [PM brief](docs/projects/archive/GATE_CHECKIN_PM_BRIEF.md))
- [ ] **Coaches Experience — End-to-End + "Wow" Pass** — **Phase 0 audit COMPLETE + 5-phase plan WRITTEN; Phase A BUILT 2026-06-05 (tsc+lint clean, dev restarted/200s, awaiting browser verification)**. Phase A shipped: new `app/coaches/layout.tsx` + `components/coaches/CoachPortalShell` (desktop rail + mobile top bar/bottom nav + multi-team switcher), `isCoachPortalShellPath()` suppressing marketing `Navbar`/`Footer` on portal routes, ghost-token fix + legacy `/my/` CSS deleted (colocated coach CSS modules), and Basic/Premium framing removed → "Your Coaches Portal" / "Take your team further". Next: Phase B (payment + check-in visibility). Full read-only walk of the tournament coach journey (register → portal → emails → prep → game day → afterglow) with three lenses (gap / wow / growth) + `/design` and `/marketing` synthesis. Key finds: the coach portal (`app/coaches/*`) missed both redesign waves — **no `app/coaches/layout.tsx` shell** (pages import legacy `/my/` CSS), **ghost tokens** in `coaches-portal.module.css` (`--text-2/3`, `--border-1`, `--surface-3` undefined → broken styling), **payment/check-in status invisible to the coach**, the **premium public game-day experience is unbridged** from the portal, and a **brand violation** ("Basic vs Premium" tiers vs. the "just your Coaches Portal" rule). Locked sequencing = Foundation first, **A→E**, full phase-adaptive Team HQ: **A** shell/correctness/de-brand · **B** payment+check-in visibility · **C** coach roster submission (the moved Gate Phase 3 — `tournament_roster_players` ready) · **D** phase-adaptive Team HQ hero + game-day bridge · **E** afterglow (placement/share + the two-bridge growth ask). Reuses public primitives (team-color, Countdown, RollingNumber, SharePageButton, usePublicTournamentLive). Pressure ladder: pre-event surfaces stay pitch-free; afterglow is the one earned ask. **Architecture decisions locked 2026-06-05:** (1) roster = persistent master `basic_coach_team_players` (new mig. 112) + per-event snapshot into `tournament_roster_players`, upgrade-ready via `team_workspace_id`; (2) multi-team-first shell + Team HQ (team switcher); (3) assistant-coach delegation deferred but schema designed for it (`basic_coach_team_users.role`), minor-DOB privacy stance required first (see [plan](docs/projects/active/COACHES_EXPERIENCE_EVAL_PLAN.md) and [PM brief](docs/projects/active/COACHES_EXPERIENCE_EVAL_PM_BRIEF.md)); **▶ EXECUTE FROM the unified plan — [FREE_TIER_COACHES_UNIFIED_PLAN.md](docs/projects/active/FREE_TIER_COACHES_UNIFIED_PLAN.md)** (A = unified Phase 1, C-master = Phase 3, B/D/E = Phase 5)
- [ ] **Free Tier Strategy — Per-Operator Free Floors, One Account** — SCOPED 2026-06-07 (planning; not built). Direction = **Option E end-state** (per-operator free-forever floors funded by payment processing + a sample-only trial), reached via **Option C steps**; do NOT announce the hybrid label until the processing line is live (a tournament-only free tier sits below the Canadian floor — RAMP/TeamLinkt/Spond give free core). **Model = "separate scoped floors, one account"** (NOT one mega-subscription unlocking all modules; NOT four parallel subscriptions): the subscription unit is the **ORG**; free = three floors scoped to their natural unit, unified via `/home` — **Free Tournament** (org on `tournament` plan, $0/no-Stripe, mig 050), **Free League Starter** (org, capped house-league free-grade: 1 season/~8 teams/1 division, no public site/exports — new SKU), **Free Basic Coaches Portal** (`basic_coach_teams`, email-keyed/no-org/not-a-sub — already free for tournament participants, **+NEW standalone on-ramp** to create a team with no tournament; mig 092 had removed the email-fallback, this is a deliberate new entry). Club paid-only (its free experience = League Starter + its coaches' Basic portals); tournament core is universal. **Decisions:** standalone Basic on-ramp APPROVED; flipping `team` (paid Premium standalone checkout) live DEFERRED until the team module is fully tested (the FREE Basic floor doesn't need that flip — `team`=Premium, not Basic). **Hard deps:** persona-routing at signup (keystone — today signup always creates a tournament org), existing-user multi-floor flow (`/create-org` + start-a-Basic-team; `/auth/signup` rejects existing emails today), payment-processing line ≤~2% + Stripe Phase G — **payment processing DEFERRED 2026-06-07 to a separate future enhancement** (free-tier model ships with NO payment processing, manual fee tracking only; facilitated payments / Stripe Connect / "Flow B" = future project w/ its own planning discussion; subscription go-live / Stripe Phase G / "Flow A" separate, bounded by the Jan-2027 founding conversion), Coaches Experience A–E (master roster = the coach-floor substance), pressure-ladder (in-portal pitch-free; "free to operate" pillar is public-site only); Activation Trial rides Timed Entitlement Grants (deferred `plan_tier` grant + founding-comp coexistence). **Codex review folded in 2026-06-08:** +D6 server-side caps, account-first /start, League-Starter-as-entitlement-profile (not new OrgPlan key), two-rail split (Activation Trial OUT of MVP → Club consultative-only), instrumentation + abuse controls; opt-C held (schedule+comms stay free); narrow public league page in (see [plan](docs/projects/active/FREE_TIER_STRATEGY_PLAN.md) and [PM brief](docs/projects/active/FREE_TIER_STRATEGY_PM_BRIEF.md)); **▶ EXECUTE FROM the unified plan — [FREE_TIER_COACHES_UNIFIED_PLAN.md](docs/projects/active/FREE_TIER_COACHES_UNIFIED_PLAN.md)** (merges this with Coaches Experience; one ordered phase list 0–9 + Future Rail). **Phase 0 + Phase 1 BUILT 2026-06-08 (branch `feat/free-tier-coaches`; tsc+lint clean; awaiting adversarial-review fold-in + browser verification):** Phase 0 trust/copy — removed TP "14-day trial" from `PricingSection` (League/Club trial copy was already removed in-tree), README free-floor note, Timed-Entitlements PM-brief status, `PRICING_PAGE_COPY` superseded-trial banner, killed "Premium-as-free" coach copy → honest Premium-only. Phase 1 coach foundation — NEW org-less team home `/coaches/team/[basicTeamId]` (identity-resolved, distinct from Premium `/coaches/teams`) + `getBasicCoachTeamForUser` resolver + `getUserAccessContexts` smart destination (bare-team coach → team home, not the empty tournament archive) + `/coaches` hub per-team cards + shell switcher retarget + Phase-A remediation (ghost tokens → `--text-secondary/--text-tertiary`, registration-presupposing copy, `btn-primary`→`btn-outline`). **Phase 2 BUILT 2026-06-08 (tsc+lint+token-guardrail clean; dev smoke 200 + correct redirects; awaiting browser verification):** account-first **`/start`** front door (picker → `/start/tournament` [signed-out=existing signup, signed-in=`AddOrgForm`→new `POST /api/org/create`], `/start/team` [new standalone Basic team via new `POST /api/coaches/teams` + exported `createBasicCoachTeam`, `source='coach_created'`], `/start/{league,club}` express-interest wrappers); **existing-user add-workspace separated from auth** (both create endpoints run on the live session — existing emails never error); **`/home`** zero-context→`/start`, single-context no longer auto-redirected (renders switcher) + "Start something new" card; base-URL login→`/home` / deep links skip it (`lib/auth-destination.ts`); **"All workspaces"→`/home`** link in admin sidebar + coaches shell; **coach name parity** — `coach-signup` now requires First/Last + writes the 4 `user_metadata` fields, `/coaches/join` + `/start/team` collect them, `/coaches/join` pre-fills from a `?coach=` passthrough on the register→join redirect (public `teams.coach` untouched). League Starter floor/caps = Phase 6; Premium `team` flip still deferred. **Signup-slug refinement (2026-06-08):** removed the Public URL field from signup + add-org form — `generateUniqueOrgSlug()` auto-assigns it (preview only); editable later as workspace IDENTITY (refined tournament/org-separation rule: modules hidden, identity per-workspace) — Tournament tiers in Event Settings → "Organization Address", League/Club in Org Settings; `/api/admin/org-settings` now `?orgSlug=`-aware (multi-org-safe). **Register-form registrant model — Part 1 BUILT 2026-06-08 (REVERSES "don't split Coach/Contact Name"):** public register form now captures the REGISTRANT (First/Last/Email = account user, prefilled+locked for logged-in coaches) not a free contact; the team's head coach defaults to the registrant and is **assigned separately in the portal — Part 2 → Phase 5**; `teams.coach` storage unchanged (combined name, no migration); `/api/coaches/basic-teams` GET returns account name for prefill. **Registration ⇄ account MERGED 2026-06-08:** registering a team now creates the coach's portal account inline (logged-out adds a password; logged-in adds nothing) → signs in → `/api/register` auto-links → "Open Coaches Portal"; returning email = password treated as sign-in; `/coaches/join` kept as email-link/claim fallback. **Email verification committed → Phase 8 as a UNIFIED platform-wide posture** (org + coach + merged register verify the same way; coach-signup currently `email_confirm:true`/no-verify — close that gap). **Phase 3 (Coach master roster) BUILT 2026-06-09 (tsc + lint + token + dictionary ratchets clean; awaiting browser verification):** new persistent master roster table **`basic_coach_team_players`** (**migration 114**, applied dev-only — identity only: name / jersey# (text) / optional guardian contact / optional consent-gated DOB / note / display_order; NO attendance/lineups/positions = Premium; column shape snapshot-compatible with `tournament_roster_players` for the Phase-5 per-event submit; upgrade-ready via `team_workspace_id`). Ownership-gated CRUD via new `lib/basic-coach-roster.ts` + shared `lib/coach-team-guard.ts` (`requireBasicCoachTeamOwner` = signed-in + platform-admin-reject + `userOwnsBasicCoachTeam`, every mutation also `.eq(basic_coach_team_id)`-scoped against IDOR) behind `/api/coaches/teams/[basicTeamId]/roster` (GET/POST) · `…/[playerId]` (PATCH/DELETE) · `…/reorder` (POST). Mobile-first `RosterEditor` client island (add/edit/remove + @dnd-kit drag-reorder, optimistic + revert) on the org-less team home — leads the page, replaces the "Master roster" coming-soon tile. **DOB posture (owner-approved):** optional, never default-shown (explicit "+ Add date of birth" opt-in), save blocked until a guardian-consent checkbox is ticked (UI-enforced; persisted consent audit → Phase 8). Optional parent/guardian contact included now (powers Phase 4 comms). `DATA_DICTIONARY.md` updated (`basic_coach_team_players` documented + sealed → 26 tables). **Migration-safety guardrails BUILT 2026-06-09** (release never auto-applies migrations — Amplify build-only, /release git-push-only): `apply-migration-api.mjs` now `--dev`(default)/`--prod`-aware (one PAT reaches both; prints a PRODUCTION banner) + new `scripts/check-prod-migration-drift.mjs` (`npm run check:migrations`, wired into the /release agent as a master/promote gate — exits 1 when prod is missing tables/columns dev has). **Phase 4 (standalone floor) IN PROGRESS — Slice 4a (team schedule) BUILT + BROWSER-VERIFIED 2026-06-09 (tsc + lint + token + dictionary ratchets clean; snapshots refreshed; dev smoke 200/401):** new table **`basic_coach_team_events`** (**migration 115**, applied dev-only — type practice|game|event / title / free-text opponent (game-only) / location / start+end / note / scheduled|cancelled status; NO scores/attendance/lineups/recurrence = Premium rep_team_events; CHECK `…_time_check` blocks end-before-start; documented + sealed → 27 tables). Ownership-gated CRUD via new `lib/basic-coach-schedule.ts` (`requireBasicCoachTeamOwner` + every mutation `.eq(basic_coach_team_id)`-scoped; opponent server-cleared on non-game in create AND update) behind `/api/coaches/teams/[basicTeamId]/events` (GET/POST) · `…/[eventId]` (PATCH/DELETE). Mobile-first `ScheduleEditor` client island (add/edit/remove, soonest-first, segmented type, optional end/location/opponent/note panel) on the org-less team home, between Roster and Tournament history. Adversarial review (1 focused verifier) found + fixed 2 MEDIUM (end-before-start PATCH bypass → DB CHECK; stale opponent on game→practice via direct PATCH → lib clear) + 1 LOW (multi-day end-date display). **Post-verify UX refinement:** end time promoted out of the optional panel + auto-defaults to start+2h (tracks the start until hand-edited) + picker `min` blocks earlier-than-start selection (the save-time error is now a backstop). Next slices: 4b fee ledger (`basic_coach_team_fees`, mig 116) · 4c comms (`basic_coach_team_announcements`, mig 117) · 4d Team HQ stat strip + scope-ceiling express-interest + standalone welcome email
  - [x] **Free Tier Phase 4 Slice 4b - manual fee ledger** - built locally on `feat/free-tier-coaches` 2026-06-09 and browser-verified. Adds dev-only migration 116 (`basic_coach_team_fees`), owner-gated fee CRUD, roster-centric `FeeEditor`, dictionary/snapshot coverage, and keeps all money handling manual only (no Stripe/payment processing).
  - [x] **Free Tier Phase 4 Slice 4c - basic team comms** - built locally on `feat/free-tier-coaches` 2026-06-09 and browser-verified. Adds dev-only migration 117 (`basic_coach_team_announcements`), owner-gated announcement send/log route, roster-contact email dedupe via existing `sendEmail`, `AnnouncementEditor`, and dictionary/snapshot coverage. One-way email only: no parent accounts, chat, SMS/push, reminders, or dues automation.
  - [x] **Free Tier Phase 4 Slice 4d - standalone Team HQ / scope ceiling** - built locally on `feat/free-tier-coaches` 2026-06-09 and **browser-verified 2026-06-09**. Adds the standalone Team HQ stat strip, owner-gated scope-ceiling express-interest capture into existing `early_access_leads`, admin-facing interest labels, and a best-effort Basic team welcome email. No migration, checkout, Stripe, Premium unlock, or entitlement mutation. **Phase 4 (standalone free coach floor) COMPLETE.**
  - [ ] **Free Tier Phase 5 - Tournament coach experience** — SCOPED 2026-06-09 via a 10-agent workflow (8 surface maps → architect synthesis → adversarial critic); decisions LOCKED 2026-06-09; building slice-by-slice with browser verification. 15 slices on `feat/free-tier-coaches`: **5·0** remove pre-event Premium CTAs (pressure-ladder, pulled early) · **5a** shared status+fee-resolver data foundation · **5b** read-only payment/check-in/roster status block (B) · **5c** claim-by-email discovery + registrationId-in-join-links (the empty-portal GAP) · **5d** seed `teams.email` on admin add/import + claim email · **5e** shared coach-email portal footer + acceptance/rejection email polish · **5f** organizer roster requirements in tournament settings JSONB (Event Settings, no migration) · **5g** extract shared `TeamHQ` shell (standalone mode, zero regression) · **5h** phase-adaptive tournament hero (Pending/Accepted-Prep) · **5i** game-day bridge (opponent names, public game links, live scorebug) · **5j** C-snapshot submit API (master→`tournament_roster_players`) · **5k** select-from-master submit UI + requirements wiring (the master/snapshot SEAM) · **5l** head-coach + contact assignment (Part 2) · **5m** afterglow earned-ask (express-interest) + game-day reminder email · **5n** organizer automatic-email on/off controls + master "pause all automatic emails" toggle (Event Settings, settings-JSONB, owner-requested) · **5o** REVIEW (closing item, owner-requested): options for a "Coaches Portal" nav link on the public pages, reconciled with the Phase 8 post-register reachability note. **✅ Deploy gate CLEARED 2026-06-11:** migrations 114–117 + 123 + 124 applied to prod (owner-confirmed); re-run `check:migrations` at promote to re-confirm. Decisions LOCKED: ADD `source_player_id`; NEW `teams.coach_email` column; EXPLICIT claim; Resend `scheduled_at`; 5n email controls ORGANIZER-side. **5·0 + 5a + 5b (detail status block) + 5c (claim-by-email) + 5e (coach-email footer) + 5d (opt-in claim email) BUILT** (typecheck + focused lint clean; awaiting browser verification + dev restart). 5a/5b: register page untouched (reuses canonical `getRegistrationAttentionFee`/`computeRegistrationAttentionPaymentStatus`); list/team-home chips deferred; review fixes (numeric-as-string $0 fee, gross-vs-remaining balance, timestamp TZ). 5c: discovery + hub claim prompt + routing (`buildClaimableContext`, gated to zero-context users) + `registrationId` on email links; reuses the existing `/coaches/join` claim screen; review fixes (race-safe link w/ 409 "already claimed" + orphan rollback, platform-admin guard + staff-excluded discovery, rejected excluded). 5e: `coachPortalFooter` claim-aware link on payment-confirmation/waitlist/schedule-published/payment-reminder + claim-aware acceptance button + rejection soft bridge + payment-confirmation voice fix; wired `registrationId`+`coachEmail` across all 6 send sites (footer kept out of shared `wrap()`); review fixes (`htmlToText` decodes `&rarr;`; `/coaches/join` auto-redirects already-linked coaches). 5d: `manualTeamRegistrationHtml` claim-aware + Add-Teams staff-email guard + bulk-importer **opt-in** "email coaches a portal link" (default off, best-effort post-commit, skips no-email/staff rows); coach email stays OPTIONAL everywhere; review fix (toggle resets per preview). **DEFERRED:** 5e computed fee+due line in acceptance email; broader email-escaping hardening pass (team/coach names raw across templates, pre-existing); importer staff-email storage (non-exploitable, 5c excludes staff). 5f BUILT + browser-verified 2026-06-10 + owner follow-up same day (typecheck+lint+dictionary clean): 7 default-OFF `roster_*` keys on `TournamentSettings` (⚠ opposite polarity from `coach_email_*`) + "Roster Requirements" CollapsibleCard in Event Settings (master On/Off → "Player names · Always" row + DOB/jersey/waiver sub-toggles + waiver-statement textarea [`roster_waiver_text`, ''=`DEFAULT_ROSTER_WAIVER_TEXT` in new `lib/roster-requirements.ts`] + min/max 1–99 + live "What the Coach Will See" preview) + explicit per-key patch-settings sanitizers before the fall-through + dictionary catalog updated; two reviews folded in (min>max IS storable → locked reader rule **min>max = no-minimum (max wins)**; waiver text trimmed at write so ''-=default holds; both documented in types/dictionary/5k spec; LOW accepted: page-open stamps the keys as false/null into legacy rows — readers key on values, never presence). 5g BUILT 2026-06-10 (typecheck + focused lint clean; dev restarted + smoke `/platform-admin/login` 200, no EACCES; browser-verified 2026-06-10): lifted the Team-HQ stat strip into a shared **server** component `components/coaches/TeamHQ.tsx` (+ co-located `TeamHQ.module.css`) with discriminated-union props (only `standalone` this slice; 5h/5i add `tournament`); the standalone page consumes `<TeamHQ variant="standalone" …/>` with already-computed counts as props; hq* CSS moved byte-identical incl. all 3 breakpoints (5-col → 2-col 701–1120px → 1-col ≤700px) + removed from `team.module.css` (kept `.historyRow`/`.rosterCount`/`.breadcrumb`/`.historyList`); dead `formatMoney`/lucide-icon/`LocalDateTime` references dropped from the page. 3-lens adversarial review (CSS verbatim · render/value parity · import-path/webpack-gotcha + dead-code) **ALL PASS, zero regressions**; only finding = 1 LOW (no `switch` exhaustiveness guard — `props satisfies never` can't compile for a single-member union, so documented inline for 5h to add with the new `case`). Zero behavior change. 5h BUILT + BROWSER-VERIFIED 2026-06-10 (typecheck + focused lint clean; dev restarted + smoke 200; accepted-prep path confirmed in browser): first `tournament` variant of the shared `TeamHQ` shell — new `lib/coach-tournament-phase.ts` (`deriveCoachTournamentPhase`: pending|rejected|accepted_prep|schedule_live|game_day|result); the tournament detail page replaces the old static status card with `<TeamHQ variant="tournament" …/>` (5b status block + Registration Details/Schedule/Announcements untouched); hero = team monogram + hue wash + status pill + phase headline + live `Countdown` ("first game in N days") + read-only checklist HUD (Registered/Accepted/Fee Paid-Owed/Roster positive-state-only/Check-in game-day-only). Honest + pitch-free: NO public link/Follow/pay/roster-prompt/Premium in any phase (5i adds the public bridge, 5m the afterglow). 4-lens adversarial review = 3 PASS + 1 FAIL folded: 5g no-regression PASS (standalone strip byte-identical); **HIGH fixed** ("Event underway" was gated `game_day||schedule_live` → re-gated to `game_day` only, so a null-startDate `schedule_live` team no longer mislabels + the dead clause is gone); LOW redundant header badge dropped (hero pill owns status). 5i BUILT 2026-06-10 (typecheck + focused lint clean, 0 project-wide TS errors; dev restarted [no `.next` clear] + smoke `/platform-admin/login` 200, no EACCES; **BROWSER-VERIFIED 2026-06-11** — live scorebug rolls on a real score change with no refresh + no re-fire on unchanged; published_teams real names+monograms+deep-links+Follow; published_generic TBD opponents matching the public page): the game-day bridge as a NEW client island `components/coaches/CoachLiveSchedule.tsx` (+ co-located CSS) mounted under the hero for accepted teams with a published division — opponent names + monograms (`lib/team-color`), each row deep-links to the public game page `/{org}/{tournament}/schedule/{gameId}`, a live **broadcast scorebug** for the in-progress game (reuses `usePublicTournamentLive` 30s game-day-gated poll + `RollingNumber` [no re-fire on unchanged]), W/L/T + final scores, plus a **Follow** toggle (same `fl_follow_team_*` localStorage key as the public dock) + `InstallAppPrompt`. **D2 (owner-approved): Follow+Install+scorebug now, alerts express-interest DEFERRED to 5m (5i stays pitch-free).** Single-poll (live game = inline broadcast row); TeamHQ + the standalone strip UNTOUCHED (separate component, not a TeamHQ variant). Gating (honesty): bridge only for accepted + division `published_teams|published_generic`; links/poll/follow ALSO require a public (active|completed) tournament else static; pending/rejected → none; NULL home/away → TBD. 4-lens adversarial review + adversarial verify: **1 HIGH fixed** — `published_generic` (organizer publishes times but HIDES matchups) was leaking real opponent names + linking to a page that shows them as TBD → now mirrors the public `getTeamDisplay` (placeholder/TBD unless `published_teams` + real id ≠ NIL_UUID; added `home/away_placeholder` to the SELECT); **1 LOW fixed** (live row shows "Awaiting score", not a fake 0–0 before a score posts); 2 LOWs dismissed. **5g standalone pixel-diff re-run = PASS** (TeamHQ git-confirmed out of the 5i changeset). **5j (C-snapshot submit API — first coach-side roster WRITE) BUILT 2026-06-11** (typecheck 0-err + focused lint + `check:dictionary` clean; migration 123 `tournament_roster_players.source_player_id` [uuid, FK → `basic_coach_team_players` ON DELETE SET NULL] applied dev-only + snapshots/DRIFT refreshed [watermark #123]; dev restarted [single stack stopped, owner-precedent no-`.next`-clear] + smoke `/platform-admin/login` 200 no EACCES; awaiting browser verification): new `app/api/coaches/tournaments/[teamId]/roster/route.ts` — GET serves everything 5k needs (parsed `requirements`+resolved `waiverText`, the coach's `masterPlayers`, current `snapshot` w/ `sourcePlayerId`, `state` flags); POST copies selected master players → `tournament_roster_players` (`source='coach'`+`source_player_id`) and stamps `roster_submitted_at` ONLY. Auth = NEW `requireCoachRegistrationAccess` (the `[teamId]` is a tournament **registration** id → explicit-link ownership via the now-exported `findLinkedBasicTeamForRegistration`, returning the owned `basicCoachTeamId` — IDOR-safe; **not** `requireBasicCoachTeamOwner`). Master/snapshot **SEAM** = pure `buildTournamentRosterSnapshot` in `lib/basic-coach-roster.ts` (full override surface jersey/DOB/position/notes; `name` always from the master; enforces required DOB/jersey + min/max [locked **min>max⇒no-min**] + waiver server-side; **NEVER** mutates the master — built complete so **5k just calls it**) + shared `parseRosterRequirements`. Lock ladder mirrors the organizer gate (reject `completed`/not-`accepted`/`roster_confirmed_at`-set → 409 "contact the organizer"; re-submit freely while only `roster_submitted_at` is set; delete-then-insert scoped to `source='coach'`, preserving `source='admin'` rows). Migration 123 joins 114–117 in the Phase-5 prod deploy gate (114 before 123, FK dep). **Owner-confirmed sub-decisions (AskUserQuestion):** lock-on-confirm, FK ON DELETE SET NULL, replace `source='coach'` only. Adversarial review (ultracode — 5 lenses + adversarial verify, 7 agents): auth/seam/requirement/migration logic held; **1 fix folded** (the `roster_submitted_at` stamp wasn't error-checked like the delete+insert → a silent failure could return a false success; now destructured + `throw`). **5k (select-from-master submit UI + requirements wiring — the master/snapshot SEAM in the UI) BUILT 2026-06-11** (typecheck 0-err project-wide + focused lint clean; dev restarted [single stack, owner-precedent no-`.next`-clear] + smoke `/platform-admin/login` 200 + `/coaches/tournaments/*` 307, no EACCES; **BROWSER-VERIFIED 2026-06-11** — owner confirmed functionally working as expected): new client island `components/coaches/TournamentRosterSubmit.tsx` (+ co-located CSS) on the tournament detail page (under "Your status"), mounted for accepted teams when a roster is required OR already submitted. GETs the 5j API (requirements + master roster + current snapshot + lock state), pre-selecting the prior submission; the coach ticks which master players go to this event (travel-roster friendly), fills only the organizer-required DOB/jersey as **per-event overrides** (a NEW snapshot DOB fires the same guardian-consent gate as the master RosterEditor — SEAM: never writes the master), ticks the waiver if required, submits → POST to 5j, then `router.refresh()` flips the server-rendered hero. `TeamHQ` gains an optional `rosterRequired` prop → the tournament Roster checklist row now shows "Not submitted"→"Submitted"→"Confirmed" (standalone variant byte-identical); detail page adds `settings` to the SELECT + `parseRosterRequirements` + the gated mount. Client validation mirrors `buildTournamentRosterSnapshot` (min/max via `effectiveMinPlayers` so **min>max⇒no-min**; required DOB/jersey; waiver) — API stays authoritative. **Adversarial review (ultracode — 4 lenses [seam · requirement-parity · TeamHQ no-regression · state/null-safety] + adversarial verify, 18 agents; 7 confirmed → 4 distinct, all folded; 7 dismissed correctly confirmed seam-preserved/standalone-byte-identical/checklist-correct):** (HIGH) DOB consent re-fired on unchanged re-submit (compared to master not the loaded value) → now tracks `initialDob`, re-prompts only on change (mirrors RosterEditor); (HIGH) waiver reset on re-submit → pre-tick when `requireWaiver && rosterSubmittedAt`; (MEDIUM) `position` was dead plumbing (no UI, organizer-assigned at the gate) → removed from the coach Selection + payload; (defensive) null-guards on `sel[id]`. Honest + pitch-free (card only for accepted + required||submitted; no upsell). **5l (head-coach + contact assignment Part 2) BUILT 2026-06-11** (typecheck 0-err + focused lint + `check:dictionary` clean; migration **124** `teams.coach_email` [text/nullable, dev-only] applied + snapshots/DRIFT refreshed [watermark #124]; dev restarted [both stacks stopped → :3000 clear → fresh server, no `.next` clear] + smoke `/platform-admin/login` 200 no EACCES + new PATCH route 401 unauth; awaiting browser verification): new `components/coaches/HeadCoachEditor.tsx` island + `PATCH /api/coaches/tournaments/[teamId]` (5j-sibling auth `requireCoachRegistrationAccess`); the registrant sets the head-coach NAME (rejects empty → never null/'' since `teams.coach` is NOT NULL on prod) + an OPTIONAL `teams.coach_email` (regex+staff-rejected; empty clears → routing reverts to `teams.email`). New shared `resolveCoachRecipient` (`coach_email ?? email`) adopted at the 5 post-registration coach-email sites (registrations/[id], admin/teams bulk, the bulk route, schedule-publish, payment-reminders); **`teams.email` stays the claim key** and the claim-link footer keeps using it; create-time emails (register/import/manual-add/resend) intentionally left on `teams.email`. Q10: reassignment doesn't notify the organizer. Mig 124 joins the Phase-5 prod deploy gate (the 3 explicit-SELECT sites would 400 on prod-without-124 — `check:migrations` gates it; `select('*')` sites degrade gracefully). Adversarial review (ultracode — 4 lenses [auth/IDOR · recipient/no-regression · migration/PROD-deploy · UI state] + adversarial verify): **0 confirmed High/Med, 1 LOW** (results-finalized email recipient still on `teams.email` — out of 5l scope, **deferred to 5m**). **5m (afterglow earned-ask + game-day reminder email) BUILT 2026-06-11** (typecheck 0-err + focused lint clean; NO migration; dev restarted [stack stopped → :3000 clear → fresh server, no `.next` clear] + smoke `/platform-admin/login` 200 no EACCES; awaiting browser verification): result-phase **afterglow** — TeamHQ "That's a wrap!" + final W-L-T + "View final standings" link + prep checklist collapses (J5-052); a detail-page afterglow section = `SharePageButton` (public-only) + the SINGLE earned ask (own-team `ScopeCeilingInterest` real capture + soft `/pricing` discovery) — **plan-independent (J5-050), result-phase ONLY (pre-event/game-day stay pitch-free)**; `CoachLiveSchedule` `isResult` retires the install banner + live-scorebug copy (J5-054). Reframed `tournamentResultsFinalizedHtml` (express-interest, `coachPortalFooter`, recipient→`resolveCoachRecipient`, drops `billing=annual`) — **folds the 5l carry-forward**. New transactional `gameDayReminderHtml` scheduled **evening-before** via Resend `scheduled_at` (`skipOptOutCheck:true`, best-effort/isolated, cancel-then-schedule idempotent) + new per-recipient `cancelScheduledEmailForRecipient` wired into all 3 reject paths. `/coaches/start` **gated** → real `CoachStartInterest` capture (via `/api/early-access`, `coach_portal`) instead of a checkout dead-end (J5-051) — makes the hub's "Express interest" card genuine (owner chose B; non-gated `TeamSignupClient` untouched, no hub markup change). **Adversarial review (ultracode, 5 lenses + verify; ⚠ first run 529'd entirely → re-run 8 agents ~450K tokens): 1 MEDIUM folded** (`resolveCoachRecipient` only trimmed while the reminder's stored `email_sends.recipient_email` was verbatim and the cancel queried lowercased → mixed-case [admin manual-add un-lowercased `teams.email`] missed the cancel → duplicate/stale reminders; **fixed by lowercasing `resolveCoachRecipient`**) **+ 1 LOW documented** (per-recipient cancel collapses two teams sharing a coach address to one reminder — schedule side keys per-recipient too, so consistent; true per-team keying = deferred). **5n (organizer automatic-email controls + master pause-all) BUILT 2026-06-11** (typecheck 0-err + focused lint + `check:dictionary` clean; NO migration; dev restarted + smoke `/platform-admin/login` 200 no EACCES; awaiting browser verification): **EXTENDED the existing `coach_email_*` system + its Event-Settings "Automatic Coach Emails" UI** (both already shipped for the 4 status emails — the plan's "build new UI / new `email_auto_*` keys / `shouldSendAutomaticCoachEmail`" notes were stale) — added 2 per-type toggles `coach_email_schedule` + `coach_email_game_day` (default-ON) gating the schedule-published email + the 5m game-day reminder in `schedule-publish` (reminder cancel kept UNCONDITIONAL so disable+re-publish clears pending), plus a master `coach_email_pause_all` (default-OFF, **opposite polarity**) that short-circuits `coachEmailEnabled` for all 6 per-type sends AND (via new `coachEmailsPaused`) the post-event results email. Owner rulings (AskUserQuestion): results keeps its existing "Post-Event Results" toggle (**NO competing per-type key**); two separate schedule/game-day toggles; plan-gating unchanged (J5-061 deferred); master = no transactional carve-out. J5-007 folded: register success "Watch your email" → honest "What happens next" copy when confirmation won't send. New keys in `lib/types.ts` + sanitizer whitelist + DATA_DICTIONARY catalog. Adversarial review (ultracode 5 lenses + per-finding verify, 7 agents ~508K tokens — a real pass): **1 MEDIUM folded** (hydration `setSaved` omitted the 3 new keys → a spurious one-time auto-save on load for non-default tournaments; not data loss; fixed). **5o DEFERRED to Phase 8 (owner-decided 2026-06-11):** the public-pages "Coaches Portal" nav link is built once alongside the Phase-8 "verify → portal" auto-land + email-verification work; interim reachability (success-screen "Open Coaches Portal" button + emailed claim-link footers) deemed sufficient. **✅ PHASE 5 COMPLETE + BROWSER-VERIFIED 2026-06-11 — 15/16 slices built, 5o deferred to Phase 8.** Next: (a) browser-verify outstanding Phase-5 slices; (b) the deferred combined standalone+tournament coach-surface design/UX pass; (c) Phase 6 League Starter caps + beta. **As-we-go fix during 5i verification (built + verified 2026-06-11):** coaches claim screen `/coaches/join` — removed the reintroduced `btn-primary` gradient + `btn-sm` on the Existing/New Team toggle (→ btn-lime/btn-ghost) and added global `select { color-scheme: dark }` (+ `[data-color-mode=light]` light guard) so native dropdowns render readable everywhere; deferred (small 5c polish): up-front "registered under a different email" message when signed in as a non-matching account. Full slice plan, decisions, build log, and the critic's must-fix checklist in [FREE_TIER_COACHES_PHASE_5_BUILD.md](docs/projects/active/FREE_TIER_COACHES_PHASE_5_BUILD.md).
  - [x] **Combined coach-surface design/UX pass** — ✅ **COMPLETE + browser-verified 2026-06-15; project docs archived.** All 6 build slices landed on `feat/free-tier-coaches` (Empty-State `9c16b52` · Theme 4 `ef19c95`/`4b0d4bb` · Theme 1 `54a25a2` · Theme 3 `4ec7c56`/`b6b6c6c` · Theme 2 `c4df666`/`fe96bb3` · coaches-a-e `5627a96` + a11y `4ad8ab9`). The reusable rule set lives on in the (never-archived) `/design` addendum below. **SPECS COMPLETE + 4 owner decisions RESOLVED 2026-06-14.** Holistic `/design`+`/ux` review of the WHOLE coach surface (standalone Basic floor + tournament-coach experience on the shared `TeamHQ` shell) run as a 13-agent workflow (6 grounded theme/seam reviewers → 6 adversarial design-coherence critics → 1 synthesis; all 6 themes accept-with-fixes + **dedup-clean**). **Cross-referenced J2 + J5: ~36 of ~103 coach findings are design-coherence (in scope); the rest stay OWNED** by FP-1 (security/correctness), the Phase-5 build slices, coaches-eval/coaches-a-e, 5e (email), FP-7 (IA), free-tier-strategy (marketing). **De-dup rule held: the owning plan keeps the IMPLEMENTATION; this pass produced the visual/interaction SPEC it feeds.** Two deliverables shipped: per-state findings + comps + route-back + design-only residual → [COACH_SURFACE_DESIGN_UX_PASS_FINDINGS.md](docs/projects/archive/COACH_SURFACE_DESIGN_UX_PASS_FINDINGS.md); the locked reusable rules (phase-hero · button/chip hierarchy · empty-state component · team-colour-wash · density/responsive + cross-shell seam) → [docs/agents/design/COACH_SURFACE_DESIGN_ADDENDUM.md](docs/agents/design/COACH_SURFACE_DESIGN_ADDENDUM.md) (living `/design` reference). **4 open decisions for owner ruling (findings §7):** (1) the central seam question — TeamHQ variants stay distinct [rec] vs converge; (2) hero wash intensity 18% [rec] / 14% / keep 9%; (3) `/coaches/join` submitBtn override vs migrate-to-btn-lime [rec]; (4) monogram font `--font-display` [rec] vs `--font-data`. **Definitive token rulings folded in:** `--text-secondary`/`--text-tertiary` are REAL globals (not ghost; no fix needed); `--text-2/3/-muted`/`--surface-3` are banned ghosts (zero coach usages); `--team-color` has no `-rgb` twin (tint via `color-mix`). Specs route the build to phase5:ref(5h/5i/5m) · coaches-eval · coaches-a-e · unified-plan · free-tier-strategy + a design-only residual list. Plan: [COACH_SURFACE_DESIGN_UX_PASS_PLAN.md](docs/projects/archive/COACH_SURFACE_DESIGN_UX_PASS_PLAN.md) · brief: [COACH_SURFACE_DESIGN_UX_PASS_PM_BRIEF.md](docs/projects/archive/COACH_SURFACE_DESIGN_UX_PASS_PM_BRIEF.md).
    - [x] **Slice 1 (design-only residual — button/CSS)** BUILT 2026-06-13 (commit `e58bb0c`): button hierarchy + lifecycle-chip CSS.
    - [x] **CoachEmptyState slice (Theme 5 — the dependency-gate, build-order #2)** BUILT 2026-06-14 on `feat/free-tier-coaches` (commit `9c16b52`; typecheck 0-err + focused lint clean; **awaiting browser verification + dev restart**): new shared `components/coaches/CoachEmptyState.tsx` (+ co-located CSS) — medallion + soft lime glow, org-less (hue = `--logic-lime`), rounded-square medallion (never a circle), btn-lime primary CTA, compact + tournament-glow variants. Migrated per the §5b full-card/compact/text-note decision rule: **full-card** = Roster/Schedule editor empties (CTA opens the add form; standalone Add hidden when empty), Announcement-recipients empty (Refresh CTA), the new-team first-run banner (3-step checklist, falls away on first entry), premium budget-vs-actual (void+blue-link→btn-lime), premium schedule (HelpCallout→component; Add Event blueprint-blue→btn-lime); **compact** = Fee no-roster sub-block, tournament schedule pending/no-schedule; **text-only `<p>` notes kept** = organizer announcements, team-wide-charges, announcement send-log "no history".
    - [x] **Theme 4 (team-colour identity band + hero wash upgrade)** BUILT 2026-06-14 on `feat/free-tier-coaches` (commits `ef19c95` + review fixes `4b0d4bb`; typecheck 0-err + focused lint clean; `/review` ran — 1 confirmed fix [44px mobile monogram font → `--font-data` per Owner Decision 4] + 1 advisory [band `--surface` base] folded, 3 refuted; **awaiting browser verification**): standalone team home plain H1 → team-colour identity band (18% wash + 30% team-hue border + 52px monogram + watermark, `--font-display`); tournament hero celebration phases (accepted/prep/schedule-live) upgraded 9%→18% wash + watermark + 44→52px monogram via `.heroCelebration`; `.hero` gains `position:relative; overflow:hidden` as the base Theme 1 layers on (build-order #1). Token-clean (color-mix off `--team-color`, no `-rgb`, no circles, lime stays interactive accent, light-mode softening).
    - [x] **Theme 1 (phase-adaptive tournament hero — highest-value output)** BUILT 2026-06-14 on `feat/free-tier-coaches` (commit `54a25a2`; typecheck 0-err + focused lint clean; **awaiting browser verification — test plan at [COACH_SURFACE_THEME1_TEST_PLAN.md](docs/projects/archive/COACH_SURFACE_THEME1_TEST_PLAN.md)**): one TeamHQ hero, six differentiated phases via per-phase left-border accent + phase-semantic wash + swapped content card. 5h = pending entry-fee preview + accepted amber/red fee glance strip + past-due checklist badge; 5i = green game-day Today card (server-derived, no poll); 5m = `.heroResultCard` (Trophy + final W-L-T) replacing the grey `.afterglow`. Data-gated/deferred per spec: champion `placement` + `waitlistPosition` (no source → degrade off). Reads only existing data; pre-event pitch-free; lime stays interactive accent. **Routes the visual spec into phase5:ref 5h/5i/5m — those are now visually COMPLETE.**
    - [x] **coaches-a-e (standalone portal shell / nav / account)** BUILT 2026-06-15 on `feat/free-tier-coaches` (commit `5627a96`; typecheck 0-err on changed files + focused lint clean; awaiting browser verification): CP-3 brand-mark + rail-active-bar + mobile-active-tab → permanent `--logic-lime` (off org-overrideable `--primary`/`--primary-light`); CP-9 "Teams"→"My Teams" + match fn widened to cover `/coaches/team/*` (J5-022 active-tab bug); CP-8 mobile account chip → bottom-sheet (email · All workspaces · Send feedback · Sign out — mobile previously had NO account/sign-out surface, J5-020); Decision 3 `/coaches/join` Create-Account/Continue buttons → `btn btn-lime` (off square-cornered `auth.module.css .submitBtn`). Tournament-list h1 "Tournament Records" rename + CP-6 breadcrumb badges remain coaches-eval residuals (out of this slice's scope; left as-is). **Combined Coach-Surface Design/UX Pass COMPLETE** (all 6 slices landed: Empty-State `9c16b52` · Theme 4 `ef19c95`/`4b0d4bb` · Theme 1 `54a25a2` · Theme 3 `4ec7c56`/`b6b6c6c` · Theme 2 `c4df666`/`fe96bb3` · coaches-a-e `5627a96`).
  - [ ] **Free Tier Phase 6 - Free League Starter (caps + beta)** — **6.0–6.6 BUILT** on `feat/free-tier-coaches` (6.0–6.5 BUILT + browser-verified 2026-06-12; **6.6 abuse controls + instrumentation BUILT 2026-06-13**: `lib/rate-limit.ts` rate-limits `/api/league/create` [per-account 3/15min · per-IP 8/hr · global 30/5min, 429+log], all 7 §13 events reuse the existing `platform_events` store [NO migration — owner ruled "reuse fully"; 5 server-fired gated to `isFreeFloorLeague`, 2 client-fired via new authed `POST /api/events/league` that verifies membership+free-floor], platform-admin orgs-list "League Starter" badge/filter + Command Center growth panel; review fix folded = `writePlatformEvent` hardened throw-proof so the create-route `await` can't roll back a new org; typecheck/lint/verify:changed green). **6.7 DONE 2026-06-13:** QA passed (owner) + **mig 125 APPLIED TO PROD** (`refresh:snapshots` watermark #125; `check:migrations` GREEN = prod in sync). `free_floor` is now in prod **ahead of the code** (correct ordering); flag `LEAGUE_STARTER_BETA` OFF in committed code/prod, ON in local `.env.local`. **✅ Phase 6 BUILD COMPLETE + prod-DB-ready (flag-off, unlisted beta). Remaining = Phase 9 launch** (deploy the 6.0–6.6 code to master + flip the flag + waitlist→"start free" CTA), a deliberate go-to-market call gated on the League Starter→League branding rename (the follow-up sub-bullet below). The 6.0–6.6 code is on `feat/free-tier-coaches`, NOT yet committed. Noted pre-existing (out of 6.6 scope): TOCTOU race on the 6.1 cap checks (no DB unique constraint) + platform-metrics events `.limit(5000)` truncation. A small house-league admin runs **one real season free** (1 season / 1 division / 8 teams / auto-schedule + standings / manual fee tracking / one narrow public schedule-standings-register page / no full org site / no exports), with caps enforced **server-side** (D6, net-new — house-league is module-gated, not cap-gated today) via a persistent `organizations.free_floor='league_starter'` **entitlement profile, not a new plan key** (migration 125); ships as a **capped beta** behind a flag; `/start/league` becomes a real create path reusing the league onboarding wizard (trigger moved plan→entitlement); also unblocks the user-journey audit's League/Club personas. Plan: [FREE_TIER_LEAGUE_STARTER_PLAN.md](docs/projects/active/FREE_TIER_LEAGUE_STARTER_PLAN.md) · brief: [FREE_TIER_LEAGUE_STARTER_PM_BRIEF.md](docs/projects/active/FREE_TIER_LEAGUE_STARTER_PM_BRIEF.md) · sequence in [FREE_TIER_COACHES_UNIFIED_PLAN.md](docs/projects/active/FREE_TIER_COACHES_UNIFIED_PLAN.md) Phase 6.
    - [x] **Follow-up — League-only destination (RESOLVED 2026-06-12):** free League Starter orgs now land on `/admin/house-league` (their league dashboard) from **both** login (`getDestinationForMembership`) and post-onboarding (`getPostOnboardingHref`) — fixing a bug where login routed them to **tournament admin** (plan_id='tournament'). Also fixed the `/home` card labelling such an org "Free League Starter" instead of "Tournament subscription". The broader generic-`/admin`-hub IA (when reached via nav/All-workspaces) remains a journey-walkthrough item.
    - [ ] **Follow-up — Branding "League Starter"/"League" → "League"/"League Plus"** — APPROVED + IN EXECUTION 2026-06-13 (owner: full rename now incl. live public copy). Free house-league floor = "League" (was "League Starter"); paid $89 tier = "League Plus" (was "League"); mirrors Tournament/Tournament Plus; internal keys (`free_floor='league_starter'`, `plan_id='league'`, Stripe) UNCHANGED. Rename **COMPLETE + owner browser-verified → ARCHIVED**; the public free-"League" pricing card/CTA is held for the Free-Tier Phase-9 launch (tracked under Free Tier, not the rebrand). Plan + strict change/never-change rules + surface inventory: [LEAGUE_REBRAND_PLAN.md](docs/projects/archive/LEAGUE_REBRAND_PLAN.md) · brief: [LEAGUE_REBRAND_PM_BRIEF.md](docs/projects/archive/LEAGUE_REBRAND_PM_BRIEF.md).
- [ ] **Registration Form QA + Stage-1 Polish** (Coach journey Stage 1; shared public surface) — BUILT 2026-06-08 on `feat/free-tier-coaches` (tsc clean; lint clean for new code; awaiting browser verification at `/dev-test-org/dev-tournament-2026/register`): removed the **duplicate fee** card on the review screen (kept the richer payment panel); **success screen redesigned to "you're in motion"** (`Countdown` "first game in N days" momentum banner + **Add-to-calendar** .ics download + restyled Save-the-dates / Watch-your-email / Create-portal ladder; honest CTAs — no team-follow pre-acceptance); **organizer payment controls** (new `TournamentSettings` JSONB keys, no migration) authored in Event Settings → Fee Schedule: a **Show/Hide fees on the public registration form** toggle (default Show) + an optional **Payment instructions** field with delivery choice (**default email-only** via the acceptance email, optional **Form & email**) — wired into `acceptanceHtml` across all three accept paths. Due-date rendering verified as already-correct (not a gap). **Also fixed a registration-blocking 500** (`POST /api/register` "Unable to confirm tournament availability"): **dev *and* prod schema drift** (verified against the live `docs/agents/db/schema-snapshots` dev/prod dumps + `information_schema`, not migrations). The deployed `master` register route selected **three** columns absent from both DBs: `tournaments.public_hidden_pages` (real column — migration 040 had never been applied to dev OR prod; it also left the "hide public pages" feature broken in prod), `divisions.contact_id` (dropped in mig 090), and `organizations.contact_email` (never an org column — it lives on `org_public_site_content`). Fixes: **applied migration 040 to dev + prod (2026-06-08)** to restore `public_hidden_pages`; removed the two stale selects (`contact_email`, `contact_id`) from `api/register/route.ts`. Verified every column the fixed path needs now exists in prod. **Then fixed a 3rd bug the 500 had masked — a read/write gating mismatch:** the register POST blocked on `!organization.is_public`, but tournament pages/registration are explicitly **independent of `is_public`** (it gates only org home/league pages; Tournament-Plus orgs are `is_public=false` by design — see the documented rule in `lib/public-tournament-data.ts` `getPublicContext`). So **every TP org (`is_public=false`) was blocked from accepting registrations — in prod too** (`master` has the same guard). Aligned the write gate to the read gate (`!organization || subscription_status==='canceled'`). **⚠️ Prod registration is NOT fully fixed until the route code changes (drop `contact_id`/`contact_email` selects + the `is_public` gate fix) are committed to `master` and deployed** — uncommitted on `feat/free-tier-coaches`. `/design` height/density pass also applied (see eval Stage-1). Detail + remaining Stage-1 items in [COACHES_EXPERIENCE_EVAL_PLAN.md](docs/projects/active/COACHES_EXPERIENCE_EVAL_PLAN.md) Stage 1.
  - [ ] **Follow-up for the basic-teams / platform-admin-leak-fix effort:** `GET /api/coaches/basic-teams` returns **401 for not-signed-in visitors**, which logs a red console error on every anonymous public register/portal load. Return `200 { user: null, teams: [] }` for the *no-session* case (keep the 401 for *platform-admin* sessions so the leak fix is preserved) to give a clean console — all three callers (`register`, `coaches/join`, `CoachPortalShell`) already handle a null user / empty teams identically. Route owned by that effort (`app/api/coaches/basic-teams/route.ts` is in their uncommitted set), so land it there.
- [x] **Data Dictionary (semantic schema + maintenance rules)** — ✅ COMPLETE 2026-06-11 (113/113 tables column-sealed, all 11 domains; only the Layer-4 `/dba` quarterly check remains ongoing). PLANNED + signed off 2026-06-08; **Phase 0 tooling BUILT** (branch `feat/free-tier-coaches`, uncommitted). Build `docs/agents/db/DATA_DICTIONARY.md`: per-field purpose/relationships/gotchas (semantic layer atop the `docs/agents/db/schema-snapshots` dumps), gotchas-first, by domain, starting with Tournaments & Registration. Codifies "decide column existence from the live dev/prod snapshots, never from migration files" + snapshot-refresh + drift-watch rules. **Phase 0 DONE:** `scripts/refresh-db-snapshots.mjs` (`npm run refresh:snapshots`) regenerates ALL dev+prod snapshot JSON + `schema-snapshots/DRIFT_dev_vs_prod.md` + the memory markdown (one Mgmt-API PAT reaches both projects; idempotent/byte-stable; run after every migration) — snapshots were FAR staler than the kickoff assumed (82–85 → **103 tables both envs**; **50 dev/prod divergences** catalogued, DB review Finding #25); `scripts/check-dictionary-coverage.mjs` (`check:dictionary`, in `verify:changed`) is the staleness ratchet (plan §11; table-level sealing added). **Phase 1 (Tournaments & Registration) BUILT 2026-06-08:** `docs/agents/db/DATA_DICTIONARY.md` (772 lines) = header (6 maintenance rules + anchor convention) + **18 tables documented & SEALED** column-granular (tournaments, divisions, teams, games, pools, pool_slots, rules, rule_items, resources, announcements, registration-fields(+answers), diamonds, venue_facilities, org_venues(+facilities), schedule_facility_lanes, tournament_archives), pinned to commit `ad9dc66`; built via 3 parallel extraction agents + 3 adversarial verifiers (4 minor corrections applied). **Layer 3 wired** (`/db`/`/dba`/`/plan` + AGENCY_RULES: "schema change ⇒ dictionary change, same unit of work"). **Phase 2 (Coaches / basic-teams) BUILT 2026-06-08:** +7 tables documented & SEALED (basic_coach_teams/_users/_registrations + team_workspaces/_org_links/_workspace_claims/_entitlements) → **25 tables sealed, ~1026 lines**; 4 parallel extractors + 2 adversarial verifiers (which caught 2 *blocking* team_workspaces errors — the org_team_addon Stripe-NULL direction was backwards, and the `source` enum was wrong — both fixed). Notable verified facts: Basic-portal access is membership-keyed (basic_coach_team_users.user_id, not email); role CHECK=owner|coach (memory's 'assistant' was wrong); team_workspaces dual org FK (workspace_org_id=tenancy anchor). (Free-tier Phase 3 then added + sealed `basic_coach_team_players` → 26 tables.) **Phase 3 (Org / Platform core) BUILT 2026-06-09 (pinned to commit `5479605`; snapshot 2026-06-09, dev 104 / prod 103, org tables zero-drift):** +8 tables documented & SEALED, inserted as the **first** domain (the FK backbone) → **34 tables sealed total**: organizations (full, incl. Stripe/billing columns), organization_members, org_member_rep_group_scopes, org_member_tournament_assignments, org_overrides, org_audit_log, org_internal_notes, org_public_site_content. Built via 4 parallel extractors + 2 adversarial verifiers (39/40 confirmed, 0 refuted; one verifier false-negative — "no revoke route" — refuted by direct check: the `[oid]` DELETE revoke exists). Verified gotchas: `organizations` has NO `contact_email` (mapOrg dead-reads it → `Organization.contactEmail` always null; real value on `org_public_site_content.contact_email`); `plan_id` default `'starter'` is legacy; `account_kind='team_workspace'||plan_id='team'`=shadow org; `role` has NO DB CHECK (8-value `OrgRole` enum in code, owner=super-user bypass, `capabilities` jsonb is additive+subtractive); scope tables are absence-means-unrestricted; `org_member_tournament_assignments` scopes `staff` notify recipients (NOT `notify_mode`); `org_overrides` timed grants are INERT by default (`ENTITLEMENT_GRANTS_ENABLED` off) — when on only `module_addon`+`subscription_status` apply, start/expiry/revoke ARE enforced (supersedes DBA #22–24), `suppress_billing` has no reader; org_audit_log (org-scoped) vs platform_audit_log (override/note mutations go there, later phase). **Phase 4a (Rep teams / team workspaces — OPERATIONS half) BUILT 2026-06-09 (pinned to commit `5479605`; snapshot 2026-06-09; all 12 tables ZERO dev/prod drift):** +12 tables documented & SEALED under a new `# Domain: Rep teams / team workspaces` (a two-half domain like Coaches — labeled `## Rep operations` subsection now; `## Rep finance` = Phase 4b): rep_teams (hub), rep_team_groups, rep_program_years (season spine), rep_roster_players, rep_team_coaches, rep_team_events, rep_team_event_attendance, rep_team_lineups, rep_team_lineup_entries, rep_tryout_registrations, rep_document_templates, rep_player_documents (147 cols). The 4 `team_workspace_*` tables the classifier files here were already sealed in P2 (not redone); their forward-links close here. Built via **6 parallel extractors + a FULL 3-agent adversarial pass** (user chose full over the pre-approved lighter 4a-fallback; 54/56 confirmed, both behavioral-bug claims reproduced — the `createRepTeamEvents` recurring-practice `recurrence_parent_id` orphan-anchor defect, and `attention-summary` querying `status='pending'` vs the real `'pending_review'` → badge always 0; 7 author fixes applied). Verified gotchas: `program_year_id` season spine + `getActiveRepProgramYear`=newest draft|active (draft already live+billable); RLS enabled but app-layer auth (`rep_team_coaches` membership = the coach gate, franchise model); `rep_team_groups`=staff-scoping anchor; `rep_roster_players` NO `source_player_id` (Phase-5 link absent); documents use a private Storage bucket + signed URLs, NO `program_year_id`. **→ live baseline 47 tables sealed after 4a.** **Phase 4b (Rep teams / team workspaces — FINANCE half) BUILT 2026-06-09 (pinned commit `5479605`; all 13 tables ZERO dev/prod drift; 158 cols):** +13 tables documented & SEALED under a `## Rep finance` subsection within the SAME domain heading → **Rep domain (25 tbl) COMPLETE; 62 tables sealed** (rep_budget_lines/_periods, rep_season_surplus, rep_player_dues_schedules/_installments, rep_dues_credits, rep_fundraisers/_entries, rep_cost_allocations, rep_allocation_splits/_installments, rep_team_expenses, rep_team_payment_requests). Built via 5 parallel extractors + a **FULL 3-agent adversarial pass** (kept ON — money/enum/Stripe class; ZERO refutes, 2 cosmetic nuance fixes). Verified gotchas: money = dollars-not-cents (all 13); **NO Stripe columns anywhere** (internal double-entry via `accounting_entry_id` + `create_accounting_transfer` RPC; Stripe surface deferred to the Stripe/Billing phase); **dual-budget-line trap** (`rep_budget_periods`/`rep_player_dues_schedules.budget_line_id` → team `rep_budget_lines`, but `rep_cost_allocations.source_budget_line_id`/`rep_team_payment_requests.budget_line_id` → org `org_budget_lines`); single-spelling status/method enums (no US/UK doublet). **3 schema/code-drift bugs verified live via an `information_schema`/`pg_trigger` probe (no triggers exist):** `rep_team_expenses` has no `notes` column yet code writes it; `org_id` NN-no-default on both installment tables yet inserts omit it; `getRepAllocationSplitsForTeam` queries non-existent `allocation_split_id`. Negative-test proved column-granular enforcement live. **All 3 FIXED 2026-06-09 (follow-up):** `org_id`+`team_id` now populated on the 3 installment-insert sites + `allocation_split_id`→`split_id` corrected (tsc clean); **migration 119 added `rep_team_expenses.notes text` to dev+prod** (user-authorized prod apply — the coach expenses form collects notes) → finance now 159 cols, still zero drift; doc gotchas flipped to ✓FIXED + `notes` documented. (Concurrent: dev grew 106→107 — a parallel basic-portal workstream added+sealed `basic_coach_team_announcements`; my seal preserved it.) **Phase 5 (League / house-league) BUILT 2026-06-10 (pinned commit `6deac4a`; snapshot 2026-06-10, dev 113 / prod 113; all 8 tables COLUMN-identical dev/prod):** +8 tables documented & SEALED under a new `# Domain: League / house-league` heading (after Rep, before Observability) → **70 tables sealed; Rep + League domains COMPLETE** (league_seasons [+`draft_state` jsonb catalog], league_divisions, league_teams, league_games, league_registrations, league_practices, league_email_log, league_notification_log). Built via 4 parallel extractors + a **FULL 4-agent adversarial pass** (kept ON; 1 blocking + 8 minor fixes folded). **MARQUEE BUG (verified live dev+prod via `information_schema`/`pg_trigger` probe — zero triggers on any league_* table):** `league_games.org_id`+`league_practices.org_id` are NN-no-default-no-trigger (migs 075/078, for 1-hop RLS) yet NO insert path writes org_id (`createLeagueGame`, `schedule/generate` save, dev seed, `createPractices`) → every house-league game/practice create violates the NN constraint (same class as the Phase-4b rep bugs). **Bug FIXED 2026-06-10 (code-only, separate from the dictionary commit `2ba5972`): `orgId` now written by `createLeagueGame`/`createPractices` + the `schedule/generate` & dev-seed inserts (from `ctx.org.id`); tsc + lint(0-err) clean; doc gotchas flipped to ✓FIXED.** Verified gotchas: `league_seasons.division` text (renamed `age_group`, mig 093) ≠ the `league_divisions` table; `league_registrations` ACCOUNT-LESS (guardian_email-keyed, non-unique, no auth.users FK); fees INTERNAL (`fee_entry_id`→`accounting_entries`, no Stripe); `league_notification_log` DEAD (superseded by `league_email_log`); practices recurrence symmetric (no rep-style orphan bug); drift-heavy = `league_practices` indexes only (mig 077 dev-only: partial-vs-full recurrence + dev-only schedule_idx). Negative-test re-proved column-granular enforcement live. **Phase 6 (Accounting) BUILT 2026-06-10 (pinned commit `cbcf7c7`; snapshot 2026-06-10, dev 113 / prod 113; all 9 tables column/constraint/CHECK/index-identical dev/prod, ZERO drift):** +9 tables documented & SEALED under a new `# Domain: Accounting` heading (after League, before Observability) → **79 tables sealed; Rep + League + Accounting COMPLETE** — 3 sub-systems: ledger (`accounting_ledgers`/`accounting_entries`/`org_payees`), shared chart + org budget (`budget_categories`/`budget_items`/`org_budget_lines`/`org_budget_periods`), billing data-retention (`billing_retention_intents`/`billing_retained_records`). Built via 4 parallel extractors + a **FULL 4-agent adversarial pass** (kept ON; 1 blocking + 1 path-fix + 5 nitpicks folded — blocking = `budget_categories` has NO unique name index, so duplicate category names are silently allowed). **Live `information_schema`/`pg_trigger` probe (dev+prod): NO triggers on any accounting table.** Verified gotchas: money = dollars-not-cents, `amount` always positive (direction via `entry_type`); transfers are paired legs created ONLY by the `create_accounting_transfer` RPC (`linked_entry_id` self-FK); `void`=soft-delete; shared chart of accounts (`org_id IS NULL`=platform defaults, mig 027) co-owned by org + rep budgets; **dual-budget-line trap** from the org side (`org_budget_lines` int `season_year` vs `rep_budget_lines` uuid `program_year_id`); `org_budget_periods` broken invariant (Σ-not-reconciled, unlike rep); budget-vs-actual coarse (org-wide, not per-line); `payee_id`/`payee_payer` exclusivity convention-only; `source_module` only writes 'league_registration'; BILLING RETENTION = `status` frozen at 'applied' + lifecycle on `retained_state` + MANUAL sweep (no pg_cron) + 'purged'=supersede not deletion. NO Stripe cols anywhere. Negative-test re-proved column-granular enforcement live. **Phase 7 (Stripe / Billing) BUILT 2026-06-10 (pinned commit `cbcf7c7`; snapshot 2026-06-10, dev 113 / prod 113; `stripe_prices` structure column/constraint/CHECK/index-identical dev/prod, ZERO drift):** +1 table documented & SEALED under a new `# Domain: Stripe / Billing` heading (after Accounting, before Observability) → **80 tables sealed** — `stripe_prices` (the governed platform price catalog) PLUS a billing-flow narrative that cross-references (does NOT redocument) the already-sealed `organizations` (Phase 3), `team_workspaces`/`team_entitlements` (Phase 2), and `billing_retention_intents`/`billing_retained_records` (Phase 6) columns. A column-light, narrative-heavy phase. Built via 2 parallel extractors + a **FULL 3-agent adversarial pass** (kept ON — billing/Stripe = the high-risk class; **63 confirmed, 0 refuted, 6 nuance [0 blocking]** across all 3 verifiers; all 6 nuances folded as one-clause tightenings). **Live probes (dev+prod `information_schema`/`pg_trigger`/`pg_class`/`pg_policy`): `stripe_prices` has NO triggers; RLS ENABLED with ZERO policies; prod grants anon+authenticated SELECT (dev does not) — so RLS-no-policies is the only thing keeping it off prod's public REST API. Content probed: 24 rows/env, 6 plan_ids, mirror-imaged (dev maps sandbox rows, prod maps live rows).** Verified gotchas: the webhook derives the plan from the PRICE via `getPlanFromPriceId` (reverse, env-blind) not metadata — an unmapped `price_id` SILENTLY no-ops entitlement + isn't even logged (webhook doesn't import observability); environment = `STRIPE_SECRET_KEY` prefix sniff (`sk_live_`→live else sandbox; NODE_ENV irrelevant), re-implemented 4+ places; CHECK value is `sandbox` not `test`; `organizations.subscription_status` can hold RAW Stripe statuses outside the 4-value TS type (org webhook writes `sub.status` verbatim; only team paths normalize) and every gate checks only `=== 'canceled'`; `invoice.payment_failed`→past_due only (full suspension/retention cascade is `subscription.deleted`-only); downgrade/cancel are DB-FIRST (Stripe reconciliation failure → `billing_stripe_reconciliation_failed` audit + contact-support 500, DB already changed); 3 Stripe-bypass paths (mock billing [prod-hard-false], no-key direct-apply, founding-season comp) write plan state with NO Stripe object; UPDATE-only table (no code INSERT; seed-only via migs 048+065); every `price_id` write requires an APPROVED catalog change request; `last_change_note`/`product_name` write-status documented (note overwrite-with-NULL, never exercised live). Negative-test re-proved column-granular enforcement live (80 sealed). **Phase 8 (Platform admin) BUILT 2026-06-10 (pinned commit `cbcf7c7`; snapshot 2026-06-10, dev 113 / prod 113; all 17 tables column/CHECK/UNIQUE/FK-identical dev/prod, ZERO structural drift):** +17 tables documented & SEALED under a new `# Domain: Platform admin` heading (after Stripe/Billing, before Observability) across 3 labeled sub-domains (`## Platform identity, RBAC & audit` · `## Plans & catalog control plane` · `## Platform ops & data import`) → **97 tables sealed; Platform-admin control plane COMPLETE** (platform_users, platform_audit_log, platform_user_notes, platform_admin_visits, platform_events; plan_gating, plan_config_overrides, platform_plan_versions, platform_plan_module_entitlements, platform_addon_catalog, platform_catalog_change_requests/_change_applications/_campaigns; platform_bulk_operations, platform_metric_snapshots, import_batches, import_batch_rows; 171 cols = 139 documented + 32 boilerplate waivers). Built via 5 parallel extractors + a **FULL 4-agent adversarial pass** (kept ON — RBAC/billing-governance/audit class; 167 claims confirmed, 1 blocking + ~9 minor/precision fixes folded; blocking = the `platform_admin`/`founding_season` event sources ARE written, not dormant). **Live probe (dev+prod `information_schema`/`pg_class`/`pg_policies`/`pg_trigger`/`role_table_grants`): all 17 RLS-ENABLED, ZERO policies, ZERO triggers; prod grants anon+authenticated full CRUD on 16 of 17 (all but `plan_gating`=service_role-only), dev only REFERENCES/TRIGGER/TRUNCATE — RLS-no-policies is the ONLY thing keeping this control plane off prod's public REST API.** Verified gotchas: RBAC bootstrap = the `PLATFORM_ADMIN_EMAILS` env allowlist (super_admin, no DB row → explains prod platform_users=0); 6-role CHECK incl. `growth`, unknown→read_only fail-safe; **runtime-source split** — only `plan_gating` (checkout gating via getPlanGatingMap) + `plan_config_overrides` (checkout-time limits) are runtime-consumed, while `platform_plan_module_entitlements`/`_plan_versions`/`addon_catalog`/`catalog_campaigns` are admin-display/planning catalogs NOT read at runtime (hasModuleEntitlement uses static PLAN_CONFIG); catalog-governance gate authorizes by `proposal.kind` not `request_type`, NO separation of duties (same manage_product submits+approves), `_change_applications` ledger best-effort (swallows errors) → `platform_audit_log` is the durable trail; `platform_events` = business-event log (31-value union, only 4 read) DISTINCT from Observability errors (app-side dedup, TOCTOU, no unique constraint); 5 built-but-unused tables (user_notes/bulk_operations/metric_snapshots/catalog_campaigns 0 rows, plan_versions 1 seed row no writer); import_batches/_rows are ORG-scoped (not platform-admin). Negative-test re-proved column-granular enforcement live (97 sealed). **Phase 9 (CRM, 1 tbl `early_access_leads`) + Phase 10 (Notifications & Push, 8 tbl across 3 sub-systems: in-app bell+prefs / web push / email) BUILT 2026-06-10 (pinned commit `9faea936`; snapshot 2026-06-10, dev 113 / prod 113; all 9 tables column/constraint/FK-identical, ZERO structural drift, ZERO triggers + ZERO CHECKs):** two new `# Domain:` headings inserted between Platform admin and Observability → **106 tables sealed; CRM + Notifications domains COMPLETE** (only `tournament_roster_players` left acknowledged). 4 parallel extractors + a FULL 4-agent adversarial pass (35 confirmed / 1 refuted / 2 nuance, all folded). **Live RLS probe refuted two assumptions:** only `notifications` carries RLS policies (2 own-row select/update, VESTIGIAL — every data path is service-role), the other 8 are RLS-enabled-zero-policies; `early_access_leads` has NO anon-INSERT policy (service-role lead capture). Verified gotchas: early_access DUAL-STATUS trap (`internal_status` live, `status` dead; conversion keys off `converted_org_id`, not the status string); `tournament_notification_preferences` = per-USER opt-out (not contact-member `notify_mode`); `notify()` fan-out hub (5 dead event types; name-collides-but-disjoint from `platform_events`); two push tables by design (both EMPTY dev+prod; member vs anonymous-fan identity); `platform_email_templates` is a MIRROR (`resolveEmailTemplate` never built — hardcoded `lib/email.ts` sends); NO `scheduled_at` DB column (Resend native param). Negative-test re-proved column-granular enforcement (106 sealed). **Observability & Feedback COLUMN-SEAL pass BUILT 2026-06-10** (atop commit `412e4036`; the Phase-3 feedback center + `request_id` Mechanism-A are uncommitted working-tree — refs working-tree-relative): the 6 Observability tables upgraded table-granular → **column-sealed** (67 anchors) → **112 tables sealed; ALL 11 domains column-sealed except the deferred `tournament_roster_players`**. 3 extractors + a 2-agent adversarial pass (17 confirmed / 0 refuted / 3 nuance, all folded). All 6 are RLS-enabled-zero-policies-zero-triggers (these DO have CHECKs); `record_error_event` is plain plpgsql (NOT security definer) while the fold/sweep ARE; `error_groups` has no org_id (2-hop org_slug filter); `occurrence_count` counts all but events are sampled; env CHECK on groups not events; `request_id` Mechanism-A = the bug→error deep-link; metrics instrument only 2 routes; `feedback_submissions` brand-new (0 rows) with many write-only admin-trail cols. Negative-test re-proved column-granular enforcement (112 sealed). **CI snapshot-freshness gate (plan §11 Layer 1½) BUILT 2026-06-11** — `scripts/check-snapshot-freshness.mjs` (`npm run check:snapshots`, wired into `verify:changed` before the coverage ratchet) fails when `supabase/migrations/` outruns the **migration watermark** (`highestMigration`/`migrationCount`) that `refresh-db-snapshots.mjs` now records in `docs/agents/db/schema-snapshots/SNAPSHOT_MANIFEST.json` on every refresh; watermark-based (no git/network → deterministic, safe in the routine local check; only a real refresh clears a stale failure), seeded #122/123, negative-tested (a higher-numbered migration trips it). Closes the residual "added a migration but forgot to refresh+commit the snapshot" hole the coverage ratchet can't see. **`tournament_roster_players` COLUMN-SEALED 2026-06-11 — the FINAL table → 113/113 tables column-sealed, `acknowledgedTables` EMPTY, the Data Dictionary is content-COMPLETE across all 11 domains.** (`source_player_id` back-link added free-tier Phase 5j, mig 123, FK ON DELETE SET NULL, dev-only; the 3 deferred notes were already flipped.) Built via 1 extractor + 1 adversarial verifier (7/7 confirmed, 2 nuance folded); +11 column anchors in Tournaments & Registration after `tournament_archives`; the stale domain end-marker de-staled. **★ Verified latent prod-break:** the coach route reads (`:112` SELECT) + writes (`:210` INSERT) the dev-only `source_player_id` UNCONDITIONALLY → the submit POST hard-500s on prod (the GET only degrades to an empty snapshot); fenced by the mig-123 deploy gate + `check:migrations`. Also `source='admin'` is a dead enum value (only coach/gate written); `updated_at` is dead (no UPDATE path). `check:dictionary` green (113/113 sealed, 0 acknowledged) + negative test. **Only ongoing work = the Layer-4 `/dba` quarterly health check.** **Live branch-drift lesson (Phase 1):** an origin/dev merge (1f61801) restored the per-game-length refactor, flipping the timing findings (kickoff now correct) — see plan §7. Kickoff at [DATA_DICTIONARY_KICKOFF.md](docs/projects/archive/DATA_DICTIONARY_KICKOFF.md) (see [plan](docs/projects/archive/DATA_DICTIONARY_PLAN.md) and [PM brief](docs/projects/archive/DATA_DICTIONARY_PM_BRIEF.md)).
- [x] **Unified Bracket Engine + Playoff-Only Tournaments** — SCOPED 2026-06-04, re-scoped same day; **Phases 1–5 BUILT** (P1–4 browser-verified 2026-06-05; P5 awaiting verification): P1 = pure `lib/playoff-bracket.ts` generator (any-N + byes, single/consolation/double-elim, GF reset) + simulation tests; P2 = wired into `PlayoffWizard` standard+reseed modes (`PlayoffConfig.format`/`grandFinalReset` + Bracket Format selector UI); P3 = GF-reset rule in `advancePlayoffs` + shared `bracketRoundInfo` round-grouping in public `LogicSyncBracket` + admin `buildBracketColumns` + list ordering; **P4 = the playoff-only tournament** — `TournamentSettings.format` + `patch-settings` + `isPlayoffOnly` helper, format control in Event Settings (Draft-only + warn/wipe schedule on switch, `delete-tournament-games` action), **Seed Teams step** (drag + Randomize + bye preview), seeds resolved to real teams at creation; **P5 = hide round robin for playoff-only** — public schedule defaults to bracket + hides stage toggle, admin schedule forces Playoffs + hides RR generator, **Standings tab/page hidden** (`lib/public-pages.ts` + layout). lint/tsc clean. **Phase 6 Tier 1 + Tier 2 BUILT 2026-06-05**: T1 = team names (not `Seed #N`) in admin builder canvas (`BracketBuilder` `labelFor`), tidied last two bracket renderers (`PublicBracketView` + legacy `app/schedule` → shared `bracketRoundInfo`), protect-top-N randomize; T2 = printable **bracket PDF** (`lib/export/bracket-pdf.ts` + admin schedule "Bracket PDF" button), **per-division format badge** on public schedule Playoffs stage, **create-flow format choice** ("Tournament style" in both setup wizard + onboarding → `patch-settings`); also consolidated to **one context-aware PDF** (bracket vs table by `layout` toggle) + removed iCal from admin export (kept public fan "Add to Calendar"). **Tier 3 #1 = Full Placement bracket BUILT 2026-06-05**: new `'placement'` format (`buildPlacement`/`rankInto` — every team ranked 1..N via championship + classification cascade; `PL{place}` deciding games; tested via terminal-ref→unique-1..N proof) **+ placement final-standings panel** (`placementPlaces`/`computePlacementStandings` → ranked 1..N on public Playoffs view). 45/45 bracket tests. **End-to-end usable.** **Tier 3 #2 (bezier double-elim tree connectors) + #3 (draw-day reveal animation) BUILT + browser-verified 2026-06-07** (50/50 tests; reveal restrained by design, owner OK'd; demo seed scripts/seed-bl-u15-doubleelim.mts on branded-light U15). **Split-pool (no-crossover) all-format brackets BUILT 2026-06-07** (each pool now runs single/consolation/double/placement via the unified engine; also fixed a latent `advancePlayoffs` cross-pool advancement bug by scoping to `bracketId`; awaiting browser verification). Remaining Phase 6: Tier 4 seed-by-number/prior-results seeding sources, pot draws, blank-bracket PDF. (Auto-scheduler double-elim dependency ordering VERIFIED correct 2026-06-07 — already builds the Winner/Loser dependency graph + generator hard-blocks out-of-order placement + unit-tested; no work needed.) Original scope: **one** bracket engine for ALL tournaments — any team count (with byes), single elimination / 2-game guarantee (consolation) / double elimination — same builder UI + look/feel everywhere. The **only** difference between tournament types is the seeding source: full tournaments seed from round-robin standings; new **bracket-only** tournaments skip round robin and let the organizer seed teams manually or by Randomize (resolved at creation). Key build: new pure `lib/playoff-bracket.ts` (seeding-agnostic generator: bye math + single/consolation/double-elim) replacing the hardcoded 2/4/8 templates in `PlayoffWizard.generatePreview()`; extend `PlayoffConfig.format`; double-elim/consolation reuse existing `advancePlayoffs` Winner/Loser routing (only new rule = the "if-necessary" grand final); tournament `format` flag in settings JSONB (no migration); Seed Teams wizard step for playoff-only; hide round-robin views on public + admin. Plus-gated. **Decisions locked 2026-06-04:** GF "if-necessary" reset = toggle default ON; V1 ships all three formats (single/consolation/double); placement = champion + optional 3rd place (full 5th/7th → Phase 6); Tournament Plus only (see [plan](docs/projects/archive/PLAYOFF_ONLY_TOURNAMENT_PLAN.md) and [PM brief](docs/projects/archive/PLAYOFF_ONLY_TOURNAMENT_PM_BRIEF.md))
- [x] **Bulk Data Importer V1** - COMPLETE 2026-06-04: XLSX-first, CSV-compatible tournament team and schedule add/update imports with current-data templates, empty templates, preview-before-commit, audit batches, Tournament Admin Data Tools hub, Recent Imports, guidance/help docs, Tournament Plus paywall enforcement, and server-side scope/role/plan/tournament locks. Migration 106 applied dev + prod; dev UAT org repaired to active Tournament Plus; full team add/update browser smoke, schedule add/update UAT smoke, combined Data Tools UAT smoke, and team/schedule route-hardening UAT smokes passed. Future work: customer-template edge cases, destructive replace/wipe design, supporting reference imports, and league/club import surfaces (see [plan](docs/projects/archive/BULK_DATA_IMPORTER_PLAN.md) and [PM brief](docs/projects/archive/BULK_DATA_IMPORTER_PM_BRIEF.md))
- [x] **Sign-up Flow Fixes** — prod sign-up review (2026-06-04): (1) removed the premature founding "you have Tournament Plus" email at signup — signup now sends **only** the verify email; (2) fixed the onboarding plan-select being **unscrollable on mobile** (the focused admin shell switches to `display:block`, which defeated the ≤900px app-shell `100dvh/overflow:hidden` scroll lock and clipped the plan cards — focused flows now use natural document scroll); (3) made **Tournament Plus first + lime-highlighted** on the plan-select screen (new `order`/`featuredPlan` props on `PricingSection`; public pricing page unchanged); (4) added **plan-choice-triggered emails** via Resend `scheduled_at` (new `scheduledAt` on `sendMarketingEmail`): choose-Plus → `tournament_plus_welcome` +1 day (no "set up first tournament" CTA), choose-Free → `tournament_plus_upsell` +7 days. Also **wiped the b2cowan prod test org/user** so the flow can be re-tested. On `dev`, not yet deployed. Follow-up: cancel/suppress the pending upsell if a free user upgrades within the week (cancel-on-upgrade) (see [plan](docs/projects/archive/SIGNUP_FLOW_FIXES_PLAN.md) and [PM brief](docs/projects/archive/SIGNUP_FLOW_FIXES_PM_BRIEF.md))
- [x] **Onboarding — Decouple Plan Selection from the Setup Wizard** — BUILT 2026-06-04 (awaiting browser verification): the plan picker was wedged into the tournament wizard as "Step 1/7", which skipped the "One Quick Question" step going forward, dead-ended Back on the plan picker, and inflated the counter. The standalone plan gate already exists (`?choosePlan=1` → `?continueSetup=1` routing), so the fix removes `'plan'` from `WIZARD_ORDER` (now a clean **6-step** flow: qualifying → tournament → divisions → welcome → venues → review, numbered /6), resumes the wizard at `qualifying`, and drops the dead in-wizard plan step. Back from step 1 is now disabled (no dead end); plan-less edge entries redirect to the gate. League/Club + later plan changes untouched. Hot-reloads (no restart) (see [plan](docs/projects/archive/ONBOARDING_PLAN_DECOUPLE_PLAN.md) and [PM brief](docs/projects/archive/ONBOARDING_PLAN_DECOUPLE_PM_BRIEF.md))
- [ ] **Tournament Create Flow — drop Year, mandatory dates, no contact step** — BUILT 2026-06-04 (awaiting browser verification): removed the redundant **Year** field from both create wizards (onboarding `app/[orgSlug]/admin/onboarding/page.tsx` + standalone `components/admin/TournamentSetupWizard.tsx` blank-create **and** clone-name flows) — the year is now **derived from the start date** (still stored for naming/sorting/clone); **start + end dates are now required** with a "you can change them later if they're still being finalized" hint (so archive-cloning always has dates); the auto-generated "`<year> Tournament`" name re-syncs to the chosen start year. Removed the **"Set public contact email"** step (onboarding 8→7 steps); the public contact now **defaults to the creating admin** — `/api/admin/setup-tournament` sets `default_contact_member_id` to the creator, and `cloneTournament` inherits the source's contact — editable anytime in Tournament Settings → Event. API derives year from `start_date` (falls back to a passed year for the League/Club org modal). tsc clean, lint 0 errors, dev server restarted + login 200.
- [x] **Public Teams Pages — Card Grid Remodel** — teams list remodeled to card grid (3-col desktop / 1-col mobile) with colored team avatars, inline record/rank/pts, live-game badge detection, division-only filter (no "all"); individual team page remodeled with hero card, stat tiles, form section; built 2026-06-02, browser verified 2026-06-03 (see [plan](docs/projects/archive/PUBLIC_TEAMS_REMODEL_PLAN.md) and [PM brief](docs/projects/archive/PUBLIC_TEAMS_REMODEL_PM_BRIEF.md))
- [x] **Dashboard Customize — In-Place Edit Mode** — separate customize panel replaced with in-place drag-to-reorder, per-card × remove, per-zone + Add menu (with phase-hidden notes), and click-to-edit stat icons; @dnd-kit wired on stat-card + analytics-panel zones (active & completed views); built 2026-06-01, browser verified 2026-06-03 (see [plan](docs/projects/archive/DASHBOARD_CUSTOMIZE_INPLACE_PLAN.md) and [PM brief](docs/projects/archive/DASHBOARD_CUSTOMIZE_INPLACE_PM_BRIEF.md))
- [x] **Public Tournament Pages — Mobile Polish** — design-review remediation: standings mobile table (Team + PTS frozen), empty-state contrast, schedule auto-jump to today, state-dependent home hero, custom-accent contrast floor; built 2026-06-01, browser verified 2026-06-03 (see [plan](docs/projects/archive/PUBLIC_TOURNAMENT_MOBILE_POLISH_PLAN.md) and [PM brief](docs/projects/archive/PUBLIC_TOURNAMENT_MOBILE_POLISH_PM_BRIEF.md))
- [x] **Tournament contact model refactor** — contacts table retired, staff member FKs in place, notify_mode routing live, Event Settings promoted to sidebar, member title field added, removal impact warning wired; migrations 088–090 applied dev + prod (see [plan](docs/projects/archive/TOURNAMENT_CONTACT_REFACTOR_PLAN.md) and [PM brief](docs/projects/archive/TOURNAMENT_CONTACT_REFACTOR_PM_BRIEF.md))
  - [x] Unified sign-in/context switcher foundation: `/home`, shared context resolver, `/auth/select-org` compatibility redirect, and same-org Coaches Portal shortcut scoping
- [x] **Tournament Coach Portal** — authenticated coach accounts at registration, now routed through `/coaches/join` and `/coaches/tournaments` with legacy `/my` redirects, auth-destination routing, resend-access admin action, and workspace invite removed (see [plan](docs/projects/archive/TOURNAMENT_COACH_PORTAL_PLAN.md) and [PM brief](docs/projects/archive/TOURNAMENT_COACH_PORTAL_PM_BRIEF.md))
- [x] **Venue Hierarchy & Org Library** — two-tier venues (Venue → Facilities), Facility Type dropdown, org venue library, import-from-library + import-from-past, schedule builder grouped optgroups, scorekeeper display; migrations 094–096 applied dev + prod 2026-05-25 (see [plan](docs/projects/archive/VENUE_HIERARCHY_PLAN.md))
- [x] **Venue conflict prevention & game timing settings** — block/warn on venue double-booking, tournament-level duration/buffer defaults, division-level overrides, `settings` JSONB on `divisions`/`pools`/`venue_facilities`; migrations 097 applied dev + prod 2026-05-26 (see [plan](docs/projects/archive/VENUE_CONFLICT_PLAN.md) and [PM brief](docs/projects/archive/VENUE_CONFLICT_PM_BRIEF.md))
  - [x] Migration 097: `settings` JSONB on `divisions`, `pools`, `venue_facilities`
  - [x] `lib/schedule-conflict.ts` — pure conflict utilities (`resolveGameTiming`, `checkVenueConflict`, `buildConflictMap`)
  - [x] Event settings page — game duration + buffer inputs (tournament defaults)
  - [x] Division settings — per-division timing override UI
  - [x] Add/Edit game modal — hard block on overlap, soft warn on buffer zone
  - [x] GameList inline editing — conflict check on time/venue change
  - [x] GameList read mode — conflict badges (red/amber) on rows
  - [x] Generator pre-population from tournament settings
- [x] **No-Cost Automated Schedule Generator** — Schedule Health, dashboard summary, scored draft comparison, generate-another-set, priority presets/controls, facility-level selection, no-pool slot drafts, temporary Facility lane resolution, partial regeneration V1, durable manual game locks, and manual travel/setup buffers implemented; migrations 104-105 applied dev + prod; browser verified 2026-06-03; remaining optional pair-by-pair venue buffer refinement (see [plan](docs/projects/archive/AUTOMATED_SCHEDULE_GENERATOR_PLAN.md) and [PM brief](docs/projects/archive/AUTOMATED_SCHEDULE_GENERATOR_PM_BRIEF.md))
- [x] **Divisions UX Rework & Tournament Settings Inheritance** — scope controls for fees/game timing/tie-breakers at tournament level, dashboard activation gate, divisions modal inheritance model, table restructure (see [plan](docs/projects/archive/DIVISIONS_UX_REWORK_PLAN.md))
  - [x] Phase 1 — PlayoffWizard: remove tie-breaker step (no effect on bracket generation)
  - [x] Phase 2 — DB & types: `game_timing_scope`, `tie_breakers`, `tie_breaker_scope`, `fee_scope` in `tournaments.settings`; migration 102; API patch-settings additions
  - [x] Phase 3 — Event Settings: scope selectors for all three categories, tie-breaker section, fee Free toggle
  - [x] Phase 4 — Dashboard: `hasGameTiming` + `hasTieBreakers` + repurposed `hasFees` checklist items; grandfathering for active/completed tournaments
  - [x] Phase 5 — Divisions modal: inheritance model (hide/show/require based on scope), Advanced accordion, Age Range merge, Display Order on Edit only
  - [x] Phase 6 — Divisions table: `TournamentAdminHeader`, Teams fill column, drop Order column, `btn-data` fixes, empty state upgrade, loading state
- [x] **Tournament Admin Settings Restructure** — flatten Settings & Access hub, move tournament identity + lifecycle into Event Settings, `+` create button on sidebar switcher, Registration Questions to sidebar Setup group, Notification Prefs to sidebar Admin group, tier-aware card visibility, fix locked-card upgrade path (see [plan](docs/projects/archive/TOURNAMENT_SETTINGS_RESTRUCTURE_PLAN.md) and [PM brief](docs/projects/archive/TOURNAMENT_SETTINGS_RESTRUCTURE_PM_BRIEF.md))
  - [x] Phase 1 — Event Settings: Tournament Name, Slug (owner-only, with warning), Year, Status selector (Draft/Active/Completed), slug availability check
  - [x] Phase 1B — Dashboard: Archive button (completed + owner only, confirm modal)
  - [x] Phase 2 — Sidebar: `+` create button with slot-limit gating (TournamentSetupWizard modal)
  - [x] Phase 3 — Settings & Access hub: removed 3-tab structure, flat 3-card grid (Reg Questions/Staff/Billing), locked cards are now links to upgrade, League/Club gets redirect message
  - [x] Phase 4 — Sidebar: Registration Questions nav item (Setup group, owner/admin only), Notification Prefs in Admin group, Event Settings role-gated (owner/admin), Settings & Access hidden for League/Club
- [x] **Bracket Graph-Layout** — COMPLETE — DEPLOYED to production 2026-06-19 (owner-verified). Columns + connectors derive from the Winner/Loser feed graph (admin + public + PDF), so renamed/legacy (`G#`) brackets render as a clean tree; custom round names persist (mig 134 applied dev + prod). Dissolved the prod legacy-bracket break. **ARCHIVED** (see [plan](docs/projects/archive/BRACKET_GRAPH_LAYOUT_PLAN.md) and [PM brief](docs/projects/archive/BRACKET_GRAPH_LAYOUT_PM_BRIEF.md))
- [ ] **Standings Page Remodel** — view toggle (Standard / Race to Playoffs), podium cards + cutoff line, follow-bar redesign, live playoff bracket (visual tree desktop / rounds list mobile), bracket flips above standings when pool play completes (see [plan](docs/projects/active/STANDINGS_REMODEL_PLAN.md) and [PM brief](docs/projects/active/STANDINGS_REMODEL_PM_BRIEF.md))
- [ ] **Notifications & Web Push** — in-app bell, per-user preferences, PWA + web push to phone (no native app) (see [plan](docs/projects/active/NOTIFICATIONS_PLAN.md) and [PM brief](docs/projects/active/NOTIFICATIONS_PM_BRIEF.md))
  - [x] Phase A — Migration 101 (notifications, push_subscriptions, notification_preferences, tournament_notification_preferences) + `lib/notify.ts` + `/api/notifications` route
  - [x] Phase B — `NotificationBell.tsx` + `NotificationPanel.tsx` + wire into AdminSidebar + CoachesSidebar
  - [x] Phase C — Wire `notify()` into existing API routes: `registration_new` (public + manual), `registration_status_changed` (accept/reject), `score_submitted`, `house_league_registration_new`
  - [x] Phase D — Preferences page (`/admin/org/notifications`) + API route + sidebar nav item; per-tournament opt-out page + API route; tournament Setup nav "Notifications" link added
  - [x] **Phase D bug fix — Inverted toggle semantics** — per-event toggles show ON=muted (backwards); fix is UI-only, no DB/API changes (see [fix plan](docs/projects/archive/NOTIFICATION_TOGGLE_UX_FIX.md))
  - [x] Phase E — PWA manifest, service worker, VAPID keys, `lib/web-push.ts`, push subscribe/unsubscribe APIs, `PushPermissionPrompt.tsx`, push dispatch in `notify.ts`; `PushPermissionPrompt` wired into org prefs page; VAPID keys in `.env.local` (Amplify env vars required before deploying)
  - [ ] Phase F — Scheduled deadline reminders (deferred; decide cron mechanism first)
- [x] **Rules & Resources UX improvements** — custom modals, inline add-section, Browse Samples drawer, public page suppression; browser verified 2026-05-27 (see [plan](docs/projects/archive/RULES_PAGE_UX_IMPROVEMENTS_PLAN.md) and [PM brief](docs/projects/archive/RULES_PAGE_UX_IMPROVEMENTS_PM_BRIEF.md))
  - [x] Phase 1 — Custom confirmation modals (delete section, delete resource)
  - [x] Phase 2 — Inline add-section blank-card flow
  - [x] Phase 3 — Browse Samples drawer (`rules-samples.ts`, `SamplesDrawer.tsx`)
  - [x] Phase 4 — Public page: suppress empty sections, remove fallback rules
  - [x] Browser verification
- [x] **Tournament settings JSONB + layout controls** — `tournaments.settings` JSONB column, per-tournament rules/resources layout toggles; browser verified 2026-05-27 (see [plan](docs/projects/archive/TOURNAMENT_SETTINGS_LAYOUT_PLAN.md))
  - [x] Migration 086: `tournaments.settings jsonb NOT NULL DEFAULT '{}'`
  - [x] Types: `TournamentSettings` interface + `settings?` on `Tournament`
  - [x] DB: `mapTournament` reads settings; `updateTournamentSettings` helper
  - [x] API: `patch-settings` action with key whitelist
  - [x] Admin: layout toggle UI in Rules & Resources section headers
  - [x] Public page: applies `rulesGridSingle` / `resourcesGrid` CSS variants
  - [x] Apply migration to dev + prod DB
  - [x] Browser verification
- [x] **Tournament Plus Repeat-Event Setup V2** — Summary-page, setup-wizard, draft-dashboard, tournament-list, configurable copy options, stale-source guidance, analytics metadata, and help alignment implemented; remaining V2 work is browser review (see [plan](docs/projects/archive/TOURNAMENT_PLUS_REPEAT_EVENT_SETUP_PLAN.md) and [PM brief](docs/projects/archive/TOURNAMENT_PLUS_REPEAT_EVENT_SETUP_PM_BRIEF.md))
- [ ] **Registration Command Center V1** — Teams / Registrations page attention strip plus dashboard attention panel for pending review, waitlist, unpaid, past-due, missing intake, and unplaced registrations; implementation underway (see [plan](docs/projects/active/REGISTRATION_COMMAND_CENTER_V1_PLAN.md) and [PM brief](docs/projects/active/REGISTRATION_COMMAND_CENTER_V1_PM_BRIEF.md))
- [ ] **Unified Coaches Portal** - one portal for tournament-only coach records, paid standalone Coaches Portal workspaces, org-billed coach access, Club coach access, upgrade, cancellation fallback, and legacy route migration into `/coaches` (see [project plan](docs/projects/active/COACHES_PORTAL_UNIFIED_PROJECT_PLAN.md) and [PM brief](docs/projects/active/COACHES_PORTAL_UNIFIED_PM_BRIEF.md))
  - [x] Phase 1 route migration: `/coaches/join`, `/coaches/tournaments`, `/coaches/start`, paid claim/checkout aliases, auth destinations, emails, nav, and `/my`/`/team` compatibility redirects
  - [x] Remaining copy/help/admin-support audit for old "Team workspace" language
  - [x] Phase 2B team-centric Basic Coaches Portal: persistent Basic coach team profiles, returning-coach team selector during tournament registration, registrations grouped by team, and upgrade path from Basic team history to Premium team management
  - [x] Phase 3 unified Premium workspace experience: `/coaches` coach-specific portal home, `/coaches/teams` premium selector, linked tournament history inside Premium team dashboards, upgrade CTA suppression for active Premium coaches, and entitlement/security confirmation
  - [x] Billing shelf UX: subscription page now treats only Tournament -> Tournament Plus as a true upgrade; Coaches Portal, League, and Club are adjacent product options
  - [ ] Phase 4 billing/cancellation: Coaches Portal billing copy, Premium cancellation fallback to Basic records, canceled Premium access suppression, 90-day Premium retention, and duplicate-free reactivation implemented; remaining work is direct cancellation/payment-failure browser simulation
- [ ] **Founding Season go-to-market** — Tournament Plus free through Dec 31, 2026 for founding orgs; homepage/pricing callouts, in-app billing banner, 4-email conversion sequence, platform admin tracking, January 2027 conversion flow (see [plan](docs/projects/active/FOUNDING_SEASON_PLAN.md) and [PM brief](docs/projects/active/FOUNDING_SEASON_PM_BRIEF.md))
  - [x] Phase 1A: Homepage founding season callout (hero eyebrow + pricing card)
  - [x] Phase 1B: Pricing page Tournament Plus badge + founding season note
  - [x] Phase 1C: `/for-tournament-organizers` founding season section
  - [x] Phase 1D: Qualifying question in onboarding wizard + API endpoint
  - [x] Phase 2A: Billing page founding season banner + API endpoint
  - [x] Phase 2B: Onboarding founding season welcome card
  - [x] Billing upgrade bug fix: Tournament to Tournament Plus founding-season upgrades apply in-app without Stripe and stamp the comp-period marker
  - [x] Phase 4A: Auto-assign `comp_period` override on org signup
  - [x] Phase 4B: Platform admin founding season filter + cohort metric
  - [x] **Founding Season email infrastructure** — opt-out/unsubscribe (migrations 099–100, `lib/unsubscribe-token.ts`, `/unsubscribe` route + confirmed page), `lib/email-sender.ts`, `founding_welcome` template, platform admin `/platform-admin/email` dashboard (scheduled sends + sent history + opt-outs + preview modal + manual send trigger), `founding_welcome` wired into signup route; **migrations 099–100 need manual DB apply** (see [plan](docs/projects/archive/FOUNDING_SEASON_EMAIL_INFRA_PLAN.md))
  - [x] Phase 3 + Phase 5: Founding season email templates (9 emails) — founding_checkin, founding_renewal, founding_final, spotlight_club/league/coaches_org/coaches_coach/club_last/full_picture; TEMPLATE_REGISTRY all built:true; audience functions for coach/club-last variants; dashboard previews all live
  - [ ] Phase 4C: January 2027 conversion flow — blocked on Stripe Phase G
- [x] **Platform Admin: User Management Actions** — Ban/Unban, Confirm Email, Edit Info, Revoke Sessions, Delete User, Support Notes; migration 103 applied dev + prod (see [plan](docs/projects/archive/CUSTOMER_USER_MANAGEMENT_PLAN.md))
- [ ] **User Management UX — Tournament & Tournament Plus** — broken upgrade links, plan context in seat banner, empty state, role guide seat model, subtitle copy, manage modal clarity (see [plan](docs/projects/active/USER_MANAGEMENT_TOURNAMENT_UX_PLAN.md) and [PM brief](docs/projects/active/USER_MANAGEMENT_TOURNAMENT_PM_BRIEF.md))
- [ ] **Brand Strategy** — umbrella positioning, four segment profiles, site architecture, Coach Portal bridge messaging (see [BRAND_STRATEGY.md](docs/agents/brand/BRAND_STRATEGY.md))
- [ ] **Brand Foundations (GTM supplement)** — mission statement, elevator pitch, 0→50→500 acquisition sequencing, push+pull outreach plan, coach advocacy program, partnership motion, SEO content priorities, competitive differentiation framework (see [BRAND_FOUNDATIONS.md](docs/agents/brand/BRAND_FOUNDATIONS.md))
- [x] **Persona landing pages** — built `/for-tournament-organizers`, `/for-leagues`, `/for-clubs`, `/for-coaches`; design QA pass complete 2026-05-25
- [x] **Homepage persona routing** — persona grid moved into hero above the fold; nav updated to persona-first links; `/for-` paths added to marketing nav detection
- [x] **Pricing page copy** - approved copy applied to `app/pricing/page.tsx`; canonical record in `docs/agents/brand/PRICING_PAGE_COPY.md`, with Coaches Portal unification tracked in the unified plan

---

- [x] **Shared Design/UX agent guidance** - Claude `/design` and `/ux` commands now point to shared Claude/Codex review guidance, with a Codex sub-agent coordination playbook in `memory/agents/`.

- [x] **Standalone Team Phase 1 foundation migration** - Migration 065 applied in dev and production.

- [x] **Standalone Team Phase 2A** - Team provisioning service implemented: lightweight workspace org, rep team, active program year, coach assignment, workspace row, entitlement row, team ledger, and dev seed route.

- [x] **Standalone Team Phase 2B** - Direct Team checkout plumbing implemented: Team checkout API, mock provisioning path, Stripe metadata, webhook provisioning recovery, subscription sync, and checkout completion redirect shell.

- [x] **Standalone Team Phase 2C** - Team-first landing and access gates implemented: Team workspaces default to the coaches portal, coaches landing/success copy is Team-aware, coach APIs enforce team entitlement plus active coach assignment, and org-wide rep-team admin remains module-gated.

- [x] **Standalone Team Phase 2D** - Legacy public Team signup surface implemented: collects team, season, billing, and coach account details, creates/signs in Team-only coach accounts, starts the existing Team checkout flow, and exposes the path from pricing/marketing navigation.

- [x] **Standalone Team Phase 2E** - Stripe sandbox checkout verification complete: readiness checker, Dev Tools mock-billing toggle, setup checklist, and real Stripe sandbox org Team add-on checkout/webhook smoke are implemented and passing.

- [x] **Standalone Team Phase 3A** - Tournament-to-Team claim funnel first slice implemented: secure claim links, prefilled claim activation page, checkout email verification, registration confirmation CTA, source tournament/team attachment, and dev claim smoke path.

- [x] **Standalone Team Phase 3B** - Organizer Team claim invites implemented: tournament admins can select team registrations, send secure Team workspace claim invitations, skip ineligible/already-claimed teams, and review generated links from the teams admin screen.

- [x] **Standalone Team Phase 4A** - Basic Team-to-org linking implemented: Team coaches can request a parent org visibility link, org owners/admins can approve or decline, link history is auditable, and help docs now explain the coach and org-admin workflow.

- [x] **Standalone Team Phase 4B** - Org-initiated Basic Team link invitations implemented: org owners/admins can invite standalone Team workspaces, coaches can accept or decline from the Coaches Portal, and acceptance preserves billing, ownership, data, and org-wide rep-team access boundaries.

- [x] **Standalone Team Phase 4C** - Org billing takeover implemented: linked Team coaches can request org billing or accept org billing invitations, org owners/admins can approve and complete the org Team add-on checkout/mock application, and the Team remains coach-operated with Basic sharing only.

- [x] **Standalone Team Phase 5A** - Ownership transfer approval foundation implemented: linked Team coaches can request ownership transfer, org owners/admins can invite or approve it, coaches can accept/decline org invitations, and mutually approved transfers are held in an auditable platform-assisted state before data reassignment.

- [x] **Standalone Team Phase 5B** - Platform-assisted ownership transfer completion implemented and smoke-tested: migration 067 applied in dev/prod, platform admin can complete mutually approved transfers from org detail, and completion moves team-scoped rep-team data/ledger ownership while retiring Team entitlements and prior workspace access.

- [x] **Standalone Team Phase 6A** - Coach-managed attendance implemented and smoke-tested: migration 069 applied in dev/prod, and coaches can mark event attendance for active roster players from the Coaches Portal schedule without changing linked-org visibility or ownership boundaries.

- [x] **Standalone Team Phase 6B** - Roster positions and lineup builder implemented and smoke-tested: migrations 070 and 071 applied in dev/prod; Coaches Portal schedule supports 9 player ball/everyone bats lineup planning and lineup PDF export, and the focused lineup smoke passes.

- [x] **Standalone Team Phase 6C** - First-run Team workspace checklist implemented and smoke-tested: the coach team overview now shows data-driven season setup progress for roster, positions, calendar, lineups, budget, and optional parent-org linking without changing linked-org visibility or ownership boundaries.

- [x] **Standalone Team Phase 7A** - Segment-first public pricing implemented and smoke-tested: `/pricing` now separates tournament, organization, and one-team buyers, sends Team buyers to the seasonal signup path, and highlights implemented Team value without changing org onboarding.

- [x] **Standalone Team Phase 7B** - Legacy public Team landing page refreshed and smoke-tested: the page explains the coach-specific season workspace, tournament continuity, free-tier local tournaments, and implemented Coaches Portal value while preserving the existing signup/checkout flow.

- [x] **Standalone Team Phase 7C** - Tournament-to-Team CTAs updated and smoke-tested: registration confirmation, public tournament banner, registration/claim emails, and post-event results emails now point coaches toward Team season workspaces while preserving existing tournament organizer CTAs.

- [x] **Standalone Team Phase 7D** - Billing-page upsells implemented: Coaches Portal billing points coaches to the parent-org link/org billing flow, and org billing/Coaches Portal Links show a Club value nudge at 3+ active org-billed Premium portals without changing access or ownership.

- [x] **Standalone Team Phase 8** - Focused launch verification complete: pricing, direct mock checkout, tournament-claim mock checkout, Team free-tier tournament limit, Basic org linking, attendance/RLS, lineups/checklist, and ownership transfer smokes pass; remaining Stripe/mobile/cancellation items are manual launch checks.

- [x] **Standalone Team Phase 9** - Help documentation and launch cleanup complete: coach/org/platform-admin help explains Team, season rollover, local tournaments, linking, billing transfer, ownership transfer, Team add-ons versus Club, and owner launch readiness; stale Club extra-team pricing copy now shows $19/$190.

- [ ] **Mobile app strategy** - PM brief for a focused FieldLogicHQ companion app versus mobile web, including level of effort, release path, store considerations, and MVP scope (see [PM brief](docs/projects/active/MOBILE_APP_STRATEGY_PM_BRIEF.md))

- [x] **Help center platform-admin documentation UX** - Searchable customer help hub plus protected platform-admin SOP documentation for employee support workflows (see [implementation plan](docs/projects/archive/codex_HELP_CENTER_PLATFORM_ADMIN_PLAN.md) and [PM brief](docs/projects/archive/codex_HELP_CENTER_PLATFORM_ADMIN_PM_BRIEF.md))
  - [x] Initial browser verification completed
  - [x] Customer-facing task-recipe content pass added across major guides
  - [x] Platform-admin Help opens separately and omits the admin side navbar on help pages
  - [x] Role-based getting started paths added for customer and platform-admin help hubs
  - [x] Accepted complete by user; future refinements are usage-driven

- [x] **League onboarding wizard** - Guide League/Club owners through first house league setup with optional tournament setup branch (see [LEAGUE_ONBOARDING_WIZARD_PLAN.md](docs/projects/archive/LEAGUE_ONBOARDING_WIZARD_PLAN.md) and [PM brief](docs/projects/archive/LEAGUE_ONBOARDING_WIZARD_PM_BRIEF.md))
  - [x] First implementation: league startup tasks, onboarding wizard, remaining-step CTAs, and create-season modal alignment
  - [x] Browser verification

- [ ] **UAT Agent** — Browser-based acceptance test suite + `/uat` slash command; Playwright scenarios for auth, plan-gating, tournament-admin, platform-admin, coaches; two-phase run → propose → sign-off workflow (see [UAT_SETUP.md](UAT_SETUP.md))
  - [x] Playwright installed, `playwright.config.ts` created
  - [x] Auth setup (`tests/uat/auth.setup.ts`), fixtures, and type helpers
  - [x] 5 scenario suites: auth, plan-gating, tournament-admin, platform-admin, coaches
  - [x] `/uat` slash command (`.claude/commands/uat.md`) with mandatory sign-off gate
  - [x] Install Playwright browsers (`npx playwright install chromium`)
  - [x] Create UAT test accounts in dev Supabase and populate `.env.local` (see UAT_SETUP.md)

## 🚀 Active Tasks (Priority Order)

- [ ] **Manual Pool Assignment** — Let organizers place teams into specific pools by hand (inline per-team dropdown + bulk "Move to pool" via Select Many), always show pool sections even when empty, and nudge to Divisions when no pools exist — instead of the current Randomize-only flow. Fixes a real dead-end (owner got stuck: pools enabled but Teams page only showed "Unassigned"). UI-only on the Teams page; backend already supports `poolId` moves, no migration. Building 2026-07-05 (see docs/projects/active/MANUAL_POOL_ASSIGNMENT_PLAN.md + _PM_BRIEF.md)

- [ ] **Dashboard "Ready to Finalize" guidance** — When a tournament's games are all resolved, the admin dashboard still says "It's game day — enter & review scores" (the top card is calendar-driven, not completion-driven, and its intended "your event has wrapped up — mark complete" state is effectively unreachable). Swap the top card to a "You're ready to finalize" message with a **Mark tournament complete** action once every non-cancelled game is resolved; also retire the contradictory day-after "dates have passed" banner and fix By-Division showing "Playoffs underway" next to a crowned champion. No migration; touches shared tournament-guidance logic + dashboard (→ `/review`). Surfaced via `/ux` from a live screenshot 2026-07-06. PLANNED, no code yet; 3 owner decisions open (see docs/projects/active/DASHBOARD_COMPLETION_GUIDANCE_PLAN.md + _PM_BRIEF.md)

- [ ] **Free-Coach Removal Safeguard** — Fix a confirmed, irreversible data-loss footgun: removing an org admin who is ALSO a free (Basic) coach silently hard-deletes their entire free Coaches Portal (teams/roster/fees/history) + account, with no warning and no notice. P1–P2: broaden the J4-036 "keep the account, remove membership only" safeguard so an active free Coaches Portal counts as off-org presence (preserve account, drop only this org's access) + make the pre-removal warning honest. P3: email the removed person (owner-confirmed). P4: warn platform-admin operators of the free-coach impact before a deliberate account delete (owner-confirmed; stays a hard delete). Logic + UI + reused email; NO migration. HIGH priority (one-click destructive action). PLANNED, not started — surfaced via /helpdesk (see docs/projects/active/FREE_COACH_REMOVAL_SAFEGUARD_PLAN.md + _PM_BRIEF.md)

- [ ] **Sign-up Invite Guard ("You've been invited" catch)** — Catch an already-invited person at the owner sign-up screen instead of walking them into "Create Your Organization": a server-side guard so signup never mints a stray org (or clobbers the invited stub's credentials) for an invited/existing email, plus an adaptive sign-up UI that recognizes the email and offers "Email me my invitation link" (reuses the shipped mig-128 reconciliation + resend link logic). Completes the signup-door gap the reconciliation project explicitly left open (its old Phase 3 was superseded; only the "I was invited" tile was guarded). Surfaced via /helpdesk 2026-07-06 (bryan.doucette@live.com, Milton). **BUILT on dev 2026-07-06 (Phases 1–3; typecheck + focused lint clean; NO migration); both decisions locked to recommendations (submit-time detection only; escape hatch → "use a different email"). Pending /review (HIGH, auth) + owner browser verification.** (see docs/projects/active/SIGNUP_INVITE_GUARD_PLAN.md + _PM_BRIEF.md)


- [ ] **Insights Weekly Digest + Smarter Findings** — Sunday-evening "your week in review" notification (bell + push, per-recipient capability-filtered, quiet week = no send) riding the existing notification system, plus four new finding rules in the Insights registry (dues-deadline proximity, game-vs-practice attendance split, position-coverage risk, momentum/milestones). Owner approved from browser mockup 2026-07-10; BUILDING (see docs/projects/active/COACH_INSIGHTS_DIGEST_PLAN.md + _PM_BRIEF.md)

### ⚡ Next Priority After Design Review

- [ ] **Multi-Sport Tournaments** — Make tournaments work for any sport (not just softball) via a `sport` field chosen at creation + a per-sport "Sport Pack" registry driving labels/defaults (score word, tie-breakers, points-per-win, diff-cap on/off, surface, period word, countdown verb). First non-softball pack = **Basketball**. Engine already generic (scores are plain numbers, ties handled, courts/rinks supported); work is centralizing the sport list, adding the sport column, and making ~10 "Runs" label surfaces pack-driven. Also fixes a pre-existing bug (clone/populate-from silently drop the settings blob). From owner review + 8-dimension adversarial audit (68 findings, 17 architectural verified); 5 owner decisions LOCKED (basketball=win%, picker=tailored-only+Other, core/not-gated, editable-with-warning, existing=softball). **Phase 0 + Phase 1 BUILT (dev).** P0 = shared sport foundation + 4 dropdowns consolidated + clone/populate settings-drop fix (+ /review hardenings). P1 = mig 136 `tournaments.sport` (DEV-applied, ⚠PROD-PENDING) + threaded + carried on clone/populate; **picker HELD to Phase 2** (owner decision) so basketball reads correctly end-to-end — NO organizer-facing change yet. P0+P1 DEPLOYED to prod (migs 135/136/137). **Phase 2 signed off (brief + 6 decisions incl. org-primary-sport default-not-lock) but ⏸ PAUSED 2026-06-24** — owner focusing on softball/baseball first; resume after. **⚠ Build guardrail while paused:** keep new softball/baseball feature work sport-neutral (route via the `lib/sports.ts` Sport Pack / `getSportPack`, reuse `DEFAULT_SPORT`/`SPORT_OPTIONS`, scores+standings math generic) so adding basketball later = add a pack, not rework — see memory/feedback_sport_neutral_no_debt.md. (see docs/projects/active/MULTISPORT_TOURNAMENTS_PLAN.md + _PM_BRIEF.md + _PHASE1_PM_BRIEF.md + _PHASE2_PM_BRIEF.md)
- [ ] **Coach Experience — Guided UX/Design Walkthrough** — owner-driven step-by-step walk of the full coach journey (register → portal pending/accepted → emails → free tier → upgrade → premium) on `feat/free-tier-coaches`; the deferred combined coach-surface design/UX pass. Test coach `b2cowan@outlook.com` reset to clean slate 2026-06-15 (demos preserved); organizer = owner@dev.local. Gate before the Coaches marketing flip (Free-Tier Phase 9) (see docs/projects/active/COACH_EXPERIENCE_WALKTHROUGH_PLAN.md + _PM_BRIEF.md)
- [ ] **Premium Coaches Portal — Walkthrough & Evaluation** — owner-driven hands-on walk of the *paid* Coaches Portal (the destination after upgrade): Overview, Roster, Schedule, Chat, Announcements, Accounting (budget/dues/expenses/fundraisers/allocations/payment-requests), Documents, History, Settings. Eval-first (recommend → owner decides → build). Checks migrated free-team data integrity, new-team empty states, head/assistant gating, mobile. Gates the Premium prod release. Started 2026-06-26 (see docs/projects/active/PREMIUM_COACHES_PORTAL_WALKTHROUGH_PLAN.md + _PM_BRIEF.md). **Player-profile Wave B queued** (new fields: emergency/medical, throws/bats, jersey size + read-only attendance/dues — needs a small migration; see docs/projects/active/PREMIUM_COACHES_PORTAL_PLAYER_PROFILE_WAVE_B_PLAN.md + _PM_BRIEF.md)
- [ ] **Coach Lineup Builder enhancements** — turn the lineup grid into an assistant: conflict + completeness checks, fair-play tally (no back-to-back sits), one-click auto-generate (Competitive / Balanced / Development position policies, even bench rotation always-on, fill-empty-only vs regenerate), Clear, blank-vs-bench semantics, default innings from sport, batting order = roster order (with discoverability hint), dugout-wall pen-fillable PDF, and named lineup templates ("Gold medal game"). **Phases 1–4 all BUILT (dev) 2026-06-28, each /review-passed** — P1 checks/fairness + P2 auto-generate (prior session); P3 dugout poster + batting-order card PDFs; P4 named templates (**mig 159 dev-applied, ⚠ PROD-PENDING**). Pending owner browser-verification + prod deploy (apply mig 159 to prod first). See docs/projects/active/COACH_LINEUP_BUILDER_PLAN.md + _PM_BRIEF.md
- [ ] **Schedule & Event Experience rework (Tier 1)** — proactive UX+design eval (2026-06-28, verified) found the Schedule is built as one generic form + one over-stacked detail panel for all 6 event types. Tier 1 (no migration): P1 per-type **grouped** event form (When/Where/Who/Notes; auto-derive game name; prominent default Home/Away; type-switch keeps data) → P2 **slimmer** detail panel (compact header, reach attendance/lineup without scrolling, Edit-prominent action row, mobile bottom-sheet) → P3 multi-day **tournament date-range + calendar spanning + game-slot attach**. Owner decisions captured (per-type forms OK, uniform = own field). Tier-2 fast-follow (small migration): arrival/call time + field/diamond + uniform fields; recurrence for games/team events + series-edit scope; **event resources — attach labelled links (YouTube drills, rules pages, practice plans) V1, file uploads V2 via Documents storage; coach-facing today, parent-ready later**. W-L-T DECIDED = default league+tournament (exclude scrimmage) + live per-category toggles that remember the choice. **P1+P2+P3 BUILT on `dev` (uncommitted) — P3 (2026-06-29): multi-day tournaments span every day in week/month views + read as a date range in list; "Game (Tournament)" nested under "Tournament" in the add menu + a "Which tournament?" attach selector that blocks orphaned game slots; NO migration; /review-passed (1 low fix), typecheck+lint clean, dev restarted.** **Tier-2 game-day fields BUILT on `dev` 2026-06-29 (mig 160 DEV-applied / PROD-PENDING): arrival/call time + field/diamond # + uniform(games) on the form, detail header, spreadsheet + calendar exports; venue DECIDED = "smart free-text" (NO venue library — org library is admin/League-Club-only & empty for standalone teams; org-venue suggestions a clean future add). **Location split into NAME + optional ADDRESS (mig 161): name shows on schedule + themed "recent" chips that refill both; address powers the Google Maps link (replaced the native datalist popup that rendered as an unstyled white box).** /review-passed high-risk (1 low fix), typecheck+lint clean, dev restarted. ⚠ apply migs 160 + 161 to prod BEFORE promoting (event create/edit 500s pre-mig; reads safe).** **W-L-T filter BUILT on `dev` 2026-06-29 (NO migration): Season Record defaults to League+Tournament (Scrimmage off), live per-category toggle chips, choice remembered per team, optional per-category Breakdown; widget now shows for any finalized game (was league-only).** Help docs synced (game-day fields + name/address Maps behaviour). **Recurrence/series-edit BUILT on `dev` 2026-06-29 (NO migration after all): league games & team events can repeat weekly (was practice-only); editing a repeating event offers This / This & future / All (mirrors delete), bulk-applying shared fields + time-of-day per scope (each keeps its date). Also FIXED the latent rep_team_events recurrence-anchor defect (first occurrence is now a real anchor, inserted before children; delete "this & future" from the first occurrence now removes it). /review-passed high-risk (independent adversarial pass; pre-empted a self-FK insert-ordering risk).** **Phase 4 event-resource LINKS BUILT on `dev` 2026-06-29 (mig 162 `rep_team_events.resources` jsonb, DEV-applied / PROD-PENDING): a "Links" section on every event (form + detail) — labelled web links w/ per-type hints, tappable icon rows opening in a new tab; typed array reserves room for FILE attachments V2 (no schema change); http(s)-validated + 10-cap, sanitized on all write paths; /review-passed high-risk + clean security pass. ⚠ apply mig 162 to prod before promoting.** **Attendance tab REDESIGNED 2026-06-29 (owner-added, /design-approved, NO migration): compact one-line rows (icon-only status control + on-demand note glyph w/ lime has-note dot) + a metric/filter chip bar (All + In/Late/Out/No-reply, live counts, tap to filter, selected fills status colour); GameChanger-inspired on our palette (No-reply = muted, not blue); ~2× players per screen.** **SCHEDULE & EVENT UX REWORK COMPLETE (P1–P4 + Tier-2 + W-L-T + attendance redesign, all on dev/uncommitted); only Files V2 + a future parent/player surface remain as separate tracks.** Release: apply migs 160/161/162 to prod before promoting. (see docs/projects/active/SCHEDULE_EVENT_UX_PLAN.md + _PM_BRIEF.md)
- [ ] **Coaches Portal — Focused Mobile Pass** — make the Premium Coaches Portal phone-usable surface-by-surface against ONE shared mobile system. **Conventions LOCKED 2026-06-29 (`/design`, logged in memory/design_decisions.md).** **Phase 0 + Schedule + Roster + Overview + Accounting-exemplar BUILT on `dev` (uncommitted) 2026-06-29 — typecheck + focused-lint clean; NO migration; awaiting owner device verification (360–414px) + `/review`.** Phase 0: collapsed breakpoints to 900 shell / 640 content (migrated 600→640 slide-over sheet, 520→640 form-grid) + documented header + 4 shared primitives in coaches.module.css (`.tableAsCards`, `.scrollX`/`.scrollXSticky`, `.sheetOnMobile`, `.stickyActionBar`). Schedule: week→vertical day-stack @640, lineup grid sticky batting#/player columns (mode-aware `--lineup-lead`) + "swipe across innings →" hint, attendance status/note controls →36px @640, event form opts into `.sheetOnMobile` (Save bar clears the home indicator) + Links rows stack + Home/Away seg full-width + tab strip wraps. Roster: drag confirmed disabled on touch → added mobile-only up/down reorder buttons (reuse the reorder endpoint) + status toggle →40px. Overview: audited clean (no changes). Accounting: Dues table → `.tableAsCards` (exemplar). **Remaining: 4 other Accounting tables (expenses/allocations/budget-vs-actual/fundraiser detail — currently controlled-scroll, not silent) + Tournaments/Announcements/Documents/Settings audit.** Key reframe: portal is already partly mobile-built (roster→cards @640, slide-over→sheet @600, lineup scroll + summary chip-swap @640, snapshot auto-fit, safe-area sticky bars) → this is **standardize breakpoints (collapse 900/640/600/520 → 900 shell + 640 content) + extract 5 shared primitives (tableAsCards / scrollX sticky-first-col / sheetOnMobile / stickyActionBar / documented bps) + fill gaps + audit**, not build-from-scratch. Sweep order: **Schedule** (week→day-stack, lineup sticky player col + scroll hint, attendance touch ≥36, event form→sheet, Links rows stack) → **Roster** (audit card reflow + touch drag-reorder) → **Overview** (audit) → rest (Accounting table→cards; Chat already done). Mobile becomes part of "done" for future sections. NO migrations. (see docs/projects/active/COACHES_PORTAL_MOBILE_PLAN.md + _PM_BRIEF.md)
- [ ] **Coach Portal Nav Rebuild — team-scoped shell** — Rework the org-less Coaches Portal shell to be team-scoped like tournament-admin is tournament-scoped: team name + status chip at the rail top (dropdown only when >1 team), nav links become that team's sections, drop the duplicate "My Teams" link + team-list + breadcrumb, add a brand subtitle. PLANNED (staged companion to the walkthrough); build next session with its own `/review` — touches every coach page. 6 open questions pending owner sign-off (see docs/projects/active/COACH_NAV_REBUILD_PLAN.md + _PM_BRIEF.md)
- [ ] **Invite reconciliation + pending-invite UX** — Let an invited user who self-registers/logs in (instead of clicking the email link) get in successfully: reconcile pending invites by email, show a "You've been invited to {org} as {role} — Accept/Decline" card on /home, stop signup from minting a junk org when a pending invite exists, and relax the one-org constraint's missing status filter. From a live prod ticket 2026-06-14. HIGH risk (auth/signup paths); needs Phase-0 migration (`organization_members.invited_email`). PLANNED, no code yet (see docs/projects/active/INVITE_RECONCILIATION_PLAN.md + _PM_BRIEF.md)
- [x] **Signup / org decoupling** — Decouple account creation from org creation: branch on intent at /start (owner path unchanged; new "I was invited" → account-only signup → verify → /home invite card), minimal Phase 3 interstitial retired, platform-admin cleanup (bulk unconfirmed-user filter + empty-org indicator/filter), generic Get-Started CTAs repointed to /start fork. Multi-org formally allowed; plan stays create-org→plan-select. BUILT + /review (HIGH) folded + browser-verified on `feat/free-tier-coaches` (dev); mig 128 applied to PROD. Remaining = code deploy to master (see docs/projects/archive/SIGNUP_ORG_DECOUPLING_PLAN.md + _PM_BRIEF.md)
- [x] **Schedule Publishing Simplification** — collapse `divisions.schedule_visibility` from three states to two (drop the placeholder/"generic names" publish path coaches said is not a real use case): publishing always uses real team names and closes the division (atomic), reopening unpublishes (atomic), slot mode stays admin-draft-only; migration 129 reverts generic rows to unpublished + tightens the CHECK. **BUILT + committed on `fix/fp3-volunteer-dayof` (d4f7475); /review high-risk funnel passed (2 fixes folded); logic+DB verified 2026-06-15 (visual pass + prod apply pending).** ⚠ **mig 129 PROD-PENDING — and `check:migrations` is BLIND to it** (diffs only tables/columns, not CHECK clauses); apply manually to prod before this code reaches master or legacy generic rows leak names publicly. (see docs/projects/archive/SCHEDULE_PUBLISH_SIMPLIFY_PLAN.md + _PM_BRIEF.md + _TEST_PLAN.md)
- [ ] **Schedule Health rules (organizer-defined)** — inline editor on the Schedule Health panel to set max games/day, min rest, and target games/team; live-rates the score/warnings, persists per tournament (`settings.schedule_health_rules`, no migration), and seeds the auto-Generator's max-games/day default. Replaces the hardcoded "more than 2 games/day" threshold (see docs/projects/active/SCHEDULE_HEALTH_RULES_PLAN.md + _PM_BRIEF.md)
- [ ] **Playoffs — Manual brackets (free) + Tiered auto-split (Plus)** — Free tier can build playoff brackets by seed (all formats, manual scheduling) via new `playoff_manual` feature; auto-schedule optimizer stays Plus. New `crossover:'tiers'` splits one round robin into N tiered brackets (e.g. 9 teams → 1–5 / 6–9). BUILT on `feat/free-tier-coaches`, dev only, awaiting browser verification (see docs/projects/active/PLAYOFF_MANUAL_AND_TIERS_PLAN.md + _PM_BRIEF.md)
- [ ] **Playoff Bracket Builder — manual canvas (free) + auto-generator (Plus)** — Free "Build Bracket" → new `ManualBracketBuilder` (reuses the canvas): seed a first round (top-N 1v8/2v7) or start empty, then add rounds/matchups by hand with single-use Seed/Winner/Loser dropdowns + live links; saves as `playoff_manual` (free). The full format-based auto-generator + auto-schedule moved to Tournament Plus ("Auto-Generate Bracket" in the Auto menu, locked+upsell). Also: cascade-clear delete of bracket-dependent games, surfaced silent write failures, Add Game/inline placeholder pickers, and a `BracketBuilder` round-code-collision fix. No migration. **BUILT 2026-06-12 on `feat/free-tier-coaches`, dev only** (typecheck/lint clean, 69 bracket tests, multiple adversarial reviews folded); awaiting browser verification (free-tier test login: `free-owner@dev.local`/`devpass123`). See docs/projects/active/PLAYOFF_BRACKET_BUILDER_UX_PLAN.md + _PM_BRIEF.md
- [x] **Bracket Builder — inline edit mode (manual editing on main screen)** — Move manual bracket editing OUT of the modal onto the Schedule screen as a View⇄Edit mode (inline canvas editor, one surface for List/Bracket/Timeline); modal exists ONLY for the Plus auto-generator (config + uneditable preview); Save is a score-preserving DIFF; hide buggy playoff "Add Game"; round-naming fix. Owner-directed lead initiative. **P1 + P2 + P3 BUILT 2026-06-13 on `feat/free-tier-coaches`, dev only** (P2 + P3 browser-verified): P2 extracted the read-only renderer to shared `components/BracketColumns.tsx` (`readOnly` prop); the wizard shows an uneditable preview and lands in View after Generate. **P3 polish:** live Bracket-Health panel inside the inline `BracketEditor` (org-configured min-rest); List-view playoff rows get an "Edit in bracket" entry opening the canvas focused/scrolled on that game (`focusGameId`→`focusSourceGameId`); mobile canvas touch polish. Timeline stays drag-to-reschedule (canvas entry via toolbar "Edit Bracket"); round-naming was already fixed in P1. typecheck clean, 91 bracket tests, two focused adversarial reviews folded (P2: 13 low; P3: 16 low + 2 medium fixed). No migration (see docs/projects/archive/BRACKET_BUILDER_INLINE_EDIT_PLAN.md + _PM_BRIEF.md)
- [ ] **Inline tiered bracket editing (free) + tiers in diagrams** — Make the manual inline editor tier-aware: load/show tiers as separate sections, build tiers by hand (free, all plans), and fix the public + admin bracket diagrams to split tiers into titled sections (today they only split by pools). **Phases 0 + 1 + 3 BUILT + DEPLOYED to production 2026-06-19** (commit 1a17a4a; mig 137 `games.bracket_label` applied dev+prod + threaded; inline-edit save now preserves each tier's id + name; public + admin diagrams split tiers into titled sections via `groupGamesByBracketId`; typecheck + prod build green; browser verification on prod pending). **Phase 2 BUILT 2026-06-19 dev** (dev-only, awaiting browser verification): editor loads/edits existing tiers as separate titled sections + new "Split into tiers" build-mode control (free, all plans) seeds N tiered brackets from overall standings; typecheck + prod build green. Still pending: persist `crossover:'tiers'`/`tierConfigs` to division (optional consistency nicety) + PDF tier split. 3 owner decisions locked (free / editor+diagrams / store tier name). See docs/projects/active/INLINE_TIERED_BRACKET_EDITING_PLAN.md + _PM_BRIEF.md
- [ ] **Bracket Builder — free vs paid UX improvements** — Roadmap to polish the two tier experiences: Phase-1 P0s = "Edit Bracket" reopen-loaded canvas, optional venue/TBD (structure-first), badge scheduling-order violations in the read-only bracket view, honest tier labels ("Build Bracket Manually" vs "Auto-Generate (Tournament Plus)"), and a live free Bracket-Health panel. Follow-ons: Plus post-generation editing/lock/regenerate-preview, mobile canvas polish, starter templates. 6 owner decisions pending (Edit-Bracket reload?, venue optionality, upsell aggressiveness…). PLANNED 2026-06-12, no migration (see docs/projects/active/BRACKET_BUILDER_TIER_UX_PLAN.md + _PM_BRIEF.md)
- [ ] **Tie-breakers — Coin Toss option + max run-diff-per-game cap** — 5th tie-breaker "Coin Toss" (flags the tie; organizer records the winner in the Standings table + a dashboard nudge → drives standings + playoff seeding) + optional per-game run-diff cap (caps RD column only; RF/RA stay real; per-division overridable). Tie-breaker list is now drag-and-drop + add/remove. **BUILT 2026-06-10 on `feat/free-tier-coaches`, dev only** (typecheck clean, 185 unit tests, 4-dim adversarial review folded, no migration); awaiting browser verification (see docs/projects/active/PLAYOFF_TIEBREAKER_COINTOSS_RUNDIFF_PLAN.md + _PM_BRIEF.md)
- [x] **Architecture Hardening** — Phases 1–4 implemented on dev (cross-org write hole closed across 9 routes; structural tier boundary via org-layout + proxy.ts; UpgradeGate refactor; notify.ts guard; hygiene); `tsc` clean; pending dev-server restart + browser verification (see docs/projects/archive/ARCHITECTURE_HARDENING_PLAN.md)
- [ ] **Stripe Phase G — Go live** — Production Stripe setup is configured and audited; **HIGH IMPORTANCE TODO:** defer the live-card smoke test until closer to paid pricing so the small non-refundable Stripe fee is incurred only when billing launch is near. Must complete before enabling paid self-serve subscriptions or founding-season conversion billing. (See [Stripe production smoke-test TODO](docs/projects/active/STRIPE_PRODUCTION_SMOKE_TEST_TODO.md) and the [Stripe Integration section below](#4-stripe-integration--end-to-end-billing--subscriptions))
- [ ] **Contact visibility toggles (per audience)** — Event Settings → Notifications & Contact now has independent **"Communication with coaches"** and **"Show on public site"** toggles for the contact email (privacy: keep the org-owner email reaching coaches but off the public web). One resolver `resolveTournamentContactEmail(id, orgFallback, 'public'|'coach')` now drives ALL surfaces and resolves the selected contact *member* (was reading legacy `contact_email`/org owner): coach emails (registration/waitlist **+ acceptance/decline/payment**) and the Coaches Portal (status banner + fee line) honor the coach toggle; public tournament pages honor the public toggle. BUILT on `feat/free-tier-coaches`; migration 120 (cols `contact_show_to_coaches` + `contact_show_on_public`) **applied dev + prod** (prod-drift gate green). Code still on the feature branch (not yet on master); needs browser verification, then ships with the next master deploy.

---

- [x] **Divisions rename (age groups → divisions)** — Full-stack terminology rename: DB table `age_groups` → `divisions` (migration 093), all FK columns, TypeScript types, API routes, file names, and UI copy; migration 093 applied dev + prod (see [docs/projects/archive/DIVISIONS_RENAME_PLAN.md](docs/projects/archive/DIVISIONS_RENAME_PLAN.md))
- [x] **Post-rename dead-code cleanup** — Deleted 25-file pre-multi-tenant `app/admin/` directory (redirected by layout, never reachable); fixed critical onboarding bug where `POST /api/admin/contacts` (dropped in migration 090) was still called in `saveSetupDraft` (now sets `contact_email` only); removed dead Contacts nav from AdminBottomNav; migrated `/api/admin/diamonds` → `/api/admin/venues` in all callers; fixed remaining "age group" → "Division" UI copy in team signup, league register, and onboarding

- [ ] **Multi-org creation for existing users** — Logged-in users cannot currently self-serve a second organization; `/auth/signup` creates a new user + org together and rejects existing emails. Build: a `/create-org` page for authenticated users, a `POST /api/auth/create-org` route that creates an org and links the existing `user_id` as owner (no new auth user), and a "＋ Create new organization" entry point on the `select-org` page. This is separate from reactivation — a user who cancelled Tournament Plus and now wants a League workspace is starting a new subscription, not reinstating the old one. (see pending plan doc)

- [ ] **Tournament admin design review** — Systematic design review of all 41 tournament admin pages and shared navigation components; one checkpoint row per page with screenshotted / reviewed / decisions logged / done columns (see [agent_TOURNAMENT_DESIGN_REVIEW.md](docs/agents/design/agent_TOURNAMENT_DESIGN_REVIEW.md))

- [ ] **Tournament owner/admin mobile implementation** - Mobile-first owner/admin improvements for base Tournament, starting with shared foundations and highest-use workflows (see [implementation plan](docs/projects/active/codex_TOURNAMENT_OWNER_MOBILE_IMPLEMENTATION_PLAN.md), [PM brief](docs/projects/active/codex_TOURNAMENT_OWNER_MOBILE_PM_BRIEF.md), and [review](docs/projects/active/codex_TOURNAMENT_OWNER_MOBILE_REVIEW.md))
  - [x] P0 shared mobile foundation slice implemented and static verified.
  - [x] Schedule admin mobile density/readability pass: compact header, balanced status filters, and two-line game rows.
  - [x] Schedule admin follow-up: compact mobile mode selects, registrations-style status filters, locked cancelled-game actions, and published schedule edit flow.
  - [x] Schedule admin toolbar alignment follow-up: desktop filter row keeps filters right-aligned and shows Division label; mobile uses Division row, schedule-local side-by-side mode dropdowns, Venue plus compact actions on one row, then search/status filters; venue-less rows keep matchup alignment.
  - [ ] Authenticated 390x844 browser walkthrough for core admin tournament routes.

- [x] **Export Enhancements** - Standardized XLSX-first exports, CSV secondary, iCal schedules, branded PDF reports, export catalog, help docs, plan gates, and pricing/marketing updates (see [MERGED_EXPORTS_IMPLEMENTATION_PLAN.md](docs/projects/archive/MERGED_EXPORTS_IMPLEMENTATION_PLAN.md))
  - [x] Phase B — Shared export foundation: lib/export/ layer, ExportMenu component, plan feature keys, exceljs (replaces xlsx — CVE), ics, jspdf, jspdf-autotable installed and audited clean
  - [x] Phase C — Migrate 5 existing CSV exports to ExportMenu + xlsx default (schedule, results, registrations, ledger, early-access leads); legacy /admin routes deferred pending Open Decision #1
  - [x] Phase D1 — P0 new table exports: HL registrations, tryout registrations, coaches roster (with contact opt-in), coaches dues, budget-vs-actual
  - [x] Phase E1 — iCal: public tournament schedule — "📅 Add to Calendar" button; all games or team-filtered; standalone button (not in admin ExportMenu); links back to schedule page
  - [x] Phase D2 — P1 new table exports: coaches schedule (xlsx/csv/ics), HL schedule (xlsx/csv/ics), HL standings (xlsx/csv), HL teams (xlsx/csv), accounting budget plan (xlsx/csv), rep roster admin view (xlsx/csv/pdf-coming-soon)
  - [x] Phase E2/E3 — iCal: coaches portal schedule + HL season schedule admin
  - [x] Phase D3 — P2 new table exports: org members, member audit log, venues/diamonds, platform admin orgs, customer users, audit log (xlsx+csv; audit log route now supports format=xlsx)
  - [x] Phase G — Help docs: lib/help-content/exports.tsx, /admin/help/exports page, hub card, cross-links in tournaments + accounting help
  - [x] Phase F1 — DB migration (`ALTER TABLE organizations ADD COLUMN pdf_settings JSONB DEFAULT '{}'` run on dev + prod) + API routes (`GET`/`POST /api/admin/org/pdf-settings`) + PDF Settings admin page (`/{orgSlug}/admin/org/settings/pdf`) with all E3 spec fields + org hub tile
  - [x] Phase F2 — `lib/export/pdf.ts` full implementation: `buildTablePDF()` + `downloadPDF()` with lazy-load jsPDF/autotable, header/logo/accent/footer/density/grouping/page-numbers
  - [x] Phase F3 — PDF P0 surfaces: tournament registrations (portrait, grouped by division), schedule (landscape+compact), results (portrait, grouped, champions callout); PDF settings nudge callout on all three; `HelpCallout` added to registrations page
  - [x] Phase F4 (PDF P1 surfaces) — coaches roster, player dues statement, budget-vs-actual board report; GET pdf-settings relaxed to all org members
  - [x] Phase H (Pricing/marketing) — pricing comparison table (Data & Exports category), plan card feature lines, upgrade bridge copy, platform pages, home page, and org help subscription section

- [x] **Platform admin Phase 1** - Support console foundation complete: nav grouping, customer users search, org support summary, reset links, owner contact, timeline, retention UI cleanup, and billing invariant follow-up (see [merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md](docs/projects/archive/merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md))
  - [x] Free Tournament billing invariant: Tournament plan changes, mock billing, and cleanup migration no longer preserve `trialing` subscription state
- [x] **Platform admin Phase 1.5** - Information architecture and layout pass complete: org detail restructure and platform-admin-wide layout audit for clearer workflows before adding more functionality (see [merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md](docs/projects/archive/merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md))
  - [x] Org detail layout restructured with account snapshot, needs-attention strip, primary actions, and workflow tabs
  - [x] Org detail readability spacing improved for snapshot fields, account context, and workflow tables
  - [x] Platform-admin-wide layout audit documented for Overview, Organizations, Customer Users, Retention, Plans & Pricing, Early Access, Platform Users, and Audit Log
  - [x] Organizations directory cleaned up with account snapshot, needs-attention strip, primary account search, and grouped directory table
  - [x] Organizations directory simplified to read-only account navigation; plan/limit edits moved out of directory rows
- [x] **Platform admin Phase 2** - Metrics command center: live-query dashboard, durable lifecycle events, snapshot infrastructure, and since-last-visit alerts implemented and browser-verified (see [merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md](docs/projects/archive/merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md))
  - [x] Overview dashboard now shows subscription health, growth funnel, product usage, estimated MRR/ARR, and platform admin alerts
  - [x] Overview dashboard expanded with new organizations by plan, early-access conversion rate, top source paths, owner inactivity, and tournaments created in the last 30 days
  - [x] Apply migration 052 in dev and production for reliable tournament-created metrics
  - [x] Durable event-backed Overview metrics for cancellations, downgrades, past-due recovery counts, and recovery rate
  - [x] Apply migration 053 in dev and production for `platform_events`
  - [x] Overview dashboard lower metrics grouped into tabs to reduce scrolling, with clearer metric-note copy
  - [x] Browser verification for Overview metrics and action links
  - [x] Daily metric snapshot table/API and "since last admin visit" metrics
  - [x] Apply migration 054 in dev and production for metric snapshots, admin visits, and structured notes
- [x] **Platform admin Phase 3** - Billing and product safety complete: plan impact previews, expanded audit logging, structured timestamped org notes, override semantics, and audit log investigation tools (see [merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md](docs/projects/archive/merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md))
  - [x] Plans & Pricing now shows subscriber impact and plan/status breakdowns before product or billing config edits
  - [x] Plan availability, limits/trials, and Stripe price ID changes now support last-change notes and audit entries
  - [x] Apply the plan/pricing change-note migration in dev and production
  - [x] Browser verification for Plans & Pricing impact summaries, note capture, and save flows
  - [x] Stripe price ID validation against Stripe where environment credentials allow it
  - [x] Structured timestamped org notes implementation
  - [x] App-native confirmation modal for structured note deletion
  - [x] Browser verification for structured org notes
  - [x] Org detail plan/limit edits moved into guarded Billing & Access workflow with reason and confirmation
  - [x] Audit log investigation tools: CSV export, org filter action, action labels, and full JSON value viewer
  - [x] Browser verification for newest org billing workflow and Audit Log investigation tools
- [x] **Platform admin Phase 4** - Permissions and governance complete: platform admin roles, guarded actions, reasons, confirmations, and role-scoped controls (see [merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md](docs/projects/archive/merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md))
  - [x] Initial role model and permission gates implemented for support, billing, product, read-only, and super-admin roles
  - [x] Apply migration 055 in dev and production for platform admin role constraint/default
  - [x] Browser verification for role-scoped platform admin controls
- [x] **Platform admin Phase 5** - Growth and product catalog complete for launch scope: early-access conversion tracking, plan versions, add-ons, effective dates, campaigns, approvals, and bulk operations (see [merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md](docs/projects/archive/merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md))
  - [x] Early Access conversion tracking implementation: converted timestamp, org link, follow-up due date, next action, conversion metrics, CSV export fields, and growth role support
  - [x] Apply migration 057 in dev and production for early-access conversion fields and growth role constraint
  - [x] Browser verification for Early Access conversion and follow-up flows
  - [x] Product Catalog read-only foundation: plan version records, add-on catalog records, and live feature matrix review tab
  - [x] Apply migration 058 in dev and production for product catalog foundation
  - [x] Browser verification for Plans & Pricing Product Catalog tab
  - [x] Product Catalog governance implementation: change requests, approval statuses, and campaign tracking
  - [x] Apply migration 059 in dev and production for product catalog governance
  - [x] Browser verification for Product Catalog planned changes and campaign tracking
  - [x] Approval enforcement implementation: live plan availability, limits/trials, and Stripe price ID changes require an approved catalog change request
  - [x] Inline Stripe price approval workflow: editing a price row without a selected approval now opens a prefilled Product Catalog request modal, surfaces pending pricing requests on Overview and Plans & Pricing, and applies the proposed price ID automatically when the request is approved
  - [x] Central Change Requests queue: Platform Admin now has a dedicated approval workspace for filtering requests by type, status, attention owner, and submitter, with price requests approved and applied from the queue
  - [x] Change Requests queue follow-up: default Needs Review view, compact modal-driven review table, Severity filter/column, severity-then-updated sorting, and duplicate Stripe-price request closure when the live slot already matches the proposal
  - [x] Plans & Pricing header cleanup: top account/plan impact summaries were compressed, the global Live Change Approval panel was removed, and direct approval selectors now appear only beside Availability and Limits live-change tables
  - [x] Stripe Prices product-first cleanup: main table now lists products once per environment, with monthly/annual price status summaries and a modal for editing individual price IDs
  - [x] Plans & Pricing Product Catalog approval cleanup: duplicate planned-change/status controls were removed from the Product Catalog tab; approvals and implementations now live in the central Change Requests queue
  - [x] Plans & Pricing plan-centered workspace first slice: compact main table now lists plans once with status, impact, pricing, limits/trials, pending changes, and a Manage modal for scoped plan details/edits
  - [x] Plans & Pricing generated request workflow: availability and limits/trials edits now open prefilled review modals and are applied from Change Requests like Stripe price edits; plan modal pricing uses one table for subscription and related add-on prices
  - [x] Apply migration 060 in dev and production for catalog approval application logging
  - [x] Browser verification for approved-request enforcement on live pricing/config changes
  - [x] Feature Matrix draft editor implementation: product admins can toggle proposed plan/module entitlements and save the proposal as a Product Catalog change request
  - [x] Browser verification for Feature Matrix draft editor
  - [x] Feature Matrix publishing implementation: approved feature-matrix requests can be previewed and published into the live product catalog matrix with audit/application logging
  - [x] Apply migration 062 in dev and production for feature matrix publishing
  - [x] Browser verification for approved Feature Matrix publishing
  - [x] Bulk operations foundation implementation: role-gated account selection, preview, required reason, confirmation, batch logging, and audit entries for status overrides, comp-period grants, and plan changes
  - [x] Apply migration 063 in dev and production for platform bulk operation batch records
  - [x] Browser verification for bulk status override, comp-period grant, and plan change workflows
  - [x] Bulk module/add-on enablement implementation: product-gated bulk enable/remove controls for organization-specific module overrides
  - [x] Apply migration 064 in dev and production for bulk module/add-on batch records
  - [x] Browser verification for bulk module add-on enable/remove workflows
- [x] **Platform admin Phase 6** - Final readability QA complete; remaining punch-list items are deferred as future usage-driven cleanup (see [merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md](docs/projects/archive/merged_PLATFORM_ADMIN_IMPLEMENTATION_PLAN.md))
  - [x] Shared Platform Admin shell responsiveness: sidebar stacks into a top navigation band on narrower screens and main content padding is reduced for mobile scanability
  - [x] Browser verification for shared Platform Admin shell on desktop and mobile widths
  - [x] Header/action-label consistency pass for Retention Queue, Platform Users, and Audit Log
  - [x] Complete final section-by-section readability punch list
  - [x] Browser verification for Phase 6 header/action-label consistency pass
  - [x] Plans & Pricing Product Catalog density cleanup: split catalog work into Planning, Feature Matrix, and Catalog Records inline workspaces
  - [x] Browser verification for Plans & Pricing Product Catalog sub-workspaces
  - [x] Remaining Phase 6 punch-list items deferred to future usage-driven cleanup
- [ ] **Future platform-admin Plans & Pricing workspace follow-up** - Consider folding Feature Matrix editing/publishing deeper into the plan detail workflow; keep Product Catalog/Campaigns separate unless catalog records become directly editable

- [x] **Tournament Plus enhancements** - Launch scope complete: Plus is repositioned as the serious tournament operations plan, with a clear free starter tier, coach acquisition loops, registration control tools, organizer productivity, post-event reporting, and measured upgrade funnels (see [TOURNAMENT_PLUS_ENHANCEMENT_PLAN.md](docs/projects/archive/TOURNAMENT_PLUS_ENHANCEMENT_PLAN.md))
  - [x] Phase 0 - Packaging, metrics, and feature taxonomy
  - [x] Phase 1 - Free vs. Plus boundary and branding gate
  - [x] Phase 2 - Coach acquisition and public growth surfaces
  - [x] Phase 3 - Registration Control Bundle
    - [x] Custom registration questions, private file collection, admin answer details, CSV export foundation, and Plus gates
    - [x] Migration 056 applied in dev and production
    - [x] Bulk action bar, dedicated bulk route, and waitlist tracking
    - [x] Email-selected workflow via Phase 5.2 targeted communication
    - [x] Browser verification for registration control and communication workflows
  - [x] Phase 4 - Payment and deposit value
    - [x] Plus-gated payment dashboard, payment status filters, selected-team payment reminders, and payment-ready CSV fields
    - [x] Online tournament payment collection intentionally deferred to a separate research and architecture decision
  - [x] Phase 5 - Organizer Productivity Bundle
    - [x] Plus-gated tournament cloning into a draft setup without registrations, payments, games, scores, or private notes
    - [x] Targeted announcements and selected-recipient communication workflow
    - [x] Tournament staffing guidance and setup-template follow-up
    - [x] Settings & Access layout uses tabs for Tournament setup, People & access, and Account sections
    - [x] Tournament Plus upgrade CTAs stay inside tournament admin subscription/pricing instead of org admin billing
  - [x] Phase 6 - Post-event summary, results, and renewal loop
    - [x] Retention strategy, next-tournament prompts, and export-compatible summary/report plan
    - [x] Plus-gated manual post-event summary API/page with registration, payment, schedule, division recap, print/share actions, and completed/archived nav entry
    - [x] Automatic results notification implementation and renewal CTA tracking
    - [x] Apply migration 061 in dev and production before testing the notification toggle
  - [x] Phase 7 - Rollout, QA, and documentation
    - [x] Rollout code audit and tournament help documentation updates
    - [x] Final browser smoke pass for Phase 3 registration control before external Plus marketing

- [ ] **Online tournament payment collection research** - Decide whether future tournament entry payments should use Stripe Connect, manual payment links, or another architecture before building payment processing (see [TOURNAMENT_PLUS_ENHANCEMENT_PLAN.md](docs/projects/archive/TOURNAMENT_PLUS_ENHANCEMENT_PLAN.md) Phase 4.3)

- [x] **Tournament scorekeeper experience** - MVP complete and archived: dedicated mobile-first scorekeeper/day-of scoring flow, invite-to-first-score UAT, durable Free/Plus scorekeeper UAT users, shared scoring service, admin Results integration, help docs, and migrations 066/068 applied in dev/prod (see [implementation plan](docs/projects/archive/codex_TOURNAMENT_SCOREKEEPER_EXPERIENCE_PLAN.md), [PM brief](docs/projects/archive/codex_TOURNAMENT_SCOREKEEPER_EXPERIENCE_PM_BRIEF.md), and [browser sign-off](docs/projects/archive/codex_TOURNAMENT_SCOREKEEPER_BROWSER_SIGNOFF.md))

- [x] **Tournament help documentation UX review (v1)** - Shipped grouped contents, search, quick answers, and FAQs; superseded by the v2 redesign below after the layout was found cluttered (see [TOURNAMENT_HELP_DOCS_REVIEW_PLAN.md](docs/projects/archive/TOURNAMENT_HELP_DOCS_REVIEW_PLAN.md))

- [ ] **Help system redesign (v2)** — Comprehensive IA/layout/content + in-app contextual-help overhaul (tournament-organizer focus). Review complete; owner approved single-scroll guides (+sub-groups+adaptive map), slide-over in-context help, simplified hub. Phased: 1 (help center usable) → 1.5 (content accuracy, other guides) → 2 (in-context drawer) → 3–4 (discovery layer) → 5 (tours, optional). See [HELP_SYSTEM_REDESIGN_PLAN.md](docs/projects/active/HELP_SYSTEM_REDESIGN_PLAN.md) + [PM brief](docs/projects/active/HELP_SYSTEM_REDESIGN_PM_BRIEF.md).
  - [x] **Phase 1** — Single-scroll guide layout + sub-groups + adaptive sticky map; simplified hub (search + card grid + "New here?" disclosure); Tournaments content restructured + fact-checked; entitlement-gated hub. COMMITTED + owner-approved on dev.
  - [x] **Phase 1.5** — Content accuracy audit (House League, Registrations, Rep Teams, Coaches, Accounting, Org Admin, Exports) vs live product; coach content reframed standalone-first. COMMITTED on dev.
  - [ ] **Phase 2 — In-context ("?") help** — PLANNED 2026-06-18 (mockup + plan + PM brief, awaiting owner sign-off): "?" HelpDrawer slide-over on every work-page header, always-visible FieldHint, touch/keyboard tooltip fix, heads-up warnings before irreversible actions, standalone coaches-portal help entry point (see [plan](docs/projects/active/HELP_PHASE2_INCONTEXT_PLAN.md) + [PM brief](docs/projects/active/HELP_PHASE2_INCONTEXT_PM_BRIEF.md))
  - [ ] **Phase 5 — Discovery &amp; Orientation (Layer 3)** — **5a + 5a fast-follow + 5b persona panel BUILT on dev 2026-06-20 (commits `bea5558`→`747f539`→`f07b53d`; awaiting owner browser sign-off).** Shipped: dashboard "what's next" guidance rail across lifecycle stages + dismissible "Did you know?" nudges + "See common tasks" outcome shortcuts + first-run wizard copy + Staff Kit intro fix; the "I want to…" shortcuts now also surface on **every** tournament page's "?" drawer; and the **"What everyone else sees" persona panel** (3 audience cards) on the dashboard. localStorage-only / no migration. Passed /review (3 copy fixes). **Still deferred (held for explicit go):** cross-device dismiss (needs a schema change) + the scattered page-level capability spotlights. (see [plan](docs/projects/active/HELP_PHASE5_DISCOVERY_PLAN.md) + [PM brief](docs/projects/active/HELP_PHASE5_DISCOVERY_PM_BRIEF.md))

- [ ] **QA Infrastructure** — Design and build a layered automated test suite (unit, integration, E2E smoke) with a test catalog, plan-gate matrix, post-release update workflow, CI/CD integration, and QA_RULES.md process documentation; use the prompt written 2026-05-21 as the starting brief for that session


### 3. Chart Library Investigation
- [ ] **Investigate chart libraries** — Evaluate recharts, chart.js, or @nivo for use in budget vs. actual and dashboard screens; assess bundle size, SSR compatibility, and dark-theme support before adding a dependency

### 3. Light / Dark Theme Toggle
- [ ] **Per-user light/dark theme preference** — Allow each user to toggle light/dark theme from their own settings screen (org admins in org settings, coaches in coach portal settings, etc.). Theme preference stored per-user (not per-org). Also expose a light/dark toggle on the public org and tournament websites so visitors can choose their preferred mode.

### 4. Stripe Integration — End-to-End Billing & Subscriptions
*Reference plan archived at [STRIPE_INTEGRATION_PLAN.md](docs/projects/archive/STRIPE_INTEGRATION_PLAN.md); live remaining work is tracked here.*

- [ ] **Billing downgrade and data retention flow** - Add FieldLogicHQ-guided downgrade/cancel review with over-limit data retention choices (see [BILLING_DOWNGRADE_RETENTION_PLAN.md](docs/projects/active/BILLING_DOWNGRADE_RETENTION_PLAN.md) and [PM brief](docs/projects/active/BILLING_DOWNGRADE_RETENTION_PM_BRIEF.md))
  - [x] First implementation slice: retention schema, owner review APIs/UI, cancellation suspension, and platform-admin retention queue
  - [x] Migration 038 applied in dev and production
  - [x] Retention expiry warnings and pending-purge processing
  - [x] Migration 039 applied in dev and production
  - [x] Dev/mock upgrades restore retained downgrade tournaments when plan limits allow
  - [ ] Hard purge job after pending-purge review policy is finalized
- [x] **Phase A** — Stripe dashboard setup: products, prices, webhooks, Customer Portal, API keys + price IDs configured in dev (test environment complete)
- [x] **Phase B** — App infrastructure: Stripe SDK, price map, DB migrations
  - [x] Stripe package + lib/stripe.ts singleton complete
  - [x] ~~Price map: getPlanPriceId() in lib/plan-config.ts~~ → replaced by DB-backed stripe_prices table (migration 048)
  - [x] Migration 047: subscription_period, current_period_end, rep_team_subscription_item_id — applied dev + prod
  - [x] Migration 048: stripe_prices table — price IDs in DB, managed via Platform Admin → Plans & Pricing — applied dev + prod
  - [x] Migration 049: plan_config_overrides table — per-plan tournament limit, seat limit, trial day overrides; lib/plan-config-db.ts helpers — applied dev + prod
  - [x] Platform Configuration: Plans & Pricing admin page at /platform-admin/plans-pricing (plan availability, limits & trials, Stripe price IDs)
- [x] **Phase C** — Webhook handler: subscription lifecycle → org plan tier sync
  - [x] runtime = nodejs; checkout.session.completed; invoice.payment_succeeded; trial_will_end email; subscription.created/updated writes new columns; subscription.deleted dedup guard (intent-type aware)
- [x] **Phase D** — Checkout + Customer Portal APIs + billing settings page
  - [x] Checkout billing-cycle readiness: monthly/annual selection now flows into checkout/mock checkout with future annual Stripe price guards
  - [x] create-checkout route: full Stripe checkout with gating + mock dev path
  - [x] portal route: Stripe Customer Portal session
  - [x] Billing settings page: upgrade cards, usage meters, downgrade/cancel review
  - [x] Stripe reconciliation (D4): downgrade/confirm calls subscriptions.update() or cancel(); cancel/confirm calls subscriptions.cancel(); webhook dedup guard is intent-type aware (downgrade vs cancellation)
  - [ ] Trial lifecycle reminders for League 30-day and Club 90-day windows — deferred to F4 (see Deferred Enhancements)
- [x] **Phase E** — Per-team billing: quantity sync (E1–E2), billing preview API (E3), team creation modal (E4), program year hook (E5), billing page add-on section (E6)
- [x] **Phase F** — UpgradeGate component (F1), onboarding→Checkout verified (F2), soft upsell prompts (F3); F4 deferred (see Deferred Enhancements)
- [ ] **Phase G — Go live with Stripe** (production cutover checklist)
  - **Current state:** live Stripe account, products/prices, webhook, Customer Portal, production env vars, and DB-backed live price IDs are configured. `scripts/audit-stripe-billing.mjs` passed with `30 pass, 0 warn, 0 fail` on 2026-06-02. Live-card smoke test is intentionally deferred until closer to paid pricing.
  - [x] D4 gap resolved: downgrade/cancel confirm routes now call Stripe after DB write
  - [x] In Stripe Dashboard (Live mode): create all live subscription products and monthly/annual CAD prices; record 12 live `price_xxx` IDs
  - [x] In Stripe Dashboard (Live mode): create production webhook endpoint at `https://fieldlogichq.ca/api/billing/webhook`; record live `whsec_...`
  - [x] Configure Stripe Customer Portal for live mode with FieldLogicHQ branding, payment method updates, invoice history, and app-owned cancellation posture
  - [x] Enter all 12 live price IDs via **Platform Admin -> Plans & Pricing** live price rows
  - [x] In Amplify production: set `STRIPE_SECRET_KEY=sk_live_...`, `STRIPE_WEBHOOK_SECRET=whsec_...`, and `NEXT_PUBLIC_APP_URL=https://fieldlogichq.ca`
  - [x] Run `node scripts\audit-stripe-billing.mjs --env .env.production.local` and confirm `0 fail, 0 warn`
  - [ ] **HIGH IMPORTANCE TODO - deferred live-card smoke test:** run the full checklist in [STRIPE_PRODUCTION_SMOKE_TEST_TODO.md](docs/projects/active/STRIPE_PRODUCTION_SMOKE_TEST_TODO.md) before opening paid self-serve subscriptions or founding-season billing conversion
  - [ ] Monitor first real transactions in Stripe dashboard and verify webhook delivery

### 5. Email Strategy

- [ ] **Email Strategy Investigation**:
    - [ ] Investigate best-of-breed providers (Resend, Postmark, AWS SES) for system notifications
    - [ ] Define the architecture for a "Contact Us" inquiry system

---

## 🧭 Deferred Enhancements (Confirmed scope, build later)

- [ ] **F4 — Trial checkpoint emails (League + Club)** — League orgs on a 30-day trial receive setup check-in emails at day 7, day 21, and ~day 27 (3-day warning already handled by Stripe `trial_will_end` webhook). Club orgs on a 90-day trial receive check-ins at day 7, day 30, day 60, and day 80. These are proactive activation emails (not just payment warnings). Implementation requires: a daily cron route at `/api/cron/trial-emails` protected by a shared secret, called by AWS EventBridge Scheduler; a `trial_email_sent` bitmask column on `organizations` for idempotency; and 4–5 email templates in the Resend stack. **Blocked on:** decision to enable EventBridge Scheduler in the Amplify account. See [STRIPE_INTEGRATION_PLAN.md](docs/projects/archive/STRIPE_INTEGRATION_PLAN.md) Phase F.

- [ ] **House League — Coach Draft Room** — Shareable per-team link (no login required) that lets coaches participate in the draft live. Each team gets a token-scoped URL; coaches see the current pick state and submit their pick when it's their turn. Requires: real-time state sync (polling or WebSocket), a `draftTokens` map in `draft_state`, and a public-facing draft room page. See conversation context from Phase 5G planning. **Architecture note:** Phase 5G is designed to not block this — draft business logic is kept auth-layer-agnostic so a token path can be added to `/draft/route.ts` without restructuring the state machine.
- [ ] **House League — Practice Scheduling** — Allow league admins to schedule practices for individual teams alongside the game schedule. A practice belongs to one team (not two), has no score, and does not affect standings. Confirmed scope post-Phase 5H. Build as an extension of the schedule page: separate "Practices" tab or filter, same date/time/location fields, team selector instead of home/away. No schema migration needed — can reuse `league_games` with `away_team_id = null` and a `game_type` column, or use a separate `league_practices` table (decide at build time).
- [ ] **Calendar Sync for Team Schedules** — Allow parents/coaches to export a team's game schedule as an `.ics` file or subscribe via a calendar URL (Google/Apple Calendar). Technically straightforward (generate `.ics` from schedule query). Applies to both house league team schedules and rep team schedules in the coaches portal. Build during a parent-facing polish pass.
- [ ] **Bulk Operations for Admins** — Bulk-change registration statuses, bulk-assign teams, bulk-edit schedule slots. High value for orgs with 100+ registrations. Requires multi-select UI, confirmation flows, and async batch API routes. Revisit after Phase 3 UX ships and an org is operating at scale.
- [ ] **Tournament setup templates** - Post-clone enhancement for orgs that run several similar tournaments and want reusable setup patterns without choosing a prior event each time.
- [ ] **Tournament staff role presets** - Later enhancement for Plus orgs if real usage shows registrar, scheduler, communications, and scorer-manager presets would reduce setup mistakes beyond current role/capability overrides.
- [ ] **Custom domain investigation** — Research feasibility and effort of allowing orgs to point a custom domain (e.g. miltonbats.com) to their FieldLogicHQ public page. Covers: DNS verification flow, wildcard SSL or per-org cert provisioning, reverse proxy / Amplify routing changes, and potential upsell pricing. Do not design or implement until `module_public_site` is fully shipped. **Also investigate:** pros and cons of a custom domain for the Stripe Customer Portal (e.g. `billing.fieldlogichq.ca`) — level of effort, DNS setup, whether it meaningfully improves trust vs. billing.stripe.com, and when this becomes worth prioritizing.
- [ ] **Public Site Offering Evaluation** — After the first external org enables `module_public_site`, review the offering across three dimensions: (1) ease of setup — is the path from enabling the module to a live page clear enough for a non-technical org owner? (2) customization level — structured fields are correct; assess whether anything is missing without going full CMS; (3) base UX improvements. Produce a prioritized fix list before moving to the next module.

---

## ✅ Completed Projects

### Coach Lineups IA + Insights Hub (2026-07-10)
- [x] **Coach Lineups IA + Insights Hub** — Lineups page un-stacked into Games | Templates tabs with a scope-chip + "Needs lineup" filter bar (bulk lineup-status flag replaced per-game probes); the scattered season analytics consolidated into ONE "Insights" destination in the Season nav group, rebuilt three times to the owner-approved V3 "Scoreboard + What stands out" dashboard: season scoreboard band, plain-language findings engine (safety→money→attendance→fairness→good-news ladder, 17 unit tests), question-titled report pages ("How are we doing?" game log + past-seasons archive; "Is playing time fair?" unified per-player table), Overview arm-care safety bridge. Season-over-season comparisons RETIRED (owner: youth seasons aren't comparable). Money reports stay in Money behind a gated cross-link. Two /review funnels (11 confirmed findings fixed total), /docs synced, owner browser-verified 2026-07-10 (see docs/projects/archive/COACH_LINEUPS_IA_AND_INSIGHTS_HUB_PLAN.md + _PM_BRIEF.md)

### Rain Delay / Day-of Operations (2026-07-07)
*(Two complementary features — built, owner-tested, reviewed (2× /review, 0 Critical/High), and documented on **dev**, unpushed. **(A) Broadened fan notifications:** a public top-bar notification bell lets anyone opt into tournament-wide alerts (score / schedule / message categories) without following a team, and a posted day-of alert now **pushes** to opted-in fans + coaches instead of being a silent banner (mig **177** `fan_push_categories`, dev-applied/**prod-pending**). **(B) Bulk "shift the day" tool:** from **Tools ▾ → Rain delay** on the Schedule page, an organizer picks a day (today or any upcoming day), optionally **filters by division and/or venue**, and moves/cancels those games in one atomic action — server-side bracket-order hard-block, wall-clock (America/Toronto) time math, atomic via mig **178** `bulk_reschedule_games` RPC — then a prefilled **announce step** pushes fans + staff/coaches (new `tournament_announcement` notify event). Gated to **Tournament Plus** (`bulk_reschedule` key; /strategy logged + /billing wired 2026-07-07; free orgs keep manual reschedule + the free pinned banner + coach email). Also **reorganized the Schedule admin page** (Auto→**Tools ▾** with Build+Adjust groups; stage-aware header primary — Add Game in Round Robin / Build-Edit Bracket in Playoffs; Clear-bracket moved into the editor; removed the duplicate Conflicts chip) and fixed the shared Export button's mobile height across ~21 pages. /docs synced (Tools-menu location + Plus gate + free-plan guardrail + filters). Plan + brief archived: [RAIN_DELAY_DAYOF_OPS_PLAN.md](docs/projects/archive/RAIN_DELAY_DAYOF_OPS_PLAN.md) + [_PM_BRIEF](docs/projects/archive/RAIN_DELAY_DAYOF_OPS_PM_BRIEF.md). **Remaining to ship:** apply migs **177 + 178 to prod** → /marketing changelog → promote; **not committed** (awaits owner OK).)*

### J4-001 fix — Next 16 sync-params migration (2026-06-12)
*(18 client pages still used the legacy sync `params` signature, so in Next 16 they read `undefined` off the params Promise, fetched `/teams/undefined/...`, and rendered dead for every user: the rep-teams admin season cluster (program-year overview/coaches/schedule/tryouts, team overview, history ×2) and the entire premium coach accounting suite (overview, budget, budget-vs-actual, dues, expenses, fundraisers ×2, allocations, payment requests, documents, history). Found as fix-now finding J4-001 in the journey audit ([JOURNEY_J4_CLUB_PRESIDENT.md](docs/projects/active/journeys/JOURNEY_J4_CLUB_PRESIDENT.md)). Fix = minimal-diff migration to the official pattern: `params: Promise<...>` + `const params = use(paramsPromise)` as the first body line; bodies untouched. Verified: repo-wide residual-signature sweep clean (pages, layouts, route handlers), `lint:focused` 0 errors, full `typecheck` clean, live browser probe now fetches real IDs, program-year page renders roster/coaches/events. Uncommitted on feat/free-tier-coaches; unblocks the J2 walk (audit Phase 4).)*

### Stale tournament-slot cap fix — migration 121 (2026-06-10)
*(Bug: a Tournament Plus org saw "Your plan allows 3 tournament slots" when moving a tournament to draft. Root cause = stale `organizations.tournament_limit = 3` left over from when Tournament Plus was capped at 3 ([migration 037](supabase/migrations/037_tournament_plan_limits.sql)); after the cap was raised to unlimited (9999) in `lib/plan-config.ts`, `getEffectiveTournamentLimit()` still honors a stored finite cap verbatim for unlimited-tier plans — the intended platform-admin override path — so the stale value kept enforcing. Fix = data migration `121_lift_stale_unlimited_tournament_caps.sql` raising stored caps to 9999 for tournament_plus/league/club where `< 9999`, **excluding `[UAT_PROTECTED]` fixtures** (uat-plus-org/uat-club-org deliberately at 10). Applied dev (`dev-test-org` 3→9999; UAT fixtures preserved) + prod (0 rows — already clean, idempotent backstop). Data-only change: no schema/dictionary/snapshot impact. No code change — honoring a stored finite cap on unlimited tiers is the intended override mechanism.)*

### Release guardrail — prod migration safety (2026-06-09)
*(Migrations are never auto-applied by the release process — Amplify only builds, the `/release` agent only pushes git — so a dev-applied/prod-missing migration would 500 in prod, e.g. the migration-040 incident. Two guardrails added, local tooling: (A) `scripts/apply-migration-api.mjs` now takes a `--dev`(default)/`--prod` target so applying to prod is a deliberate one-liner; (B) new `scripts/check-prod-migration-drift.mjs` / `npm run check:migrations` queries both live DBs and exits 1 when prod is behind dev — wired into the `/release` agent preflight + promote gate + a hard rule. Verified: the check correctly blocks today on `basic_coach_team_players` (mig 114, dev-only).)*

### Playoff Bracket Scheduling
*(Archived — Playoff Builder supports bracket-only creation, optional scheduled playoff windows, dependency-aware round rest, temporary playoff facility lanes, draft health, saved playoff health, protected Build from current regeneration, Keep/Release on scheduled playoff games, round-robin delete protection, API delete-policy tests, and user browser verification. See [plan](docs/projects/archive/PLAYOFF_BRACKET_SCHEDULING_PLAN.md) and [PM brief](docs/projects/archive/PLAYOFF_BRACKET_SCHEDULING_PM_BRIEF.md).)*

### Public Tournament Mobile Experience
*(Archived — coach/parent public mobile experience is implementation-complete. Registration review/waitlist, Tournament Day home mode, Follow My Team, mobile schedule controls, compact selectable schedule rows, public game details, readable table-first Results/Standings, completed-tournament public record, and shared public empty/unavailable states are complete. Browser visual/design refinements are split to a separate future design project; route-specific analytics are deferred. See [implementation plan](docs/projects/archive/codex_PUBLIC_TOURNAMENT_MOBILE_EXPERIENCE_PLAN.md) and [PM brief](docs/projects/archive/codex_PUBLIC_TOURNAMENT_MOBILE_EXPERIENCE_PM_BRIEF.md).)*

### Tournament Experience Excellence
*(Archived — admin review, UX reformat, Plus/org-scope hardening, design consistency, responsive hardening, and final browser sign-off prep complete. See [experience plan](docs/projects/archive/TOURNAMENT_EXPERIENCE_EXCELLENCE_PLAN.md), [PM brief](docs/projects/archive/TOURNAMENT_EXPERIENCE_EXCELLENCE_PM_BRIEF.md), [journey audit](docs/projects/archive/TOURNAMENT_EXPERIENCE_PHASE_1_JOURNEY_AUDIT.md), [section review](docs/projects/archive/TOURNAMENT_SECTION_REVIEW_PLAN.md), [findings](docs/projects/archive/TOURNAMENT_REVIEW_FINDINGS.md), and [admin UX reformat reference](docs/projects/archive/TOURNAMENT_ADMIN_UX_REFORMAT_PLAN.md).)*
- [x] Phase 2C core admin data-rich QA for Registrations, Schedule, and Results across Free/Plus desktop/mobile
- [x] Phase G Plus gate and org-scope hardening across tournament admin/API flows
- [x] Phase F design consistency pass and archive empty-state improvements
- [x] Phase H responsive hardening and final browser sign-off checklist
- [x] Future scorekeeper route and public tournament mobile work split into separate planning prompts

### Tournament Signup Experience
*(Archived — all phases and browser verification complete. See [TOURNAMENT_SIGNUP_EXPERIENCE_FIXES.md](TOURNAMENT_SIGNUP_EXPERIENCE_FIXES.md))*
- [x] Phases 1–4 trust, setup, conversion, and plan-aware onboarding refinement
- [x] Full walkthrough polish series (30+ incremental improvements)
- [x] Non-browser hardening pass
- [x] Browser verification of signup-to-registration flow complete

### Free Tournament Organizer UX Cleanup
*(Archived — all items complete. See [docs/projects/archive/TOURNAMENT_FREE_TIER_UX_IMPLEMENTATION_PLAN.md](docs/projects/archive/TOURNAMENT_FREE_TIER_UX_IMPLEMENTATION_PLAN.md))*
- [x] Reusable Manage Tournaments setup wizard with existing-venue reuse
- [x] Tournament Settings & Access section
- [x] Phase 1 trust fixes: mobile activation API path, secured message sending, active contact mapping
- [x] Phase 2 signup and onboarding clarity copy
- [x] Phase 3 item 8: draft-to-publish dashboard checklist
- [x] Phase 3 item 10: draft public-page messaging and preview-link clarity
- [x] Phase 4 item 11: external payment expectations and fee-mode alignment
- [x] Phase 4 item 12: admin score entry respects finalization rules
- [x] Phase 4 item 13: public announcement posting is clearly separated from email communication
- [x] Phase 4 item 14: communication targeting now supports teams, divisions, statuses, and contacts
- [x] Phase 4 item 15: mobile tournament More menu includes day-of tools
- [x] Phase 5 item 16: plan feature gates and pricing copy align free Tournament with Tournament Plus-and-above benefits

### Rep Teams — Groups & Per-Team Billing
*(Archived — all phases complete. See [docs/projects/archive/REP_TEAMS_ENHANCEMENTS_PLAN.md](docs/projects/archive/REP_TEAMS_ENHANCEMENTS_PLAN.md); Phase 3 tracking moved to docs/projects/archive/STRIPE_INTEGRATION_PLAN.md Phase E)*
- [x] **Phase 1** — Rep team groups: `rep_team_groups` table, group management UI, team assignment, group filter on lists and accounting views — migration 035 applied dev+prod
- [x] **Phase 2** — Staff group scoping: `org_member_rep_group_scopes` junction table, multi-group selection per member, `repGroupIds` on auth context, hard 403 gating on all rep team admin routes, group access UI in Manage Member modal — migration 036 applied dev+prod
- [x] **Phase 3** — Per-team billing: synced via Stripe Phase E (quantity sync, billing preview API, team creation modal with charge confirmation, program year status hook, billing page add-on section)

### Non-Billing UAT Remediation
*(Archived - all 8 items complete. See [docs/projects/archive/NON_BILLING_UAT_REMEDIATION_PLAN.md](docs/projects/archive/NON_BILLING_UAT_REMEDIATION_PLAN.md))*
- [x] Signup URL guardrails, legacy admin routing, public tournament reads, public registration reads, official scorekeeper scoping/states, Next.js proxy migration, and lint-gate stabilization

### Slot-First Roster & Schedule Architecture
*(Archived — all 7 phases complete. See [docs/projects/archive/SLOT_ROSTER_PLAN.md](docs/projects/archive/SLOT_ROSTER_PLAN.md))*
- [x] Foundation–Phase 7: pool slots as division roster, atomic slot claiming (PG fn), slot board + waitlist registrations page, publish control per division, public schedule visibility, cleanup

### Accounting Enhancements — Org & Rep Team Budget Planning
*(Archived — all phases complete. See [docs/projects/archive/ACCOUNTING_ENHANCEMENTS_PLAN.md](docs/projects/archive/ACCOUNTING_ENHANCEMENTS_PLAN.md))*

### Public Tournament UAT
- [x] Default tournament/public-site customization to FieldLogicHQ colors and remove preview-only setup CTA
- [x] Apply light/dark theme in tournament preview, group theme controls, and add public-style preview nav pages
- [x] Fix light-mode public tournament hero surfaces so text and icons remain visible
- [x] Tune public tournament palette contrast and add combined dark/light theme preview samples
- [x] Public tournament light-mode polish — solid public CTAs, readable light-mode accents, restored Battle Purple preset
- [x] Public tournament preview light-mode section backgrounds inherit the selected mode below the hero
- [x] Tournament branding palettes — normalize presets for dark/light readability and gate custom colors to Tournament Plus+
- [x] Public tournament light-mode accent tokens use readable primary text on pale surfaces
- [x] Public tournament page visibility controls for hiding News, Schedule, Standings, Teams, Rules, or Registration
- [x] Tournament branding settings reorder included controls above Tournament Plus advanced customizations
- [x] Tournament branding logo placeholder polish and branding API 500 cleanup

### In-App Documentation & Help System
*(see [HELP_SYSTEM_PLAN.md](docs/projects/archive/HELP_SYSTEM_PLAN.md))*
- [x] **Phases A–I complete** — foundation + Tournaments + House League + Rep Teams + Coaches Portal + Accounting + Org Admin & Onboarding + Platform Admin contextual cues (H) + Help Hub & context-aware navigation (I)

### Pricing
- [x] **Phase 1** — Update plan-config (4 tiers), rewrite PricingSection component, build public `/pricing` page. Full plan in [PRICING_IMPLEMENTATION_PLAN.md](docs/projects/archive/PRICING_IMPLEMENTATION_PLAN.md). Content spec in [PRICING_PAGE_COPY.md](docs/agents/brand/PRICING_PAGE_COPY.md).
- [x] **Phase 2 / Functionality Audit** — Billing page rewritten (monthly/annual toggle, modules section, outcome-focused upgrade cards); onboarding checklist updated with conditional module steps; all stale plan name references removed.
- [x] **Launch positioning update** — Public and billing pages now sell Tournament/Tournament Plus as live tiers while League/Club are marked coming soon/early access; checkout blocks League/Club self-serve purchase.
- [x] **Early-access lead capture** — Join Early Access CTAs now open a modal that collects name, email, organization, interest areas, and consent into `early_access_leads`.

### Tournament Admin URL Restructure
*(see [TOURNAMENT_URL_RESTRUCTURE_PLAN.md](docs/projects/archive/TOURNAMENT_URL_RESTRUCTURE_PLAN.md))*
- [x] Moved all tournament operational pages from `admin/{page}` to `admin/tournaments/{page}` — matches module URL pattern used by house-league, rep-teams, and accounting

### Role-Based UX Improvement Review — Phases 1–5
*(see [UX_REVIEW_PLAN.md](docs/projects/archive/UX_REVIEW_PLAN.md))*

**Phase 1 — Critical bugs & multi-tenancy**
- [x] **1A** — Replace default `/{orgSlug}` page: FieldLogicHQ-branded placeholder (no public site), tournament selector for 2+ active tournaments
- [x] **1B** — Fix accounting sidebar: rename "Overview" → "Ledgers", remove broken "Org Ledger" duplicate
- [x] **1C** — Remove House League "Notifications" sidebar link (currently resolves to a 404)
- [x] **1D** — Fix staff auto-redirect: change target from `/admin/tournaments` to `/admin/dashboard`

**Phase 2 — Coaches portal gaps**
- [x] **2A** — Build `CoachesBottomNav` for mobile navigation in the coaches portal
- [x] **2B** — Improve "Not assigned" screen: add org contact email and "Back to [org]" link
- [x] **2C** — Add unconfigured-state message to coaches accounting page when no dues/expenses exist
- [x] **2D** — Show program year name alongside team name in coaches sidebar team list

**Phase 3 — Owner & operator improvements**
- [x] **3A** — Expand onboarding checklist: add conditional steps for enabled modules (house league, rep teams, public site)
- [x] **3B** — Add "Modules" section to billing page: per-module plan inclusion, active status, and upgrade CTA
- [x] **3C** — Create `/admin/org` hub page (Members, Diamonds, Tournament Records, Billing, Settings tiles); update admin hub tile href
- [x] **3D** — Add inline status-transition buttons to house league season cards
- [x] **3E** — Build House League Notifications page at `/admin/house-league/seasons/[seasonId]/notifications` (email preview + sent history log); re-add Notifications sidebar link
- [x] **3F** — Season detail index page: sidebar sub-nav renders correctly on the index page; original issue no longer exists
- [x] **3G** — Add cross-module "Needs attention" strip to admin hub (pending registrations, open tryouts)

**Phase 4 — Platform admin & treasurer**
- [x] **4A** — Add health indicators to platform admin overview: `past_due` org count, new signups in 7 days
- [x] **4B** — Add org/date-range filter and pagination to platform audit log (200-row hard cap, no search)
- [x] **4C** — Add active-route highlighting to platform admin sidebar nav
- [x] **4D** — Add "Go to Admin →" link in platform admin org rows and detail page (label standardised to "↗ Admin")
- [x] **4E** — Allow treasurers (not just owners) to create tournament ledgers; expanded Add Entry / Add Transfer / Edit / Void gates to treasurer too
- [x] **4F** — Skipped: platform users list is internal staff only (no cap issue); org users found via org → users drill-down
- [x] **4G** — Treasurer: add CSV export to ledger detail page
- [x] **4H** — Treasurer: category `<datalist>` now fetches distinct categories from this org's ledger entries; falls back to static defaults for new orgs

**Phase 5 — Public experience & polish**
- [x] **5A** — Add rep teams tryout section to org home page when module enabled and tryouts open
- [x] **5B** — Add house league CTA to default `/{orgSlug}` branch when module enabled and registration open
- [x] **5C** — Improve tryout registration closed page: show org contact email (open-date field deferred — column does not exist in schema)
- [x] **5D** — Add "Contact Us" label to contact email link on public site page
- [x] **5E** — Add pending-action badges to coaches team hub cards (overdue installments, upcoming events in 7 days)
- [x] **5F** — Audit and standardise empty/loading states app-wide (coaches portal: loadingState CSS, emptyStateTitle/Sub)
- [x] **5G** — Add season switcher dropdown to House League sidebar when inside a season (client-side fetch pattern)
- [x] **5H** — Official/Scorekeeper score entry: type="number", 56px input height, 48px button height, filter selects 44px
- [x] **5I** — House league public registration form: clear capacity labels + status lookup page at `/{orgSlug}/league/{seasonSlug}/status`

### Platform Admin — Superuser Tooling
*(see [PLATFORM_ADMIN_IMPROVEMENTS_PLAN.md](docs/projects/archive/PLATFORM_ADMIN_IMPROVEMENTS_PLAN.md))*
- [x] **Phase A** — `enabled_addons` toggle UI + direct admin link
- [x] **Phase B** — Users page org context enrichment
- [x] **Phase C** — Org notes column + drill-down detail page
- [x] **Phase D** — `org_overrides` table: subscription status override + comp/billing grace period
- [x] **Phase E1+E2** — Platform audit log table + write helper
- [x] **Phase E3** — Audit log read page at `/platform-admin/audit`
- [x] **Phase F** — Org search/filter + users pagination
- [x] Platform company users — DB-managed platform admin access (`platform_users` table, invite/deactivate/remove UI)
- [x] Early Access Platform Admin — League/Club lead pipeline with protected APIs, filtering, detail/status/notes workflow, copy/export helpers, outreach templates, migrations 044/045 applied dev+prod, and browser verification complete

### Platform Roadmap — Add-On Modules
*(see [PLATFORM_ROADMAP.md](PLATFORM_ROADMAP.md))*
- [x] **Phase 1** — Archives B2: back-to-admin link on public archive detail pages
- [x] **Phase 2** — Plan entitlements architecture: `moduleEntitlements` in PlanConfig, `enabled_addons` migration, `hasModuleEntitlement()` helper
- [x] **Phase 3** — `module_public_site` (see [docs/projects/archive/PUBLIC_SITE_MODULE_PLAN.md](docs/projects/archive/PUBLIC_SITE_MODULE_PLAN.md))
- [x] **Phase 4** — `module_accounting` (see [ACCOUNTING_MODULE_PLAN.md](docs/projects/archive/ACCOUNTING_MODULE_PLAN.md))
- [x] **Phase 5** — `module_house_league` — All phases 5A–5M complete (see [HOUSE_LEAGUE_MODULE_PLAN.md](docs/projects/archive/HOUSE_LEAGUE_MODULE_PLAN.md))
- [x] **Phase 6** — `module_rep_teams` — All phases 6A–6N complete (see [REP_TEAMS_MODULE_PLAN.md](REP_TEAMS_MODULE_PLAN.md))
  - [x] 6A — DB schema: Migration 021 + Supabase Storage bucket setup
  - [x] 6B — TypeScript types + DB helpers
  - [x] 6C — Coach role expansion + module shell
  - [x] 6D — Team + program year management (admin API + pages)
  - [x] 6E — Public tryout registration form
  - [x] 6F — Tryout approval queue (admin): offer → accept/decline flow + status emails
  - [x] 6G — Roster management (coaches portal; franchise model)
  - [x] 6H — Player documents (Supabase Storage: upload API, signed URLs, template management)
  - [x] 6I — Coaches portal foundation (layout, auth guard, sidebar, context, dashboard)
  - [x] 6J — Coaches portal: unified team calendar (6 event types + recurrence)
  - [x] 6K — Accounting: org cost allocation + team payment schedules
  - [x] 6L — Accounting: coach-managed team budget (dues, expenses, tournament payables)
  - [x] 6M — Accounting: automated payment reminder emails (coach-initiated)
  - [x] 6N — Past program years (admin history + coaches portal history, read-only)
  - [x] Franchise model audit — 9 violations fixed across admin/coaches layers

### Infrastructure — Dev Environment
*(see [AMPLIFY_DEV_ENV_PLAN.md](docs/projects/archive/AMPLIFY_DEV_ENV_PLAN.md))*
- [x] AWS Amplify Hosting Strategy — branch-based deployment, `dev.fieldlogichq.ca`, per-branch env var scoping
- [x] Live Dev Environment — AWS Amplify `dev` branch live
- [x] RESEND_API_KEY added to Amplify environment variables
- [x] NEXT_PUBLIC_APP_URL added to Amplify environment variables
- [x] Signup DB hardening - `025_service_role_api_grants.sql` applied in dev and production; secret-key prefix logging removed locally
- [x] Platform admin/dev tools hardening - bootstrap admin preservation, DB-aware platform access, and auth-gated seed/wipe APIs
- [x] Local Supabase safety guard - localhost refuses the known production Supabase project and signup key-prefix logging is removed

### Auth & Membership Constraints
- [x] One-user-one-org enforcement — delete auth user on member removal; cross-org invite guard (see [docs/projects/archive/ONE_ORG_CONSTRAINT_PLAN.md](docs/projects/archive/ONE_ORG_CONSTRAINT_PLAN.md))

### Code Quality
- [x] Fix 9 implicit `any` parameters in `lib/db.ts` row-mapper callbacks
- [x] Add resource-aware agent verification workflow and changed-file lint command

### Tournament Landing Page Polish
- [x] Hero title now dynamic from `tournament.name`; remaining registration CTA polish deferred

### Platform Improvements — Phases 1–3
*(see [PLATFORM_IMPROVEMENTS_PLAN.md](docs/projects/archive/PLATFORM_IMPROVEMENTS_PLAN.md))*
- [x] Forgot password / password reset flow
- [x] Invite and re-invite flow with PKCE, display names, existing-user notifications
- [x] Member suspension (status column, suspend/reinstate API + UI)
- [x] Officials seat exclusion + 80% upgrade nudge
- [x] Onboarding flow (3-step checklist, redirect, skip logic)
- [x] Audit log (`org_audit_log` table, read-only view)
- [x] Display names (column, members list, invite acceptance)
- [x] Org offboarding — "Request Account Deletion" form

### Module-Level Capabilities — Phase A
*(see [MODULE_CAPABILITIES_PLAN.md](docs/projects/archive/MODULE_CAPABILITIES_PLAN.md))*
- [x] 7 module caps in `Capability` type, `ROLE_DEFAULTS` updated
- [x] `userCapabilities` in OrgContext
- [x] Grouped module/action sections in capability override UI

### Tournament Lifecycle Cleanup
*(see [TOURNAMENT_LIFECYCLE_PLAN.md](docs/projects/archive/TOURNAMENT_LIFECYCLE_PLAN.md))*
- [x] All status transitions freely selectable via dropdown
- [x] Seal button gated to completed status only (UI + server guard)
- [x] Archived tournaments hidden from admin switcher
- [x] Admin archives page: two-section layout, seal flow, "Past Tournaments" sidebar label

### Tournament Redesign — Phases 1–5
*(see [TOURNAMENT_REDESIGN_PLAN.md](TOURNAMENT_REDESIGN_PLAN.md))*
- [x] DB schema: `status` + `slug` columns, partial unique index
- [x] TypeScript: Tournament type, `mapTournament`, `getTournamentBySlug`
- [x] Admin UI: status transitions, slug field, active-limit enforcement
- [x] Billing: usage meter counts non-archived tournament slots, plan-config `tournamentLimit`
- [x] URL restructuring: `/[orgSlug]/[tournamentSlug]/` route tree, redirects, OrgNavContext, Navbar updates

### Multi-Tenancy — Phases 3, 4, 6
*(see [MULTI_TENANT_ARCHITECTURE.md](MULTI_TENANT_ARCHITECTURE.md))*
- [x] Phase 3: Multi-Org Routing — pages moved under `/[orgSlug]`
- [x] Phase 4: Discovery Portal & Search (`/discover`)
- [x] Phase 6: Org Admin UX & Seat Management

### Admin Design & Architecture
*(see [ADMIN_HUB_NAVIGATION_PLAN.md](docs/projects/archive/ADMIN_HUB_NAVIGATION_PLAN.md))*
- [x] Admin Hub Navigation — hub tile grid, section-aware sidebar, smart tournament redirect
- [x] Sidebar restructure — Tournament / Organization sections, labeled nav groups
- [x] Admin theme cleanup — HUD tokens, Option B palette separation
- [x] Member roles Phase 0 — API capability gates, score finalization enforcement
- [x] Member roles Phase 1 — `capabilities` column, tournament assignments, `getAuthContextWithScope()`
- [x] Member roles Phase 2 — RLS activation, `authClient()` mutations

### Admin UX & Documentation
- [x] In-app role/permissions documentation — Role Guide panel, permission matrix, invite modal hints
- [x] Admin help gaps — Billing status alerts, slug-change warning, Randomize copy, Results legend
- [x] Walkthrough polish — subscription upgrade cards match onboarding plan chooser styling
- [x] Walkthrough polish — remove ad hoc module upgrade section from Subscription
- [x] Subscription polish — upgrade comparison appears before muted downgrade/cancel controls
- [x] Walkthrough polish — remove tournament onboarding get-started flash after wizard save
- [x] Walkthrough polish — replace tournament admin Back to Site with admin-only Preview Site
- [x] Walkthrough bugfix — admin tournament preview resolves draft tournaments without 404
- [x] Tournament branding UX — collapse dark/light choices into one toggle with a single live preview

### Platform Administration (Super Admin)
*(see [PLATFORM_ADMIN_PLAN.md](PLATFORM_ADMIN_PLAN.md))*
- [x] Site Admin Dashboard — email-allowlist auth guard, org plan override, user password reset, global stats

### Other
- [x] Brand Pivot — sports-authority copy across landing page, auth pages, navbar
- [x] Age Group Preference Persistence — cookie-based tab persistence
- [x] Schedule/Results Revamp — inline scores, team filter, bracket view, `/standings` page
- [x] Stock Logo Library — curated sport icon set, plan-tiered access
