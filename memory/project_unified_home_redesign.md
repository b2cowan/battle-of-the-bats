# Unified Home IA Redesign — GameChanger-style front door

**BUILD STATUS — Phases 0+1 BUILT ON DEV 2026-07-18 (uncommitted; typecheck + verify:changed clean; NOT owner-tested; NO commit yet).**
- **P0 (nav/shell/redirects):** ConsumerNav TABS → Home(/discover, Home icon)/Scores/Chat(/chat, MessageCircle)/Account; desktop "Your workspaces" util link removed. `/chat` added to `CONSUMER_SHELL_PREFIXES` + SW `NEVER_CACHE_PREFIXES` + `CACHE_VERSION` v4→**v5**. New static warm `/chat` placeholder (logged-out/fan pitch, NO inbox). `/home` retired → `permanentRedirect('/discover')` stub (home.module.css deleted); **proxy.ts `/home` auth-gate + matcher entry REMOVED** (was bouncing anon /home→login; now 308→/discover for everyone). Fast-path MOVED into `getAuthDestination` → new `getAuthDestinationDetail()` returns `{destination, hasWorkspace}`; solo-workspace→their workspace, else /discover; `/api/auth/destination` + login `resolveLoginDestination` now key deep-link `next` off `hasWorkspace` (not the old `/home` sentinel). `start/tournament` + `auth/suspended` redirects repointed; all `/home?pick=1` links → `/discover`.
- **P1 (warm Home + all-following):** warm tokens = **additive** `--home-*` props on `.warm` (components/consumer/warmTheme.module.css) — NEVER override dark tokens, so nav/Scores/Account stay dark this phase (transitional). New `/api/consumer/home` (client-fetched: workspaces + rolled-up follows + invites + lapsed; anon-safe SSR shell kept — FP-2 pattern). `lib/home-following.ts` (PURE): `followStatusText` (moved from follow-feed, which re-exports) + `rollupFollowFeedByTournament` + `ConsumerHomePayload`. Dedupe: `getCoachedRegistrationTeamIds` (basic-coach reg ids) drops coached teams from Following (role wins). `getLapsedWorkspacesForUser` → reactivate cards. `HomePersonalization` (invites/workspaces/following·tournaments/what's-new intro) inserted into warm DiscoverClient above Browse (hidden while text-searching). All-following (`FollowingList`) warm restyle: "← Home" + "All following · N" one row, per-team rows grouped Tournaments/Past, star=unfollow (`unfollowTeamEverywhere`), alerts pointer to Account·Notifications.
- **/simplify DONE:** consolidated 3 group-rank tables → one exported GROUP_RANK; login skips invite lookup unless fast-path viable; cache()-memoized getActiveMembershipRows + basic-coach scan (request-scoped dedup on Home); removed dead FollowRow busy state; deleted 3 orphaned components (FollowFeed/FollowFeedCard/FollowCardList). DEFERRED: warm-CSS composes convergence (→ P3-P5), coach-dedupe on /following (Home-scoped by design), lapsed-as-context-kind (broad blast radius).
- **/review DONE (high-risk, 4 lenses):** FIXED [High] All-following + Home mislabeled "not following anything" when getFollowFeed drops an unpublished/canceled tournament's card → now uses raw followCount (added to payload) + "game info unavailable" line vs. true empty; FIXED [Med-High] /api/consumer/home missing Cache-Control: no-store (added, matches sibling APIs); FIXED [Low] signup /home fallbacks → /discover. VERIFIED clean: both cache() wraps are request-scoped in route handlers (no cross-request staleness; membership dedup is load-bearing for active-vs-lapsed consistency — documented); anon-safe SSR; optimistic-unfollow fire-and-forget = consistent w/ app-wide follow pattern.
- **/docs DONE:** synced lib/help-content/tournaments.tsx fan-experience section — "Following tab" retired → Home ("one card per tournament, tap in") + All-following page; Scores-tab description UNCHANGED (Scores not restructured until P3); faq-following-tab anchor kept (no href breaks), keywords updated. ⚠ Doc copy is AHEAD of prod — must promote in the SAME bundle as the P0+1 code (don't ship docs separately).
- **Tail:** owner phone test → commit (per-action OK) + prod-promote bundle (code + docs together). Search stays TOURNAMENTS-ONLY this phase (Team/Org search = P2). Scores(P3)/Chat inbox(P4)/Account trim(P5) NOT built.

**Status: PLANNED (2026-07-18). Direction + 6 decisions owner-ratified; mockup rounds NOT started.**
Plan: docs/projects/active/UNIFIED_HOME_IA_REDESIGN_PLAN.md (+ PM brief). Successor design round to Unified App Consumer Layer (its deferred §8 item, expanded).

## Ratified direction (owner, 2026-07-18)
- Nav → **Home / Scores / Chat / Account** (same shape signed-in/out). Discover + Following tabs retired; routes survive (`/discover` stays the canonical SEO URL behind Home; `/home` → permanent redirect; `/following` = "All following" overflow).
- Home = search (Tournament/Team/**Organization** — NOT "League", plan-tier collision) + pending invites + Workspaces cards (full role-chip fidelity, reuse existing destination resolution — tier separation rule) + **Following: Tournaments** section + browse grid. Personalization is CLIENT-FETCHED (`/api/consumer/home`); SSR shell stays anon-safe (SW-cache PII pattern).
- **Two-tier follow model (load-bearing owner refinement):** team-in-tournament follow = tournament-scoped (highlights + notifications in that event); on Home it presents AS a tournament card with "your team" context line, tap → tournament pages. Persistent **Teams** follow (rep-team entity, schedules/scores across events, parent chat) = FUTURE phase, hard-gated on PIPEDA/CASL review. Resolves the no-season-entity + no-canonical-team-URL blockers by product definition.
- Sign-in fast-path KEPT (solo-workspace users land in their workspace at login); Home tab tap never auto-redirects; `?pick=1` retired.
- Scores = union of memberships + follows (coach sees own team unfollowed): My Events rollup strip (one chip per event, "● 3 live") + My Games day-grouped rows, Live pinned. Poll MVP, no realtime.
- Chat tab: member cross-context inbox (new aggregator + self-heal on load); logged-out/fan = STATIC preview only (never real messages — no anon read path exists at DB layer, keep it that way); report-a-message + self-mute ship same release; `/chat` needs SW denylist + CACHE_VERSION bump.
- Account slims to settings (profile/notifications/install/help/legal/sign-out); legal links currently absent from the whole consumer shell — this adds them.
- No family-member row (omit entirely, no dead slot).

## Mockup Round 1 — DECIDED 2026-07-18 (artifact de1c87a1, rev 3 = binding Home spec; logged to design_decisions.md)
**R1-4 = WARM LIGHT (C), consumer shell ONLY** (paper #F8F4ED, white 14px cards, sans names + mono retained for scores/kickers, lime→olive #57651E on warm ground; tournament/coaches/admin shells keep their themes — separate future decision; ALL later rounds render warm). **R1-2 = tap lands on TOURNAMENT HOME** (existing page: followed-team card + score dock already there; warm→branded theme handoff deliberate). **R1-3 = composition approved** (Search→Invites→Workspaces→Following·Tournaments→Browse; All-following sub-page: back link reads "← Home" (destination-naming, owner call) + "All following · N" title right-aligned on the SAME row (rev 4), star-unfollow, alerts pointer to Account·Notifications). **R1-1 superseded** — seam-grid retires from the consumer app. Browse section (search pills incl.) is IDENTICAL in every auth state — rev 4 fixed a fan-frame omission. Rev 4 = binding spec.

## Mockup Round 2 — Scores (artifact 63ec5baa, published 2026-07-18 — AWAITING OWNER DECISIONS)
Warm-light frames: game-day (events strip w/ Following/Coach chips + Live-pinned day-grouped My Games + "Show earlier results" cap), quiet-day (Next up, never dead), signed-out (platform live board + pitch). **R2-3 DECIDED (owner 2026-07-18): 2-col GRID** capped at 2 rows; "+N more" expands IN PLACE → "Show fewer" (never navigates off Scores; All following stays the manage surface). **Event lifecycle:** cards self-sort live → upcoming (by next game) → completed LAST (dimmed, "Completed · date", first behind +N, can never displace live/upcoming); completed events stay ONE WEEK after end then leave Scores entirely (remain followed under All following → Past events). Rev 4 folds the grid into all frames. **R2-1 + R2-2 ACCEPTED at the recommendations (owner 2026-07-18) — ROUND 2 FULLY DECIDED; logged to design_decisions.md; rev 4 = binding Scores spec.**

## Mockup Round 3 — Chat: DECIDED 2026-07-18 (artifact a8622786 = binding Chat spec; logged to design_decisions.md)
R3-1 inbox (event-grouped, unread rollups, mute excluded) · R3-2 safety sheet SAME RELEASE (Report to organizers → moderation queue + Mute room) · R3-3 rooms from the consumer app render WARM (coaches-portal/admin chat skins unchanged — one engine, per-shell presentation) — ALL ACCEPTED.

## PHASE M COMPLETE 2026-07-18 — all mockup rounds decided
Binding specs: R1 Home (de1c87a1 rev 4) · R2 Scores (63ec5baa rev 4) · R3 Chat (a8622786). **Account tab = NO mockup round (owner-accepted): build straight from plan §3g + established warm conventions.** Next: BUILD Phases 0+1 in a NEW chat via docs/projects/active/UNIFIED_HOME_BUILD_P01_PROMPT.md (owner-authorized 2026-07-18; includes /chat static placeholder + SW denylist). Sequencing: 0+1 first (one chat); then Scores (P3) ∥ Chat (P4) may run in parallel chats (disjoint surfaces, small frequent owner-OK'd commits); P2 search rides alongside; P5 Account/badges/metrics LAST; P6/P7 per plan. Union rule (coached/admin teams auto-included, role chip wins over Following) already ratified — rendered, not re-asked. Next after lock: Round 3 Chat.

## Phasing
M mockup rounds (blocking; resolve seam-grid vs rounded-card in R1) → 0 nav+redirects (+`next=`/email-template/manifest inventory) → 1 Home consolidation (+lapsed-workspace reactivate card, what's-new intro) → 2 search expansion (org `list_in_directory` migration; team search scoped to listed tournaments) → 3 Scores → 4 Chat → 5 Account+polish(+metrics) → 6 fast-follow whole-tournament/org follows (org landing page prerequisite; account-only, pin-independent) → 7 gated Teams phase.

## Provenance
12-agent workflow analysis 2026-07-18 (6 readers → 5 designers → critic); risk register (23 items) in the plan §10. Business decisions logged to BUSINESS_DECISIONS.md same day (logged-out-chat posture; two-tier follow model).
