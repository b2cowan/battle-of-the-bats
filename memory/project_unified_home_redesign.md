# Unified Home IA Redesign — GameChanger-style front door

**BUILD STATUS — Phases 0+1 BUILT ON DEV 2026-07-18 (uncommitted; typecheck + verify:changed clean; NOT owner-tested; NO commit yet).**
- **P0 (nav/shell/redirects):** ConsumerNav TABS → Home(/discover, Home icon)/Scores/Chat(/chat, MessageCircle)/Account; desktop "Your workspaces" util link removed. `/chat` added to `CONSUMER_SHELL_PREFIXES` + SW `NEVER_CACHE_PREFIXES` + `CACHE_VERSION` v4→**v5**. New static warm `/chat` placeholder (logged-out/fan pitch, NO inbox). `/home` retired → `permanentRedirect('/discover')` stub (home.module.css deleted); **proxy.ts `/home` auth-gate + matcher entry REMOVED** (was bouncing anon /home→login; now 308→/discover for everyone). Fast-path MOVED into `getAuthDestination` → new `getAuthDestinationDetail()` returns `{destination, hasWorkspace}`; solo-workspace→their workspace, else /discover; `/api/auth/destination` + login `resolveLoginDestination` now key deep-link `next` off `hasWorkspace` (not the old `/home` sentinel). `start/tournament` + `auth/suspended` redirects repointed; all `/home?pick=1` links → `/discover`.
- **P1 (warm Home + all-following):** warm tokens = **additive** `--home-*` props on `.warm` (components/consumer/warmTheme.module.css) — NEVER override dark tokens, so nav/Scores/Account stay dark this phase (transitional). New `/api/consumer/home` (client-fetched: workspaces + rolled-up follows + invites + lapsed; anon-safe SSR shell kept — FP-2 pattern). `lib/home-following.ts` (PURE): `followStatusText` (moved from follow-feed, which re-exports) + `rollupFollowFeedByTournament` + `ConsumerHomePayload`. Dedupe: `getCoachedRegistrationTeamIds` (basic-coach reg ids) drops coached teams from Following (role wins). `getLapsedWorkspacesForUser` → reactivate cards. `HomePersonalization` (invites/workspaces/following·tournaments/what's-new intro) inserted into warm DiscoverClient above Browse (hidden while text-searching). All-following (`FollowingList`) warm restyle: "← Home" + "All following · N" one row, per-team rows grouped Tournaments/Past, star=unfollow (`unfollowTeamEverywhere`), alerts pointer to Account·Notifications.
- **/simplify DONE:** consolidated 3 group-rank tables → one exported GROUP_RANK; login skips invite lookup unless fast-path viable; cache()-memoized getActiveMembershipRows + basic-coach scan (request-scoped dedup on Home); removed dead FollowRow busy state; deleted 3 orphaned components (FollowFeed/FollowFeedCard/FollowCardList). DEFERRED: warm-CSS composes convergence (→ P3-P5), coach-dedupe on /following (Home-scoped by design), lapsed-as-context-kind (broad blast radius).
- **/review DONE (high-risk, 4 lenses):** FIXED [High] All-following + Home mislabeled "not following anything" when getFollowFeed drops an unpublished/canceled tournament's card → now uses raw followCount (added to payload) + "game info unavailable" line vs. true empty; FIXED [Med-High] /api/consumer/home missing Cache-Control: no-store (added, matches sibling APIs); FIXED [Low] signup /home fallbacks → /discover. VERIFIED clean: both cache() wraps are request-scoped in route handlers (no cross-request staleness; membership dedup is load-bearing for active-vs-lapsed consistency — documented); anon-safe SSR; optimistic-unfollow fire-and-forget = consistent w/ app-wide follow pattern.
- **/docs DONE:** synced lib/help-content/tournaments.tsx fan-experience section — "Following tab" retired → Home ("one card per tournament, tap in") + All-following page; Scores-tab description UNCHANGED (Scores not restructured until P3); faq-following-tab anchor kept (no href breaks), keywords updated. ⚠ Doc copy is AHEAD of prod — must promote in the SAME bundle as the P0+1 code (don't ship docs separately).
- **Tail:** owner phone test → commit (per-action OK) + prod-promote bundle (code + docs together). Account trim(P5) NOT built.

**PHASE 2 — Search expansion (Teams + Organizations) BUILT ON DEV 2026-07-19 (uncommitted; typecheck + verify:changed + focused lint clean; endpoint smoke-tested live against dev; NOT owner-tested; NO commit yet). NO MIGRATION. Files disjoint from Phase 3 Scores + Phase 4 Chat.**
- **Reconciliation (owner-settled 2026-07-18, via AskUserQuestion):** plan §3d proposed a NEW `organizations.list_in_directory` column, but the DB-house-rule schema check found `organizations.is_discoverable` (bool, NOT NULL, default true) ALREADY EXISTS on dev+prod and already means "real, publicly-findable org" (today gates the coach→org link picker, `isNormalLinkableOrg`). **Owner chose REUSE — no migration** → fan-search visibility = the coach-link-picker switch (one flag, not two). Plan §3d + §4 updated to match. So Phase 2 dodges the migration/snapshot/dictionary workstream AND the Phase-4 mig-193 number collision entirely.
- **Search data layer (`lib/directory.ts`, additive):** `searchOrgsForDirectory` (predicate `is_discoverable AND is_public AND account_kind≠team_workspace AND plan_id≠team AND status≠canceled` → mirrors the `/{orgSlug}` page's own notFound gate so no result is a dead link; href `/{orgSlug}`) + `searchTeamsForDirectory` (scoped STRICTLY to accepted teams inside `list_in_directory` tournaments — same hard filter as Browse; a team in an unlisted/private event is NEVER searchable; href `/{orgSlug}/{tournamentSlug}/teams/{id}`; name+event only, NO PII) + `searchDirectory` orchestrator (fans out all three types on one call; tournaments reuse `getDirectoryListings({timeframe:'all'})`; returns `{q, tournaments/organizations/teams: {items, total}}`). Consts `SEARCH_MIN_CHARS=2`, `SEARCH_RESULT_LIMIT=8`. Same "keep user text out of SQL" JS-filter + SCAN_CEILING-warn pattern as `getDirectoryListings`.
- **Endpoint:** `/api/public/search?q=&types=` (NEW; force-dynamic + `Cache-Control: no-store` so never staler than the tournament pages a result links to; `withObservability`; public/anon, no auth, no PII). SW blanket-no-caches `/api/*` → NO denylist/CACHE_VERSION change (no new top-level route).
- **Home wiring (`DiscoverClient.tsx` + `page.module.css`):** typing (≥2 chars) drives a SEPARATE unified-search lifecycle (epoch-guarded, freshness-gated on `data.q===query` so no stale flash / no setState-in-effect). While searching: HomePersonalization + Browse HIDE, results take over in place (§3c); Browse `q` param REMOVED (Browse now filters by timeframe/sport/region only — search owns text). Type chips (`All` + Tournaments/Organizations/Teams) each render ONLY when that type has ≥1 result → **Teams chip never silently empty** (binding). `All` = titled groups w/ "See all" + honest "showing first N of M" cap note; single chip = flat list. Extracted shared `TournamentCard` (grid+list) reused by Browse and search. Warm tokens only; new `.resultRow`/`.chipRow`/`.resultGroup` styles.
- **/simplify DONE:** collapsed 4 near-dup result-render branches → one data-driven `sections` array (+`countOf` folded in); removed dead `.searchHead` CSS; extracted `scopeListedTournaments()` — ONE definition of "publicly-listed tournament" now composed by getDirectoryListings (main+facet) / getDirectorySitemapEntries / searchTeamsForDirectory (was 4 hand-copied chains; generic-cast helper avoids TS2589 on supabase builder); `includeFacets:false` opt-out so search skips the ~2000-row facet scan per keystroke; parallelized org-resolve+teams-fetch in team search (3 round-trips→2); AbortController on the search effect. SKIPPED (noted): org-predicate/result-row reuse (cross into team-org-links.ts + /{orgSlug} page + Phase-1 HomePersonalization — out of scope; SQL-filter vs JS-predicate shapes differ), getCanceledOrgIds double-call (concurrent, zero added latency), freshness/epoch "overlap" (FALSE POSITIVE — fix reintroduces the set-state-in-effect lint; both guards load-bearing).
- **/review DONE (high-risk, 4 lenses, gate green):** FIXED [High] search fetch had NO `res.ok` check → a 5xx (which returns valid JSON `{error}`) passed as a payload w/o `.q` → freshness gate never matched → infinite "Searching…" spinner w/ no error UI/retry; now throws on !r.ok, sets a query-keyed error payload, renders "Search unavailable — Try again" w/ a retry (retryNonce re-fires). FIXED [Low] org `${name} ${slug}` concat → cross-boundary false matches → rankByNeedle now matches each field independently (`o=>[o.name,o.slug]`). FIXED [Low-Med] activeType chip persisted across a full exit-to-Browse → reset to 'all' when the box drops below SEARCH_MIN_CHARS. NOT-FIXED (routed): [Med] directory scope keys on per-tournament list_in_directory NOT org is_public — PRE-EXISTING getDirectoryListings behavior (by-design per-tournament opt-in; team names already public); → logged to BUSINESS_DECISIONS.md as an open /strategy question. [Low] getCanceledOrgIds no `.limit()` — pre-existing, implausible at scale, advisory. Refuted: 80-char truncation stuck-spinner (both sides slice identically); epoch/abort/freshness coordination sound.
- **/docs DONE:** synced lib/help-content/tournaments.tsx — fan-experience `public-site-preview` section gained a "Search on Home" paragraph (finds Tournament/Team/Organization; team matches only from listed events, name+event only) + NEW `faq-home-search` FAQ; admin `faq-public-directory` now states listing also makes accepted teams findable in Home search; keywords+searchText updated on both. No anchors renamed (no href/hub changes). ⚠ doc copy AHEAD of prod — ship in the SAME bundle as the code.
- **/strategy DONE:** BUSINESS_DECISIONS.md 2026-07-19 entry — reuse-`is_discoverable` = **Decided**; the is_public-vs-directory interaction = **Decided 2026-07-19 (KEEP AS-IS — per-tournament `list_in_directory` opt-in stands; org `is_public` does NOT gate directory/search; no code change, no note UI)**.
- **Tail:** owner phone test → commit (per-action OK; stage ONLY these 4 pathspecs — `lib/directory.ts`, `app/(consumer)/discover/DiscoverClient.tsx` + `page.module.css`, `app/api/public/search/route.ts` — + plan doc + help doc + BUSINESS_DECISIONS.md + this memory; NEVER the concurrent Phase 3/4 files or mig 193). No prod DB dependency (no migration) → promotes with the P0+1 code bundle whenever it ships.

**PHASE 4 — Chat member inbox + moderation BUILT ON DEV 2026-07-19 (uncommitted; typecheck + verify:changed + focused lint all clean; NOT owner-tested; NO commit yet). One engine, per-shell presentation — reuses the mig-141 chat engine wholesale; NO new chat engine. Files disjoint from Phase 3 Scores.**
- **DB (mig 193, applied DEV only — ⚠ PROD-PENDING; dictionary + dev/prod snapshots refreshed, watermark #193):** `chat_message_reports` (service-role only, RLS-on zero-policy like fan_alert_prefs; denormalized org_id; UNIQUE(message_id,reporter) idempotent) + `chat_room_members.notifications_muted_at` (member SELF-mute — DISTINCT from organizer `muted_until`: silences push + drops from unread, but never blocks own posting). check:migrations shows the expected dev-ahead drift.
- **Data layer (lib/chat-service.ts, additive):** `getChatInboxForUser` (builds on `listRoomsForUser` — which already SELF-HEALS memberships on load, so no false "no chats"; adds sender-prefixed previews "You:/Organizer:/{name}:", event grouping, mute-excluded rollup); `setSelfMute`; `getUnreadTotalForUser` now excludes self-muted (universal — coaches can't self-mute so no behavior change for them); `postChatMessage` skips self-muted from the general push fan-out (mentions still pierce); report helpers `createMessageReport`/`listOpenReportsForRoom`/`countOpenReportsForRoom`/`resolveMessageReport`/`resolveReportsForMessage`. `ChatRoomListItem` gained optional `lastMessageSenderId`/`selfNotifMuted` (coach/admin ignore).
- **APIs:** new `/api/consumer/chat/inbox` (client-fetched, anon-safe empty, `Cache-Control: no-store` — mirrors /api/consumer/home; no per-user chat SSR'd); member `/api/chat/rooms/[roomId]/self-mute` + `/report`; admin roster GET now returns `reports[]`; moderate route gained `dismiss_report` + auto-clears a message's reports on `delete`.
- **Shared ChatPanel (additive, coaches/admin skins byte-for-byte unchanged):** `variant='warm'` root class → `.panelWarm` SCOPED token remap in ChatPanel.module.css (paper ground, white incoming bubbles, olive-tint outgoing, mono sender kickers, pill composer, LIME send via captured `--home-lime`) — never redefines a global dark token; `onLongPressMessage` hook (500ms touch hold / right-click, 10px cancel threshold) → consumer safety sheet. warmTheme.module.css gained `--home-lime` + `--home-*-rgb` triples.
- **Consumer UI (app/(consumer)/chat/):** page = anon-safe SSR shell → `ChatTab` (client branch: signed-out/fan → the UNCHANGED static `ChatPreview`; signed-in member w/ rooms → `ChatInbox`). `ChatInbox` = event-grouped rows (mono kicker, monogram, sender-prefixed preview, olive unread badge; muted = slash-bell + dim + no badge), master/detail full-swap → `ChatConversation` (`<ChatPanel variant="warm">` + `ChatSafetySheet`). `ChatSafetySheet` = long-press bottom sheet (Report to organizers [danger, hidden for own/removed] + Mute this room + Cancel; inline fixed overlay inside `.warm`, keyed by target id for fresh state).
- **Nav badge:** ConsumerNav Chat tab shows a red (`--danger`) rolled-up unread via `useChatUnread(signedIn)` + `/api/chat/unread` (now mute-excluded), desktop link + mobile icon.
- **Organizer:** ChatManagePanel gained a "Reported messages" queue block (Remove message [reuses moderate delete] / Dismiss) + a count badge on the Manage-room button; wired in the admin chat page. Admin chat SKIN otherwise untouched.
- **SW:** `/chat` already denylisted + CACHE_VERSION bumped in P0; inbox is client-fetched only — no change needed.
- **Verified:** `/chat` 200 (anon → warm preview), `/api/consumer/chat/inbox` anon → `{signedIn:false,rooms:[],unreadTotal:0}`, dev log clean (no EACCES/compile), dev server restarted clean.
- **/simplify DONE (4 lenses):** 12 behaviour-preserving cleanups — folded the self-mute lookup into the send-path recipient query (1 fewer round-trip); parallelized inbox role-lookup+hydrate; collapsed ChatTab's inbox/preview state (derive from room count); Map-based inbox grouping; dropped a pointless useCallback; de-duped the report-resolution payload; reused lastSeenLabel; one `runModeration` runner for 4 admin handlers; **new `lib/chat-display.ts` `roomDisplayName`** (centralized the "All coaches / division" rule across inbox + ChatRoomsPanel + admin); ConsumerNav per-tab badge MAP (not `href==='/chat'`). SKIPPED: reuse ChatUnreadBadge across 4 badge spots (per-surface colour/position + cross-module override fragility), effect-based sheet reset (would reintroduce the set-state-in-effect lint).
- **/review DONE (high-risk, 5 lenses + main-loop adjudication):** deterministic gate green (typecheck + token/snapshot/dictionary/org-context ratchets). **8 confirmed defects FIXED** — (1) [High] report table was table-wide UNIQUE not partial-`WHERE status='open'` → after a dismissal, re-report silently no-op'd with a FALSE "Reported" success → **mig 193 changed to a partial unique index** (`chat_message_reports_open_uniq`; DROP old constraint + re-applied to dev + snapshots/dictionary synced); (2) [Med] `--sender-tint` inline defeated the warm `.panelWarm .sender` override (inline beats class) — the /simplify altitude change was WRONG; now the inline tint is set ONLY on the dark skin and warm sets its color directly; (3) [Med] safety-sheet auto-close `setTimeout` wasn't cleared on unmount → could snap a LATER sheet shut → timer ref + unmount cleanup; (4) [Med] ChatInbox `refresh()` could clobber an optimistic mute → monotonic version guard; (5) [Med] safety-sheet report/mute failures were silent → error state surfaced; (6) [Low-Med] `delete` moderate action coupled to `resolveReportsForMessage` w/o isolation → best-effort try/catch; (7) [Low] long-press timer no unmount cleanup → added; (8) [Low] inbox event-group header could show a lone room name when the tournament-name lookup fails → `eventName` now null → client "Conversations". Security lens CLEAN (RLS zero-policy service-role, membership guards, org-scoping, PII, cross-room/cross-org all verified safe). Post-fix: typecheck + focused lint + hard ratchets all green; dev server restarted clean.
- **/docs DONE:** synced the chat guides for the Phase-4 flows — coach guide (`coaches.tsx` `#recipe-tournament-chat`): Chat-tab inbox framing + a "Keeping chat comfortable" para (press-and-hold → Report to organizers / Mute this room) + new FAQ `faq-tournament-chat-report-mute` (+ keywords/searchText); organizer guide (`tournaments.tsx` `#tournament-chat`): "Reported messages come to you" para + new FAQ `faq-tournament-chat-reports` (reports queue in Manage room, Remove/Dismiss, reporter anonymous) (+ keywords/searchText). No new sections/anchors renamed → no hub-card change. Flows to both customer + platform-admin help mirrors. ⚠ Doc copy AHEAD of prod — promote in the same bundle as the code.
- **Tail = owner browser test (member inbox/warm conversation/safety sheet/organizer queue need a logged-in member + realtime) → commit (per-action OK) → prod bundle MUST include mig 193 + the P0/P1 doc sync.**

**PHASE 3 (Scores restructure) BUILT ON DEV 2026-07-19 (uncommitted; typecheck + verify:changed clean for these files; NOT owner-tested; NO commit yet; NO migration).**
- New PURE contract `lib/scores-view.ts` (ScoresPayload/ScoresEvent/ScoresGameRow, REASON_LABEL/RANK+strongerReason, `dayOffset`, `buildScoresView` day-bucketing — same server↔client split as home-following.ts). New server resolver `lib/scores-feed.ts` (`getScoresFeedForUser` + `getScoresFeedForDeviceFollows`): UNION of coached basic-teams (Coach) + fan follows (Following) → My Games rows; org-admin/official orgs' tournaments (Staff/Official, bounded to active-or-completed-≤7d, cap 40 + log) → My Events tiles only; one getPublicTournamentPageData('schedule') fetch per unique tournament (Promise.allSettled clean-drop, reuses follow-feed batching); event rollup (live/today/next + 1-wk completed grace-drop). Reason precedence coach>staff>official>following.
- New endpoint `app/api/consumer/scores/route.ts`: GET=session union / POST {teams}=device follows (public, anon-safe like follows/feed); both `Cache-Control: no-store`. New poll hook `lib/hooks/useScoresFeed.ts` (visibility-aware, 30s-while-live / 5min-idle backoff, GET-then-POST device mode — mirrors useFollowFeed). New `ScoresClient.tsx`+`.module.css` (WARM: `[All][Live·N]` filter, My Events 2-col grid cap-4 + "+N more"/"Show fewer" in place, My Games Live-pinned → day groups → Show later/earlier caps, scorebug rows + W/L chip `#3E7A32`, signed-out/zero-follow platform live board + sign-in nudge). `scores/page.tsx` rewritten anon-safe (SSR live board only → ScoresClient).
- NO SW/CACHE_VERSION change needed (/scores pre-existing consumer route; /api/ blanket no-cache). Dev server NOT restarted — running server already serves /scores + GET/POST /api/consumer/scores correctly (verified 200 + correct payloads); avoided .next wipe to not disrupt parallel P4 agent. **One scope call flagged to owner: admin/official events = My Events tiles only (no My Games rows); premium rep-team game-rows DEFERRED.**
- **/simplify DONE (4 lenses):** collapsed a redundant event-fragment branch; dropped dead updatedAt+`.freshness`; de-recursed the poll mode switch; extracted DayGroupWindow (show-later/earlier); one `withinCompletedGrace` helper; **hoisted opponentNameFor/teamScoreFor to lib/game-status.ts (follow-feed now imports them — single source)**; resolveCoachedTeams now uses the exported `getBasicCoachTournamentTeamsForUserCached` (was double-scanning per poll); **added a narrow `'scores'` section to getPublicTournamentPageData (games+teams only, gated as 'schedule') — drops venues/divisions/announcements fetches every poll**; factored calendar-day math into `daysBetweenDateStrings` in lib/timezone.ts (scores-view dayOffset + calendarDaysBetween both use it). DEFERRED: shared poll-hook + batch-fetch extraction (would rewire shipped follow-feed), getUserAccessContexts slimming, route-parse + warm-CSS dedup (established per-module convention).
- **/review DONE (high-risk, 5 lenses, adversarially verified):** FIXED **[Critical]** event-tile sortMs bands were inverted (completed ranked ahead of upcoming) → new 0 / 1e15+nextMs / 2e15−endMs bands, verified live<upcoming<completed; FIXED **[High/Med]** hidden-schedule tournament (publicHiddenPages 'schedule') leaked a "Completed" tile + misclassified active events → both loops now skip `!data.pageEnabled`; FIXED **[Med]** rejected coach registrations lingered as Coach tiles → resolveCoachedTeams filters `status!=='rejected'` (pending/waitlist still surface); FIXED **[Med]** stale signed-out device payload survived a cross-tab follow change → renderedKeyRef reset clears a signed-out payload on teamsKey change; FIXED **[Med-High]** "Next up" kicker could sit above a finished game → now gated on a real `state==='upcoming'` row; FIXED **[Med copy]** Live-filter empty line → "None of your games are live right now". VERIFIED CLEAN: no cross-tenant/PII/auth/cache leak (POST public-data-only like follows/feed; 'scores' section preserves org/status/coach-name gates; SW no-store); all 5 shared-module refactors behavior-preserving for shipped callers (Home/Following/public pages, verified via caller-grep + typecheck). NOTED (not fixed): pre-existing follow-feed hidden-schedule name leak in Following feed (Phase 2, out of scope — route to a separate pass); signed-in re-GET on device-follow change (Low perf, by-design); cross-tab sign-in-while-open doesn't re-derive mode (Low, by-design); endDate-vs-actual-last-game for rain-delay (Low). typecheck + verify:changed clean; endpoints re-probed 200.
- **/docs DONE:** synced lib/help-content/tournaments.tsx fan-experience — `faq-following-tab` no longer says "Scores = platform-wide live board / discovery"; now describes the personalized Scores tab (My Events strip + My Games, live pinned) with the platform "live right now" board only as the signed-out/zero-follow fallback; `faq-public-directory` Scores mention reworded (board = the no-own-teams view); section keywords + searchText gained scores tab/my events/my games/live board terms. Coaches guide untouched (portal surface; the union's coach/staff auto-inclusion is self-explained by in-product reason chips). Content-only (hot-reload, no restart). ⚠ Doc copy AHEAD of prod — promote in the SAME bundle as the P3 code.
- **Tail:** owner phone test → commit (per-action OK) → prod bundle (code + docs together). NO migration.

**PHASE 5 — Account tab + shell polish BUILT ON DEV 2026-07-20 (uncommitted; typecheck + verify:changed gate GREEN — 0 errors, ratchets pass, no new literal hex; NOT owner-tested; NO commit yet). NO MIGRATION. The four owner decisions below were taken up-front via AskUserQuestion before any code.**
- **Owner decisions (2026-07-20):** (1) **Legal row = OMIT + flag** — no platform ToS/Privacy pages exist anywhere (no routes, footer has none, only org-scoped tryout-consent privacy); shipping a Legal row would 404, so it's omitted and flagged as a real content gap (installed-PWA users have no other path to these docs → needs /marketing + legal write-up before the row can exist). (2) **Help & support = mailto** `hello@fieldlogichq.ca?subject=FieldLogicHQ support` — no consumer-facing help page exists (all Help is admin/coach/platform-scoped); email is the only non-dead destination. (3) **Unified badge policy = RED "needs you" (Chat unread [built] + Home pending-invites [new]); Scores NONE** — red numeric count (cap 9+) means an action awaits; Scores keeps no nav badge (its Live·N is in-page; a badge lit every game-day would dull the red). (4) **Warm the shared nav = YES**, scoped to the four tabs only.
- **Warm token refactor (warmTheme.module.css):** split the token-only `.warmVars` (just the `--home-*` custom props) out of `.warm` (now `composes: warmVars` + the paper ground/flex). `warm.warm` still resolves to BOTH classes → every content surface unchanged. The nav wears `.warmVars` ALONE (tokens, no ground). (A concurrent agent had also added a `@media .warm::after` paper-strip fix over the mobile bottom-nav clearance — reconciled, kept.)
- **Warm nav (ConsumerNav.tsx + ConsumerShell.module.css):** ConsumerNav route-gates warmth — `WARM_PREFIXES` = /discover /following /scores /chat /account, with `WARM_HOLDOUTS` = /account/notifications (see deferral). On warm routes the topbar/bottomNav get `warm.warmVars` + `.topbarWarm`/`.bottomNavWarm`; new override rules paint solid paper-white ground, ink/olive links + active-tab, olive-tint active icon pill, warm live-red badges. Auth/select-org/suspended pages (same shell) stay DARK — the dark path is byte-for-byte unchanged (never redefines a global dark token; only `--home-*`). No literal hex (ratchet green).
- **Account tab (app/(consumer)/account/):** rewritten warm per §3g — outer `warm.warm` + `.accountFill` (full-width paper), inner max-width column. Order: Identity block → Notification settings (link) → **Install app** (new `AccountInstallRow.tsx`, client) → **Help & support** (mailto) → Sign out → pinned "Run a tournament" note. **REMOVED "Your FieldLogicHQ →"** (Home carries workspaces). Legal omitted (decision 1). Signed-out unchanged (Sign in [ink-on-lime primary] / Create free account / Run a tournament) + device-follow note. New `account.module.css` (warm settings rows). `AccountSignOutButton.tsx` re-skinned to a warm row (imports the account module by alias — its only user). Install row reuses the shared `InstallAppPrompt` (mounted in the consumer layout) via the `flhq:show-install` event (Android one-tap + iOS Add-to-Home-Screen + generic fallback), gated on `isInstallEligibleBrowser()` so it hides on desktop + inside the installed app (no dead row).
- **Home pending-invites badge:** new `/api/consumer/invites/count` (read-only `listPendingInvitesForUser` count; no reconcile — that rides Home's own load; no-store; no new top-level route → no SW change) + new `lib/use-pending-invites.ts` `usePendingInviteCount(signedIn)` hook (mount + visibility refetch, no polling/realtime — invites are rare; returns 0 when disabled without set-state-in-effect). ConsumerNav `badges` map now keys BOTH `/discover` (invites) and `/chat` (unread, unchanged); distinct aria noun per badge. Chat badge + on-Home PendingInvitationsCard both untouched (no regression).
- **Success-metrics instrumentation (§6) — NO migration (`platform_events.event_type` is free text):** reuses the existing platform-events store like the League §13 pattern. New `lib/consumer-events-client.ts` `fireConsumerEvent` (allowlist: `home_ready`, `home_card_tapped`, `chat_tab_opened`) + new `/api/events/consumer` ingest (accepts ANON — signed-out Home/Chat views are metrics; drops platform staff; rate-limited authed-by-uid / anon-by-fwd-IP). Extended `PlatformEventType` with 6 consumer events. CLIENT events touch only 2 committed surfaces: HomePersonalization (`home_ready {signedIn,workspaces,follows,invites,ms}` = TTI + composition + bounce baseline; `home_card_tapped {kind}` on workspace/following/lapsed cards) + ChatTab (`chat_tab_opened {signedIn}`). SERVER events (awaited, throw-proof) added to routes the interaction already hits: `directory_search {hadResults,types}` (/api/public/search, anon by design), `chat_inbox_loaded {roomCount}` (/api/consumer/chat/inbox — coach DAU), `auth_workspace_landing {hasWorkspace}` (/api/auth/destination — fast-path health, aggregate rate, no extra auth call). The "where did my stuff go" support watch is an OPERATIONAL helpdesk-tag watch (no code event).
- **DEFERRALS (flagged, not silently dropped):** (a) **Platform ToS/Privacy pages don't exist** → the Account Legal row is omitted; writing + linking them is a real follow-up (route to /marketing + legal). (b) **Notification-settings sub-page (`/account/notifications`) warm reskin DEFERRED** — 5 CSS modules incl. interactive push/toggle chrome on a high-stakes settings surface; a half-warm wrap (paper ground + dark cards) would look worse, so it stays fully DARK (self-consistent) and the nav stays dark there too (WARM_HOLDOUTS) — a clean theme handoff on navigation, not a mid-page seam. Warming it well is its own focused pass.
- **Files (stage EXPLICIT pathspecs only):** `components/consumer/warmTheme.module.css`, `components/consumer/ConsumerNav.tsx`, `components/consumer/ConsumerShell.module.css`, `components/consumer/AccountSignOutButton.tsx`, `components/consumer/HomePersonalization.tsx`, `app/(consumer)/account/page.tsx` + `account.module.css` + `AccountInstallRow.tsx`, `app/(consumer)/chat/ChatTab.tsx`, `lib/use-pending-invites.ts`, `lib/consumer-events-client.ts`, `lib/platform-events.ts`, `app/api/consumer/invites/count/route.ts`, `app/api/events/consumer/route.ts`, `app/api/public/search/route.ts`, `app/api/consumer/chat/inbox/route.ts`, `app/api/auth/destination/route.ts` + plan/PM-brief/this memory. NEVER the concurrent Phase-2/3/4 files, mig 193, or loose foreign files (.claude/settings*, pnpm-workspace.yaml).
- **/simplify DONE (4 lenses):** 6 applied — nav reuses `isConsumerShellPath` (dropped a duplicate WARM_PREFIXES list; drift-proof); deleted ~65 lines of orphaned ConsumerPage Account CSS (`.accountPage`/`.identity*`/`.accountActions`/`.blockBtn`/`.pinBottom`); factored a `.surface` composes-block in account.module.css (identity+rows); `CONSUMER_CLIENT_EVENTS` const is now the single source (client union type derives from it, ingest imports it); ingest reuses `clientIpFrom`; **the 3 server metric writes were moved to `after()`** (SUBSEQUENTLY REVERTED — see /review). 4 skipped: warm-nav token-remap (would redefine global dark tokens — binding constraint forbids), shared polling-hook extraction (touches shipped hooks), ingest-route factory (rule-of-three unmet), staff-check cache (none exists).
- **/review DONE (high-risk, 4 lenses + main-loop adjudication; gate + runtime re-verified):** **8 fixed, 2 accepted-with-note, 0 false positives.** [High] the /simplify `after()` change would SILENTLY DROP the 3 server metrics on prod — **Amplify Lambda SSR wires no `after()`/`waitUntil` bridge (`@next/request-context`) anywhere in the repo**, so post-response work isn't guaranteed → **REVERTED all 3 to awaited `writePlatformEvent` (throw-proof; one cheap insert; the tiny latency is the right trade for a reliable metrics deliverable). See reference memory [[reference_next_after_amplify]].** [Med] stale Home invite badge after in-place accept/decline → new `flhq:invites-changed` window event (PendingInvitationsCard dispatches, `usePendingInviteCount` listens + AbortController race guard). [Med] `home_ready` mis-fired on HTTP error (polluted bounce baseline) → fire only on a resolved payload. [Med] unbounded anon metadata → `boundMetadata` (≤12 primitive keys, strings truncated, nested dropped) in the ingest. [Low] `directory_search` `types` truncated; [Low] `chat_tab_opened` comment fixed (client-fired); [Low] invite-hook race guarded. [Med, docs] corrected the now-stale "don't warm the nav bar" premise in UNIFIED_HOME_TOURNAMENT_NAV_MERGE_PLAN.md. ACCEPTED w/ note: anon rate-limit keyed on spoofable XFF (documented `clientIpFrom` limitation, bounded impact after metadata cap); client self-reported metric fields (inherent to client analytics, bounded). Verified: forged server-event → 400, home_ready+metadata → 200, all surfaces 200, log clean.
- **/docs DONE:** synced tournaments.tsx `faq-iphone-alerts` — the Account tab's new "Install app" row is now named as a second add-to-home-screen path (installing gates iPhone alerts); keywords `install app`/`account tab` added, answerText synced. No anchors renamed; notification-settings path unchanged (those FAQs stay accurate); no stale "Your FieldLogicHQ" refs in help.
- **Tail:** owner browser test (warm nav on all 4 tabs + dark on auth/notifications; Account rows + install prompt + mailto; invite badge incl. resolve-in-place clearing it) → commit (per-action OK). Joins the SAME pending prod bundle as Phases 0–4 (mig 193 must precede that code on prod).

**Status: PLANNED (2026-07-18). Direction + 6 decisions owner-ratified; mockup rounds NOT started.**
Plan: docs/projects/active/UNIFIED_HOME_IA_REDESIGN_PLAN.md (+ PM brief). Successor design round to Unified App Consumer Layer (its deferred §8 item, expanded).

## Ratified direction (owner, 2026-07-18)
- Nav → **Home / Scores / Chat / Account** (same shape signed-in/out). Discover + Following tabs retired; routes survive (`/discover` stays the canonical SEO URL behind Home; `/home` → permanent redirect; `/following` = "All following" overflow).
- Home = search (Tournament/Team/**Organization** — NOT "League", plan-tier collision) + pending invites + Workspaces cards (full role-chip fidelity, reuse existing destination resolution — tier separation rule) + **Following: Tournaments** section + browse grid. Personalization is CLIENT-FETCHED (`/api/consumer/home`); SSR shell stays anon-safe (SW-cache PII pattern).
- **Two-tier follow model (load-bearing owner refinement):** team-in-tournament follow = tournament-scoped (highlights + notifications in that event); on Home it presents AS a tournament card with "your team" context line, tap → tournament pages. Persistent **Teams** follow (rep-team entity, schedules/scores across events, parent chat) = FUTURE phase, hard-gated on PIPEDA/CASL review. Resolves the no-season-entity + no-canonical-team-URL blockers by product definition.
- Sign-in fast-path KEPT (solo-workspace users land in their workspace at login); Home tab tap never auto-redirects; `?pick=1` retired.
- Scores = union of memberships + follows (coach sees own team unfollowed): My Events rollup strip (one chip per event, "● 3 live") + My Games day-grouped rows, Live pinned. Poll MVP, no realtime.
- Chat tab: member cross-context inbox (new aggregator + self-heal on load); logged-out/fan = STATIC preview only (never real messages — no anon read path exists at DB layer, keep it that way); report-a-message + self-mute ship same release; `/chat` needs SW denylist + CACHE_VERSION bump.
- Account slims to settings (profile/notifications/install/help/legal/sign-out); legal links currently absent from the whole consumer shell — this adds them.
- No family-member row (omit entirely, no dead slot).

## Mockup Round 1 — DECIDED 2026-07-18 (artifact de1c87a1, rev 3 = binding Home spec; logged to design_decisions.md)
**R1-4 = WARM LIGHT (C), consumer shell ONLY** (paper #F8F4ED, white 14px cards, sans names + mono retained for scores/kickers, lime→olive #57651E on warm ground; tournament/coaches/admin shells keep their themes — separate future decision; ALL later rounds render warm). **R1-2 = tap lands on TOURNAMENT HOME** (existing page: followed-team card + score dock already there; warm→branded theme handoff deliberate). **R1-3 = composition approved** (Search→Invites→Workspaces→Following·Tournaments→Browse; All-following sub-page: back link reads "← Home" (destination-naming, owner call) + "All following · N" title right-aligned on the SAME row (rev 4), star-unfollow, alerts pointer to Account·Notifications). **R1-1 superseded** — seam-grid retires from the consumer app. Browse section (search pills incl.) is IDENTICAL in every auth state — rev 4 fixed a fan-frame omission. Rev 4 = binding spec.

## Mockup Round 2 — Scores (artifact 63ec5baa, published 2026-07-18 — AWAITING OWNER DECISIONS)
Warm-light frames: game-day (events strip w/ Following/Coach chips + Live-pinned day-grouped My Games + "Show earlier results" cap), quiet-day (Next up, never dead), signed-out (platform live board + pitch). **R2-3 DECIDED (owner 2026-07-18): 2-col GRID** capped at 2 rows; "+N more" expands IN PLACE → "Show fewer" (never navigates off Scores; All following stays the manage surface). **Event lifecycle:** cards self-sort live → upcoming (by next game) → completed LAST (dimmed, "Completed · date", first behind +N, can never displace live/upcoming); completed events stay ONE WEEK after end then leave Scores entirely (remain followed under All following → Past events). Rev 4 folds the grid into all frames. **R2-1 + R2-2 ACCEPTED at the recommendations (owner 2026-07-18) — ROUND 2 FULLY DECIDED; logged to design_decisions.md; rev 4 = binding Scores spec.**

## Mockup Round 3 — Chat: DECIDED 2026-07-18 (artifact a8622786 = binding Chat spec; logged to design_decisions.md)
R3-1 inbox (event-grouped, unread rollups, mute excluded) · R3-2 safety sheet SAME RELEASE (Report to organizers → moderation queue + Mute room) · R3-3 rooms from the consumer app render WARM (coaches-portal/admin chat skins unchanged — one engine, per-shell presentation) — ALL ACCEPTED.

## PHASE M COMPLETE 2026-07-18 — all mockup rounds decided
Binding specs: R1 Home (de1c87a1 rev 4) · R2 Scores (63ec5baa rev 4) · R3 Chat (a8622786). **Account tab = NO mockup round (owner-accepted): build straight from plan §3g + established warm conventions.** Next: BUILD Phases 0+1 in a NEW chat via docs/projects/active/UNIFIED_HOME_BUILD_P01_PROMPT.md (owner-authorized 2026-07-18; includes /chat static placeholder + SW denylist). Sequencing: 0+1 first (one chat); then Scores (P3) ∥ Chat (P4) may run in parallel chats (disjoint surfaces, small frequent owner-OK'd commits); P2 search rides alongside; P5 Account/badges/metrics LAST; P6/P7 per plan. Union rule (coached/admin teams auto-included, role chip wins over Following) already ratified — rendered, not re-asked. Next after lock: Round 3 Chat.

## Phasing
M mockup rounds (blocking; resolve seam-grid vs rounded-card in R1) → 0 nav+redirects (+`next=`/email-template/manifest inventory) → 1 Home consolidation (+lapsed-workspace reactivate card, what's-new intro) → 2 search expansion (org `list_in_directory` migration; team search scoped to listed tournaments) → 3 Scores → 4 Chat → 5 Account+polish(+metrics) → 6 fast-follow whole-tournament/org follows (org landing page prerequisite; account-only, pin-independent) → 7 gated Teams phase.

## Provenance
12-agent workflow analysis 2026-07-18 (6 readers → 5 designers → critic); risk register (23 items) in the plan §10. Business decisions logged to BUSINESS_DECISIONS.md same day (logged-out-chat posture; two-tier follow model).
