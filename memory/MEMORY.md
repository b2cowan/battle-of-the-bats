# FieldLogicHQ Memory Index

- [Design decisions log](design_decisions.md) — binding visual rulings, newest first (latest 2026-09-07: ⚖ **A MONEY FIGURE HIDES AT ZERO BY ITS KIND, NOT BY ITS SCREEN** — on the dues drawer's tile row, its phone receipt and the player page, Fundraising / Other credits / Handed back are EVENTS and hide at zero; Dues / Paid / Balance are the BILL and always show (Paid $0.00 in the quiet ink); the table keeps its dash. REVERSES the ladder mockup's "zero terms stay visible" — three screens had drifted three ways for one family. ⚠ Second half: the player page reads the ladder in the ladder's ORDER — a row with no operator glyphs has made its order the arithmetic, so "same five figures" is not the whole test. Prior 2026-09-06: ⚖⚖ ONE TABLE STANDARD + ONE EXCEPTION REGISTER (`docs/agents/design/TABLE_AND_LIST_STANDARD.md` + `TABLE_EXCEPTION_REGISTER.md`), owner-approved from 319 rendered measurements — ONE AXIS OF DIFFERENCE DOES NOT LICENSE THE OTHERS; a difference is in the register or it is a bug. Seven rulings A-01–A-07 all as recommended: density by CONTENT (a caption makes a row comfortable, via `:has()`), platform body 14px mono, the register's phantom zebra DELETED (it named a token defined nowhere), worklists remember / reports open folded, public tables keep 16px padding (K-16), headings keep the display face, the money grids' private ladder stays (K-02). ⚠⚠ The finding: a global `td { font-size }` ELEMENT rule beat every table-level size — 15px was the app's commonest body size and NO stylesheet asked for it; the coach list hairline was PAPER on white in warm. The global th/td baseline IS the standard now; guard `table-recipe-guard.test.ts` (ladder membership + every named token must EXIST) and `check:layout` rules `control-width` + `type-ladder` + a `storage` hook that sweeps the report views. Prior 2026-09-06, ⚖ RATIFIED by owner QA on the built screen: A ROOM READS PLAN → ACTUAL → IDENTITY — what is owed, then what actually moved, then what the record IS; the fields go LAST (`RoomShell` grew a `fields` slot below History) and this EXTENDS the 09-04 "schedule leads, Details follows" ruling rather than reversing it. ⚠ General form: order a record’s blocks by how often they are READ, not how permanent they are — a block set once at creation never belongs on the path between two blocks that explain each other. Plus A SUMMARY EARNS THE ROW ITS DETAIL VACATED (identity line on the doors’ row, built FROM THE DRAFT because the fields autosave and the list re-reads on a delay; nothing invented for an empty record). And ⚠⚠ A CHIP THAT REPORTS A BALANCE MAY NOT WEAR A DELTA’S MARK — "still owing ▼ $523.00" on a $17.00 payment: the number was right and the ARROW was the lie, so ▲/▼ means a change and → means where it ends up; a bare figure was rejected because an absent mark reads as an oversight. ⚠ A screen reader was never wrong here — an accessible reading is not evidence the sighted one is sound. Rejected: nesting payments under installments (a payment SPILLS FORWARD, so nesting invents a row), and collapsing the fields behind a second fold. Also 2026-09-06, ratified on the built screen by QA §143+§144 7/7 zero flags: ⚠⚠ A PANEL WHOSE ONLY EXITS ARE ITS ROWS DEAD-ENDS FOR ANYONE WHO CANNOT WRITE — the month grid’s plan panel hands navigation to its content (every budget line a link that lands on the line, which is why the owner declined a button “for consistency”), and the SAME write gate that removes the links removes the only way out, beside a twin panel whose doors were never write-gated at all; the rule is A DOOR APPEARS EXACTLY WHEN THE ROWS ARE NOT LINKS, one button in the siblings’ corner, invisible to a writer. ⚠ General form: when a surface hands its navigation to its content, audit it for every reader whose content is INERT. Plus A DOOR TAKES THE NAME OF THE PLACE IT OPENS — shipped “Open Budget Plan” over the mockup’s “Open the budget”, put to the owner as the walk’s one open question and returned without a note, so the deviation is RATIFIED not pending. And carried from §143 as a design rule: A GUARD THAT WATCHES CLICKS DOES NOT PROTECT STATE A DEEP LINK WRITES — arriving at a form by address is a write onto it, the Back button is not a click, and an overlay that covers the way out makes Back the only exit from a dirty form. Prior 2026-09-04 later: A FIGURE A READER CANNOT INSPECT IS NOT AN ANSWER — both figures on a Budget-vs-Actual row open the records behind them, the actual column too; a report that names the records behind ONE column must name them behind the other; the permanent "$X paid · $Y back" sub-row is DELETED into that panel; ⚠ where the objects behind a figure are not ALL openable the panel STATES and never links (four doors, three of them 404); ⚠ a payload optimisation outlives its reason SILENTLY — stripping those records broke no render, it opened an empty panel. And ONE STATUS, ONE CARRIER: "not planned" is REMOVED beside the tint — third ruling on the same two square inches (trimmed 08-15 → restored D5.5 09-02 → removed 09-04) and the evidence is that a redundant signal RE-ASSIGNS meaning to its neighbours, so the tint read as a GROUPING; the non-visual carrier is NOT the tint, a hidden sentence replaces the word or the fact leaves the product; screen copy and help copy move in ONE unit of work (they contradicted each other for two days). Plus: a table has ONE opening gesture at every level, and A WALK THAT PASSES CAN STILL MISS THE ROW — §132 passed 42/42 with all three defects on the default screen, because no step said "read one ROW". Prior 2026-09-04: THE BILL’S PAGE DISSOLVES INTO A ROOM over the Ledger — and three rules outlive the phase. (1) ⚠ A LIST’S LAST COLUMN MAY KEEP ONE WORDED ACTION BESIDE THE CHEVRON: this QUALIFIES the 09-03 one-shape ruling rather than reversing it — the test is whether the control fires on MOST rows or on an EXCEPTION (the §134 pill fired on a minority and charged its width to every row; the Ledger’s Record fires on nearly every unpaid row, and the by-due-date view is the month-end instrument). The chevron itself is UNCONDITIONAL on every row — these are plain clickable table rows, i.e. no keyboard door at all — and `.cardActionCorner` does NOT apply to a cell that also holds a worded button. (2) A RECORD’S NAME IN THE TITLE SLOT TAKES NO REQUIRED MARKER (title-slot exemption; settles the plan’s open Name item). (3) IN A ROOM THE SCHEDULE LEADS AND DETAILS FOLLOWS — the reverse of the page, SAME 2026-07-09 rule, because the tiles never scroll. ⚠⚠ And three shell defects only a stacked overlay exposes: a hand-rolled overlay has NO accessibility floor (Escape closed the room under the question), a collapsible section seeds its open state ONCE at mount, and `justify-content: space-between` does not right-align a LONE child. Prior 2026-09-03: THE WARM MUTED INK IS #615A54 — corrected a SECOND time; the lesson is the GROUND LIST, not the colour. Two grounds the palette test never held (money card row #E0DBC9, empty-state chip #D9D8C5) were both found by the RENDERED sweep. ⚠ The first attempt (#655E57) cleared only the ground the finding named and passed a SCOPED run — a portal-wide token change needs a portal-wide sweep. Fix a token defect at the TOKEN, never at the call site (605 usages). Re-opens below rgb(211,210,191). Prior 2026-09-02: THE LEDGER TOOLBAR SPLITS BY VOLATILITY — two decks (static cast up top: View, cash, Export, create; per-view filter strip under a hairline; the owner's own static-vs-changing reasoning recorded); prior 2026-09-01: ONE TAG IDIOM — the tag manager is a DRAWER over the form, ratified hands-on from a clickable prototype (the portal's FIRST and ONLY working-surface side sheet without a further ruling); one door label "Manage tags…" as the LAST ROW of every tag picker, a FILTER never carries a door; delete-in-use orphans WITH the count and the confirm offers "Merge instead" live; shared tags listed read-only with one sentence; usage counts in the manager only; a Tags shelf under Team settings; olive = yours / blue = the club's, everywhere. Prior 2026-08-31: the "where it lands" preview retires — a form states the consequence in ONE sentence, in words true in every mode; and A DRIVE OPENS IN PLACE — entries-first, the drill-in retires, one act at a time while an inline editor is open. ⚖ RATIFIED 2026-09-06: the 2026-09-04 Player Dues "set once, chase weekly" entry is walked and settled (QA §140 ✅ 37/37, zero defects) — "Showing" keeps its two jobs, the reminder preview keeps showing a SAMPLE letter, and the never-paid nudge now stamps; do not re-open.)
- [Iterate visual fixes with Playwright](feedback_iterate_visual_with_playwright.md) — for CSS/alignment bugs, drive the app + measure computed heights/screenshot, don't fix blind on user screenshots
- [Agent review workflow](agents/codex-subagent-coordination.md)
- [Agent verification workflow](agent-verification-workflow.md) — resource-aware checks; ⚠ `verify:changed` now also fails on a NEW dead/clashing CSS class (`check:css-selectors`) and on unlisted files at the repo root (`check:root`)
- [League onboarding wizard](league-onboarding-wizard.md)
- [Tournament free-tier UX cleanup](tournament-free-tier-ux.md)
- [Launch pricing positioning](launch-pricing-positioning.md)
- [Pricing strategy pointer](project_pricing_strategy.md) — canonical facts live in PLAN_PRICING_FACTS.md + BUSINESS_DECISIONS.md; safe-in-memory durable facts only (latest 2026-08-23: roster export = Premium Coaches Portal inclusion, ratifying shipped behaviour — no feature key, the gate is architectural; ⚠ never read plan inclusions from lib/export/catalog.ts, its minPlan/moduleGate are documentation nothing enforces)
- [Billing plan invariants](billing-plan-invariants.md)
- [Platform admin information architecture](platform-admin-information-architecture.md)
- [Pitch Deck Studio](project_pitch_deck_studio.md) — ALL FOUR STAGES on dev 2026-08-21 (A library view · B pull editor · C composer · D prospect links), QA §72/§76/§77/§78, migs 257–258 dev-only; stage C: decks are ROWS (`pitch_decks`) — reordering a deck re-orders its live page MECHANICALLY (resolvePullIds takes the LIVE deck; "never blank" outranks "always a subset"), `deckProblems()` refuses held numbers BY NAME and allows cross-audience mixing on purpose, #18–#20 STAY held; stage D: every owner deck mints an unguessable /pitch/<slug> at birth (noindex, no fallback — broken 404s; the prospect label never reaches the page BY CONSTRUCTION; deleting the deck IS the revoke; PDF = the link printed); ⚠⚠ DECKS ARE DATA / SLIDES ARE CODE — never add an edit affordance for slide COPY; stage B made the two public pulls rows (`pitch_page_pulls`) with the code `fallbackPull` as a permanent safety net (lenient `resolvePullIds` read; a broken/slow/rotten row can NEVER blank a page), ONE rulebook `pullProblems()` shared by the save API and the guard test, meta + SEO description DERIVED (byte-identical for today's pulls), and F4 CLOSED — `pageAnswer`+`seoPhrase` are required slide fields (the ten missing answers were code-verified first: the lineup flag is PER-GAME, the family link is FOLLOWER-tier only); the pull editor has NO order control on purpose (deck order is the invariant); the picture column is DECLARATION health not freshness (a failed re-capture leaves the old PNG, so stale passes CI) and success criterion 4 is NOT met; a DRAWING has no manifest entry so it is neither passed nor failed; the plan-line column was BUILT AND DELETED the same day — the rule that replaced it is a BUILD CHECK, which is stronger than a column
- [Local dev server launch](local-dev-server-launch.md)
- [Non-billing UAT remediation](non-billing-uat-remediation.md)
- [Next.js framework conventions](nextjs-framework-conventions.md)
- [Tournament Plus subscription positioning](tournament-plus-positioning.md)
- [Tournament Experience Excellence](tournament-experience-excellence.md)
- [Tournament scorekeeper experience](tournament-scorekeeper-experience.md)
- [Automated schedule generator](automated-schedule-generator.md)
- [Export strategy](export-strategy.md)
- [Import strategy](import-strategy.md)
- [Help documentation planning rule](help-documentation-planning.md)
- [Standalone Team workspace](standalone-team-workspace.md)
- [Coaches Portal unified product](coaches-portal-unified-product.md)
- [Coach Insights reports portal](project_coach_insights_reports_portal.md) — seven tabs on one page, no money anywhere, live-season-only by construction; P1 dev 08-18 (QA §58 passed), P2 dev 08-19 (QA §61 owed). ⚠⚠ P2 took THREE owner decisions BEFORE any code and TWO reverse the approved mockup: the attendance table may not sort or badge a child (a support read that ranks children is a leaderboard however neutrally drawn — guarded in SOURCE, because worst-first is one .sort() whose unit tests would all still pass), and arm care may not draw an innings budget (the product stores NO weekly or season ceiling, only a per-game cap the coach set, and inventing one is the product proposing a figure as though it were a rule). Third: RSVP reply rate renamed Recorded — nobody replies, so check the actor performs the act before naming a rate after them. ⚠ Two fixture bugs found, both hid features behind a green check: .maybeSingle() on a deliberately-duplicated name ERRORED and the discarded error re-seeded six games on every run; and the demo built all six lineups from ONE grid so the new recency matrix rendered one number repeated.
- [Coach Payables Rebuild](project_coach_payables_rebuild.md) — a commitment holds INSTALLMENTS (the plan) + PAYMENTS (what happened), mirroring dues since mig 232; approved 08-19, QA §64. ⚠⚠ ORIGIN IS THE LESSON: §27 PASSED CLEAN and the owner still found FOUR defects — a checklist of existing behaviour cannot find an ABSENCE. No partial payment, no undo on Mark paid, a paid Schedule row that is a dead end, nothing repeats; and `Schedule | Commitments` showed a PARENT AND ITS CHILDREN as two reports. Settled = paid IN FULL, partly paid counts as UNPAID; over-payment is ACCEPTED not refused; a payment attaches to the COMMITMENT and applies oldest-unpaid-first. ⚠⚠ Bulk scopes may NEVER touch a settled installment, but “this payment only” STILL EDITS one (2026-08-16 ruling — locking it reverses a live ruling). ✅ The monthly engine was ALREADY BUILT (`c404bd4b`, 43 tests, no callers by design) — the owner said so and an agent said otherwise: SEARCH docs/projects/ before claiming a feature does not exist. Supersedes + archives COACH_RECURRING_PAYABLES_PLAN. ✅ **P1–P5 ON PRODUCTION 08-27** (prod HEAD `7f21df47`, job 260); §64 A–E walked and passed, F/G/H closed UNWALKED 08-21 and restated as the release check. ⚠⚠ **That deferral said "none of this work is on production" — IT SHIPPED, so F/G/H are LIVE checks now and H (the demo money story) is OVERDUE, not pending.** ✅ **Dead columns dropped — mig 270, dev 08-28, PROD OWED:** nine columns + 2 FKs + 2 partial indexes; `amount` stays (R2). ⚠ `deposit_amount` has ~93 hits and nearly all are the TOURNAMENT fee block or importer field names — **attribute every hit to its TABLE**. ⚠⚠ **A DROP IS INVISIBLE TO `check:migrations`** (it only sees prod-behind-dev, same blind spot as mig 264's data-only DELETE) — `check:schema-parity` is the gate that catches it. ⚠⚠ P4’s lesson: **a cap and the EDITOR that forced it must lift in ONE change** — the 2-piece cap existed only because the form was a deposit/balance two-field editor, so raising it alone re-creates the silent truncation; the export’s 4 Deposit/Balance columns and the typed Total went with them. **Monthly only, no cadence control** (§4.1’s Weekly/fortnightly were written before anyone read the engines). ⚠ **A positional guard fails BOTH ways once a plan can hold six** — `updateRepTeamExpense`’s “dropped piece carries money” test refused legitimate deletes and never fired on the case it was written for; replaced by the OUTCOME test `whyPlanStrandsPaidMoney`. ⚠ `check:layout` CANNOT see a modal, so it measured none of P4. → **NEXT (owner-called 08-21, planning only, opens when this closes): “where does a coach log money?”** — the MODEL converged (installments+payments both sides) and the DOORS did not (Transactions, Payables, Dues, Fundraising, Club each grew their own add-form). Carries §64’s absence as one question, not two: a family can front a WHOLE cost but not one PAYMENT of a bill, because a payer is a field on the cost form and a payment record has no payer. Brief = §9 of the payables plan. ⚠⚠ /review then caught a MONEY defect: a POSITIONAL row key meant removing a mid-list paid installment rewrote its payment (and its ledger entry) to a different figure — **audit every position-keyed match when a list becomes editable in the middle**; fixed by naming rows, verified by a live DB round trip. ✅ **§64 C+D WALKED 08-21 — five findings, every one an ABSENCE or a FALSE SENTENCE.** ⚠⚠ One stale claim lived in FOUR places (the arithmetic, the grid, the EXPORT, the on-screen note) and each was "fixed" while three copies stayed live — **a behaviour lives in more than the code that computes it; sweep copy + exports + help after changing what a screen MEANS** (a fifth copy sat in the help and was found only by a review lens). The 2025 prior-season column and the money-tag filter were both REMOVED from Budget vs. Actual — ⚖ **the tag filter WORKED**: it narrowed SPENDING while the PLAN stayed whole (a budget line carries no tag), so **HEADROOM ROSE as you filtered** ($8,905→$10,900) — a comparison report cannot half-filter a comparison; tag filtering stays on Transactions, which lists rather than compares. **Budget vs. Actual evaluates THIS season only.**
- [Money arithmetic has one home per question](coach-money-one-arithmetic.md) — cash on hand · the season close-out pot (⛔ deliberately separate) · the report; on Budget vs. Actual a feed that walks the raw records for itself is a DEFECT. Enforced by `tests/unit/money-one-arithmetic-guard.test.ts` + `npm run check:money-report` (which **fails over a fixture too thin to disagree**). ⚠ Two things not to "fix": the two date rules are both correct (org-noon stamps vs click-time instants), and the Months grid's month-splitting of a commitment was always the right one
- [Free Tier strategy project](project_free_tier_strategy.md)
- [Dev database schema](reference_db_schema.md) — every table and column, regenerated from the LIVE dev database by `npm run refresh:snapshots`; never hand-edited, and never a substitute for the snapshots when deciding whether a column exists
