# FieldLogicHQ Memory Index

- [Design decisions log](design_decisions.md) — binding visual rulings, newest first (latest 2026-08-25: NULL IS NOT ZERO WHEN A SEASON CARRIES MONEY FORWARD (mig 262, D-2) — a season’s opening_balance reads null for "nothing was carried" (every opening line hides; the settings row says "None carried") and 0 for "carried at exactly zero" (a figure shows): same number, different facts, and a `?? 0` in ANY reader collapses them. ⚠⚠ AND A GUARD OVER A FIXTURE THAT CANNOT DISAGREE IS NOT EVIDENCE, for the third time in this log: check:money-report’s claim 6 was a hardcoded `const opening = 0` naming ITSELF as the thing to change the day carry-forward shipped — it now reads the field BY NAME and treats an ABSENT field as a FAILURE not a zero, but that alone was not enough (over a season carrying nothing the claim adds zero and is arithmetically the hardcoded one it replaced), so the UAT fixture now CARRIES $500 and the guard exits 2 when a run’s fixture has no carry; same reasoning added a gate for revenue groups with ROWS behind them. Structural: the register’s 500-line book assembly moved to lib/coach-register-book.ts because Start-next-season writes the carried figure PERMANENTLY and must compute it from the register’s own walk rather than become a fourth arithmetic — dues-definition-guard fired on the emptied route, which was it WORKING, and the entry MOVED WITH THE CODE (loosening its pattern would have unguarded the dues arithmetic in its new home). ⚠ ORDER THAT MATTERS IS ORDER THAT IS STATED: "Paid back to families" sat at the foot of the expense band BY ACCIDENT (event-only categories are appended last) and giving it real per-family rows moved it into the planned list, where it would have jumped to the TOP of a treasurer’s table. ⚠ Panel chrome: an ITEM panel is a FILTER of its category’s list (every record carries its grid ROW key) never a second copy in the payload; GridLineResult gained itemId rather than every reader splitting it back out of the row id (a category NAME may contain a pipe — how the LAST drill-in here broke); and which doors a panel offers + the word over its total live in ONE function including the expense band’s two answers ("Possible" never "Total" on a pledge). Also 2026-08-25: THE PHONE TITLE BAND STAYS AND ITS HEIGHT WAS NEVER THE TITLE — the 44px band is the TAP FLOOR ON THE HELP "?" (the h1 is ~25px of it, the tile 36px), so deleting title TEXT saves nothing and the portal-wide prize is 4–8px a screen; a band's height belongs to its tallest CONTROL, look there before blaming its text. APPROVED: margin 16→12px at ≤640 + Overview's setup chip takes its phone SYMBOL form so it joins the title line (116→56px — the one large number, and it belongs to ONE screen), and the help "?" moves to the masthead's corner **AT EVERY WIDTH, rendered LAST** framed as HELP PLACEMENT NOT SPACE (8px on a phone, **0px on desktop** — the create "+" deliberately does NOT follow it, because a page-scoped action in a bar reading "U13 Rockets" cannot say what it will add). ⚠⚠ It was approved PHONE-ONLY and the owner's follow-up caught the drift — the 08-11 ruling says the "?" is chrome "always LAST, top-right AT EVERY WIDTH … one findable home", so phone-only gives it TWO homes. ⚠⚠ **NOT the top strip**: that strip holds "only genuine leave-this-place doors" and the CHAT icon was REMOVED from it 07-31 for the two faults a "?" repeats (not an exit; duplicates a door reachable at the same width) — the sidebar already carries a Help item ~200px away. ⚠⚠ **AND I HAD TO CORRECT MY OWN JUSTIFICATION**: "help stops scrolling away" is TRUE ON DESKTOP (that masthead never collapses → permanently visible at ZERO pixel cost, since the band is set by the 36px tile and the "?" is 34px) and **FALSE ON A PHONE** (the collapsed bar hides its whole right slot, so it folds away anyway) — ruled to fold with the collapse, the 08-02 bare-name rule NOT reopened, a carve-out raised and left to the owner. **Generalise: check whether a benefit survives the SCROLL STATE of the bar you move a control into, not just its width.** **BUILT + MEASURED same day, and the measurement CORRECTED TWO OF MY OWN CLAIMS**: ⚠⚠ the "?" move is worth **0px on most screens, not 8px** (44→36 only happens where the band has NO action; with a create in the corner the 44px BUTTON still sets the floor), and ⚠⚠ **the phone masthead GREW 3px** (56→59: a 44px tap target in a ~41px row), which eats the margin trim — **measured net at 390: Overview −57px, actionless screen −9px, ordinary corner-action screen −1px, desktop 0px**; never quote the 4px trim without the 3px the bar took back. ⚠ 900–1100 is CLEAR (row 41px = one line at 900/1000/1100/1280/1440, no wrap, no sideways scroll); desktop scrolled 600 the "?" is STILL VISIBLE, phone scrolled the bar collapses to 36 and the "?" goes with it. ⚠⚠ **MOST DURABLE LESSON = a regression it caused and measuring caught**: reserving 44px on the masthead row (so the "?" landing after hydration could not shift the bar) also reached the COLLAPSED bar — a scrolled phone went from its ruled 36px to **55px**, a 19px permanent cost to buy back 8px — **a reservation added for ONE state of a bar applies to ALL its states.** ⚠⚠ NOT DONE — folding the page name into the masthead: THE EMPTY RIGHT HALF IS NOT SURPLUS, IT IS A SLOT THAT FAILED (emptied 08-24 because a short string could not be made to fit at 390px; two shrink attempts failed) — filling it makes the UNBOUNDED TEAM NAME the compressible half, and an identity bar whose identity gives way to a page name has stopped being one; the scroll collapse would also make the page name vanish-and-return. BEFORE FILLING AN EMPTIED SLOT, FIND OUT WHY IT EMPTIED. ⚠ The band is NOT direction D (D is a DESKTOP merge, its strip is display:none ≤900, so 0px on a phone) — the conflation had reached two shipped docs, both corrected. ⚠⚠ WITHDRAWN, and this is the durable half — DO NOT RENAME THE PHONE BAR'S FIFTH TAB to the section you are in: the "you are here" signal is ALREADY BUILT TWICE (the More tab takes the full active treatment inside a More section, and the sheet marks the current row), so the rename was the only addition and it is the risky half; NO EFFECTIVE PRECEDENT EXISTS and the canonical one does the opposite (UIKit's auto More tab has kept "More" + ••• since 2008 and puts the section name in a TITLE BAR AT THE TOP — the same answer this ruling reaches by another route; Material keeps bottom-nav labels STATIC; the one dynamic fifth tab in wide use is a PROFILE tab where the avatar changes and the label does not); and it would ATTACH A NUMBER TO THE WRONG NOUN — that tab carries the notification count, so renamed to "Money" the pip reads as two things waiting in Money. Owner asked for a precedent and the honest answer — there isn't one — killed my own proposal. Also 2026-08-25: ONE REQUIRED MARKER, AND IT IS NOT RED — a required field takes a PLAIN asterisk in the label ink and the dedicated red marker is RETIRED, because in this portal red means something has GONE WRONG (money owed, overdue, a refused save) and a field is not in error for being required — colour crying wolf on the calmest signal a form has. ⚠ The state it corrects was NOT a screen-by-screen split but MIXED INSIDE SINGLE FILES (44 labels / 8 files, 29 red vs 15 plain, with Player Dues AND the money record form each using both), and it reaches BEYOND money (roster, schedule, head-coach editor, start-interest) — do not scope it as money work. SWEPT 2026-08-25 in money-centralization P3 and THE MEASUREMENT ABOVE WAS UNDERSTATED — really 32 red across 9 FILES (the Club tab carried its own .req copy and was missed) and ~58 sites across 11 files, so a session scoping it from that paragraph would be wrong about where it reaches; roster and schedule turned out CLEAN, the two dirty files were the money ones. THE CSS IS DELETED NOT MERELY UNUSED — both portal rules plus FOUR LOCAL COPIES, each replaced by a headstone, because a rule left in place is a rule the next form finds (one of the four was --home-live not --danger, so the marker had already drifted into two reds). SCOPE RULED: COACHES PORTAL ONLY — 12 further red markers outside it (public tryout + league registration, one platform-admin) were left alone deliberately and a future session must not finish the job without its own ruling. Found because the owner spotted ONE red asterisk among a dozen grey ones on Date paid. Before that 2026-08-24 (BvA Months): THE ANSWER IS THE LOUDEST ROW, NOT THE SUBTOTAL — the band subtotals were shouting over the Running balance (the figure the view exists to deliver, pinned so it never scrolls away); A SHARED TREATMENT CARRIES AN ASSUMPTION ABOUT POSITION AND COUNT — the heavy closing-total recipe was written for ONE total at the foot and became wrong when two landed mid-table, with nothing editing it. Band headings separated by SPACE not more colour; "Paid back to families" is the one expense row allowed to disappear (no plan, no schedule, ever). ⚠ Fell out of it: the band totals set colour on the VALUE span above the .pos/.neg classes, so DIFFERENCE's green/red was silently plain ink. Also: A PINNED COLUMN MAKES THE WHOLE TABLE ITS PROBLEM — a sticky column forces the WHOLE table to paint a ground and to draw hairlines as INSET SHADOWS not borders (collapsed borders live in the table layer and do not travel with a sticky column); the label-column wrap rule had been a silent no-op, outranked by the table nowrap; the current month is a TINTED COLUMN not a left border; THREE of five were first fixed per-row and re-reported within the hour. And ONLY THE CLUB ROW IS EVER RENAMED BY A LENS (an unpaid instalment is still dues; a request nobody answered is a question, not money back). Also 2026-08-24: THE IDENTITY BAR'S RIGHT-HAND SLOT DOES NOT EXIST ON A PHONE, GAME DAY INCLUDED (owner: "coaches know they have a game that day") — a game-day carve-out was argued and OVERRULED CORRECTLY because it did not fit either (two attempts to shrink it failed: one printed OVER the meta line, the other truncated the bench-console door to "6:30 p.m. · O…" — a door ending in an ellipsis is not a door) and the door was never homeless (Schedule is on the phone bottom bar and its game row grows its own Game day action); ≤640 not the masthead's usual ≤900, desktop untouched, and the hide is WRAPPED so the public-site flip survives (it shares that corner but is a DOOR). ⚠⚠ Same pass: A FORM FIELD'S RULE WAS DECIDING A TOOLBAR'S LAYOUT — .segChoice's ≤640 full-width rule, whose own comment names Home/Away/Neutral, reached Roster's view toggle, took the whole row, and pushed the export into a wrap that put it BOTTOM-LEFT, breaking house rule 2's "pinned right at EVERY width": a shared class whose COMMENT names a context has a scope its SELECTOR does not state. And a COMMENT WAS TRUE ABOUT THE WRONG THING — .teamHeaderMeta proves a separator cannot orphan when a segment is ABSENT, which is not the WRAP case that was orphaning it. ⚠ The export could NOT be proved from the UAT fixture (its phone-surviving choices are plan-gated and that team lacks the plan — a green run would have been a check over an empty fixture); proved by injecting a probe into the real cascade. Text/ink rule from that pass: the masthead status value is a SENTENCE so it takes --font-sans, never the data face. Before that 2026-08-24: A PINNED COLUMN MAKES THE WHOLE TABLE ITS PROBLEM — a sticky column forces the WHOLE table to paint a ground and to draw hairlines as INSET SHADOWS not borders (collapsed borders live in the table layer and do not travel with a sticky column, so they round apart at fractional row heights); the label column wrap rule had been a silent no-op, outranked by the table nowrap; the current month is a TINTED COLUMN not a left border. ⚠ THREE of five were first fixed per-row/per-lens and re-reported within the hour — a pinned column is a property of the TABLE. Same day: ONLY THE CLUB ROW IS EVER RENAMED BY A LENS (dues/sponsorships keep their names — an unpaid instalment is still dues; a request nobody answered is a question, not money back), and the D-2 drill-in model — chevron opens WHERE THE MONEY CAME FROM, the number opens WHAT MAKES IT UP (read-only), at most TWO doors: the ledger and the thing itself. Before that 2026-08-23: MONTHS IS THE SEASON'S CASH STATEMENT — Option D, owner-approved from the mockups and D-1 BUILT: a REVENUE band over the EXPENSES categories plus a "Paid back to families" group, a total closing each, a Net-for-the-month row and a Running balance whose PINNED Total cell carries the ending value (Cash on hand never scrolls away); CASH IN BOTH BANDS is the load-bearing call, so a per-item figure can differ from the Statement's — two labelled truths, one guard each. ⚠⚠ THE GUARD RULING: "statement = grid" is DELIBERATELY DELETED from check:money-report (it would now fail on any team ever refunded a dollar) and replaced by BOTH BANDS = THE REGISTER month-by-month, category-by-category and group-by-group, plus opening + net = ending = Cash on hand — and the one real coverage loss (the chart lost its per-month partner) is written into that script's header rather than absorbed. DIFFERENCE IS SIGNED PER BAND — costs read plan−actual, revenue reads actual−plan, or a dues shortfall prints green like an under-budget cost. A footnote's TENSE is part of its truth ("on this plan you go short" was wrong under Actual, found by reading the public demo back). Same day, earlier: "MONEY IN IS PLAYER DUES ONLY" REVERSED — the BvA cash-flow strip owes ALL cash both directions with a register-identity guard; the 07-30 exclusion outlived its model through three changes — a footnote explaining a rule is its expiry checklist; build assigned to BVA_MONTHLY_INCOME_BUILD_AND_PLANNING_PROMPT.md. Before that 2026-08-22: A FORM FIELD THAT CHOOSES ONE VALUE IS A DROPDOWN, NOT A SEGMENTED PILL ROW — owner convention going forward, set at the money P1 gate; supersedes mockup 02's five-segment "How" row on that point only; does NOT touch the 08-19/20 reporting-filter pill convention (report toolbars keep their labelled pills) and radio rows with per-option sub-text are unaffected. Before that 2026-08-21: THE MEASURE IS THE USER'S MENTAL MODEL, NOT THE CLICK COUNT — owner principle set for the money-centralization session and explicitly meant to outlive it, binding on all design sessions: proximity is not clarity (five one-tap buttons are five things to learn), the test is what sentence is already in the user's head, and DELETION is a preferred proposal; first applied 2026-08-21 approving the one-money-conversation framework — five add-forms become ONE recording conversation whose first question is "who was the money with?" (COACH_MONEY_CENTRALIZATION_PLAN.md — all three rulings taken same day, ready to build). Before that 2026-08-20 riders: A PHONE CAPTURE MUST BE SHORT ENOUGH TO BE DRAWN AT FULL WIDTH — re-photographing the coach money screens on their real phone layouts closed the phone defect (33%→92%, 39%→92% at 390px, measured, shape drift <0.9%), but the settlement sheet's FIRST phone crop was the whole 390×1000 modal, a 0.39 ratio the fixed stage draws ~165px wide on a laptop: the stage spends its height budget through the WIDTH, so a very tall picture is punished exactly as a very wide one is on a phone; dropping nine lines of small figures took it to 359×395 and bought ~90% on a phone AND ~100% on a laptop — aim near 0.65, never reach for a taller crop because it contains more. And A RING EARNS ITS PLACE FROM THE PICTURE'S SCALE, NOT THE SENTENCE BESIDE IT: slide #01's two column rings were DROPPED rather than re-pointed when the phone layout turned out to have cards instead of columns (a ring on a card is a row position wearing a different shape, and the re-anchor moves it onto a family who has paid) — at 92% of true size the job rings exist for is already done. Before that: PITCH SLIDES GET ONE FIXED PICTURE FRAME — contain, never fill, so a tall phone capture, a wide table crop and a drawn explainer share one rhythm; and on a phone the PICTURE LEADS at full width, which is the 2026-08-08 wrapped-toolbar rule applied to an image (measured: both shipped coach captures render at ~16% and are illegible at 390px). Flagged as a SYSTEM EXTENSION — no fixed aspect convention existed anywhere. ⚠⚠ BUILT 2026-08-20 and the near-miss is the durable lesson: "CONTAIN" IS NOT A HEIGHT CAP — `width:100%` + aspect-ratio + `max-height:100%` SQUASHES anything taller than the frame (a 350×546 capture rendered 350×413, i.e. we were about to publish distorted screenshots of our own product); `object-fit:contain` is NOT the fix because the rings are positioned against that box; spend every limit on the WIDTH and measure it, never assert it. ⚠ HOW LARGE a picture may be drawn is TWO rules not one (owner correction mid-walk): a PHONE-subject capture never grows (enlarged it claims to be a desktop app), a DESKTOP crop grows to fill the frame bounded by its own pixels (captures are 2x density, so they are already there) — the blunt "never enlarge anything" version stranded the bracket at 480px in a 671px frame. Also the two rules COLLIDED: on a phone the aspect lock is RELEASED (16:10 is a floor, not a cage — contained inside it a phone-shaped capture renders ~140px, i.e. the defect inverted). ⚠⚠ And the durable arithmetic: CROPPING ROWS SHORTENS A PICTURE, IT DOES NOT NARROW IT, and on-screen scale follows WIDTH — measured at 390px the bracket went 33%→68% (it was ~520px of drawing in a 984px section) and the dues table 33%→33%, so the phone defect is HALF closed and only a phone-width re-capture can close the rest. ⚠ RIDER RAISED NOT RULED: lime is spent FIVE times (SIX since the callout rings landed) on the shipped walkthrough panel against a binding one-lime-action rule; defensible on a slide (no CTA to compete with), open on the page. Also 2026-08-20: pitch artifacts PRINT DARK, one slide per page — ratifying a build-time choice that existed nowhere in this log, because 21 slides are about to inherit it. Also 2026-08-19: THE TAP FLOOR IS A TOUCH RULE — 44px is a fingertip measurement, so the layout sweep stops asking it above 768px, argued once at TAP_FLOOR_MAX_WIDTH; nothing moved on screen and the chrome slimdown stands. ⚠⚠ The durable lesson is the number nobody opened: the brief AND the owner mockup both said "about twenty-five accepted exceptions"; it was 1,928, with 1,524 unargued — and 753 of them at PHONE/TABLET widths where everyone believed the floor WAS enforced (156 measurements = 70 distinct controls under 24px, smallest 13px — entries ≠ controls, say which). A baseline count is evidence; a sentence about one is not. Touch debt now named + sized in COACH_TOUCH_TARGET_DEBT_PLAN.md; do NOT close it by bulk-writing reasons, the unexplained count is the instrument. Also retired 1,175 desktop entries of which 304 carried the SAME hand-written reason — when a sentence is written 304 times into a data file, the decision belongs in the rule. Also 2026-08-19: A RECORD IS SPELLED WITH A HYPHEN (12-4-2), owner choosing against the recommendation; a SCORE keeps the en dash (W 5–3). ⚠ The "more work" objection counted SURFACES not DEFINITIONS — the formatter is one function, so the non-recommended option was the smaller change AND strengthened the single source of truth. ⚠⚠ The audit named 2 duplicate spellings; there were SEVEN, found over THREE passes — sweeping for the DASH CHARACTER found 5 (three JSX-inline {wins}–{losses}, invisible to a search for the helper name OR the template shape), then /review found the two worst: the Season Wrapped SHARE PNG (the on-screen card was fixed, the image that LEAVES THE APP was not) and the coach Overview's last-season card, which hand-rolled an en dash in a file that already imported formatRecord and called it 400 lines below. Search for the OUTPUT not the abstraction — and scope the sweep by BEHAVIOUR not by FOLDER: pass 1 searched coach-named paths, and wrapped-share-card.ts renders a coach feature without the prefix. ⚠ The FAMILY portal was NOT swept and is an OPEN CALL. TeamHQ still prints 12-4-0 vs 12-4 — flagged, not changed. Before that 2026-08-18: TEXT NAMES A ROLE, NOT A SHADE — coach text inks with --text-primary/-secondary/-tertiary, retuned in dark only (warm already passed); the dim "hierarchy" was drift, the same stat label sat at three shades. ⚠⚠ The durable lesson is about the METRIC: the July token guardrail centralizes colour by VALUE not ROLE and deliberately ignores the white-alpha ladder, so every CSS baseline read ZERO while unreadable 1.65:1 text shipped and 1,176 text colours stayed hand-managed — a rebrand was one edit, legibility was 1,176. Say out loud what a green gate does not look at. Admin still has the identical gap. Also 2026-08-18: WHO A CLUB MAY EMAIL — dues + tryout mail is TRANSACTIONAL and skips the opt-out, but the exemption is PAID FOR by naming the club (an exempt sender that does not identify itself is not exempt, just unaccountable); announcements are never exempt; a household is CONFIRMED not inferred before money moves. Durable lesson: compliance carried in a PARAMETER lapses — the through-the-person expansion moved into the shared suppression list so every sender gets it, not just the door that remembered. Before that 2026-08-16: THE SEASON DIAL IS DELETED — no switcher, no page-title season chip, ONE nav whose first slot swaps Overview to Season's End; a year parameter is now a DECISION guarded by HISTORY_ENDPOINTS, and the guard finally covers PAGES too. Before that 2026-08-13: page-level ACTIONS — a button belongs to the nearest chrome that NAMES what it acts on, so a hub header holds only hub-wide `Import ▾ / Export ▾` menus while creates drop into the tab's own toolbar; cap is two buttons + help; phones get outputs not files ⚠ empty states MUST keep their import offer at 390px. Before that 2026-08-12: Overview's icon exception RETIRED — tried, shipped, rejected on sight; the One Thing anchor arranged in 3 rows not 5, 228→144px desktop / 267→199px phone, resolver untouched. Before that 2026-08-11: coach page headers — no subtitles, masthead owns season+role, actions right; BUILT IN FULL, `.pageSub`/`.breadcrumb` deleted so rule 1 has no mechanism left to break)
- [Iterate visual fixes with Playwright](feedback_iterate_visual_with_playwright.md) — for CSS/alignment bugs, drive the app + measure computed heights/screenshot, don't fix blind on user screenshots
- [Agent review workflow](agents/codex-subagent-coordination.md)
- [Agent verification workflow](agent-verification-workflow.md)
- [League onboarding wizard](league-onboarding-wizard.md)
- [Tournament free-tier UX cleanup](tournament-free-tier-ux.md)
- [Launch pricing positioning](launch-pricing-positioning.md)
- [Pricing strategy pointer](project_pricing_strategy.md) — canonical facts live in PLAN_PRICING_FACTS.md + BUSINESS_DECISIONS.md; safe-in-memory durable facts only (latest 2026-08-23: roster export = Premium Coaches Portal inclusion, ratifying shipped behaviour — no feature key, the gate is architectural; ⚠ never read plan inclusions from lib/export/catalog.ts, its minPlan/moduleGate are documentation nothing enforces)
- [Billing plan invariants](billing-plan-invariants.md)
- [Platform admin information architecture](platform-admin-information-architecture.md)
- [Pitch Deck Studio](project_pitch_deck_studio.md) — ALL FOUR STAGES on dev 2026-08-21 (A library view · B pull editor · C composer · D prospect links), QA §72/§76/§77/§78, migs 257–258 dev-only; stage C: decks are ROWS (`pitch_decks`) — reordering a deck re-orders its live page MECHANICALLY (resolvePullIds takes the LIVE deck; "never blank" outranks "always a subset"), `deckProblems()` refuses held numbers BY NAME and allows cross-audience mixing on purpose, #18–#20 STAY held; stage D: every owner deck mints an unguessable /pitch/<slug> at birth (noindex, no fallback — broken 404s; the prospect label never reaches the page BY CONSTRUCTION; deleting the deck IS the revoke; PDF = the link printed); ⚠⚠ DECKS ARE DATA / SLIDES ARE CODE — never add an edit affordance for slide COPY; stage B made the two public pulls rows (`pitch_page_pulls`) with the code `fallbackPull` as a permanent safety net (lenient `resolvePullIds` read; a broken/slow/rotten row can NEVER blank a page), ONE rulebook `pullProblems()` shared by the save API and the guard test, meta + SEO description DERIVED (byte-identical for today's pulls), and F4 CLOSED — `pageAnswer`+`seoPhrase` are required slide fields (the ten missing answers were code-verified first: the lineup flag is PER-GAME, the family link is FOLLOWER-tier only); the pull editor has NO order control on purpose (deck order is the invariant); the picture column is DECLARATION health not freshness (a failed re-capture leaves the old PNG, so stale passes CI) and success criterion 4 is NOT met; a DRAWING has no manifest entry so it is neither passed nor failed; the plan-line column was BUILT AND DELETED the same day — the rule that replaced it is a BUILD CHECK, which is stronger than a column
- [Local dev server launch](local-dev-server-launch.md)
- [Non-billing UAT remediation](non-billing-uat-remediation.md)
- [Next.js framework conventions](nextjs-framework-conventions.md)
- [Tournament Plus subscription positioning](tournament-plus-positioning.md)
- [Tournament Experience Excellence](tournament-experience-excellence.md)
- [Tournament scorekeeper experience](tournament-scorekeeper-experience.md)
- [Automated schedule generator](automated-schedule-generator.md)
- [Export strategy](export-strategy.md)
- [Import strategy](import-strategy.md)
- [Help documentation planning rule](help-documentation-planning.md)
- [Standalone Team workspace](standalone-team-workspace.md)
- [Coaches Portal unified product](coaches-portal-unified-product.md)
- [Coach Insights reports portal](project_coach_insights_reports_portal.md) — seven tabs on one page, no money anywhere, live-season-only by construction; P1 dev 08-18 (QA §58 passed), P2 dev 08-19 (QA §61 owed). ⚠⚠ P2 took THREE owner decisions BEFORE any code and TWO reverse the approved mockup: the attendance table may not sort or badge a child (a support read that ranks children is a leaderboard however neutrally drawn — guarded in SOURCE, because worst-first is one .sort() whose unit tests would all still pass), and arm care may not draw an innings budget (the product stores NO weekly or season ceiling, only a per-game cap the coach set, and inventing one is the product proposing a figure as though it were a rule). Third: RSVP reply rate renamed Recorded — nobody replies, so check the actor performs the act before naming a rate after them. ⚠ Two fixture bugs found, both hid features behind a green check: .maybeSingle() on a deliberately-duplicated name ERRORED and the discarded error re-seeded six games on every run; and the demo built all six lineups from ONE grid so the new recency matrix rendered one number repeated.
- [Coach Payables Rebuild](project_coach_payables_rebuild.md) — a commitment holds INSTALLMENTS (the plan) + PAYMENTS (what happened), mirroring dues since mig 232; approved 08-19, QA §64. ⚠⚠ ORIGIN IS THE LESSON: §27 PASSED CLEAN and the owner still found FOUR defects — a checklist of existing behaviour cannot find an ABSENCE. No partial payment, no undo on Mark paid, a paid Schedule row that is a dead end, nothing repeats; and `Schedule | Commitments` showed a PARENT AND ITS CHILDREN as two reports. Settled = paid IN FULL, partly paid counts as UNPAID; over-payment is ACCEPTED not refused; a payment attaches to the COMMITMENT and applies oldest-unpaid-first. ⚠⚠ Bulk scopes may NEVER touch a settled installment, but “this payment only” STILL EDITS one (2026-08-16 ruling — locking it reverses a live ruling). ✅ The monthly engine was ALREADY BUILT (`c404bd4b`, 43 tests, no callers by design) — the owner said so and an agent said otherwise: SEARCH docs/projects/ before claiming a feature does not exist. Supersedes + archives COACH_RECURRING_PAYABLES_PLAN. ✅ **P1–P4 all on dev 08-20** (§64 A+B passed; C/D/E owed). ⚠⚠ P4’s lesson: **a cap and the EDITOR that forced it must lift in ONE change** — the 2-piece cap existed only because the form was a deposit/balance two-field editor, so raising it alone re-creates the silent truncation; the export’s 4 Deposit/Balance columns and the typed Total went with them. **Monthly only, no cadence control** (§4.1’s Weekly/fortnightly were written before anyone read the engines). ⚠ **A positional guard fails BOTH ways once a plan can hold six** — `updateRepTeamExpense`’s “dropped piece carries money” test refused legitimate deletes and never fired on the case it was written for; replaced by the OUTCOME test `whyPlanStrandsPaidMoney`. ⚠ `check:layout` CANNOT see a modal, so it measured none of P4. → **NEXT (owner-called 08-21, planning only, opens when this closes): “where does a coach log money?”** — the MODEL converged (installments+payments both sides) and the DOORS did not (Transactions, Payables, Dues, Fundraising, Club each grew their own add-form). Carries §64’s absence as one question, not two: a family can front a WHOLE cost but not one PAYMENT of a bill, because a payer is a field on the cost form and a payment record has no payer. Brief = §9 of the payables plan. ⚠⚠ /review then caught a MONEY defect: a POSITIONAL row key meant removing a mid-list paid installment rewrote its payment (and its ledger entry) to a different figure — **audit every position-keyed match when a list becomes editable in the middle**; fixed by naming rows, verified by a live DB round trip. ✅ **§64 C+D WALKED 08-21 — five findings, every one an ABSENCE or a FALSE SENTENCE.** ⚠⚠ One stale claim lived in FOUR places (the arithmetic, the grid, the EXPORT, the on-screen note) and each was "fixed" while three copies stayed live — **a behaviour lives in more than the code that computes it; sweep copy + exports + help after changing what a screen MEANS** (a fifth copy sat in the help and was found only by a review lens). The 2025 prior-season column and the money-tag filter were both REMOVED from Budget vs. Actual — ⚖ **the tag filter WORKED**: it narrowed SPENDING while the PLAN stayed whole (a budget line carries no tag), so **HEADROOM ROSE as you filtered** ($8,905→$10,900) — a comparison report cannot half-filter a comparison; tag filtering stays on Transactions, which lists rather than compares. **Budget vs. Actual evaluates THIS season only.**
- [Money arithmetic has one home per question](coach-money-one-arithmetic.md) — cash on hand · the season close-out pot (⛔ deliberately separate) · the report; on Budget vs. Actual a feed that walks the raw records for itself is a DEFECT. Enforced by `tests/unit/money-one-arithmetic-guard.test.ts` + `npm run check:money-report` (which **fails over a fixture too thin to disagree**). ⚠ Two things not to "fix": the two date rules are both correct (org-noon stamps vs click-time instants), and the Months grid's month-splitting of a commitment was always the right one
- [Free Tier strategy project](project_free_tier_strategy.md)
- [Dev database schema](reference_db_schema.md) — every table and column, regenerated from the LIVE dev database by `npm run refresh:snapshots`; never hand-edited, and never a substitute for the snapshots when deciding whether a column exists
